# Secrets
*.token
*.secret
token_*.txt
.env
.env.*

# Private reporter lookup (issue number -> name). Generated locally from the
# GitHub issues; NEVER commit — it carries personal names and the PII gate in
# AGENTS.md forbids identifying content in the repo. The published dataset
# names only the service plus the issue number.
/reporters_private.md

# IDE / OS state
.claude/
.vscode/
.DS_Store
Thumbs.db

# Build artifacts
__pycache__/
*.pyc
*.bak
*.bak.*
*.old
*.old.*
*_old.*
*_backup.*
*.log
node_modules/
scratch.*
tmp.*
temp_*

# Generated audit runs
audit/run_*/

# Local-dev building detail JSONs (PRIVATE; served statically on localhost only — copied
# from Varna_buildings/output/.../details/buildings/v1/. NEVER commit; uploaded to R2 by
# Petar. See docs/local_dev_detail.md).
details/

# Address backfill script ephemera (cache + progress state)
data/.address_backfill_cache.json
data/.address_backfill_progress.json

# Source data — too large, regeneratable from external sources
geo_fire_hydrants.*
*.kmz
*.kml
*.shp
*.shx
*.dbf
*.prj
*.cst
wfsrequest.txt

# Stray files
New Text Document.txt
hydrants_varna (7).html
hydrants_varna (7).html.bak.*
# ---- ЛОТ 1 (план от 01.09.2026, подписан 02.09): шумът по подписания списък; нищо не е изтрито ----
# [fv_all] Цялата папка е шум: галерия, 8 OSM/overpass geojson-а и 66.8 MB копие. Нула доказателства вътре — проверено файл по файл.
/scratch/boundary_gallery/
# [fv_all] Хваща само снимките в корена на scratch. Доказателствата там са .md/.json/.py/.mjs — правилото не ги докосва.
/scratch/*.png
# [fv_all] Само трите точкови дъмпа. quarters_cleanup_spec/quarters_list са .md и остават извън шаблона.
/scratch/quarters_*.js
# [fv_all] Трите прегледни изгледа. Не съвпада с нито един .md от кварталния цикъл.
/scratch/quarters_*.html
# [fv_all] Точен път — изглед на регистър, чийто авторитет е тракнат в Varna_buildings/config.
/scratch/registry_view_20260730.html
# [fv_all] Точен път; одитът patch_live_osm_audit_20260811.md е .md и остава видим.
/scratch/patch_compare_20260811.html
# [fv_all] Точен път към дубликата; никой друг .geojson в корена на scratch не съществува.
/scratch/varna_quarters_osm_copy.geojson
# [fv_all] Точен път; тракнатият scratch/probe_geo_fv.mjs не се засяга.
/scratch/shot_geo_fv.mjs
# [fv_all] Точен път; отчетите и .mjs харнесите в същата папка остават видими за git add.
/scratch/basemap_b2/index_html.diff
# Гейтовете пишат тук (coverage.md/json, break_glass.log) — изход, не източник.
gates/out/
