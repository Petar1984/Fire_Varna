# Оборител №2 — находка №12 (входовете, „550 документа не стигат до геокодера")

HEAD на Fire_Varna към проверката: `6460961` (`git -C C:/git/Fire_Varna rev-parse --short HEAD`).
Скриптове: `s1.py` … `s13.py` в тази папка. Read-only; `recall_sweep.py` НЕ е импортиран и НЕ е пускан
(`sha256sum C:/git/Fire_Varna/scratch/places_search/recall_sweep_rows.json` = `e56266f24e91d01b…`).

## 1. Възпроизведените числа на находката (всички верни като аритметика)
| число | моя команда | резултат |
|---|---|---|
| 5 314 документа | `s1.py` — `EN["documentCount"]`, `len(storedFields)`, `len(set(documentIds))` | 5 314 / 5 314 / 5 314 |
| 3 079 сгради | `s1.py` — `len({r["building_cadnum"]})` | 3 079 |
| 4 764 в геокодера | `s1.py` — `len([e for e in geocoder.entries if e.get("en")])` | 4 764 |
| 4 764 в доставката | `s1.py` — същото върху `Fire_Varna/data/search_index.json` | 4 764 |
| 89,65 % | `4764/5314` | 89,6499… → 89,65 |
| 1 748 групи | `s1.py` — `len({e["g"]})` върху входовите записи | 1 748 |
| доставка ≡ геокодер | `s9.py` — мултимножество `(pin,en)` | **равни, 0 разлика в двете посоки** |

## 2. Оборването — четири независими лоста

### (а) Грешен авторитет. Геокодерът е 1:1 със своя вход — 0 загуба.
`search_index_entrances.json` **не храни геокодера**. Той се строи от
`output/strategic_intel.json` (`js/build_search_index_entrances.mjs:2`), чието поле
`source_data_date` = **2026-05-12**. Геокодерът чете `output/section_units.json`
(`js/build_geocoder_index.mjs:1097` — `for (const e of unit.entrances || [])`), файл от 15.08.

`s6.py`:
```
section_units entrance rows (en non-empty): 4764  unique (cadnum,en): 4764  with lat: 4764
SU∩G 4764   SU-only 0   G-only 0
```
**Покритието авторитет→геокодер по входовете е 100,00 %, а не 89,65 %.**
Отделно: `grep -c search_index_entrances C:/git/Fire_Varna/index.html` = **0**; нула препратки
в целия Fire_Varna. Артефактът се ползва само вътре в Varna_buildings (app_m1, регресии).

### (б) „550" е НЕТНА разлика, не брой изпуснати.
`s4.py`: EN-only = **608**, геокодер-only = **58**, 608 − 58 = 550.
Формулировката „550 документа не стигат" крие, че разликата е ДВУПОСОЧНА.

### (в) „3 079 срещу 1 748" сравнява различни нива.
`3 079` = кадастрални **секции** (4-сегментен cadnum). `1 748` = **физически сгради** —
`g` е соленият detail-ключ `bd` (`js/build_fire_varna_search_index.mjs:96`).
Съпоставимите двойки (`s3.py`, `s4.py`):
* секции ↔ секции: **3 079 ↔ 2 940** (разлика 139, не 1 331);
* комплекси ↔ физически сгради: **1 782 ↔ 1 748**;
* записи ↔ записи: **4 764 ↔ 4 764**.
Хистограмата „секции на комплекс" (`s3.py`) е {1:1425, 2:152, … 23:3} — групирането е реално,
не загуба.

### (г) Оборена е и обявената НЕВЪЗМОЖНОСТ да се измери.
Черновата §5 т.4: „`search_index_entrances.json` е MiniSearch индекс **без координати**".
`s1.py`: `EN has lat/lng in stored: True` — `storeFields` включва `lat` и `lng`
(`js/build_search_index_entrances.mjs:44-57`) за всичките 5 314 документа.
Съпоставката един по един Е възможна и е направена по-долу.

## 3. Какво остава след оборването (по-слаба, преформулирана находка)
`s12.py` — след сгъване латиница→кирилица (A→А, B→В, E→Е, …) и главни букви:
```
missing before fold: 608
missing AFTER fold : 382     => 226 са ЧИСТА азбучна/регистрова нормализация
section_units entrances absent from EN after fold: 0   (SU ⊆ EN)
```
`s13.py` разбива остатъка:
| клас | записи | секции | апартаменти зад тях | всички ли са в геокодера |
|---|---:|---:|---:|---|
| A) секцията Е в `section_units`, но липсва точно тази буква | **190** | 167 (150 от тях пазят ≥1 друг вход) | 710 | 167/167 **да** |
| B) секцията изобщо я няма в новото поколение (друга КАИС извадка) | **192** | 122 | 4 035 | 122/122 **да** |

**Нула сгради са изгубени.** И 289-те засегнати секции имат адресни/mf редове в геокодера.

## 4. Вреден ли е предложеният „ход"?
Да, в текущия си вид. „Точката за копане (3 079 срещу 1 748)" праща изпълнителя да търси
несъществуваща загуба на ~1 331 сгради върху артефакт, който не се доставя. Ако входовете
са тема, копането е по **190-те записа върху 167 секции** (клас А) — единственото място,
където сградата е в данните, а буквата ѝ липсва.

## 5. Тежест за пожарникар на терен
Минимална. Сградата се намира по адрес във всички случаи; губи се само търсенето по
конкретна буква вход при 167 секции (0,3 % от 4 764-те входови записа стигат до екрана).
Нищо от това не е измеримо покритие на живата карта — то е дълг между две поколения КАИС.
