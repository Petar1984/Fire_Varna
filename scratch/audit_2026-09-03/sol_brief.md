# БРИФ ЗА СОЛ · одит на механиката (системният одит на живата карта, 03.09.2026)

Ти си външното око по кода. САМО ЧЕТЕНЕ: никакви редакции, никакви комити, никакво превключване на клонове. HEAD на `C:/git/Fire_Varna` се движи (изпълнител комитва CSS/тестове по лот К1/К2) — цитирай ред + `git rev-parse --short HEAD`.

## Какво да прочетеш

- Черновата на одита: `C:/git/Fire_Varna/scratch/audit_2026-09-03/ОДИТ_чернова.md` — изцяло; особено §2 (дублетите на адресите), §3 (търсачката), §6 находки 1/2/5/7, §7.2 (въпросите към теб).
- Доказателствата в същата папка: `search_summary.md` + `search_recall_all.py` (361 записа × 5 заявки през Python-референцията, 1 825 реда), `address_duplicates.json`, `address_coverage.json`, `kind_zone_anomalies.json`.
- Кодът: `C:/git/Fire_Varna/index.html` — IIFE `initAddressSearch` (`baseAddressLabel`, `formatAddressHit`, `dedupeDisplayRows` ~5085–5110 при ecfaf93, `SEARCH_LIMIT = 8`) и IIFE `initPlacesSearch` (`search()`: клонът А3′ — редът с `R.every((t) => zk.has(t.s)) && !R.some((t) => nm.has(t.s))`; клонът M1-category — редът с `if (!R.length) return { category: true, … orderCategory(cls, c) }`).
- Референцията `C:/git/Fire_Varna/scratch/places_search/recall_sweep.py` (има `if __name__` предпазител — импортът не пише; не пускай `main()`).
- Данните: `data/search_index.json` (86 232 записа: kind address/parcel/entrance/…, label, pin, tk, d), `data/address_rows.json` (80 510), `data/places.json` (135), `data/hotels.json` (226), `data/place_categories.json`.
- Билдът на адресния индекс: `C:/git/Varna_buildings/output/fire_varna_staging/generation_manifest.json` и скриптът, който го произвежда (намери го по името в манифеста).
- Стълбата на кварталите: `C:/git/varna_3d/src/place_zones.py`.

## Осемте въпроса (§7.2) — по ред, по кода, с редове, и с ОБОРВАНЕ на всяко предложено решение (какво чупи)

1. А3′ решава за целия клас: ако някой токен от остатъка е именен токен на КОЙТО И ДА Е запис в класа, филтрираният клон не тръгва и „хотел одесос" връща 1 ред (ПАРК ХОТЕЛ ОДЕСОС, Златни пясъци) вместо 22-та хотела в район Одесос. Напиши поправката per-record (или обединение: именните съвпадения първи, после зоново-филтрираният списък) и я обори. Гейт за печат: „хотел одесос" → 22, „хотел морска градина" → 18, „училище морска градина" → 6, при непокътнати контроли „хотел златни" → 85, старите 103 заявки от гейта непроменени.
2. M1-category срещу име на запис: хотел „ГРАДИНА" е невидим по собственото си име (заявката е ключ и връща 46-те детски градини). Как се залепя ТОЧНОТО име най-отгоре, без да се счупи категорийният път? Тест: „ГРАДИНА" → хотелът първи, 46-те градини под него.
3. `dedupeDisplayRows`: ключът е `norm(label) + '||' + g`, а address/parcel нямат `g` → 9 053 ключа / 67 357 записа дават ЕДИН ред, 545 от тях на >200 m. Кой пин печели ДНЕС (редът в масива? `_ord`?) и кое е най-евтиното, което прави втория видим? Какво чупи това в G3 (адресният корпус е байт-равен по договор — всяка промяна в `initAddressSearch` е нов подпис)?
4. Пълен JS-паритет: пусни 1 825-те заявки от `search_recall_all.py` през JS (изрежи токенизатора/`search` от `index.html` под node, или headless Chrome, спиран САМО по PID) и кажи къде JS ≠ Python.
5. Гейт „две места на <5 m = падаме": къде да седне в конвейера на varna_3d (`src/export_fire_varna_places.py` / `src/export_fire_varna_hotels.py`), за да хване следващия Явор/ГОЛДЪН ЛАЙН (0,0 m) и Диспансер/ДГ№52 (2,7 m)?
6. Латиницата: 31 916 записа (13 683 уникални етикета) стигат до пожарникаря на латиница, а `address_rows` са 100 % кирилица. Преводът в билда (Varna_buildings да пише кирилски `label`) или в показа (`formatAddressHit`)? Проследи защо `label` е латиница.
7. `SEARCH_LIMIT = 8`: втора страница или вдигане на прага („цдг приказка" е ранг 8 от 13)?
8. Стълбата: ако курортната кутия отстъпи на кадастъра на стъпка 1 (12-те хотела в ивицата 43,2707–43,2778 носят „к.к. Чайка" вместо „к.к. Златни пясъци"), какво друго се мърда?

За всяка находка/предложение: БЛОКЕР / ГОЛЯМА / МАЛКА и „правило или данни". Пиши на български, стегнато, таблици: въпрос → находка → доказателство (ред/команда) → присъда.
