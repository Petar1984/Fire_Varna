# ОДИТ · ЛОТ 0 „Гейтове“ (кръг едно) — 05.09.2026

> Записан от изпълнителя по структурирания изход на одитора (read-only агент, който по договор не пише файлове) — Амандамент №1, т. A.10; комит F0-е.
> Обхват на одита: комитите на ЛОТ 0 — `13ead91` (F0-а), `f76fd12` (F0-б), `3870ee5` (F0-в), `f2e4243` (F0-г).

## Присъда: НЕГОДНО — четири major дефекта

### Major 1 · неподписан allow-файл прави покритието зелено
`load_allow` чете `signed_by` само в `meta` — никой не го проверява. Allow-файл без подпис (или с подпис на кого да е) отменя всеки загубен и всеки сменен ред и гейтът излиза 0.

### Major 2 · сляп за изтрити редове
`pairs = min(len(base), len(cand))` — сравняват се само първите толкова реда. 100 изтрити хотела → изход 0.

### Major 3 · пълното пренареждане обезоръжава гейта
Пренаредените редове се пропускат ПРЕДИ сравнението, а `reordered` не участва в изходния код. 225 ротирани реда + всички квартали унищожени → изход 0.

### Major 4 · `run_gates` се срива с `UnicodeEncodeError` на cp1252 конзола
Сривът настъпва преди да е отпечатан дори един ред от таблицата — човекът вижда traceback, не гейт.

## Minor находки

1. Break-glass следата е gitignore-ната (`gates/out/`) — остава ефимерна.
2. Жълтото не блокира: `run_gates` връща 0 при ⚠, тоест „pending — Петър“ минава като подпис.
3. Одитният файл не е записан (одиторът е read-only; записва го изпълнителят).

## Бележка на изпълнителя (не е част от изхода на одитора)

Четирите major и двете първи minor са адресирани в комит `1d129be` (F0-д): подписът на allow-файла се проверява (липсващ ключ = червено, чужд/`pending` подпис = жълто), разликата в броя редове и пренареждането са червени, съединяването е по `(файл, име)` с ordinal само като резервен ключ, нетният спад `after < before` е червено независимо от съединяването, конзолата е utf-8 без `PYTHONIOENCODING`, и жълтото връща изход 1. Minor 1 (ефимерната следа) остава по решение A.9 — по подразбиране без tracked ред. Доказателствата (счупените входове, командите и изходите) са в `scratch/places_search/ОТЧЕТ_ЛОТ0-fix_05.09.md`.
