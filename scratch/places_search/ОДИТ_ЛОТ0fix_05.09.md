# ОДИТ · ЛОТ 0-fix (кръг две) — 05.09.2026

> Записан от изпълнителя по структурирания изход на одитора (read-only агент, който по договор не пише файлове) — Амандамент №1, т. A.10; комит F0-з.
> Обхват на одита: комитите на ЛОТ 0-fix — `1d129be` (F0-д), `bf9e72a` (F0-е).
> Находките са преписани дословно от Амандамент №2, т. A.12, където архитектът ги е приел и ги е насочил към F0-ж.

## Присъда: ГОДНО с бележки

Четирите major от кръг едно са затворени. Кръг две намира две нови major и шест бележки — всички те стават работа на F0-ж.

## Находки (дословните заглавия от А№2, A.12)

### (а) MAJOR — подписът се приема по префикс
`startswith("Петър")`: „Петърчо Иванов“ и „Петър — pending, НЕ подписвам“ минават като подпис → ТОЧНО сравнение `signed_by.strip() == "Петър"` на едно място, споделено от `coverage.py` и `run_gates.py`; тест със счупен вход за трите форми.

### (б) MAJOR — нетният спад
Т. 7 на А№2 решава: покрит, когато всеки ред е поименно в подписан allow.

### (в) повреден allow-файл
Списък вместо обект; ред-низ → `ValueError` → изход 4, не traceback.

### (г) `reordered` е файлово число
Вън от per-field редовете (или `file_reordered`).

### (д) allow-ред покрива ТОЧНО толкова редове, колкото са изброени
Брояч по ключ; РОЯЛ ×2 иска два реда.

### (е) при `--base` по команден ред — изричен ⚠ ред
„подписаният baseline НЕ е проверен“.

### (ж) числото 194 в ОТЧЕТ_ЛОТ0-fix се сверява/поправя
Одиторът мери 192; счупените входове се пазят в scratchpad за възпроизводимост.

### (з) `docs/plans/ПЛАН_ЛОТ_OSM-hints.md` е файл на архитекта
Друга тема, не на ЛОТ 0 — отбелязано, без действие.

## Бележка на изпълнителя (не е част от изхода на одитора)

Всички находки (а)–(ж) са изпълнени в комит `59076db` (F0-ж) заедно с А№2 A.7 (покрит нетен спад при подписан allow), A.8 (слабата котва при кандидатите е бележка, не ⚠) и A.10 (куката без подпорката `PYTHONIOENCODING`). (з) остава без действие, както е записано.

Разминаването по (ж) е измерено, а не изгладено: двата „ротирани“ входа са РАЗЛИЧНИ файлове. Входът на изпълнителя (`broken/hotels_rotated.json`, sha256 `692fb1a1…`) дава **194**, входът на одитора (`aud/hotels_rot.json`, sha256 `28012d80…`) дава **192**; разликата са точно два реда `district changed` върху дублираното име РОЯЛ (ординали 111 и 188), което ротацията на изпълнителя разменя, а тази на одитора — не. И двете числа са верни за своя файл; 194 в `ОТЧЕТ_ЛОТ0-fix_05.09.md` не се поправя, а получава име на файла и sha.

Командите, изходите и пътищата на всички счупени входове са в `scratch/places_search/ОТЧЕТ_F0zh_05.09.md`.
