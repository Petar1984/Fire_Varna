# ПРИСЪДА: **НЕГОДНО** — 3 blocker, 5 major, 5 minor

**Одитирано на живо репо.** По време на одита изпълнителят комитна: `a749930` → `38e9dd9`. Всички числа по-долу са на `38e9dd9`.

**Днешната база (измерена, не самопризната):**
- `PYTHONIOENCODING=utf-8 python -m unittest discover -s tests` → `Ran 310 tests … OK`
- `python -m gates.run_gates` → `✓ всички гейтове зелени` (7/7)
- `git ls-files scratch/places_search` = **46**; `git status --porcelain -uall scratch/places_search` = **42**
- висящи пътища (Г1-подобна мярка, `:!docs/archive`) = **82** от 195 уникални

---

## BLOCKER

**1. Входната предпоставка на лота вече е невярна.** Планът стъпва на „A.2-11 мести `ЗА_ПОДПИС_A.md` в `docs/archive/queues/`, което този лот индексира“ (§ 0 М7, § 1.2, Е-0 STOP).
```
$ git log -1 --format='%h %s'
38e9dd9 gates: A.2-11 — … queue enumerated from HEAD recursively (A.2-11, archive step deferred)
$ git -c core.quotepath=false ls-tree -r --name-only HEAD -- scratch/places_search | grep ПОДПИС
scratch/places_search/ЗА_ПОДПИС_A.md
$ git ls-tree -r --name-only HEAD -- docs/archive
docs/archive/activeContext_2026-07-04.md
docs/archive/activeContext_2026-09-04.md
```
Архивната стъпка е **отложена**; `docs/archive/queues/` не съществува. Е-0 STOP („`docs/archive/queues/ЗА_ПОДПИС_A.md` липсва“) се задейства на първия комит.
**Поправка:** § 0 М7 и Е-0 STOP се пренаписват срещу `38e9dd9`; предпоставката става „A.2-11 е комитнат (38e9dd9); архивирането на опашка A е ОТЛОЖЕНО и **влиза в този лот** като Е-в, стъпка 0“ — или лотът чака отделен комит, който я мести.

**2. Ако архивирането на опашката се изпълни както е записано, гейтовете стават ЧЕРВЕНИ — измерено.** Симулация в еднократен клонинг (`git mv scratch/places_search/ЗА_ПОДПИС_A.md docs/archive/queues/` + комит):
```
$ python -m gates.release ; echo EXIT=$?
  ✗ няма опашка scratch/places_search/ЗА_ПОДПИС_*.md — нищо не е разрешено
⛔ BLOCKED (1)          EXIT=6
$ python -m gates.run_gates ; echo EXIT=$?
⛔ ЧЕРВЕНО: 6 · release (референция ↔ двигател ↔ подписана опашка)   EXIT=1
```
Механизмът: `QUEUE_DIR = "scratch/places_search"` (`gates/release.py:171`) → `queues_in_head()` (`:437`) → `find_queue` връща `None` (`:461-472`) → `block()` (`gates/release.py:1265`).
Следствие вътре в самия план: **Г9 („точно 0 или 1“) противоречи на Г4 („0 червени“)** — 0 опашки е червено. И Е-0 приемането „`python -m gates.release` → една опашка в HEAD“ е неизпълнимо в точно това състояние.
**Поправка:** Г9 → **точно 1**; изрична предпоставка „лотът тръгва само когато HEAD носи ЕДНА опашка под `scratch/places_search`“ (т.е. след като опашка Г/A.3 е създадена), а архивирането на опашка A се прави **в същия комит**, в който новата опашка влиза. Плюс изричен STOP: „`python -m gates.release` върне 6 → спираме“.

**3. Г1 и Г2 се заслепяват точно за това, което трябва да проверят.** И двете носят `":!docs/archive"` — тоест не гледат преместените файлове.
Конкретен сценарий на провал, доказан с днешния текст:
```
$ git grep -n "p7_zone_aliases.json" -- . ':!docs/archive'
scratch/places_search/p7_attack_brief.md:22:## 3 · Мярката (…; `scratch/places_search/p7_zone_aliases.json` …)
```
`p7_attack_brief.md` И `p7_zone_aliases.json` са **и двата** кандидати на Е-в. След преместването ред 22 сочи несъществуващ път — но файлът вече е под `docs/archive`, значи Г1 не го чете и „нито един нов висящ път“ е зелено при счупен път. Същото прави Г2 самоизпълняващ се за всяка взаимно цитираща се двойка (редът на местене решава присъдата).
**Поправка:** Г1 се пуска върху **цялото** дърво, включително `docs/archive`, и сравнява **МНОЖЕСТВА**, не броеве (равен брой ≠ същите пътища); Г2 се пуска **преди първото местене**, върху цялото дърво, и класификацията се замразява в списък, който изпълнителят не пипа.

---

## MAJOR

**4. PII-гейтът е фалшиво зелен по конструкция.** Планът казва „PII-скан **преди** `git add`“, а командата е `git grep … -- docs/archive` — `git grep` не вижда непроследени файлове. Измерено:
```
$ printf 'contact: ivan.petrov@example.com tel +359888123456\n' > docs/archive/2026-09_places_search/fake_brief.md
$ git grep -nEi "@[a-z0-9.-]+\.(com|bg)|\+359|reporters_private" -- docs/archive
docs/archive/activeContext_2026-09-04.md:92: … (само стар, проследен файл)
rc=0   ← подхвърленият имейл и телефон НЕ са намерени
$ grep -rnEi "@[a-z0-9.-]+\.(com|bg)|\+359" docs/archive
docs/archive/2026-09_places_search/fake_brief.md:1:contact: ivan.petrov@example.com tel +359888123456
```
Второ: рискът, който планът сам назовава, е **лични имена** (`reporters_private.md`), а регулярът лови само `@`/`+359`.
**Поправка:** `grep -rniE "…" docs/archive/` по файловата система (или `git grep --no-index`), пуснат след `git add -N`/преди комита, плюс шаблони `\b0[87][0-9]{8}\b` и списък на имената от `reporters_private.md` като deny-list; изход ≠ празно = STOP.

**5. Приемането на Е-а е аритметично грешно.** Измерено (2 променени файла):
```
$ git diff HEAD~1 -U0 -- docs/plans | grep -c "^+"      → 6      (брои и „+++ b/…“)
$ git diff HEAD~1 -U0 -- docs/plans | grep -c "^+[^+]"  → 2
```
Освен това `git grep -c "^# " -- docs/plans` дава 34 **реда**, но `h1_shared_core_spatial_dedup.md:2` и `h2_kmz_adapter_plan.md:2` носят по 2 заглавни реда — сумата е 36, не 34. И `git grep -L "СТАТУС" → само живите, изписани като такива` е неопровержимо: `-L` изброява файлове БЕЗ думата, значи жив файл със записан статус никога не се показва.
**Поправка:** `grep -c "^+[^+]"`; формулировка „34 **реда** изход“; очакван изход на `-L` = **празно**.

**6. Нарушен е „мери първо“ (глобален инвариант 2) точно там, където боли.** § 0 признава „не е пускана нито една команда“; М2 и М3 са „предстои“, а **М9 („базата преди мърдане“) е „не е мерено“** — а тя е базата на Г1, централния предпазител на лота. Gate 1 подпис върху неизмерена база е това, което инвариантът забранява. Освен това: планът няма нито един ред за **базата на тестовете и гейтовете** — а тя е волатилна (по време на този одит една и съща работна директория даде `191 tests / ImportError`, после `310 / 4 червени`, после `310 / OK`, защото изпълнителят пишеше в `gates/*.py` в 19:50).
**Поправка:** в § 0 влизат измерените днес числа (46 / 42 / 82 от 195 / 310 OK / 7 зелени) с командите си, и правило „Г3/Г4 се пускат само на тихо дърво — `git status --short` без чужди промени“.

**7. Сблъсък на write-set с „Границите“ Г0-б, и обявеното място в реда вече не съществува.** Планът казва „преди Г0-а на Границите“, но Г0-а е **комитнат** (`a749930`, „(Г0-а · C83)“). Оставащото място е между Г0-а и Г0-б, а `docs/plans/ПЛАН_ЛОТ_Границите_v2_05.09.md:100` дава на Г0-б write-set, който съдържа `docs/plans/PARKED.md`, `docs/activeContext.md` и `docs/plans/ПЛАН_ЛОТ_Границите.md` (само статус) — и трите са и в write-set на Ред (Е-б, Е-д, Е-а).
**Поправка:** Ред отстъпва трите файла на Г0-б (маха ги от Е-а/Е-б/Е-д) или тръгва СЛЕД Г0-б; изборът се записва изрично, заедно с новия текст „мястото в реда“ срещу `38e9dd9`.

**8. `git branch -D lot1-client` (§ 3 т. 3) отказва по конструкция.** Клонът е зает от работно дърво, а планът никъде не споменава работни дървета:
```
$ git worktree list
C:/git/Fire_Varna         38e9dd9 [main]
C:/git/_wt/fv_lot1_client dcd951d [lot1-client]
$ git branch -avv
+ lot1-client   dcd951d (C:/git/_wt/fv_lot1_client) …
```
**Поправка:** или `lot1-client` отпада от списъка, или преди него се назовава `git worktree remove C:/git/_wt/fv_lot1_client` (пак с ръката на Петър) — и се добавя ред в § 0, че репото има второ работно дърво.

---

## MINOR

**9. Г7 не е изпълнима.** „Броят редове в `docs/archive/INDEX.md`“ няма команда и никога не може да е равен на броя файлове (заглавие, шапка на таблицата). **Поправка:** сравнение на МНОЖЕСТВА — пътищата в таблицата на INDEX срещу `git ls-files docs/archive` минус самия INDEX; симетричната разлика = празна.

**10. Г8 описва грешен изход.** `git grep -c "^## 0 " -- AGENTS.md CLAUDE.md` печата `AGENTS.md:1` и `CLAUDE.md:1`, не числото 2. **Поправка:** „2 реда изход, всеки завършващ на `:1`“.

**11. DO-NOT-MOVE: сгрешени редове + два неназовани живи файла.** Действителните места: `gates/release.py:154-157` (двигател/референция/expectations/M7), `:159-161` (манифестите), `:171-172` (`QUEUE_DIR`/`QUEUE_GLOB`), `queues_in_head` на `:437`, `find_queue` на `:461` — планът дава 152-155 / 157-159 / 169-170 / 435-442. Липсват:
- `scratch/places_search/m7_significance_gate.py` — проследен, жив по амандамент № 4 т. 4 („литералният клон остава само в `m7_significance_gate.py --literal` — нарочното червено“);
- `scratch/places_search/probe_out/token_parity.json` — `docs/decisions/010_signed_quarter_boundaries.md:62`: „**Замразените тела са ТРИ** … паритетният корпус `probe_out/token_parity.json`, който днес не е вързан от нищо и влиза в `expectations._meta.inputs`“ — тоест точно файлът, който „не е цитиран от код“, е жив вход на следващата доставка.

**Поправка:** и двата реда влизат в таблицата; всички номера се преизчисляват на HEAD (A.2-11 премести редовете в `release.py`).

**12. Е-а не покрива всичките 34 плана.** Изброените са 30 (13 англоезични + v2 + амандаменти 1–9 и 11 + 6 ЛОТ-а). Без статус остават `ПЛАН_ИЗЧИСТВАНЕ_05.09.md` (има го), `ИДЕЯ_златен_списък.md` (Е-б), `ПЛАН_ЛОТ_Границите_v2_05.09.md` и — необяснено — **`ПЛАН_ИЗЧИСТВАНЕ_v2_амандамент_10_05.09.md`**. **Поправка:** изричен ред „тези 4 файла не получават статус, защото …“.

**13. Кандидат-списъкът на Е-в пропуска два проследени, нецитирани брифа:** `scratch/places_search/lot1v_brief_kimi.md` и `lot1v_brief_sol.md` (поправка № 1 в § 0 спасява само `lot1v_v_brief_*`). Не е вредно (списъкът е обявен за необвързващ), но прави Г7 и оценката на времето в § 4 неверни.

---

## Какво е ЧИСТО (проверено, не приета дума)

- **Нищо не се трие:** в нито един write-set няма `git rm`; Г6 (`--diff-filter=D`) е валиден за чисти преименувания.
- **Опашка в архива не се брои от `find_queue`:** `queues_in_head()` ограничава `git ls-tree -r … -- scratch/places_search` (`gates/release.py:437-458`), значи `docs/archive/queues/` е извън обхвата — обосновката на § 1.2 е вярна (проблемът е обратният, вж. blocker 2).
- **Гейтовете не четат нищо под `docs/archive/`**; единствените `docs/`-пътища в код са `docs/audits/*` (`scripts/import_etr_kmz.py:74,75,818,819`, `scripts/backfill_addresses_20260511.py:455`, `tests/verify_h4.py:4`) и коментари в `index.html:3161,3887,5820,6241,6653` — планът ги пази. `.git/hooks/pre-push:58` и `scripts/hooks/pre-push:58` съдържат само низ в съобщение за грешка, не четене.
- **Живите входове на „Границите“** (`osm_quarters_measure_05.09.md`, `agkk_inspire_measure_05.09.md`, `granitsi_astra_official_boundaries_05.09.md`, `agkk_inspire_2026-09-05/`, `granitsi_*brief_*`, `ОТЧЕТ_Ф0б_05.09.md`) правилно са извадени от местенето (поправка № 2 на § 0) — сверено срещу `ПЛАН_ЛОТ_Границите_v2_05.09.md:96,100`.
- **М6 е вярна:** `docs/activeContext.md:6` = „HEAD: A.2-4“, `:51` = „Ran 266 · FAILED (failures=6)“, `:52` = „⛔ ЧЕРВЕНО: проверка 6“ — срещу днешните 310 OK и 7/7 зелени. Е-д е наистина нужен.
- **Правилата на къщата** (§ 3, § 5): агентът не пуши, не трие клон, кръстореповата изолация (Е-ж в отделна сесия) е спазена; изричен `git add`, ограничени write-set-ове.

Ключови файлове: `C:/git/Fire_Varna/gates/release.py` (`:171`, `:437`, `:461`, `:1265`), `C:/git/Fire_Varna/docs/plans/ПЛАН_ЛОТ_Границите_v2_05.09.md` (`:92`, `:100`, `:177`), `C:/git/Fire_Varna/docs/decisions/010_signed_quarter_boundaries.md:62`, `C:/git/Fire_Varna/scratch/places_search/p7_attack_brief.md:22`, `C:/git/Fire_Varna/docs/activeContext.md` (`:6`, `:50-53`).