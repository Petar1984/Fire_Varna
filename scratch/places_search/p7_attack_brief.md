# БРИФ ЗА АТАКА · П7 — кварталните псевдоними в зоновите токени на търсачката на местата (Fire_Varna)

**Дата:** 03.09.2026 ~01:00 · **Възложител:** Петър („съгласен — не само за детските градини… пусни плана на Кими и Сол да видим какво може да се счупи и ако мине атаките прилагаме"). · **Планът:** `docs/plans/places_phase2_plan.md` §10 (v1.5) — тук е разгърнат с мярката. · **Нищо не е приложено.** Кодът, който се цитира, е HEAD `7a6ea1d` на `C:/git/Fire_Varna` (клиентът: `index.html`, IIFE `initPlacesSearch`, редове ~6222–6917; референцията: `scratch/places_search/recall_sweep.py`; данните: `data/places.json` 135 места, `data/hotels.json` 226 хотела, `data/place_categories.json` речникът).

## 1 · Проблемът (измерен)

Всяко място носи `zone` — низ по подписаната стълба на 3D-картата (курорт → КАИС-кварталът на сградата → чертана обвивка → район). Кадастърът пише едни сгради „ж.к. Владислав Варненчик", други „кв. Владиславово"; стълбата нарочно НЕ преименува (Gate 1b). Търсачката филтрира по ТОЧНО членство на токена в `zkset` (зона + вид): „владиславово детска градина" → само ОДЗ „Маргаритка" (зона „кв. Владиславово"), а ДГ 39 „Приказка" (зона „ж.к. Владислав Варненчик") липсва. Регистърът на кварталите (`C:/git/Varna_buildings/config/quarter_registry.json`, 84 записа, display + aliases) държи двете изписвания в ЕДИН запис.

## 2 · Правилото П7 (предложение; v2 след мярката в §3)

**Данни (varna_3d, `src/build_place_categories.py` → `data/place_categories.json`, копие в Fire_Varna):** нов ключ `zones`: `{ <зона дословно както е в доставката>: [низове] }`, където низовете са display + aliases на регистровия запис на семейството (без самата зона), САМО за 35-те зонови стойности, налични в двата данни файла. Съвпадението зона↔регистър е по низ (display или alias), не по прилика. Зоните без запис (7: „к.к. Св. Константин", „район Приморски", „Изгрев 1", „район Одесос", „Аспарухово/Галата", „к.з. Прибой", „Виница/север") остават без псевдоними и се изброяват в `_meta.zones_unmatched`. Нищо не се съчинява: всеки низ е дословен от регистъра.

**Клиент (`buildIndex`, index.html:6455–6465):** за всеки запис `ztk` се допълва с токените на `cats.zones[h.zone]` през същия `placeTokens`, с филтър:
- (а) без числови токени (иначе „II м.р." вкарва „2" в `zkset` и П3 брои числото за покрито);
- (б) без токени ≤2 знака (както А6 за старите имена);
- (в) без адресните маркери `ADDR = ['бл','блок','вх','вход','ул','бул','кв','жк','№']`;
- (г) **предпазител срещу сливане (ново, от мярката):** без токен, който е СОБСТВЕН токен (от самата `zone`) на ДРУГО регистрово семейство в доставката — измерено: `briz` (→ Св. Никола), `asparuhovo` (→ Дружба), `primorski` (→ Морска градина, Салтанат, Горчивата чешма). Изпуснатите се записват в `_meta.zone_alias_dropped`.
Токените влизат САМО в `zkset` (и `ztk`), НЕ в `ntk`/`nset`/`aset` — качеството и покритието на името (П2) са непроменени. Речникът остава незадължителен (С3): без него — както днес. Бюджет ≤15 реда JS. Референцията `recall_sweep.py` — същото правило; редовете се регенерират; JS = Python по ред (М5).

**Вариант (б′) за присъда:** да се прескачат ЦЕЛИ псевдоними, които са адресоподобни (съдържат цифра, „ул.", „№", запетая) — напр. `м-т Свети Никола, ул. " Арх. Стефан Колчев " №8`, `ж.к. Бриз, м-ст Акчелар`, `ИЗГРЕВ 552-2`, `ж.к. Възраждане 4, ж.к. Възраждане 1` — вместо да се филтрира по токен. Ефект: падат `arh/stefan/kolchev/akchelar/im`. Кой вариант е по-честен и по-безопасен?

## 3 · Мярката (03.09 ~00:50; `scratch/places_search/p7_zone_aliases.json` — картата, изпуснатите, несъвпадналите)

Зони в доставката 35 · с регистров запис 28 · без запис 7. **Добавени токени по зона** (само тези, които не са вече собствени):

| зона (брой записи) | собствени токени | +П7 (преди предпазителя г) |
|---|---|---|
| Морска градина (28) | gradina, morska | park, primorski, primorskiat |
| кв. Изгрев (18) | izgrev, kv | zhkizgrev |
| м-т Шашкъна (9) | m, shashkana, t | mestnost |
| Западна промишлена зона (9) | promishlena, zapadna, zona | promoshlena, zapadana, zpz |
| ж.к. Владислав Варненчик (8) | k, varnenchik, vladislav, zh | im, **vladislavovo** |
| ж.к. Възраждане (7) | k, vazrazhdane, zh | vazradzhane |
| ж.к. Младост (5) | k, mladost, zh | mikroraion |
| ж.к. Дружба (3) | druzhba, k, zh | asparuhovo (alias „ж.к. Дружба-Аспарухово") |
| с.о. Зеленика (2) / м. Зеленика (1) | zelenika … | chast |
| м-т Салтанат (2) | m, saltanat, t | mestnost, park, primorski |
| ж.к. Бриз (1) | briz, k, zh | akchelar |
| м-т Свети Никола (1) / кв. Свети Никола (1) / м. Св. Никола (1) | nikola, sveti … | arh, briz, kolchev, stefan |
| м-т Сотира (1) | m, sotira, t | mestnost, tira |
| кв. Владиславово (1) | kv, vladislavovo | im, **varnenchik, vladislav** |
| м-т Горчивата чешма (1) | cheshma, gorchivata, m, t | gorchiva, mestnost, park, primorski |
| останалите 13 (Златни пясъци 85, к.к. Чайка 38, кв. Чайка 19, Аспарухово 8, Трошево, Галата, Виница, Вилите, Победа, Прибой…) | — | нищо ново |

**Сливания на различни семейства, които П7 би създал без предпазителя (г):** `briz` — ж.к. Бриз ↔ Св. Никола (alias „м. Свети Никола / Бриз/"); `asparuhovo` — кв. Аспарухово ↔ ж.к. Дружба; `primorski` — район Приморски (без запис) ↔ Морска градина/Салтанат/Горчивата чешма (alias „Приморски парк" — официалното име на Морската градина). Вече съществуващи (не от П7): `chaika` к.к. ↔ кв.; `izgrev` кв. ↔ „Изгрев 1"; `galata`/`asparuhovo`/`vinitsa`/`priboi` с несъвпадналите „X/Y" зони; типовите думи `kv/zh/k/m/t/raion/s/o`.

**Добавени токени, които са и именни токени на записи:** `park` (12 записа — „Парк хотел…"), `primorski` (1), `briz` (1 — хотел БРИЗ), `stefan` (1). Няма добавен токен, който е форма от речника (ключ).

## 4 · Механиката, срещу която се удря (цитирана)

- `tokenMatch` (index.html:6501): име → стар-псевдоним → `zkset` (точно; „изяжда" токена, cat `zk`) → размито име. Псевдонимът на зоната се вижда само през `zkset`.
- `scoreRec` (6526–6546): `ok: qual && !numFail` — **ред има само при значещо съвпадение по ИМЕ/стар-псевдоним**; `zk`-съвпадението дава `total++`, `sum += 3`, влиза в ключа на подредбата `[-bestName, -named, -total, -sum, бивш, uncovered]`.
- `search` А3′ (6611–6616): при ключ, ако ВСИЧКИ останали токени са в обединения `zkset` на класа И НИТО ЕДИН не е именен токен на класа → филтриран списък `cls.filter(r => R.every(t => r.zkset.has(t.s)))`, подреден по близост (`orderCategory`). Това е ЕДИНСТВЕНОТО място, където П7 създава/маха редове.
- `significant` (6509–6525): А5/П5/П6 — само `name`/`alias`; `zk` никога не е значещ.
- `buildIndex` (6455–6465): `zkset = new Set(ztk.concat(ktk))`.
- Референцията `recall_sweep.py`: `place_tokens` (ред 179), `token_match` (383), `score` (438), `split_keys` (487), `search` (538); еталонът `recall_sweep_rows.json` (72 заявки/1 337 реда) — **внимание:** модулът презаписва еталона при импорт (находка №3 на одита C14; ще получи `if __name__` в същия комит).
- Гейтовете: §3 на плана 72/72 (JS = Python по ред); G3 — адресният код (`initAddressSearch`, 4780–6181 на 6ac2235) байт-непипнат, CDP-проба 9 заявки без ключ байт-равна; G5 бюджет; G6 първо зареждане; 164 теста (`tests/test_places_search_primitives.py` — sha пинове на трите файла, таблица на `placeTokens`).

## 5 · Гейтът, който П7 трябва да мине (добавка към §3)

„владиславово детска градина" → 2 (Маргаритка, Приказка) · „детска градина владислав варненчик" → 2 · „владиславово училище" → 4 · „хотел владиславово" → 2 · „дкц владиславово" → ДКЦ 3 · „хотел свети никола" → трите изписвания на м-т Свети Никола в един списък · „хотел зеленика" → с.о. + м. Зеленика · „училище изгрев"/„болница изгрев" непроменени · **контроли:** §3 72/72 остават; „хотел чайка" непроменен (к.к. и кв. Чайка са два записа); „хотел бриз" → хотел БРИЗ (не хотелите в Св. Никола); „училище аспарухово" → само кв. Аспарухово (не Дружба) — освен ако присъдата каже друго; „хотел приморски" → само район Приморски; „2 оу"/„1 ег"/„7 су" както в §9; G3 байт-равен; G6 равен.

## 6 · Въпросите

**Към Сол (механиката — външно око):** (1) Къде П7 може да СЧУПИ съществуващ резултат от 72-та в §3 или някой от контролите — конкретна заявка + защо (по кода, не по усет)? (2) Предпазителят (г): достатъчен ли е, или трябва семейството да се пази и от собствените токени на НЕСЪВПАДНАЛИТЕ зони („район Приморски" няма запис — primorski е негов собствен токен)? (3) А3′ с обединения `zkset` на класа: ако `R` = [vladislavovo] и класът е „хотел", `zk` вече го има (от кв. Владиславово, ако там има хотел) — при клас БЕЗ запис в кв. Владиславово (напр. болници) П7 добавя `vladislavovo` в zkset на болниците в ж.к. Владислав Варненчик — има ли път, по който филтърът връща празно и пада към скорирания клон с различен резултат от днес? (4) Подредбата: `zk` вдига `total`/`sum` — може ли П7 да размести първия ред на заявка с име + квартал („хотел адмирал златни" няма псевдоними; но „хотел марина свети никола"?) (5) Референцията и клиентът: къде двата тоукънайзера могат да се разминат върху псевдонимите (Unicode кавички в `" Арх. Стефан Колчев "`, „-" в „Дружба-Аспарухово", „/" в „/ Бриз/")? (6) G3: има ли начин П7 да пипне адресния контейнер (не пипаме кода, но `cats` товарът е по-голям — G6/кешът `fire-varna-hotels-v2-226`, sha на речника)? (7) Вариант (б′) срещу (а–г): кой е по-безопасен и по-обясним? (8) Какво в гейта в §5 е НЕПРОВЕРИМО или недостатъчно?

**Към Кими (данните — по ред):** (1) За всяка от 28-те зони с псевдоними: присъда ДА/НЕ/УСЛОВНО по добавените токени — кой псевдоним е легитимно име на същото място и кой е адресна отломка, печатна грешка на кадастъра („Влладислав", „промошлена", „Западана", „Възраджане") или чуждо име (Бриз в Св. Никола, Аспарухово в Дружба, Приморски в парка)? (2) Печатните грешки като точни токени: безвредни (никога сами не раждат ред) или шум, който трябва да отпадне? (3) Седемте несъвпаднали зони: коя може да се съедини по низ ЧЕСТНО (напр. „к.к. Св. Константин" ↔ регистровото „к.к. Св. Св. Константин и Елена") и кой ход е легитимен — псевдоним в регистъра на Varna_buildings (решение на Петър, отделен репо) или нищо? (4) „Изгрев 1" срещу alias „ж.к. Изгрев-1" на кв. Изгрев — същото място ли е? (5) Лицензът: псевдонимите са от `quarter_registry.json` (собствен регистър, изведен от КАИС-низове) — влиза ли нов лицензен ред в README? (6) Кой ред от гейта в §5 е грешно очакване по данните (напр. „владиславово училище" → 4 — верни ли са четирите; „хотел владиславово" → 2 — Комитово/Станкино ханче са в ж.к.)? (7) Какво ЛИПСВА в мярката?

*Отговорите се вписват в §11 на `docs/plans/places_phase2_plan.md` (v1.6) преди изпълнение. Изпълнение само ако атаките минат; иначе — обратно при Петър.*
