# ОДИТ A.2-11 — ГОДНО (с 3 находки за ратификация от Петър)

HEAD `38e9dd9` · `Claude Executor <executor@local>` · родител `a749930` (точно базата, която изпълнителят е мерил) · 1 комит · нищо не е пушнато (`origin/main` = `fd13907`, 3 напред, 2 от които на архитекта) · reflog без reset/rebase.

## Проверено в работното дърво (реално репо)
| команда | изход |
|---|---|
| `python -m unittest discover -s tests` | `Ran 310 tests … OK` |
| `python -m gates.run_gates` | `✓ всички гейтове зелени`, **rc=0** |
| `python -m gates.release` | `✓ release: зелено`, **rc=0** |

Забележка по метода: `... \| tail` връща кода на `tail`, не на python — всички rc по-долу са мерени с пренасочване към файл, не през тръба.

## Четирите счупени входа — възпроизведени от мен в еднократен клонинг (изтрит)
Всеки е сглобен наново от мен, не изпълнен като чужд тест. **Всичките четири блокират**, и — решаващото — три от тях са **напълно зелени срещу стария код** `a749930`, т.е. фикстурите са носещи, не декоративни.

**(а) втора КОМИТНАТА опашка, махната от дървото** — нов код: `release rc=6`, `run_gates rc=1`
```
✗ повече от една опашка в HEAD: scratch/places_search/ЗА_ПОДПИС_A.md, scratch/places_search/ЗА_ПОДПИС_B.md
```
стар код върху същия вход: `release rc=0`, `run_gates rc=0`, `✓ всички гейтове зелени`. Дупката е била реална.

**(б) „да“ с `тяло` в скрит регион** — двете страни:
- писалката: `python -m gates.sign R2 да` → `sign rc=3`, `✗ ред R2 носи скрит регион (code fence или HTML-коментар) на ред 278, 279, 280 …`, `НИЩО НЕ Е ЗАПИСАНО`.
- гейтът: `release rc=6` — `✗ ред R2 е „да“ по gates/baseline/MANIFEST.json, а няма видимо поле „тяло“ … (тялото сега е bf6a27cf06a0)`.
- **стар код:** писалката отчита `sign rc=0` и „✓ ред R2 · тяло bf6a27cf06a0 → gates/baseline/MANIFEST.json“, а дайджестът ляга **между двата ```**; после `run_gates rc=0`, `✓ всички гейтове зелени`, и в изхода липсва редът „тялото … = дайджеста на ред R2“ — тялото е вързано за нищо. Блокерът от одита на A.2-10 е потвърден дословно и е затворен.

**(в) блок „да“, върнат дословно от агент** — файлът е **байт-идентичен** с подписания оригинал (`git diff --stat 38e9dd9 HEAD -- …` празен), само историята е друга:
```
✗ ред R2: „да“ е въведено от 'Agent Bot' в 427a4d1 (най-новият комит на блока), а подписва само Petar1984
```
Ескалирах атаката до реалистичния ѝ вид — агентът връща блока, след което **Петър прави последния комит по файла** (обезврежда грубия пазач „последният комит е на агент“). Нов код: `release rc=6`, `run_gates rc=1`. **Стар код: `release rc=0`, `run_gates rc=0`, `✓ всички гейтове зелени`** — т.е. чуждо разрешение е минавало 7/7. Това е най-сериозната от четирите поправки и тя работи.

**(г) проследена опашка, изтрита от диска** — `release rc=6`:
```
✗ опашката scratch/places_search/ЗА_ПОДПИС_A.md е в HEAD, а в работното дърво я няма — изтрита опашка не сваля решенията си (амандамент №10 т. 3)
```
Стар код: също червено, но с **грешното изречение** `няма опашка … нищо не е разрешено` и с **проверка 7 ЗЕЛЕНА**. Новият код прави и двете червени. Изпълнителят е описал точно това в таблицата си (не е надул находката) — честно.

## Мои допълнителни атаки
- **опашка, скрита в подпапка** `scratch/places_search/sub/ЗА_ПОДПИС_C.md`, махната от дървото → блокира (`повече от една опашка в HEAD: …/sub/ЗА_ПОДПИС_C.md, …`). `-r` върши работа.
- **артефакт в `table`, но не в `artefacts`** (пътят, който новият блок на гейт 6 пропуска) — всеки такъв клон вече вика `block(...)` на `release.py:1150/1157/1163`. Fail-closed, няма дупка.
- **мълчалива грешка:** `except ValueError: tracked = False` в `run()` и `except ValueError` в проверка 7 — и двата продължават към `block`/`check.fail`. Няма път, по който грешка да стане зелено.

## Находки

**1 · MINOR (scope) — файл извън обявения write-set.**
`docs/plans/ПЛАН_ЛОТ_Границите_v2_05.09.md:91` дава write-set: `gates/sign.py`, `gates/release.py`, `gates/run_gates.py`, `docs/archive/queues/ЗА_ПОДПИС_A.md`, `scratch/places_search/ЗА_ПОДПИС_A.md` (изтрит). Комитът пипа и **`tests/test_places_search_gate.py`** (+134 реда):
```
$ git show --name-status --format="" HEAD
M gates/release.py · M gates/run_gates.py · M gates/sign.py · M tests/test_places_search_gate.py
```
Планът иска „четири счупени входа“ като гейт, но не е сложил тестовия файл в write-set-а (за F13-а на ред 166 го е сложил изрично). Разширяването е добросъвестно и по духа на плана, но е разширяване — иска дума от Петър. Съдържанието проверих: тестовете не са празни, а тест (в) носи и собствен анти-вакуум гейт (`assertEqual(release.introduced_by_text(rel, block)[0], his, "най-старият комит на блока не е на Петър — фикстурата не проверява това, което трябва")`).

**2 · MINOR — съобщението не е това от плана, и „резервно съобщение“ в плана НЯМА.**
Планът дава едно-единствено съобщение, завършващо `…; queue A archived outside the queue dir (A.2-11)`. Използвано е `…HEAD recursively (A.2-11, archive step deferred)`. `grep -rn "archive step deferred\|visible rows only" docs/ scratch/` намира **само** реда на плана — никъде в репото няма резервен вариант. Отклонението е ПРАВИЛНО по същество (да напишеш „archived“ за неизвършено архивиране би било лъжа в историята), но е неподписано. Ратификация от Петър.

**3 · MINOR — точката „37 непроследени“ вече е 40.**
`git status --porcelain | wc -l` → 40, всичките под `scratch/places_search/`, извън write-set-а — **0**. Трите нови (`granitsi_blocks_brief_astra.md` 20:05, `granitsi_blocks_brief_kimi.md` 20:05, `granitsi_v2_astra_S7-S13_05.09.md` 19:55) са **след** комита в 19:54:21 и са от паралелната работа на архитекта по „Границите“, не на изпълнителя. Твърдението му е било вярно към момента на комита.

## STOP-ът за архивирането — потвърден, не е извинение
Възпроизведох хода в клонинга (`git mv` + комит, нов код):
```
release rc=6 · ✗ няма опашка scratch/places_search/ЗА_ПОДПИС_*.md — нищо не е разрешено
run_gates rc=1 · ✗ 6 · release   (проверка 7 остава ЗЕЛЕНА)
Ran 310 tests … OK
```
Дословно съвпада с отчета, включително че **проверка 7 остава зелена** и пакетът остава 310 OK. Причината е коректно диагностицирана: `QUEUE_DIR = "scratch/places_search"` (`release.py:171`), `docs/archive/queues` е извън обхвата, а `run()` блокира безусловно при `find_queue() is None`. **Спирането е правилно** — архитектурно решение, не изпълнителска поправка. Двата пътя, които изпълнителят предлага на Петър, са верни; трети път (`--queue` override) съществува, но заобикаля изброяването от HEAD и **не е достъпен от куката** (`.git/hooks/pre-push:52` вика `python -m gates.run_gates` без аргументи) — не го приемайте като решение.

## Останалите проверки
- Автор на комита: `Claude Executor <executor@local>` ✓ (не Petar1984, не архитект).
- Никакъв `signed_by: "Петър"` не е писан от агента: комитът не пипа нито allow-файл, нито манифест; единственото „Петър“ в дифа е низ в docstring на тест.
- Детерминизъм: `queues_in_head` използва `-z` (кирилицата не се цитира) + `fnmatch.fnmatchcase` + `sorted` — подредбата е стабилна; `introduced_commits` е ЕДНО `git log -S`, четено от двата края, така че двата отговора не могат да се разминат.
- PII: няма.
- Работното дърво след моя одит: 0 промени извън заварените непроследени; двата еднократни клонинга са изтрити.

**Присъда: ГОДНО.** Кодът прави точно каквото амандамент №10 т. 1, 2, 3, 5 иска, трите от четирите дупки са доказано пропускали публикация до днес, спирането преди архивирането е честно и възпроизводимо. За Gate 2 остават трите minor-а: тестовият файл извън write-set-а, промененото съобщение и решението по архива (амандамент №12).