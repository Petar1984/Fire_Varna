# Brief for Astra (Codex, GPT-6, ultra) · ADR 010 + PLAN "Границите" v2 — lens: mechanics, gates, regressions. REFUTE.

You are an adversarial reviewer with one lens: mechanics and verification. Read only; no edits, no commits, no generators. Output at most 1800 words; quote at most 3 lines per point; never print large JSON or whole files; measure with small python one-liners over the named files only. Format: S7–S13; each ACCEPT / REFUTE / WITH CONDITION + evidence (2–6 sentences) + the exact rule you would write into the plan (cite D-number or §). No preamble, no summary.

Read: `docs/decisions/010_signed_quarter_boundaries.md` (ADR 010; D0 source ladder, D6 rank-3 channel with six states, D7 edge, D12 search normaliser, D13 atomicity, D19 red interval, gates G0–G24), `docs/plans/ПЛАН_ЛОТ_Границите_v2_05.09.md` (§0 measures М0.1–М0.23, §2 the lots A.2-11 → Г0 → Г1 → Ф0-б → Р → Г2 → P8 → F13 → push → Г3, §6 nine decisions, §7 done), `scratch/places_search/agkk_inspire_measure_05.09.md`, `scratch/places_search/ОТЧЕТ_Ф0б_05.09.md`. Code you may inspect: `C:/git/varna_3d/src/fire_varna_locations.py` (resolver, lines 673–905), `C:/git/varna_3d/src/qa_fire_varna_m6.py`, `C:/git/Fire_Varna/scratch/places_search/recall_sweep.py` (search, location_rows, bare_location_rows, place_tokens), `C:/git/Fire_Varna/index.html` 6248–7140 (client mirror), `C:/git/Fire_Varna/gates/release.py`, `gates/run_gates.py`. Your S1–S6 of today (in `docs/plans/ПЛАН_ЛОТ_Границите.md` §6.2) were accepted in full. The owner said: no regressions, only improvements; and: addresses must be easy to find.

S7. **Regressions.** Enumerate every path by which this plan can make the LIVE map worse for a firefighter (a place that disappears, a quarter that changes for a written row, a search that returns fewer or different rows, a client that rejects the whole blob) and, for each, name the gate in ADR section Гейтове that catches it BEFORE the push, or say "uncaught". Default to REFUTE where a path is uncaught.
S8. **D0 — one channel for two polygon sources** (official АГКК + declared). Is `SIGNED_POLYGON` as a single channel with a per-feature `source` mechanically sound in `_resolve_one` (rank 3, writes only in empty rows), or does the official polygon need its own rank (above KAIS-addr? above REG?) given the measured 109/110 agreement and the one dispute (ПАРК ХОТЕЛ БЕЛВЮ)? What breaks when both an official polygon and a `POST-2018` row exist for one place with different codes?
S9. **`POST-2018` as a rank-2 channel** (Ф0-б report sections 2–6): the join is by `address.street_phrase` + `house_key` with a normaliser and a difflib cutoff of 0.86 (7 fuzzy hits). Which of these can silently assign a wrong quarter (fuzzy street match, "всички" rows, missing odd/even, streets spanning two areas)? Write the acceptance gate and the fixture that must fail.
S10. **Search (D12) and "addresses easy to find".** The plan fixes quarter-phrase search (connective, dots, numbers, 11 collision tokens). Does anything in it change the building ADDRESS search (`initAddressSearch`, ADR 007) or the A3-street branch? Prove that byte-equality expectations are stated. What is missing for "addresses easy to find" that this lot does NOT do (say it plainly; do not plan it).
S11. **Order and blocking.** A.2-11 → Ред (Phase E archive) → Границите. Is Phase E (docs/archive moves, statuses in titles) mechanically safe for the gates (`release.find_queue` enumerates `scratch/places_search` from HEAD; `manifest_anchor_gate`; tests reading scratch paths)? Name every file the gates or tests read by path under `scratch/` and `docs/` that an archive move would break.
S12. **The eleven touches of the owner and the red interval D19/G20.** Which touches can be merged without weakening a gate; which cannot. Is the D19 interval enumerable today (run the suite read-only and say which tests would go red when `location_phrases` changes)?
S13. **What breaks** (at most 4, with evidence) and the single strongest point of the v2 plan.
