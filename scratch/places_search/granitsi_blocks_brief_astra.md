# Brief for Astra (Codex, GPT-6, ultra) · The idea "a micro-district is its blocks" — lens: mechanics, data sufficiency, end-to-end delivery. REFUTE IT.

You are an adversarial reviewer with one lens: mechanics and verification. Read only; no edits, no commits, no generators. At most 1500 words; measure with small python one-liners over the named files only; never print large JSON. Format: B1–B6; each ACCEPT / REFUTE / WITH CONDITION + evidence (2–6 sentences) + the exact rule. No preamble, no summary.

**Situation (05.09.2026, 20:30).** The owner (a firefighter) wants CLEARLY DELIMITED quarters of Varna on the public map — explicitly "Възраждане 1, Възраждане 2, Възраждане 3 …" — and to find them in the search. He is exhausted: "many ideas, none works the way I want". Measured today (files in `C:/git/Fire_Varna/scratch/places_search/`: `agkk_inspire_measure_05.09.md`, `ОТЧЕТ_Ф0б_05.09.md`; plan `C:/git/Fire_Varna/docs/plans/ПЛАН_ЛОТ_Границите_v2_05.09.md`, ADR `docs/decisions/010_signed_quarter_boundaries.md`): no source carries boundaries of the housing complexes — OSM has one node per complex (polygons only for villa zones), АГКК INSPIRE has official polygons for 6 quarters and the 5 districts but only points for Възраждане/Младост; the KAIS building attributes (`C:/git/varna_3d/web/varna_buildings_info.json`: `dict.addr` 19 813 strings, `dict.quar` 449, `rows` 80 497 = [addr_idx, func, reg, quar_idx, prop, floors, apps, area_m2, street, num]; geometry with `properties.i` in `C:/git/varna_3d/web/varna_buildings_3d.geojson`) give a residential quarter code for 19 000 buildings via the registry `C:/git/Varna_buildings/config/quarter_registry.json` (resolver `C:/git/varna_3d/src/fire_varna_locations.py` TypedRegistry.resolve), but WITHOUT the micro-district number: Възраждане 696 buildings, 225 with a block number ("ВЪЗРАЖДАНЕ бл 72", "ж.к. Възраждане №83"), a micro-district number in ~30 strings ("I-ви м.р.", "V"); Владислав Варненчик 2 779 buildings, 625 with block numbers; Младост 1: 5, Младост 2: 0, Възраждане 1/2/3: 1/1/0. The v2 plan had the owner DRAW ~26 polygons by local knowledge.

**Owner's clarification (20:40):** "Възраждане 1, 2, 3" was an EXAMPLE. The goal is that ALL 32 residential quarters of the registry (18 кв. + 11 ж.к. + 3 к.к.; the list is in `agkk_inspire_measure_05.09.md` §4) get a clear, delimited boundary and every place inside is findable by the quarter name. So the question is the OVERALL strategy by source class, not one complex: (i) the panel complexes (Възраждане 1–4, Младост 1–2, Вл. Варненчик I–V, Трошево, Победа, Чайка кв.); (ii) quarters with an official АГКК polygon (6) and villa zones with dense KAIS addresses (Виница, Изгрев, Бриз, Галата); (iii) the centre with no official source at all (Левски partly via the postal directory; Гръцка махала, Цветен квартал, Колхозен пазар, Център, Максуда). For each class: which source is honest, which is sufficient, which needs the owner's hand.

**The idea to refute (class i).** A micro-district is DEFINED by its blocks. With a signed table "micro-district → block numbers" (from the owner or, better, an official list: the municipal district site, the ЦИК election-section address lists "ж.к. Възраждане бл. 1–50", the ГРАО nomenclature "I м.р."), the boundary of Възраждане 1 = union of the cadastral footprints of its blocks (KAIS open data) + a declared buffer/hull; the quarter of a place = its block or its neighbourhood; the search "възраждане 3" returns the places in those blocks. Nobody draws. Second leg, "neighbour consensus": the quarter of a place without one = the code carried by ≥ 80 % of ≥ 8 buildings within 250 m in their own KAIS text; measured against the 201 written rows: 181 agree, 3 disagree, 17 no consensus; it adds a quarter to 40 of the 174 empty rows, nothing in the centre.

B1. **Data sufficiency.** From `varna_buildings_info.json` measure: how many buildings carry a parseable block number per complex (Възраждане, Младост, Владислав Варненчик, Чайка, Трошево, Победа, Изгрев, Бриз, Левски); how many of the 375 places (`C:/git/Fire_Varna/data/places.json`, `hotels.json`) lie within 60 m of such a block; whether block numbers are unique per complex (a "бл. 5" in Възраждане AND in Младост). Say plainly which micro-districts CAN be built from blocks and which cannot.
B2. **Geometry construction.** Union of block footprints → hull: which hull (convex / concave alpha / cadastral-cell union / buffer to the street axis) does not invent territory, is reproducible, and yields non-overlapping neighbours (Възраждане 1 vs 2)? What are the gates (validity, overlap ≤ N m, every block of the table inside its polygon, no block of another micro-district inside)? Cost in hours.
B3. **The table itself.** If only partial official lists exist (ЦИК sections list blocks per section, not per micro-district), what is the mechanically honest derivation, and what must be signed by the owner? What fixture proves a wrong table (a block assigned to two micro-districts; a block number absent from KAIS)?
B4. **Neighbour consensus as a WRITING channel** (rank next to KAIS-addr, states like D6): thresholds, edge handling (a place on a boundary street), the 3 disagreements (`Хоспис Филаретова` borovets_yug vs galata; `ДЯ №11 Иглика` troshevo vs mladost; `РИВИЕРА БИЙЧ` zlatni_pyasatsi vs chaika_kk) — what do they tell about the rule? Gate and fixture.
B5. **Search.** With micro-district codes on places (vazrazhdane1..4, mladost1/2), what changes in the reference (`C:/git/Fire_Varna/scratch/places_search/recall_sweep.py` location_phrases / bare_location_rows) so that "възраждане 3" returns exactly Възраждане 3, "възраждане" returns 1–4 + the parent, and "младост 2" keeps identity (S4)? Registry `parents` needed?
B6. **Verdict.** Which OVERALL strategy delivers "all 32 quarters clear and findable" end-to-end? For each class (i)–(iii) rank the paths — (a) the owner draws polygons in the viewer; (b) block table + KAIS footprints; (c) official АГКК polygons; (d) neighbour consensus on KAIS addresses; (e) OSM nodes / Voronoi — by mechanical soundness, hours to a visible result on the live map, and regression risk; give ONE recommendation per class, the total hours, and say plainly what remains impossible without the owner's hand. Then the single rule you would write into the plan.
