# Brief for Astra (Codex, GPT-6, ultra) · Find READY quarter boundaries for Varna in GIS services — lens: data discovery with exact, queryable URLs. Read-only; web search allowed; at most 1400 words; no large outputs.

The owner (a firefighter) wants the residential quarters of Varna (32 registry codes: Владиславово, Възраждане 1–4, Чайка кв./к.к., Бриз, Св. Иван Рилски, Св. Св. Константин и Елена, Трошево, Изгрев, Левски, Гръцката махала, Дружба, Младост 1–2, Кайсиева градина, Тракия, Галата, Победа, Виница, Аспарухово, Розова долина, Христо Ботев, Център, Максуда, Евксиноград, Колхозен пазар, Златни пясъци; plus Цветен квартал) marked on a map as polygons that he will CONFIRM one by one; he will draw nothing. Known so far (do not repeat): OSM has only NODES for these quarters (13 polygons, all villa zones; 2 admin_level-10 micro-district relations); АГКК INSPIRE (`inspire.cadastre.bg/arcgis/rest/services`) has official polygons for 6 quarters and the 5 districts; KAIS building addresses do not carry micro-district numbers; the postal directory 2018 assigns streets to some quarters; no full official boundary set exists (your earlier survey). The owner says the OSM node positions match reality better than address-derived blobs.

Your task: find MACHINE-READABLE polygon sources we have not yet queried, and return EXACT URLs the architect can call with curl (no sandbox network here — you find, we fetch):
A1. **Община Варна GIS.** The municipality links an ArcGIS Experience app (`https://experience.arcgis.com/experience/54f02e81882f40a5bcd69d75a871dc03/page`). Find the underlying ArcGIS Online items / FeatureServer or MapServer URLs (services.arcgis.com/<orgId>/arcgis/rest/services/…) and any layer named квартали / микрорайони / райони / жилищни комплекси / устройствени зони / зелена система по квартали. Also search for "gis.varna.bg", "varna.bg ГИС", "Община Варна ArcGIS Online", "Варна отворени данни shapefile квартали", data.egov.bg datasets from Община Варна with geometry. Give item ids and REST endpoints with `?f=pjson` when you find them; say "не мога да проверя" if you only see a UI.
A2. **Other public polygon layers naming Varna quarters:** НСИ НРНМ spatial data (which units?), the Varna "Общ устройствен план" web map (agup.varna.bg viewer — does it expose WMS/REST?), the Ministry of Regional Development "ГИС на устройствените планове", EEA Urban Atlas (functional urban areas — not quarters, say so), GADM/geoBoundaries (only admin levels — say so), Wikidata items for Varna quarters with `P625` coordinates and any `P3896` geoshape, OSM relations OUTSIDE our bbox query (search taginfo/Nominatim for "Възраждане, Варна" as a relation), and Nominatim results for each of the 32 names (does Nominatim return a polygon for any?). For each: URL, format, licence, and whether a polygon exists.
A3. **OSM micro-district admin relations.** The two admin_level-10 relations we saw ("4-ти микрорайон" 11262307, "8 микрорайон" 11261680): which complex are they in, who mapped them, when, and are there sibling relations (1st–9th микрорайон) elsewhere in Bulgaria or in Varna's history (deleted)? Give relation ids.
A4. **Verdict:** rank the newly found sources (if any) by (polygon exists · official · licence clear · covers how many of the 32); then say plainly whether a Voronoi partition of the OSM quarter nodes, clipped to the АГКК district polygons and to the built-up mask, with the owner confirming each cell, is the least-dishonest way to get 32 confirmable polygons tonight, and what fixture would catch its worst failure (a node placed off-centre).
Format: A1–A4, findings with URLs only; no plans, no code.
