# Brief for Sol (Codex) · LOT 1в "Expanded names and addresses of places" — lens: mechanics, gates, the outside eye

You are an adversarial reviewer with one lens: mechanics and verification. You do not execute. Answer with REFUTATIONS and verdicts, each with evidence; no general advice. If you cannot verify something, say "cannot verify" — do not guess. Read the repo, do not write to it.

## What you review

The plan: `C:/git/Fire_Varna/docs/plans/ПЛАН_ЛОТ1в_имена_и_адреси.md` (read it whole; Bulgarian). Context: `C:/git/Fire_Varna/docs/plans/recommendations_2026-09-03.md` §2 and amendments №8–№11 (LOT 1 just closed on `main`: places search `initPlacesSearch` in `index.html` ~6245–7100 with the KEY model A1–A7 / P2–P7, EXTRA_FORMS, KIND_GROUP; the Python reference `scratch/places_search/recall_sweep.py` with `search(q) -> (rows, branch)`, frozen artefact `scratch/places_search/recall_sweep_rows.json` (122 rows, anchor 23af63f), the headless probe `scratch/places_search/probe_places_fv.mjs` (М5 parity, G3 digest = the address search `initAddressSearch` IIFE (index.html 4833–6234) must stay byte-equal, sameEnv, С6′ corpus, С8′), tests `tests/test_places_search_gate.py`; the three delivery blobs are generated in `C:/git/varna_3d` (`src/export_fire_varna_places.py`, `src/build_place_categories.py`, `data/place_aliases.json` → `old_names`) and pinned by sha in `index.html` (`PLACES2_SHA256`, executed at load by `fetchValidatedJson`). Petar signed Gate 1-A (lot A, expanded names). Lot B (addresses) waits for a measurement and Gate 1-B.

## Questions (answer by number)

S1. **Lot A mechanics.** Aliases enter as `old_names`/`aliases` via varna_3d `data/place_aliases.json` → export → blob → sha pin. Verify the claim "no client change needed": does `buildIndex` (A6, index.html ~6553) index `old_names` tokens into the alias set, and does `significant()`/`tokenMatch` treat alias tokens like name tokens for the M2/A3 branches? Refute with a concrete query where an alias would NOT be reached (e.g. multiword alias "Висше военноморско училище, Варна" — is the comma stripped; do stop-words like "варна" pollute; would "военноморско" alone hit via alias?). Measure with the reference if you can run Python.

S2. **Per-alias source (the attribution debt).** The plan proposes `aliases: [{name, src}]` (or a parallel `old_names_src`) and the card printing the alias's source. Which shape breaks fewer things: the client's `SRC_LINE` (index.html ~6277–6284), the validators (`validatePlaces2`), the reference's corpus (С6′ counts names + old_names), the tests that pin the blob's key set (`tests/test_places_public_bundle.py` — 8 keys per row?). Propose the minimal shape and the exact gates.

S3. **Dictionary words** (plan §1 item 1): forms added to `place_categories.json` (built in varna_3d `src/build_place_categories.py`; the client's A1 treats forms as class keys). Refute: does adding "гимназия" → училище change existing answers (the frozen 122 rows)? Which rows move — measure with a prototype copy if possible (the earlier prototype with 3 aliases moved 0/122; forms are different: they become KEYS and change the branch M1/A3). What is the right freeze procedure (re-freeze vs additive)?

S4. **Lot B search rules** (plan §2 item 3): a new token class "street" (`sset`) + house number significant only next to a street token; never in `zkset`. Attack: (a) collisions between street names and zone aliases/quarter names ("Владислав Варненчик" is both a ж.к. and a бул.; "Република"; "Чайка"); (b) collisions with hotel/place NAMES ("ГРАДИНА", "Приморски", "Морска градина"); (c) numbers: "детска градина 12" today means №12 (ordinal → name number); with streets "дойран 9" means house 9 — how to keep both without regressions (A5 rule); (d) the KEY model: which branch handles "<class> <street>" and "<street> <number>" — a new A3′-street branch or the existing A3′ with a street token set; propose the minimal rule and 6 gate queries with expected answers you can defend; (e) interaction with the buildings' address search (`initAddressSearch` — untouchable): a query like "дойран 9" today goes to the address search results; the places section must not steal or duplicate it — what does the UI show?

S5. **Data for lot B:** the `address` field per record with source priority KAIS body > registry > OSM > none, canonicalised ("ул. ДОЙРАН 9" → "ул. „Дойран" 9"). Refute the canonicalisation (Bulgarian street-name casing is not derivable from ALL-CAPS: "бул. ОСМИ ПРИМОРСКИ ПОЛК"; "ул. ЦАР ОСВОБОДИТЕЛ"); propose a rule that never invents casing (e.g. keep the source casing, or title-case only with a signed exception table). Byte budget: 375 × ~40 B; `PLACES2_SHA256`/`HOTELS_SHA256` pins; the hotels blob's `uins` join with NTR `TOAddress` (11 rows carry a cadastral identifier inside the address — the masking gate).

S6. **Gates and freeze for lot B:** which of the 122 frozen rows will move when street tokens enter the index (queries with numbers: "градина 12", "дг 12", "детска градина 12", "хотел 3"?). Propose the measurement and the acceptance: exact named list, fail-loud, reference re-frozen once, G3 byte-equal, probe М5 N/N, tests.

S7. **Deployment safety.** Petar asks "we will not break the map, will we?" Before the push: `data/hydrants.json`, `data/search_index.json`, `data/address_rows.json`, `sw.js`, `worker/` are byte-identical to `origin/main`; the address IIFE is byte-identical; tests 186 OK; `index.html` 526→532 KB; the SW registers only behind a committed-false flag. Refute: is there any path by which the LOT 1 changes (three place blobs + sha pins + `initPlacesSearch` rules + `KIND_GROUP`/`GROUP_COLOUR`/`EXTRA_FORMS`) could break hydrant display, the Топ 5/Близо/Всички modes, the report flow, or the first load on a slow phone? Name the check you would run.

S8. Anything else that breaks this plan that I did not ask. Max 5 points with evidence.

## Answer format

Numbered verdicts S1–S8; each: ACCEPT / REFUTE / WITH CONDITION + evidence in 2–6 sentences + the exact change to the plan (quote the line). No preamble, no summary.
