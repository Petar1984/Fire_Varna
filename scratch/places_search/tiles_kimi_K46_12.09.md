# Кими К46 — леща върху делтата К9а → К9б (12.09.2026, ultra)

Вход (без геометрия): дифът на index.html b737284..c886f1c + функцията дословно, §3.Д + О36–О50, корпусът Ф11. Присъда: нищо не спира Gate 2; регексът е кирилско-безопасен (`\s`, не ``), „св св“ съзнателно извън списъка, приетият клас О40 правилно извън §3.Д, предпазките верни; две бележки: слепването не е котвено в началото (теоретичен случай с инициали — не се пипа, нова мярка при стесняване) и корпусът няма разредени „ж к“/„в з“/„с о“ (влизат в К7ж). Записът получава ред в ALLOWED_NEW в К7ж (О46-правилото). Координати няма.

---

• Одитът приключи. По-долу находките — препратките са относително към корена на копието, а „симулация" означава, че изпълних дословно регекса от `01_k9b_diff_and_function.md:12-20` в Node и цитирам реалния изход.

**Находки**

1. **ОК — Обхватът на дифа отговаря точно на О48.** Дифът пипа само `flat` (четирите двойки, `01_k9b_diff_and_function.md:12-20`) и двете предпазки (`01_k9b_diff_and_function.md:30-31`). Тирето НЕ се пипа: старият и новият ред запазват `.replace(/[,-]/g, ' ')` (`01_k9b_diff_and_function.md:15-16`) — съгласно решението по О45 (кандидат Б, тирето остава интервал). Също така двете извадки на `quarterTitleWord` във файл 01 (редове 41–67 и 92–168) са припокриващи се погледи върху едно и също място — има една дефиниция на `flat`, не две разминати.

2. **ОК — Регексът е кирилско-безопасен и без `u` флаг.** Кодът използва `(^|\s)` и `(?=\s|$)` (`01_k9b_diff_and_function.md:13`), а не `\b` — правилно, защото в JS `\b`/`\w` са ASCII-only и `\bк к\b` върху кирилица би дал грешни граници; `\s` не зависи от `u` флага. Главните букви са покрити от `toLowerCase()` преди регекса (`01_k9b_diff_and_function.md:16`): симулация `"К К Чайка" → "кк чайка"`. Повторения: `"к к к" → "кк к"`, `"ж к ж к" → "жк жк"` — детерминистично, и няма такъв реален запис в корпуса. Споделеният `/g` регекс няма теч на състояние между извикванията: `lastIndex` след `replace` е 0 (измерено), за разлика от `test()/exec()`, които биха били бъг тук.

3. **БЕЛЕЖКА — Слепването не е котвено в началото на низа.** Контекстът го описва като „на граница на дума", но `(^|\s)` с флаг `g` слепва двойка навсякъде в низа. Теоретичен фалшив случай: инициали — `"В. З. Иванов, …"` → `flat` → `"вз иванов"`, и тъй като `вз` ∈ `FLAT_TYPES` (от `в.з.`, `01_k9b_diff_and_function.md:96-97,121`), клон (а) би отрязал сегмента като представка. В корпуса такъв низ няма: единствените еднобуквени токени в `03_corpus_F11.json` са цифри, входни букви след `vh` (`vh v`, `vh zh`, `vh z`, `vh k` — не образуват двойка) и `k chaika`, `k sv konstantin i elena`. Симулирани безопасни низове: `"вх. В зала" → "вх в зала"`, `"бл. 5 с обзавеждане" → "бл 5 с обзавеждане"`, `"ул. К Константин" → "ул к константин"` — lookahead-ът `(?=\s|$)` изисква втората буква да е самостоятелен токен. **Препоръка: не се пипа сега** (мерената 0/0 по О45 е постигната с тази формулировка); ако някога се стеснява, точната формулировка е `'^(к к|ж к|в з|с о)(?=\\s|$)'` — но това изисква нова мярка индекс по индекс.

4. **ОК — „св св" е съзнателно извън списъка.** Най-силното доказателство, че затвореният списък е правилен: `"св. св. Константин и Елена" → "св св константин и елена"` (неслепнато), докато `"к к св св константин и елена" → "кк св св константин и елена"` — само представката се слепва, светците остават. Корпусът носи точно тази заявка (`03_corpus_F11.json`, заявка „к к св св константин и елена") и записа `-1 / address / k sv konstantin i elena`. Това е и втората половина от критерия на О39.

5. **ОК — Приетият клас О40 (12 записа / 37 реда) е коректно извън §3.Д.** Трасировка по кода за „к.к. Златни пясъци, к к златни пясъци апартхотел бендита": има `, ` → клон (а)/(б) (`01_k9b_diff_and_function.md:144-153`) реже САМО водещия сегмент („к.к. Златни пясъци", lead `кк` ∈ `FLAT_TYPES`), опашката „к к златни пясъци апартхотел бендита" остава дословно. „Евтина" втора резка (режи опашката, ако започва с преизписване на името) би сгрешила при обекти, чието собствено име легитимно започва с името на квартала, а резка „по стебло" е точно дългът ИБ2-4 с условието „еднозначно сред 90-те". Съгласен с О40/О50: не се реже сега и не е спирачка за Gate 2.

6. **ОК — Предпазките `panelBlockLabel`/`popupLabel` са правилни.** И двете стават `(quarter && <label> && quarterTitleWord(...)) || <label>` (`01_k9b_diff_and_function.md:30-31`) — при празен надпис `quarterTitleWord` вече не се вика и панелът/попъпът пазят днешното поведение вместо да покажат голото име на клетката (регресията от О38(4)). Симетрични са и следват съществуващия стил на реда. Няма път, в който панелът показва различна ДУМА от реда: и двете повърхности минават през същия `quarterTitleWord` (Т9, STOP 19 от §3.Д). Малка оговорка: проверката е truthiness, не trim — надпис от само интервали би подминал предпазката, но по О44 `baseAddressLabel` никога не връща празен низ (последна клауза „(адрес)"), затова предпазката е латентна закалка.

7. **БЕЛЕЖКА — Покритие на корпуса за разредените двойки.** В 30-те заявки на `03_corpus_F11.json` разредена двойка има само за „к к" („к к чайка", „к к св св константин и елена"); разредени „ж к", „в з", „с о" форми липсват (има слепнати „жк бриз", „кк златни пясъци"). Ако `SpacedPrefixTest` от О47 ги покрива извън корпуса — ок; иначе точната препоръка е: добави по една заявка за всяка от трите двойки (напр. „ж к бриз", „с о ален мак", „в з …") във Ф11. Останалите числа на корпуса съвпадат с протокола: 30 заявки / 151 записа (О47); 61 угасени студентски записа (47 `ul studentska 1` + 14 `ul studentska`) + 3 нови `-1` (`k sv konstantin i elena`, `aladzha manastir`, `akatsia 0`) = 64 с `-1` (О38(5), О47).

8. **ОК — Нищо не спира Gate 2.** Кръстосаната проверка на трите файла не показва разминаване: дифът е в рамките на О48 (т. 1), регексът е безопасен за реалния товар (т. 2–4), приетият клас е аргументирано оставен (т. 5), предпазките са верни (т. 6), а отминалият проблем с половината `client_before_the_dotted_fix` е затворен с К7е по О49 (трета котва върху `SPACED_PREFIXES`) — нищо във файлове 01/03 не го противоречи. Единственото, което бих вдигнал преди гейта, е т. 7 — да се потвърди, че „ж к"/„в з"/„с о" разредените форми са покрити от тестовете (бележка, не блокер).

