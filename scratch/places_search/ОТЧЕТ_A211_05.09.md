**A.2-11: кодът на гейтовете е вкаран; архивирането на опашка A е СПРЯНО (STOP) преди хода.**

## Мярка (baseline, преди промяната, HEAD = a749930)
- `PYTHONIOENCODING=utf-8 python -m unittest discover -s tests` → `Ran 306 tests … OK` (0 червени)
- `python -m gates.run_gates` → `✓ всички гейтове зелени`, 7/7, `rc=0`
- `python -m gates.release` → `✓ release: зелено`, `rc=0`
- Опашки в HEAD: `git ls-tree -r --name-only -z HEAD -- scratch/places_search | grep ЗА_ПОДПИС` → точно 1 (`scratch/places_search/ЗА_ПОДПИС_A.md`); 27 реда „да“, всеки блок с ЕДИН въвеждащ комит `c060f43 Petar1984` (т.е. т. 2 не почервенява доставката); всеки „да“ ред с артефакт носи видимо `тяло` (т.е. новият блок на гейт 6 не почервенява доставката).

## След промяната (HEAD = 38e9dd9)
- unittest → `Ran 310 tests … OK` (306 + 4 нови, 0 червени)
- `python -m gates.run_gates` → 7/7 зелени, `rc=0`
- `python -m gates.release` → зелено, `rc=0`

## Какво е вкарано (амандамент №10, т. 1, 2, 3, 5)
- `C:/git/Fire_Varna/gates/sign.py` — `decide_row` обхожда само ВИДИМИТЕ редове (`release.visible_and_hidden`, една истина с парсера); нова `hidden_region_complaint(lines, row)` — скрит регион между заглавието на реда и следващото ВИДИМО заглавие = отказ на писалката (викана в `main` до `duplicate_field_complaint`, преди каквото и да е записване).
- `C:/git/Fire_Varna/gates/release.py` — нова `queues_in_head()` (`git ls-tree -r --name-only -z HEAD -- <QUEUE_DIR>`, `-z` заради кирилицата); `find_queue` изброява от HEAD (>1 → `повече от една опашка в HEAD`); проследена опашка, изтрита от диска → изричен блок „…е в HEAD, а в работното дърво я няма“; нова `introduced_commits()` (един `git log -S`, двата края), `introduced_by`/`introduced_by_text` вече само я четат; `yes_row_authorship` иска И най-новия, И най-стария комит на блока да са `Petar1984`; гейт 6 блокира „да“ по артефакт без видимо поле `тяло`.
- `C:/git/Fire_Varna/gates/run_gates.py` — проверка 7 чете `release.hidden_decisions` върху блоба на HEAD; оплаква се, когато `find_queue` върне None, а `queues_in_head()` не е празно; `queue_rel` се смята и когато файлът го няма на диска (изтрита опашка вече не минава мълчаливо).
- `C:/git/Fire_Varna/tests/test_places_search_gate.py` — 4 фикстури в `FreezeAndAnchorTest`.

## Четирите фикстури: червени преди, зелени след
| фикстура | преди (изход) | след |
|---|---|---|
| (а) `test_a_second_queue_in_head_blocks_even_when_the_disk_shows_one` | `AttributeError: module 'gates.release' has no attribute 'queues_in_head'`; при revert само на `find_queue` към glob-а по диска: `AssertionError: 0 != 6` (втора КОМИТНАТА опашка, махната от дървото, е невидима — гейтът зелен) | зелена |
| (б) `test_a_body_digest_written_into_a_hidden_region_is_no_signature` | `AttributeError: … no attribute 'hidden_region_complaint'`; revert на `if (i+1) in hidden: continue` → `AssertionError: '```' != '- **тяло:** aaaa…'` (писалката пише дайджеста ВЪВ фенса); revert на новия блок на гейт 6 → `AssertionError: 0 != 6` | зелена |
| (в) `test_a_yes_block_an_agent_returned_is_not_a_permission` | `AssertionError: 0 != 6` срещу СТАРИЯ код (върнат дословно блок: най-старият комит на Петър → зелено) | зелена |
| (г) `test_a_tracked_queue_deleted_from_the_disk_is_named` | `AttributeError: … 'queues_in_head'`; revert на `find_queue` → `['няма опашка ./ЗА_ПОДПИС_*.md …']` вместо име на изтриването; revert на пазача в проверка 7 → `['опашката …е извън това репо…'] is not false` | зелена |

## STOP: архивирането не се извърши
Мярка в еднократни клонинги (не в репото), с НОВИЯ код: `git mv scratch/places_search/ЗА_ПОДПИС_A.md docs/archive/queues/ЗА_ПОДПИС_A.md` + комит →

```
✗ 6 · release (референция ↔ двигател ↔ подписана опашка)
      ✗ няма опашка scratch/places_search/ЗА_ПОДПИС_*.md — нищо не е разрешено
⛔ ЧЕРВЕНО: 6 · release
```
(пакетът остава `Ran 310 … OK`; проверка 7 остава зелена — в HEAD наистина няма опашка, така че кръстосаната проверка на т. 5 няма за какво да се оплаче.)

Причина: `release.run` блокира безусловно при `find_queue() is None` („нищо не е разрешено“) — замразената доставка с 0 делти няма изключение за „няма опашка“; `find_queue` по амандамент №10 т. 3 гледа САМО `scratch/places_search`, а `docs/archive/queues` е извън обхвата. Ходът иска архитектурно решение (амандамент): или директорията-регистър на опашките включва архива, или замразена доставка без делти има право на 0 опашки. Затова: гейт-кодът влиза, архивирането — не; използвано е резервното съобщение от плана.

## Комит
`38e9dd9` · `Claude Executor` · `gates: A.2-11 — visible rows only, both ends of a yes block by Petar, queue enumerated from HEAD recursively (A.2-11, archive step deferred)`
`git status --short` след комита: 0 проследени промени (само 37-те заварени непроследени файла в `scratch/places_search/`, недокоснати). Нищо не е пушнато, нищо не е замразявано, никакъв подпис не е пипан.