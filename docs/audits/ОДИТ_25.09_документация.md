# ОДИТ 25.09.2026 · документацията и ненужните файлове

**Файл:** `docs/audits/ОДИТ_25.09_документация.md` · **Дата:** 2026-09-25 · **Измерено при:** `8b80a20` (`data: cycle #45` = `main` = `origin/main`) · **Клон на прегледа:** `claude/gracious-hamilton-24npor` · **Поръчка (Петър, в чата):** „прегледай проекта за несъответствия в документацията … да го почистим от ненужни файлове и да актуализираме информацията в документацията“ · **Статус:** изпълнено в четири комита + този доклад; таблицата в §4 чака думата на Петър.

> Този документ е доклад, не план. Каквото е поправено, е поправено в комитите по §3 и е проверимо с командата до всяко число. Каквото не е изтрито, стои в §4 с причината и с препоръка — решението е на Петър (гейт за разрушителни операции, CLAUDE.md § Hard Rules 4).

## 1 · Обхват и метод

**Четени изцяло:** `AGENTS.md`, `CLAUDE.md`, `README.md`, `docs/activeContext.md`, `.gitignore`, `.gitattributes`, `docs/plans/PARKED.md`, `worker/README.md`, `extract_hydrants.py`, `scripts/replay_historical_new_hydrant.py` (докстринг), `tests/address_slice_probe.mjs` (краят). **Четени по редове:** статусните редове на всички 15 ADR-а и на плановете със статус; главите на `docs/sessions/*`, `docs/moderation_log.md`, `docs/archive/*`; `buildNavActions` и `data-type` в `index.html`; етикетите в `worker/index.js`.

**Инвентар (детерминиран):** `git ls-files` → 794 тракнати файла при `8b80a20`; по папки: `scratch/` 578, `docs/` 108, `tests/` 33, `data/` 24, `scripts/` 12, `gates/` 10, `worker/` 9, `audit/` 9, `vendor/` 2, корен 8.

**Мерки (командата е до всяко число в `docs/activeContext.md § Current state`):** записи 7400; `index.html` 623742 B; първо зареждане 1949029 B; тестове `Ran 481 · FAILED (failures=2)` на Linux; гейтове `✓ всички гейтове зелени`; отворени issue-та 0; етикетът `app-feedback` съществува.

**Скрипт за връзките:** всеки `[…](относителен път)` във всички `.md` извън `scratch/` → съществува ли целта. 107 файла, 15 счупени връзки преди, 0 след (§3).

**Какво не е измерено оттук:** кодът на GitHub Pages и `GET /issues` на Worker-а — прокси-то на сесията отказва `github.io` и `workers.dev` (403 CONNECT); локалните клонове на машината на Петър (прегледът тече в свеж клон).

## 2 · Находки

Подредени по тежест. „Поправено“ = в комитите по §3; „чака“ = §4 или §5.

| # | Къде | Несъответствие | Доказателство | Изход |
|---|---|---|---|---|
| Н1 | `docs/activeContext.md` | Страницата беше 11 хроникални бележки (05–15.09) върху тяло от 05.09; таблицата казваше 7407 записа, `index.html` 579399 B, 266 теста, „last pushed 6da5d9a 2026-09-04“, а редът над него — „published c92224c, 12.09“. Собственото ѝ правило: „една страница, всяко число с командата си“. | `git log -1 origin/main` → `8b80a20 2026-09-25`; `wc -c < index.html` → 623742; пакетът 481 | **Поправено:** хрониката замразена в `archive/activeContext_2026-09-15.md`, страницата пренаписана и премерена (`e18bf60`). |
| Н2 | `README.md` (двата езика) | „🧭 един tap … Google Maps при по-голямо разстояние, вграден компас в последните 100 м“ — няма такъв поток: картичката има четири външни линка (Waze, пеша, карта, Street View). ⚠-бележките от 02.09 сочеха редове 4132/4205/4397/4507 — днес 4653/4918/5028. Липсваха видът `missing` и шестият бутон на лот О. „Без runtime зависимости“ — `VECTORGRID_CDN` от unpkg. | `grep -n -A10 'function buildNavActions' index.html`; `grep -o 'data-type="[a-z_]*"' index.html \| sort -u` → 6 вида | **Поправено** (`05fc0da`): бележките са ✓ към 25.09 с команди без номера на редове. |
| Н3 | `AGENTS.md` § Implemented Features 6 | „Hybrid navigation — >100 m Google Maps, ≤100 m in-app compass target“ — виж Н2. | същото | **Поправено** (`05fc0da`). |
| Н4 | `AGENTS.md` § Known Tech Debt 1 | Цитира `updateCard()`, която не съществува в `index.html`. | `grep -c updateCard index.html` → 0; `grep -n 'function targetCardHTML' index.html` → 3146 | **Поправено:** сочи `targetCardHTML()` + пренавързването по CLAUDE.md. |
| Н5 | `AGENTS.md` § Dual-Claude-Code Workflow | Изречението казва „model per `~/.claude/agents`, not pinned here“, а таблицата под него пинва „Opus“ във всеки ред; CLAUDE.md също не пинва. | редове 150 срещу 154–157 | **Поправено:** таблицата препраща към `~/.claude/agents`. |
| Н6 | `AGENTS.md` § Current Repo State | Дървото нямаше `gates/` (10 файла, гейтовете на изданието), куката `scripts/hooks/pre-push`, node пробите в `tests/`, `docs/sessions`, `docs/archive`, половината `data/`; сочеше `extract_hydrants.py` и `hydrants_varna.json`. | `git ls-files gates` → 10 | **Поправено** (`ade93a6`, `05fc0da`). |
| Н7 | `AGENTS.md` § Report Flow, § Implemented Features 9 | Лот О (15.09) не присъстваше: шестият вид `app_feedback`, етикетите `app-feedback` + `fb-<вид>` + `pending-review`, четворното затваряне на тръбата към картата. | `docs/audits/амандамент_подаване_15.09.md`; `worker/index.js` ред 327 `labels=report` | **Поправено** (`05fc0da`). |
| Н8 | `CLAUDE.md` § Verification | Казва само `python -m unittest`; куката на Петър пуска и `python -m gates.run_gates` без break-glass — червен гейт блокира пуша, а изпълнителят не беше длъжен да го пусне. | `scripts/hooks/pre-push` редове 5–9 | **Поправено** (`05fc0da`). |
| Н9 | `docs/archive/activeContext_2026-09-04.md` | Връзката към хрониката от 07.04 беше `archive/activeContext_2026-07-04.md` — счупена, откакто файлът е преместен в `docs/archive/`. | скриптът за връзките | **Поправено** (`e18bf60`); новият архив от 15.09 е записан с правилните връзки. |
| Н10 | `docs/audits/governance_proposal_20260508.md` | 14 счупени връзки: пътища спрямо корена, не спрямо `docs/audits/`, и котвата `#tri-agent-workflow`, преживяла преименуването на секцията (CLAUDE.md § Workflow Expectations изисква котвите да се обновят в същия проход). | скриптът за връзките | **Поправено**, текстът непроменен. |
| Н11 | `docs/plans/PARKED.md` | „Текущият лот в движение: Фаза A“ — Фаза A е пушната на 05.09 (`6da5d9a..fd13907`). | `docs/archive/activeContext_2026-09-15.md`, бележката от 05.09 16:55 | **Поправено:** главата казва „няма“, редът от 05.09 е запазен; редовете P1–P3, P5 непипнати. |
| Н12 | корен: `extract_hydrants.py`, `hydrants_varna.json` | Скриптът търси `<script id="hydrantData">` с JSON вътре — тагът е празен от пивота към `fetch`; файлът е компактният KMZ износ от 05.05 (3 934 записа), изместен от `data/hydrants.json` + provenance. | `sed -n '1814p' index.html` → празен таг | **Изтрити** (`ade93a6`); в историята при `8b80a20`. |
| Н13 | `data/*.pre_*.json` (3.19 MB), `scripts/replay_historical_new_hydrant.py`, `docs/audits/…stderr.log` | Снимки от май, които `.gitattributes` сам наричаше „Dead snapshots“; единственият им консуматор — еднократният H1 replay, вече пуснат, без импорт от тест; `.log` файл, тракнат въпреки `*.log` в `.gitignore`. | `grep -rl replay_historical tests` → нищо | **Изтрити** (`ade93a6`). |
| Н14 | `tests/address_slice_probe.mjs` (редове 524–525) | `process.stdout.write(JSON…); process.exit(…)` — на Linux pipe-ът е асинхронен и `exit` реже отговор > 64 KiB; два теста падат с „Ф9 не върна json … Unterminated string“. На Windows pipe-ът е синхронен — там пакетът е зелен, както казва бележката от 15.09. | `python -m unittest tests.test_address_entrances_under_building …` | **Чака (§5):** поправката е един ред (`process.exitCode`), но пробата е пинован харнес (ADR 012 D7) — влиза през ред в план. |
| Н15 | `docs/plans/hotels_search_plan.md` | Статус „DRAFT — AWAITING PETAR SIGNATURE“ от 22.08, а хотелите са на живо от 05.09 под ADR 006 и доставката на местата. | `data/hotels.json` 225 реда; README § Хотели | **Чака (§5):** статусният ред е на плана — Петър решава „затворен/изместен“ или архив. |
| Н16 | `docs/decisions/002_osm_pmtiles_basemap_offline.md` | Статус „DRAFT — STOP B0“ от 05.07, а изданието на базовата карта е в `data/basemaps/`, `sw.js` е в репото, флагът е `false`. | `grep -n 'BASEMAP_PMTILES_ENABLED = ' index.html` | **Чака (§5).** |
| Н17 | `AGENTS.md` редове 15, 19, 45 | HTML коментари „сверка 01.09.2026: спорно, виж `C:\git\plan.md` приложение Е“ — сочат файл извън репото, непроверим оттук; „непроверено“ маркерът е в правилата и остава. | — | **Чака (§5):** само Петър знае дали `plan.md` още съществува. |
| Н18 | `origin/batch-ingest/2026-05-28`, `origin/ingest/issue-62` | Два отдалечени клона от 28.05, замразени следи; изтриването е push. | `git for-each-ref refs/remotes` | **Чака (§4).** |

Без находка (проверено и вярно): Worker версия `5accc88e` (README на Worker-а и AGENTS.md); KV TTL 30 s / poll 15 s (CLAUDE.md); функциите от CLAUDE.md § Specific Gotchas съществуват (`targetCardHTML`, `showReportModal`, `showReportTypePicker`, `wireReporterBar`, `wireFormHandlers`, `pollIssues`, `lastPollSince`, `deepLinkFramed`, `isBasemapPmtilesActive`, `ensureDetailSheet`); правилото за `wrong_location` (0 `field_*` id-та, 27 `__NAT` суфикса); няма `.github/` (CI отсъства, както пише); хешовете, цитирани като произход (`e846b87`, `142a494`, `914dc2a`, `bc18d54`, `c92224c`, `fd13907`, `a3042ed`) съществуват; `app-feedback` етикетът е създаден (дългът на лот О е затворен).

## 3 · Какво е направено (комитите на клона, по ред)

| # | Комит | Файлове | Нетно |
|---|---|---|---|
| 1 | `ade93a6` chore: retire dead root artifacts, May 2026 snapshots and a tracked log | −7 файла (Н12, Н13); `.gitattributes` −4 реда; `AGENTS.md` 4 реда | −≈ 4,9 MB от чекаута |
| 2 | `e18bf60` docs(state): re-measure activeContext on 25.09, freeze the 05–15.09 chronicle | `docs/activeContext.md` (пренаписан), `docs/archive/activeContext_2026-09-15.md` (нов), `docs/archive/activeContext_2026-09-04.md` (връзка), `docs/plans/PARKED.md` (глава) | Н1, Н9, Н11 |
| 3 | `05fc0da` docs: README, AGENTS.md and CLAUDE.md brought to the state measured on 25.09 | трите документа | Н2–Н8 |
| 4 | `9acffab` docs: repair the 14 dead links of the 08.05 governance proposal | `docs/audits/governance_proposal_20260508.md` | Н10 |
| 5 | docs(audit): the 25.09 documentation audit | този файл | — |

**Проверки след комитите:** скриптът за връзките → 0 счупени; `python -m unittest discover -s tests` → Ran 481, същите 2 червени (Н14, само Linux); `python -m gates.run_gates` → зелено; `git status --short` чист; `data/hydrants.json`, `index.html`, `worker/`, `sw.js` байт-равни на `8b80a20`.

**Отклонение от CLAUDE.md § Hard Rules 1, обявено:** клонът `claude/gracious-hamilton-24npor` е пушнат от сесията към `origin` — сесията тече в облачен контейнер, който се рециклира, и без push работата изчезва. `main` не е пипан; Gate 2 остава изцяло у Петър (преглед на дифа, merge, push към `main`). Куката `scripts/hooks/pre-push` не е инсталирана в този клон (свеж clone), така че нищо не е заобикаляно; пакетът и гейтовете са пуснати на ръка преди пуша.

## 4 · Таблица за решение — какво НЕ е изтрито и защо

Критерий за „носещ“ файл: сочен от `tests/`, `gates/`, `scripts/`, `worker/`, `index.html` или `sw.js` (не от `.md`). Числата са от `git ls-files` + сканиране на съдържанието при `8b80a20`.

| Кандидат | Обем | Носещи | Само от документи | Без нито една препратка | Защо чака | Препоръка |
|---|---|---|---|---|---|---|
| `scratch/places_search/` | 161 файла · 16 MB | **15** (`recall_sweep.py`, `expectations.json`, `recall_sweep_rows.json`, трите манифеста `lot1v_v_*`, `m7_*`, `ib_corpus_11.09.json`, `granitsi_fixtures_0{6,7}.09.json`, `probe_places_fv.mjs`, `lot1_reference_preview_v2.md`, `lot1v_addresses_for_signature.md`, `tiles_kimi_K4{5,6}_12.09.md`) | 74 | 72 | Гейт 6 пинва дайджестите на 8 от тях; Петър ги е подписал (`c060f43`). Прегледите на Astra/Кими (`*_astra_S*.md`, `plan_ib_*`) са до 1 MB всеки — свидетели на Gate 2. | Носещите остават. За 72-та без препратка + 3 PNG в `probe_out/screens/` (1,2 MB) — един подписан списък, както на 02.09 (`614fb34`). |
| `scratch/audit_2026-09-03/` | 237 файла · 4,3 MB | 0 | 24 | 213 | Одитът на данните от 03.09 (`ОДИТ_03.09_система.md` го цитира); съдържа пръстени под ключ `pins` — редът за „семейството П“ на гейта D-25. | Кандидат за `docs/archive/` или изтриване като папка — след думата на Петър. |
| `scratch/kais_func_2026-09-03/` | 114 файла · 4,8 MB | 0 | 6 | 108 | Геокодирани регистри и парцели от 03.09; вход за доставка, която стана в `varna_3d`. | Както горе. |
| `scratch/` корен | 36 файла | 1 (`apply_c38.py` — гейтът `b3_g11.py` го чете) | 16 | 19 | `apply_c33..c45.py` са адаптерите на циклите (moderation_log ги цитира); `ПРОМПТ_*`, `dev_notice_*`, `governance_migration_plan.md`, `marker_redesign_frame.md`, `BOARD.md` — рамки от август, влезли в git на 02.09 по подписания списък „нищо не е изтрито“. | Адаптерите остават. Рамките и промптите — Петър решава; изтриването им не пипа нито един тест. |
| `scratch/basemap_e0/`, `basemap_b2/`, `pozarna_gz/` | 30 файла · 0,3 MB | 3 (`basemap_e0/README.md`, `basemap_b2/{copy,vendor}_report.json`) | 6 | 21 | E0 е затворен от Петър (PARKED P4: „не се премерва“); `pozarna_gz/px_wgs84.json` е латентно попадение на гейта D-25 („първият комит, който го пипне, ще иска ред в LEGACY“). | Остават — цената на изтриването е ред в LEGACY, не е за този проход. |
| `audit/` | 9 файла · 83 KB | 0 | `AGENTS.md`, `CLAUDE.md`, 8 документа от май | — | Май 2026: `apply_field_reports.py` (предшественикът на `scripts/apply_approved_reports.py`), двата ingest доклада от 28.05 (входът на изтрития replay), пробите на Worker-а преди/след комит 15. | Преместване в `docs/archive/audit_2026-05/` с пренаписване на препратките, или изтриване. Дребно — по избор на Петър. |
| `docs/audits/*.json` (3 големи) | 5,5 MB | 3 (`scripts/import_etr_kmz.py`, `scripts/migrate_to_verbose_schema.py` ги пишат като изход) | — | — | Следата на мутациите на данните (сух прогон H2, миграцията от 08.05, backfill 11.05). | **Остават** — това е одитната пътека, която README обещава. |
| `docs/sessions/` | 3 файла · 197 KB | 0 | 9 плана и ADR 011 ги цитират по път | — | Предавания между сесии на цикъла „Границите“ (06–08.09); лотът е затворен (И-А изпълнен 11.09). | Остават на място — преместването чупи цитатите в подписани планове; един ред „затворен“ в главата им е достатъчен, ако Петър иска. |
| `origin/batch-ingest/2026-05-28`, `origin/ingest/issue-62` | 2 клона | — | — | — | Следи от май; `git push origin --delete` е push. | Петър ги трие с една команда или ги оставя като следи. |

Общо изтриваемо без риск за тест или гейт, ако Петър каже „да“ на целите папки: ≈ 9 MB в `scratch/audit_2026-09-03` + `scratch/kais_func_2026-09-03`, плюс ≈ 5 MB без препратка в `scratch/places_search`. Изтриването остава обратимо (`git show <sha>:<път>`), но е разрушителна операция по CLAUDE.md § Hard Rules 4 и се прави по подписан списък, не по преценка на агент.

## 5 · Дългове извън обхвата на този проход (за план, не за този клон)

1. **Н14** — `tests/address_slice_probe.mjs`: `process.exit` след асинхронен `stdout` реже отговора на Linux. Един ред (`process.exitCode = out.ok ? 0 : 1` и естествен край), но пробата е пинована от ADR 012 D7 и от тестовете на лотове 4в-Б/5 — иска ред в план и повторно пускане на харнеса на Windows и Linux.
2. **Н15, Н16** — статусните редове на `hotels_search_plan.md` (изместен от ADR 006) и ADR 002 (базовата карта е в репото, флагът е изключен): Петър решава думата, изпълнителят я пише.
3. **Н17** — трите коментара „спорно, виж `C:\git\plan.md`“ в `AGENTS.md`: ако файлът вече не съществува, коментарите да се махнат или да сочат репо.
4. `docs/plans/PARKED.md` P1–P3, P5 — непроменени; отпушването е по правилото в самия файл.
5. `web manifest` — все още 0; README и AGENTS.md вече не обещават инсталируем PWA, но ако Петър иска манифест, това е нов лот (ADR 002/005 го докосват).
