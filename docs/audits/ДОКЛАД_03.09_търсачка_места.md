# ДОКЛАД · Нощна смяна 02→03.09 — търсачката на живата карта (хотели), курортите, римските цифри, фаза 2

**Файл:** `docs/audits/ДОКЛАД_03.09_търсачка_места.md` · **Дата:** 02.09.2026 (следобед–вечер) · **Автор:** Fable (архитект) + Opus (изпълнители, мерещи агенти, одитор) · **Възложител:** Петър (промптът `varna_3d/docs/sessions/ПРОМПТ_нощна_смяна_търсачка_02.09.md`, подписан 02.09).
**Правилото на нощта:** нищо не се пушва; всичко е локално; ти пробваш → ти публикуваш; паднал гейт = стоп (спря три пъти — C0, C2, C4 — и трите пъти правилно).

## 0 · Присъдата в три реда

1. **Хотелите са в търсачката, локално, готови за пробата ти:** 8 комита във Fire_Varna (C0–C5 + C4-pre + C7), одит **ГОДНО**, 150 теста OK, адресната търсачка **байт-равна** преди/след (пробата в истински Chrome), първото зареждане непроменено. Чака Gate 2 + пробата (§3) + твоя push.
2. **Курортите и римските цифри — измерени докрай, планирани, НЕ строени** (по твоето условие „ако мярката е чиста"): Кими събори извора на курортите (после втора мярка с числа), Сол събори механиката на римските (четири блокера). Планове v1.1 с условията, за подпис.
3. **Три решения са само твои** (фаза 2): лицензът на регистрите, PII-филтърът, класът „общежития/социални грижи" (66 КАИС сгради) — §6.

## 1 · Заповедта — четирите искания и какво стана с всяко

| искане | какво стана | къде |
|---|---|---|
| „хотел адмирал" → и трите; хотелите професионално, нищо счупено | **Изпълнено локално** (C0–C5, одит C6 ГОДНО) — планът v1 → v2.6 след две атаки (Сол, Кими) и два recall-прогона (226/226) | `docs/plans/places_search_plan_2026-09-02.md` §0–§15, ADR 006, комитите по-долу |
| училища/университети/болници/градини | **Само план** (както подписа): фаза 2 v0 — мярката на фунията + трите решения | `docs/plans/places_phase2_plan.md` |
| Златни пясъци и Св. Константин като квартал | **Мярка ×2 + план-лот, НЕ изпълнено** — оградата от парцелите има дупки (преброени), К&Е има 5 компонента в Долна Трака, буферът ≠ 0 разлива към Чайка | `Varna_buildings/docs/plans/plan_quarters_resorts_2026-09-02.md` (§7 Кими, §8 втората мярка) |
| „VII училище" → 7-мо | **Мярка + план-лот, НЕ изпълнено** — правилото не бива да влиза в `skel` (1 872 адресни „и"); Сол събори v1 (4 блокера) | `varna_3d/docs/plans/ПЛАН_римски_цифри.md` (§4 Сол) |

## 2 · Търсачката на местата — как v1 стана v2.6 (всяка стъпка е измерена)

1. **v1 → v2 (Сол върна v1):** отделен клон `initPlacesSearch()` след `initAddressSearch()`, собствен контейнер под адресния, собствен кеш-helper, маркер, попъп — адресният код байт-непипнат (одитът: `initAddressSearch` sha равен от двете страни, 84 765 B).
2. **Recall-прогон 1** (Python реплика върху 226-те): 16/35 разминавания, 4 неоткриваеми (ГРАДИНА, ГАЛЕРИЯ ГРАФИТ, Ritsa, ПРЕСТИЖ ДЕЛУКС АКВАПАРК КЛУБ — ключ с празен клас гълта името) → **§10**.
3. **Кими** (данни): потвърди същите шевове + редът на чистенето, ДКЦ-чиповете, число-като-филтър, PII с изключение за институции, ODbL → **§11**.
4. **Сол** (окабеляване): Д6↔Д9 несъвместими; синтетичният Escape с възстановен текст възкресява закъснели заявки; скритият контейнер е `display:none` (`offsetTop` 0); кешът без SHA → **§12**: **след избор на място полето остава празно** (при адрес текстът остава, както досега) — ще го видиш; попъпът се самовъзстановява; SHA-256 на тялото срещу константа.
5. **Recall-прогон 2:** 31/36 → П2 (покритие на името преди близостта), П3 (числата конюнктивни), П4 (празният клас е и филтър), П5 („йо") → **226/226, 0/36, 10/10, детерминистичен** → **§13**.
6. **C2 спря честно (§14):** закованото `17800b5d`/83 008 беше CRLF-работно дърво; проследеният LF-blob (същите git-обекти и във varna_3d HEAD 9f55c08) е `72055444…`/78 601 — заковано е това, което браузърът тегли.
7. **C4-pre намери три неща извън цикъла** (за теб): тайминг-дефект в самото приложение — `panTo` по време на анимирания `setView` се отрязва до цели пиксели различно при всеки пуск (1–2 px, само по пътя „панел с детайли"); на localhost панелът чете `details/buildings/v1/{g}.json` (8 485 файла на тази машина) — пробата се снема на същата машина; headless Chrome 152 не изстрелва focus-събитие без focus-емулация — капан за всяка бъдеща проба.
8. **C4 спря на два гейта-правила (§15):** **G5 бюджетът — 563 реда / +33 110 B срещу ≤300 / ≤12 000 (×1.88 / ×2.76); таванът НЕ е вдигнат**, отклонението е в тялото на комита; първото зареждане не расте с нито байт заявка; флагът `HOTELS_SEARCH_ENABLED` е kill-switch; `git revert` го връща цял (доказано). **Алтернативата е твоя:** клонът във външен `js/places-search.js`, зареждан при първи фокус (прецедентът `varna_3d/web/poi-search.js`) — G5/G6 минават по конструкция, но променя Д4 и „app shell = inlined логика" в AGENTS.md → отделен лот. G12в — еталонът не носеше правилото Б1 (апарткомплекс → две думи) → прегенериран, „парк" = 12 точни + 7 размити.

**Комитите (локални, `main`, чакат Gate 2 + твоя push; бъндъл след всеки в `C:/git/plan_2026-09-01_files/backup/`):**

| # | хеш | какво | гейтове |
|---|---|---|---|
| C0 | `07ee724` | базова линия + байтов манифест (14 файла) | 126 теста; флаки-бележка |
| C1 | `9104ddf` | планът v2 (+§10–§13) + ADR 006 | mojibake 0 |
| C2 | `7dfde78` | `data/hotels.json` (78 601 B, `72055444…`), `data/place_categories.json` (36 203 B, `6bf5dd32…`) — OID-ите 4c5d72c4/b6950ace са СЪЩИТЕ обекти като във varna_3d | G1 |
| C3 | `cc7ea2b` | bundle-тест (17) + примитиви (5); трите нарочни повреди ЧЕРВЕНИ | 148 OK |
| C4-pre | `7cdd721` | CDP-пробата (865 реда), before.json ×2 байт-равен, G6 базата | детерминизъм |
| C4 | `60a13a7` | клонът на местата (563 реда) + after.json + g4.json + еталонът (962 реда) | G3 равен, G4/G12в 46/46, G12б 18/18, 149 OK |
| C5 | `0e563bd` | README BG+EN: хотелите, лицензният ред дословно ×2, офлайн-бележката + тестът за него | G9 тича и пада ×3, 150 OK |
| C7 | (този комит) | докладът, планът v2.6 (§14–§15 влизат в git — MEDIUM на одитора), фаза-2 план v0, ADR 006 (sha), activeContext | — |

**Одитът C6 (трети агент, само четене): ГОДНО.** Находки: MEDIUM — §14/§15 бяха само в работното дърво (C7 ги комитва); LOW-MEDIUM — C4 затегна собствения си гейт-тест (assert ≥1 → ==2; одиторът сам извади трите примитива: по 2 байт-равни копия); LOW — G5 над бюджета (записано, не вдигнато); INFO — G3 доказва байт-равенство за корпуса с 0 наши редове (при „адмирал" Д6 маха `.visible` от адресния контейнер, когато той е само „Няма съвпадения" — планирано); еталонът чете от `varna_3d` (свеж клонинг не го прегенерира); отказът на доставката е тих (един `console.warn`); „хидрантите пренаредени" е доказано слабо (0 → 5) — за очите ти.

## 3 · Пробата ти (копирай дословно; `python -m http.server 8000 --directory C:\git\Fire_Varna` → http://127.0.0.1:8000/ в чист профил / изчистен кеш; sw.js не е пипан)

| напиши | трябва да видиш |
|---|---|
| `адмирал` | адресните редове „Адмирал Грейг" отгоре КАКТО ДОСЕГА; под тях хедър **Хотели** и три реда: АДМИРАЛ · к.к. Златни пясъци, Адмирал · к.к. Св. Константин, АМИРАЛ · район Приморски (най-близкият до кадъра първи; АМИРАЛ е така в регистъра) |
| `хотел адмирал` · `хотел адмиралл` · `хотел амирал` | същите три |
| `адмирал златни` · `хотел адмирал златни пясъци` | АДМИРАЛ (Златни) първи, общо три |
| `хотели` · `хотел` · `хотелите` | категорийният списък: най-близките 8 до центъра на кадъра + „…и още 218" |
| `хотел златни` | 85-те хотела в Златни пясъци, най-близките първи |
| `семеен хотел` | само семейните (50) |
| `берлин голдън бийч` · `лти берлин` · `lti` | БЕРЛИН ГОЛДЪН БИЙЧ първи (старо: LTI Берлин ГБ) |
| `роял` / `royal` | РОЯЛ (Одесос), РОЯЛ (Златни), после Royal Beach, после размита опашка |
| `синчец` · `хотел синчец` | ДАНА ПАЛАС · старо: Синчец |
| `русалка` | РУСАЛКА (Виница/север), Русалка (Св. Константин) · бивш |
| `бонита` / `bonita` · `хелиос спа` / `спа хелиос` · `градина` · `йо` | БОНИТА/BONITA · ХОТЕЛ ХЕЛИОС СПА · хотел ГРАДИНА · хотел Йо |
| `парк` | адресните (Аспарухов/Приморски парк) отгоре както досега; отдолу 12 хотела с „ПАРК" + 7 размити накрая („…и още 11") |
| клик на хотел | оранжев пин, хидрантите („Топ 5") пренаредени около хотела, попъп: име · вид ★ легла · зона · старо име · **ред-източник** · Кола/Пеша/Карта/Street View; **полето остава празно** (избор на място = чисто поле) |
| Enter върху `lti` (само наши редове) | избира БЕРЛИН ГОЛДЪН БИЙЧ; при адресни редове Enter е адресният, както досега |
| Escape / празно поле / клик на адрес | всичко наше изчезва |
| **контролите, че нищо не е мръднало:** `бл. 402 вх. 3` · `макгахан 15` · `макгахан 153` · `кв владиславово бл 402` · `бл. 402` · `чайка` · `виница` · `43.2100, 27.9100` · `ьььь` | същите редове, същите пинове/попъпи/панели (пробата ги сравни байт по байт: `scratch/places_search/probe_out/before.json` ≡ `after.json`, 75 клика с backing identity) |
| 375px: трите режима на хидрантите, докладът, follow | непроменени (G11 — твоите очи); нашият списък се побира в екрана при „парк" (мерено) |

**Формулировки за твоя подпис при Gate 2** (не са измислени, но не са и подписвани дословно): ред-източник за 22-та записа със `src` „Sol/OSM идентификация": `име: публична идентификация (OSM, официални сайтове, общински регистри) · координати: КАИС Отворени данни (АГКК)` (дословно от лицензния ред); `aria-label="Хотели"`; README-изреченията на C5 (в тялото на `0e563bd` и в README редове 23–30/78/202); „избор на място = празно поле" (§12 В1); отказът на доставката е тих (Д2).

## 4 · Курортите като квартал — измерено докрай, НЕ строено

- **Изворът е налице:** подписаният регистър вече носи `zlatni_pyasatsi` и `kk_konstantin_elena`; оградите от КАИС парцелите (303 и 261) съществуват; дупката е само в токените (`qtk`) — лотът е обобщение на Р38-клона (Виница) в проекцията.
- **Кими събори v1; втората мярка отговори с числа:** дупки на оградата 12 (Златни) / 44 (К&Е) парцела; „display-ът не е в alias-списъка" — вярно, следствие 0; истинската дупка при К&Е са 13 парцела с три изписвания (+71 154 m², +22 записа) → в регистъра преди оградата; фалшиви отрицателни на `d`≠Приморски: **0**; разливане към Чайка при 0 m: **0** (сечение 0 m²), но при буфер 50/100/200 m Златни поглъща 5/12/20 чаикенски реда, К&Е 5/13/34 → **0 m е единственият безопасен буфер**; **5 компонента на К&Е (20 парцела, 20 записа) са в Долна Трака**, 390–1 717 m западно от ядрото; display-префиксът: Златни 0 реда, К&Е 68 (66 смесени с латински улични етикети) → само токен.
- **Твоите решения (Gate 1):** В-1 буфер (0 m) · В-2 К&Е = геометрия ∧ самодоказателство или изключени траки-компоненти · В-3 без display-префикс · В-4 13-те парцела в alias-списъка (нов SHA256) като предпоставка. После: Сол върху проекцията → изпълнение → staging → publish-гейт → Fire-цикъл (нов индекс) → G3-пробата наново.

## 5 · Римските цифри — измерено, планирано, НЕ строено

- **Мярката:** 11/370 poi + 3/576 места + 8 в регистъра на МОН; `skel` свива I=II=III→`i`, VII=VIII→`vi`; кирилското І (U+0406) не се среща с латинското i; **в адресния индекс токен `i` = 1 900 записа, 1 872 от тях са съюзът „и"** → правилото не бива да влиза в `skel/norm/foldTokens` (заявка „1" би върнала ≈5 825 вместо 3 953 адреса). В именния канал, позиционно пред тип-токен: 9 poi + 3 места, „1" от 5 на 9 реда от 946. Два от четирите „работещи" случая днес работят по случайност (стар OSM-псевдоним).
- **Сол — четири блокера:** `web/varna_poi_names.json` е untracked/ignored (обещаният комит не съществува → генератор + QA със schema 4); `place_aliases.json` се прегенерира от твърдо кодиран списък; `tokens()` в `poi-search.js` е общ за индекс и заявка (правилото — само в заявката, иначе „и ег" от 2 → 0); **Enter в 3D-то избира адрес пред обект при частични адресни попадения** („7 су" → „borovets 7") — твое правило от 20.08, за преразглеждане; гейтът беше fail-open.
- В Fire_Varna клиентът вече знае VII/седмо/7-мо/І → 7 (§3 Т1, G12б 18/18) — за фаза 2.

## 6 · Фаза 2 — трите решения, които са твои (планът v0)

1. **Лицензът на регистрите:** правило 3 (19.08) забранява показване на нелицензирани регистри, а 35 регистрови имена (ИАМН/МОН) вече са в `places.json`; ФАЗА_0 мълчи за трите регистъра. Същият ред-образец „отделни факти" като НТР — ДА/НЕ?
2. **PII:** 48 кандидата в OSM-имената (лични практики на жилищна сграда); институциите, кръстени на личност, остават; нов гейт над двата файла.
3. **Нощно население:** 66 КАИС сгради „Общежитие"/„Заведение за социални грижи" (62 без име) = лицензно чист клас — влиза ли като чип?
Фунията: образование 152 → 76 → 8 (68 вече изпечени под OSM име); здраве 120 → 27 → 27 (+25 еднозначни жилищни като резерва с PII-риск); университети — без източник (мини-цикъл от нула).

## 7 · Здраве, инциденти, какво чака

- **Репота:** Fire_Varna `fsck --no-dangling --no-reflogs` чист след всеки комит; `gc.auto=0`; никакъв repack/reflog-чистене (по думата ти); 8 бъндъла в `plan_2026-09-01_files/backup/`. varna_3d/Varna_buildings — само нови планови документи + мерки (собствени комити).
- **Флаки тест (предсъществуващ):** `tests/test_etr_kmz_adapter.py::DeterminismTest::test_two_runs_byte_identical` пада периодично — фикстурата `write_kmz` щампова локалното време в ZIP-хедъра (2 s), адаптерът хешира KMZ-ите. Поправката е един ред (фиксиран `date_time` в `ZipInfo`) — за твой подпис. Тази нощ падна веднъж (C0), после мина 6 пъти.
- **Инцидент (мой):** при sanity-проверка на headless Chrome (14:3x) убих `chrome.exe` по име на процес, не по PID — ако си имал отворен Chrome, той е бил затворен. Правилото „само по PID" е вписано за изпълнителите и спазено от пробата.
- **Дефект в приложението, намерен от пробата:** `renderDetailSheet → positionPinInBand → map.panTo` по време на анимирания `setView` се отрязва до цели пиксели различно при всеки пуск (1–2 px) — извън всеки write-set, чака подпис.
- **Известни граници (казани, не скрити):** зона без ключ не намира („морска градина" → 0 наши реда; „хотел морска градина" → 2); „Йо" се намира и с голо „йо" (П5); координатният „стар ред" при Enter е тяхно предсъществуващо поведение; двата overlay-а могат да се припокрият за един observer-tick при hotel-first.
- **Чака теб:** Gate 2 върху диффовете (C1–C5 + C7) · пробата (§3) · push · отговорите: §3 формулировките, G5 (приемаш 563/33 KB или външен файл), курортите В-1..В-4, римските (Enter-правилото), фаза 2 (1)–(3), флаки-фикстурата, panTo-дефектът.

## 8 · Извадка „цъкни тук → трябва да видиш" (протоколът ти)

| напиши и цъкни | трябва да видиш |
|---|---|
| `хотел адмирал` → **АДМИРАЛ · к.к. Златни пясъци** | оранжев пин в Златни, попъп „АДМИРАЛ · Хотел · 600 легла · к.к. Златни пясъци · име: Национален туристически регистър · координати: КАИС Отворени данни (АГКК)"; хидрантите около него |
| `хотел адмирал` → **Адмирал · к.к. Св. Константин** | „Адмирал · Хотел · ★2 · 196 легла · к.к. Св. Константин" |
| `хотел адмирал` → **АМИРАЛ · район Приморски** | „АМИРАЛ · Хотел · ★4 · 100 легла · район Приморски" — регистърът го пише така (не е грешка на картата) |
| `синчец` → **ДАНА ПАЛАС** | заглавие ДАНА ПАЛАС, ред „старо име: Синчец" |
| `русалка` → **Русалка · к.к. Св. Константин · бивш** | заглавие „Русалка (бивш)" |
| `lti` → **БЕРЛИН ГОЛДЪН БИЙЧ** | „старо име: LTI Берлин ГБ"; Enter върху този ред го избира |
| `bonita` → **БОНИТА/BONITA** | двуезичното име, к.к. Златни пясъци; ред-източник „публична идентификация (OSM, официални сайтове, общински регистри)" при записите със `src` Sol/OSM |

*Съставено 02.09.2026 вечерта. Нищо не е пушнато. Компютърът е изключен по думата ти след записа на този доклад, паметта и бъндълите.*
