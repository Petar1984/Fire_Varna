# СУТРИН 08.09 · Б3 — какво е готово и как продължаваш

Състояние към края на нощта (07→08.09): планът Б3 v3 + амандамент 1 са изпълнени по §4 **стъпки 0–7**, одитирани **ГОДНО** (нула блокери), прегледани от Кими (К30) и Astra (S30) — виж записите в `scratch/places_search/`. **Нищо не е комитнато и нищо не е публикувано.** В `C:\git\Fire_Varna` са стейджнати точно 13 пътя (трите файла с данни, `index.html`, двата bundle теста, харнесът и фикстурите 07.09, Node-сондата, `gates/probe/b3_g11.py`, `tests/test_b3_gates.py`, двигателят), а два проследени файла са мръсни и НЕ стейджнати по замисъл (`probe_out/token_parity.json`, `lot1v_v_reference_manifest.json` → комит 5).

Пътят до пробите има пет твои действия. Всяко е една-две команди в PowerShell (`cd C:\git\Fire_Varna` първо; по една команда на ред, без `&&`).

## 1 · Подпис на амандамент 2 (само делтата)
Файл: `docs/plans/ПЛАН_Б3_07.09_амандамент_2.md`. Четири механични реда: А2-1 второто копие на името на кеша в сондата `probe_places_fv.mjs` → v7; А2-2 `ALLOWED_NEW` + `tests/test_b3_gates.py`; А2-3 манифестът на референцията пътува с комит 5; А2-4 гейтът G11 приема `--cached`. Нищо от 17-те решения не се пипа.
**Ти:** пишеш ми „подписвам амандамент 2“. **Аз:** изпълнител прилага А2-1/А2-2 (два реда) и ги стейджва, одитор ги сверява (~10 минути).

## 2 · Атомният комит 2 (твоята ръка)
```powershell
git status --short
```
Очакваш: 13 реда със стейдж (`M `/`A `/`D `; ще станат 15 след прилагането на амандамент 2 — `probe_places_fv.mjs` и `b3_g11.py`) и два реда ` M` (незастейджнати: `token_parity.json`, `lot1v_v_reference_manifest.json`). Ако видиш друго — спри и ми кажи.
```powershell
git diff --cached --stat
```
```powershell
git commit -F scratch\places_search\COMMIT_MSG_B3.txt
```
```powershell
git log -1 --pretty='%h %an <%ae> · %cn <%ce> · %s'
```
Последният ред трябва да показва **Petar1984** и като автор, и като комитър.

## 3 · „давай“ → стъпки 9–11 (агенти, ~30–40 минути)
Червеният набор се записва; ЕДИН прогон на очакванията (`--manifest`); опашката за подпис `ЗА_ПОДПИС_Б3_07.09.md`, старата опашка се преименува в `архив/АРХИВ_A_05.09.md`, новият allow-файл (138 реда), старият се маха. Един агентски комит (`docs: queue and manifests for the Б3 delivery signatures (07.09)`) + одитор. Ще ти дам точния идентификатор за подписа.

## 4 · Подписите (твоята ръка)
```powershell
python -m gates.sign <ид> да --all da --queue scratch\places_search\ЗА_ПОДПИС_Б3_07.09.md
```
(точната команда ти я давам след стъпка 3, с реалния `<ид>`; писалката отпечатва комита, който правиш). После **12б**: изпълнителят слага низа за границите в подножието на картата, ти комитваш: `feat(map): signed quarter-boundary line in the map attribution (decision 8)`.

## 5 · „давай“ → замразяване (стъпка 13, агент) и твоят комит 5
```powershell
git add scratch\places_search\recall_sweep_rows.json scratch\places_search\expectations.json scratch\places_search\probe_out\token_parity.json scratch\places_search\lot1v_v_reference_manifest.json
```
```powershell
git commit -m "freeze: reference + expectations + parity (Б3)"
```
После агентите пускат пълния гейт (§5) и одитора. При зелено — пробите.

## 6 · Пробите (§8), с числата по подписания вариант Б
```powershell
python -m http.server 8000
```
Отваряш `http://localhost:8000/index.html`, ширина 375 px, конзолата отворена, Hard reload (кешът е v7). Конзолата трябва да е чиста, без реда „legacy_by_row: друг пакет…“.

| # | пишеш | очакваш |
|---|---|---|
| 1 | `Кайсиева градина` | в нашата секция няма квартални редове (3 реда по адрес, M2-failopen); адресната търсачка отгоре: 9 реда. Нито едно място не е в полигона. |
| 2 | `училище Възраждане 3` | 2 реда ПО ИМЕТО (3 ОУ Ангел Кънчев [кв. Левски], III ПМГ [кв. Тракия]) — шум по името, не квартал. |
| 3 | `възраждане`, после `възраждане 2` | 5 реда (родител + Възраждане 1/2), после 0. Детската заявка не намира детето — подписано (О16). |
| 4 | `левски`, после `кв. Левски` | 14 реда (12 с кв. Левски) и **пак 14** — вариант Б работи. Същото: `кв. Тракия` 4, `кв. Христо Ботев` 4, `ж.к. Св. Иван Рилски` 4. |
| 5 | `бриз` | 4 реда: Жолио Кюри и Менделеев (двата нови, ж.к. Бриз), Камелия, ПАРК ХОТЕЛ БРИЗ (по име). |
| 6 | `чайка` | 30 реда, смесени к.к. Чайка + кв. Чайка (О16). |
| 7 | `аспарухово`, после `дружба` | 16 реда без ЦДГ 43 „Пинокио“; после 2. |
| 8 | `Никея` → картончето | ред „вид · к.к. Чайка · район Приморски“; НЯМА ред за извор; долу вляво до © OpenStreetMap стои низът за границите (след 12б). |
| 9 | `училище район Младост`, после `младост` | 12, после 20 (осемте училища и градини със собствен квартал са напуснали голото „младост“, подписано). |
| 10 | надписът при 375 px | бутоните `+`/`−` и локацията остават видими и натискаеми при трите базови карти. Ако не са — казваш „къс низ“ (§9 т. 7). |

## 7 · След пробите
Ако всичко е както в таблицата: публикуването е твоята ръка (§4 стъпка 17: първо `git rebase --autostash origin/main` от интерактивен терминал, после публикуваш `main`). После аз затварям документите (стъпка 18) и ти правиш второ публикуване.

## Ако нещо е различно
Спираш и ми пишеш какво виждаш. Никой агент не комитва данни, не подписва и не публикува — тези три остават твои.
