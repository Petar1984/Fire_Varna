# PARKED — обхватът е замразен, докато Фаза A не е пушната

Правило §0.1 на `ПЛАН_ИЗЧИСТВАНЕ_v2_05.09.md` (подписан от Петър 05.09 02:50): **един лот в движение.** Нищо ново не се отваря, докато текущият не е пушнат. Идея, находка или предложение, което не е Фаза A → ЕДИН ред тук, с връзка към документа, който вече го описва. Нищо не се планира, не се измерва и не се кодира по паркиран ред, преди Петър да отпуши обхвата.

**Текущият лот в движение:** Фаза A — F12 (доставката P7 → `data/`, М7, двата манифеста, allow-файлът от 150 реда). Спрямо `origin/main` това са комитите след `6da5d9a`; отпушването е неговият push.

| # | Паркирано | Къде е описано | Защо чака |
|---|---|---|---|
| P1 | ЛОТ „OSM-hints“ v2 §7 — OSM точките като кандидат-доказателство за квартал/местност | `docs/plans/ПЛАН_ЛОТ_OSM-hints.md` (§7 = минималната v2 след атаките на Кими и Astra) | Нов източник на доказателство върху данни, чиято доставка още не е пушната; §0.1. Подписът на Петър („действаме според препоръките ти“) е цитиран непълно в §3 на плана — иска се изричен Gate 1 преди изпълнение. |
| P2 | ЛОТ 1г „Телата“ — къде стои пинът и какво стои зад записа (62 червени + 79 жълти по борда на истинността) | `docs/plans/ПЛАН_ЛОТ1г_телата.md` (замразен по собствения си §6.1 / S8) | Собственото му S8: референцията на лот В още не е замразена, износите пишат стенен часовник, публичната схема няма `place_id`/тяло/етаж (иска ADR 009, Фаза B–C). |
| P3 | ИДЕЯ „Златният списък“ — остатъкът след §10 | `docs/plans/ИДЕЯ_златен_списък.md` §9 (Opus-панелът обори идеята като изход) и §10 (минималната v2, чиято част „12 override по `place_id`“ вече е в подписаните факти) | Оборена като изход от кръга; остатъкът е идея, не лот. Влиза наново само ако Петър я поиска след Фаза D. |
| P4 | E0 — числата на `scratch/basemap_e0` (прогонът, който `git reset --hard` изтри на 05.09) | `docs/plans/ПЛАН_ИЗЧИСТВАНЕ_v2_амандамент_2_05.09.md` §5 (щетата и правилото) · въпрос Q10 на опашка A | **НЕ чака — ЗАТВОРЕНО. Петър, 05.09: не ни трябват — не се премерва.** Редът стои, за да не се отваря наново; файловете остават на комитнатото си съдържание (`048e404`). |

## Как се вади ред оттук

1. Фаза A е пушната (Петър).
2. Петър казва кой ред се отпушва.
3. Планиращият агент пише датиран план за него (нов файл, не редакция на този), Кими и Astra го оборват, Петър подписва.
4. Редът се маха от таблицата в комита, който открива лота.
