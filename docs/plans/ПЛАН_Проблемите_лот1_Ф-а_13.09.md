# ПЛАН · „Проблемите“ — **Лот 1 · Един стандарт за всяка форма при хидрант** (бъг 1 + бъг 2 на формата: #769, #716, #731)

> **ПОДПИСАН от Петър, 13.09.2026 (Gate 1)** — след локален тест на прегледа на порт 8001; думите му по ред: „искам да е със звездичка“ (Р1 = задължителен), „искам всички да са един стандарт“ и „добави ги“ (обхватът расте до трите форми + ингеста), „разбрах- давай да вкарваме поправката в живата карта“ (подписът). Ревизия **v6**: v5 описваше само бъг 1 с осем решения; v6 записва решенията както са взети и обхвата както е тестван. Изпълнителят не пише в този файл.

`docs/plans/ПЛАН_Проблемите_лот1_Ф-а_13.09.md` · **13.09.2026** · база **8512ab6** (= `origin/main` = `main` = merge-base, измерено 13.09) · дърво `C:/git/_wt/fv_problems`, клон `problems` · верига: **Планировчик → Gate 1 (Петър ✔) → Изпълнител → Одитор → Кими → Gate 2 (Петър) → Петър пушва**.

**Клас на риска: 🔴 АРХИТЕКТУРЕН.** Спусъци: гейт-файл се редактира (`gates/probe/b3_g11.py`, `ALLOWED_NEW`); подписаният договор от 09.05 се изменя с амандамент (ратифицираният ред „Hide completed pickers in `exists_confirmed`“ и правилото „`wrong_location`: no type or operational fields“); нов UI надпис; сменя се какво носи полеви доклад **и как ингестът го прилага** (`scripts/lib/hydrant_core.py`, два обработчика). **Не са спуснати:** нула байта в `data/`, нула публикуване/флаг, нула нов извор, нула кросрепо, нула промяна по Worker-а и `sw.js`.

**Работна директория за всяка команда без `-C`: `C:/git/_wt/fv_problems`** (`gates.run_gates` е пакетен внос, `-s tests` е относителен път, `gates/probe/*.py` викат `git diff` в текущата директория). За основното дърво — `-C C:/git/Fire_Varna`; **python няма `-C`**.

---

## 0 · Червени редове

1. **Агент никога не пуска `git push`** (никакъв флаг, никакво `--no-verify`). Подагентите са на Opus. Куката не се пуска от това дърво — тя тича при пуша от основното дърво.
2. **Ръцете:** код и тестове → `Claude Executor`; документи (планът, амандаментът, `docs/activeContext.md`) → `Claude Architect`; данни и подписани тела → **само Петър**. Периметърът се проверява **машинно** (Г15, Г16).
3. **Приватност в проследени файлове:** нула координати (точка или запетая), нула имена на докладчици („докладчикът на #769“), нула кадастрални номера, нула imot провенанс, нула сурови 64-знакови дайджести, нула дълбок линк към хидрант. Всеки НОВ проследен път иска ред в `ALLOWED_NEW` в **същия** комит.
4. **Гейт = команда с очакван изход + отрицателна половина, която ТИЧА и ПАДА.** Тежките прогони — един след друг.
5. **Принципът за триенето** беше поставен на Gate 1 и Петър го реши изрично: въпросът е **задължителен** за всяка форма (Р1(б)); „не съм проверявал“ остава валиден отговор.
6. **UI текст = подпис (Р2).** Текущата стойност е ЧЕТИМ надпис над **НЕИЗБРАН** ред.
7. **Един отворен лот.** Subject: английски, ≤ 72 знака; `git add` само с явни пътеки.
8. **Браузърната проверка НЕ подава истински доклад** (ражда публично GitHub issue) и не влиза в офлайн режим. Доказателството е DOM-ово + headless.
9. **Червен гейт с причина извън лота → STOP и питай.**
10. **Обхватът е точно дифът, който Петър тества на 13.09** (§5). Нищо повече.
11. **`docs/moderation_log.md` не се пипа тук**; #716/#731/#769 ги затваря темата за модерация със запис „поправено в <комит>“.

---

## 1 · Думата на Петър (дословно)

Промптът на темата и беклогът са **непроследени файлове в основното дърво** (`C:/git/Fire_Varna/scratch/ПРОМПТ_нова_тема_проблемите_12.09.md`, `…/БЕКЛОГ_Fire_Varna_12.09.md`).

- Промптът: „**Лот 1 · Ф-а — въпросът „Работи ли?“ се показва винаги** (бъг 1 на формата; докладван в #769)“; „**без предварителен избор на състоянието** … текущата стойност се показва като четим надпис над реда, през `OPERATIONAL_LABELS`“.
- Докладчикът на #769 (цикъл #35 на дневника): *„на този и преди смяната на локацията не ми дава да отбележа дали работи или не“* — бъг 1 и бъг 2 в едно изречение.
- Петър, 13.09, при локалния тест: „защо Работи ли е без звездичка? искам да е със звездичка“ · „не ме интересува хидранта от № 769 — искам всички да са един стандарт“ · „добави ги“ (същият блок в „Грешна локация“ и „Повреден“ + шестте реда в ингеста) · „разбрах- давай да вкарваме поправката в живата карта“.

---

## 2 · Мярката на базата (13.09.2026 при 8512ab6, свързаното дърво)

| Мярка | Стойност | Команда |
|---|---|---|
| база и клон | `problems`; `HEAD` = `origin/main` = `main` = merge-base = `8512ab6` | `git rev-parse --short HEAD origin/main main` · `git merge-base origin/main HEAD` |
| пакет | **Ran 372 · OK · 122 s**; шум: `ResourceWarning` от `scratch/places_search/recall_sweep.py:258-260` (H-1, лот 2) | `python -m unittest discover -s tests` |
| пакет **след** промяната в ингеста (прегледът) | **Ran 372 · OK · 128 s** — нула регресии, паритетният тест и златният еталон остават зелени | същата команда |
| гейтове | **изход 0 · 8 s · 7/7 зелени** (release: референция 203/3160 = кандидат 203/3160, делти 0); гейт 6 приема `expectations.json` след подписа, защото последният комит по него е на `Petar1984` (ИБ-2) | `python -m gates.run_gates` |
| проби | B3: изход 0 · `{"scope": 19, "geo_hits": [], "unreadable": [], "stray_new": []}` · ИБ1-Г15 без операнди: изход 2 по конструкция | `PYTHONIOENCODING=utf-8 python gates/probe/b3_g11.py 8512ab6` · `python gates/probe/ib1_g15.py` |
| записи | **7403**: `(None,None) 6684` · `(verified,not_tested) 537` · `(verified,None) 56` · `(verified,works) 111` · `(verified,not_working) 15` → `verified` 719, със състояние **663**; `review_status` null навсякъде; с `type` 2769 | брояч по двете полета в `data/hydrants.json` |
| размери | `index.html` **579399 B** · `data/hydrants.json` **1322968 B** · първо зареждане 1902367 B при таван 5242880 B | `os.path.getsize` |
| непроследени · стешове | основно дърво **39**, свързано **0** преди лота · стешове **2**, непипнати | `git ls-files --others --exclude-standard` · `git stash list` |
| кука · общ конфиг | `scripts/hooks/pre-push` ↔ `.git/hooks/pre-push` байт-идентични (2574 B) · `core.bare = false`, `.git/config` непроменен след прогоните | байтова сверка · `git config --get core.bare` |
| идентичности | `Claude Architect <architect@local>` · `Claude Executor <executor@local>` · `Petar1984`; `user.name = Petar1984` в общия конфиг | `git log -40 --format='%an <%ae>'` |
| launch config | `.claude/` е игнориран; Browser pane-ът чете `C:/git/.claude/launch.json` (root `C:/git`), не дървото — конфигурацията `fire-varna-problems-8001` е там (`--directory C:/git/_wt/fv_problems`) | `git check-ignore -v .claude/launch.json` |

**Находки по прочит.** (1) `b3_g11.py` брои всеки ДОБАВЕН път (`changed()` `:99-104`, `:137`) → всеки нов файл иска ред; преименувания не влизат — беклог лот 5. (2) IIFE `:1804`–`:7821` → payload-ът се съди headless. (3) Договорът ратифицира скриването (`:46`, `:162`) и казва „no type or operational fields“ за `wrong_location` (`:124`) → амандамент. (4) Ингестът пише състояние само при позната стойност (`if rop in CANONICAL_OPERATIONAL`, `hydrant_core.py:329-331`) → `null` се игнорира по всички пътища (ингест, `reportTypeToSemanticPatch`, полинг). (5) `not_tested` Е канонична → посоката надолу е записваема (111 + 15 записа); защитата е модерацията (`apply_approved_reports.py:69`) → Р7. (6) `apply_wrong_location` (`:505-545`) не четеше `type`/`operational_status` → формата би пращала полета, които ингестът изхвърля → шестте реда СЛЕД re-key блока. (7) Формата „Повреден“ предизбираше „не съм проверявал“ без валидация → тих downgrade (R-01). (8) `docs/activeContext.md:39` декларира 7407 при измерени 7403 (D-52, лот 2). (9) `tests/test_b3_gates.py` не съществува → D-26 нищожно.

---

## 3 · Диагнозата (по ИМЕ на функция; редовете са котви при 8512ab6)

- **Бъг 1.** `typeFieldsHTML` (`:2862`): `const showOpPicker = !(h && h.operational_status);` (`:2870`) криеше пикера за всеки запис със стойност, включително `not_tested`; огледалото `opVisible` в `onSubmitClicked` (`:3099`). Отговорът вече се събираше (`readDraftFromForm` `:3003`, `buildReportObject` `:3169-3172`) — липсваше само излъчването.
- **Бъг 2.** Клонът `wrong_location` на `typeFieldsHTML` (`:2930-2937`) връщаше само описание; `buildReportObject` пращаше `type: null`, `operational_status: null` за него (`:3176-3183`); `apply_wrong_location` не ги четеше. Клонът `damaged` (`:2938-2949`) нямаше тип и предизбираше „не съм проверявал“ (`showReportModal` `:2988-2990`), непроверено във валидацията (`:3081-3084`).
- **Речниците.** Записът пази `works/not_working/not_tested`; пикерът показва `да/не/не съм проверявал`; `radioRowHTML` слага `selected` само при точно съвпадение → предизбор на състоянието е невъзможен и нежелан (датирано наблюдение); надписът минава през `OPERATIONAL_LABELS` (`:1822-1826`). Типът е в речника на пикера → предизбира се.
- **Клиентският път** (`reportTypeToSemanticPatch` `:3888`, `hydrantStatusClass` `:1929`): всеки доклад дава жълт пин веднага; цветът се сменя чак след модерация + ингест + билд.

---

## 4 · Решенията — както са подписани на 13.09

| № | Решение | Подписано |
|---|---|---|
| Р1 | задължителен ли е въпросът „Работи ли?“ | **(б) задължителен за всяка форма** („искам да е със звездичка“) |
| Р2 | текстът на надписа | **(а)** „сега: “ + `OPERATIONAL_LABELS[value]` с главна буква, 1:1 от кода |
| Р3 | договорът от 09.05 | **(а)** отделен датиран амандамент `docs/audits/амандамент_подаване_13.09.md` + ред-указател под ратификационната таблица; таблицата непокътната |
| Р4 | планът като проследен файл | **(а)** Архитектът го комитва (К1); редът в `ALLOWED_NEW` идва с К3; между К1 и К3 Г11 е червен по конструкция |
| Р5 | отрицателните половини | **(а)** самостоятелни тестове в новия файл; манифестът (len == 23) не се пипа |
| Р6 | редът в `docs/activeContext.md` | **(а)** след `merge --ff-only`, преди пуша, в отделен блок |
| Р7 | посоката надолу | **(i)** приема се; защитата е модерацията; записано в амандамента §6 |
| Р8 | launch config | Изпълнителят/Архитектът — конфигурацията стои в `C:/git/.claude/launch.json` (игнориран за всяко репо), не в дървото |
| **Р9** | **един стандарт** | **същият блок „Тип + Работи ли?“ в „Грешна локация“ и „Повреден“; „Повреден“ получава избор на тип; ингестът прилага двете полета и за двата типа** („добави ги“) |

---

## 5 · Стъпките (ръцете са в §7)

**Спецификацията на кода е дифът, който Петър тества на 13.09** — вече в работното дърво (`git diff -- index.html scripts/lib/hydrant_core.py`, 110 + 14 реда). Изпълнителят не го пренаписва; той го **сверява** с този раздел, добавя теста и `ALLOWED_NEW`, и комитва.

**К1 · Планът (Architect).** Този файл.

**К2 · Амандаментът (Architect).** `docs/audits/амандамент_подаване_13.09.md` (нов, на английски — езикът на договора) + един ред-указател под ратификационната таблица в `docs/audits/backfill_and_submission_extension_plan_20260509.md` (таблицата байт за байт).

**К3 · Кодът и тестът (Executor). Четири файла, един комит.**

**К3.1 · `index.html`** — какво носи дифът:
- `const AT_HYDRANT_TYPES = new Set(['exists_confirmed', 'wrong_location', 'damaged']);` и `function hydrantPickersHTML(d)` (преди `typeFieldsHTML`): блокът „Тип хидрант * (потвърди или поправи)“ (`radioRowHTML('hydrant_type', …, d.hydrant_type)`) + „Работи ли? *“ с `hasOp ? '<div class="hint">сега: ' + escapeHtml(knownOp) + '</div>' : ''` над `radioRowHTML('operational', ['да','не','не съм проверявал'], d.operational)` и `.err` винаги; `knownOp = OPERATIONAL_LABELS[h.operational_status] || h.operational_status`.
- `typeFieldsHTML`: `exists_confirmed` = блокът + бележката; `wrong_location` = блокът + описанието; `damaged` = блокът + описанието на повредата (старият неозаглавен ред „Работи ли?“ пада). `missing`, `new_hydrant` — без промяна.
- `showReportModal`: `hydrant_type` се предизбира от `hydrant.type` за `AT_HYDRANT_TYPES`; `operational` е винаги `''` (подразбирането „не съм проверявал“ за `damaged` пада).
- `onSubmitClicked`: един блок `if (AT_HYDRANT_TYPES.has(reportType) || reportType === 'new_hydrant')` иска `hydrant_type` и `operational` (старият отделен блок за `new_hydrant` и старият `exists_confirmed` блок с `opVisible` падат); коментарът „whichever pickers are visible“ пада.
- `buildReportObject`: `type` и `operational_status` се излъчват за `AT_HYDRANT_TYPES` + `new_hydrant`.
- `reportTypeToSemanticPatch`: `type` и `operational_status` се сливат за `AT_HYDRANT_TYPES`.
- Нула промяна другаде: `readDraftFromForm`, `wireFormHandlers`, `locationFieldHTML`, `buildReportYAML` (редът на ключовете вече носи двете полета), `buildIssueBody`, `labelsForType`, `dedupHash`, опашката, `applyReports`, `hydrantStatusClass`, CSS, двете търсачки.

**К3.2 · `scripts/lib/hydrant_core.py`** — `apply_wrong_location`: след `diff_set(rec, "existence_status", "verified", …)` (тоест СЛЕД re-key блока) — `rtype`/`rop` с проверка в `CANONICAL_TYPES`/`CANONICAL_OPERATIONAL` → `diff_set`; `apply_damaged`: същите редове за `type` (за `operational_status` вече имаше). Нищо друго.

**К3.3 · Нов тест `tests/test_report_form_operational.py`** (Executor; кирилица само в низове; нула четене и нула git на модулно ниво — ИБ1-Г13 копира `tests/` в голо дърво и тича `discover`, провалите там трябва да са назовани).
- **Харнес:** `tests/granitsi_client_probe.mjs` (в `ALLOWED_NEW`; STDIN `{slice, exports, asks}`, `{ask:"call", name, args}` → изход `{ok, answers}`), държан като подпроцес — образец `tests/test_address_quarter_render.py`. Нов `.mjs` не се създава.
- **Срезът се реже по ИМЕ:** `block(text, anchor)` — първият ред, чийто `.strip()` започва с котвата; `text.count(anchor) == 1`; `depth += line.count('{') - line.count('}')`; спира на първия ред с `depth == 0` и завършек `;` или `}`. Котви на кандидата: `const OPERATIONAL_LABELS = {` · `function escapeHtml(s) {` · `function radioRowHTML(field, options, selected) {` · `const NOTE_HINT_HTML =` · `const AT_HYDRANT_TYPES =` · `function hydrantPickersHTML(d) {` · `function typeFieldsHTML(t, d) {`; за БАЗАТА последните две липсват по конструкция (само там са незадължителни). Маркер в продукционния код = STOP.
- **Базата е литерал със свое име:** `BASE = os.environ.get("FIRE_VARNA_LOT1_BASE") or "8512ab6"` (не merge-base, не `FIRE_VARNA_BASE_COMMIT`); `git show <BASE>:index.html` неразрешим → `self.fail`; блобът == работния файл → `self.fail("base equals candidate")`. `INDEX = pathlib.Path(os.environ.get("FIRE_VARNA_INDEX_HTML_PATH") or (REPO / "index.html"))`; всеки метод чете `INDEX`.
- **Формулярни фикстури** (`hydrant_type`, `operational`, `free_text`, `description`, `damage_description` празни): Ф-със-състояние, параметризирана по `not_tested`/`works`/`not_working` · Ф-с-тип-и-състояние (`hydrant.type` = `надземен`, `hydrant_type` = `надземен`) · Ф-без-състояние (`hydrant: {}`).
- **Седемнайсет метода.**
  `OperationalPickerTest` (8): `test_the_question_is_emitted_for_every_known_status` (трите форми × трите състояния: `id="fld_operational"` + ред `data-radio="operational"` с три чипа) · `test_no_chip_is_preselected_and_the_type_row_keeps_its_own` (нула `selected` в реда `operational`; ровно един в реда `hydrant_type` върху Ф-с-тип-и-състояние; трите форми) · `test_the_current_status_is_shown_as_a_readable_label` (надписът е `'сега: ' + OPERATIONAL_LABELS[S]`, речникът взет от клиента през пробата; Ф-без-състояние няма „сега: “) · `test_the_required_marker_is_present_on_every_form` (блокът съществува И носи `class="req"` и `class="err"` за всяка фикстура, трите форми — Р1(б)) · `test_the_untouched_branches_and_the_status_less_case_are_byte_equal_to_the_base` (`missing` и `new_hydrant` за всички фикстури + `exists_confirmed` с Ф-без-състояние: изходът на кандидата == изходът на базата) · `test_every_status_in_the_dataset_gets_a_picker_and_a_label` (различните непразни стойности от `data/hydrants.json`; броят на stderr, не се пинова; нула → `self.fail`) · `test_the_validation_gate_is_the_signed_line` (текстов: `if (AT_HYDRANT_TYPES.has(reportType) || reportType === 'new_hydrant') {` ровно веднъж; `opVisible`, `showOpPicker`, `whichever pickers are`, `reportType === 'damaged' ? 'не съм проверявал'` отсъстват) · `test_the_payload_and_the_patch_emit_both_fields_for_the_three_forms` (текстов: в `buildReportObject` двете условия `(AT_HYDRANT_TYPES.has(reportType) || reportType === 'new_hydrant')` за `type:` и `operational_status:`; в `reportTypeToSemanticPatch` `AT_HYDRANT_TYPES.has(t) && report.type` и `AT_HYDRANT_TYPES.has(t) && report.operational_status`).
  `PerimeterTest` (2): `test_everything_outside_the_changed_regions_is_byte_equal_to_the_base` (от двата текста се изрязват: областта от края на блока `const NOTE_HINT_HTML =` до края на блока `function typeFieldsHTML(t, d) {`, и блоковете `function showReportModal(`, `function onSubmitClicked(`, `function buildReportObject(`, `function reportTypeToSemanticPatch(`; остатъкът е байт за байт равен) · `test_the_pinned_functions_are_byte_equal_to_the_base` (`function locationFieldHTML(t, placedCoord) {`, `function readDraftFromForm() {`, `function wireFormHandlers(`, `function hydrantStatusClass(`, `function escapeHtml(s) {`).
  `IngestTest` (3, `sys.path` → `scripts/lib`, `hydrant_core`): `test_wrong_location_applies_type_and_status_under_the_new_id` (запис `works`/`подземен`; доклад с `reported_coord` на ~50 m, `type` `надземен`, `operational_status` `not_working` → id сменен, `type`/`operational_status` записани, старите стойности в `provenance[new_id]`, `manual_field == "multiple"`) · `test_wrong_location_without_answers_changes_only_coords_and_id` (`type: None`, `operational_status: None` → `changes` носи само `coords`, `id`; типът и състоянието остават) · `test_damaged_applies_type_and_status_and_ignores_null` (с отговори → и двете; без → нищо не се понижава).
  `NegativeHalfTest` (4, подпроцес `python -m unittest <модул>.<клас>` с `FIRE_VARNA_INDEX_HTML_PATH=<копие под $TEMP/fv_lot1_fixtures>`, котвите строго `count == 1`, очакван изход 1 + игла): `test_removing_the_operational_row_turns_the_gate_red` (`radioRowHTML('operational', ['да','не','не съм проверявал'], d.operational) +` → `'' +`; пада методът за трите чипа) · `test_restoring_the_old_validation_gate_turns_the_gate_red` (подписаният ред → `if (reportType === 'new_hydrant') {`; пада текстовият метод) · `test_a_byte_in_an_untouched_branch_turns_the_gate_red` (един байт в клона `missing` на `typeFieldsHTML`; пада байт-равенството) · `test_a_byte_in_the_tail_segment_turns_the_perimeter_gate_red` (един байт в реда `function refreshMarkerIconsForHydrant(h) {`; пада периметърът).
- Тестът не печата запис от `data/hydrants.json` и не носи координата.

**К3.4 · `gates/probe/b3_g11.py`, `ALLOWED_NEW`** — три нови реда с коментар кой лот ги ражда: `"tests/test_report_form_operational.py"`, `"docs/plans/ПЛАН_Проблемите_лот1_Ф-а_13.09.md"`, `"docs/audits/амандамент_подаване_13.09.md"`. Нищо друго; остарелият ред `tests/test_b3_gates.py` не се трие (беклог лот 5).

**К4 · `docs/activeContext.md` (Architect, в основното дърво, след `merge --ff-only`, преди пуша). Един ред:**
`- **13.09 · Проблемите, лот 1 (един стандарт):** трите форми при хидрант („Хидрантът е там“, „Грешна локация“, „Повреден“) носят един и същ блок „Тип + Работи ли?“ — задължителен, нищо предизбрано, текущото състояние като надпис; ингестът прилага двете полета и за грешна локация/повреден (#769, #716, #731). Комит <кратък id>, `index.html` <нов размер> B; нула байта в `data/`.`

**Какво НЕ се пипа.** `data/` · `worker/` · `sw.js` · `initAddressSearch`/`initPlacesSearch` · `locationFieldHTML`, `readDraftFromForm`, `wireFormHandlers`, `hydrantStatusClass`, `escapeHtml`, `buildReportYAML`, `buildIssueBody`, `labelsForType`, `dedupHash`, опашката, `applyReports` · `docs/moderation_log.md` · `scratch/apply_c*.py` · `tests/negative_halves_manifest.json`, `tests/test_negative_halves.py`, `tests/test_ui_ascii_mojibake.py`, `tests/granitsi_client_probe.mjs`, `tests/test_apply_approved_reports_parity.py`, `tests/golden_apply_v0.py` · `gates/release.py`, `gates/run_gates.py` · CSS · клоновете `missing`, `new_hydrant` · `apply_missing`, `apply_new_hydrant`, `apply_exists_confirmed`, `process`, `DISPATCH` · `C:/git/Fire_Varna` (до сливането), `C:/git/_wt/fv_lot1_client`, двата стеша.

---

## 6 · Гейтовете (CWD = дървото; тежките един след друг)

Присвояване първо: bash `M=tests.test_report_form_operational; O=$M.OperationalPickerTest; P=$M.PerimeterTest; I=$M.IngestTest; N=$M.NegativeHalfTest` · PowerShell `$M='tests.test_report_form_operational'; $O="$M.OperationalPickerTest"; $P="$M.PerimeterTest"; $I="$M.IngestTest"; $N="$M.NegativeHalfTest"`. Диапазоните `"$(git merge-base main HEAD)..HEAD"` са в кавички (PowerShell ги цепи иначе).

**Г1 · Формите.** `python -m unittest $O` → 0 (осемте метода). **Червено преди:** същата команда с `FIRE_VARNA_INDEX_HTML_PATH=<копие на git show 8512ab6:index.html>` → изход 1 (пикерът липсва за записите със състояние; `wrong_location` няма блок).
**Г2 · Периметърът в `index.html`.** `python -m unittest $P` → 0. Половина: Г8.
**Г3 · Ингестът.** `python -m unittest $I` → 0. **Червено преди:** `git stash`-ове не се пипат — червеното е доказано в сесията от 13.09 с базовия `hydrant_core.py` (докладът с `type`/`operational_status` ги губеше); Изпълнителят го повтаря през `git show 8512ab6:scripts/lib/hydrant_core.py > $TEMP/…/hydrant_core_base.py` и `python -c` с двата модула → базата не пише полетата.
**Г4–Г8 · Половините** (всяка → изход 0, защото подпроцесът ѝ е червен): `python -m unittest $N.test_removing_the_operational_row_turns_the_gate_red` · `$N.test_restoring_the_old_validation_gate_turns_the_gate_red` · `$N.test_a_byte_in_an_untouched_branch_turns_the_gate_red` · `$N.test_a_byte_in_the_tail_segment_turns_the_perimeter_gate_red`. Одиторът ги пуска сам.
**Г9 · Пакетът.** `python -m unittest discover -s tests` → **Ran 389 · OK** (372 + 17). 389 е очакването преди `git rebase main`; след него 389 + каквото `main` е добавил (изброено с `-v`, приписано на `main` преди STOP).
**Г10 · `python -m gates.run_gates`** → 0, 7/7.
**Г11 · B3.** `PYTHONIOENCODING=utf-8 python gates/probe/b3_g11.py $(git merge-base main HEAD)` → 0, `stray_new: []` (между К1 и К3 — червен по конструкция).
**Г12 · Приватност.** Изменени файлове: `python gates/probe/ib1_g15.py --added-lines 8512ab6 -- index.html scripts/lib/hydrant_core.py gates/probe/b3_g11.py docs/audits/backfill_and_submission_extension_plan_20260509.md` (непразен diff за всеки, доказан с `git diff --unified=0 8512ab6 -- <path>`); нови файлове — целофайлов режим: `python gates/probe/ib1_g15.py tests/test_report_form_operational.py docs/plans/ПЛАН_Проблемите_лот1_Ф-а_13.09.md docs/audits/амандамент_подаване_13.09.md`. Очакван ред `IB1-G15 OK operands=<n> hits=0`. `docs/activeContext.md` — след К4 (§9).
**Г13 · Браузърът.** Направен от Петър на 13.09 на порт 8001 (трите форми на хидранта от #769, зелен и сив хидрант; хедърът 7403 преди и след). Изпълнителят го повтаря DOM-ово след теста: трите форми → `#fld_operational` с `.req`, `.err`, надпис при състояние, нула `selected`; нула нови грешки в конзолата извън CORS към Worker-а от `localhost:8001` (средата). Без „Изпрати“.
**Г14 · Редът на комитите.** `git merge-base --is-ancestor <К2> <К3>` → 0; разменени → 1.
**Г15 · Байтовете в `data/`.** `git status --porcelain -- data/` празен · `git diff --exit-code --stat --cached -- data/` → 0 · `git diff --exit-code --stat "$(git merge-base main HEAD)..HEAD" -- data/` → 0. Половина: `git diff --exit-code --stat 8512ab6~1..8512ab6 -- data/` → 1.
**Г16 · Периметърът по пътища.** `git diff --name-only "$(git merge-base main HEAD)..HEAD" | sort` → точно: `docs/audits/backfill_and_submission_extension_plan_20260509.md`, `docs/audits/амандамент_подаване_13.09.md`, `docs/plans/ПЛАН_Проблемите_лот1_Ф-а_13.09.md`, `gates/probe/b3_g11.py`, `index.html`, `scripts/lib/hydrant_core.py`, `tests/test_report_form_operational.py`. Забранена зона: `git diff --quiet --exit-code "$(git merge-base main HEAD)..HEAD" -- data scratch worker sw.js docs/moderation_log.md tests/negative_halves_manifest.json tests/test_negative_halves.py tests/granitsi_client_probe.mjs tests/test_ui_ascii_mojibake.py tests/test_apply_approved_reports_parity.py tests/golden_apply_v0.py gates/release.py gates/run_gates.py` → 0; половина за `8512ab6~1..8512ab6` → 1.
**Г17 · Кодировката.** `python -m unittest tests.test_ui_ascii_mojibake` → 0; мойбейк-сканът от AGENTS.md върху `index.html`, теста и двата документа → 0 маркера.

---

## 7 · Комитите (дословни; идентичността се налага с флагове)

**К1 · Architect** — `git add -- docs/plans/ПЛАН_Проблемите_лот1_Ф-а_13.09.md` · `git -c user.name="Claude Architect" -c user.email="architect@local" commit -F <msg извън репото>`
```
docs(plan): lot 1 - one standard for every hydrant form

The plan signed by Petar on 13.09 after a local test: the three report forms
made at a hydrant carry the same required type / operational pickers, the
current status is a readable label above an unselected row, and the ingest
applies both fields for wrong_location and damaged. Nothing in data/ changes.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

**К2 · Architect** — `git add -- docs/audits/амандамент_подаване_13.09.md docs/audits/backfill_and_submission_extension_plan_20260509.md` · същите флагове
```
docs(audit): amendment - one standard for every hydrant form

The 2026-05-09 contract ratified "hide completed pickers in exists_confirmed"
and "wrong_location: no type or operational fields". Issue #769 showed the
cost of both. This separate amendment supersedes those rows: the three forms
made at a hydrant carry the same required pickers, the stored status is a
readable label, and the ingest applies both fields for wrong_location and
damaged. It records the downward route and its guard (moderation). The
ratification table is untouched; a pointer line was added below it.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

**К3 · Executor (един комит)** — `git add -- index.html scripts/lib/hydrant_core.py tests/test_report_form_operational.py gates/probe/b3_g11.py` · `git -c user.name="Claude Executor" -c user.email="executor@local" commit -F <msg извън репото>` · `git status --short`
```
fix(report): one standard for every hydrant form (#769)

The three report forms made at a hydrant - exists_confirmed, wrong_location
and damaged - now carry the same block: the type picker, pre-selected from
the record, and the operational picker, never pre-selected, with the stored
status shown as a readable label. Both answers are required (Petar, 13.09).
Before, exists_confirmed hid the operational picker for every record that
already carried a status (including "not_tested"), wrong_location carried no
type and no status at all, and damaged pre-selected "not checked" without
validation, silently downgrading known statuses. The ingest applies type and
operational_status for wrong_location (after the re-key, so provenance lands
under the new id) and type for damaged; null stays ignored.

tests/test_report_form_operational.py raises typeFieldsHTML headless through
tests/granitsi_client_probe.mjs, pins the base as a literal under its own env
name, byte-compares everything outside the changed regions, exercises the two
ingest handlers in memory and carries four negative halves that run and fail.
The new paths are declared in ALLOWED_NEW of gates/probe/b3_g11.py.

Zero bytes in data/, worker/ and sw.js.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

**К4 · Architect, в основното дърво, след `merge --ff-only`** — `git -C C:/git/Fire_Varna add -- docs/activeContext.md` · `git -C C:/git/Fire_Varna -c user.name="Claude Architect" -c user.email="architect@local" commit -F <msg>`
```
docs(state): lot 1 - one standard for every hydrant form

One line for the lot: what changed, the commit, and the new index.html size.
The stale byte rows of this page stay untouched - they are backlog D-52 (lot 2).

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```
Преди К4: `git -C C:/git/Fire_Varna status --porcelain --untracked-files=no` празен, без прекъснат merge/rebase.

---

## 8 · STOP-условия

1. Дифът в дървото се различава от §5 (Изпълнителят го сверява ред по ред преди теста).
2. Червен гейт с причина извън лота.
3. Харнесът не вдига функциите без маркер в продукционния код.
4. `git show 8512ab6:index.html` не се разрешава, или базата е равна на кандидата.
5. Координата, име на докладчик, кадастрален номер, imot провенанс, суров дайджест или дълбок линк на път към проследен файл.
6. Нужда да се пипне `data/`, `worker/`, `sw.js`, търсачката, паритетният тест или златният еталон, `docs/moderation_log.md`, манифестът на половините, стеш.
7. Пакетът не дава 389 и разликата не се обяснява с тестове от `main` (изброени с `-v`).
8. Основното дърво не е чисто по проследените файлове преди К4 (`status --porcelain --untracked-files=no`, `MERGE_HEAD`/`rebase-merge`).
9. Нужда от истинско подаване за доказателство — не се прави.
10. `git diff --stat 8512ab6..main -- index.html scripts/lib/hydrant_core.py` не е празен след ребазирането — чужда тема е пипнала файл, срещу чийто литерал съди байт-равенството.

---

## 9 · Одит и Gate 2

**Одиторът (Opus, read-only, различен агент) дава ГОДНО/НЕГОДНО по редове:** (1) дифът на К3 = §5 (нищо повече, нищо по-малко); (2) периметърът е машинно доказан (Г2, Г15, Г16); (3) седемнайсетте метода съществуват с обявените имена и пакетът дава 389; (4) **четирите половини падат пред очите му** (Г4–Г8, пуска ги сам); (5) ингестът: трите случая на Г3 + червеното на базата; (6) `ALLOWED_NEW` носи трите пътя, `b3_g11` → `stray_new: []`; (7) комитите с правилните ръце и дословните съобщения, Г14; (8) Г12 с доказано непразен diff за изменените и целофайлов режим за новите; (9) DOM проверката на Г13 цитирана; (10) нула мъртъв код (`opVisible`, `showOpPicker`, старият `damaged` подразбиране).

**Кими (еднократно копие, само одитор, вижда плана и дифа):**
```
mkdir -p C:/Users/Petar/AppData/Local/Temp/fv_lot1
git diff "$(git merge-base main HEAD)..HEAD" > C:/Users/Petar/AppData/Local/Temp/fv_lot1/lot1_diff.patch
cp docs/plans/ПЛАН_Проблемите_лот1_Ф-а_13.09.md docs/audits/амандамент_подаване_13.09.md C:/Users/Petar/AppData/Local/Temp/fv_lot1/
bash C:/Projects/Project_wiki/kimi.sh "Одит на лот 1 (един стандарт) на Fire_Varna: сверѝ дифа срещу плана и амандамента — тихо разширяване, гейт без отрицателна половина, разминаване между решенията Р1–Р9 и кода, път по който null или стар доклад променя данни" ultra C:/Users/Petar/AppData/Local/Temp/fv_lot1 lot1_diff.patch ПЛАН_Проблемите_лот1_Ф-а_13.09.md амандамент_подаване_13.09.md
```

**Командите за Петър — дават се, никога не се пускат от агент.**
*Блок А (Gate 2):*
```
cd C:/git/_wt/fv_problems
git rebase main
python -m unittest discover -s tests        # 389 + каквото main е добавил
python -m gates.run_gates                   # 7/7
git diff --stat 8512ab6..main -- index.html scripts/lib/hydrant_core.py   # ПРАЗНО, иначе STOP-10
git -C C:/git/Fire_Varna merge --ff-only problems
```
*Предаване:* Петър вика Архитекта за К4; после `cd C:/git/Fire_Varna && python gates/probe/ib1_g15.py --added-lines <sha на К4>^ -- docs/activeContext.md` → `IB1-G15 OK operands=1 hits=0`.
*Блок Б (само когато нищо не виси):*
```
git -C C:/git/Fire_Varna push origin main
```

---

## 10 · Какво затвориха кръговете

v1–v5 (13.09, преди подписа): 37 находки на трима опровергатели, 12 на критика, 10 на Astra, 3 на Кими — вакуумният гейт за `data/`, merge-base като база на тест, посоката надолу (Р7), fail-open Г2, Г4 без половина, слепият зелен на `--added-lines` върху нов файл, `-C` към python, PowerShell цепи `$(…)..HEAD`, договорът от 09.05, D-26 нищожно. Прегледът (13.09): Astra 8/8 „could not refute“ върху първия диф (вкл. валидацията, изпълнена headless); Петър тества и реши Р1(б) и „един стандарт“; след разширението — headless проба за петте типа доклад, DOM на трите форми, ингест в паметта (четири случая), пакетът 372 OK. Остатъци за беклога: преименуванията в `b3_g11`, ИБ1-Г13 без проверка на идентичността на инжектирания провал, `tests/test_b3_gates.py` в `ALLOWED_NEW` (лот 5).

---

## 11 · Какво НЕ е в лота

- **Лот 3 е погълнат** (F-3, F-4, F-5, F-6, R-01); остава само F-15 — темата за модерация записва „поправено в <комит>“ и затваря #716/#731/#769 в следващия си цикъл.
- `missing` без канонични полета (нищо за тестване); `new_hydrant` вече беше стандартът.
- `docs/moderation_log.md` · H-1/D-52 (лот 2) · ИБ-2 · лот 4б · беклог лот 5. Историята не се пренаписва; непроследени файлове не се трият.

---

## 12 · Преамбюл (AGENTS.md § Planner Plan Preamble Checklist)

**Обхват.** Лот 1 „един стандарт“: бъг 1 + бъг 2 на формата (F-1, F-2, F-3, F-4, F-5, F-6, R-01; #769, #716, #731) в `C:/git/_wt/fv_problems` при 8512ab6.
**Инвентар (пипнатите пътища).** `index.html` · `scripts/lib/hydrant_core.py` · `tests/test_report_form_operational.py` (нов) · `gates/probe/b3_g11.py` · `docs/plans/ПЛАН_Проблемите_лот1_Ф-а_13.09.md` (нов) · `docs/audits/амандамент_подаване_13.09.md` (нов) · `docs/audits/backfill_and_submission_extension_plan_20260509.md` (ред под таблицата) · `docs/activeContext.md` (основното дърво, след сливането).
**Файлове, четени:** `CLAUDE.md`, `AGENTS.md`, `.gitignore`, `index.html` (функциите от §3/§5, CSS на пиновете, IIFE, котвите на търсачките), `scripts/lib/hydrant_core.py`, `scripts/apply_approved_reports.py`, `scripts/hooks/pre-push`, `tests/` (списъкът), `tests/test_hydrant_core.py`, `tests/test_address_quarter_render.py`, `tests/test_negative_halves.py`, `tests/negative_halves_manifest.json`, `tests/test_adr_amendment.py`, `tests/granitsi_client_probe.mjs`, `tests/test_ui_ascii_mojibake.py`, `gates/probe/b3_g11.py`, `gates/probe/ib1_g15.py`, `gates/run_gates.py`, `gates/release.py`, `docs/activeContext.md`, `docs/moderation_log.md` (цикъл #35), договорът от 09.05, `docs/decisions/011a…`, `docs/plans/ПЛАН_Картата_imot_И-Б1_11.09.md`. Мерени с команда: `data/hydrants.json`, куката, общият конфиг.
**Отрицателни находки.** втори викащ на `typeFieldsHTML` — няма · тест, който пинова скрит пикер или предизбраното „не съм проверявал“ — няма · гейт по sha/размер на `index.html` — няма · нов CSS клас — не трябва · нов YAML ключ — не трябва · път, по който `null` променя запис — няма · пазач на новия амандамент в `tests/` — няма (Г14).
**Цитирани метаданни (дословно).** договорът `:46` „Hide completed pickers in `exists_confirmed` | Petar amendment | … | Ratified“, `:124` „`wrong_location`: corrected coords + description; no type or operational fields.“, `:161-162` · `index.html` `:2867-2868` „The operational picker keeps the original 'hide when already populated' behavior“ · `tests/negative_halves_manifest.json` `_meta.rule` „len == 23 …“ · `AGENTS.md:166` „UI label / wording changes -> Petar approval“ · `.gitignore:15` `.claude/`.
**Леджер.** Р1–Р9 в §4 — всички с подпис от 13.09; „нула байта в `data/`“ — действащо (Г15 + Г16).
**Отворени въпроси (не блокират).** (1) F-15 — записът в дневника на модерацията при следващия цикъл. (2) Екранната снимка от 375 px остава извън репото. (3) Кодът на прегледа беше написан от Архитекта по думата на Петър преди този план — Изпълнителят го сверява с §5, не го пренаписва.
