# Амандамент №3 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 05:20) — след одита на F12: НЕГОДНО (3 major, 5 minor), всичко приема се и се лекува преди A.2

Нов датиран файл по §0.2. F12-д (0bc5acc) и F12-е (6e539c5) са доставени и измерени (bundle-тестовете следват доставката; М7 само значими думи, 33 задействащи; паритет клиент ↔ референция 203/203). Одиторът върна три major и пет minor. Решения:

## Major → F12-ж, F12-з, A.2-4 (преработена)
1. **F12-ж — базовата котва на манифестите се чете от blob-а, не от диска.** `recall_sweep.py` `sha_and_bytes(path)` чете работното дърво (CRLF, 266 021 B, `e5901cd8…`), а `_meta.base` твърди `commit: f06ac06` (blob: 256 070 B, `0bc7a189…`). → нова `sha_and_bytes_at_commit(commit, path)` през `git show <commit>:<path>`; всяка котва с `commit` се смята от blob-а; двата манифеста се регенерират report-only; **доказателство от ЧИСТ клон** (`git clone` → checkout HEAD → `--manifest`) — байт-идентични с работното дърво. Гейт: манифест с котва от диска (нарочно) → червено. Съобщение: `test(search): manifest anchors are blob bytes at the named commit, proven from a clean clone (F12-ж)`.
2. **F12-з — `activeContext.md` казва истината за HEAD** (§ Current state: ahead, commits on main, index.html байтове, тестове 21 червени по клас; абзацът „F12 stops there“ вън; m7 бележката = „33 задействащи думи, чака подпис“; изричен хеш вместо HEAD~4); в PARKED.md ред за E0: „Петър, 05.09: не ни трябват — не се премерва“ (Q10 затворен). Съобщение: `docs: activeContext follows F12-д/е; E0 closed by Petar's word (F12-з)`.
3. **Класът „чака подпис“ се разделя** (одитът е прав): (а) тестове, които четат `signed_by` и позеленяват с подписа; (б) `Lot1vVGateTest::test_gate` сравнява срещу ЗАКОВАНИ константи `LOT1V_V_GAINS`/`LOT1V_V_CONTROLS` (числата на предишната доставка) — подписът не го позеленява. → **A.2-4 (преработена):** константите изчезват от кода; очакванията за гейт-заявките (6 gains + 4 controls) живеят в подписваемия манифест (BASE→P7 ги носи като `gate_queries`); тестът твърди срещу ПОДПИСАНИЯ манифест → до подписа е червен от клас (а) с преди/след на всяка от 10-те заявки в манифеста за Петър (R3). Един механизъм за всички очаквания, зависими от доставката.

## Minor → влизат в A.2
4. **Траен гейт за М7:** `check_m7_gate()` в `recall_sweep.py` (типовите префикси `к/кв/жк/м/с/о/т/и/зона` не задействат; седемте контролни места задействат) + тест в `tests/test_places_search_gate.py`, който твърди, че двигателят възпроизвежда `m7_trigger_tokens.json` ред по ред (45/45); дотогава `m7_significance_gate.py` и `gen_m7_tokens.py` се копират от scratchpad в `scratch/places_search/` (tracked) — в F12-ж.
5. **Едно правило за значимост в клиента:** `sigTokens` вика `isSignificantToken` (`index.html` ~6666) — нулева промяна днес, невъзможно разминаване утре (A.2 или F12-ж, по-малкият комит).
6. **`GENERIC_WORDS` празен без речник → fail-closed:** ако речникът не се е заредил, М7 не се задейства изобщо (референцията винаги има речник; деградираният клиент не бива да измисля списък). В A.2 с тест.
7. **Броячът на ЛОТ 1в-В гейта** брои уникални заявки с неуспех, не редове (печаташе „-3/10“). В A.2.
8. **Пробата** (`probe_places_fv.mjs`): трите ръчно поддържани очаквания (LOC_OFF таблицата, D9 картончето за „детска градина морска звездица“ → ж.к. Младост, `probe_out/before.json` от порт 8010) се премерват СЛЕД подписа от одитор в отделен checkout — ред в опашка A (R11) като известен дълг, не се крие.

## Ред
F12-ж, F12-з (Fire_Varna, изпълнител) → одит (целият F12, фокус: котвите от blob-а, чист клон, activeContext, класификацията на червените) → A.2 (по v2 + т. 3–7 тук) → одит → A.3 опашката (R1–R6, Q7, R9 + R3 с 10-те гейт-заявки преди/след + R11 дългът на пробата; Q10 затворен).
