# Амандамент №4 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 06:40) — след третия одит на F12 (НЕГОДНО: 2 major, 4 minor); всичко се приема и се влива в A.2

Нов датиран файл по §0.2. F12-ж (8083a6a) и F12-з (bf574c0) са доставени: котвите на манифестите са blob-байтове при назован комит, доказани от чист клон; трайният М7 гейт е в `scratch/places_search/`. Одиторът опроверга две твърдения и намери четири дупки. Решения:

## Major
1. **„Подписът прави тестовете зелени“ е невярно — измерено: след подпис + `--freeze` червените са 32, не 0.** Класовете, преброени честно: (а) 1 тест чете `signed_by`; (б) 17 сравняват със заковани литерали в кода/теста (LOT1V_V_GAINS/CONTROLS, P7_EXPECTED „7 tokens in 6 zones“, LOT1V_A/B_ANCHOR комити, три ERROR-а по несъществуващи атрибути `REF.ZONE_PHRASES`/`rec.zph`/`rec.ztk`); (в) 3 „replay“ теста сравняват двигателя със замразената референция, която няма `signed_by`, и позеленяват само след замразяването, а то презаписва и `LOT1_DATA_CHANGED/ADDED` и числата 122/1998. → **A.2-4 се прилага към ВСИЧКИТЕ 21:** нито едно очакване, зависимо от доставката, не остава литерал в код или тест. Едно проследено тяло `scratch/places_search/expectations.json` (произведено от `--manifest`, носи `signed_by`, съдържа: гейт-заявките с преди/след, P7 добавените токени/зони, LOT1 бройките, котвените комити на кофите, replay-очакванията по кофа); тестовете четат САМО него; `--freeze` го подписва наведнъж заедно с референцията. Трите ERROR-а се лекуват като част от същата редакция (тестът назовава атрибути, които двигателят вече няма — редакцията е механична, не поведенческа). Гейт: след A.2-4 `python -m unittest discover -s tests` → всеки червен тест чете `expectations.json`/`signed_by` (доказано с grep по имена); след подпис + едно замразяване в изолиран клон → **0 червени** (одиторът повтаря експеримента).
2. **`--freeze` пише проследената референция ПРЕДИ да смята гейтовете** (`recall_sweep.py:3502–3514`, гейтите след това) → редът се обръща: гейтовете първо; запис само при 0 червени И `signed_by == "Петър"` на манифеста; записът е последната операция. Гейт на счупен вход: `--freeze` върху днешната неподписана доставка → изход ≠ 0 и `git status` чист. §A.4 получава това като предусловие.

## Minor
3. `_meta.inputs` на манифестите се смята от blob-а (`sha_and_bytes_at_commit("HEAD", path)` или запис с `commit: "HEAD"`, за да го хваща структурният обход); `manifest_anchor_gate.py` получава счупен вход с CRLF-диск за `inputs`.
4. `gen_m7_tokens.py --check` сравнява ТЕКУЩИЯ двигател с проследения файл (зелено = значи нещо); литералният клон остава само в `m7_significance_gate.py --literal` (нарочното червено). Никакво „червено по замисъл“ в проследен инструмент.
5. `lot1v_v_reference_manifest.json` (проследен, но генераторът го пише в системния temp; тялото е застояло: 180 944 B срещу 165 099 B регенерирано) → генераторът го пише в `scratch/places_search/` като другите два и се регенерира в същия комит; един манифест, едно тяло.
6. **Авторството:** плановите комити на архитекта (амандаменти 2 и 3) са с автор Petar1984 → от този момент архитектът комитва като `Claude Architect <architect@local>`; само `gates.sign`, пусната от Петър, комитва като Petar1984. D.3 гейтът сверява автора на комита, който ВЪВЕЖДА `signed_by: "Петър"`. (Изпълнителските комити са коректни: 9/9 `Claude Executor`.)
7. `activeContext.md` § „The 21 red, by class“ → трите класа (а) 1 / (б) 17 / (в) 3 с бележката „след A.2-4 остава само (а)“ — в комита на A.2-4 (write-set: docs/activeContext.md).

## Ред
A.2 (по v2 + амандамент 3 т. 3–7 + този: A.2-1 release-гейт; A.2-2 `gates.sign`; A.2-3 куката; A.2-4 очакванията → `expectations.json`, `--freeze` последен, `inputs` от blob, `--check` смислен, третият манифест регенериран, activeContext) → одит (вкл. експеримента „подпис + freeze в изолиран клон → 0 червени“) → A.3 опашката.
