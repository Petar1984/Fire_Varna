# ПЛАН · Картата imot — под-лот **И-А** (varna_3d + документи)

`Fire_Varna/docs/plans/ПЛАН_Картата_imot_И-А_08.09.md` · 08.09.2026 · **редакция 4** (след шест одиторски кръга: два общи + „И-А кръг 1“, „И-А кръг 2“ и двата кръга върху редакция 3; плюс Astra S40/Кими К39 и финалните проверки, свити от Архитекта като редове ИА-О30 нататък в §2 — броят им расте и се чете там и в червен ред 10, не тук — ИА-О38) · Планировчик (Opus, само четене) → **подпис на Петър (Gate 1)** → Изпълнител → Одитор → локален преглед → **Gate 2 и пуш от Петър**

**Какво прави този лот.** Прави плочката на imot.bg **канонична геометрия** в частното репо `varna_3d` — замразеният вход, подписаното тяло с решенията, нов билдър, негов собствен гейт, архив на v1 — и допълва три документа във Fire_Varna. **Нищо във Fire_Varna освен тези три проследени документа и един поименно обявен ИГНОРИРАН файл за преглед не се пипа.** Живата карта, доставката Б3, `index.html`, `data/`, `tests/`, `gates/` на Fire_Varna остават непроменени и си остават работа на **И-Б**.

**Предшественик.** `docs/plans/ПЛАН_Картата_imot_08.09.md` (редакция 4, **проследен** — М: `git ls-files --error-unmatch` → 0, последен комит по пътя `146beac`, ред-статус „заменен от И-А/И-Б“ отгоре; байтове и редове **не се пинват** — ИА-О32/ИА-О33). Този под-лот **изпълнява** неговите §3 И-А, §5.А и Приложение Б; всяко място, където се разминава с него, е ред в §2. Планът v4 остава на диска като предшественик и **не се изтрива**.

Всяко число долу носи котва: **(Ч)** = `файл:ред`, **(М)** = командата, която пуснах, и изходът ѝ. Числа без котва в този план няма. Всяко число на редакция 3, което този кръг обори, е поправено ТУК с нова мярка и е назовано поименно на мястото си.

---

## 0 · Червени редове

1. **Нищо не се редактира преди подписа.** Първата команда на Изпълнителя е `git status --porcelain`, не `Write`. **Няма изключение за `Д1` (ИА-О32):** самият този план е ВЕЧЕ проследен и комитнат от Архитекта (М: `git ls-files --error-unmatch docs/plans/ПЛАН_Картата_imot_И-А_08.09.md` → 0; последен комит по пътя — Архитект). Подписът на Петър е думата му в чата; Архитектът (не Изпълнителят) вписва един ред-статус „подписан от Петър, <дата>, ревизия <sha>“ отгоре и го комитва като **К1**; ако Петър поиска редакции, Архитектът ги внася ПРЕДИ подписа. Изпълнителят никога не пише в `Д1`.
2. **Никакъв `git push`.** Пушът е само на Петър (универсален инвариант №1).
3. **Данните и геометрията ги комитва Петър** (ADR 010 D16, Ч `010_signed_quarter_boundaries.md:68`). Агентът ги оставя стейджнати с изричен pathspec.
4. **Fire_Varna: САМО три ПРОСЛЕДЕНИ документа (Д1, Д2, Д3) + един поименно обявен ИГНОРИРАН файл за преглед (Д4).** Никакъв ред в `index.html`, `data/`, `tests/`, `gates/`, `scratch/places_search/*.py|*.json`. Докосване на пети път във Fire_Varna = STOP.
5. **Нула квартална геометрия и нула суров провенанс в проследен файл на Fire_Varna.** Геометрията, `advert_id`, `raw_sha256`, `points` и **всяка координата** живеят само в **частното** репо `varna_3d` и в игнорираната папка на Fire_Varna (М: `git check-ignore -v scratch/boundary_gallery/granitsi_preview_05.09/varna_polygons_08.09.geojson` → `.gitignore:64:/scratch/boundary_gallery/`). **Този план е проследен файл на Fire_Varna — затова в текста му няма нито една двойка `lat/lon`; навсякъде, където предишна редакция цитираше координата, стои „(координата в частния файл)“. Спазването се съди механично от ИА-19.** Обобщени производни (площи, брой върхове, хистограма на ръба, хешове) **не са координати** и остават позволени — обявено в ИА-О24, дългът за правило и гейт е ИБ-18.
6. **Гейт, чиято команда не може да върне ≠ 0, не е гейт.** Всяка обявена отрицателна половина ТРЯБВА да е тичала и да е паднала с записан изход; „ще падне“ не е доказателство (поука `gates-lie-more-than-code`). **Ред, за който предварително се знае, че няма да падне, НЕ стои в колоната „отрицателна половина“** — той е мярка и се записва в отчета.
7. **Нула регресии.** Всеки от деветте измерени изхода на **§1.2** се запазва — **със заявената в §5 команда за съответната фаза** (преди и след преместването на v1 командата е РАЗЛИЧНА; изходът е същият). Различен изход = STOP.
8. **Подписано тяло не се редактира на място.** Нова версия — нов файл, стар в архива, никога `git rm`.
9. **Кодът е на латиница.** Имена на модули и коментари на английски; на български остават документите и текстовете за човек (поука `code-in-english`).
10. **Собствени пространства на номерата — затворени и без сблъсък по азбука.** Гейтове **ИА-1 … ИА-19**, отклонения **ИА-О1 … ИА-О45** (ИА-О25 … ИА-О29 не съществуват — номерата са прескочени, за да не се преномерират препратки; ИА-О30 … ИА-О45 са сверките на Архитекта), спирачки **ИА-STOP 1 … 18**, файлове **Ф1 … Ф11**, точкови редакции **Т1 … Т15**, разлики в подписаното тяло **Х1 … Х13**, пинове **П1 … П18**, база **Н1 … Н9**, решения **Р1 … Р21**, комити **К1 … К9**, дългове към И-Б **ИБ-1 … ИБ-20**. (М, ИА-О33: `grep -rIl "ИА-[0-9]" C:/git/Fire_Varna/docs C:/git/varna_3d/src C:/git/varna_3d/docs` → **точно два файла: този план и Д3** — `docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` §20 води дневника на сверките от ИА-О30 нататък; нищо в varna_3d.) **Кръговете на опровергателите се цитират винаги с името на лещата — „Кими К37“, „Astra S40“; голо „К37“ не е комит (ИА-О33).**
   **Пространството `Б` НЕ се използва от този план в нито един вид.** Кръг 3 намери, че `Б1…Б15` (точкови редакции) и `Б-1…Б-16` (дългове) се различаваха само по тире, а „Б1“ и „Б3“ вече значат живи лотове в кода (М: `build_quarters_signed.py:1408` `"lot": "Б1"`, `:1563` `fail("червена черта", …лот Б1…)`, `qa_quarters_signed.py:1197` „Gate Б1“) и живата доставка Б3. Затова редакциите станаха **Т**, дълговете — **ИБ**. Никакъв `G4`, `G11`, `G15`, `G17`, `G21`, `G22`, `G23`, `G29`; никакъв гол `О7`/`О8`/`О16` — те са заети в `ПЛАН_Б3_07.09.md:240`, `:241`, `:249` (Ч), а пространството `О` там стига до **О30** (Ч `:263`). **Латинско `A` не се използва като СОБСТВЕНО пространство (`A<n>`) никъде** — цитатите на `A13`/`A14` на стария билдър и „I-A“ в съобщенията на комитите са цитати и имена, не пространство (ИА-О35).
11. **Лицензът не е тема.** Дословният низ за атрибуция се **попълва** по решение Р10; не се обсъжда, не се проучва.
12. **Sha на доставка = блобът.** Във Fire_Varna работното копие МОЖЕ да е ≠ блоб (CRLF); за файловете, които този лот чете, днес са равни (М, ИА-О39: `data/address_rows.json` — `eol: lf`, 0 CRLF, един и същ sha), но правилото пак е „чети блоба“. Във **varna_3d разлика няма**: `.gitattributes:4` е `* text=auto eol=lf` (Ч), М: `git check-attr -a data/quarters_signed.geojson` → `eol: lf`, и работното копие, и блобът дават `c1551d24…`, 301 917 B. Правилото пак е „чети блоба“; varna_3d просто няма как да те излъже.
13. **Блоб се адресира стабилно, не през HEAD.** Навсякъде, където се чете блоб, ревизията се взема с `git -C <репо> log -1 --format=%H -- <път>` (М днес за черновата с решенията → `7556227…`; за `district_resolutions_2026-09-07.json` → `e6c706d…`; HEAD на Fire_Varna в деня на мярката беше същият `7556227` — съвпадение, което **не се ползва**; HEAD е променлив, ИА-О32). **Вход, който НЕ живее в никое git-репо, не може да се адресира по това правило и се пинва по sha256 на прочетените байтове** — това е точно случаят на слоя с петте района (Р21, ИА-О23). **ИА-О35: същото важи за всеки вход, за който Ф2 няма `--*-repo/--*-rev` флагове (`address_rows`, регистърът) — байтовете са пинът, `rev` е свидетел; блобово се сверяват само Ф7, резолюциите и диф-базата (§3.Г.7).**

---

## 1 · Мярка (всичко мерено 08.09; нула редакции)

### 1.1 · Състояние на репата

| | стойност | котва |
|---|---|---|
| `varna_3d` HEAD | `05f485e` · `Petar1984` · 2026-09-07 · „data: p8a quarter assignment ledger 07.09“ | (М) `git -C C:/git/varna_3d log -1 --pretty='%h %an %ad %s' --date=short` |
| `varna_3d` клон | `rezhimi` | (М) `git branch --show-current` |
| `varna_3d` ahead от `origin/main` | **74** | (М) `git rev-list --count origin/main..HEAD` |
| `varna_3d` мръсни проследени | **1** — `scratch/place_bodies/qa_place_bodies.md` | (М) `git status --porcelain`, преброени редовете без `??` (без тръба в клетката — ИА-О35) |
| `varna_3d` непроследени | **41** | (М) `git status --porcelain`, преброени редовете с `??` (ИА-О35) |
| `varna_3d` `core.autocrlf` / `.gitattributes` | `true` / `* text=auto eol=lf` (Ч `:4`) → **работно копие = блоб** | (М) `git config core.autocrlf`, `git check-attr -a` |
| `Fire_Varna` HEAD | **ПРОМЕНЛИВА — не се пинва** (Петър приема доклади за хидранти; Архитектът комитва документи). Записва се на стъпка 0 като факт, не като очакване (ИА-О32). | (М на стъпка 0) `git log -1 --pretty='%h %an %s'` |
| `Fire_Varna` ahead | **ПРОМЕНЛИВА — записва се, не се пинва** (ИА-О32) | (М на стъпка 0) `git rev-list --count origin/main..HEAD` |
| `Fire_Varna` стейджнати пътища (Б3) | **14** (`M` places/hotels/place_categories/index.html/probe_places_fv.mjs/recall_sweep.py/test_granitsi_fixtures/test_hotels_public_bundle/test_places_public_bundle · `A` b3_g11.py/granitsi_fixtures_07.09.json/granitsi_client_probe.mjs/test_b3_gates.py · `D` granitsi_fixtures_06.09.json) | (М) `git diff --cached --name-status` |
| `Fire_Varna` мръсни НЕстейджнати | **записват се на стъпка 0** (ИА-О32); очаквани и допустими: `scratch/places_search/lot1v_v_reference_manifest.json`, `scratch/places_search/probe_out/token_parity.json` (мръсни нарочно от Б3), евентуално `scratch/places_search/karta_imot_plan_reviews_08.09.md`; **`docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` е ЧИСТ** (§20 е комитнат) | (М на стъпка 0) `git status --porcelain` |
| `Fire_Varna` приватностен гейт | `{"scope": 19, "geo_hits": [], "unreadable": [], "stray_new": []}` · **изход 0** | (М) `python gates/probe/b3_g11.py HEAD --cached` |
| **временни дървета (базова линия — ИА-О36)** | `Fire_Varna`: **2 чужди, допустими, не се пипат** — `C:/git/_wt/fv_lot1_client` (клон `lot1-client`) и `C:/Users/Petar/AppData/Local/Temp/claude/wt_fv` (detached, от 07.09; чужд път, не е на този лот); `varna_3d`: **0**. Записват се на стъпка 0 като БАЗОВА ЛИНИЯ; критерият на стъпка 18 е „нула НОВИ дървета спрямо нея“, не „нула дървета“ | (М) `git worktree list` в двете репа |
| **`Д1` на диска ДНЕС** | **ПРОСЛЕДЕН и чист** (последен комит по пътя — Архитект), заглавен ред „редакция 4“ + сверките от ИА-О30 нататък (растат с всяка проверка; последната е в червен ред 10); **0 двойки координати** (М, ИА-О33, с командата на ИА-19 в поправения ѝ вид: `grep -nE -e '\b4[23][.,][0-9]{3,}\b' -e '\b2[78][.,][0-9]{3,}\b'` → изход 1). Байтовете и броят редове **не се пинват** — Д1 расте с всяка сверка (ИА-О32) | (М на стъпка 0: `git ls-files --error-unmatch`, `git status --porcelain -- <Д1>` = празно, ИА-19 = изход 1) |

> Заб.: HEAD-ът на Fire_Varna мърда сам (Петър приема доклади за хидранти). **Нито един гейт и нито един STOP тук не пинва HEAD на Fire_Varna** — само блобове, адресирани по правилото на червен ред 13.

### 1.2 · Базата на `varna_3d` е ЗЕЛЕНА (за разлика от Fire_Varna) — **девет реда**

| # | команда (М, тичана днес) | изход | какво печата |
|---|---|---|---|
| **Н1** | `python src/qa_quarters_signed.py data/quarters_signed.geojson --registry C:/git/Varna_buildings/config/quarter_registry.json` | **0** | 20 проверки C1–C20; печата и „решени по подписан файл (`d494d4d256ae…`, решение 9): `kochmar→mladost`, `vazrazhdane4→mladost`“ |
| **Н2** | `python src/qa_p8a.py --ledger scratch/refactor/_addr/lot1v_locations_375_p8a_07.09.json` | **0** | „присвояването по подписани граници е годно за комита на Петър“ |
| **Н3** | `python src/qa_fire_varna_places_export.py` | **0** | „артефактът може да тръгне към Fire_Varna“ |
| **Н4** | `python src/qa_place_zone_aliases.py` | **0** | „passes — every quarter alias is a verbatim string of the registry“ |
| **Н5** | `python src/qa_fire_varna_export.py` | **0** | „артефактът може да тръгне към Fire_Varna“ |
| **Н6** | `python src/qa_fire_varna_location_isolation.py` | **0** | „глобалната стълба, майсторът и чуждата тема са непокътнати“ |
| **Н7** | `python -m unittest discover -s tests` | **0** | „Ran **16** tests … OK“ |
| **Н8** | `python src/qa_fire_varna_m6.py` | **3** | „ЧАКА ПОДПИС: **8** реда с `pending_signature` — билдът е здрав, но ПУБЛИКАЦИЯ и `--freeze` са забранени“ (М: `qa_fire_varna_m6.py:41` (докстрингът) и `:489` (проверката) „блокът `pending_signature` брои същото · 8 срещу 8“) |
| **Н9** | `python scratch/granitsi/zone_alias_negatives.py` | **0** | харнесът на отрицателните проби на `qa_place_zone_aliases` (П13) |

**Изход 3 на Н8 е НАСЛЕДЕН и НЕ Е ЧЕРВЕН** (Ч `qa_fire_varna_m6.py:526` `sys.exit(3)`). Записва се като база; смяна на числото 8 = STOP.

**Н7 се сверява по ИМЕНА, не по брой.** Комит К4 добавя `tests/test_quarters_signed_imot_fixtures.py` — след него броят расте. Пинът е: **изход 0 И шестнайсетте днешни идентификатора са сред пуснатите, всичките зелени.** Снемат се със **стабилна** команда (не с `grep " ... ok"` — `-v` печата докстринга вместо името при 6 от 16-те, мерено):

```
python -c "import unittest;s=unittest.defaultTestLoader.discover('tests');
ids=lambda t:[y for x in t for y in (ids(x) if isinstance(x,unittest.TestSuite) else [x.id()])];
print(chr(10).join(sorted(ids(s))))"
```

М днес → **16 идентификатора**, всичките в `test_quarters_signed_fixtures.SignedQuartersFixtures`: `test_artifact_carries_the_signed_shape`, `test_build_refuses_a_moved_vertex_in_a_union_part`, `test_build_refuses_a_moved_vertex_in_the_frozen_set`, `test_build_refuses_a_moved_vertex_in_the_mladost2_face`, `test_build_refuses_a_resolution_against_the_au5_witness`, `test_build_refuses_a_resolution_for_a_code_without_a_dispute`, `test_build_refuses_any_other_output_name`, `test_district_disputes_are_named_and_resolved_by_the_signed_file`, `test_every_negative_fixture_actually_fails`, `test_gate_is_green_on_the_signed_artifact`, `test_gate_refuses_a_resolution_for_a_code_without_a_dispute`, `test_gate_refuses_a_resolutions_file_against_the_au5_witness`, `test_rebuild_is_byte_identical`, `test_the_resolutions_input_is_the_signed_file`, `test_the_union_is_the_geometry_petar_signed`, `test_union_recipe_is_declared_honestly`.

### 1.3 · Схемата на v1 (какво трябва да възпроизведе v2, ключ по ключ)

Блобът: **301 917 B**, `sha256 = c1551d248e6450025664a4338cf7cb8a7e84e8275eba2ffd53b4a41990e871b3` (М).

- **Върхови ключове (3):** `_meta`, `features`, `type`.
- **`_meta` — 36 ключа** (М, поименно): `artifact_class, attribution, canonicalization, counts, d1_schema, decisions_author, decisions_based_on, decisions_blob_before_attribution, decisions_commit, decisions_path, decisions_sha256, decisions_sha256_rule, decisions_sha256_worktree, declared_deviations, district_resolutions, district_witness_agkk, district_witness_disputes, excluded, generated_by, inputs, licences, lot, method_licence, parent_child, parent_child_without_polygon, plan, precision_floor_m, precision_m_by_method, publication, registry_pending, rows_without_feature, signature, source_terms, union_crs, union_recipe, union_witness`.
- **Feature — 3 ключа:** `geometry`, `properties`, `type`. **`properties` — 41 ключа** (М): `approval_digest, approved_at, approved_by, attribution, basemap, chosen_reason, chosen_source, code, confirmed_at, confirmed_by, district, district_src, drawn_at, drawn_by, fetched_at, geometry_origin, imported_from, kind, license, license_label, license_terms, method, name, note, parent, parent_display, parent_geometry_check, precision_m, raw_geometry_sha256, screenshot_sha256, signed_by, source, url, version, viewport, visible_layers, witnesses, wm_id, wm_ids, wm_polygon_sha256, wm_title`.
- **Нулираните полета.** М поименно: **седемте** на `_meta.d1_schema.nulled_on_import` (`drawn_by, drawn_at, basemap, viewport, visible_layers, screenshot_sha256, approval_digest`) са `null` по **всичките 60**; `wm_ids` е непразно по **1**; `wm_id` — по **58**; `wm_title` — по **60**.
- `_meta.precision_floor_m = 50`; `_meta.artifact_class = "signed_by_plan_signature"`; `_meta.counts` = **12** полета; `_meta.licences` = **2**; **`_meta.parent_child` = 11 двойки** `{child,parent}` (М, поименно: `abatko→kk_konstantin_elena`, `druzhba→asparuhovo`, `kaisieva→vladislavovo`, `kokardzha_generic→izgrev_kv`, `mladost1→mladost`, `mladost2→mladost`, `rozova_dolina→asparuhovo`, `saltanat→morska_gradina`, `vazrazhdane1/2/3→vazrazhdane`); `properties.parent` е непразно само по **6** Feature-а (М) — **двата източника на родство НЕ съвпадат и авторитетът за родство е `_meta.parent_child`** (виж следващата точка); `_meta.parent_child_without_polygon = []`; `_meta.registry_pending` = **16**.
- **`_meta.rows_without_feature` на v1 има ДВЕ кофи — `alias_of` (1 ред: `vayalar → sredna_traka`) и `deferred` (6 реда)** (М, дословно). v2 ще носи същия ключ с **друг набор кофи** — това е обявено отклонение ИА-О21.
- **`_meta.excluded` носи ЕДИН ред** (М, дословно): `vinitsa_sever` · `display "с.о. Виница-север"` · `kind "с.о."` · `registry_action` „сутрешен ход на Петър: деактивиране на кода в `quarter_registry.json` — регистърът НЕ се пипа от изпълнителя“ · `why` „Петър (07.09): «Виница-север го изключваме» …“. **В черновата с решенията същият код е ред 6295 с `include = "да"` и `kind_registry = "вз"`, 294 сгради, 0,303 km² (М) — тоест v2 го връща. Обратът е решение Р17 и се записва, не се премълчава** (ИА-О15).
- **Геометрия:** 60 × `Polygon`. **Площ (М, EPSG:32635):** сбор **66,886 km²**, обединение **61,597 km²**, **51 застъпващи се двойки > 1 m²**.
  **ПОПРАВЕНО ЧИСЛО (кръг 3 обори „47“):** от 51-те двойки **11 са двойка от `_meta.parent_child`** (М, поименно: `abatko–kk_konstantin_elena`, `asparuhovo–druzhba`, `asparuhovo–rozova_dolina`, `izgrev_kv–kokardzha_generic`, `kaisieva–vladislavovo`, `mladost–mladost1`, `mladost–mladost2`, `morska_gradina–saltanat`, `vazrazhdane–vazrazhdane1/2/3`) → **40 застъпвания извън родство**. Числото „47“ се получава само ако родството се чете от `properties.parent` (само 6 непразни, 4 от които в застъпване) — **изворът на родството в целия този план е `_meta.parent_child`, изрично и навсякъде.**
- **Покритие (М):** v1 покрива **68 314 / 80 510 = 84,85 %** от адресните точки (при обявеното в §1.5 правило за развръзка).

### 1.4 · Замразеният снапшот на imot.bg (входът)

| | стойност | котва |
|---|---|---|
| `varna_polygons_08.09.geojson` | 175 736 B · sha256 `77b405640000a304ec5dca94f4865cc1b5a4d552c0edfc4847fa4663e6662a1f` | (М) |
| `varna_dictionary_08.09.json` | 6 274 B · sha256 **`feba76e3c816a3e313d8ace0dcb373b4a1ee4ecb9b6356a122254d4846d22fdf`** · списък от **98** `{id, name}` | (М) |
| `_meta` на снапшота | 12 ключа: `crs, dictionary_endpoint, dictionary_missing_polygons, dictionary_n, dictionary_sha256, fetched, list_endpoint, missing, n, raw_kept, source, tools`; `fetched = "2026-09-08"`, `n = 95`, `missing = []`, `dictionary_n = 98` | (М) |
| **`_meta.list_endpoint` — ДОСЛОВНО** | **`https://www.imot.bg/obiavi/prodazhbi/grad-varna (95 with adverts, counts)`** — **с опашката**. **`url` на всеки от 95-те Feature-а = ЧИСТИЯТ адрес без опашката** (`source_terms.imot_bg.url` е друго поле — страницата с условията, Р10) (`https://www.imot.bg/obiavi/prodazhbi/grad-varna` — §3.Г.2 ред `url`, ИА-8, ИА-О30 т. 2; ИА-О38 поправя този ред, който ИА-О30 беше пропуснал) | (М) |
| **`_meta.dictionary_sha256` ВЪТРЕ в снапшота** | **(хешът на суровия отговор е частен провенанс и стои само в снапшота, не в този проследен план — ИА-О31)** — **НЕ е** sha на файла-речник. Това е хешът на **суровия отговор** на `dictionary_endpoint` (`https://api.imot.bg/mob_api/dictionary/search/1?town=5`), а суровият отговор е „scratchpad (private), not in the repo“ (Ч `_meta.raw_kept`). **Авторитетът за гейта е sha-то на ФАЙЛА (`feba76e3…`); вграденото число се записва като свидетел, който никой не сверява.** | (М) |
| Feature-и | **95**, всички `Polygon`, по 1 пръстен | (М) |
| свойства на Feature | `center, fetched, n_adverts, n_pts, name, provenance, raion, raion_num, slug, src`; `provenance = {endpoint, advert_id, raw_sha256, raw_bytes, fetched_local, field}` — **низът `raw_response_sha256` се среща 0 пъти**; `properties.fetched` има **една** стойност `"2026-09-08"` | (М) |
| **знаци след точката — ПОПРАВЕНО** | **7 640 числа** в `coordinates`, хистограма **{5: 7, 6: 53, 7: 642, 8: 6938}** — **кофа с 2 знака НЯМА**. (Контрола: 7 640 / 2 = **3 820** = сборът на `n_pts` в тялото с решенията.) *(Редакция 3 казваше „2, 5, 6, 7, 8 · 1/19/195/653/6963“ — нито едно от петте числа не съвпада; оборено и заменено.)* | (М) |
| и двата файла | **игнорирани** — `.gitignore:64:/scratch/boundary_gallery/` | (М) `git check-ignore -v` |

**Върху 90-те доставяни клетки (не върху 95-те полигона; ИА-О42 — премерено наново, конвейерът възпроизвежда старите 79-клетъчни числа до цифра), след канонизацията по D1:** **0 невалидни преди поправка** · **0 невалидни след канонизация** · **0 застъпващи се двойки с площ > 0** · **3 476 отсечки с кратност** (= 2 689 различни + 787 споделени; различни върхове **2 607**) · **2 689 различни отсечки** · **787 споделени отсечки** · дължина на ръба (EPSG:32635, **върху съвкупността от 3 476 отсечки — споделените броени два пъти; върху различните статистиката е друга и не се пинва**): медиана **94,7 m**, средна **128,4**, p10 **35,2**, p90 **250,2** (перцентилите по nearest-rank; при линейна интерполация p90 = 249,8 — ИА-О43; Изпълнителят ги записва в ОТЧЕТА по nearest-rank, никой гейт не ги съди — ИА-О44), max **1 484,3**, min **8,71** · площ **87,025 km²** (М). *(За 79-те клетки на редакция 4 бяха: 2 849 = 2 137 + 712, върхове 2 061, медиана 96,5, площ 74,615 — история.)*

### 1.5 · Адресните точки — маската, затворена и сверена

`Fire_Varna/data/address_rows.json`: **80 510** реда, `coordinate_order = "[lat,lng]"`, 5 073 137 B, sha256 **`2a4766bd474f34817081e2d4e49b0ab2abbe54080c809b0402e079e1b9a0904a`** (М, върху **блоба** на HEAD).

| клас (затворен набор от 3: `outside_all_95`, `excluded_cell`, `deferred_cell`; плюс двата отчетни `in_one_cell`, `tie_break` — ИА-О35) | брой | котва |
|---|---|---|
| **`in_one_cell`** — в точно една клетка „да“ (канонизирани) | **78 235** (ИА-О42; за 79 клетки беше 77 210) | (М) |
| **`tie_break`** — на общия ръб на две клетки „да“ | **1** — една точка в район Приморски **(координата в частния файл)**, между **5055 `chaika_kk`** и **5053 `alen_mak`** | (М) |
| **М1 · `outside_all_95`** — извън всичките 95 полигона | **2 274** | (М) |
| **М2 · `excluded_cell`** — вътре в клетка, **която v2 изключва: петте села с `kind_registry = null`** (ИА-О42: петте „обекта“ вече са клетки — техните 1 025 точки са в `in_one_cell`) | **0** — петте села **0** | (М) |
| **М3 · `deferred_cell`** — вътре в **отложена клетка С ВИД** (ИА-О42: няма такива — шестте са клетки; кофата остава дефинирана и празна) | **0** | (М) |
| **сбор** | 78 235 + 1 + **2 274** (= 2 274 + 0 + 0) = **80 510** | (М) |

> **Дефиницията, поправена след кръг 3 и ИА-О42:** `excluded_cell` покрива **петте села** — точно клетките, чиито редове отиват в `_meta.excluded` по §3.Г.1; `deferred_cell` покрива клетките в `rows_without_feature.deferred` — след ИА-О42 **нула**. Числово и двете са 0 (селата хващат 0 точки), но **дефиницията в §1.5 и дефиницията в §3.Г.1 вече са една и съща**, иначе ИА-11 не може да бъде написан еднозначно. Полето `mask.in_cell` от отрицателната половина на стар ИА-11 **не съществува** и е махнато; закотвените имена са `in_one_cell`, `tie_break`, `outside_all_95`, `excluded_cell`, `deferred_cell`, `address_rows_sha256`, `outside_note`, `tie_break_rule`.

- **Покритие на доставката: 78 236 / 80 510 = 97,18 %** (ИА-О42: петте „обекта“ са клетки; за 79 клетки беше 77 211 = 95,90 %). Това е числото „97,2 %“ в ADR 011 (Ч `011_kartata_imot.md:47`) — К8 го пише точно.
- **Дифът v1 → v2 по адресна точка — ПОПРАВЕН и възпроизводим.** Кръг 3 обори двойката „49 926 / 17 453“: тя не следва от никое обявено правило, защото **4 517 от 80 510-те точки попадат в ≥ 2 полигона на v1** (М), а правило за развръзка на страната v1 нямаше. **Обявено правило (Р20): за v1 печели азбучно най-малкият код; за v2 — най-малкият `raion_num` (Р6).** При него (М, ИА-О42 за 90 клетки): същата дума **49 397** · сменена **18 232** · изгубена **685** · спечелена **10 607** · без дума и преди, и сега **1 589**. Сборът е 80 510; 49 397 + 18 232 + 685 = **68 314** (v1) ✔; 49 397 + 18 232 + 10 607 = **78 236** (v2) ✔. *(За 79 клетки: 49 397 / 17 982 / 935 / 9 832 / 2 364 — история.)* Правилото се записва дословно в `_meta.diff_v1_v2.tie_break_v1` и се преизмерва от ИА-13.
- Речникът на ADR 011 за М1 („порт, промзона, село, грешна координата“, Ч `011:47`) се записва в `_meta.mask.outside_note` **дословно**; той е бележка, не клас, и никой гейт не го съди.

### 1.6 · Тялото с решенията (черновата днес, **след комит 7556227**)

`Fire_Varna/scratch/places_search/imot_decisions_1_2026-09-08_draft.json` — **проследен и чист**. Стабилна ревизия: `git log -1 --format=%H -- <път>` → **`755622726bac3e8b977f9fd484b2e2d164661267`** (`Claude Architect`). **Блоб `sha256 = 8626ad43aea7a02d132f1e16ccc3b755ab782a87773d55f83eab51fc348fcaf8`, 51 275 B** (М).

- Върхови ключове: `_meta`, `rows`, `virtual_parents`. **`_meta` — 9 ключа** (М): `what, snapshot, dictionary_n, polygons, rule, signed_by, privacy, kind_registry_rule, decisions_open`.
- **98 реда, 16 ключа** (М): `area_km2, buildings, cls, code, code_src, district_agkk, imot_name, include, kind, kind_note, kind_registry, n_adverts, n_pts, parent, raion_num, slug`. **Няма ключ `display`.**
- `include` (М): **79 „да“ · 11 „отложено (0 сгради)“ · 5 „решение“ · 3 „решение (няма полигон…)“**.
- **`_meta.kind_registry_rule` — дословно** (Ч): „imot вид → вид от затворената таблица `KIND_NORM` (`varna_3d/src/fire_varna_locations.py:150-154`): `кв.→кв` · `ж.к./подрайон→жк` · `район→кв` (**ДА СЕ ПОДПИШЕ**; алтернатива `мр`) · `м-т→м-т` (клас locality) · `к.к.→кк` · `в.з.→вз` · промишлена зона→`пз` · Метро/Завод Дружба→`зона` · с./обект → без вид (не влизат в v2)“. **Котвата `:150-154` е ГРЕШНА** (М: `git grep -n KIND_NORM` → `src/fire_varna_locations.py:152`; таблицата е `:152-155`) — поправя се в самото тяло с разлика **Х9**, не само в плана.
- **`_meta.decisions_open` — 7 отворени решения** (Ч, дословно): „район→кв или мр“ · „5 обекта: точка/alias, не клетка“ · „3 района без полигон: чакане/ръчно/no_boundary“ · „11 отложени (0 сгради)“ · „**13** без район по АГКК → район по решение“ · „precision_m за плочката“ · „низът за атрибуция“. **Числото 13 е ГРЕШНО** (М: редовете без `district_agkk` са **16** = 98 − 82) — поправя се в тялото с разлика **Х10**. **Всичките седем са покрити от §9** (Р15/Р16 · Р3 · Р4 · Р7 · Р14 · Р8 · Р10).
- **`kind_registry` по 98-те реда** (М): `кв 34 · м-т 31 · жк 10 · пз 5 · кк 4 · зона 2 · вз 2 · **null 10**`.
- **`kind_registry` по 79-те „да“** (М): `кв 32 · м-т 27 · жк 10 · кк 4 · пз 3 · зона 2 · вз 1` = 79. **Нула стойности извън `KIND_NORM`.** (Суровият imot-вид по същите 79 е `м-т 27 · район 19 · кв. 13 · ж.к./подрайон 10 · зона 5 · к.к. 4 · в.з. 1` — 46 от тях са извън `KIND_NORM`; **точно това преобразуване е решено в самия файл, не се измисля от Изпълнителя**.)
- **Десетте реда с `kind_registry = null` в ЧЕРНОВАТА** (М, поименно, с довода от `kind_note`): пет **обекта** — 5841 `biznes_park_varna`, 6183 `biznes_hotel`, 5847 `grand_mol`, 6134 `letishte`, 5864 `pristanishte_varna` („обект: не е клетка → без вид“) — **ИА-О42: Петър ги включва като клетки, Х13 им дава `kind_registry = "кв"` (същото правило като „район→кв“, Р15: райони около ориентир, както Операта и Автогара)**; пет **села** — 740 `zvezditsa`, 751 `kazashko`, 755 `kamenar`, 764 `konstantinovo`, 821 `topoli` („село: няма вид в `KIND_NORM` → отложено“). **В Ф7 null вид остава само при петте села ⇔ ред в `_meta.excluded` с `delivered: false`.**
- **Трите НЕдоставяни реда С ВИД** (М, след ИА-О42): без полигон — 5848 (кв), 6294 `malka_chaika` (кв в черновата → `м-т` по Х11), 5866 (пз). Шестте отложени с вид в черновата — 6293 `pz_topoli`(пз), 6296 `vz_zvezditsa`(вз), 6299 `krushkite`, 6300 `lazur`, 6301 `orehcheto`, 6302 `pripek` (м-т) — **стават клетки по думата на Петър (Р7 обърнато, Х12)**. 90 + 5 + 3 = **98** ✔
- **`virtual_parents` е ОТДЕЛЕН списък от 5**, всеки `{code, name, kind, children}`; **нито един от петте не е ред в `rows`** (М). Децата са **17**, всичките `include = "да"` (М): `kv_levski` 4 (`bazar_levski, levski1, levski2, tsveten_kvartal`) · `vladislavovo` 5 (`vladislav_varnenchik1, vladislav_varnenchik2, kaisieva, pz_planova, bokluk_tarla`) · `vazrazhdane` 4 · `mladost` 2 · `pobeda_group` 2 (`konfuto, pobeda`).
- **Петте `virtual_parents` НЯМАТ `kind_registry`**; полето им `kind` е прозата „виртуален родител = обединение на децата“ (М). Дупката се запълва **без измисляне**: четирите вече съществуват в **подписания регистър** — `kv_levski → кв`, `vladislavovo → кв`, `vazrazhdane → жк`, `mladost → жк` (М: и регистърът, и v1 дават същото); само `pobeda_group` няма ред никъде и е **решение Р16** (предложение `кв` — двете му деца са `кв`).
- **97 различни кода за 98 реда** — `"TBD"` два пъти: 5848 „Електроразпределение Варна“ и 5866 „Северна промишлена зона“ (М). **След Х4: 98 различни в `rows`; с петте виртуални родителя — 103 различни в цялото тяло; доставяни — 95 (ИА-О42).**
- **Приватност (М, върху БЛОБА `8626ad43…`, по имена на ключове):** `advert_id, raw_sha256, raw_response_sha256, points, raion_points, localid, cadnum, center, coordinates, geometry, centroid, polygon, bbox` → **по 0 попадения**; float-ове в кутията на Варна → **0**. Подниз `advert` → **98**, всичките от `n_adverts` — затова сканът е **закотвен по име на ключ**.
- **Остава производно на частната геометрия:** `n_pts` (98 реда, сбор **3 820**) и `area_km2` (**95** реда) — вж. ИА-О9 и ИБ-15.

### 1.7 · Кодовете, регистърът и районът (върху ДОСТАВЯНИТЕ 95 — ИА-О42)

**v2 = 90 клетки „да“ + 5 виртуални родителя = 95 кода** (обосновката е в §3.Г.1; ИА-О42: 79 + 5 обекта + 6 отложени с вид).

| мярка (М) | стойност |
|---|---|
| v1 ∩ v2 | **51** |
| **v1, които изчезват** | **9** — `abatko, druzhba, gorchivata_cheshma, kokardzha_generic, morska_gradina, rozova_dolina, sredna_traka, sv_ivan_rilski, trakia` |
| нови в v2 | **44** (51 + 44 = 95; 51 + 9 = 60 — двете сметки затварят; ИА-О42) |
| виртуални родители, които вече са в v1 | **4** — `kv_levski, mladost, vazrazhdane, vladislavovo` (`pobeda_group` е нов) |
| регистър | **84 записа**, `schema_version 1.1`; записите носят **15 различни ключа** (М): `aliases, aliases_note, aliases_pending_p3, children_note, display, id, kind, label_at, map_drop, map_label, osm_confirmed, osm_refused, parents, type_conflict_p3, why`; **6 записа носят `parents`** |
| **v2 − регистър** | **39** (ИА-О42: 28 + 11) — `atanas_tarla, avtogara, bazar_levski, biznes_hotel, biznes_park_varna, chataldzha, festivalen_kompleks, franga_dere, generalite, grand_mol, hei, kantara, konfuto, krushkite, lazur, letishte, levski1, levski2, lyatno_kino_trakiya, operata, orehcheto, ostrovna_pz, pobeda_group, pogrebite, pripek, pristanishte_varna, pz_topoli, slanchev_den, sportna_zala, stadion_spartak, tsentralna_poshta, tsveten_kvartal, vins_cherven_ploshtad, vladislav_varnenchik1, vladislav_varnenchik2, vz_zvezditsa, zavod_druzhba, zhp_gara, zimno_kino_trakiya` |
| **регистър − v2** | **28** — `abatko, akchelar_vinitsa_seam, borovets, botanicheska, byalata_cheshma, deli_sava, druzhba, franga_kokardzha_seam, gabena, golyama_kokardzha, gorchivata_cheshma, kokardzha_generic, malka_chaika, malka_kokardzha, mikro4, mikro8, morska_gradina, rozova_dolina, shashkana, sredna_traka, starite_lozya, sv_ivan_rilski, trakata_so, trakata_vz, trakia, vayalar, vilite, yuzhna_pz` |
| от тях **жилищни** по `RESIDENTIAL_KINDS = {"кв","жк","кк"}` (Ч `src/build_quarters_candidate.py:56`) | **4** — `druzhba` (жк), `rozova_dolina` (кв), `sv_ivan_rilski` (кв), `trakia` (кв) |
| **клетки „да“ без район от НАШИЯ слой** | **7** (М, ИА-О42: 83 от 90-те получават точно един район по представителна точка; `letishte, pz_topoli, vz_zvezditsa, krushkite, lazur, orehcheto, pripek` са извън петте района на града — и по нашия слой, и по АГКК — и получават `district = null`, `district_src = "outside_city_districts"`, Р14) |
| **редове с непразен `district_agkk`** | **82 от 98** (М) |
| **знаменателят на свидетеля — обявен точно** | **82** = редовете, които имат **И** район от нашия слой, **И** район по АГКК (М: върху 95-те полигонни реда; 12 нямат наш район, 13 нямат АГКК) |
| съгласие наш слой ↔ свидетел АГКК | **77 от 82** (върху редовете на черновата; ИА-О42 не мени знаменателя — четирите обекта с АГКК вече бяха в него) |
| разминавания | **5** — `vazrazhdane4`, `kochmar`, `so_planova` (5857), `salzitsa` (5860) — и четирите `include = "да"`, наш слой `vladislav_varnenchik`, АГКК `mladost` — плюс `pristanishte_varna` (наш `odesos`, АГКК `asparuhovo`; **клетка от ИА-О42, `delivered: true`, `resolution: "pending_petar"`**). 77 + 5 = 82 ✔ |
| съгласие само по **доставяните** клетки | **77 от 82** (ИА-О42: 90 „да“ минус осемте без АГКК — `perchemliyata` и седемте извън града; за 79 клетки беше 74 от 78) |
| вече подписани спорове | **2** — `district_resolutions_2026-09-07.json`: `kochmar → mladost`, `vazrazhdane4 → mladost` (М) |
| **нови спорове** | **2** — `so_planova` и `salzitsa` |
| редове без `district_agkk` | **16** (М, поименно): `letishte`, `perchemliyata`, шестте отложени с вид, петте села, трите без полигон. От тях „да“ след ИА-О42 са **осем**: `perchemliyata` — поименната подписана резерва вече съществува (Ч `qa_place_zone_aliases.py:74` `SIGNED_DISTRICT_RESERVE = {"perchemliyata": "primorski"}`) и получава `primorski` от нашия слой — и седемте извън града (`letishte, pz_topoli, vz_zvezditsa, krushkite, lazur, orehcheto, pripek`), които нямат район и по нашия слой → `district = null` (Р14) |
| **район на петте виртуални родителя** | по представителна точка в нашия слой: `kv_levski → primorski`, `vladislavovo → vladislav_varnenchik`, `vazrazhdane → mladost`, `mladost → mladost`, `pobeda_group → mladost` (М) |

**Подписаният вход за споровете, пиннат по БЛОБ:** `Fire_Varna/scratch/places_search/district_resolutions_2026-09-07.json` — проследен; **ревизия `e6c706dffa8eb20897e66a16510297a54465ae50`** (`Claude Executor`, 2026-09-07), **блоб `sha256 = d494d4d256aeb1e3115d01a97a422202eb7247934b2576996eb6159b169844dc`, 513 B** (М). Закотвен в `varna_3d/src/qa_quarters_signed.py:124-125` (`RESOLUTIONS_PATH`) и `:126` (`RESOLUTIONS_SIGNED_SHA256`).

**Шестият решаващ вход — слоят с петте района, който НЕ живее в git.** `district` на 88 от 95-те Feature-а идва от `C:/git/m6000_private/number_viewer/quarters_layer.geojson` (седем клетки са извън петте района — Р14, ИА-О42) (Ч `varna_3d/src/fire_varna_locations.py:135` `QUARTERS_LAYER`; Ч `src/build_quarters_signed.py:84` `DEFAULT_DISTRICTS`; филтърът е `authority == "osm"` и `display ∈ DISTRICTS`, Ч `:396-398`, а `DISTRICTS` е затворената петорка на `fire_varna_locations.py:141-147`). **М: `git -C C:/git/m6000_private log` → `fatal: not a git repository` — папката НЕ е репо, файлът няма блоб и не може да се адресира по червен ред 13.** Мярка на файла: **201 130 B, sha256 `26e39b001ce151203058d70bc53367a5b40c77c4b0dd7113cf2a7085c376cc22`**; в него `authority == "osm"` има 48 Feature-а, от които **точно 5** са с `display` от петорката (М). Затова: **пинва се по sha256 на прочетените байтове** (Р21, ИА-О23), влиза в `_meta.inputs` като шести вход и ражда дълг ИБ-17.

**Закритата таблица на видовете** (Ч `src/fire_varna_locations.py:152-155` `KIND_NORM`, 14 ключа; коментарният ѝ надслов е `:149-151`; проверката за вид извън нея е `:646`, `die()` — `:647-648`): `жк, кв, кк, со, с.о., вз, в.з., м-т, местност, махала, зона, пз, парк, мр`.

**Класът на вида** (Ч `src/fire_varna_locations.py:156-160` `KIND_CLASS`): `{жк, кв, кк, со, вз} → quarter`; `{местност, зона, пз, парк, мр} → locality`; `махала → DROP` — по НОРМАЛИЗИРАНИЯ вид: `с.о.` стига до `со` и `м-т` до `местност` през `KIND_NORM` (`:152-155`), затова доставените `с.о.`/`местност` след Х11 се класифицират чак след нормализация (ИА-О36). **Разпределението на 95-те доставяни (М, ИА-О42):** по черновата ПРЕДИ Х11 (обектите вече като `кв` по Х13) — видове `кв 40 · м-т 31 · жк 12 · кк 4 · пз 4 · зона 2 · вз 2` = 95, класове 58 quarter / 37 locality; **СЛЕД Х11 (това доставя И-А): `кв 38 · с.о. 15 · жк 14 · м-т 13 · кк 4 · пз 4 · местност 4 · зона 2 · вз 1` = 95; класове **72 quarter** и **23 locality**.** *(За 84: `кв 33 · с.о. 15 · жк 14 · м-т 9 · кк 4 · местност 4 · пз 3 · зона 2`, 66/18 — история.)* **По D6 locality НИКОГА не пълни `quarter`** — измерена последица за публикуването, работа на **И-Б** (ИБ-11), не на И-А: И-А не публикува нищо.

### 1.8 · Пиновете, които сочат v1 — изчерпателно, мерено с `git grep`

(М) `git grep -l "quarters_signed.geojson" -- .` → **13 проследени файла**; `git grep -c` → **41 реда общо** (1+2+1+1+3+3+6+2+1+2+2+2+15). Долу е таблицата по ПИН — **18 реда**.

| # | пин | котва | какво пинва |
|---|---|---|---|
| П1 | `QUARTERS_SIGNED = ROOT/"data"/"quarters_signed.geojson"` | Ч `src/fire_varna_locations.py:93` | пътят |
| П2 | `QUARTERS_SIGNED_SHA256 = "c1551d24…"`, `die()` на `:974-976` | Ч `src/fire_varna_locations.py:94` | блобът |
| П3 | `RECORDED_INPUTS` — последният елемент е `"data/quarters_signed.geojson"` | Ч `src/fire_varna_locations.py:116` | **генераторът на картата `inputs`** — `:1960-1961` пише `{p: sha256_of(ROOT/p) for p in RECORDED_INPUTS if (ROOT/p).exists()}`, а `:2079` презаписва файла |
| П4 | провенансни низове `"path": "data/quarters_signed.geojson"` | Ч `src/fire_varna_locations.py:1689`, `:1968` | пътят |
| **П5** | **докстринг** „`data/quarters_signed.geojson` — подписаните граници като съдия“ | **Ч `src/fire_varna_locations.py:467`** | текст, който след И-А ще лъже, ако не се пипне |
| **П6** | **`die(f"пинът на quarters_signed.geojson в {INPUTS.name} е {pinned}…")`** | **Ч `src/fire_varna_locations.py:975`** | съобщение на гейт, което назовава файл, различен от прочетения |
| П7 | `by_source = {wikimapia, wikimapia_union, declared_streets}` + `die(...)` | Ч `src/fire_varna_locations.py:601-618` | **`imot_bg_tiling` няма ред → `die`** |
| П8 | `SIGNED_SHA`, `EXPECT_ROWS 375`, `EXPECT_WRITTEN 201`, `EXPECT_EMPTY`, `EXPECT_FEATURES 60`, `EDGE_FLOOR_M 50`; `--signed` по подразбиране | Ч `src/qa_p8a.py:60-66`, `:296`; сверката е `sha256_of(signed_path)` на `:318-321` | v1 |
| П9 | `SIGNED`, `EXPECT_SIGNED_CODES = 60`, докстрингът „one of the 60 SIGNED codes“ | Ч `src/qa_place_zone_aliases.py:66`, `:75`, `:26` | v1 |
| П10 | `QUARTERS_SIGNED`, `LOCATION_INPUTS`; `ATTR_SRC = {"wikimapia","openstreetmap"}` и трите ѝ спътници | Ч `src/qa_fire_varna_places_export.py:84`, `:85`, `:76`, `:79`, `:81`, `:82` | v1 |
| **П11** | **`check(False, "data/quarters_signed.geojson съществува", "липсва")`** | **Ч `src/qa_fire_varna_places_export.py:547`** | текст на изхода, който след Т11 ще назовава друг файл |
| П12 | `ARTIFACT`, `features == 60`, `human_polygon == 58`, докстрингът `:12` | Ч `tests/test_quarters_signed_fixtures.py:37`, `:172`, `:175`, `:12` | v1 |
| **П13** | **`SIGNED = ROOT/"data"/"quarters_signed.geojson"`, четен на `:67`** | **Ч `scratch/granitsi/zone_alias_negatives.py:28`** — **проследен харнес за отрицателните проби на `qa_place_zone_aliases`** | v1 |
| **П14** | `"data/quarters_signed.geojson": {sha256, bytes}` **вътре в картата `inputs`** — **ВТОРО място, различно от `quarters_signed.path`** | **Ч `data/fire_varna_location_inputs.json:66`** (сверявано на Ч `fire_varna_locations.py:974-976`) | блобът + пътят |
| П15 | `"path": "data/quarters_signed.geojson"` в `quarters_signed` блока | Ч `data/fire_varna_location_inputs.json:87` | пътят |
| **П16** | **червени черти по БАЗОВО ИМЕ:** `fail("червена черта", "този лот НЕ произвежда quarters_signed.geojson")` и „този гейт не съди файл с име `quarters_signed.geojson`“ | **Ч `src/build_quarters_candidate.py:664-665`** и **`src/qa_quarters_candidate.py:432-433`** | **безвредни за И-А** — v2 запазва името в `data/`, архивът има друго име. **Нула байта в тях** |
| П17 | `"path": "data/quarters_signed.geojson"` в `_meta.inputs` на леджера p8a | Ч `scratch/refactor/_addr/lot1v_locations_375_p8a_07.09.json:19` | **исторически запис**; `qa_p8a` НЕ го чете (М: сверява се само `--signed` по `SIGNED_SHA`) |
| П18 | `!data/quarters_signed.geojson` | Ч `.gitignore:34`, с описателен блок `:24-33`, който описва **v1** | пътят + документацията над него |

**Клоновете на стария билдър/гейт, които плочката би ударила** (защо не се адаптира — §2 ИА-О7): `build_quarters_signed.py:1542` `--raw-snapshot required=True` · `:1086-1096` `sources` по `wm_id` · `:1128-1149` три пътя за Feature, четвърти няма · `:1118-1119` `declared_streets != 1 → fail` · **`:1106-1107` A14 иска `row["display"]` и **регистров** `row["kind"]` — черновата няма `display` и носи imot-вид под името `kind`, значи A14 би паднал 98 пъти** · `:1563` червената черта, а `"lot": "Б1"` е на **`:1408`** · `:115-120` `PLAN` пинва плана на Б1.
**Гейтът:** `qa_quarters_signed.py:83` `LICENCE_BY_SOURCE = {wikimapia, declared_streets, wikimapia_union}` · `:359-368` блокът C9, в който **`:360` е буквално `for source in ("wikimapia","openstreetmap")`**, `:362` са шестте ключа на `source_terms`, `:367` иска `len(_meta.licences) >= 2` · `:170-171` C1 пинва `plan.file` и `decisions_sha256` към литерали · `:325-340` C8 няма врата за код извън регистъра и **`:338-340` е ТВЪРДО червено за жилищен код без ред в решенията** · `:1198-1199` **задължителен позиционен път** + `--registry required` · `:1214-1216` `if not path.exists(): return 1` · **`:1230-1231` чете `decisions_doc["decisions"]` и слива редове по код** (всичко Ч).
**Две поправени твърдения:** (1) `qa_quarters_signed.py:920` `make_fixtures(...)` **НЕ мутира документа** — `clone()` на `:929-930` е дълбоко копие (М). (2) **Старият гейт СТРУКТУРНО не може да прочете Ф7**: `:1230` иска ключ `decisions`, а тялото носи редовете под `rows` (М) — затова доводът „двата `TBD` биха се слели мълчаливо в стария гейт“ е **невалиден** и обосновката на Х4 е пренаписана към новия гейт.

### 1.9 · Механиката на `.gitignore` във `varna_3d` (М)

- `:4` = `data/*`; `:10-11` = обяснението защо изключение вътре в игнорирана ПАПКА не работи; `:12` = `!data/known_doors.json`; `:17` = `!data/street_aliases.json`; `:23` = `*.geojson`; `:24-33` = коментарният блок „ТРЕТОТО ИЗКЛЮЧЕНИЕ“, който описва **v1**; `:34` = `!data/quarters_signed.geojson`. Общо **238 реда**.
- **Проверено:** `git check-ignore -v data/archive/x.json` → `.gitignore:4:data/*` — **папка под `data/` не може да се върне.** `docs/archive/x.md` → нищо. `docs/archive/x.geojson` → `.gitignore:23:*.geojson` — иска поименно изключение след ред 23.
- **`tests/fixtures/**/*.geojson` е ИГНОРИРАН** (М: `git check-ignore -v tests/fixtures/qa_quarters_signed_imot/x.geojson` → `.gitignore:23:*.geojson`, изход 0). **Мярка, която затваря въпроса:** `git ls-files tests/fixtures` → **само 2 файла** (`identity_rekey_base_2026-09-07.json`, `readonly_ast_negative.py`); цялата съществуваща папка `tests/fixtures/qa_quarters_signed/` (20+ `.geojson`) **не е проследена**, а репото го казва дословно: `tests/test_quarters_signed_fixtures.py:21-22` — „The fixtures are regenerated here from the tracked code, never committed (`.gitignore:23` — `*.geojson`), so the write-set of the lot stays exhaustive.“ **Следствие: фикстурите на И-А НЕ са път от write-set-а и не се комитват** (ИА-О20).
- `docs/archive/` **вече съществува и е проследен** (М: `git ls-files docs` → `docs/archive/ПЛАН_блокове_поправка_v2_2026-08-20.md`); `docs/decisions/` също (М).

---

## 2 · Обявени отклонения

| № | Отклонение | Защо |
|---|---|---|
| **ИА-О1** | **Планът v4 СЪЩЕСТВУВА** — `docs/plans/ПЛАН_Картата_imot_08.09.md`, **проследен** (комит `146beac`, с ред-статус „заменен от И-А/И-Б“ отгоре; байтове/редове не се пинват — ИА-О32) и е суровината на този под-лот. | STOP по „измерено число се разминава“ важи първо за §2. |
| **ИА-О2** | **v2 носи 84 Feature-а — 79 клетки + 5 виртуални родителя** [ОТМЕНЕНО от ИА-О42: **95 = 90 + 5** — обектите и отложените с вид са клетки по думата на Петър]. | Мерено: обединенията на 17-те деца дават **5 единични `Polygon` без вътрешни пръстени, всичките валидни**. Друго решение би оставило 17 деца със `parent`, сочещ код без геометричен носител, и четирите оцелели родителски кода щяха да изчезнат от артефакта (изчезващите щяха да станат 13, не 9). |
| **ИА-О3** | **Тялото с решенията се МЕСТИ в `varna_3d`** (`data/quarters_decisions_imot_2026-09-08.json`). Черновата във Fire_Varna **остава на място непокътната**. Решение Р1. | ADR 011 И-D3 (Ч `011:17`) днес назовава път във Fire_Varna — **това решение се обръща с ред в комит К8, не мълчаливо** (инвариант 7). |
| **ИА-О4** | **Замразеният вход СЕ КОПИРА в `varna_3d/data/`** и става проследен там (Р13). | Иначе каноничният артефакт стъпва на файл, който **никое репо не носи**. `varna_3d` е частно, а червен ред 5 изрично разрешава суровия провенанс там. |
| **ИА-О5** | **v1 се архивира в `docs/archive/`, не в `data/archive/`.** | `data/*` е ПАПКОВО изключване; връщане назад е механично невъзможно (§1.9). |
| **ИА-О6** | **Осемнайсетте пина към v1 се ОБРАБОТВАТ по §3.Б (пренасочени към архивния път, непипнати или разширени — Т1…Т15)** вместо да бъдат пребазирани към v2 (ИА-О36: „пренасочват се“ беше хлабаво — Т3/Т6/Т8/Т10/Т12 не се пипат, П16/П17 са нула байта, П18 се разширява). Пиновете по sha (`c1551d24…`, 60, 301 917 B) **не се пипат**; П16 и П17 остават с нула байта. | Така целият стълб на `varna_3d` остава ЗЕЛЕН, а v2 се съди само от собствения си гейт. |
| **ИА-О7** | **`build_quarters_signed.py` и `qa_quarters_signed.py` не се пипат нито с един байт.** Пише се НОВ билдър и НОВ гейт. | §1.8, последният блок: това не е смяна на константи, а друг конвейер — старият гейт дори не може да ПРОЧЕТЕ новото тяло (`:1230` иска ключ `decisions`). |
| **ИА-О8** | **`--diff` СЕ ДОБАВЯ — но в НОВИЯ гейт.** ADR 010 D9 (Ч `010:54`) и G15 (Ч `010:129`) са регистрирани и **важат**: v2 е точно случаят над праговете (**17 982** сменени места при праг > 2; обединената площ 61,597 → 74,615 km² = **+21,1 %** при праг > 10 % [ИА-О42: за 90 клетки **18 232** и 61,597 → **87,025** = **+41,3 %** — още по-над праговете]). Затова **ИА-13 има две половини**: без `--diff-signed` → **изход 4** (ИА-О32/ИА-О33: собствен код „иска подпис“; 2 е argparse-грешка = ИА-STOP 5), с `--diff-signed "Р18"` → 0 плюс двоен запис и пълно преизмерване. | Нищо не се отменя: G15 се **изпълнява** в новия конвейер, а ADR 010 не се пипа. |
| **ИА-О9** | **`n_pts` (сбор 3 820) и `area_km2` (95 реда) остават в проследената чернова на Fire_Varna за целия интервал И-А → И-Б.** | Днес: `advert_id`, `raw_sha256`, координати → **0 попадения** (М). Изчистването/архивирането на черновата е И-Б (ИБ-15); тук се записва като **датиран дълг**, не се премълчава. |
| **ИА-О10** | **`_meta` на v2 е ЗАТВОРЕН набор от 41 имена** = 36-те на v1 **∪** двете, които ADR 011 И-D2 изисква и които v1 няма (`dictionary_sha256`, `snapshot_date`) **∪** трите обявени нови (`diff_v1_v2`, `mask`, `registry_pending_new`). `decisions_sha256` и `source_terms` **вече са сред 36-те** (М, поименно) и не се броят два пъти. Пълният списък е в §3.Г.8. ИА-6 съди срещу **тези 41**. | Срещу 36 гейтът е червен по конструкция (ИА-13 иска `diff_v1_v2`); срещу 39 два ADR-задължителни ключа стават червени. |
| **ИА-О11** | **`_meta.lot = "И-А"`, `_meta.plan.file = "Fire_Varna/docs/plans/ПЛАН_Картата_imot_И-А_08.09.md"`.** | ИА-6 сверява ИМЕНАТА на ключовете; съдържанието е на новия лот. |
| **ИА-О12** | **ИА-11 чете `Fire_Varna/data/address_rows.json` по sha на ПРОЧЕТЕНИЯ файл, не по литерал.** | HEAD-ът на Fire_Varna мърда сам. Човешката котва (80 510 / 78 235 / 1 / 2 274 — ИА-О42; преди 77 210 / 1 / 3 299) стои в §1.5 и в отчета за Gate 2. |
| **ИА-О13** | **Изход 3 на Н8 се приема като база**, а не се „поправя“. | Ч `qa_fire_varna_m6.py:526`. |
| **ИА-О14** | **`scratch/refactor/_addr/lot1v_locations_375_p8a_07.09.json:19` НЕ се пипа.** | Подписано тяло от 07.09 и **исторически запис**; `qa_p8a` не го чете (М). Червен ред 8 забранява редакция на място. |
| **ИА-О15** | **`vinitsa_sever`: подписаното изключване на v1 се ОБРЪЩА.** v2 го доставя. Обратът стои като **ред в `_meta.excluded` със `status: "reinstated"` и `delivered: true`**, носещ дословно стария довод, номера на решението (Р17) и датата — **и в `declared_deviations[]`**. | Изпразването на `excluded[]` изтрива записа на подписано решение; червен ред 8 и §10 т. 5 забраняват точно това. |
| **ИА-О16** | **`d1_schema.nulled_on_import` расте от 7 на 10** — при извор без уикимапийски обект нулирани са и `wm_id`, `wm_ids`, `wm_title`. v1 не се пипа; v2 носи собствения си, честен списък. | „Копирай `d1_schema` дословно от v1“ прави ключа описание, което не описва артефакта. |
| **ИА-О17** | **Твърдото правило на C8 („жилищен код без ред в решенията = червено“, Ч `qa_quarters_signed.py:338-340`) в НОВИЯ гейт става „изброени поименно с довод“** за четирите (`druzhba, rozova_dolina, sv_ivan_rilski, trakia`). | Намаляване на строгостта върху подписан регистър е ОТКЛОНЕНИЕ и мястото му е тук. Старият C8 остава непокътнат и продължава да съди v1 (ИА-12). |
| **ИА-О18** | **ВСИЧКИ ефемерни пътища на лота живеят ИЗВЪН двете репа, под `%TEMP%`, и не са пътища от write-set-а.** Изчерпателно: `…/ia_build_a` и `…/ia_build_b` (двата билда за ИА-10) · `…/ia_broken/*` (счупените копия на входове и на артефакта — механики 1 и 4) · `…/ia_fixtures` (отрицателните фикстури) · `…/ia_builder_nd.py` (нарочно недетерминираният вариант на билдъра, половината на ИА-10) · `…/ia_docs/*` (счупените копия за ИА-19 и изнесеният блоб `d1_head.md` за стъпка 2) · `…/ia_neg_fv` и `…/ia_neg_v3` (двете временни git-дървета) · `…/tmp*` на Ф3 (`TemporaryDirectory`, чисти се сам — ИА-О35). М: `%TEMP%` = `C:\Users\Petar\AppData\Local\Temp`. Всичко се маха на стъпка 18 и се сверява с `git worktree list` в **двете** репа **срещу базовата линия на стъпка 0** (ИА-О36) + `git status --short`. **Дърво не надживява ползвателя си** (ИА-О39, правилото на стъпка 13). Извън списъка, но известни и безвредни (ИА-О36): `scratch/granitsi/_zone_alias_neg/` на Н9/Н9′ (`zone_alias_negatives.py:31`, игниран по `.gitignore:77`) — пише се и на стъпка 0, не е редакция на проследен файл. | Кръг 3: ИА-STOP 1 и 13 спираха Изпълнителя върху собствената му машинария, защото ИА-О18 покриваше само дървото. Сега покрива **всяка** ефемерна пътека. |
| **ИА-О19** | **ОТПАДА (ИА-О32):** `docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` е ЧИСТ — §20 е комитнат от Архитекта (`0304a3c` и следващите). **К9 носи само новия §21.** Ако на стъпка 0 файлът се окаже мръсен, хънкът се показва дословно и К9 НЕ го носи (той е чужд ред) — ИА-STOP 2 не се задейства от него. | Иначе `git status --short` след К9 „изчиства“ чужд ред, който никой не е обявил. |
| **ИА-О20** | **Отрицателните фикстури НЕ са път от write-set-а и НЕ се комитват.** „Ф4“ отпада като файл; номерацията Ф1…Ф10 остава непроменена, за да не се разчита таблицата наново. Фикстурите се раждат в `%TEMP%/ia_fixtures` от Ф2 и се съдят от Ф3. | М: `.geojson` под `tests/fixtures/` е игниран (`.gitignore:23`), нито един съществуващ фикстурен `.geojson` не е проследен, и `tests/test_quarters_signed_fixtures.py:21-22` казва точно това. Комит на `.geojson` фикстури би искал ПЕТИ ред в `.gitignore` — файл, който Т1 обявява за пипнат ВЕДНЪЖ и вече е в К2 — и би бил геометрия с ръката на Изпълнителя срещу D16. |
| **ИА-О21** | **`_meta.rows_without_feature` на v2 носи ДРУГ набор кофи от v1** — v1 има `alias_of` + `deferred` (М, дословно), v2 има `deferred` + `no_boundary`. | Един и същ ключ с различна структура е скрита схемна смяна; влиза поименно в `declared_deviations[]` и в §3.Г.1. |
| **ИА-О22** | **Ф2 приема позиционния път като НЕЗАДЪЛЖИТЕЛЕН (`nargs="?"`)** и в режим `--decisions-diff` (ИА-16) работи без артефакт. (Частта за „Д1 се записва след подписа“ **отпада** — ИА-О32: Д1 е проследен и чист, без координати; подписът е ред-статус от Архитекта, К1.) | Образецът `qa_quarters_signed.py:1198` прави пътя задължителен; при пренасяне ИА-16 щеше да пада с argparse-грешка (изход 2), която ИА-STOP 3 чете като червено. |
| **ИА-О23** | **Слоят с петте района се пинва по sha256 на прочетените байтове (`26e39b00…`, 201 130 B), не по блоб.** | М: `C:/git/m6000_private` не е git репо. Решаващ вход извън `_meta.inputs` противоречи на D10 — затова той Е шестият вход, а невъзможността да се версионира е записана като дълг ИБ-17, не като мълчание. Решение Р21. |
| **ИА-О24** | **Обобщените производни (площи, брой върхове/отсечки, хистограма на ръба, Hausdorff, хешове на частни файлове) СТОЯТ в трите проследени документа на Fire_Varna.** Обхватът на ИА-7 (данни) и на ИА-19 (проза) е обявен така: **ИА-7 гледа само файлове с данни, ИА-19 гледа само координати в проза.** | Червен ред 5 забранява геометрия, суров провенанс и **координати**; агрегатът не е нито едно от трите. Че дупката „производни в проза“ е реална, а не теоретична, е измерено (ИБ-4/ИБ-18) — но затварянето ѝ иска правило И гейт, а те са И-Б. Тук се обявява, не се премълчава. |
| **ИА-О30** | **Сверка на Архитекта след кръг 5 (08.09 нощ).** Шест поправки, всяка отбелязана на мястото си с „ИА-О30“: (1) `--privacy-scope <път>` влиза в CLI-договора на Ф2 (§3.А), защото ИА-7 го ползва; (2) `url` в артефакта = **чистият** адрес `https://www.imot.bg/obiavi/prodazhbi/grad-varna` — описателната опашка на `_meta.list_endpoint` не е URL (§3 ред `url`, ИА-8, Р10); (3) `_meta.source_terms.imot_bg` = **три** ключа `{url, recorded_on, position}` — отклонение от шесткльчовия образец C9 на v1, защото за imot.bg не е правена работа по условията и никой не е отварял страницата; ИА-8 съди трите (Р10 дава алтернативата с шест само по изрична дума на Петър); (4) записите в `_meta.excluded` носят петорката на v1 `{code, display, kind, registry_action, why}` + `delivered`/`status`, без ключ `reason` (ИА-17) **[наборът беше разширен с `district_witness_note` от ИА-О31 т. 2 / ИА-О32 т. 10 и стеснен обратно от ИА-О43 — ИА-17 носи окончателния: петорка + `delivered` + подмножество на {`status`}]**; (5) ИА-19 хваща и българската десетична запетая и има две отрицателни половини (точка и запетая); (6) ИА-19 → ИА-STOP 17 при изход 0 на `grep` (полярността е записана изрично). Числата в §1.4, §1.5, §1.7, Х5 и §5 остават тези на редакция 4; Р15/Р17 казват какво се преизмерва при „мр“ / „не“. [ОТМЕНЕНО от ИА-О42: всички са преизмерени за 90 клетки] | Петър подписва редакцията с тези шест; кръгове 1–5 на опровергателите са в `scratch/places_search/karta_imot_plan_reviews_08.09.md`. |
| **ИА-О31** | **Втора сверка на Архитекта (08.09 нощ, след Astra S40 и Кими К39).** (1) Хешът на суровия отговор на речника е махнат от §1 (частен провенанс не стои в проследен план); (2) §3.Г.1 записите в `_meta.excluded` = петорката на v1 + `delivered`/`status` (+ `district_witness_note` за пристанището [ОТМЕНЕНО от ИА-О42/ИА-О43: пристанището е клетка, ключът не съществува]), без `reason`; (3) §3.Г.8 и Д2: `source_terms.imot_bg` = ТРИ ключа навсякъде; (4) §3 ред `license/license_terms/license_label/attribution` носи дословните стойности; (5) ИА-19: полярността е една навсякъде — изход 0 = намерена координата = ИА-STOP 17, изход 2 = ИА-STOP 5, изход 1 = чисто; (6) стъпка 0 не пуска Н7 (тестът регенерира фикстури в репото) и не изисква стария хънк на Д3; стъпка 3 съди `!`-правилото и `ls-files --error-unmatch`, не изхода на `check-ignore`; (7) К6 (Петър) предхожда К5 (четците към архива); (8) стъпка 12 доказва, че доставеният файл е байт-равен на прогон a (`cmp`); (9) Р4 е обявено отклонение от ADR 010 D4 (`no_boundary` без Feature) с амандамент в Д2; ИА-14 праща четирите жилищни кода без Feature в `rows_without_feature.deferred[]` **[ОТМЕНЕНО от ИА-О32 т. 4: те са в `_meta.registry_pending`]**; (10) „1 025“ са СГРАДИ по тялото, адресните точки ги мери ИА-11 **[ОТМЕНЕНО от ИА-О33: колоната `buildings` брои адресни редове — 1 025 Е броят адресни точки, пинът се връща]**; (11) ИБ-19 (присъда за условията преди публикуване) и ИБ-20 (скан за сурови хешове в прозата) добавени в §11. | Astra S40 и Кими К39 са в `scratch/places_search/adresi_astra_S40_08.09.md` и `adresi_kimi_K39_08.09.md`; всяка поправка е отбелязана на мястото си с „ИА-О31“. |
| **ИА-О32** | **Трета сверка на Архитекта (след финалната проверка 2; пространството се затваря на ИА-О32 — [ОТМЕНЕНО: авторитетът е червен ред 10]).** (1) `Д1` е проследен и чист — К1 = ред-статус от Архитекта, Изпълнителят не пише в Д1 (червен ред 1, ИА-О22, стъпка 2); (2) §1.1: HEAD, ahead, мръсните и байтовете на Д1/предшественика са ПРОМЕНЛИВИ — записват се на стъпка 0, не се пинват; ИА-О1 и ИА-О19 поправени; (3) §4 стъпки 14/15 разменени: К6 (Петър) преди К5; (4) ИА-14: четирите жилищни кода без Feature са в `_meta.registry_pending`, не в `deferred[]` (инвариантът 98 = 79 + 10 + 6 + 3 остава) [ОТМЕНЕНО от ИА-О42: 98 = 90 + 5 + 0 + 3]; (5) Н7′ само в `%TEMP%/ia_neg_v3` преди счупването; (6) червен ред 10 затворен на ИА-О32 и ИБ-20 [ОТМЕНЕНО: ИА-О35]; препратките ИА-О25/ИА-О28 → ИА-О31; (7) хешът на суровия отговор махнат и от §3.Г.4; (8) ИА-13 (а) → изход 4; (9) ИА-STOP 11 по критерия `ls-files --error-unmatch` за Ф5–Ф8; (10) ИА-17 — точният набор от ключове допуска `status` и `district_witness_note` [ОТМЕНЕНО от ИА-О43: само `status`]; (11) Ф1 получи CLI-договор; (12) `declared_deviations[]` носи и отклонението от D4 (Р4), и смяната на смисъла на `url`. | Финалната проверка 2 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md` („И-А финална проверка 2“). |
| **ИА-О33** | **Четвърта сверка на Архитекта (след финалната проверка 3 — две лещи, 7 блокера + 15 бележки; пространството се затваря на ИА-О33 — [ОТМЕНЕНО: авторитетът е червен ред 10]).** (1) Предшественикът в заглавието: проследен, `146beac`, байтове/редове не се пинват; (2) ИА-13 (а) = **изход 4** на всичките места (ИА-О8, ИА-STOP 3, Р18) — 2 е argparse-грешка = ИА-STOP 5; (3) ИА-19: командата е с два `-e` образеца **без знак тръба** — старият запис с екранирана тръба беше литерална тръба в ERE и гейтът не можеше да падне (М: старата команда дава изход 1 върху ред с координата; поправената — изход 0 върху ред с точка и върху ред със запетая в счупено копие под `%TEMP%`, и 0 попадения в трите проследени `.md`); §1.1 премерено наново с нея; (4) §3.Д Д1: Изпълнителят не пише в Д1; (5) Д3/К9: файлът е чист, К9 носи само §21; (6) §3.Г.8: `declared_deviations[]` = **16 записа поименно** (+ `Р4`, `Г.2-url`, `Р17`, `Р10`), ИА-6 ги съди по `id`; `_meta.plan` = `{file, rev, blob_sha256}`; (7) ИА-11/Р3: пинът **1 025 адресни точки** се връща — колоната `buildings` на черновата брои адресни редове (Ч `imot_decisions_draft_08.09.py:87-93`), ИА-О31 т. 10 отменено, етикетът е „покритие по адресни точки“; (8) ИА-18 тича само в `%TEMP%/ia_neg_v3`; Ф3 строи v2 сам от пиннатите входове; Н7′ на стъпка 13 = командата „ПРЕДИ“ (дървото е от HEAD преди К5/К6); (9) **ИА-STOP 18** за реда К6 → К5; (10) Х2 → ИА-О31 т. 9 / Р19; (11) Ф1 се вика с всичките осем флага **[ОТМЕНЕНО от ИА-О35 т. 2: девет флага = осем входа + `--out`]**; `--draft-rev`/`--decisions-rev`/`--resolutions-rev` по подразбиране = червен ред 13; (12) Х8: `plan_rev` + `plan_body_sha256` = блобът на Д1 при К1, сверяван от ИА-16; (13) §2 е една таблица — ИА-О30/31/32 са редове в нея, оборените им точки носят маркер „ОТМЕНЕНО“; (14) К1 = 53 знака; червен ред 10: `grep` → два файла (планът и Д3); кръговете на лещите се цитират с име („Кими К37“, „Astra S40“); (15) стъпка 2 без екранирана тръба. | Финалната проверка 3 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md` („И-А финална проверка 3“); всяка поправка е отбелязана на мястото си с „ИА-О33“. |
| **ИА-О34** | **Пета сверка на Архитекта (след финалната проверка 4 — две лещи, 2 + 4 блокера, 35 бележки; пространството се затваря на ИА-О34 — [ОТМЕНЕНО: авторитетът е червен ред 10]).** (1) ИА-STOP 11: единственият критерий е `ls-files --error-unmatch`; `git check-ignore -v` мълчи с изход 1 за път в индекса (М) — `!`-правилото се показва с `--no-index` като мярка; (2) стъпка 2: блобът на Д1 се изнася в `%TEMP%/ia_docs/d1_head.md` и е единственият операнд на израза — `grep` с файлови операнди пренебрегва stdin (М); §5 казва „след К1“; (3) G29: котвата е `амандамент_2:107`, построен в `qa_night_pins.py:206-242` с отрицателна половина `:402-444`, остава в сила (предшественикът `:31`) — Х3 и ИБ-14 поправени; (4) **Х11**: 25 клетки в подписания регистър вземат вида на регистъра дословно (16 сменят класа quarter/locality) — A14 на стария билдър се запазва, ИА-14 го съди, Р16 го обхваща, Х7/Х9 го записват в тялото; дифът на ИА-16 = **единайсет** разлики [ОТМЕНЕНО от ИА-О42: тринайсет]; (5) Ф1: редът при fail е „build-fail <n>: <причина>“ — без латинско `A`; (6) регистърът е **седмият** вход в `_meta.inputs` (Ф1 го чете; ИА-9 съди седем; Ф3 строи от седемте) **[ОТМЕНЕНО от ИА-О35 т. 2 за броя: ИА-9 съди ОСЕМ, Ф3 строи от осемте — регистърът остава седмият]**; (7) ИА-16 тича и на стъпка 9; в режим `--decisions-diff` на стъпка 6 Ф7 се чете от диска; (8) `_meta.plan.file` (както във v1/ИА-О11), не `path`; (9) стъпка 18: дървото се вдига наново за ИА-18/Н7′ и се маха преди сверката; (10) ИА-2: 2 849 върха по отсечки = 2 137 + 712, различните са 2 061 [ОТМЕНЕНО от ИА-О42: 3 476 = 2 689 + 787, върхове 2 607]; (11) ИА-10: часовник с микросекунди; (12) стъпки 15/16/17 назовават писането на Ф9/Ф10/Д2/Д3; (13) червен ред 13 без остарял литерал за HEAD; котвата на Н8 → `qa_fire_varna_m6.py:41`/`:489`; (14) ИБ-4: координатите в трите проследени `.md` записа изчистени, обхватът назован. | Финалната проверка 4 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md` („И-А финална проверка 4“); всяка поправка е отбелязана на мястото си с „ИА-О34“. |
| **ИА-О35** | **Шеста сверка на Архитекта (след финалната проверка 5 — две лещи, 5 + 4 блокера, 29 бележки).** (1) §3.В заглавието: ЕДИНАЙСЕТ разлики [ОТМЕНЕНО от ИА-О42: ТРИНАЙСЕТ]; (2) **v1 е ОСМИЯТ вход** (`--diff-base`, `role: diff_base`, блоб `c1551d24…` при `6486b48…`) — без него Ф1 не може да напише `_meta.diff_v1_v2`; ИА-9 съди осем; блобово (`git show rev:path`) се сверяват само Ф7, резолюциите и диф-базата, **по байтове** — снапшот, речник, `address_rows`, слоят, регистърът (Ф2 няма repo/rev флагове за тях; ИА-STOP 14 забранява дърво във Varna_buildings) — механика 1 за двата, червен ред 13 допълнен; (3) Х11 „днес“ = `м-т` ×19, `кв` ×3, `вз` ×1, `зона` ×1, `пз` ×1; (4) §1.7 и ИБ-11: разпределението СЛЕД Х11 — `кв 33 · с.о. 15 · жк 14 · м-т 9 · кк 4 · местност 4 · пз 3 · зона 2`, 66 quarter / 18 locality (М) [ОТМЕНЕНО от ИА-О42: `кв 38 · с.о. 15 · жк 14 · м-т 13 · кк 4 · пз 4 · местност 4 · зона 2 · вз 1` = 95, 72/23]; (5) Р20: три правила, „по реда на файла“ = азбучно (v1 е подреден по код), старата двойка махната; (6) §1.1: без екранирани тръби в клетките; (7) Ф2: `--out` отпада, двете семантики на `--decisions-path` обявени; (8) ИА-О18: `TemporaryDirectory` на Ф3; (9) ИА-4: сборът не се прави от отпечатаните площи; (10) червен ред 10: латинското `A` е забранено като собствено пространство, не като цитат; (11) §1.5: трите затворени класа поименно; (12) Р16/§8: `sveti_nikola` — единствената клетка, която губи кварталната дума, влиза в извадката (7 места); (13) историческите „затваря се на ИА-О32/33/34“ с маркер. | Финалната проверка 5 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md` („И-А финална проверка 5“); всяка поправка е отбелязана на мястото си с „ИА-О35“. |
| **ИА-О36** | **Седма сверка на Архитекта (след финалната проверка 6 — две лещи, 3 + 2 блокера, 28 бележки; цялата мярка и всичко от ИА-О35 възпроизведени до цифра).** (1) **Базова линия за временните дървета**: Fire_Varna днес носи две чужди (`_wt/fv_lot1_client`, `%TEMP%/claude/wt_fv` от 07.09) — записват се на стъпка 0 (§1.1), стъпка 18 и ИА-STOP 1 съдят „нула НОВИ дървета“, чуждите не се пипат; (2) маркери [ОТМЕНЕНО] върху ИА-О34 т. 6 (седем → осем входа) и ИА-О33 т. 11 (осем → девет флага); (3) Х2 амандира и клаузата за `kind` в `_meta.rule` (същият ключ; броят разлики е в §3.В — след ИА-О42 тринайсет); (4) Г.7 т. 8: празна ревизия при `--diff-base` към стейджнатия архивен път на стъпка 13 — допустима само за пресните билдове; (5) ИА-16 командата с `--decisions-path` (и repo/rev на 13/18); механики на трите отрицателни половини на ИА-16, на ИА-12 и на стъпка 10 назовани; (6) ИА-19 тича и на 13; (7) Д2: амандаментът на D4 казва и къде живее `deferred`; (8) ИА-О6 — „обработват се по §3.Б“, не „пренасочват се“; (9) §1.4: ръбът е върху отсечките с кратност (споделените два пъти), различните дават друга статистика [числата 2 849/2 137 ОТМЕНЕНИ от ИА-О42: 3 476/2 689]; ИА-4: закръглените площи не се сумират; (10) §1.7: `с.о.`/`м-т` стигат до класа през `KIND_NORM`; ИБ-4: ~80 файла, променливо; (11) ИА-О18: `_zone_alias_neg/` на Н9 назован (игниран); (12) §8: 8 кода в 7 точки [ОТМЕНЕНО: 9 кода в 7 точки по ИА-О37, после 11 кода в 9 точки по ИА-О42]; §1.1 ред Д1 и червен ред 10 без остарели диапазони; заглавието брои и финалните проверки [ОТМЕНЕНО от ИА-О38: заглавието не брои — броят е в §2 и червен ред 10]. | Финалната проверка 6 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md` („И-А финална проверка 6“); всяка поправка е отбелязана на мястото си с „ИА-О36“. |
| **ИА-О37** | **Осма сверка на Архитекта (след финалната проверка 7 — две лещи, 1 + 2 блокера, 10 бележки; цялата мярка отново възпроизведена до цифра).** (1) Ф10: **7** от 28-те нови кода [ОТМЕНЕНО от ИА-О42: от 39-те] са деца на виртуален родител (М), не 17 — 17 са всичките деца, 10 от тях вече са в регистъра; (2) §8 т. 4: **9 кода в 7 точки** [ОТМЕНЕНО от ИА-О42: 11 кода в 9 точки]; (3) ИА-19 с абсолютни пътища (тича и от varna_3d на 13/18); (4) §1.4: „2 849“ са отсечки с кратност, различните върхове 2 061 [числата ОТМЕНЕНИ от ИА-О42: 3 476, върхове 2 607]; (5) стъпка 13 маха дървото след Н7′/ИА-18 [ОТМЕНЕНО от ИА-О39: всеки ползвател вдига и маха своето]; (6) ИА-9: отрицателна половина и за речника (осем входа — осем половини). | Финалната проверка 7 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md` („И-А финална проверка 7“); всяка поправка е отбелязана на мястото си с „ИА-О37“. |
| **ИА-О38** | **Девета сверка на Архитекта (след финалната проверка 8 — леща „изпълнимост“ ГОДНО; леща „мерки“ с 2 блокера, 14 бележки; цялата мярка отново възпроизведена).** (1) §1.4: `url` в артефакта = ЧИСТИЯТ адрес — редът, който ИА-О30 т. 2 беше пропуснал; (2) заглавието и §1.1 ред Д1 вече не броят сверките (броят е в §2 и червен ред 10); (3) ИА-10 изваден от диапазоните на Ф2 (§3.А, §5 увод, стъпка 9); (4) ИА-2: етикетът „отсечки с кратност“; (5) Г.7 т. 4: `address_rows` по байтове, блобът свидетел; (6) уводът на Ф2 без числото „три“; (7) ИА-18/стъпка 13: прясно дърво за ИА-18/Н7′, механиките 2/3 вдигат и махат своите, всичко се маха в края на стъпката, `ia_neg_fv` назован; (8) ИА-16 на стъпка 9 с блобовата тройка; (9) котвата `:156-157`. | Финалната проверка 8 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md` („И-А финална проверка 8“); всяка поправка е отбелязана на мястото си с „ИА-О38“. |
| **ИА-О39** | **Десета сверка на Архитекта (след финалната проверка 9 — леща „мерки“ ГОДНО; леща „изпълнимост“ с 1 блокер, роден от ИА-О38 т. 7).** (1) **Едно правило за еднократните дървета** (стъпка 13, важи и на 18): пътищата `ia_neg_v3`/`ia_neg_fv` не са постоянни — всеки ползвател (Н7′, ИА-18, всяка половина по механика 2/3) вдига, ползва и маха своето, по реда на §5, никога две на един път (М: второ `worktree add` → изход 128); ИА-18 и ИА-О18 подравнени; (2) спирачки по вида си: `cmp` на стъпка 12 = ИА-STOP 6, стъпка 13 = STOP 4 само за регресия на Н′; (3) стъпка 3 назовава изворната папка на Ф5/Ф6; (4) Ф3: `--diff-base` назован буквално за 13 и 18; (5) червен ред 12: „може да е ≠ блоб“, за address_rows днес равни (М); (6) §1.4: `url` на всеки Feature, не `source_terms.imot_bg.url`; (7) §2: маркери върху ИА-О36 т. 12 и ИА-О37 т. 5, старите маркери сочат червен ред 10 без номер. | Финалната проверка 9 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md` („И-А финална проверка 9“); всяка поправка е отбелязана на мястото си с „ИА-О39“. |
| **ИА-О40** | **Преименуване по думата на Петър (08.09 обед): префиксът `imot_bg_` пада от имената на двата замразени файла.** Ф5 = `data/varna_polygons_08.09.geojson`, Ф6 = `data/varna_dictionary_08.09.json`; същите имена в игнорираната изворна папка, в Т1 (двата `!`-реда), К2, ИА-1, §1.4, Г.7 т. 1–2, червен ред 5 и стъпка 3. **Съдържанието не е пипано: байтовете и sha (175 736 B / `77b40564…`; 6 274 B / `feba76e3…`) остават пиновете.** Изворът остава записан вътре в снимката: `_meta.source` (описателният низ „imot.bg — полигоните на районите, както ги чертае самият сайт“), `_meta.list_endpoint` и провенансът по всеки от 95-те Feature-а — името на файла не е носител на провенанс; `imot_bg_tiling` е ИЗХОДНОТО поле `properties.source` на v2 (§3.Г.2), не входното (Astra S41-3). Промяна на текст, не на мярка; проверява се с `grep` (0 стари имена в трите проследени документа и в игнорираната папка) и от Astra (механика) преди изпълнението. | Думата на Петър: „искам да махнеш imot_bg от името на файла“; „одобрявам плочките — дай ги на Astra и на Кими за проверка и да ги вкарваме“. |
| **ИА-О41** | **Присъдите на лещите върху плочките (08.09 обед): Astra S41 — ГОДНО за старт (запис `scratch/places_search/tiles_astra_S41_08.09.md`); Кими К40 — ГОДНО със задължителни промени (запис `tiles_kimi_K40_08.09.md`).** Внесено: (1) **Х11 = 26 реда** — 25-те клетки плюс `malka_chaika` (ред `no_boundary`; регистърът дава `м-т`, черновата `кв`) — правилото „код в регистъра → видът на регистъра“ важи за всеки ред на Ф7 (Х7, Р4, Р16, §1.6 подравнени; ИА-16 остава единайсет разлики [ОТМЕНЕНО от ИА-О42: тринайсет] — Х11 е една разлика с 26 реда); (2) Р7: „0 сгради“ = 0 адресни точки в НАШИЯ адресен регистър; селата са извън покритието му, не са празни (обяви 108/185/61) — низът в Ф7 не се пипа; (3) Ф10 носи бележките на Кими за регистъра (franga_dere/Р23 на регистъра, evksinograd без why, зона/пз несистемни, dolna_traka display, chataldzha override, ostrovna_pz ↔ yuzhna_pz — въпрос към регистъра, не към плочката: v1 няма полигон за Южна ПЗ, М); (4) Кими К40-3 потвърждава изключването на петте обекта и настоява дупката от 1 025 точки да е ЗАПИСАНА като известна (тя е: `_meta.mask.excluded_cell`, ИБ-12) [ОТМЕНЕНО от ИА-О42: обектите са клетки; `excluded_cell` = 0, точките са в `in_one_cell`]; К40-1/К40-6 приемат кодовете от регистъра и децата на родителите; (5) страницата за подпис брои Х11 и по редовете без полигон. Нищо в мярката, гейтовете, реда на комитите и write-set-а не се мени. | Astra S41-3 поправи една дума в ИА-О40 (входното `_meta.source` е описателен низ). Двете лещи са с различни въпроси (механика / данни), както е уговорено. |
| **ИА-О42** | **Думата на Петър (08.09 ~14:30): „понеже търсачката ни има възможност за търсене на геокоординати директно … мисля че е добра идея да ги включим“ → „давай“ за двете групи.** **Петте „обекта“ и шестте отложени с вид стават клетки; петте села остават вън; трите без полигон остават имена.** Правило: **всеки полигон с вид е клетка**. v2 = **90 клетки + 5 родителя = 95 Feature-а**; 98 = 90 + 5 + 0 + 3. Ф7 получава **Х12** (`include → "да"` за 11 реда) и **Х13** (`kind_registry = "кв"`, `cls = "клетка"` за петте обекта — районите около ориентир по правилото на Р15); ИА-16 = **тринайсет** разлики. **Всичко преизмерено (М, конвейер, който възпроизвежда 79-клетъчните числа до цифра):** 0 застъпвания; 3 476 = 2 689 + 787 отсечки, върхове 2 607; ръб медиана 94,7 / средна 128,4 / p10 35,2 / p90 250,2 / max 1 484,3 / min 8,71; клетки 87,025 km² (сбор = обединение), Feature-и 95,318 / 87,025; маска 80 510 = 78 235 + 1 + 2 274 (excluded_cell 0, deferred_cell 0); покритие **97,18 %**; диф 49 397 / 18 232 / 685 / 10 607 / 1 589 (v1 68 314, v2 78 236); v1∩v2 51, изчезващи 9, нови 44; в регистъра 56, нови кода 39, registry_pending 28; видове след Х11 `кв 38 · с.о. 15 · жк 14 · м-т 13 · кк 4 · пз 4 · местност 4 · зона 2 · вз 1`, 72 quarter / 23 locality; **7 клетки без район** (извън петте района и по нашия слой, и по АГКК) → `district = null`, `district_src = "outside_city_districts"`, поименно в Р14 и ИА-15; свидетелят 82 реда, 77 съгласия; спорове 5, всичките `delivered: true` (`pristanishte_varna` е клетка); `excluded` = 5 села + `vinitsa_sever` = 6 записа; Р2/Р3/Р7/Р8/Р14/Р17/Р18/Р20/Р21, §1.4–1.7, §3.Г, §5 (ИА-1/2/3/4/6/8/11/13/14/15/16/17), §8, §11 подравнени; ИА-О2/О8/О12 с маркер (ИА-О43 добави маркерите върху ИА-О32/34/35/36/37/41). Кими К40-3 (обектите вън) е надживяно от думата на Петър — записано, не премълчано. | Мярката: `scratchpad/ia_o42_measure.py` (сесията) — старата и новата конфигурация една до друга; страницата за подпис показва 11-те реда като „включвам (Петър 08.09)“. Следва финална проверка (две лещи) преди К1. |
| **ИА-О43** | **Сверка след финалната проверка 12 (две лещи: всички числа на ИА-О42 възпроизведени до цифра от суровите файлове; блокерите са текстови).** (1) Р17: `vinitsa_sever` е **шестият** запис в `excluded`; (2) Г.1: никой запис не носи `district_witness_note`; (3) Г.8 `declared_deviations` текстът за `url`: 95; (4) Х5: двата брояча са 28 и 39; (5) К7: „39 codes outside the registry“ (дължината остава 57); (6) **Г.5: трета стойност `district_src = "outside_city_districts"` с `district = null` за седемте клетки извън слоя** — Ф1 не измисля най-близък район; (7) §2: маркери [ОТМЕНЕНО от ИА-О42] върху ИА-О32 т. 4, ИА-О34 т. 4/10, ИА-О35 т. 1/4, ИА-О36 т. 3/9, ИА-О37 т. 1/2/4, ИА-О41 т. 1; (8) §1.4: перцентилите по nearest-rank (p90 250,2; линейна интерполация дава 249,8). | Финалната проверка 12 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md`; всяка поправка е отбелязана на мястото си с „ИА-О43“. |
| **ИА-О44** | **Сверка след финалната проверка 13 (леща „изпълнимост“ ГОДНО; леща „съгласуваност“ с 4 текстови блокера).** (1) Х9 сменя и клаузата „обект → без вид“ в `_meta.kind_registry_rule` на „обект → кв“ (същият ключ, пак тринайсет разлики); (2) маркер върху ИА-О41 т. 4 (1 025 → `in_one_cell`); (3) маркери за `district_witness_note` върху ИА-О31 т. 2 / ИА-О32 т. 10, ИА-О30 т. 4 казва окончателния набор; (4) маркер върху последното изречение на ИА-О30; (5) Г.8: `registry_pending` е `[{code, why}]`; (6) Г.2: `license_label` на родителите — „обединение на райони <номера на децата>“; (7) Х2 носи и клаузата за `include`; (8) ИА-11: тройката е от класове, не ключове; (9) веригата маркери за §8 сведена до едно; (10) перцентилите са в отчета, не в Ф2; (11) „Р23 / Р27–Р37 на регистъра“ — квалифицирани. Нито едно число, гейт, команда или ред на комит не се мени. | Финалната проверка 13 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md`; всяка поправка е отбелязана на мястото си с „ИА-О44“. |
| **ИА-О45** | **Сверка след финалната проверка 14 (леща „съгласуваност“ ГОДНО; леща „изпълнимост“ с 5 реални дупки).** (1) **Д4 има производител**: Ф2 получава `--review-html <път>` и я пише на стъпка 13 от артефакта, `--diff` и `--address-rows` — никакъв скрипт във Fire_Varna, никакъв нов ефемерен път; (2) **v2 е ред в write-set-а — Ф11** (`data/quarters_signed.geojson`, К6, Петър); червен ред 10: Ф1 … Ф11; (3) **Х10 покрива целия `_meta.decisions_open`** — редове 2 и 4 (обектите и отложените) не тръгват като „отворени“ в подписаното тяло; (4) **Х1 сменя и `_meta.what`** („ЧЕРНОВА … подписва Петър“ → „КАНОНИЧЕН … подписан от Петър на 08.09.2026“) — една разлика, тринайсет остават; (5) `registry_pending.why` на `malka_chaika` казва истината (ред `no_boundary` в тялото). Нито едно измерено число не се мени. | Финалната проверка 14 е в `scratch/places_search/karta_imot_plan_reviews_08.09.md`; всяка поправка е отбелязана на мястото си с „ИА-О45“. |

## 3 · Write-set с котви (изчерпателен — път извън тази таблица = ИА-STOP 1)

### 3.А · `varna_3d` — нови файлове

| # | път | какво | комит |
|---|---|---|---|
| **Ф1** | `src/build_quarters_signed_imot.py` | **НОВ билдър** — правилата в §3.Г. **CLI-договор (ИА-О32):** `--tiling <снапшот>` · `--dictionary <речник>` · `--decisions <Ф7>` · `--districts <слой с петте района>` · `--address-rows <път>` · `--resolutions <district_resolutions_2026-09-07.json>` · `--registry <quarter_registry.json>` · **`--diff-base <v1>`** (ИА-О35: без него Ф1 няма как да напише `_meta.diff_v1_v2`; на стъпка 8 = `data/quarters_signed.geojson`, на 13/18 = `docs/archive/quarters_signed_v1_2026-09-06.geojson` — същият блоб `c1551d24…`) · `--out <път>`; всички задължителни (девет флага = осем входа + `--out`); без позиционни аргументи; изход 0/1 (1 = вътрешен fail с ред „build-fail <n>: <причина>“ — без латинско `A`, червен ред 10; ИА-О34) | **К4** |
| **Ф2** | `src/qa_quarters_signed_imot.py` | **НОВИЯТ ГЕЙТ** — реализира **ИА-1 … ИА-9, ИА-11, ИА-13 … ИА-17** (ИА-10 е командата на Ф1 + `cmp`, ИА-О38), всяка проверка написана ТУК наново, нищо внесено от `qa_quarters_signed.py`. **CLI-договорът е обявен изчерпателно долу.** | **К4** |
| **Ф3** | `tests/test_quarters_signed_imot_fixtures.py` | **реализира ИА-18**: ражда фикстурите в `%TEMP%/ia_fixtures`, прогонва всяка с нейния `--only`, настоява за изход ≠ 0 **по своя номер**; проверява, че гейтът е зелен върху истинския v2 и че двата билда са байт-еднакви — **като САМ строи v2 с Ф1 в `TemporaryDirectory` от осемте пиннати входа на §3.Г.7 (Ф5/Ф6/Ф7 са в HEAD от К2/К3; ИА-О34/ИА-О35; `--diff-base` = `data/quarters_signed.geojson` на стъпка 13 — дървото е от HEAD преди К5/К6 — и `docs/archive/quarters_signed_v1_2026-09-06.geojson` на 18; ИА-О39); НЕ чете `data/quarters_signed.geojson` (ИА-О33: на стъпка 13 v2 още не е в HEAD)** | **К4** |
| ~~Ф4~~ | — | **ОТПАДА** (ИА-О20): фикстурите са `.geojson`, игнорирани от `.gitignore:23`, и по установената практика на репото **никога не се комитват**. Номерът се оставя празен, за да не мърда останалата номерация. | — |
| **Ф5** | `data/varna_polygons_08.09.geojson` | **байтово копие** на снапшота (175 736 B, `77b40564…`) | **К2 · Петър** |
| **Ф6** | `data/varna_dictionary_08.09.json` | **байтово копие** на речника (6 274 B, `feba76e3…`) | **К2 · Петър** |
| **Ф7** | `data/quarters_decisions_imot_2026-09-08.json` | **каноничното подписано тяло** (Р1) — блобът `8626ad43…` плюс изброените в §3.В **тринайсет** разлики и нищо друго (ИА-О42) | **К3 · Петър** |
| **Ф8** | `docs/archive/quarters_signed_v1_2026-09-06.geojson` | v1, **`git mv`**, байт за байт (301 917 B, `c1551d24…`) | **К6 · Петър** |
| **Ф9** | `docs/archive/РЕГИСТЪР_архив.md` | регистърът на архива: `стар път → нов път → комит → защо → кой го чете сега`; първи ред v1, втори ред „черновата с решенията — архивиране в И-Б“, трети ред „`granitsi_decisions_2/3/4` — отменени, физически архивирани в И-Б“ | **К5** |
| **Ф10** | `docs/decisions/ПРЕДЛОЖЕНИЕ_регистър_imot_08.09.md` | **само предложение** за `Varna_buildings/config/quarter_registry.json`. Схемата на записа е **измерената**: `id, display, kind, aliases, why` **плюс `parents`** — точно **7** от 39-те нови кода са деца на петте виртуални родителя (М, ИА-О37/ИА-О42: `bazar_levski, konfuto, levski1, levski2, tsveten_kvartal, vladislav_varnenchik1, vladislav_varnenchik2`; другите 10 от 17-те деца вече имат ред в регистъра — старото „17“ броеше всички деца), а йерархията в регистъра се носи от `parents` (М: 6 записа днес го носят). Съдържа: **39**-те нови кода (ИА-О42: 28 + 11-те, които Петър включи), **28**-те регистрови кода без Feature, отделно четирите жилищни, деветте изчезващи, **бележките на Кими К40 за твоята ръка (ИА-О41): `franga_dere` — вид `местност` по Р23 на регистъра, свързан с `pending_p3`; `evksinograd` — регистровият `кв` е без `why`; `zpz` (`зона`) срещу `pz_metro`/`pz_planova` (`пз`) и `yuzhna_pz` (`зона`) — несистемни видове за промишлените зони в самия регистър; `dolna_traka` — display без префикс при `с.о.`; `chataldzha` — подписано в `block_overrides` (Р9, 30.07) без собствен запис; `ostrovna_pz` (imot: Островна ПЗ, 1,745 km², островът между каналите) срещу регистровия `yuzhna_pz` (Южна ПЗ, 162 парцела, БЕЗ полигон в v1 — М) — дали са едно място не се измерва от нашите данни и остава въпрос към регистъра, не към плочката**, и **бележка за `malka_chaika`** (регистърът го дава като `м-т`, тялото му дава `kind_registry = "кв"`; редът е `no_boundary`, затова разминаването днес не удря гейт). **Нищо не се прилага.** | **К7** |
| **Ф11** | `data/quarters_signed.geojson` — **v2, главният изход на лота** (ИА-О45: липсваше като ред в write-set-а, макар стъпки 8/12 и К6 да го произвеждат и комитват) | **95 Feature-а** (90 клетки + 5 родителя) по §3.Г; строи се от Ф1 в `%TEMP%/ia_build_a` на стъпка 8, копира се на този път на стъпка 12 (байт-равен на прогон a — `cmp`), сверява се от §5 на 13/18; **старият v1 се мести с `git mv` в Ф8 в същия комит** | **К6 · Петър** |

**CLI-договорът на Ф2 (обявен, защото отрицателните половини на ИА-9 и ИА-16 минават през флаговете му):**
`path` — **позиционен, `nargs="?"`** (ИА-О22) · `--registry` · `--tiling <снапшот>` · `--dictionary <речник>` · `--address-rows <път>` · `--districts <слой>` · `--decisions-repo/--decisions-rev/--decisions-path` (**Ф7 във varna_3d**; **ИА-О34: в режим `--decisions-diff` на стъпка 6 Ф7 се чете от диска по `--decisions-path`, защото блоб още няма — блобовата тройка важи от стъпка 9 нататък**) · `--resolutions-repo/--resolutions-rev/--resolutions-path` (**`district_resolutions_2026-09-07.json` във Fire_Varna**) · `--draft-repo/--draft-rev/--draft-path` (**черновата във Fire_Varna, само за `--decisions-diff`**; **ИА-О33: `--draft-rev`, `--decisions-rev` и `--resolutions-rev` по подразбиране = `git -C <repo> log -1 --format=%H -- <path>` — червен ред 13; подават се изрично само в отрицателните половини, където сочат дангълинг комит във временно дърво**) · `--diff <път до v1>` · `--diff-signed <низ>` · `--only <ИА-номер>` · `--make-fixtures --fixtures-dir <път>` · `--decisions-diff` · `--privacy-scope <път>` (обхватът на ИА-7: по подразбиране Ф7; повтаря се за всеки допълнителен файл с данни) · **`--review-html <път>` (ИА-О45: пише Д4 — пробата за окото — от артефакта, `--diff <v1>` и `--address-rows`: 95-те Feature-а върху подложката, 2 274-те точки извън всички полигони, деветте изчезващи имена върху старите им места и 685-те адреса, които губят дума; вика се САМО на стъпка 13 с път Д4; геометрия и точки в него са позволени, защото папката е игнорирана — червен ред 5)**. **`--out` отпада (ИА-О35: никой не го викаше; отчетът на гейта е stdout, който Изпълнителят записва дословно).** **`--decisions-path` има две семантики, обявени: без `--decisions-repo/--decisions-rev` = път на диска (стъпка 6); с тях = път в репото за `git show` (от стъпка 9).** **(Сверка на Архитекта след кръг 5, ИА-О30: флагът `--privacy-scope` беше ползван от ИА-7, но липсваше тук.)**
**Без `--resolutions-*` отрицателната половина на ИА-9 не може да се напише: гейтът би прочел истинския файл и би върнал 0** (кръг 3, БЛОКЕР).

### 3.Б · `varna_3d` — точкови редакции (**архивният път е един и същ навсякъде**: `docs/archive/quarters_signed_v1_2026-09-06.geojson`)

| # | файл:ред | днес (Ч) | става |
|---|---|---|---|
| **Т1** | `.gitignore` — **ЕДНА редакция, един блок**: коментарният блок `:24-33` се пренаписва (той описва v1), и **непосредствено след `:34` се добавят четирите реда** | — | `!data/varna_polygons_08.09.geojson` · `!data/varna_dictionary_08.09.json` · `!data/quarters_decisions_imot_2026-09-08.json` · `!docs/archive/quarters_signed_v1_2026-09-06.geojson`. **Четирите стоят СЛЕД `:23` и СЛЕД `:4`, значи бият и двете правила** (§1.9). **ПЕТИ ред за `tests/fixtures/` НЕ се добавя** (ИА-О20). **Файлът се пипа ВЕДНЪЖ и се комитва ВЕДНЪЖ — в К2, ръката на Петър.** |
| **Т2** | `src/fire_varna_locations.py:93` | `QUARTERS_SIGNED` | архивният път + коментар, който назовава интервала И-А→И-Б |
| **Т3** | `src/fire_varna_locations.py:94` | `QUARTERS_SIGNED_SHA256` | **НЕ СЕ ПИПА** |
| **Т4** | `src/fire_varna_locations.py:116` (`RECORDED_INPUTS`) | `"data/quarters_signed.geojson"` | архивният път — **иначе `:1960-1961` ще пренапише картата `inputs` и ще разсинхронизира П14** |
| **Т5** | `src/fire_varna_locations.py:1689`, `:1968`, **`:467` (докстринг), `:975` (текстът на `die`)** | низът на пътя | архивният път **на четирите места** |
| **Т6** | `src/fire_varna_locations.py:601-618` | `by_source` | **НЕ СЕ ПИПА** → И-Б |
| **Т7** | `src/qa_p8a.py:296` | `--signed` по подразбиране | архивният път |
| **Т8** | `src/qa_p8a.py:60-66` | шестте константи | **НЕ СЕ ПИПАТ** → И-Б |
| **Т9** | `src/qa_place_zone_aliases.py:66` и докстрингът `:26` | `SIGNED` | архивният път (текстът „the 60 SIGNED codes“ получава „(v1, архивиран — И-Б го пребазира)“) |
| **Т10** | `src/qa_place_zone_aliases.py:75` | `EXPECT_SIGNED_CODES = 60` | **НЕ СЕ ПИПА** → И-Б |
| **Т11** | `src/qa_fire_varna_places_export.py:84` **и текстът на `:547`** | `QUARTERS_SIGNED`; `check(False, …)` | архивният път на двете места |
| **Т12** | `src/qa_fire_varna_places_export.py:76`, `:79`, `:81`, `:82` | `ATTR_*` | **НЕ СЕ ПИПАТ** → И-Б |
| **Т13** | `tests/test_quarters_signed_fixtures.py:37` и докстрингът `:12` | `ARTIFACT` | архивният път (`:172` = 60 и `:175` = 58 **остават** — те съдят v1) |
| **Т14** | **`scratch/granitsi/zone_alias_negatives.py:28`** (четен на `:67`) | `SIGNED` | архивният път — **без този ред отрицателните проби на `qa_place_zone_aliases` тихо започват да четат v2 и спират да падат** |
| **Т15** | `data/fire_varna_location_inputs.json` — **ДВЕТЕ места**: ключът `:66` в картата `inputs` и `:87` `quarters_signed.path` | `"data/quarters_signed.geojson"` | архивният път на двете места; **`sha256`, `bytes`, `features`, `commit`, `artifact_class`, `decisions_sha256` НЕ се пипат.** Файлът е под `data/` → **комитва го Петър (К6)** |

**`src/build_quarters_signed.py`, `src/qa_quarters_signed.py`, `src/qa_fire_varna_m6.py`, `src/qa_fire_varna_export.py`, `src/build_quarters_candidate.py`, `src/qa_quarters_candidate.py`, `scratch/refactor/_addr/lot1v_locations_375_p8a_07.09.json` — нула байта.**

**Как се пенсионира старият билдър: не се пенсионира — пенсионира се ВХОДЪТ му.** `build_quarters_signed.py` продължава да строи v1 байт за байт; `qa_quarters_signed.py` продължава да го съди по позиционен път; `tests/test_quarters_signed_fixtures.py` продължава да е зелен, само сочи архивния път. **Това е гейтът за нерегрес ИА-12.** За доставка старият билдър не се вика никога повече.

### 3.В · `Ф7` (подписаното тяло) срещу черновия блоб `8626ad43…` — **точно ТРИНАЙСЕТ разлики, нула други** (Х11 от ИА-О34; Х12/Х13 от ИА-О42)

Гейтът ИА-16 чете черновия блоб (`--draft-repo C:/git/Fire_Varna --draft-path scratch/places_search/imot_decisions_1_2026-09-08_draft.json`; ревизията се намира с `git log -1 --format=%H -- <път>`), сверява sha == `8626ad43aea7a02d132f1e16ccc3b755ab782a87773d55f83eab51fc348fcaf8`, 51 275 B, и настоява дифът да е **точно този списък**:

| # | ключ | днес | става | защо |
|---|---|---|---|---|
| **Х1** | `_meta.signed_by` **и `_meta.what`** (ИА-О45: една разлика — подписът) | `"pending — Петър"` · „ЧЕРНОВА на каноничния файл … предлага Claude Architect, подписва Петър; без геометрия“ | `"Petar"` · „КАНОНИЧЕН файл с решенията за плочката на imot.bg (ADR 011 И-D3) — предложен от Claude Architect, подписан от Петър на 08.09.2026 по ПЛАН И-А §9; без геометрия“ | подписът; иначе подписаното тяло се нарича чернова |
| **Х2** | `_meta.rule` (клаузите за `district` И за `kind` — ИА-О36) | „…`district` = свидетел АГКК AU5 по центроид **(G23)**“ · „kind по префикса на imot“ | „`district` = НАШИЯТ слой от пет района по представителна точка (`district_src = fire_varna_district`); АГКК AU5 е **независим свидетел** по амандамента в ADR 011 И-D5 (Р19); замяна само с ред в подписан `district_resolutions`; всяко разминаване се записва поименно“ · „kind: код в подписания регистър → видът на регистъра дословно (Х11); иначе по префикса на imot (Р15)“ · „include: да (клетка) / отложено (селата — без вид) / решение (няма полигон)“ (ИА-О44, след Х12) — една разлика, един ключ, за да не тръгне каноничното тяло с остаряла клауза (ИА-О36) | `src/qa_district_official.py` **не съществува** (М) — G23 сочи скрипт-фантом; правилото се амандира в ADR, не се пренаписва мълчаливо в JSON (ИА-О31 т. 9 / Р19; амандаментът е ред в Д2 през К8, не ред в §2) |
| **Х3** | `_meta.privacy` | „…в игнорираната папка **(G29)**“ | „…суровият провенанс живее в частното репо `varna_3d` (`data/varna_polygons_08.09.geojson`) и в игнорираната папка `scratch/boundary_gallery/` на Fire_Varna, доказано с `git check-ignore -v`“ | **G29 не е регистриран в ADR 010** (М: `grep -c G29 010_signed_quarter_boundaries.md` → **0**), но **СЪЩЕСТВУВА и остава в сила** (предшественикът `:31`): дефиниран в `ПЛАН_ЛОТ_Границите_v2_амандамент_2_06.09.md:107` (препратки `:62`, `:104`, `:141`; `:71` е мярката на игнорирането), ПОСТРОЕН в `scratch/places_search/qa_night_pins.py` (`check_ignore` `:206`, докстрингът G29 `:229`, отчетът `:242`, отрицателната половина `:402-444`) — ИА-О34 поправя старата котва `:71`; `_meta.privacy` спира да се позовава на номер, който ADR 010 не познава, и казва какво е доказано |
| **Х4** | `rows[5848].code`, `rows[5866].code` | `"TBD"` × 2 | `"elektrorazpredelenie_varna"`, `"severna_promishlena_zona"` | **`"TBD"` не е код и чупи биекцията ред ↔ код, която ИА-3 съди** (98 реда → 97 различни кода, М). *(Доводът „старият гейт би ги слял мълчаливо на `:1231`“ е ОТТЕГЛЕН: `:1230` чете ключ `decisions`, а тялото носи `rows` — старият гейт не може да го прочете изобщо. Съдникът е ИА-14.)* |
| **Х5** | нов `_meta.counts` | — | **15 полета, всяко сверено от ИА-3**: `{dictionary: 98, polygons: 95, cells: 90, virtual_parents: 5, features: 95, excluded_rows: 5, excluded_entries: 6, deferred: 0, no_boundary: 3, distinct_codes_rows: 98, distinct_codes_body: 103, distinct_codes_delivered: 95, registry_entries: 84, registry_pending: 28, registry_pending_new: 39}` (ИА-О42). **98 = 90 + 5 + 0 + 3 ✔** | брояч без сверка не е брояч. **`excluded_rows` брои ДОМА на редовете (5 — селата), `excluded_entries` — дължината на списъка (6, с върнатия `vinitsa_sever`)** — кръг 3 показа, че едно име за двете числа прави ИА-3 червен по конструкция. **Двата брояча — `registry_pending` (28) и `registry_pending_new` (39) — носят имената на `_meta`-ключовете, които броят**, за да не са верни по случайност (ИА-О43: след ИА-О42 не са равни). |
| **Х6** | **`kind_registry` на петте `virtual_parents`** | липсва (полето `kind` е проза) | `kv_levski → "кв"`, `vladislavovo → "кв"`, `vazrazhdane → "жк"`, `mladost → "жк"` — **преписани от подписания регистър** (М: и регистърът, и v1 дават същото) — и `pobeda_group → "кв"` по **решение Р16** | 79-те клетки вече носят `kind_registry` от комит `7556227`; петте обекта го получават с Х13; **петте родителя са другата дупка и тя се затваря с извор, не с измисляне** |
| **Х7** | **нов `_meta.kind_registry_decisions`** | — | `{"район→кв": "Р15 (алтернатива мр = клас locality, не пълни quarter)", "pobeda_group→кв": "Р16", "регистър>префикс (26 реда: 25 клетки + malka_chaika)": "Р16/Х11", "обекти→кв (5 клетки)": "Р3 обърнато 08.09 (Петър) — Х13", "отложени с вид→клетки (6)": "Р7 обърнато 08.09 (Петър) — Х12"}` | двете места, където правилото на самата чернова казва „ДА СЕ ПОДПИШЕ“, стават следа в тялото |
| **Х8** | нов `_meta.signature` | — | `{signed_by: "Petar", signed_at, basis: "ПЛАН_Картата_imot_И-А_08.09.md §9", plan_rev, plan_body_sha256}` — **правилото (ИА-О33): `plan_rev` = ревизията на К1 (`git -C C:/git/Fire_Varna log -1 --format=%H -- docs/plans/ПЛАН_Картата_imot_И-А_08.09.md`, взета на стъпка 6), `plan_body_sha256` = `sha256(git show <plan_rev>:<път>)` — пин към блоб, не към движещ се файл; ИА-16 го сверява** | по образеца на v1 `_meta.signature` (Ч); „тялото“ е целият блоб на Д1 при К1, ред-статусът включително |
| **Х9** | **`_meta.kind_registry_rule` — котвата и редът на изворите** | `…fire_varna_locations.py:150-154…` | `…fire_varna_locations.py:152-155…` + изречението „код в подписания регистър → видът на регистъра дословно (D14, Х11); код извън регистъра → по префикса на imot (Р15)“ (ИА-О34) **+ клаузата „с./обект → без вид (не влизат в v2)“ става „обект → кв (район около ориентир, Р3/Р15, Х13); с. → без вид (не влиза в v2)“ (ИА-О44: същият ключ, пак една разлика — каноничното тяло не тръгва с клауза, която собствените му редове опровергават)** | М: `git grep -n KIND_NORM` → `:152`; таблицата е `:152-155`. **Поправя се в ТЯЛОТО, не само в плана** — Ф7 е каноничният файл, който следващите лотове ще четат, а червен ред 8 после забранява да се пипа на място |
| **Х10** | **`_meta.decisions_open` — редове 2, 4 и 5** (един ключ, една разлика; ИА-О45) | „5 обекта: точка/alias, не клетка“ · „11 отложени (0 сгради)“ · „**13** без район по АГКК → район по решение“ | „5 обекта → клетки `кв` (Р3 обърнато от Петър 08.09, Х12/Х13)“ · „11 отложени → 6 клетки с вид (Х12) + 5 села в `_meta.excluded` (Р7 обърнато 08.09)“ · „**16** без район по АГКК → район по решение (седем клетки без район: Р14)“ | М: 98 − 82 = **16**, изброени поименно в §1.7 и в Р14; редове 2 и 4 иначе тръгват в каноничното тяло като „отворени“ въпроси, които собствените му редове (Х12/Х13) вече са затворили — същият довод като Х9 |
| **Х11** | **`kind_registry` на 26 реда, които СА в подписания регистър: 25 клетки + 1 ред без полигон (`malka_chaika` — Кими К40-5, ИА-О41)** (ИА-О34; ИА-О42 не го мени — нито един от 11-те нови реда не е в регистъра, М) | видът по префикса на imot (`kind_registry` на черновата: 25-те клетки — `м-т` ×19, `кв` ×3, `вз` ×1 (`vinitsa_sever`), `зона` ×1, `пз` ×1 — М, ИА-О35; плюс `malka_chaika` `кв`) | **видът от регистъра, дословно** (D14; същият принцип като Р16 и като A14 на стария билдър, Ч `build_quarters_signed.py:1106-1107`): `balam_dere, borovets_sever, borovets_yug, dobreva, dolna_traka, gorna_traka, kochmar, manastirski_rid, menteshe, pchelina, perchemliyata, priboy, salzitsa, so_planova, vinitsa_sever` → `с.о.` · `bokluk_tarla, rakitnika, saltanat, tv_kula` → `местност` · `sveti_nikola` → `м-т` · `briz, pobeda` → `жк` · `evksinograd` → `кв` · `pz_metro` → `пз` · `zpz` → `зона` · **`malka_chaika` → `м-т`** (ред `no_boundary`, не Feature — правилото „код в регистъра → видът на регистъра“ важи за всеки ред на Ф7, не само за клетките; ИА-О41) | М (ИА-О34, финална проверка 4, блобът `8626ad43…` срещу регистъра с 84 записа): от 79-те клетки **52 са в регистъра**, **25** носят друг вид по черновата; **16 от тях сменят КЛАСА** (14 × `с.о.`→quarter срещу `м-т`→locality, плюс `evksinograd` и `sveti_nikola`; Ч `KIND_CLASS` `fire_varna_locations.py:156-160`) — без Х11 16 клетки нямаше да пълнят кварталната дума или щяха да я пълнят без извор. Суровият imot-вид остава в `kind` на реда като свидетел; нищо друго в реда не се пипа |
| **Х12** | **`include` на 11 реда → `"да"`** (ИА-О42, думата на Петър 08.09: „мисля че е добра идея да ги включим … давай“) | `"решение"` (5 обекта) · `"отложено (0 сгради)"` (6 отложени с вид) | `"да"` за `biznes_hotel, biznes_park_varna, grand_mol, letishte, pristanishte_varna` (обекти) и `pz_topoli, vz_zvezditsa, krushkite, lazur, orehcheto, pripek` (отложени с вид) | Р3 и Р7 обърнати; петте села остават `"отложено (0 сгради)"` (без вид в `KIND_NORM`), трите без полигон остават `"решение (няма полигон…)"`. Клетките стават 90, Feature-ите 95; всички числа в §1.4, §1.5, §1.7, §3.Г, §5, §8, §9 и §11 са преизмерени (ИА-О42) |
| **Х13** | **`kind_registry` и `cls` на петте обекта** | `kind_registry = null`, `cls = "обект"`, `kind_note` „обект: не е клетка → без вид“ | `kind_registry = "кв"`, `cls = "клетка"`, `kind_note` = „район около ориентир → кв (Р15/Р3, Петър 08.09)“; суровият `kind = "обект"` остава като свидетел | Същото правило като за „район“ (Р15): imot.bg именува района по ориентира; никой от петте не е в регистъра (М), затова видът идва от правилото, не от регистъра; `кв` е клас quarter и пълни кварталната дума |

**`mask`, `diff_v1_v2`, `registry_pending_new` и `excluded` НЕ влизат в `Ф7`** — те са в `_meta` на артефакта (ИА-О10). **Човешката котва за Gate 2 не е предварителен sha (той е неизвестим преди Х1–Х13), а изходът на ИА-16: „тринайсет наименувани разлики, нула други“, плюс sha-то, което Изпълнителят отпечатва след записа.**

### 3.Г · Правилата на новия билдър (`Ф1`) — дословно, без празни места

**Г.1 · Кой ражда Feature и къде отива всеки от 98-те реда.**
`include = "да"` → Feature (**90** — след Х12; ИА-О42). Всеки ред от `virtual_parents` → Feature с геометрия = обединението на децата му (**5**). **Общо 95.** Останалите **8** реда се разпределят в **два обявени дома, затворено**:

1. **Домът `_meta.excluded`, **5 РЕДА** (и **6 ЗАПИСА**; ИА-О42):** петте села с **`kind_registry = null`** (поименно в §1.6), всеки с **петорката на v1** — `code`, `display` (= `imot_name` от тялото), `kind` (суровият imot-вид), `registry_action` (= `"deferred_village"`), `why` (= дословният `kind_note` от тялото) — плюс **`delivered: false`** (**ИА-О31**, Кими К39-2/К39-6: никакъв ключ `reason`, никакви `raion_num`/`imot_name` в записа — те са в Ф7); плюс **един ШЕСТИ запис `vinitsa_sever` със `status: "reinstated"`, `delivered: true`** (ИА-О15, Р17), който **не е дом на ред** — редът му е Feature — а е запис на обърнато подписано решение. Петте обекта вече НЕ са тук — те са клетки (Х12/Х13); `pristanishte_varna` носи бележката към спора за район в `_meta.district_witness_disputes`, не в `excluded`. **Биекцията, която ИА-17 съди: `kind_registry = null` ⇔ запис в `excluded` с `delivered: false`. Записите с `delivered: true` са точно тези, за които §2 носи отклонение — днес един.** Никой запис не носи `district_witness_note` (ИА-О43: `pristanishte_varna` е клетка; спорът му за район е в `_meta.district_witness_disputes`).
2. **`_meta.rows_without_feature` — затворена ДВОЙКА кофи, 3 реда (ИА-О42):** `deferred[]` (**0** — празна, но остава като кофа: шестте отложени с вид станаха клетки по Х12) и `no_boundary[]` (**3**: `elektrorazpredelenie_varna`, `malka_chaika`, `severna_promishlena_zona`). Трета кофа няма; `virtual_parent` **не е кофа**. **Наборът кофи се различава от v1 (`alias_of` + `deferred`) — обявено в ИА-О21 и в `declared_deviations[]`.**

**Инвариантът, който ИА-3 съди (ИА-О42): 98 = 90 (Feature) + 5 (excluded_rows) + 0 (deferred) + 3 (no_boundary); `len(_meta.excluded) == counts.excluded_entries == 6`.**

**Г.2 · Всичките 41 полета на `properties` — правило за всяко:**

| поле | правило за v2 |
|---|---|
| `code` | кодът от решенията (90) / от `virtual_parents` (5) |
| `name` | `imot_name` с префикса на вида / `virtual_parents[].name` |
| `kind` | **`kind_registry` от Ф7** — от закритата таблица `KIND_NORM` (Ч `fire_varna_locations.py:152-155`); **за 56-те кода в регистъра равен на регистъра дословно (Х11)**. **Нула измислени стойности** (М: 95/95 покрити; петте обекта — `кв` по Х13) |
| `parent` / `parent_display` | кодът/името на виртуалния родител при 17-те деца, иначе `null` |
| `parent_geometry_check` | `"дете ⊆ родител по конструкция (обединение)"` за 17-те; `"няма родител"` за останалите 78 |
| `source` | `"imot_bg_tiling"` (95 ×) |
| `method` | `"external_tiling"` (90) / `"imot_tiling_union"` (5) |
| `geometry_origin` | `"imot_bg_points"` / `"imot_bg_points_union"` |
| `imported_from` | `"imot_08.09#raion=<raion_num>"` / `"imot_08.09#union=<code>"` |
| `version` | `2` (95 ×) |
| `precision_m` | по Р8 (**100**) |
| `district` / `district_src` | §3.Г.5 |
| `signed_by` · `approved_by` | `"Petar"` |
| `approved_at` · `confirmed_at` | `"2026-09-08"` — **датата на подписа, не часовник** (детерминизъм) |
| `confirmed_by` | `"Petar (Gate 1, ПЛАН_Картата_imot_И-А_08.09)"` |
| `fetched_at` | `properties.fetched` на снапшота (М: една стойност, `"2026-09-08"`) |
| `chosen_source` | `"imot_bg_tiling"` / `"imot_bg_tiling_union"` |
| `chosen_reason` | `""` за клетките; за родителите — `"обединение на поименно подписаните деца (ADR 011 И-D4)"` |
| `license` · `license_terms` · `license_label` · `attribution` | **дословно (ИА-О31, Р10):** `license` = `"условия на imot.bg — присъда няма; геометрията не пътува публично (виж _meta.source_terms.imot_bg)"` · `license_terms` = `"https://www.imot.bg/obshti-uslovia"` · `license_label` = `"граница по районите на imot.bg (район <raion_num>, свалена 2026-09-08) — извор на всекидневната употреба, не официален кадастър"` — **за петте родителя (`method = imot_tiling_union`) вместо `район <raion_num>` стои `обединение на райони <raion_num на децата, възходящо, разделени със запетая>` (ИА-О44: родителите нямат собствен номер; нищо не се измисля)** · `attribution` = `"Кварталните граници в този файл са районите на imot.bg (© imot.bg), снапшот 08.09.2026. Геометрия НИКОГА не пътува публично."` — и четирите непразни по всичките 95 (ИА-8) |
| `url` | **чистият адрес на списъка на imot.bg за град Варна: `https://www.imot.bg/obiavi/prodazhbi/grad-varna`** — това е `_meta.list_endpoint` на снапшота БЕЗ описателната опашка „(95 with adverts, counts)“, която е бележка, не URL (ИА-О30; Ф1 отрязва всичко от първия интервал нататък) — **непразен и еднакъв по всичките 95** (ИА-О31: смисълът на `url` се сменя от „обектът-извор“ (v1, 60 различни) към „списъкът-извор“ (v2, един) — и това отклонение стои и в `_meta.declared_deviations[]`) |
| `note` | `""`, освен за петте родителя, където носи изброените деца |
| `raw_geometry_sha256` | sha256 на компактния `{"type","coordinates"}` на **суровия** полигон (клетки) / на **проектираното обединение, върнато в WGS84 преди канонизация** (родители) |
| `wm_polygon_sha256` | **sha256 по канонизацията на D1** върху крайната геометрия — полето запазва смисъла си „хеш по D1“; `_meta.canonicalization` го казва изрично |
| `witnesses[]` | §3.Г.4 |
| **десетте нулирани** — `drawn_by, drawn_at, basemap, viewport, visible_layers, screenshot_sha256, approval_digest, wm_id, wm_ids, wm_title` | **`null` по всичките 95**. Седемте са договорът `D1_NULL_ON_IMPORT` (Ч `_meta.d1_schema.nulled_on_import`; М: null × 60 във v1); трите уикимапийски се добавят, защото извор няма (ИА-О16). **ADR 011 И-D2 иска низ в `drawn_by` — това е разминаване и се поправя в К8, не в артефакта.** |

**Г.3 · Канонизация по D1:** 6 знака, дедупликация на съседни върхове, външен пръстен обратно на часовника, ротация към лексикографски най-малкия връх, UTF-8 без BOM, `sort_keys`, компактно, финален LF. **Доказано безопасна** (М: 0 невалидни преди и след канонизация; 787 споделени отсечки остават споделени — ИА-О42).

**Г.4 · Провенанс и преименувания, обявени поименно.** `witnesses[]` носи `{kind: "imot_bg_details", advert_id, raw_response_sha256, fetched_at, raion_num}` + `{kind: "imot_bg_dictionary", sha256, fetched_at, n: 98}`. **Съответствието с входа е обявено, защото имената НЕ съвпадат** (М): `witnesses.raw_response_sha256` ← `properties.provenance.raw_sha256` (низът `raw_response_sha256` се среща **0 пъти** в снапшота); `witnesses.fetched_at` ← `properties.fetched`; `witnesses[dictionary].sha256` ← **sha256 на файла-речник `feba76e3…`**, а НЕ вграденото `_meta.dictionary_sha256 = (хешът на суровия отговор — само в снапшота, ИА-О32)`, което е хешът на суровия HTTP-отговор и се записва отделно в `_meta.inputs[dictionary].raw_response_sha256` **като свидетел, който никой не сверява**. Родителите носят `witnesses` = обединението на витнесите на децата си. **Тези полета живеят САМО в частното репо** (червен ред 5).

**Г.5 · Районът.** `district` = кодът от НАШИЯ слой (`C:/git/m6000_private/number_viewer/quarters_layer.geojson`, филтър `authority == "osm"` и `display ∈ DISTRICTS`, Ч `build_quarters_signed.py:396-398`, `fire_varna_locations.py:141-147`) по **представителна точка**; `district_src = "fire_varna_district"`, или `"agkk_au5_confirmed_by_petar"` за код с ред в подписания `district_resolutions`, или **`"outside_city_districts"` с `district = null`** за клетка, чиято представителна точка е извън петте района на слоя (ИА-О43: точно седемте от Р14 — `letishte, pz_topoli, vz_zvezditsa, krushkite, lazur, orehcheto, pripek`; Ф1 НЕ измисля най-близък район; осми `null` = ИА-15 червен). Виртуалните родители минават през **същото** правило (М: и петте получават еднозначен район). Разминаванията със свидетеля АГКК се изброяват поименно в `_meta.district_witness_disputes`, всяко с `delivered` и `resolution` (`"petar_agkk"` за подписаните, `"pending_petar"` за новите), по амандамента Р19.

**Г.6 · Виртуалните родители.** Обединение в **EPSG:32635**, връщане в WGS84, същата канонизация; `_meta.union_crs = "EPSG:32635"`, `_meta.union_recipe.authority = "projected"`, `_meta.union_witness.by_code` носи хеша на всяко обединение. **D8 е изпълнен по построяване.** Мерено: и петте дават единичен `Polygon` без вътрешни пръстени; площите **1,981 (kv_levski) / 3,453 (vladislavovo) / 1,205 (vazrazhdane) / 1,041 (mladost) / 0,612 (pobeda_group) km²**.

**Г.7 · Входовете по sha — ОСЕМ (ИА-О34: регистърът е седмият; ИА-О35: v1 като диф-база е осмият), и ВСИЧКИТЕ ОСЕМ живеят в `_meta.inputs[]`** (така D10 е изпълнен буквално; другите `_meta` ключове само **препращат** към записа в `inputs`, не го заместват):

1. полигоните (`77b40564…`, 175 736 B) — файл в `varna_3d/data/`;
2. речникът (`feba76e3…`, 6 274 B) + вграденото `(хешът на суровия отговор — само в снапшота, ИА-О32)` като несверяван свидетел;
3. тялото с решенията Ф7 — **прочетено като блоб на varna_3d** (`{path, rev, sha256, bytes}`); `_meta.decisions_sha256` / `decisions_commit` / `decisions_path` са същите стойности, изнесени поименно по образеца на v1;
4. `Fire_Varna/data/address_rows.json` (`2a4766bd…`, 5 073 137 B) — **файл, пиннат по прочетените байтове; блобът на Fire_Varna е свидетел (`{repo, rev, path}`), ИА-О35/ИА-О38**; `_meta.mask.address_rows_sha256` препраща към него;
5. **`Fire_Varna/scratch/places_search/district_resolutions_2026-09-07.json` — БЛОБ** (`rev e6c706d…`, sha `d494d4d256ae…`, 513 B); `_meta.district_resolutions.input` препраща към записа в `inputs`;
6. **`C:/git/m6000_private/number_viewer/quarters_layer.geojson` — ФАЙЛ извън git** (`26e39b00…`, 201 130 B), с `note: "извън git репо — пинва се по прочетени байтове (Р21, ИА-О23); версионирането е дълг ИБ-17"`;
7. **`C:/git/Varna_buildings/config/quarter_registry.json` — БЛОБ на Varna_buildings** (М, ИА-О34: `git -C C:/git/Varna_buildings log -1 --format=%H -- config/quarter_registry.json` → `627b240…`, 52 299 B, sha `7d217401…`; 84 записа) — регистърът ражда `counts.registry_entries`, `registry_pending`, `registry_pending_new` и вида на 56 кода (Х11); четен е само (D14, изолация на репата); **пинът е sha256 на прочетените байтове, `{repo, rev, path}` са свидетел** (ИА-О35: Ф2 няма `--registry-repo/-rev`, а трето временно дърво във Varna_buildings е забранено от ИА-STOP 14 — счупва се по механика 1);
8. **v1 като диф-база — БЛОБ на varna_3d** (ИА-О35): `role: "diff_base"`, `{repo: varna_3d, rev: 6486b48…, path: data/quarters_signed.geojson, sha256: c1551d24…, bytes: 301 917, note: "след К6 живее в docs/archive/quarters_signed_v1_2026-09-06.geojson (Ф8); блобът е същият"}` — ревизията е `git -C C:/git/varna_3d log -1 --format=%H -- <пътя, подаден на --diff-base>` (М днес: `6486b48…`); той ражда САМО `_meta.diff_v1_v2`, не геометрия; ИА-9 го сверява с `git show <rev>:<path>`, който е стабилен и след `git mv` (червен ред 13). **Празна ревизия (ИА-О36):** на стъпка 13 `--diff-base` сочи архивния път, чийто `git mv` е само стейджнат (К6 е стъпка 14) → `git log -1 -- <път>` е празен; Ф1 записва `rev: null` + `note: "път без история — пресен билд за ИА-10"` и хешира байтовете; това е допустимо САМО за пресните билдове на ИА-10 (сравняват се помежду си), доставеният файл от стъпка 8 носи истинската ревизия.

**Разлика между пина и прочетените байтове = `sys.exit(1)`** за всичките осем. **Кой как се сверява (ИА-О35):** по БЛОБ (`git show <rev>:<path>` в своето репо) — Ф7 (`--decisions-repo/-rev/-path`), резолюциите (`--resolutions-repo/-rev/-path`) и диф-базата (записаните `rev`/`path` в артефакта); по БАЙТОВЕ от диска (sha256; `rev` е свидетел, където файлът е в git) — снапшотът, речникът, `address_rows`, слоят с районите, регистърът. Правилото: блобово адресиране само където Ф2 има тройка флагове или артефактът носи ревизията; иначе байтовете са пинът.

**Г.8 · `_meta` — ЗАТВОРЕНИЯТ НАБОР ОТ 41 ИМЕНА, изброен изчерпателно:**

`artifact_class · attribution · canonicalization · counts · d1_schema · decisions_author · decisions_based_on · decisions_blob_before_attribution · decisions_commit · decisions_path · decisions_sha256 · decisions_sha256_rule · decisions_sha256_worktree · declared_deviations · dictionary_sha256 · diff_v1_v2 · district_resolutions · district_witness_agkk · district_witness_disputes · excluded · generated_by · inputs · licences · lot · mask · method_licence · parent_child · parent_child_without_polygon · plan · precision_floor_m · precision_m_by_method · publication · registry_pending · registry_pending_new · rows_without_feature · signature · snapshot_date · source_terms · union_crs · union_recipe · union_witness`

(= 36-те на v1 **+** `dictionary_sha256`, `snapshot_date` по ADR 011 И-D2 **+** `diff_v1_v2`, `mask`, `registry_pending_new`; `decisions_sha256` и `source_terms` вече са сред 36-те.)

Съдържанието: `artifact_class = "signed_by_plan_signature"` · `lot = "И-А"` · `plan` = `{file, rev: plan_rev, blob_sha256: plan_body_sha256}` (`file` както във v1 и ИА-О11; същите стойности като Х8 — ИА-О33/ИА-О34) · `generated_by = "Claude Executor"` · `decisions_author = "Claude Architect (черновата, 7556227) → Петър (подпис)"` · `decisions_based_on` = черновият блоб `8626ad43…` + ревизията му · `decisions_blob_before_attribution` = същият · `decisions_sha256_rule` = дословно правилото на червен ред 12 · `decisions_sha256_worktree` = `{path, bytes, note: "varna_3d е eol=lf — работното копие Е блобът (git check-attr)"}` · `d1_schema` = полетата + **десетте** нулирани (ИА-О16) · `canonicalization` = трите правила + изричното изречение за `wm_polygon_sha256` при извор без уикимапийски обект · `dictionary_sha256` = **sha на ФАЙЛА** `feba76e3…` · `snapshot_date = "2026-09-08"` · `method_licence = {external_tiling: …, imot_tiling_union: …}` · `licences[]` = **един** запис (imot.bg) · `source_terms.imot_bg` = **трите** ключа на Р10 (`url, recorded_on, position` — ИА-О30/ИА-О31) · `attribution` = редът на Р10 · `publication` = „Геометрия НИКОГА не пътува публично; публично излизат само име, код и `src`“ · `precision_floor_m = 50` · `precision_m_by_method = {"external_tiling": 100, "imot_tiling_union": 100}` · `parent_child` = **17** двойки (**и това е ЕДИНСТВЕНИЯТ извор на родство, който гейтовете четат**) · `parent_child_without_polygon = []` · **`excluded` = 6 записа / 5 реда (Г.1, ИА-О42)** · `rows_without_feature` = двете кофи (Г.1) · `registry_pending` = **28**-те регистрови кода без Feature, **като списък `[{code, why}]`** (ИА-О44: четирите жилищни носят довода по амандамента на D4, `malka_chaika` — `why = "регистров код с ред no_boundary в тялото (без полигон)"`, останалите 23 — `why = "регистров код без ред в тялото на imot"` (ИА-О45: 4 + 1 + 23 = 28); ИА-14 ги чете по `code`) · `registry_pending_new` = **39**-те доставяни кода без ред в регистъра (ИА-О42) · `district_resolutions` = приложените + препратка към вход 5 · `district_witness_agkk` = картата код → АГКК (**82** реда с двете стойности) · `district_witness_disputes` = **5** (всичките с `delivered: true` — ИА-О42: `pristanishte_varna` е клетка) · `union_crs` / `union_recipe` / `union_witness` = Г.6 · `signature` = Gate 1 · `counts` = 15-те полета на Х5 · `mask` = §1.5, включително `tie_break_rule` · `diff_v1_v2` = **изчислен от Ф1 срещу `--diff-base` (осмият вход, §3.Г.7) и преизмерен от ИА-13 (б)**, включително `tie_break_v1` (Р20) — ИА-О35 · `declared_deviations[]` = **дословният списък от 16 записа (ИА-О33), по един запис на ред, всеки `{id, text}` с точно тези `id`:** `ИА-О2` · `ИА-О4` · `ИА-О10` · `ИА-О12` · `ИА-О15` · `ИА-О16` · `ИА-О17` · `ИА-О21` · `ИА-О23` · `ИА-О24` · `Г.2-drawn_by` (`drawn_by = null`) · `Г.4` (един запис с двете преименувания) · **`Р4` (отклонение от ADR 010 D4: `no_boundary` без Feature)** · **`Г.2-url` (смисълът на `url` = списъкът-извор, един за всичките 95, не обектът-извор)** · **`Р17` (`vinitsa_sever` — обърнато подписано изключване, `delivered: true`)** · **`Р10` (`source_terms.imot_bg` с три ключа вместо шестте на C9 — ИА-О30 т. 3)** — **всяко отклонение живее и в артефакта, не само в плана; ИА-6 съди списъка по `id`.**

**Г.9 · Детерминизъм.** Никаква дата „сега“, никакъв `set` в изхода, никаква подредба, зависеща от речник; два прогона → еднакви байтове (ИА-10).

### 3.Д · `Fire_Varna` — три проследени документа и един игниран файл

| # | път | какво |
|---|---|---|
| **Д1** | `docs/plans/ПЛАН_Картата_imot_И-А_08.09.md` | **този текст, редакция 4, както го подписва Петър** — **вече проследен и чист** (ИА-О32/ИА-О33); единствената промяна е ред-статусът на Архитекта в К1 (стъпка 2); **Изпълнителят никога не пише в него** (червен ред 1). **Нула координати в текста** (червен ред 5, съди се от ИА-19). |
| **Д2** | `docs/decisions/011_kartata_imot.md` | **ред-статус** („И-А изпълнен; тялото и геометрията са в varna_3d; И-Б отворен“) + поправките: **И-D3 (`:17`) — каноничният път става `varna_3d/data/quarters_decisions_imot_2026-09-08.json`** · **И-D2 (`:15`)** `drawn_by` → `null`, `raw_response_sha256` → обявеното съответствие с `provenance.raw_sha256`, `source_terms.imot_bg` от два на **три** ключа (`url, recorded_on, position` — ИА-О30/ИА-О31; не шест: за imot.bg не е правена работа по условията), **амандамент на ADR 010 D4** (`no_boundary` без Feature — Р4; и че `deferred` не живее в `_meta.deferred`, а в `rows_without_feature.deferred[]` за imot-редовете и в `_meta.registry_pending` за регистровите кодове — наследено от v1, което също няма `_meta.deferred`; ИА-О36), и изричното потвърждение, че `dictionary_sha256` + `snapshot_date` СА в `_meta` · **И-D4 (`:19`)**: виртуалните родители СА Feature-и (95 = 90 + 5 — ИА-О42) · **И-D5**: нови редове за архива на v1, за преместеното тяло, за копирания снапшот, **за обръщането на `vinitsa_sever` (Р17)**, **за амандамента на ADR 010 D0/G23 (Р19)** и **за пина по байтове на слоя с районите (Р21)** · **„Последствия“ (`:47`)**: `97,2 %` → **97,18 % за доставката (90 клетки, ИА-О42)**; `:48` „около 18 280 сменят думата“ → измереното **18 232 сменени · 685 изгубени · 10 607 спечелени** (ИА-О42, 90 клетки) (при обявеното правило Р20); площите `61,7 km²` → **61,597 обединение / 66,886 сбор** за v1 и **87,025 / 95,318** за v2 (ИА-О42). **Статусът остава `Proposed`** (Ч `011:3`). |
| **Д3** | `docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` | **§21** — „Под-лот И-А затворен“: какво е построено, къде живее, кои гейтове са зелени, какво чака И-Б, входна точка за следващата сесия. **Файлът е чист (§20 е комитнат); К9 носи само §21** (ИА-О19 в редакцията му от ИА-О32; мръсен хънк на стъпка 0 се показва дословно и НЕ се комитва). |
| **Д4** | `scratch/boundary_gallery/granitsi_preview_05.09/imot_v2_review_08.09.html` | **пробата за окото на Петър** (§8 т. 3). **Произвежда я Ф2 с `--review-html` на стъпка 13 (ИА-О45).** **ИГНОРИРАН** (М: `.gitignore:64:/scratch/boundary_gallery/`), никога проследен, никога комитван. Изброен ТУК поименно, защото червен ред 4 забранява ДОКОСВАНЕ, не проследеност — без този ред §8 искаше нарушение (кръг 3, ГОЛЯМ). |

**Механичната червена черта:** след всеки от трите комита се пуска `python gates/probe/b3_g11.py HEAD --cached`. Мерено защо това е безопасно: `changed()` (Ч `:70-83`) събира `new` от `git diff --cached --diff-filter=AMR HEAD`, тоест **от ИНДЕКСА срещу новия HEAD** — комитнатият файл вече е в HEAD и не е в дифа, значи `stray_new` остава `[]`, а **`ALLOWED_NEW` (Ч `:40-46`, седем записа; `LEGACY` е `:39`, `GEO` е `:37`) не се пипа**. `inventory()` (Ч `:84-93`) гледа само `data/*.json`, а **обхватът е Ч `:105-106`** (`scope = sorted(set(tracked) | set(f for f in touched if f.lower().endswith((".json", ".geojson"))))`; `tracked` идва от `:98`, `touched` — от `:104`) — **трите документа са `.md` и не влизат в него.** *(Редакции 2 и 3 цитираха `:104-108` и `:109-111`; и двете са грешни — `:109-111` е тялото на цикъла по `scope`. Поправено на двете места, където се ползва: тук и в ИБ-4.)*

---

## 4 · Ред на изпълнение

| стъпка | кой | какво | спирачка |
|---|---|---|---|
| 0 | Изп | Пълната мярка на §1 **без нито една редакция**. Записва OID-ите на 14-те стейджнати пътя (`git ls-files -s -- <14-те>`), **`git worktree list` на двете репа като базова линия (ИА-О36: Fire_Varna днес носи две чужди дървета — §1.1)**, изходите **Н1–Н6, Н8, Н9**, **16-те идентификатора на тестовете** (по списък, без прогон) и **състоянието на Д3** по `git status --porcelain -- docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` (чист или мръсен — записва се фактът; **ИА-О31:** старият хънк на §20 е комитнат в `0304a3c`, не се изисква). **Н7 (`python -m unittest discover -s tests`) НЕ се пуска на стъпка 0** — `tests/test_quarters_signed_fixtures.py:156-157` (пазачът и `shutil.rmtree`) изтрива и регенерира фикстури в репото (Ч); мери се на стъпка 13 в еднократното `git worktree`. | разлика в Н1–Н6, Н8, Н9 или в 16-те идентификатора = **ИА-STOP 2** |
| 1 | **Петър** | **Gate 1** — подписва този план и **двайсет и едното** решения на §9. | без подпис нищо не тръгва |
| 2 | **Архитект** (не Изп) | **К1 = ред-статус „подписан от Петър, <дата>, ревизия <sha>“ отгоре на Д1**, само ако Петър подпише без редакции (при редакции Архитектът ги внася преди подписа); Изпълнителят на стъпка 2 само пуска **ИА-19** върху БЛОБА на Д1 в HEAD (`git show HEAD:<Д1> > %TEMP%/ia_docs/d1_head.md`, после изразът на ИА-19 — двата `-e` образеца — върху ТОЗИ файл като единствен операнд; ИА-О34: `grep` с файлови операнди пренебрегва stdin, затова тръба към командата на §5 съди работното дърво, не блоба — М) и `b3_g11.py HEAD --cached` = 0 (ИА-О32) | ИА-19 с изход **0** (`grep` намери координата) = **ИА-STOP 17**; изход **2** (грешка на командата) = **ИА-STOP 5**; изход 1 = чисто; b3_g11 ≠ 0 = **ИА-STOP 9** |
| 3 | Изп | Копира Ф5, Ф6 от игнорираната папка `Fire_Varna/scratch/boundary_gallery/granitsi_preview_05.09/` (червен ред 5; М: `git check-ignore -v` → `.gitignore:64`) в `varna_3d/data/`; прави **единствената** редакция Т1; `git check-ignore -v --no-index` за Ф5, Ф6 → **печата разрешаващото `!`-правило на Т1** (ИА-О34: БЕЗ `--no-index` командата мълчи с изход 1 за всеки път, който вече е в индекса — М в еднократно репо и в живото varna_3d върху `data/quarters_signed.geojson`; редът се ЗАПИСВА като мярка, не спира) → `git add -- data/varna_polygons_08.09.geojson data/varna_dictionary_08.09.json .gitignore` → **`git ls-files --error-unmatch -- <двата>` = 0** (това е критерият). **Не комитва.** | `ls-files --error-unmatch` ≠ 0 = **ИА-STOP 11** (единственият критерий — ИА-О34) |
| 4 | **Петър** | Комит **К2** (замразеният вход + `.gitignore` — **и четирите unignore реда, включително този за архива, са в ТОЗИ комит**). | — |
| 5 | Изп | Пише Ф1, Ф2, Ф3. Още нищо не се стартира срещу истински данни. | — |
| 6 | Изп | Подготвя **Ф7** в `varna_3d/data/`; пуска **ИА-16** (тринайсетте разлики срещу черновия блоб) **без позиционен път** (ИА-О22) — **Ф7 се чете от диска по `--decisions-path` (блоб още няма; работно копие = блоб във varna_3d, червен ред 12 — ИА-О34)** — и отпечатва sha-то на записаното. `git add -- data/quarters_decisions_imot_2026-09-08.json`. **Не комитва.** | разлика извън списъка = **ИА-STOP 3** |
| 7 | **Петър** | Комит **К3** — подписаното тяло. | — |
| 8 | Изп | Строи два пъти с **пълния CLI-договор на Ф1 (деветте флага на §3.А, вкл. `--diff-base data/quarters_signed.geojson`; Ф1 няма стойности по подразбиране — ИА-О33/ИА-О35)**, различава се само `--out`: `%TEMP%/ia_build_a/q.geojson` и `%TEMP%/ia_build_b/q.geojson` → `cmp` (ИА-10). | различни байтове = **ИА-STOP 6** |
| 9 | Изп | Пуска Ф2 върху `<tmp>` артефакта: **ИА-1 … ИА-9, ИА-11, ИА-14 … ИА-17** (ИА-10 е вече минал на стъпка 8; ИА-16 тук — с блобовата тройка, Ф7 е в HEAD от К3). После **ИА-13 в двете си половини, с `--diff data/quarters_signed.geojson`** — v1 още стои там. **ИА-12 НЕ се пуска тук: командата му сочи архивния път, който още не съществува** (М: `qa_quarters_signed.py:1214-1216` `if not path.exists(): return 1`). | ≠ очакваното = **ИА-STOP 3** |
| 10 | Изп | `--make-fixtures --fixtures-dir %TEMP%/ia_fixtures` **върху построения артефакт**. **Причината стъпката да е ТУК е една: преди билда артефакт няма.** *(Не е вярно, че `make_fixtures` мутира документа — `clone()` на `:929-930` е дълбоко копие, М.)* Прогонва всяка фикстура с нейния `--only` и записва **истинския ѝ изходен код** и първия ред от изхода. Двете временни дървета се вдигат и се махат по ИА-О18; счупените копия по механика 1 живеят в `%TEMP%/ia_broken/` (ИА-О36). | фикстура с изход 0 = **ИА-STOP 5** |
| 11 | Изп | Комит **К4** (билдър, гейт, тест). **Фикстури НЕ се комитват** (ИА-О20). | — |
| 12 | Изп | Редакции Т2, Т4, Т5, Т7, Т9, Т11, Т13, **Т14**; подготвя **Т15 (двете места)**. `git mv data/quarters_signed.geojson docs/archive/quarters_signed_v1_2026-09-06.geojson`. Копира артефакта от `%TEMP%/ia_build_a` в `data/quarters_signed.geojson` и **проверява идентичността: `cmp %TEMP%/ia_build_a/q.geojson data/quarters_signed.geojson` = 0** (ИА-О31, Astra S40 бл. 7: ИА-10 доказва повторяемост a↔b, това доказва, че ДОСТАВЕНИЯТ файл е прогон a). `git add --` изричните пътища. | `cmp` ≠ 0 = **ИА-STOP 6** (ИА-О39: байтова разлика има свой номер) |
| 13 | Изп | **Пълният §5 върху новите пътища**: ИА-1 … ИА-19 (сега ИА-12 и ИА-13 сочат `docs/archive/…`) и **Н1′–Н9′**; **Н7′ (`python -m unittest discover -s tests`) и ИА-18 се пускат САМО в еднократно дърво `%TEMP%/ia_neg_v3`, вдигнато непосредствено преди и махнато непосредствено след прогона, в което нищо не е чупено (ИА-О18/ИА-О39)** — никога в работното дърво на varna_3d (тестът регенерира фикстури, ИА-О32). Произвежда **Д4** с `Ф2 <артефакт> --diff docs/archive/quarters_signed_v1_2026-09-06.geojson --address-rows C:/git/Fire_Varna/data/address_rows.json --review-html <Д4>` (ИА-О45: единственият инструмент, който пише Д4; никакъв скрипт във Fire_Varna, никакъв нов ефемерен път). **Правилото за дърветата (ИА-О39, важи и на 18): пътищата `%TEMP%/ia_neg_v3` и `%TEMP%/ia_neg_fv` НЕ са постоянни дървета — всеки ползвател (Н7′, ИА-18, всяка отрицателна половина по механика 2/3) сам вдига (`worktree add --detach … HEAD`), ползва и маха (`worktree remove --force` + `prune`) дървото си, един след друг по реда на §5 (ИА-3, ИА-5, ИА-16 са ПРЕДИ ИА-18); на един път никога няма две дървета едновременно (М: второ `worktree add` към зает път → `fatal: already exists`, изход 128); в края на стъпката `git worktree list` == базовата линия на стъпка 0.** | регресия на Н1′–Н9′ = **ИА-STOP 4**; друго отклонение — по вида си (ИА-STOP 3/5/6) |
| 14 | **Петър** | Комит **К6** — v2 + `git mv` на v1 + `fire_varna_location_inputs.json`. **ПРЕДИ К5** (ИА-О31/ИА-О32): архивът трябва да е в HEAD, преди четците да сочат към него. | — |
| 15 | Изп | Пише **Ф9**; комит **К5** (кодът + Ф9 — четците следват v1 в `docs/archive/`). **`.gitignore` НЕ е тук** — вече е в К2. | К5 преди К6 = **ИА-STOP 18** |
| 16 | Изп | Пише **Ф10**; комит **К7** (предложението за регистъра). | — |
| 17 | Изп | Пише ред-статуса и поправките в **Д2**, после **§21 в Д3**; комити **К8** и **К9** във Fire_Varna → **ИА-19 върху Д2 и Д3 преди всеки** → `b3_g11.py HEAD --cached` = 0 след всеки. | ИА-19 с изход **0** (`grep` намери координата) = **ИА-STOP 17**; изход **2** (грешка на командата) = **ИА-STOP 5**; изход 1 = чисто; b3_g11 ≠ 0 = **ИА-STOP 9** |
| 18 | Изп | Пуска §5 наново, **отгоре докрай, върху комитнатото състояние**, и записва всеки изход дословно. **Дърветата (`ia_neg_v3`, `ia_neg_fv`) се вдигат и махат от всеки ползвател поотделно по правилото на стъпка 13 (ИА-О39); сверката е след последния.** Проверява `git worktree list` в **двете** репа и `git status --short` — **нула остатъци от пътищата на ИА-О18 и нула НОВИ дървета спрямо базовата линия на стъпка 0** (ИА-О36: двете чужди дървета на Fire_Varna от §1.1 не са остатък на лота и не се махат). | нов остатък/дърво на лота = **ИА-STOP 1** |
| 19 | **Одитор** | Независим преглед на дифа срещу този план, ред по ред. | НЕГОДНО = **ИА-STOP 8** |
| 20 | **Петър** | §8 локален преглед → **Gate 2** → пуш (varna_3d `rezhimi`, Fire_Varna `main`). | — |

---

## 5 · Гейтове

**Кой гейт от кого се изпълнява (обявено, защото кръг 3 намери, че Ф2 „покрива ИА-1…ИА-18“, а две от тях не са в него):**
**Ф2** → ИА-1 … ИА-9, ИА-11, ИА-13 … ИА-17. **Ф1 + `cmp`** → ИА-10. **Старият гейт `src/qa_quarters_signed.py`** → ИА-12. **Ф3 (unittest)** → ИА-18. **Обявена команда с grep** → ИА-19.

**Как се чупи вход.** Четири механики, защото входовете са четири вида:

1. **Файл на диска** (снапшот, речник, `address_rows`, слоят с районите, регистърът — петте, които се пинват по байтове, §3.Г.7): счупено копие в `%TEMP%/ia_broken/`, гейтът се вика с път към него. (ИА-О35: `address_rows` и регистърът са ТУК, не в механика 2/3 — Ф2 няма repo/rev флагове за тях, а ИА-STOP 14 забранява дърво във Varna_buildings.)
2. **Блоб на Fire_Varna** (`district_resolutions`, черновата за ИА-16): `git -C C:/git/Fire_Varna worktree add --detach C:/Users/Petar/AppData/Local/Temp/ia_neg_fv HEAD` → счупва се копието ТАМ → `git -C <wt> add -f -- <път>` → **`git -C <wt> -c user.name=… commit`** (детачнат HEAD, дангълинг комит, реалните клонове не мърдат) → гейтът се вика с `--resolutions-repo <wt> --resolutions-rev HEAD` (или `--draft-repo <wt>`) → **задължително** `git -C C:/git/Fire_Varna worktree remove --force <wt> && git -C C:/git/Fire_Varna worktree prune`.
3. **Блоб на varna_3d** (Ф7): **ВТОРО, отделно дърво** `git -C C:/git/varna_3d worktree add --detach C:/Users/Petar/AppData/Local/Temp/ia_neg_v3 HEAD`, същата процедура, `--decisions-repo <wt> --decisions-rev HEAD`, махане с `git -C C:/git/varna_3d worktree remove --force`. **Едно дърво не стига: Ф7 живее във varna_3d и този път просто НЕ СЪЩЕСТВУВА в дърво на Fire_Varna** (кръг 3, БЛОКЕР).
4. **Артефакт** (v2) и **проза** (за ИА-19): счупено копие в `%TEMP%`, гейтът се вика с него.

**Само `git add -f` НЕ стига** — мерено на цитирания образец (Ч `scratch/places_search/qa_night_pins.py:15-20`, `:111`, `:118`, `:171-175`): той доказва проследеност и сверява `worktree_sha256`, **но не подменя съдържание на блоб**. **Никой от тези пътища не е път от write-set-а** (ИА-О18).

**Всяка отрицателна половина се вика така, че да пада по СВОЯТА проверка.** Гейтът приема `--only <ИА-номер>`, който изключва останалите. Всяка се записва с: командата, счупения вход, `--only`, **истинския изходен код** и първия ред от изхода.

| # | гейт | команда · кога | очаквано | отрицателна половина (ТРЯБВА да даде ≠ 0) |
|---|---|---|---|---|
| **ИА-1** | валидност ПРЕДИ поправка | Ф2, `<артефакт> --tiling …` · стъпки 9, 13, 18 | 0; „95 входни полигона · **0 невалидни преди поправка** · 90 доставяни клетки · **0 невалидни след канонизация**“ (М, ИА-О42) | копие на снапшота с пръстен „папийонка“ (самопресичане) → 1 |
| **ИА-2** | 0 застъпвания сред клетките · споделени ръбове | Ф2 · 9, 13, 18 | 0; **0 застъпващи се двойки > 0 m² сред 90-те клетки** · **787 споделени отсечки** · **3 476 отсечки с кратност (= 2 689 различни + 787 споделени; РАЗЛИЧНИТЕ върхове са 2 607 — ИА-О42)** · **2 689 различни отсечки** (М) | копие на снапшота, в което една клетка е **буферирана с +0,0002°** → застъпване с всеки съсед → 1. (**Не „изместен връх“** — изместване навън прави процеп и гейтът остава 0.) |
| **ИА-3** | всеки от 98-те реда има точно един дом | Ф2 · 9, 13, 18 | 0; **98 = 90 Feature + 5 excluded_rows + 0 deferred + 3 no_boundary** (ИА-О42); `len(_meta.excluded) == 6`; всяко id от речника точно веднъж; **`_meta.counts` (15 полета) съвпада дума по дума с преброеното**; **95 Feature-а = 90 + 5** | (блоб varna_3d) копие на Ф7 с махнат ред → „id без дом“ → 1; копие на артефакта с ръчно пипнат `counts.cells` → 1; копие с `counts.excluded_rows = 6` → 1 |
| **ИА-4** | застъпванията в артефакта са САМО родител–дете | Ф2 · 9, 13, 18 | 0; **17 двойки > 1 m², всичките в `_meta.parent_child`, всяка равна на пълната площ на детето; нула други** (М). Сбор на площите **95,318 km²**, обединение **87,025 km²** (ИА-О42; сборът се смята от непечатаните стойности; отпечатаните до третия знак НЕ се сумират — ИА-О35/ИА-О36) | копие на артефакта, в което геометрията на един родител е свита → двойката вече не е „дете ⊆ родител“ → 1 |
| **ИА-5** | D8 по конструкция | Ф2 · 9, 13, 18 | 0; всеки от **5**-те родителя е точно обединението на изброените си деца (17 общо), дете ⊆ родител, DAG без цикъл, `parent_child` = **17** двойки | (блоб varna_3d) копие на Ф7 с махнато дете от `children` → „родителят не е обединението“ → 1 |
| **ИА-6** | схемата | Ф2 · 9, 13, 18 | 0; **3 върхови ключа · 3 Feature ключа · 41 `properties` ключа** дословно по §1.3; **`_meta` = ЗАТВОРЕНИТЕ 41 имена на §3.Г.8**; **десетте нулирани полета са `null` по всичките 95**; **`rows_without_feature` носи точно кофите `deferred` и `no_boundary`** (ИА-О21); **`_meta.declared_deviations[]` носи точно 16-те `id` на Г.8, всеки с непразен `text`** (ИА-О33) | копие с махнат запис от `declared_deviations` → 1; копие с махнат `wm_polygon_sha256` → 1; копие с добавен `n_pts` в `properties` → 1; копие с добавен 42-ри `_meta` ключ → 1; копие без `snapshot_date` → 1; копие с кофа `alias_of` → 1 |
| **ИА-7** | **приватност — САМО файлове с ДАННИ** | Ф2, `--privacy-scope <път>` (в CLI-договора на Ф2, §3.А) · 9, 13, 18 | 0. **Обхватът е ЕДИН път: `Ф7`** — единственият файл с данни, който този лот създава или мени и който е тяло, а не геометрия. **Ф10, Д1, Д2, Д3 са ПРОЗА и са извън обхвата по конструкция — тях ги съди ИА-19** (ИА-О24); `data/quarters_signed.geojson` и `data/varna_polygons_08.09.geojson` са извън обхвата по конструкция — те СА геометрията в частното репо. По имена на ключове: `advert_id, raw_sha256, raw_response_sha256, points, raion_points, localid, cadnum, center, coordinates, geometry, centroid, polygon, bbox` → **0** и **0 float-а в кутията на Варна** (М днес върху блоба `8626ad43…`: 0 и 0; подниз `advert` = 98 от `n_adverts` и НЕ се брои) | копие на `Ф7` с добавен ключ `"advert_id"` → 1; копие на `Ф7` с ключ `"center"` и двойка числа в кутията на Варна → 1 |
| **ИА-8** | `source_terms` + атрибуция | Ф2 · 9, 13, 18 | 0; `_meta.source_terms.imot_bg` носи **ТОЧНО трите** ключа `url, recorded_on, position` (Р10; **ИА-О30: отклонение от шесткльчовия образец C9 на v1** — `quote/terms/verdict/checked_on` НЕ се пишат, защото никой не е отварял страницата и датата на „проверка“ би била атестация за нещо, което не се е случило); `len(_meta.licences) == 1`; всеки от 95-те носи непразни `license`, `license_terms`, `license_label`, `attribution` и **`url` == `https://www.imot.bg/obiavi/prodazhbi/grad-varna`** (чистият адрес, §3 ред `url`) | копие с празен `position` → 1; копие с четвърти ключ `checked_on` в `source_terms.imot_bg` → 1; копие с Feature, чийто `license` е голо „CC BY-SA“ → 1; копие, в което `url` носи опашката „(95 with adverts, counts)“ → 1 |
| **ИА-9** | **осемте входа са пиннати и сверени** | Ф2 · 9, 13, 18 | 0; `_meta.inputs` носи **точно осем** записа (§3.Г.7; ИА-О34: регистърът е седмият; ИА-О35: диф-базата v1 е осмият); **блобовите ТРИ** (Ф7, `district_resolutions`, диф-базата) се четат с `git show <rev>:<path>` в СВОЕТО репо; **файловите ПЕТ** (снапшот, речник, `address_rows`, слоят с районите, регистърът) се хешват от диска, `rev` е свидетел (ИА-О35); **шестият (слоят с районите) носи `note` за живота си извън git**. Записва се и мярката „varna_3d е `eol=lf`: работно копие = блоб“ (М) — **доказан капан, не отрицателна половина** | копие на артефакта с подменен `decisions_sha256` → 1; копие на снапшота с един променен байт при непроменен пин → 1; копие на речника с махнат ред при непроменен пин → 1 (механика 1 — ИА-О37); **(блоб Fire_Varna, дърво `ia_neg_fv`) копие на `district_resolutions` с трета резолюция, подадено с `--resolutions-repo/--resolutions-rev` → 1**; копие на слоя с районите с изтрит район → 1; копие на регистъра с махнат запис в `%TEMP%/ia_broken/`, подадено с `--registry` при непроменен пин → 1 (механика 1); копие на `address_rows` с един изтрит ред, подадено с `--address-rows` → 1 (механика 1); копие на артефакта с подменен `inputs[diff_base].sha256` → 1 (механика 4) |
| **ИА-10** | **два билда → едни байтове** | `Ф1 <осемте входни флага по §3.А> --out %TEMP%/ia_build_a/q.geojson && Ф1 <същите> --out %TEMP%/ia_build_b/q.geojson && cmp` (ИА-О33/ИА-О35: всяко викане носи всичките девет флага; на 13/18 `--diff-base` сочи архивния път и двата ПРЕСНИ билда се сравняват помежду си, не с доставения файл — записът `inputs[diff_base]` носи пътя на момента) · 8, 13, 18 | `cmp` мълчи, изход 0 | **`%TEMP%/ia_builder_nd.py`** — копие на Ф1 с една стойност от `datetime.now().isoformat(timespec="microseconds")` (ИА-О34: дата без часовник би дала еднакви байтове в един ден) — пуснато два пъти → `cmp` → 1. *(„Билд с подменен снапшот“ НЕ е половината на този гейт: по Г.7 подмененият вход спира билда на пина, файл не се пише и `cmp` пада с „No such file“ — доказва пина, не детерминизма.)* |
| **ИА-11** | всяка адресна точка отчетена | Ф2, `--address-rows C:/git/Fire_Varna/data/address_rows.json` · 9, 13, 18 | 0; гейтът **преизчислява** и сравнява със записаното в `_meta.mask`: **80 510 = 78 235 (`in_one_cell`) + 1 (`tie_break`, развързан по Р6) + 2 274** (ИА-О42); маската е **затворена тройка от КЛАСОВЕ** (не ключове — ключовете на `mask` са осемте на §1.5; ИА-О44): `outside_all_95` **2 274** · `excluded_cell` **0** (вътре в някоя от **петте** изключени клетки — селата; за 79 клетки беше 1 025 = точките в петте обекта, сега в `in_one_cell`) · `deferred_cell` **0** (кофата е празна — няма отложени клетки); `address_rows_sha256` == sha на прочетения блоб (`2a4766bd…`) | копие на артефакта с `mask.in_one_cell` = 78 236 → преизчисленото не съвпада → 1; копие с четвърти клас в маската → „клас извън затворената тройка“ → 1. (**Не „махни клетка“** — точките само се местят и сборът остава 80 510.) |
| **ИА-12** | **нерегрес: v1 още се съди от своя гейт** | `python src/qa_quarters_signed.py docs/archive/quarters_signed_v1_2026-09-06.geojson --registry …` · **само 13 и 18** (преди стъпка 12 архивният път не съществува, Ч `qa_quarters_signed.py:1214-1216`) | **0**, същите 20 проверки, същите числа като Н1 | копие на архива с махнат Feature в `%TEMP%/ia_broken/`, подадено като позиционен път на СТАРИЯ гейт (механика 4 — ИА-О36) → 1 |
| **ИА-13** | **D9 / G15 — дифът, в ДВЕ половини** | (а) `Ф2 <артефакт> --diff <v1>` · (б) същата + `--diff-signed "Р18"`. **`<v1>` = `data/quarters_signed.geojson` на стъпка 9, `docs/archive/quarters_signed_v1_2026-09-06.geojson` на 13 и 18** | (а) **изход 4** (ИА-О32: „иска подпис“ получава собствен код 4, за да не се бърка с argparse-грешката 2, която ИА-STOP 5 чете като грешка на командата) и ред „над праговете на D9: **18 232** места > 2 · площ **+41,3 %** > 10 % — иска подпис“ (ИА-О42) — **това е G15, изпълнен, не отменен, и се записва в ОТЧЕТА като мярка**; (б) **0**, при което `_meta.diff_v1_v2` (записан от Ф1 по `--diff-base`, ИА-О35) носи и гейтът **преизмерва** срещу `--diff`: 60 → **95** Feature · сбор 66,886 → **95,318 km²** · обединение 61,597 → **87,025 km²** · застъпвания **извън `_meta.parent_child`** **40 → 0** · **51 → 17** двойки · **51 общи кода** · **9 изчезващи, поименно** · **44 нови** · покритие **84,85 % → 97,18 %** · по адресна точка **49 397 / 18 232 / 685 / 10 607 / 1 589** (ИА-О42) при `tie_break_v1 = "азбучно най-малък код"` (Р20) | **половина (а) НЕ е отрицателната половина** — тя е гейт върху истински вход и мястото ѝ е в отчета (червен ред 6). Отрицателната половина е: копие на артефакта, в което едно от деветте изчезващи имена липсва от списъка → 1 при (б); и копие с `tie_break_v1` = друг низ при непроменени числа → 1 |
| **ИА-14** | регистърът и видовете | Ф2 · 9, 13, 18 | 0; **всеки от 95-те кода е или в регистъра (84 записа), или поименно в `_meta.registry_pending_new` (39)**; `_meta.registry_pending` == `set(registry) − 95-те` (**28**; ИА-О42); **четирите жилищни кода без Feature (`druzhba, rozova_dolina, sv_ivan_rilski, trakia`) са изброени поименно с довод ВЪТРЕ в `_meta.registry_pending`** (те са регистрови кодове без ред в тялото — подмножество на 28-те; `rows_without_feature.deferred[]` е празна след ИА-О42, §3.Г.1, инвариантът 98 = 90 + 5 + 0 + 3 и `counts.deferred = 0` — ИА-О32 поправя ИА-О31 т. 9); това е множеството `deferred` на ADR 010 D4 в смисъла на регистъра; **`kind` на всеки от 95-те е ключ на `KIND_NORM`** (М: 95/95); **за 56-те кода, които са в регистъра (52 клетки + 4 родителя — нито един от 11-те нови не е в него, М), `kind` == видът на регистъра ДОСЛОВНО — A14 на стария билдър (Ч `build_quarters_signed.py:1106-1107`) се запазва, не се разхлабва (ИА-О34, Х11); за 39-те нови `kind` == `kind_registry` на Ф7 (петте обекта: `кв` по Х13)**; **`"TBD"` като код = червено; нула повтарящи се кодове** | копие с върнат `"code": "TBD"` → 1; копие с `kind: "кв"` за `briz` (регистърът дава `жк`) → 1; копие с два реда на един код → 1; копие с код нито в регистъра, нито в `registry_pending_new` → 1; копие с `kind: "обект"` → 1; копие с `kind: "район"` (суровият imot-вид) → 1 |
| **ИА-15** | районът | Ф2 · 9, 13, 18 | 0; **88 от 95 имат `district` от затворената петорка; точно 7 клетки — `letishte, pz_topoli, vz_zvezditsa, krushkite, lazur, orehcheto, pripek` — носят `district = null` и `district_src = "outside_city_districts"` (Р14, ИА-О42: извън петте района и по нашия слой, и по АГКК); осми null = червено** (М); `_meta.district_witness_disputes` носи **точно 5** реда (`vazrazhdane4, kochmar, so_planova, salzitsa, pristanishte_varna` — всичките с `delivered: true`), първите два със `resolution: "petar_agkk"` от блоба `d494d4d256ae…`, останалите три с `"pending_petar"` (`pristanishte_varna`: наш `odesos`, АГКК `asparuhovo`); `perchemliyata` носи `fire_varna_district` и бележка към подписаната резерва (Ч `qa_place_zone_aliases.py:74`); **знаменателят на свидетеля е 82 = 77 + 5**, а по доставяните клетки — **77 съгласия от 82** (90 минус осемте без АГКК) | копие с изтрит ред от `district_witness_disputes` → 1; копие с `district` извън петорката → 1; копие с `district: null` за клетка извън седемте → 1; копие с `delivered: false` за `pristanishte_varna` → 1 |
| **ИА-16** | **`Ф7` срещу черновия блоб** | `Ф2 --decisions-diff --decisions-path data/quarters_decisions_imot_2026-09-08.json --draft-repo C:/git/Fire_Varna --draft-path scratch/places_search/imot_decisions_1_2026-09-08_draft.json` (на стъпка 6 — Ф7 от диска; на 9/13/18 плюс `--decisions-repo C:/git/varna_3d --decisions-rev <rev>` — ИА-О36/ИА-О38) — **без позиционен път** (ИА-О22) · 6, 9, 13, 18 (ИА-О34: стъпка 9 го включва) | 0; ревизията се намира с `git log -1 --format=%H -- <път>` (**`7556227…`**), sha-то на блоба е **`8626ad43…`**, 51 275 B; дифът е **точно тринайсетте разлики Х1–Х13, нула други** (ИА-О42); **Х8: `plan_body_sha256` == sha256 на блоба на Д1 при `plan_rev` (ИА-О33)**; отпечатва sha на `Ф7` за Gate 2 | (блоб Fire_Varna, дърво `ia_neg_fv`) копие на черновата с четиринайсета разлика → 1; копие, в което Х4 е върнато на `"TBD"` → 1; копие на Ф7 с подменен `plan_body_sha256` → 1 (механика 1 на стъпка 6 — копие в `%TEMP%/ia_broken/` по `--decisions-path`; механика 3 на 13/18 — дърво `ia_neg_v3`) |
| **ИА-17** | **изключените редове** | Ф2 · 9, 13, 18 | 0; `_meta.excluded` носи **6** записа (ИА-О42) **с петорката на v1 `{code, display, kind, registry_action, why}` + `delivered`, плюс незадължителния `status` (за върнатия ред) — гейтът съди ТОЧНИЯ набор „петорка + delivered + подмножество на {status}“** (ИА-О42: `district_witness_note` вече не е тук — `pristanishte_varna` е клетка и спорът му е в `district_witness_disputes`) (ИА-О30/ИА-О32: никакъв ключ `reason` — доводът живее в `why`, както старият C7 го съди, Ч `qa_quarters_signed.py:320-322`): **петте села с `kind_registry = null`, всеки с `delivered: false` и `why` = дословният `kind_note`**, плюс **един със `status: "reinstated"`, `delivered: true`** (`vinitsa_sever`, старият довод дословно в `why`, „Р17“). **Биекция: `kind_registry = null` ⇔ запис с `delivered: false`.** Запис с `delivered: true` иска ред в `_meta.declared_deviations` | копие на артефакта с махнат ред от `excluded` → „ред с null вид без запис“ → 1; копие без реда `vinitsa_sever` → „подписано изключване обърнато без запис“ → 1; копие с `delivered: true` без ред в `declared_deviations` → 1 |
| **ИА-18** | **мета-гейт: половините са тичали** | `python -m unittest tests.test_quarters_signed_imot_fixtures` — **тича САМО в еднократно дърво `%TEMP%/ia_neg_v3`, вдигнато непосредствено преди него и махнато непосредствено след него, в което нищо не е чупено (ИА-О33/ИА-О39; същото правило като Н7′); отрицателните половини по механика 3 (ИА-3, ИА-5, ИА-16) тичат ПРЕДИ него по реда на §5, всяка в свое дърво на същия път, вече махнато (ИА-О39)** · 13, 18 | 0; тестът **ражда фикстурите в `%TEMP%/ia_fixtures`** и **прогонва всяка** с нейния `--only`, настоявайки за изход ≠ 0 **по своя номер** | фикстура, нарочно направена валидна → тестът пада → 1 |
| **ИА-19** | **прозата не носи координати** | `grep -nE -e '\b4[23][.,][0-9]{3,}\b' -e '\b2[78][.,][0-9]{3,}\b' C:/git/Fire_Varna/docs/plans/ПЛАН_Картата_imot_И-А_08.09.md C:/git/Fire_Varna/docs/decisions/011_kartata_imot.md C:/git/Fire_Varna/docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` (абсолютни пътища — командата тича и от varna_3d на стъпки 13/18; ИА-О37) (**ИА-О33: двата `-e` образеца са ИЛИ без знак „тръба“, за да стои командата в клетка на таблица и да се копира дословно; старият запис с екранирана тръба в ERE беше ЛИТЕРАЛНА тръба — гейтът не можеше да падне, М: изход 1 върху ред с координата**) · **преди К1 (Архитект, работното копие на Д1), СЛЕД К1 (Изп, стъпка 2, блобът на Д1 в HEAD, изнесен в `%TEMP%/ia_docs/d1_head.md` като единствен операнд — ИА-О34), преди К8, преди К9, на 13 (в „пълния §5“) и на 18** | **0 попадения** (`grep` изход 1 = няма съвпадение = ЗЕЛЕНО; изход 0 = ЧЕРВЕНО; **ИА-STOP 17 се задейства при изход 0**). Изразът хваща и **българската десетична запетая** (ИА-О30, кръг 5: ширина, записана с запетая, е координата колкото записаната с точка) и три и повече знака след разделителя. Обхватът е **само трите проследени `.md` на този лот**; Д4 е игниран и не се съди. М (Архитект, 08.09 нощ, ИА-О33, с командата в ТОЗИ вид): **0** попадения и в трите (изход 1); същата команда върху счупено копие в `%TEMP%/ia_docs/` с ред с ширина, записана с ТОЧКА, и ред с ширина, записана със ЗАПЕТАЯ → изход 0, двата реда намерени (самите примери не се преписват тук — червен ред 5) | копие на Д1 в `%TEMP%/ia_docs/` с една добавена двойка координати с ТОЧКА → `grep` намира → **ИА-19 червен**; второ копие с двойка със ЗАПЕТАЯ → **червен**. **Двете половини са задължителни: доказват, че изразът хваща и двете нотации** |

**Н1′–Н9′ · нерегрес на базата** — командите **се менят на стъпка 12**, изходите **не**:

| # | команда ПРЕДИ стъпка 12 | команда СЛЕД стъпка 12 | изход |
|---|---|---|---|
| Н1′ | `qa_quarters_signed.py data/quarters_signed.geojson --registry …` | **`qa_quarters_signed.py docs/archive/quarters_signed_v1_2026-09-06.geojson --registry …`** (= ИА-12) | **0** |
| Н2′ | `qa_p8a.py --ledger …` | същата (пътят е по подразбиране, пренасочен от Т7) | **0** |
| Н3′–Н6′ | четирите гейта | същите (константите пренасочени от Т9, Т11) | **0** ×4 |
| Н7′ | `unittest discover -s tests` — **само в `%TEMP%/ia_neg_v3`** (ИА-О33: на стъпка 13 дървото е от HEAD ПРЕДИ К5/К6, Т13 не е там — това е командата „ПРЕДИ“; на стъпка 18 — „СЛЕД“) | същата; сверява се по **16-те идентификатора**, не по броя | **0** |
| Н8′ | `qa_fire_varna_m6.py` | същата | **3**, същите **8** реда |
| Н9′ | `scratch/granitsi/zone_alias_negatives.py` | същата (харнесът П13, пренасочен от Т14) | **0** |

---

## 6 · Комити

**Идентичности** (ADR 010 D16, Ч `010:68`): `Claude Executor <executor@local>` за код и документи; `Petar1984 <petar.dikov2019@gmail.com>` за данни, геометрия и подписи.

**Задължителната двойка команди** (мерено: `git commit -m "…" -- b.txt` върху непроследен файл дава `error: pathspec … did not match any file(s) known to git`, **изход 1**; след `git add -- b.txt` същият комит минава с изход 0):

```
git add -- <изричните пътища>
git -c user.name="Claude Executor" -c user.email="executor@local" \
    commit -m "<точното съобщение>" -- <изричните пътища>
```

Никакъв `git add -A`, никакъв `git add .`, никакъв `--no-verify`. Всяко съобщение завършва с реда `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Тема **≤ 72 знака** (Ч `varna_3d/AGENTS.md:160`), **на английски, без кирилица** — дължините са премерени поименно (М).

| # | стъпка | репо / ръка | съобщение (дословно) | зн. (М) | пътища |
|---|---|---|---|---|---|
| **К1** | 2 | Fire_Varna · **Архитект** (ИА-О32) | `docs(plan): sub-lot I-A signed by Petar (status line)` | **53** | `docs/plans/ПЛАН_Картата_imot_И-А_08.09.md` (само ред-статус; при подпис с редакции те са в отделен предшестващ комит на Архитекта) |
| **К2** | 4 | varna_3d · **Петър** | `data: frozen imot.bg tiling snapshot 08.09, 95 polygons` | **55** | `data/varna_polygons_08.09.geojson`, `data/varna_dictionary_08.09.json`, `.gitignore` (**и четирите unignore реда**) |
| **К3** | 7 | varna_3d · **Петър** | `data: signed decisions for the imot.bg tiling` | **45** | `data/quarters_decisions_imot_2026-09-08.json` |
| **К4** | 11 | varna_3d · Изп | `feat(quarters): imot.bg tiling builder and its own gate` | **55** | `src/build_quarters_signed_imot.py`, `src/qa_quarters_signed_imot.py`, `tests/test_quarters_signed_imot_fixtures.py` — **без фикстури** (ИА-О20) |
| **К5** | **15 (СЛЕД К6 — ИА-О31)** | varna_3d · Изп | `chore(quarters): v1 readers follow it to docs/archive` | **53** | `src/fire_varna_locations.py`, `src/qa_fire_varna_places_export.py`, `src/qa_p8a.py`, `src/qa_place_zone_aliases.py`, `tests/test_quarters_signed_fixtures.py`, `scratch/granitsi/zone_alias_negatives.py`, `docs/archive/РЕГИСТЪР_архив.md` |
| **К6** | **14 (ПРЕДИ К5 — ИА-О31)** | varna_3d · **Петър** | `data: quarters_signed v2; v1 archived, never deleted` | **52** | `data/quarters_signed.geojson`, `docs/archive/quarters_signed_v1_2026-09-06.geojson` (`git mv`), `data/fire_varna_location_inputs.json` |
| **К7** | 16 | varna_3d · Изп | `docs: registry proposal for 39 codes outside the registry` | **57** | `docs/decisions/ПРЕДЛОЖЕНИЕ_регистър_imot_08.09.md` |
| **К8** | 17 | Fire_Varna · Изп | `docs(adr): ADR 011 status line and the 08.09 corrections` | **56** | `docs/decisions/011_kartata_imot.md` |
| **К9** | 17 | Fire_Varna · Изп | `docs(state): section 21 - sub-lot I-A closed, I-B open` | **54** | `docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` (**само новият §21; чужд хънк не се носи — ИА-О19/ИА-О33**) |

**Няма капан на реда.** В И-А **нито един sha на v2 не се пинва в код** — всичките осемнайсет пина гледат v1 по непроменените си стойности. **ИА-О31: К6 (Петър: v2 + архивът на v1) ПРЕДХОЖДА К5 (четците сочат архива)** — иначе К5 е негоден самостоятелен комит, защото сочи файл, който още не е в HEAD (Astra S40 бл. 6); К6 не иска препинване след себе си. Пинът на решенията живее в `_meta.decisions_sha256` и се сверява **по време на прогона** срещу блоба, не като литерал в код (Р9).

**Следпроверка след всеки комит:** `git status --short` (само планираните пътища), `git log -1 --pretty='%h %an %s'` (правилната ръка), `git log -1 --pretty=%s | awk '{print length}'` ≤ 72; за Fire_Varna — `python gates/probe/b3_g11.py HEAD --cached` = 0.

**Котвата за стейджнатия Б3.** OID-ите на 14-те пътя се записват на стъпка 0 и се сверяват след К1, К8 и К9. **В И-А те не мърдат нито веднъж.** Разлика = **ИА-STOP 7**.

---

## 7 · STOP

| # | условие | действие |
|---|---|---|
| **ИА-STOP 1** | пипнат е път извън §3, **или след стъпка 18 е останал ефемерен път/временно дърво НА ЛОТА (ИА-О18) — ново спрямо базовата линия на стъпка 0** (пътищата под `%TEMP%` по ИА-О18 НЕ са пътища от write-set-а, докато лотът тече; чуждите дървета от §1.1 не спират — ИА-О36) | спри, покажи `git status --short` и `git worktree list` за двете репа, питай |
| **ИА-STOP 2** | някой от Н1–Н6, Н8, Н9 или 16-те идентификатора се различава на стъпка 0 (Н7 се мери на стъпка 13; състоянието на Д3 само се записва — ИА-О31) | спри — базата не е тази, за която е писан планът |
| **ИА-STOP 3** | Ф2 или ИА-16 дават изход, различен от очаквания в §5 (за ИА-13 очакваното е **4** без подпис и **0** с него — ИА-О32/ИА-О33; изход 2 там е грешка на командата = ИА-STOP 5), и находката НЕ е в §9 | спри, покажи находките дословно |
| **ИА-STOP 4** | някой от Н1′–Н9′ сменя изхода си (вкл. броя 8 на Н8′) | спри — регресия |
| **ИА-STOP 5** | обявена отрицателна половина **не падна** (изход 0; за ИА-19 — `grep` върна 1 върху счупеното копие), или падна с чужд номер въпреки `--only`, или командата излезе с грешка (изход 2) | спри — по червен ред 6 това е по-лошо от липсващ гейт |
| **ИА-STOP 6** | двата билда не са байт-еднакви | спри — билдът не е детерминиран |
| **ИА-STOP 7** | OID на някой от 14-те стейджнати пътя на Fire_Varna се е сменил | спри — доставката Б3 е пипната |
| **ИА-STOP 8** | одиторът връща НЕГОДНО | спри, върни се при Петър |
| **ИА-STOP 9** | `b3_g11.py HEAD --cached` ≠ 0 след Fire_Varna комит | спри — приватностният гейт е червен |
| **ИА-STOP 10** | измерено число се разминава с §1 | спри и питай — **не** „поправяй“ числото в плана |
| **ИА-STOP 11** | след `git add` на Ф5/Ф6 (стъпка 3), Ф7 (стъпка 6) или `git mv` на Ф8 (стъпка 12) **`git ls-files --error-unmatch -- <път>` ≠ 0** — това е ЕДИНСТВЕНИЯТ критерий (ИА-О34: `git check-ignore -v` без `--no-index` мълчи с изход 1 за всеки път в индекса, М; `!`-правилото на Т1 се показва с `--no-index` като МЯРКА и не спира) | спри — архив и вход, които git не вижда, не са архив и вход |
| **ИА-STOP 12** | иска се `git rm`, `git checkout`, `git stash`, `git reset` или изтриване на подписано тяло | спри — никога |
| **ИА-STOP 13** | появи се нужда от ред в `Fire_Varna/index.html`, `data/`, `tests/`, `gates/`, или в **пети** път на Fire_Varna извън Д1–Д4 | спри — това е И-Б |
| **ИА-STOP 14** | появи се нужда от ред в `Varna_buildings` | спри — това е ръката на Петър по D14 |
| **ИА-STOP 15** | Петър не е подписал някое от **двайсет и едното** решения на §9 | спри — не се подразбира |
| **ИА-STOP 16** | блобът на черновата вече не е `8626ad43…`, **или** блобът на `district_resolutions_2026-09-07.json` вече не е `d494d4d256ae…`, **или** sha на слоя с районите вече не е `26e39b00…` | спри — основата на Ф7 или решаващ вход се е сменил |
| **ИА-STOP 17** | ИА-19 намери координатна двойка в Д1, Д2 или Д3 | спри — червен ред 5 е нарушен в проследен файл на публично репо |
| **ИА-STOP 18** | К5 е комитнат преди К6 (четците сочат `docs/archive/…`, а архивът още не е в HEAD), или който и да е комит е направен извън реда на §4 | спри — редът на комитите е част от подписания план (ИА-О31/ИА-О32/ИА-О33) |

---

## 8 · Локален преглед (кои проби гледа Петър)

Всички проби са **в игнорираната папка** на Fire_Varna и не пътуват никъде:

1. `scratch/boundary_gallery/granitsi_preview_05.09/imot_city_08.09.html` — **95-те полигона**. На око: Чайка, Левски, Владиславово, Аспарухово, Галата седят там, където Петър ги очаква.
2. `…/imot_decisions_sign_08.09.html` — **страницата за подпис**: 98-те реда с `include`, кода, вида, **`kind_registry`** и района. Тук се вземат Р2–Р5, Р7, Р15, Р16, Р17.
3. **Д4** — `…/imot_v2_review_08.09.html`, произведен от Изпълнителя на стъпка 13 (**обявен поименно в §3.Д, игниран, никога проследен**): **95-те Feature-а** върху подложката, оцветени клетка/родител, **плюс 2 274-те точки на маската** (`outside_all_95`; `excluded_cell` и `deferred_cell` са 0 — ИА-О42), **плюс деветте изчезващи имена** върху старите им места и **685-те адреса, които губят дума**.
4. Извадка от **11 кода в 9 точки** за сверка на око (правилото „примери за поправка“): `avtogara` (район→кв, Р15) · двойката на общия ръб `chaika_kk` / `alen_mak` (Р6) · `perchemliyata` (единственият „да“ без свидетел АГКК) · `so_planova` и `salzitsa` (двата нови спора) · **`vinitsa_sever`** (изключеният от 07.09, който v2 връща — Р17) · **`pobeda_group`** (единственият виртуален родител, който v1 не познава, и единственият вид без извор — Р16) · **`sveti_nikola`** (единствената клетка, при която регистърът ОТНЕМА кварталната дума — Х11/Р16, ИА-О35) · **`letishte`** (обект → клетка без район, Х12/Х13, Р14 — ИА-О42) · **`grand_mol`** (обект → клетка `кв` в Младост, 274 адреса получават дума — ИА-О42).
5. **Живата карта НЕ се пипа и НЕ се гледа** — тя е на старите данни; това е И-Б.

---

## 9 · Решения за Петър (Gate 1) — **двайсет и едно**

Всяко решение има **предложение по подразбиране**. „Да“ на плана без възражение = приемане на предложенията. **Седемте отворени точки, които самото тяло изброява в `_meta.decisions_open` (Ч), са покрити от Р15/Р16, Р3, Р4, Р7, Р14, Р8, Р10.**

| # | Въпрос | Измереното | **Предложение по подразбиране** |
|---|---|---|---|
| **Р1** | **Къде живее подписаното тяло?** | Днес е `Fire_Varna/scratch/places_search/imot_decisions_1_2026-09-08_draft.json` (проследен, публично репо, блоб `8626ad43…`, 51 275 B). Носи `n_pts` (3 820) и `area_km2` (95 реда). Машинарията за подпис във Fire_Varna е ЧЕРВЕНА (ИБ-1); varna_3d е ЗЕЛЕНА (§1.2). | **МЕСТИ се в `varna_3d/data/quarters_decisions_imot_2026-09-08.json`.** Черновата остава непокътната; архивирането ѝ е И-Б. **ADR 011 И-D3 се поправя с изричен ред в К8.** |
| **Р2** | **Виртуалните родители Feature-и ли са?** | И-D4 (Ч `011:19`) ги иска като обединения; v1 носи 4 от 5 като Feature-и; М: и петте обединения дават **единичен валиден `Polygon` без вътрешни пръстени**. | **ДА — 95 Feature-а (90 + 5; ИА-О42).** Иначе 17 деца сочат родител без геометричен носител, а списъкът „изчезващи“ скача от 9 на 13. |
| **Р3** | **Петте „обекта“ — ОБЪРНАТО от Петър (08.09 ~14:30, ИА-О42)** | Тялото им дава **`kind_registry = null`** с довод „обект: не е клетка → без вид“ (Ч) — класификация на Архитекта, не на imot.bg: за imot.bg „Бизнес хотел“ или „Гранд Мол“ е район около ориентир, точно както „Операта“ и „Автогара“, които са клетки. И петте имат полигон; и петте **липсват от регистъра**. **Държат 1 025 адресни точки** (355/354/274/26/16, М — колоната `buildings` брои адресни редове, Ч `imot_decisions_draft_08.09.py:87-93`). Кими К40-3 ги приемаше за изключени; Петър реши обратното, защото търсачката работи и по координата и думата е нужна. | **ВКЛЮЧЕНИ като клетки** (Х12: `include = "да"`; Х13: `kind_registry = "кв"`, `cls = "клетка"`). Точките им са в `in_one_cell`; покритието е **97,18 %**; `letishte` няма район (Р14). Думата на Петър: „понеже търсачката ни има възможност за търсене на геокоординати директно … мисля че е добра идея да ги включим“ → „давай“. |
| **Р4** | **Трите без полигон** — 5848, 5866, 6294 | Речникът ги знае, imot няма полигон; `buildings = null`, `area_km2 = null` (М). `malka_chaika` е в регистъра като `м-т`, а тялото му дава `kind_registry = "кв"` (М) — **поправя се в Ф7 с Х11 (26-ият ред; Кими К40-5, ИА-О41)**, макар редът да е `no_boundary` и да не стига до Feature. | **`no_boundary` — БЕЗ Feature.** Изброяват се в `_meta.rows_without_feature.no_boundary[]` с довод. **Не** се прави Feature с `geometry: null` — **обявено отклонение от ADR 010 D4** (`:44` предвижда Feature с `geometry: null` за `no_boundary`): в v2 гейтовете броят Feature-и = клетки, затова `no_boundary` живее в `_meta.rows_without_feature.no_boundary[]` (ИА-О31; амандаментът на D4 влиза в Д2 с К8). Разминаването по `malka_chaika` влиза в Ф10. |
| **Р5** | **Двата кода `"TBD"`** | М: 98 реда → 97 различни кода. | **`elektrorazpredelenie_varna` и `severna_promishlena_zona`.** Реални, уникални, по правилото на Кими К37 (slug). ИА-14 прави `"TBD"` и повтарящ се код червено. |
| **Р6** | **Точката на общия ръб** (една) | Върху **суровите** полигони — **0** двойни попадения; след канонизацията по D1 — **1**, между **5055 к.к. Чайка** и **5053 м-т Ален мак** (М). **(Координатата ѝ живее в частния файл и не се преписва тук.)** | **Развръзка по най-малък `raion_num`.** Записва се в `_meta.mask.tie_break` и `tie_break_rule`, проверява се от ИА-11. Тук печели **5053 „м-т Ален мак“**. |
| **Р7** | **Единайсетте отложени — ОБЪРНАТО за шестте с вид (Петър, 08.09, ИА-О42)** | Шест носят вид; **пет са села с `kind_registry = null`**. И единайсетте имат **0 адресни точки в нашия адресен регистър** (низът „0 сгради“ в черновата значи точно това), нито един няма район. **Селата НЕ са празни** — Кими К40-4: обяви Звездица 108, Тополи 185, Константиново 61 — те са **извън покритието на адресния регистър** (град Варна); вилните зони и местностите също. Сумарно 10,691 km². | **Правилото става просто: всеки полигон с вид е клетка.** Шестте с вид (`pz_topoli, vz_zvezditsa, krushkite, lazur, orehcheto, pripek`) → **клетки** (Х12; днес без адреси — ще потрябват при думата за хидрантите и за адреси извън града); `deferred[]` остава празна кофа. **Петте села → `_meta.excluded[]`** (null вид = изключен ред; моделът няма вид „село“ — отделен лот). Шестте нови клетки нямат район (Р14). |
| **Р8** | **`precision_m` за плочката** | Медианата на ръба на **90-те канонизирани клетки** е **94,7 m** (М, ИА-О42; за 79 беше 96,5). v1 дава 100 за `human_polygon`, 50 за `street_bounded_face`; подът по D7 е **50** (Ч `010:50`). | **`precision_m = 100` за всичките 95.** Измерената рисувателна стъпка е под 100 и над пода 50. |
| **Р9** | **Как се пинва тялото?** | Литерал `DECISIONS_BLOB_SHA256` в гейта трябва да е написан **преди** комита К3, който ражда блоба — капанът, който събори кръгове v2–v5. | **Без литерал.** Гейтът чете `_meta.decisions_sha256` и настоява да е равно на `sha256(git show <rev>:<път>)` на файла, който наистина е прочел. **Човешката котва за Gate 2 е изходът на ИА-16** („тринайсет наименувани разлики, нула други“) плюс отпечатаното sha. |
| **Р10** | **Атрибуцията и `source_terms`** (попълва се, не се обсъжда) | C9 на v1 иска на извор **шест** ключа (Ч `qa_quarters_signed.py:362`) — **за v2 важи ИА-8 с ТРИ** (ИА-О30/ИА-О31); ADR 011 И-D2 дава два — поправя се в К8. **`_meta.list_endpoint` е с опашката** (М). | **`_meta.attribution` и `properties.attribution`:** „Кварталните граници в този файл са районите на imot.bg (© imot.bg), снапшот 08.09.2026. Геометрия НИКОГА не пътува публично.“ · **`license_label`:** „граница по районите на imot.bg (район `<raion_num>`, свалена 2026-09-08) — извор на всекидневната употреба, не официален кадастър“ · **`url` = чистият адрес `https://www.imot.bg/obiavi/prodazhbi/grad-varna`** (без описателната опашка на `_meta.list_endpoint`) · **`_meta.source_terms.imot_bg` = ТОЧНО ТРИ ключа:** `{url: "https://www.imot.bg/obshti-uslovia", recorded_on: "2026-09-08", position: "Публично пътуват само име, код и src на квартала; геометрията остава в частното репо."}` — **без `quote`, `terms`, `verdict`, `checked_on`**: никой не е отварял страницата, а дата на „проверка“ в подписан артефакт би била атестация за нещо, което не се е случило (ИА-О30). Докато присъда няма, **нищо от този артефакт не пътува публично** — записва се в `_meta.publication`; дългът е **ИБ-19** (§11). **Алтернатива само с изрична твоя дума:** Изпълнителят отваря страницата, преписва абзаца дословно в `quote`, вписва `checked_on` и `terms`, ти подписваш присъдата с ред в опашката — тогава ключовете стават шест и ИА-8 се пренаписва за тях. |
| **Р11** | **Архивните пътища** | `data/archive/` е **механично невъзможен** (М, §1.9); `docs/archive/` вече съществува и е проследен (М). | **`docs/archive/quarters_signed_v1_2026-09-06.geojson`** (+ ред `!…` в блока на Т1) и **`docs/archive/РЕГИСТЪР_архив.md`**. Старите `granitsi_decisions_2/3/4_*.json` **не се местят в И-А** — те са пиннати от `build_quarters_signed.py:89` и от `Fire_Varna/tests/test_b3_gates.py:34`; преместването им е И-Б. |
| **Р12** | **Двата нови спора за район** — `so_planova`, `salzitsa` | `kochmar` и `vazrazhdane4` вече имат подписано решение (блоб `d494d4d256ae…`, ревизия `e6c706d…`, М). Знаменателят на свидетеля е **82** (77 + 5). | **Нашият слой печели; спорът се ЗАПИСВА, не се решава.** `district_src = "fire_varna_district"`, двата влизат в `_meta.district_witness_disputes` с `resolution: "pending_petar"`. |
| **Р13** | **Къде живее замразеният вход?** | Днес и двата файла са само в **игнорираната** папка на **публичното** репо (М). Никое репо не ги носи. | **Копират се в `varna_3d/data/` и стават проследени там** (Ф5, Ф6; комит на Петър). Иначе v2 не е възпроизводим от нито едно репо. |
| **Р14** | **Шестнайсетте кода без район от АГКК — и седемте клетки извън петте района (ИА-О42)** | Трите без полигон по Р4 и петте села по Р7 не са Feature-и; `perchemliyata` е покрит от вече подписаната поименна резерва (Ч `qa_place_zone_aliases.py:74`) и получава `primorski` от нашия слой (М). **Седем клетки „да“ — `letishte, pz_topoli, vz_zvezditsa, krushkite, lazur, orehcheto, pripek` — са извън петте района на града и по нашия слой, и по АГКК (М, представителна точка / центроид)**: летището и местностите край Тополи, Каменар и Звездица са на землища извън ЕКАТТЕ 10135. **Черновата казва „13“; вярното е 16** — поправя се с Х10. | **`district = null`, `district_src = "outside_city_districts"` за седемте, поименно и затворено; ИА-15 допуска точно тях.** Нищо не се измисля (най-близък район би било измислица); думата за район при тях остава празна, докато слоят с районите не покрие землищата — дълг към И-Б/стъпка 2. Този ред е за да го видиш и да кажеш „да“ веднъж. |
| **Р15** | **`район` → `кв` или `мр`?** (19 клетки) | Тялото само го маркира „**ДА СЕ ПОДПИШЕ**“ (Ч). `мр` е в `KIND_NORM`, но `KIND_CLASS` го праща в **locality** (Ч `fire_varna_locations.py:156-160`), а по D6 locality **никога не пълни `quarter`** — 19-те най-всекидневни имена (Автогара, ЖП гара, Операта, Гръцката махала…) биха останали без квартална дума. | **`район` → `кв`.** Записва се в `_meta.kind_registry_decisions` (Х7). Алтернатива `мр` — само с изрична твоя дума, и тогава И-Б трябва да отвори `LOCALITY_CODES`. |
| **Р16** | **Видът идва от подписания регистър — петте виртуални родителя И 26 реда (25 клетки + `malka_chaika` без полигон)** (ИА-О34/ИА-О41) | Полето `kind` на родителите е проза (М). Четирите съществуват в **подписания регистър** и във v1 с един и същ вид (М). `pobeda_group` няма ред никъде; двете му деца са `кв` (М). **25 клетки и един ред без полигон (`malka_chaika`) носят по черновата вид, различен от регистровия (Х11, поименно); 16 от клетките сменят класа quarter/locality** (М). Кими К40-2 потвърждава регистъра ред по ред (Спека 12.08 за Свети Никола и Бриз, Р27–Р37 на регистъра за с.о.-групата); единственият регистров вид без записан довод е `evksinograd` (`кв`, празно `why`) — Ф10 го отбелязва за твоята ръка. | **Четирите родителя се преписват от регистъра; `pobeda_group → "кв"`; 26-те реда от Х11 вземат вида на регистъра дословно** — регистърът е твоята ръка (D14), префиксът на imot е свидетел. **Цената, поименно (ИА-О35): 15 клетки ПЕЧЕЛЯТ кварталната дума (14 × `с.о.` + `evksinograd`), ЕДНА я ГУБИ — `sveti_nikola` (чернова `кв`, регистър `м-т` = locality); тя влиза в извадката за окото (§8 т. 4).** Ако кажеш „не“ за някой код, редът му излиза от Х11 и Х11/ИА-14 се преизмерват преди изпълнението. |
| **Р17** | **`vinitsa_sever` — обръщане на подписано изключване** | v1 `_meta.excluded` носи един ред за него (Ч дословно); черновата го дава като клетка 6295, `include = "да"`, `kind_registry = "вз"` (регистърът: `с.о.` — Х11 печели, класът е същият: quarter), 294 сгради, 0,303 km² (М). | **Обръща се съзнателно: v2 го доставя.** Записва се като **шести** запис в `_meta.excluded` (ИА-О42: петте села + този) със `status: "reinstated"`, `delivered: true`, стария довод дословно и „Р17“, плюс ред в `declared_deviations` и в И-D5. **Ако кажеш „не“ — редът излиза от 90-те и v2 става 89 + 5 = 94; всяко число в §1.5, §1.7 и §5 се преизмерва преди изпълнението.** |
| **Р18** | **D9 / G15 — дифът е над праговете** | Ч `010:54` и `:129`. Мерено (ИА-О42, 90 клетки): **18 232** места сменят думата, обединената площ расте с **41,3 %** (61,597 → 87,025 km²). | **Подписваш дифа.** ИА-13 (а) тича без подпис и връща **4** („иска подпис“ — ИА-О32/ИА-О33; 2 би било argparse-грешка = ИА-STOP 5); ИА-13 (б) тича с `--diff-signed "Р18"`, преизмерва всичко и връща 0. **G15 не се отменя — изпълнява се.** |
| **Р19** | **Амандамент на ADR 010 D0/G23** | Ч `010:36`: „всяко бъдещо разминаване АГКК ↔ `district.code` = STOP“; Ч `:137` регистрира G23. М: `varna_3d/src/qa_district_official.py` **не съществува**. Плочката ражда два нови спора (Р12). | **Ред в ADR 011 И-D5** (не в ADR 010, който е извън write-set-а): за плочката АГКК AU5 е **независим свидетел**; разминаване, **записано поименно** с `resolution` (подписан) или `"pending_petar"`, не е STOP; разминаване **без запис** е STOP. ИА-15 го изпълнява; дългът е ИБ-16. |
| **Р20** | **Правилото за развръзка на страната v1 в дифа** | М: **4 517** от 80 510-те точки попадат в ≥ 2 полигона на v1 — без обявено правило числата „същата/сменена дума“ не са възпроизводими (кръг 3 обори двойката 49 926/17 453). Три правила дават три различни резултата (М, ИА-О35, премерено за 79-те клетки на редакция 4 — правилото не зависи от броя клетки: азбучно 49 397/17 982; най-малка площ 50 100/17 279; най-голяма площ 48 662/18 717; „по реда на файла“ съвпада с азбучно, защото Feature-ите на v1 са подредени по код — М: списъкът на кодовете == `sorted(...)`; старата двойка „49 925/17 454“ не се възпроизвежда при никакъв прочит и е махната). | **Азбучно най-малкият код печели.** Довод: детерминизъм, независим от реда на файла — същият клас правило като Р6. Записва се в `_meta.diff_v1_v2.tie_break_v1` и се преизмерва от ИА-13. Числата (ИА-О42): **49 397 / 18 232 / 685 / 10 607 / 1 589**. |
| **Р21** | **Слоят с петте района — вход, който git не вижда** | М: `C:/git/m6000_private` **не е git репо**; файлът е 201 130 B, sha256 `26e39b00…`, дава точно 5 района с `authority == "osm"`. Той решава `district` на 88 от 95-те Feature-а (седем са извън него — Р14, ИА-О42). | **Влиза като ШЕСТИ вход в `_meta.inputs`, пиннат по sha256 на прочетените байтове**, с `note` защо не е блоб. Алтернатива: да се копира и във `varna_3d/data/` — **не се предлага в И-А**, защото е чужд слой и иска отделно решение за собственост; дългът е **ИБ-17**. |

---

## 10 · Какво НЕ се пипа

1. **`Fire_Varna/index.html`** — нито един байт. `QUARTER_SRC`, `LOCALITY_SRC`, `DISTRICT_CODES`, `QUARTER_CODES`, `LOCALITY_CODES`, `ATTRIB_KEYS`, `ATTRIB_SRC`, `validQuarterAttribution`, кешът, трите sha пина, `LEGACY_BUNDLE_SHA` — всичките са И-Б.
2. **`Fire_Varna/data/`** — `places.json`, `hotels.json`, `place_categories.json`, `address_rows.json`, `approx_addresses_v1.json`, хидрантите. Само за четене.
3. **`Fire_Varna/tests/` и `Fire_Varna/gates/`** — включително `gates/probe/b3_g11.py` (`GEO` `:37`, `LEGACY` `:39`, `ALLOWED_NEW` `:40-46`), `gates/release.py`, `gates/coverage.py`, `gates/allow/`, опашката `ЗА_ПОДПИС_*`, `tests/test_b3_gates.py`.
4. **Стейджнатата доставка Б3** — 14-те пътя, с непроменени OID. **Двата мръсни нестейджнати JSON-а** също не се пипат.
5. **`Varna_buildings`** — регистърът (84 записа, `schema_version 1.1`, 15 ключа) е подписано тяло по D14. И-А произвежда **само предложение**.
6. **`varna_3d/src/build_quarters_signed.py` и `src/qa_quarters_signed.py`** — нула байта (ИА-О7).
7. **Пиновете по sha на v1** — `fire_varna_locations.py:94`, `qa_p8a.py:60-66`, `qa_place_zone_aliases.py:75`, двата блока в `fire_varna_location_inputs.json`, `test_quarters_signed_fixtures.py:172`/`:175`. v1 не мърда байт.
8. **`scratch/refactor/_addr/lot1v_locations_375_p8a_07.09.json`** — подписан леджер и исторически запис (ИА-О14). **`src/build_quarters_candidate.py` и `src/qa_quarters_candidate.py`** — червените им черти по базово име (П16) остават непокътнати.
9. **`docs/decisions/010_signed_quarter_boundaries.md`** — нула байта. Амандаментът по Р19 живее в ADR 011.
10. **`C:/git/m6000_private`** — само за четене; И-А не копира и не мести слоя (Р21).
11. **Адресният резолвер** и планът „Адресите по полигон“ — отделен лот. **Живата карта, Worker-ът, R2, публикуването.** Нищо не се публикува; пушът е на Петър.
12. **Лицензът като тема.** Полето се попълва по Р10 и толкова.

---

## 11 · Пренесено към И-Б (измерени дефекти, които И-А не докосва)

**ИБ-1 · Базата на Fire_Varna е ЧЕРВЕНА и не е поправена от този лот.** М днес: `python -m unittest discover -s tests` → **342 теста, 24 паднали** (**21 в `test_places_search_gate`, 3 в `test_b3_gates`** — М по модул); `python -m gates.run_gates` → **изход 1**; `python -m gates.release` → **изход 6**; `python -m gates.coverage` → **изход 4**. И-Б трябва да обяви интервала по D19 (Ч `010:74`), да изброи точния набор падащи тестове и да не позволи нищо извън него.

**ИБ-2 · `scratch/places_search/expectations.json` е пренаписан след подписа** — М (`gates.release`): „тялото **55cbf2c7f8f2** ≠ дайджеста на ред R8 (**f8c4e43f5e85**) — пренаписано след подписа“; плюс „✗ двигателят не е кандидатът, който е подписан: **a73fa006ab0b** ≠ **61967588780f**“ и мръсния `lot1v_v_reference_manifest.json`.

**ИБ-3 · Покритието е пробито ПРЕДИ лота.** Гейт 4 на `run_gates` (М, дословно): `places zone_named: before=127 after=91 lost=44 changed=44 gained=8`; `hotels zone_named: before=199 after=167 lost=39 changed=11 gained=7`; „coverage изход 2 — 30 непокрити загубени/сменени реда“. `gates/allow/2026-09-05_lot1v_v.json` не ги покрива.

**ИБ-4 · Приватностно нарушение вече в HEAD:** `scratch/places_search/adresi_astra_S39_08.09.md` (проследен, М) носи дословно `"advert_id"`, `"raw_sha256"` и ендпойнта на `mob_api/details`; **координатните двойки в кутията на Варна**, които той, `adresi_astra_S40_08.09.md` и `karta_imot_plan_reviews_08.09.md` носеха (М, ИА-О34: 3 + 1 + 2 реда по израза на ИА-19), са **изчистени от Архитекта на 08.09** до „(коорд.)“ — остава провенансът в S39. **Обхватът е по-широк от лота:** същият израз намира координатни числа в **~80** проследени `.md` на Fire_Varna (М: 84 преди изчистването на трите записа, 80 след него — ПРОМЕНЛИВО число, премерва се при И-Б; стари одити, планове и записи от 05.2026 насам — ИА-О34/ИА-О36) — сметката е на ИБ-18 (правило + гейт), не на И-А. `b3_g11.py` е **структурно сляп** за `.md` (обхватът му е проследени `data/*.json` ∪ променени `*.json/*.geojson`, **Ч `:105-106`**). Иска се изчистване или подписано изключение + разширяване на обхвата.

**ИБ-5 · Приватностното правило „списък от ≥ 4 числа“ е fail-open за именувани координати.** Пръстен `[{"lat":…,"lon":…}, …]` минава и през него, и през черния списък по имена.

**ИБ-6 · `index.html` — котвите за пребазиране (М, проверени поименно):** `:1901`/`:1918` `OSM_ATTRIB`; `:6268` `PLACES_CACHE`; `:6291-6293` трите sha пина (`HOTELS_SHA256`, `PLACES2_SHA256`, `CATS_SHA256`); `:6304` `LEGACY_BUNDLE_SHA`; `:6316` `QUARTER_SRC`; `:6320` `LOCALITY_SRC`; `:6322` `DISTRICT_CODES`; `:6324` `QUARTER_CODES` (**28** кода, М); `:6332` `LOCALITY_CODES` (**5**, М); `:6619` `ATTRIB_KEYS`, `:6620` `ATTRIB_SRC`, `:6625` `validQuarterAttribution` (викан на `:6579` и `:6600`).

**ИБ-7 · `tests/test_b3_gates.py`:** `:34` `DECISIONS_REL` сочи отменения `granitsi_decisions_4_2026-09-07_proba.json`; `:90-91` чете `doc["attribution"]` от **работното дърво**; `:172` `len(want) == 338`; `:174` sha `c6d29d4e…`; `:194` блоб; `:197` `yes_row_authorship(...) == []`. **Три от тестовете падат днес** (М).

**ИБ-8 · Опашката и allow-ът:** `queues_in_head` глобва по basename (Ч `gates/release.py:437`); `signable()` (`:189`) глобва **работното дърво** и при брой ≠ 1 ключът `allow` изчезва без грешка; `gates/coverage.py:87` `SIGNER = "Петър"`, `:96` `is_signed_by_petar`.

**ИБ-9 · `ALLOWED_NEW`** (Ч `gates/probe/b3_g11.py:40-46`, седем записа) е затворена седморка; всеки нов проследен път на доставката иска ред там. `LEGACY` е `:39`, `GEO` е `:37`.

**ИБ-10 · `scratch/places_search/qa_night_pins.py` е ЧЕРВЕН по конструкция** (пинове от 06.09). Механиката иска отделен, чист скрипт; `--worktree-negative` доказва проследеност и сверява `worktree_sha256` (Ч `:15-20`, `:111`, `:118`, `:171-175`), **не подменено съдържание на блоб** (§5).

**ИБ-11 · Преизносът на местата и хотелите + леджерите.** Регистърът първи (D13), после `EXPECT_SIGNED_CODES` (60 → **95**), `ATTR_SRC`/`ATTR_LABEL`/`ATTR_LICENCE`/`ATTR_URL_HOST`, `by_source` в резолвера (`fire_varna_locations.py:601-618`), **ЧЕТИРИТЕ екземпляра** на `KIND_NORM`/`KIND_CLASS` с **четирите различни отказа** (М, `git grep`): `src/fire_varna_locations.py:152`/`:156` → `die` на `:647-648`; `src/qa_fire_varna_m6.py:113`/`:117` → **гол `KeyError`** на `:210`; `src/qa_fire_varna_places_export.py:355` → **мълчалив `DROP`** на `:401`; **`src/qa_fire_varna_export.py:201` → тих `None` на `:247`** (`KIND_NORM.get(e["kind"])`). Плюс `qa_p8a.py:60-66`, двата блока в `fire_varna_location_inputs.json`, `polygon_version`/`polygon_sha256` по D3, и **П17**. **И измереното последствие на И-А (след Х11 и ИА-О42):** от 95-те доставяни кода **23 са клас `locality`** (м-т 13 · местност 4 · пз 4 · зона 2; за 84 бяха 18), а доставяните `kind` включват `с.о.` (15) и `местност` (4) — стойности, които консуматорите виждат за пръв път и ЗАДЪЛЖИТЕЛНО минават през `KIND_NORM` и по D6 **никога не пълнят `quarter`** — И-Б трябва да реши какво вижда потребителят на тези места.

**ИБ-12 · Деветте изчезващи имена** (`abatko, druzhba, gorchivata_cheshma, kokardzha_generic, morska_gradina, rozova_dolina, sredna_traka, sv_ivan_rilski, trakia`), **685-те адреса, които губят дума** (ИА-О42; изключени клетки с точки вече няма) и публичните редове на `data/approx_addresses_v1.json` — таван на загубите и поименен allow.

**ИБ-13 · Номерацията на гейтовете** — `G4`, `G15`, `G17`, `G21`, `G22`, `G23` значат по две неща в ADR 010 и в живия план Б3; **`О1`–`О30` са заети** в `ПЛАН_Б3_07.09.md:234-263` (М). И-Б трябва да пребазира номерацията, преди да пише таблица.

**ИБ-14 · `G29` не е регистриран в ADR 010** (М: `grep -c G29 010_signed_quarter_boundaries.md` → **0**; таблицата върви G0–G24), а СЪЩЕСТВУВА и тича: дефиниция в `ПЛАН_ЛОТ_Границите_v2_амандамент_2_06.09.md:107`, построен в `qa_night_pins.py:206-242` с отрицателна половина `:402-444` (ИА-О34 поправя старата котва `:71`). Дългът: ред в регистъра на гейтовете на ADR 010/011, не име на файл.

**ИБ-15 · `n_pts` и `area_km2` в черновата на Fire_Varna** (ИА-О9) — производни на частната геометрия в проследен файл на публичното репо; изчистване или подписано изключение, плюс архивиране на самата чернова, след като Ф7 е в HEAD.

**ИБ-16 · `qa_district_official.py` (ADR 010 G23) не е построен** (М). Амандаментът по Р19 го превръща в дълг с обявена котва; И-Б или го строи, или го пенсионира с ред в ADR 010.

**ИБ-17 · Слоят с петте района няма версия.** `C:/git/m6000_private/number_viewer/quarters_layer.geojson` (201 130 B, sha `26e39b00…`) решава `district` на всеки Feature на v1 И на v2, а **папката не е git репо** (М). И-Б трябва да реши: собствен проследен екземпляр, submodule, или подписан пин по байтове с процедура за смяна. Днес И-А го пинва по байтове (Р21, ИА-О23).

**ИБ-18 · Производните на частната геометрия в ПРОЗА нямат нито правило, нито гейт.** И-А добавя три проследени `.md` в публичното репо с площи по обект, статистика на ръба, брой върхове/отсечки, Hausdorff и sha-та на частни файлове; ИА-19 съди само **координати**, ИА-7 съди само **данни**. Че дупката е реална, а не теоретична, показва ИБ-4. И-Б трябва да напише правилото (какъв агрегат е позволен) и гейта, който го съди — за `.md` също, не само за `.json`.

**ИБ-19 · Условията на извора imot.bg нямат присъда.** И-А записва само `{url, recorded_on, position}` в `_meta.source_terms.imot_bg` (Р10, ИА-О30) и заключва публикацията в `_meta.publication`. Присъдата (кой отваря страницата, кой преписва абзаца, кой подписва реда в опашката) е дълг на И-Б преди първата публична доставка Б4; никакъв лицензен разговор с Петър — само ред клас „въпрос“ с `тяло: <sha>` (червен ред 11).

**ИБ-20 · Скан за суров провенанс в прозата.** ИА-19 хваща координати; никой гейт не хваща 64-знакови хешове на сурови отговори в проследените `.md`. И-А ги маха на ръка (ИА-О31, Astra S40 бл. 1); И-Б добавя към ИА-19 израз за `[0-9a-f]{64}`, който допуска само хешовете на БЛОБОВЕ (`git show`) и на проследени файлове.

