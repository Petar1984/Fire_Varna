# ПЛАН · „Проблемите“ — **Лот О · Обратна връзка за приложението**

`docs/plans/ПЛАН_Проблемите_лотО_обратна_връзка_15.09.md` · **15.09.2026** · **v1.3 (след К6 и финалния одит — §9) — v1.0 ПОДПИСАН от Петър на 15.09.2026 (Gate 1): „подписвам но да внимаваме да не счупим нещо работещо и да не създадем проблеми и бъгове“ — Р1–Р10 и Н1–Н8 както са предложени; думата му е и гейт: нито един стар тест не се пренастройва, старите клонове на формата са байт-равни на базата (Г-О2/Г-О4), Г11 в браузъра преди одита** · Планировчик (Opus, read-only), транскрибиран от Архитекта · база **758f35e** (`HEAD` = `problems` = `main` = `origin/main`) · дърво `C:/git/_wt/fv_problems` · верига: Планировчик → **Gate 1** → Изпълнител → Одитор → Astra (механика) + Кими (данни) → Gate 2 → **Петър пушва**.

**Клас на риска: 🔴 АРХИТЕКТУРЕН (Вариант А: амандамент + отделен Одитор + двете външни лещи).** Спуснати спусъци: (1) **схемата на подаването** расте с ШЕСТИ `report_type` и ДВА нови ключа в главата на YAML-а (договорът от 09.05 + амандаментът от 13.09); (2) ново **семейство етикети в ПУБЛИЧНОТО хранилище** — външна повърхност с предусловие извън кода (Р8); (3) нов публичен бутон в **доставяемото** `index.html`; (4) много места в един подсистем. **Не са спуснати:** нула байта в `data/`, `worker/`, `sw.js`; нула флаг/публикуване; нула нов извор; нула кросрепо; нула канонична мутация. Затова лотът носи **К1б — амандамент**, не нов ADR: точно прецедентът на лот 1 (`docs/audits/амандамент_подаване_13.09.md`).

**Думата на Петър (15.09, подписана рамка):** „нека е в публичното — действаме според препоръките“. Образецът е работещият прототип `scratchpad/group2/proto_feedback_v2.py` (шест плочки, кутия с контекст, която се пречертава при всяка плочка). Доставя се неговата форма **МИНУС** прототипните части (преглед вместо POST, `console.log`, кръпката на `payload.labels` в `submitReport`, `\u`-екранировките) **ПЛЮС** Р1–Р10.

**CWD: `C:/git/_wt/fv_problems`**; пред всяка проба `PYTHONIOENCODING=utf-8`.

---

## §0 · Червени редове

1. Агент не пуска `git push`; подагентите са на Opus.
2. Ръцете: `index.html`, тестове, гейтове → `Claude Executor <executor@local>`; план и амандамент → `Claude Architect <architect@local>`.
3. **Нула байта** в `data/`, `worker/`, `sw.js`, `scripts/`, `scripts/hooks/`. Съди се машинно (Г6). *(v1.1, Р-К5 (з): единствено изключение `scripts/build_reports_dashboard.py` — четвъртият консуматор на потока, който одитът намери.)*
4. **НЕ се пипат:** `tests/negative_halves_manifest.json`, `tests/test_negative_halves.py`, `tests/golden_apply_v0.py`, `tests/granitsi_client_probe.mjs`, `gates/release.py`, `gates/run_gates.py`, `gates/probe/lot_perimeter.py`, `gates/probe/ib1_g15.py`, `expectations.json`, **и всяка функция на `initAddressSearch`** (ADR 006 D12 — куката за търсенето живее ИЗВЪН IIFE-то, затова лотът няма амандамент 006d).
5. Гейт = команда с очакван изход + отрицателна половина, която **ТИЧА и ПАДА**. Половина без измерена червенина не се доставя.
6. Фикстури — само в `$TEMP/fv_lotO`; нула фикстури в дървото; **нула низове, които палят ИБ1-Г15** (`4[23][.,]\d{3,}` и `2[78][.,]\d{3,}`) — фалшивата „координатна“ заявка е `11.111111, 22.222222`.
7. Базата се пинова като литерал **`FIRE_VARNA_LOTO_BASE=758f35e`**; никога `FIRE_VARNA_BASE_COMMIT`.
8. UI-низовете влизат като **истинска кирилица**, не `\uXXXX` (екранировките са артефакт на кръпчика). Subject: английски, ≤ 72 знака; `git add` само с явни пътеки.
9. **Никакъв истински POST** към Worker-а: Г11 върви на **порт 8001**, който не е в `ALLOWED_ORIGINS` → preflight 403 (§1 ред 14). Нула issue-та в публичното хранилище преди Петър да създаде етикетите.

---

## §1 · Мярката (Планировчик, четене+grep при 758f35e — никой да не мери наново)

| # | Мярка | Стойност |
|---|---|---|
| 1 | Котвите от заданието | **потвърдени 1:1**: FAB `:1705`/`:3600-3602`; пикер `:2769-2821` (бутони `:2774-2785`, капанът предизбор `:2812-2818`); `modalTitleForType :2824-2832`; `nameFieldHTML :2872-2878`; `typeFieldsHTML :2924-2984` (край `return '' :2983`); `showReportModal :3001-3040`; име 2..50 `:3102-3106`; `readDraftFromForm :3042-3062`; `wireFormHandlers :3064-3091`; `buildReportObject :3155-3194` (описание `:3177`); `dedupHash :3196-3199`; `buildIssueTitle :3207-3213`; ред на YAML `:3224-3229`; `typeNames :3237-3243`; `labelsForType :3275-3284`; `submitReport :3303-3317`; 422 `:3342-3360`; `queueReport :3379`; `applyReports` бележката `:4015-4024` |
| 2 | `index.html` | **8119 реда**, ≈595 936 B (Ф0 мери байтовете наново) |
| 3 | `labelsForType` — всички викащи | **три места**: дефиниция `:3283`, `submitReport :3308`, `retryQueuedReports :3401`. Смяна на подписа на `(report)` пипа точно два реда |
| 4 | опашката при 422 | `retryQueuedReports :3407-3411` задържа само 0/503/403; **422 = „queue drop“** → етикет, който още не съществува, изхвърля доклада при следващ опит (обявена дупка, Р-Н4) |
| 5 | `setReporterName(null)` | **безопасно**: `:2648` `typeof name !== 'string' → return` — `reporter: null` не трие запазеното име |
| 6 | лентата „Докладва“ | `reporterBarHTML :2735-2736` връща `''` при празно име → `reporter: ''` в чернова я маха сама; `wireReporterBar :2743-2744` е предпазено (`if (!btn) return`), `:2761-2762` също |
| 7 | отпечатък на изданието | **`generation_manifest` не се чете от страницата** (0 попадения) → отпечатъкът е `ADDRESS_QUARTERS_SHA256.slice(0,12)` = `7642d3ec334b` (`:4867`) |
| 8 | средата | `mode :2010`, `basemapMode :4703`, `satelliteOn :4234`, `HYDRANTS :1913`, `activeTargetId :2008`, `APP_VERSION :2598`. Всички са в същия IIFE **след** формата → четат се само във функция (потребителско време), никога на модулно ниво (TDZ) |
| 9 | търсачката | `#addrSearchInput :1675`, `#addrSearchResults :1678`, ред `.asr-item` + заглавие `.asr-title` (`buildExactItem :5722-5736`); `renderResults :5946`, `selectResult :6383` са **ВЪТРЕ** в `initAddressSearch` → D12 (`docs/decisions/006_places_in_search.md:157-171`) ги забранява. Прецедент за слушател отвън: `index.html:8009` |
| 10 | Worker | POST `:164-171` подава `labels` дословно, **нула проверка на `report_type`**; GET `:325-330` твърдо `labels=report` → обратната връзка не влиза в тръбата на картата. **Worker-ът не се пипа.** |
| 11 | ингестът | `hydrant_core` вече пропуска непознат вид като `not_a_hydrant_report` (лот 6, К3 `6ced4e6`) — нищо за правене |
| 12 | headless стойка | `tests/granitsi_client_probe.mjs` вдига **срез** с `new Function` и десет стъба; **няма `navigator`, `getElementById` връща `null`**, чужд стъб не може да се инжектира без да се пипне пробата (§0.4) → събирачът на контекст трябва да е **тотален** (`typeof`-предпазители) |
| 13 | стойката на формите | `tests/test_report_form_operational.py` (`index_text`, `span`/`block`, `ask_client`, `replace_once`) + `tests/test_report_form_note_optional.py` (базов литерал, доктрина „мъртъв пин = падане“) — нашият модул ВНАСЯ първия като модул и пише СВОЙ `ask()` (експортите на лот 1 са фиксирани) |
| 14 | Г11 е безопасен | `worker/index.js:9-15` `ALLOWED_ORIGINS` = `github.io` + `localhost/127.0.0.1` на **8000 и 8080**; **8001 липсва** → preflight 403 → `status 0` → офлайн клонът. Нула създадени issue-та |
| 15 | ИБ1-Г15 | `gates/probe/ib1_g15.py:36-37` — двата израза; фикстурите на лота ги избягват по §0.6 |
| 16 | ALLOWED_NEW | `gates/probe/b3_g11.py:40-128`; прецедент лот 6 `:117-123` — блок с коментар, **в същия комит, който ражда файловете** |
| 17 | НЕ е мерено (Ф0 го мери) | (а) `N₀` = `python -m unittest discover -s tests` → **очаквани 443**; (б) `python -m gates.run_gates` седем отметки, `python -m gates.release` 0 делти, `python gates/probe/b3_g11.py 758f35e` → 0/`[]`/`[]`; (в) точните байтове на `index.html`; (г) `node --version` (без node гейтът е червен, не пропуснат) |

---

## §2 · Комитите

### К1 · Планът (**ръка: Architect**) — `docs/plans/ПЛАН_Проблемите_лотО_обратна_връзка_15.09.md`
```
docs(plan): the app-feedback channel of lot O, measured and bounded

Lot O gives the "+" menu a sixth, visually distinct entry - a problem or
an idea about the app - behind the report modal it already owns: six
tiles for the kind, a read-only box that says what the app will send,
and one sentence, required only for an idea or "other". The plan pins
every anchor at 758f35e, the ten signed recommendations, the eight
decisions the measurement forced, and the gates, including the browser
one on port 8001, which the Worker's origin allowlist refuses by
construction, so nothing can reach the public repository before Petar
creates the labels.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

### К1б · Амандаментът (**ръка: Architect**) — `docs/audits/амандамент_подаване_15.09.md`
Амандира договора от 09.05 (`docs/audits/backfill_and_submission_extension_plan_20260509.md`) и амандамента от 13.09. Съдържание (кратко, по образеца от 13.09): шестият `report_type: app_feedback`; двата нови ключа в главата — `feedback_kind` (шест стойности) и `feedback_context` (**JSON низ на един ред**, защото Worker-ът чете главата ред по ред); `reporter: null` по замисъл (публично хранилище, име не помага на бъг); етикетите `app-feedback` + `fb-<kind>` + `pending-review` и **никога `report`**; трите непокътнати инварианта: Worker GET само `labels=report`, ингестът пропуска вида като `not_a_hydrant_report`, картата не се мени.
```
docs(contract): a sixth report type that never enters the map pipe

The submission contract of 2026-05-09 knows five report types, all of
them about a hydrant. Lot O adds a sixth that is about the APP: it
carries feedback_kind and feedback_context in the head, no reporter and
no coordinate, and it is labelled app-feedback / fb-<kind> /
pending-review, never report. Measured and written down here, because
three guards depend on it: the Worker fetches only labels=report, the
ingest already skips an unknown type as not_a_hydrant_report, and the
client refuses to write a note for a type outside its allowlist.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

### К2 · Каналът (**ръка: Executor**) — `index.html`, `tests/test_app_feedback_channel.py` (нов), `gates/probe/b3_g11.py`
**Редакциите в `index.html`** (вмъкване, освен изрично назованите изменени редове):
- **CSS** след `:1477` — `.type-btn.feedback` (пълна ширина, пунктир, сиво-синьо), `.fb-tiles`, `.fb-ctx`; нула байта по съществуващите правила.
- **Речник и строители** пред `// ----- Type picker -----` (`:2766`): `FEEDBACK_KINDS` (шест реда, всеки **завършващ с `},`**, за да реже котвачът), `feedbackKind(id)`, `feedbackEnvSnapshot()` (ЕДИНСТВЕНОТО място с `typeof`-предпазители: версия, онлайн, устройство = ОС + семейство браузър, екран, режим, фон+сателит, отпечатък `7642d3ec334b`, последната грешка/търсене, зум/точки/хидрант, памет, SW), `buildFeedbackContext(kind, env)` (**чиста**: плоският речник по Р2 + маската `[координати]`), `maskFeedbackQuery(q)`, `renderFeedbackContext(kind, env)`.
- **Пикер:** `const feedbackOption = hydrant ? '' : '<button … data-type="app_feedback">💬 Проблем или идея за приложението</button>';` до `newOption` (`:2774`) + вмъкване след бутона `damaged`; **свой клон** `else if (t === 'app_feedback') { showReportModal(null, 'app_feedback'); }` пред капана за предизбор (`:2812`).
- **Заглавие** (`:2830`): `case 'app_feedback'`. **Форма:** нов клон в `typeFieldsHTML` пред `return ''` — плочките (`data-radio="feedback_kind"`), кутията `#fbCtx`, `textarea data-field="description" maxlength="500"` + подсказка „без имена и лични данни“, звездичка само при `idea`/`other`.
- **Изменени редове (десет по `numstat`, v1.1: шестте по-долу + `reporter: d.reporter` + подписът на `labelsForType` и двата ѝ викащи):** `:3028` `nameFieldHTML` става условен и `reporter` за този вид е `''`; `:3103` името се валидира само за другите видове; `:3177` описанието включва `app_feedback`; `:3198` `dedupHash` + вид + текст + заявката (`JSON.parse` в `try/catch`); `:3227` редът на YAML получава `feedback_kind`,`feedback_context`; `:3248` „Подател“ се печата само при наличен подател; `labelsForType(report)` (дефиницията `:3275` + двата викащи `:3308`, `:3401`).
- **Валидация** в `onSubmitClicked`: без избран вид → червено поле; текст задължителен само при `idea`/`other`. **`buildReportObject`:** `reporter: null`, `feedback_kind`, `feedback_context: JSON.stringify(buildFeedbackContext(...))` за този вид, `null` за всички други.
- **422 (Р-Н4):** за `app_feedback` клонът НЕ вика `postIssue(..., true)` — `queueReport(report)` + съобщение „Етикетите още не са създадени — докладът е запазен локално, кажи на Петър“.
- **`applyReports` (:3970-3971):** веднага след `if (!type) continue;` — `if (!APPLY_TYPES.has(type)) continue;` (константа до `AT_HYDRANT_TYPES`), ПРЕДИ `extractReportNote`.
**Тестът** `tests/test_app_feedback_channel.py` — деветте пина и четирите половини по §3 *(v1.3: текущата спецификация на модула е §7–§9 — 36+ метода, осем половини)*. **`b3_g11.py`:** блок в `ALLOWED_NEW` с три пътя (планът, амандаментът, тестът).
```
feat(report): a sixth entry - a problem or an idea about the app

The "+" menu, and only it, gains a sixth button: the crew at a hydrant
never sees it (measured: the hydrant picker keeps its four types). It
opens the same modal in two steps - six tiles for the kind, then a
read-only box that says exactly what the app will send, rebuilt on every
tile - and asks for one sentence only when the kind is an idea or
"other". No name is asked and none is sent: on a public repository a
name adds nothing to an app bug, so reporter travels as null and the
2..50 check is skipped for this type alone.

The issue goes to the same repository through the same Worker with
labels app-feedback / fb-<kind> / pending-review and never "report":
measured, the Worker fetches only labels=report (worker/index.js:327)
and the ingest already skips the type as not_a_hydrant_report, so the
map pipe cannot see it. The head carries feedback_kind and
feedback_context, a flat JSON STRING on one line, because the Worker
parses the head line by line. A coordinate-looking search query is
masked, dedupHash separates two different texts, the 422 label fallback
keeps the labels and queues instead of posting an unlabelled issue, and
applyReports refuses a type outside its allowlist before it can write a
free text into a live hydrant's note.

tests/test_app_feedback_channel.py raises the slice headless through the
lot 1 harness and pins all of it; its four negative halves doctor copies
of index.html outside the tree and were measured to fall.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

### К3 · Двата записвача (**ръка: Executor**) — `index.html`, `tests/test_app_feedback_channel.py`
Отделен комит, защото това са ЕДИНСТВЕНИТЕ винаги-включени слушатели на лота (цена и при затворена форма) и се връщат самостоятелно. (а) `window.addEventListener('error'|'unhandledrejection')` → `window.__fvLastError` = **един ред** (съобщение + име на файла + ред, без стек), само последният; (б) наблюдател на `#addrSearchResults` (`MutationObserver`, ИЗВЪН IIFE-то — D12) → `window.__fvLastSearch = {query, rows_shown, first_row}` при всяко изчертаване, плюс клик-слушател на контейнера за `selected_row`. **Обявена дупка:** избор с Enter не се записва.
```
feat(report): record the last error and the last search once

Two always-on recorders, in their own commit because they are the only
part of lot O that costs anything when nobody opens the form, and the
only part worth reverting alone. The error recorder keeps ONE line -
message plus file basename and line number, never a stack - so the
freeze and report tiles can say what actually broke. The search recorder
watches the dropdown from OUTSIDE initAddressSearch, because ADR 006 D12
makes every function inside it untouchable and the list is already gone
by the time the modal opens; a tap on a row adds the chosen label, and
an Enter selection is a declared gap, not a silent one.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

---

## §3 · Гейтовете

**Ф0 (преди код):** §1 ред 17 се възпроизвежда; различно число без обяснение → STOP-2.

**Пинове на теста (Г-О1…Г-О9, `python -m unittest tests.test_app_feedback_channel`):**
1. шест плочки с шестте `id` и шестте български етикета; звездичка само при `idea`/`other`; `maxlength="500"`; подсказката за лични данни — **headless** `typeFieldsHTML('app_feedback', …)`;
2. петте стари клона на `typeFieldsHTML` **байт-равни** на блоба `758f35e:index.html` (мъртъв пин = падане);
3. `data-type="app_feedback"` стои **точно веднъж** и само на реда `const feedbackOption = hydrant ? '' :`; четирите бутона при хидрант непроменени;
4. `labelsForType({report_type:'app_feedback', feedback_kind:<шестте>})` → `['app-feedback','fb-<kind>','pending-review']`, нула `'report'`; петте стари вида байт-равни на базата;
5. `buildIssueBody(<синтетичен доклад>)` → главата носи `feedback_kind` и `feedback_context`, който се парсва (двойно) в речник; **нула ключ `lat`/`lon`/`coords`/`latitude`/`longitude`**; `reporter: null`; `hydrant_ref`/`expected_coord`/`reported_coord`/`type`/`operational_status` = `null`; текстът на подателя стои **точно веднъж** в markdown-а;
6. `buildFeedbackContext('address', …)` маскира `11.111111, 22.222222` като `[координати]`, а обикновен адрес минава непокътнат;
7. `dedupHash` различен за два различни текста (и за две различни заявки), еднакъв за същия;
8. изходен пин: `APPLY_TYPES.has(type)` стои между `if (!type) continue;` и `extractReportNote` (базата го няма);
9. изходен пин: клонът на 422 за `app_feedback` не вика `postIssue(url, payload, true)` (базата носи безусловния повторен опит).

**Отрицателни половини (четири, всяка ТИЧА и ПАДА със своята игла, върху копия в `$TEMP/fv_lotO`):** (а) върнат етикет `report` → пада Г-О4; (б) махната маската → пада Г-О6; (в) махнат `APPLY_TYPES` → пада Г-О8; (г) изтрит `feedback_context` от `order` в `buildReportYAML` (тихата клопка на F-9) → пада Г-О5. **Минимум три трябва да са измерени червени във Ф0.**

**Гейтове на лота (след К3):**
- **Г1 · Пакетът:** `python -m unittest discover -s tests` → **443 + методите, които този план назовава** (К2 ≈ +13, К3 ≈ +2). Друга разлика без обяснение → STOP-6. Нито един тест на базата не се маха и не се пренастройва.
- **Г2** `python -m gates.run_gates` → 0, седем отметки. **Г3** `python -m gates.release` → 0 делти. **Г4** `PYTHONIOENCODING=utf-8 python gates/probe/b3_g11.py 758f35e` → 0, `geo_hits: []`, `stray_new: []`.
- **Г5 · Приватност:** `python gates/probe/ib1_g15.py --added-lines 758f35e -- index.html gates/probe/b3_g11.py tests/test_app_feedback_channel.py` + целофайлово за новите три файла → `hits=0`.
- **Г6 · Периметър:** `git diff --name-only 758f35e..HEAD | sort` → **точно пет пътя** (план, амандамент, `index.html`, тестът, `b3_g11.py`) *(v1.1: шест — плюс `scripts/build_reports_dashboard.py`, Р-К5 (з))*; `git diff --numstat 758f35e..HEAD -- index.html` → вмъкванията доминират, **изтритите редове са точно колкото изменените двойки, които §2 назовава, и всяка се показва по котва**; `git diff --quiet 758f35e..HEAD -- data worker sw.js scripts/lib scripts/hooks expectations.json tests/negative_halves_manifest.json tests/test_negative_halves.py tests/granitsi_client_probe.mjs gates/release.py gates/run_gates.py gates/probe/lot_perimeter.py gates/probe/ib1_g15.py` → **0**.
- **Г7** `python -m unittest tests.test_ui_ascii_mojibake` → 0 + сканът от AGENTS.md върху `index.html` и новия тест → 0 (и нула `\u04` екранировки в новите UI низове). **Г8** `python -m unittest tests.test_first_load_budget` → Ran 3, OK. **Г9 · Ръцете и редът:** К1/К1б = Architect, К2/К3 = Executor; К1 е предшественик на К2.
- **Г11 · Браузърът (ръка: Архитектът, преди Одитора):** `python -m http.server 8001`, ширина 375 px: (1) „+“ показва **шест** бутона, шестият е визуално различен; дълго натискане върху пин — **четири**, без шестия; (2) шестте плочки; всяка плочка **пречертава** кутията „Какво ще изпратим автоматично“ със своите ключове; (3) **няма преглед** на issue-то и нула реда в конзолата; (4) „Изпрати“ без избран вид → червено; при `idea` без текст → червено; (5) с избран вид и текст „Изпрати“ → **POST тръгва и умира на 403 от origin-а** (§1 ред 14) → диалогът „Няма интернет. Запази локално?“; потвърждаване записва в `hydrants_pending_reports` обект с `feedback_kind` и `feedback_context` (прочита се в конзолата), **след което записът се трие ръчно**; (6) нула грешки в конзолата при зареждане.

---

## §4 · Решения

**Подписани от Петър (15.09, „действаме според препоръките“):** Р1 без име и `reporter: null` · Р2 контекст по плочка, плосък JSON низ · Р3 записвач на грешки, един ред · Р4 „работеше ли преди?“ по желание · Р5 `dedupHash` + вид + текст (+ заявка), дроселът 30 s остава · Р6 текст `maxlength 500` + подсказка · Р7 без снимка и без изброим „последно действие“ · Р8 **предусловие, не гейт: Петър създава седемте етикета в GitHub ПРЕДИ доставката да иде живо** · Р9 циклът `/feedback` е отделен лот · Р10 периметърът се съди по `numstat` + котвени двойки.

**НОВИ решения, наложени от мярката — подписани от Петър на 15.09 заедно с плана („подписвам…“):**
- **Н1 · Класът е 🔴 и лотът носи амандамент К1б** (нов `report_type` + два нови ключа = схема на подаването). Алтернатива: 🟠 без амандамент — не се препоръчва.
- **Н2 · Лентата „Докладва“ също изчезва** при този вид (черновата носи `reporter: ''`, `reporterBarHTML` връща `''` сама — §1 ред 6). Алтернатива: лентата остава видима, но нищо не се изпраща.
- **Н3 · `labelsForType(report)` вместо `labelsForType(type)`** (два викащи реда), за да носи `fb-<kind>` и **офлайн опашката** — не кръпката на прототипа в `submitReport`.
- **Н4 · 422 с етикетна причина:** за `app_feedback` етикетите НЕ се свалят; докладът влиза в опашката + съобщение. **Обявена дупка:** `retryQueuedReports` изхвърля при 422 (§1 ред 4) → истинската защита е Р8.
- **Н5 · Куката за търсенето = наблюдател + клик-слушател ИЗВЪН IIFE-то** (D12, §1 ред 9), не два реда в `renderResults`/`selectResult` и без амандамент 006d. Обявена дупка: избор с Enter не се записва.
- **Н6 · Отпечатък на изданието = `ADDRESS_QUARTERS_SHA256[0:12]`**; `generation_manifest` не се чете от страницата (§1 ред 7).
- **Н7 · Събирачът е разделен на две:** `feedbackEnvSnapshot()` (единственото място с `typeof`-предпазители) + **чистия** `buildFeedbackContext(kind, env)` — иначе гейтът не е тестваем headless (§1 ред 12).
- **Н8 · Два комита на Изпълнителя** (каналът, после записвачите).

---

## §5 · STOP-условия

1. Дървото не е чисто или `HEAD ≠ main ≠ 758f35e` → STOP.
2. Ф0 не възпроизвежда §1 (друг `N₀`, друга котва, друг ред) → STOP: мери се наново, не се кодира.
3. `node` липсва или срезът не се вдига (котва, която не стои точно веднъж; блок, който котвачът не затваря) → STOP, не се планта маркер в `index.html`.
4. Половина, чиято червенина не е измерена във Ф0 → не се доставя.
5. Нужда да се пипне нещо от §0.4, `data/`, `worker/`, `sw.js` или функция на `initAddressSearch` → STOP и питай.
6. Пакетът не дава `443 + назованите методи` → STOP.
7. Г11 създаде истинско issue (значи origin-ът е приет) → STOP, доклад до Петър преди всичко друго.
8. Кутията с контекста покаже стойност, която прилича на координата, име или адрес на подателя → STOP (маската е дефектна).
9. Изпълнителят стигне до нужда от промяна в `worker/` или в ингеста → STOP: това е друг лот.

---

## §6 · Какво НЕ е в лота (нарочно)

- **Worker-ът** — нула байта: POST подава етикетите дословно, GET тегли само `labels=report`; това е целият предпазител и е безплатен.
- **Частно хранилище за обратната връзка** — Петър реши публичното (15.09); затова и името отпада (Р1).
- **Циклът `/feedback`** (умение като `/firehydrants`: чете `app-feedback` issue-та, преиграва адресните заявки през `tests/address_slice_probe.mjs`, подрежда идеите на борд) — **отделен лот СЛЕД първите истински доклади** (Р9).
- **Снимка на екрана** и **изброимо „последно действие“** (Р7).
- **Контекст от търсенето на места** (`places`) — само адресният ред се записва.
- **Изборът с Enter** в търсачката, **вложени/съкратени** форми на контекста, **`retryQueuedReports` при 422** — обявени дупки, всяка със свой ред в беклога, не тихо отслабване.
- **Създаването на етикетите в GitHub** — ръка на Петър, предусловие (Р8), не гейт в кода.
- **F-14** се затваря с подписа на този план; **F-8…F-13** се затварят от К2/К3 и се отбелязват в беклога чак след Gate 2.
- Историята не се пренаписва; непроследен файл не се трие от агент; пушът е само на Петър.

---

## §7 · Ревизия v1.1 — доставката, Gate 2 и К5 (Архитект, 15.09.2026)

**Доставено (Executor):** К2 `e5049ad` (каналът: index.html + `tests/test_app_feedback_channel.py` + `ALLOWED_NEW`), К3 `7e088e4` (двата записвача), К4 `061e284` (**Р-К4**, пропуск на §2, който прототипът имаше: заглавието на issue-то по вида — „[app_feedback] Адрес / търсене“; българското име в тялото; записвачът на последния вид доклад прескача самата форма). Ф0: 443 OK, седем отметки, release 0, node 22.19, index.html 595 936 B → **614 957 B** след К4 (`numstat` спрямо 758f35e 328 / 11; вмъкванията доминират; изтритите са десетте изменени реда на §2 + едната собствена на лота от К4). Пакетът **462 OK** (443 + 19: К2 14, К3 2, К4 3); четирите половини падат със своята игла („['report','fb-address',…] != …“, „'11.111111, 22.222222' != '[координати]'“, „APPLY_TYPES не пази applyReports“, „… 'feedback_context', 'damage_description'“); `run_gates`/`release`/`b3_g11 758f35e`/`ib1_g15` зелени; точно пет пътя; data/worker/sw.js/scripts байт-равни.

**Решения на Executor-а, приети (не бяха в §2):** (1) слушателят на плочките е отделна функция `wireFeedbackHandlers`, ДО `wireFormHandlers`/`readDraftFromForm` — двете са байт-пиновани от лот 1 (`test_report_form_operational.PerimeterTest.PINNED_BLOCKS`; измерено: един коментар в `wireFormHandlers` ги чупи) → условието на Петър надделя над буквата на §2; (2) `showReportModal(null, 'app_feedback', { reporter: '' })` вместо изменен ред в черновата — лентата „Докладва“ изчезва сама (Н2); (3) `window.__fvLastReportType = t;` в пикера (вмъкване; амандаментът иска `last_report_type`), от К4 с гард за самата форма; (4) `worked_before` се рисува винаги и се крие с клас `.field.hidden` (ново CSS правило, нула байта по старите); (5) маската е два израза (двойка, после самотно число с ≥ 3 десетични знака); (6) 14 метода в К2 (деветте пина + четирите половини + пин на трите реда без име).

**Г11 (Архитект, порт 8001, 375×812, при 061e284; `scratchpad/group2/g11_lotO_browser.md`):** „+“ → шест бутона, при хидрант → четири („Докладвай“ от попъпа); без поле за име и без лентата; шест плочки; `worked_before` скрит при „Предложение“; кутията се пречертава по плочка (адрес: заявка „бл 5 аспарухово“, 7 реда, първи ред „кв. Аспарухово, бл. 5“, издание 7642d3ec334b); валидацията (без вид → червено; идея без текст → червено); „Изпрати“ → POST спрян от origin-а (CORS) → „Няма интернет. Запази локално и опитай пак?“ → един запис в `hydrants_pending_reports` с `feedback_kind: idea`, `reporter: null`, без координати, плосък JSON контекст (изтрит след проверката); нула issue-та; конзолата само CORS. **Зелено по всичките девет точки.**

**Кими (данни, Gate 2):** инструментът му отказа част от четенето; едно твърдо и вярно попадение (потвърдено от Архитекта в кода): при заявка с координати търсачката рисува GPS ред с `.asr-title` = `coordTitle(r)` = координатата с 5 знака (index.html `:5586`, `:5611`), а контекстът пращаше `first_row`/`selected_row` немаскирани → маската на заявката не стигаше. → **К5 (а)**.

**Astra (механика, ultra, върху 758f35e..061e284):** старите пътища байт-равни (формите, пикерът с хидрант, валидацията, `dedupHash` — „wrong_location|fixture|AB|…“ еднакъв на базата и на HEAD, етикетите); **обявена промяна:** всяка стара YAML глава вече носи `feedback_kind: null` и `feedback_context: null` — Worker-ът (`indexOf(':')` + `JSON.parse` на стойността) и ингестът (получава нормализирани речници от Worker-а, няма собствен YAML парсър) ги търпят без промяна, никой тест не пинваше точната стара глава; парсърите не се спъват в JSON низа с `:` и `\"` (обиколка проверена); формата: редът на слушателите е верен, изборът на плочка/текст/„работеше преди“ стига до товара; allowlist-ът в `applyReports`: подправен feedback issue с валиден hydrant_id през реалната нормализация на Worker-а → нула записи на бележка. **Оборени ръбове → К5:** (б) записвачът пази последното УСПЕШНО търсене — заявка А с редове, после Б без съвпадение, после обратна връзка съобщава А; (в) записвачът на грешки пази многоредово съобщение, URL с query string и число, което прилича на координата; (г) GitHub може да отговори на липсващ етикет с общото „Validation Failed“ и етикета само в `errors[]` — този отговор не хващаше пътя на обратната връзка (модалът остава с обща грешка, докладът не се губи, но и не се опашва); (д) **най-големият риск за екипа на терен:** обратната връзка въоръжаваше общия 30-секунден дросел — истински доклад за хидрант веднага след нея получаваше „Изчакай 30 сек“. Обявени, не поправени: грешки при стартиране ПРЕДИ регистрацията на записвача (парсването на данните `:1926`, `L.map` `:1941`) не се записват; кросдомейн грешки дават само „Script error.“; `бл 5.123` се маскира (фалшиво положително без реален случай); изборът от лентата с входове (лот 5) и с Enter не се записва; при повторен 422 от опашката докладът пада само с console.warn (Р8 е защитата).

**Одитор (Workflow, 4 лещи + присъда, Opus, върху 758f35e..061e284): НЕГОДНО** — два блокера (една и съща забранена последица по два механизма) и шест дефекта, всички възпроизведени: **Б1** координата стига до публично issue през `first_row`/`selected_row` (GPS редът: `coordTitle` `:5586` → `.asr-title` `:5611`; маската покрива само заявката; гейтът Г-О5 гледа само ИМЕНА на ключове и е зелен върху течащия товар; STOP-8 на плана сработи буквално — кутията показва „първи ред: 4x.xxxxx, 2x.xxxxx“ под надписа „Без GPS“); **Б2** маската е по-слаба от собствения парсър на приложението (`parseCoordQuery` `:5546` приема `\d\.\d`): „43.21, 27.91“, „43,21 27,91“, DMS с градуси, `geo:43.21,27.91`, линк към карти минават през маската байт за байт, а гейтът я мери срещу една-единствена заявка с шест десетични знака. Дефекти: **Д1** гейт-дупка Г-О8 — изтрита дефиниция на `APPLY_TYPES` при оставен гард → тестът зелен (чете само блока на `applyReports`; на живо `ReferenceError` при първия poll); **Д2** гейт-дупка Г-О3 — изтрит ред `feedbackOption +` в конкатенацията → шестият бутон изчезва, 13 теста зелени (само Г11 го вижда); **Д3** четвърти консуматор на потока, немерен: `scripts/build_reports_dashboard.py:33-34` тегли `state=all` без филтър по етикет и брои всяко тяло с `report_type` → feedback issue надува бройките, графиките и списъка на подателите („?“); **Д4** обещанието при 422 не се пази през презареждане: `retryQueuedReports` (`:3920` при старт, `:3732` при `online`) праща същите етикети и при 422 изхвърля записа само с `console.warn` — надписът обещава обратното; **Д5** търсене без съвпадение никога не се записва (`showStatus('Няма съвпадения')` = един `.asr-status`, записвачът връща при 0 реда) → остаряла заявка; **Д6** К4 не е в плана (STOP-6 по буква). Потвърдено ГОДНО: тръбата на картата затворена (Worker GET `labels=report`, `KNOWN_REPORT_TYPES`, `APPLY_TYPES` в `applyReports`); нула регресии по петте стари вида; десетте изтрити реда са в конструкциите, които планът назовава; `b3_g11`/`ib1_g15` зелени. Документни поправки (направени във v1.1): амандамент `:14` (маската покрива само заявката), `:10` (`location_method: "hydrant_ref"` при `hydrant_ref: null`), `:27` (`worked_before` липсва при `idea`); план `:93` „шест“ → десет изменени реда; `:3283` → `:3275`; `:3102` → `:3103`; съобщението на К1 носи изречение извън блока на §2 (подписът), обявено тук.

**Р-К5 (Архитект; условието на Петър „да не счупим нещо работещо“ и двата блокера са причината):** ЕДИН комит К5 (Executor), `index.html` + `tests/test_app_feedback_channel.py` + `scripts/build_reports_dashboard.py` (§0.3 се изменя само за този файл и само за (з); Г6 става **шест** пътя):
(а) **координатите:** записвачът разпознава GPS реда още при изчертаването му (Executor-ът мери маркера на реда от `buildCoordItem`) и записва `query`, `first_row` и `selected_row` като `[координати]`, преди суровата стойност да е влязла в `__fvLastSearch`; отделно `first_row`/`selected_row` минават през маската; маската приема всички форми, които `parseCoordQuery` приема — двойка с ≥ 1 десетичен знак (точка или запетая), DMS с `°`/`'`/`"`/N/E, `geo:`, URL с числа → цялата заявка става `[координати]`; пинът мери шестте форми на одита + заглавието на GPS реда; Г-О5 получава и проверка по СТОЙНОСТ (никоя стойност в контекста не съдържа две числа с десетичен знак);
(б) изчертаване „Няма съвпадения“ (`.asr-status`, без `.asr-item`) → записвачът записва написаната заявка (маскирана по същото правило) с `rows_shown: 0`, `first_row: null`; скриването на списъка не пипа записа;
(в) записвачът на грешки: само първият ред; без query string в съобщението и файла; през маската; до 200 знака;
(г) 422: клонът на обратната връзка разпознава етикетна грешка и по `errors[].field === 'labels'`; `retryQueuedReports` при 422 за `app_feedback` ПАЗИ записа в опашката (не го изхвърля) и казва веднъж на колегата, че докладът чака етикетите; старите видове — непроменени;
(д) СОБСТВЕН дросел за обратната връзка (отделен времеви печат от `lastSubmitTs`): доклад за хидрант никога не чака заради обратна връзка и обратно;
(е) `location_method: null` за `app_feedback` (амандаментът: без местоположение); фикстурата на Г-О5 се поправя;
(ж) гейт-дупките: пин на дефиницията `const APPLY_TYPES = new Set([…петте…])` + половина (изтрита дефиниция → пада); пин на реда на употреба `feedbackOption +` в конкатенацията на пикера + половина (изтрит ред → пада);
(з) `scripts/build_reports_dashboard.py` `normalise()`: пази само петте хидрантни вида (`KNOWN_REPORT_TYPES` от `hydrant_core` или същия литерал), останалото се прескача и се брои в отделен ред „прескочени (не са доклади за хидрант): N“ на изхода, за да не изчезва тихо; ако модулът има тест — пин, ако няма — пин в нашия тест през import на модула.
Съобщение дословно: `scratchpad/group2/msg_lotO_k5.txt`. Гейтове след К5: модулът на лота, пакетът (462 + новите методи), `run_gates`, `release`, `b3_g11 758f35e`, `ib1_g15 --added-lines`, шест пътя, диф-quiet върху data/worker/sw.js и останалите скриптове, повторен Г11 от Архитекта (координатна заявка → кутията показва `[координати]` навсякъде; търсене без резултат → „показани реда: 0“; доклад веднага след обратна връзка → минава).

---

## §8 · Ревизия v1.2 — К5, Г11 наново и повторният одит (Архитект, 15.09.2026)

**К5 (Executor, `bd3faa0`):** три файла — `index.html` 105/22 (лотът спрямо 758f35e: 416/16, **620 504 B**), `scripts/build_reports_dashboard.py` 24/4, тестът +415 реда; методите 19 → **31** (пет в формата, три за записвачите, един за таблото, три половини), пакетът **474 OK** (462 + 12; изходът уловен без pipe); `run_gates` седем отметки, `release` 0 делти, `b3_g11 758f35e` 0/[]/[], `ib1_g15 --added-lines` 4 операнда / 0; точно **шест** пътя; data/worker/sw.js/scripts/lib/scripts/hooks и §0.4 байт-равни; 82 теста на съседните модули OK; всичките осем `<script>` блока минават `node --check`. Седемте половини падат със своята игла (четирите стари + „дефиницията на APPLY_TYPES стои 0 пъти“, „редът `feedbackOption +` стои 0 пъти“, „общият печат се чете или въоръжава безусловно“).

**Какво направи К5 по Р-К5:** (а) маркерът на GPS реда е `.asr-kind.gps` (единствено място, `buildCoordItem` само се чете и е пинован байт-равен) — записвачът записва `query`/`first_row`/`selected_row` като `[координати]` още при изчертаването (и маскира заявката при ВСЯКО търсене — идемпотентно с втората маска в строителя); маската покрива двойка с ≥ 1 десетичен знак (точка/запетая), DMS с °'"/N/E, `geo:`, URL с числа → цялата заявка; Г-О5 проверява и по стойност; обявен остатък: четири голи числа (DMM без символи) не са „двойка“ за регекса — десетичните им части се маскират поотделно, а при изчертаване GPS редът ги прави `[координати]` изцяло; (б) всеки `.asr-status` с текст във видим списък → запис `rows_shown: 0` (и офлайн статусът — честно, нула редове); `hideResults` не записва; двете пиновани байт-равни; (в) `feedbackErrorLine(message, filename, lineno)` — чиста функция: първи ред, без query string, през маската, до 200 знака, и за `unhandledrejection`; (г) етикетна грешка = `/label/i` върху `field`/`resource`/`code` на `errors[]` (надмножество); опашката пази `app_feedback` при ВСЕКИ 422 и казва веднъж на пас („Докладът за приложението чака етикетите в GitHub — пази се локално“); старите видове пиновани байт-равни на 758f35e; (д) собствен печат за обратната връзка + `remainingCooldownMs()` чете вида на отворената чернова (иначе бутонът на обратната връзка щеше да е сив след доклад — същото чакане през друга врата); (е) `location_method: null`; (ж) двата пина + двете половини; (з) `normalise()` връща `(rows, skipped)`, пази само `KNOWN_REPORT_TYPES`, редът „прескочени (не са доклади за хидрант): N“ (не „app_feedback“ — филтърът е „извън петте“, планът би излъгал за друг непознат вид).

**Г11 наново (Архитект, порт 8001, 375×812, при bd3faa0; `scratchpad/group2/g11_lotO_browser.md` т. 10–13):** координатна заявка в живата търсачка → GPS ред (заглавие с 5 знака), а записът е `query`/`first_row` = `[координати]`, след тап `selected_row` = `[координати]` — суровата стойност не влиза в `__fvLastSearch`; „zzqqxx“ → „Няма съвпадения“ → запис `{query: zzqqxx, rows_shown: 0, first_row: null, selected_row: null}` и кутията го показва; обратна връзка → спрян POST → опашка (kind idea, `location_method: null`, изтрит след проверката) → ВЕДНАГА пин → „Докладвай“ → „Хидрантът е там“: „Изпрати ▶“ активен, без бележка за изчакване; конзолата само CORS. **Зелено.**

**Повторен одит на К5 (Opus, read-only): НЕГОДНО с два блокера, всичко друго от първия одит потвърдено ЗАТВОРЕНО** (B1 през DOM стъб на GPS реда → `[координати]` навсякъде; петте форми на маската; D1/D2 гейт-дупките падат + собствена трета половина на одитора „GPS редът“; таблото пази петте вида; 422 по `errors[]` → опашка, повторен 422 → пази и казва веднъж; празното търсене → 0 реда; дроселите не се пресичат; `location_method` null; `feedbackErrorLine`; 89 теста на съседните модули OK; шест пътя; старите функции адитивни — `dedupHash` байт-равен за стар доклад). **Б-1:** `parseCoordQuery` приема и 4, и 6 голи числа (DMM/DMS без символи: `11 12.846 22 54.882`, `11 12 50.8 22 54 52.9`), маската — само двойки; ключалката на GPS реда пада при една дума (`Варна 11 12 50.8 22 54 52.9` → парсърът връща null → няма GPS ред → „Няма съвпадения“ → новият клон (б) записва заявката само през слабата маска → ~30 m точност в публично issue); тоест клонът (б) отвори тази врата. **Б-2:** гейт Г5 в целофайловата си форма е ЧЕРВЕН върху самия план — v1.1 цитира присъдата на одита с истинска варненска двойка с 5 знака (§7) → `ib1_g15 <план> <амандамент> <тест>` → `RED hits=2`; нарушава §0.6 на плана (ръка на Архитекта, не на К5). Дефекти: (1) опашката пази `app_feedback` при ВСЕКИ 422, не само етикетен → 422 за тяло/заглавие паркира записа завинаги с грешно съобщение; (2) `feedbackErrorLine` реже съобщението на първата `?` („Защо? няма връзка“ → „Защо“); (3) файлът не минава през маската и `#lat=…` оцелява; (4) записът (б) се пропуска, когато търсенето на места вече е свалило адресния списък (`tidyTheirs`); (5) ключалката гледа само `rows[0]` (латентно); (6) `field()` в таблото (предисторическо) при празна стойност взема следващия ред — вече пада в брояча „прескочени“. Документни: Г6 операндът `scripts` → `scripts/lib scripts/hooks` (иначе `diff --quiet` е 1 по конструкция след (з)); (з) в §7 казва друг надпис от кода; амандаментът „the context builder masks the same shapes once more“ е невярно за 4/6 числа, „four negative halves“ → седем; коментарът в `b3_g11.py` „четирите половини“ → седем; коментарът в `index.html` `:2805-2806` описва намерение, което кодът не изпълнява.

**Р-К6 (Архитект; Б-1 и дефекти 1–3, 5):** ЕДИН комит К6 (Executor), `index.html` + тестът + `gates/probe/b3_g11.py` (само коментарът): (а) маската: заявка с **≥ 4 числови токена, от които поне един с десетична част, или с ≥ 6 числови токена** → цялата става `[координати]` (покрива DMM/DMS без символи, с или без думи наоколо); адрес с до три числа (`ул. Струга 12 вх. 2 ет. 3`) и `бл 5 вх 2 ап 12 ет 4` (четири цели) остават; пинове: `11 12 50.8 22 54 52.9`, `11 12.846 22 54.882`, `11 12 51 22 54 53`, `Варна 11 12 50.8 22 54 52.9` → `[координати]`; двата адреса непокътнати; коментарът `:2805-2806` казва истината (маската покрива формите на парсъра И четири/шест голи числа; остатък няма); (б) ключалката на GPS реда гледа ВСЕКИ ред (`rows` с `.asr-kind.gps`), не само първия; (в) `retryQueuedReports` пази `app_feedback` само при ЕТИКЕТЕН 422 (същата проверка като `handleSubmitResult`, изнесена в една функция `isLabelError(json)`), друг 422 → както досега за всички видове; (г) `feedbackErrorLine`: query string и `#` се режат само от URL-подобни токени (`https?://\S+`) в съобщението и от файла, не от целия текст; файлът минава през маската; пинове: „Защо? няма връзка“ остава цяло, `app.js#lat=11.11111` → без остатък; (д) `b3_g11.py:125` коментар „седемте половини“ (след К6 — осем, с половината за маската на четири числа, която К6 добавя); (е) нова половина: махнато правилото за ≥ 4 числа → пада. Обявени, не поправени: (4) `tidyTheirs` сваля списъка преди записа (свойство на реда на двете търсачки; ред за лот 5г/О2); (6) `field()` на таблото — предисторическо. Съобщение дословно: `scratchpad/group2/msg_lotO_k6.txt`. Гейтове след К6: модулът, пакетът 474 + новите методи, `run_gates`, `release`, `b3_g11`, **`ib1_g15` и в двете форми — `--added-lines 758f35e` върху четирите кодови пътя И целофайлово върху плана, амандамента и теста → 0** (Б-2 е поправен в този документен комит: двойките в §7 са заменени с формата им), шест пътя, `diff --quiet` върху data/worker/sw.js/scripts/lib/scripts/hooks, Г11 наново (`Варна 11 12 50.8 22 54 52.9` → кутията показва `[координати]`).

---

## §9 · Ревизия v1.3 — К6, Г11 наново и финалният одит (Архитект, 15.09.2026)

**К6 (Executor, `bf62bc3`; компютърът на Петър се срина по средата на работата — некомитнатите редакции оцеляха на диска, същият Executor беше възобновен, провери ги за цялост и завърши):** `index.html` 56/22 (лотът спрямо 758f35e: 450/16, **622 756 B**), тестът +173/−6, `b3_g11.py` коментарът 3/2; методите 31 → **36** (четири нови пина + осмата половина), пакетът **479 OK** (изходът уловен без pipe); `run_gates` седем отметки, `release` 0 делти, `b3_g11 758f35e` 0/[]/[], `ib1_g15` и в двете форми 0; шест пътя; data/worker/sw.js/scripts/lib/scripts/hooks байт-равни; 89 теста на съседните модули OK; всичките осем `<script>` блока `node --check`. Осемте половини падат със своята игла. Отклонения (приети): (1) `Array.from(rows).some(isCoordRow)` вместо `Array.prototype.some.call` — гейтът на лот 5 забранява токена `prototype` в добавени редове (измерено червено, 478/1, преди поправката); (2) три пина на К5 в собствения модул на лота са пренастроени, защото К6 сменя измереното от тях поведение (ключалката по всеки ред; съобщение с `?` вече не се реже, фикстурата носи токена в URL; проверката на `errors[]` живее в `isLabelError`) — нито един тест отпреди лота не е пипан; (3) един стар коментар над `feedbackErrorLine` поправен, за да казва истината.

**Г11 наново (Архитект, порт 8001, 375×812, при bf62bc3; `scratchpad/group2/g11_lotO_browser.md` т. 14–17):** пробата на Петър „бл. 402 вх. 3“ → 8 реда, първи ред „кв. Владиславово, бл. 402 · вх. 3“, записът непроменен; „бл 408“ → 1 ред + „23 входа“ + 23 бутона (лот 5 непокътнат); дума + голи DMS числа в живата търсачка → 7 адресни реда без GPS ред, а записвачът пази заявката като `[координати]` и кутията показва `заявка: [координати]` — Б-1 от повторния одит е затворена и на живо; конзолата само CORS. **Зелено.**

**Финален одит на К6 (Opus, read-only): ГОДНО.** Б-1 затворена: 9 440 форми на координатен вход (клоновете на парсъра × разделители × обвивки × 3–7 десетични знака) → нито една не оставя число с ≥ 3 десетични знака; всичките шест форми на одита + реалистичният пин → `[координати]`; ключалката по всеки ред; `isLabelError` вярна на 10 форми; `feedbackErrorLine` по спецификация; петте стари вида байт-равни (заглавие, етикети, `dedupHash`; единствената разлика в тялото са двата договорни ключа с null); собствени половини на одитора падат (правилото за голи числа; `errors[]` клонът; и „мъртъв клон с оставен текст“ → пак пада, защото гейтът е поведенчески). Дефекти (не блокират, с мярка): (1) двойка с ≤ 2 десетични знака и необичаен разделител (`/ | \ _ ~ :`) минава през маската (парсърът я приема → GPS ред → ключалката я маскира на живия път; остатък ≤ 2 знака ≈ 1,1 km) — коментарът „nothing is left over“ е по-силен от кода; **(2) кавичката ASCII се чете като секунди → 16 от 150 имена на места (`ОУ 5 "…"`) стават `[координати]` в кутията — фалшиво положително, което крие точно реда, който колегата има предвид;** (3) линк към карта без цифра (OSM shortlink) минава дословно и през празния рендер влиза в контекста (~10–30 m информация, не в цифри); (4) откъснати десетични точки (`11. 21411`) — нито парсърът, нито маската ги приемат, няма GPS ред, приложението не издава такъв текст; (5) query string на URL без схема не се реже (теоретично). Документни: `tests/test_app_feedback_channel.py:81` „четирите половини“ → осем; §2 на плана още звучи като текуща спецификация („деветте пина и четирите половини“); `index.html:2807` „nothing is left over“.

**Р-К7 (Архитект; дефекти 2 и 3 + документните):** ЕДИН комит К7 (Executor), `index.html` + тестът: (а) формата „градуси“ на маската иска истински знак за градус/минута/секунда (`°`, `º`, `′`, `″`) или буква на полукълбо (N/E/S/W/С/И/Ю/З) непосредствено след кавичката — не всяка ASCII кавичка; пин: `ОУ 5 "Проф. Марин Дринов"` остава непокътнато, DMS с `°'"N` пак `[координати]`; (б) всеки `https?://` токен в заявката става `[линк]`, независимо дали носи цифри; пин: `https://osm.org/go/xcXbHf` → `[линк]`, маршрутът с числа → пак маскиран; (в) коментарът `index.html:2807` казва истината (остатък ≤ 2 десетични знака при необичаен разделител, покрит от ключалката на GPS реда); коментарът в теста `:81` → осем половини; §2 на плана получава бележка, че текущата спецификация е §7–§9. Съобщение дословно: `scratchpad/group2/msg_lotO_k7.txt`. Гейтове след К7: модулът, пакетът 479 + новите методи, `run_gates`, `release`, `b3_g11`, `ib1_g15` двете форми, шест пътя, диф-quiet; проверка от Архитекта на двата пина срещу базовия блоб на К6 (пробата на местата: 0 от 150 имена маскирани). Обявени, не поправени: дефекти 1, 4, 5 (ограничени по мярка).
