# Амандамент №7 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 11:40) — след одита на A.2-6 (НЕГОДНО: 1 major, 4 minor); всичко се приема; отговорите на изпълнителя

A.2-6 (b08d39d) е доставен: замразяване само при зелена присъда; „не“ печели независимо от реда; `sign` отказва широк „не“; проверка 7 чете авторството преди дайджестите; котвата на отказа е референцията на опашката; решаващият експеримент повторен в двете подредби (278 теста, 0 червени, 7/7 гейта зелени в изолиран клон). Остава един пробив и четири ръба → **A.2-7** (един комит; write-set: `gates/release.py`, `gates/sign.py`, `gates/run_gates.py`, `scratch/places_search/recall_sweep.py`, `tests/test_places_search_gate.py`) + **A.2-8** (docs: `docs/plans/PARKED.md`).

## Major
1. **Ред „не“ с точна заявка, която не улучва нито една делта, е ням** (правописна грешка → release зелен → замразяване с отказаната делта вътре). → Симетрично на „да“: „не“, което не покрива нито една жива делта И нито една разлика между референцията на опашката и замразената референция → BLOCKED („ред RN отказва заявка, която не съществува“); `gates.sign` отказва да запише „не“, ако заявката липсва в текущия набор делти на `gates.release` (грешката се хваща при подписа). Счупен вход: `gate_lot1/Градината` вместо `gate_lot1/градина` → sign отказ, release BLOCKED.

## Minor
2. Проверка 7: дайджестът се взима от blob-а на опашката в комита на ПЕТЪР, който въвежда точно този дайджест (най-старият комит с `-S<дайджест>`, автор Petar1984), не от работното тяло; иначе редът гласи „приет по опашка, комитната от Petar1984“, никога „Петър записа“.
3. Котвата на отказа: изрично поле `_meta.queue_reference` (комит), записано от `build_expectations`/`gates.sign`, вместо извеждане с `git log -S`; `queue_reference` отхвърля котва, чийто комит не е на Petar1984 → BLOCK, не мълчание. Счупен вход: агентски комит добавя второ срещане на `"signed_by": "Петър"` → BLOCKED.
4. Автоматични тестове за т. 1, 3, 4 на амандамент №6: (а) `--freeze` при незелена присъда (monkeypatch на присъдата) → изход 1 и нула записани байта; (б) проверка 7 върху временно репо с агентски комит по опашката → редът с дайджеста е ✗ без „Петър записа“; (в) `queue_reference` при пренасочен `_meta.base` → комитът на подписа, не f06ac06.
5. **A.2-8 (docs):** ред в `PARKED.md` за `test_etr_kmz_adapter.DeterminismTest` (мига 1/5 прогона; вероятна причина секундна граница в zip timestamp; Фаза E). Съобщение: `docs: PARKED — flaky DeterminismTest recorded for Phase E (A.2-8)`.

## Отговори на изпълнителя
- Формулировката на `--freeze` = дословната присъда на release-гейта — ДА (една истина, не две).
- Изрична котва `_meta.queue_reference` — ДА (т. 3).
- „не“ само по точна заявка; 150-те „да“ по клас с `кофа/*` — ДА, цената е приета (отказ на цял клас = толкова редове, колкото заявки).
- Замразените тела ги комитва Петър — ДА, записва се в опашка A като стъпка.

## Ред
A.2-7 + A.2-8 → одит (счупените входове от т. 1–3; повторен решаващ експеримент) → A.3 опашката → Петър.
