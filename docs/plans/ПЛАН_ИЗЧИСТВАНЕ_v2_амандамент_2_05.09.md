# Амандамент №2 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 04:05) — след честния STOP на F12 (5 комита готови: adad6b0, e5ddd66, f38c77f, d98fe40, 988e73f)

Нов датиран файл по §0.2. Решения на архитекта в обхвата на подписания v2; нищо ново по същество.

## Какво завари F12 (измерено от изпълнителя, потвърдено от гейтовете)
- Доставката е пренесена: хотели с квартал 96 → 152, места 44 → 49 (общо 140 → 201 от 375); речникът legacy_by_row 209 → 18; трите blob-а приети от огледалото на клиентския валидатор (0 отказа; без трите кода — 9 отказа, точно както картата предсказа); SHA пиновете и cache v6 на място; `run_gates`: 1, 2, 3, 5 ЗЕЛЕНИ (АДМИРАЛ затворен), 4 ЖЪЛТ само по подпис (allow 150 реда покрива всичко; `signed_by: pending — Петър`).
- Allow-файлът: 150 реда по клас — hull_artifact 80 (Морска градина 28, ЗПЗ 13, Шашкъна 11, ж.к. Владислав Варненчик 10, Виница/север 3…), no_witness 56, resort_pending 8, m6_changed 6.
- М7: 45 думи могат да задействат клона, 41 го правят днес; 0 стари заявки в шестте кофи са пипнати; двата манифеста детерминистични, с ВСИЧКИ редове (4 237 и 1 079).
- Тестове: 30 червени = 21 „чакат подпис“ (19 стари + 2 нови по замисъл) + **9 от друг клас**: `tests/test_hotels_public_bundle.py` и `tests/test_places_public_bundle.py` носят числата на ПРЕДИШНАТА доставка (байтове 142543/121621/64831, LEGACY_ROWS 209, речник 19/4, собствени копия на затворените списъци, quarter-by-src). Собственият им докстринг: „числата на манифеста, който Петър подписва — дрифт = редоставка“. F12 Е редоставката.

## Решения
1. **F12-д — двата bundle-теста следват доставката.** Константите се сменят с измерените (байтове 148685/122089/75818; LEGACY_ROWS 18; речник 20/5; списъците = доставените кодове; quarter-by-src хотели {REG 115, KAIS 25, SIGNED_OVERRIDE 12}, места {REG 39, KAIS 10}); всяка константа с преди/след в отчета; тестът трябва да ПАДА на старите blob-ове (`git show HEAD~5:data/hotels.json`) и да минава на новите. Съобщение: `test(bundles): pinned delivery numbers follow F12 — bytes, dictionary counts, closed lists, quarter-by-src (F12-д)`. След това червените са само класът „чакат подпис“ (очаквано 21; друг клас = STOP).
2. **F12-е — М7 взима филтъра за значимост.** Дума задейства голото място само ако е значима по същото правило като останалата машина (> 2 знака в двете изписвания; никакви типови префикси `кв`, `жк`, `к`, `м`, `с`, `о`, `т`, `и`; никакви числа). `m7_trigger_tokens.json` се регенерира (очаквано ~33, числото се мери); манифестът P7→F12 се регенерира report-only. Съобщение: `search: М7 fires only on significant tokens — no type prefixes, no numerals (F12-е)`.
3. **Двата контрола на Сол от гейт 6** („приморски“ M3 → M7 със същия първи ред; „владислав варненчик“ M3 → M7) са ПОВЕДЕНЧЕСКА промяна и влизат като подписваема делта в манифеста P7→F12 (ред R4 на опашка A: „голата фраза отговаря с местата на квартала“). Не се връщат назад тихо.
4. **Разширенията на write-set-а**, обявени от изпълнителя, се приемат: `LEGACY_BUNDLE_SHA` препиннат; `manastirski_rid` вън от `QUARTER_CODES` (речникът вече не го носи — гейтът иска точно равенство); `probe_places_fv.mjs` cache v6; `probe_out/token_parity.json`; `docs/archive/activeContext_2026-09-04.md`.
5. **Щета, призната от изпълнителя:** `git reset --hard HEAD~1` унищожи некомитнатите промени в `scratch/basemap_e0/e0_range.json` и `e0_range_report.md` (никога стейджвани → невъзстановими; файловете са на комитнатото си съдържание 048e404). **Правило от този момент (влиза в §0 на v2 и в AGENTS.md на двете репа): изпълнител никога не пуска `git reset --hard`, `git checkout -- <файл, който не е негов>`, `git clean`; поправка на комит = `git reset --soft` или нов комит; преди всяка операция, която пипа работното дърво — `git stash list` + изричен списък на чуждите мръсни файлове.** Въпрос към Петър: E0-числата (другата тема, паркирана) да се премерят ли наново с E0-хармонията, или се оставят?
6. **Бележки към картата:** Б6 („varna_3d работното дърво ≠ HEAD“) беше CRLF-артефакт, не некомитнат прогон — задрасква се; правилото „SHA на доставка = `git show HEAD:`, не байтовете на диска“ важи и за одиторите. Отрицателните проби на `qa_no_cad_ids`/`qa_no_extrude` са tracked от предишни цикли — не се повтарят при всеки комит.
7. **Редът продължава без промяна:** F12-д, F12-е → одит на целия F12 → A.2 (release-гейт, `gates.sign`, куката с unittest, прекласификация на 21-те „чакат подпис“) → одит → A.3 опашката (R1 150 реда по клас · R2 baseline · R3 манифест BASE→P7 · R4 манифест P7→F12 + думите на М7 + двата контрола · R5 8-те сблъсъка · R6 трите кода · Q7 break-glass вън · R9 М6/М7 ретроактивно · Q10 E0-премерване).
