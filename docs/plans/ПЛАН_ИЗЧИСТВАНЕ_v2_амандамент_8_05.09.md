# Амандамент №8 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 12:50) — след одита на A.2-7 (НЕГОДНО: 2 major, 3 minor); всичко се приема; отговорите на изпълнителя

A.2-7 (566c20d) и A.2-8 (81ccd17) са доставени: „не“ с несъществуваща заявка блокира и писалката го отказва; изрична котва `_meta.queue_reference`; дайджестът само от комит на Петър или „приет по опашка“; 8 нови теста; решаващият експеримент повторен (286 теста, 0 червени, 7/7). Одиторът намери два съседни пробива → **A.2-9** (един комит; write-set: `gates/release.py`, `gates/sign.py`, `gates/run_gates.py`, `tests/test_places_search_gate.py`):

## Major
1. **„не“ с `покрива` без наклонена черта (`gate_lot1градина`) е нямо** — `delta_patterns()` изхвърля шаблона, `refusals_that_cover_nothing()` прескача реда, писалката мери върху филтрирания списък. → всяко „не“, чийто `покрива` е празен ИЛИ дава нула валидни шаблона ИЛИ съдържа шаблон, който не улучва нищо → BLOCK с текст, който назовава самия шаблон („шаблонът не е във вида кофа/заявка“ / „не улучва нито жива делта, нито разлика спрямо котвата“); `gates.sign` мери върху суровия `покрива` и отказва. Проверката е ПО ШАБЛОН и в гейта (minor 3): всеки шаблон на „не“ ред трябва да улучи; недостигналите се назовават поименно.
2. **Дайджест, въведен от агент, пуска пренаписано подписано тяло след тривиален комит на Петър по опашката** — третият клон на проверка 7 („приет по опашка, комитната от Petar1984“) е ✓. → става `check.fail`: „дайджестът на тялото %s е въведен в опашката от %r в %s, а подписва само Petar1984“; `row_digests` се пълни САМО с редове, чийто дайджест е въведен от комит на Petar1984 (`queue_digest_commit(...)[1] == HUMAN_AUTHOR`). Счупен вход = сценарият на одитора (агент пренаписва тяло + агент пише дайджест + тривиален комит на Петър) → run_gates ✗, release BLOCKED.

## Minor
3. (в т. 1) проверката по шаблон в гейта.
4. **Котвата** остава „комитът, който носи замразената референция“ (не подписващият комит — той не съществува преди подписа; отговорът на изпълнителя е приет), НО: `gates.sign` отказва да запише котва, чийто комит не е на Petar1984; `release.queue_reference_anchor()` блокира при чужд автор (вече е така); тест, в който подписващият комит и комитът-носител са РАЗЛИЧНИ, утвърждава котвата = носителя.
5. `gates.sign` дописва `_meta.queue_reference` при първия подпис на тяло без котва — ПРИЕТО и записано: първият подпис на кръга променя `expectations.json` с +8/−1 реда (само котвата), LF се пази, нищо друго не се пипа.

## Отговори на изпълнителя
- Котвата = носителят на референцията — ДА (т. 4). — Писалката дописва котвата — ДА (т. 5). — Бавният end-to-end тест на `--freeze` в пакета — НЕ (seam-тестът + експериментът в изолиран клон стигат; ~50 s не влизат в пакета). — Цената на отказа (точна заявка + съществуваща) — потвърдена.

## Ред
A.2-9 → одит (двата счупени входа на одитора + повторен решаващ експеримент) → A.3 опашката → Петър.
