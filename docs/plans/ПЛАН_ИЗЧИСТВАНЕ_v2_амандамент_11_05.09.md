# Амандамент №11 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 14:25) — „давай по път 1 — искам чист пуш“ (Петър)

## Решения на Петър (дословно и следствията)
1. **Пушът минава само през куката** (никакъв `--no-verify`).
2. **Дайджестът на тялото** (амандамент №10 т. 4): за доставка A се приема вариант (а) — несъвпадение след замразяване се прощава, когато последният комит по артефакта е на Petar1984 (кодът вече е такъв); пълната строгост (замразяването преподписва реда) е за Фаза D.
3. **Амандамент №10 т. 1–3, 5 (A.2-11)** се отлагат СЛЕД пуша на доставка A и ПРЕДИ следващата доставка: блокерът на писалката (дайджест в скрит регион) не се задейства, когда опашката няма code fence и HTML-коментари — затова опашка A се пише без такива региони (правило за файла, проверено от изпълнителя с `release.visible_and_hidden`: 0 скрити редове).

## Ред за чистия пуш (днес)
1. **A.3** — изпълнител пише `scratch/places_search/ЗА_ПОДПИС_A.md` (класове въпрос/артефакт/делта; без fence/коментари; „да“ по клас `кофа/*`; доказва в scratch-копие, третирано като подписано, че `gates.release` парсва всеки ред и покрива всички текущи делти) и комитва (Claude Executor).
2. **Петър:** `python -m gates.sign --all да --queue scratch/places_search/ЗА_ПОДПИС_A.md`, после комитът, който писалката отпечатва (негов автор). (Първият подпис дописва котвата в `expectations.json`.)
3. **Изпълнител:** единственото `python scratch/places_search/recall_sweep.py --freeze` (само при зелен release).
4. **Петър:** комит на замразените тела (референция, очаквания, паритет) — писалката/замразяването отпечатват точните файлове.
5. **Проверка:** `python -m unittest discover -s tests` → 0 червени; `python -m gates.run_gates` → 7/7; кратък одит (read-only).
6. **Петър:** `git fetch origin` · `git rebase --autostash origin/main` · `git push origin main` (куката пуска тестовия пакет и гейтовете).
7. **Проверка след пуша:** SHA на публикуваните blob-ове = локалните; Адмирал на Златни пясъци; „златни“ вади хотелите; кешът v6.
