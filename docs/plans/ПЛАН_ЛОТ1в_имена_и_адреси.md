# ПЛАН · ЛОТ 1в „Разгърнати имена и адреси на местата" (04.09.2026) — чака Gate 1

**Повод (Gate 2 на ЛОТ 1, думите на Петър):** „не виждам военноморското" · „искам да имаме и точните адреси на тези заведения — не само имената им… искам да може да се търси и по адрес… трябва да намерим точните адреси, за да ги поставим в картончето" · „искам да може да се търси и по адреса и по името на детската градина".

**Мярка преди плана (04.09, трима на Opus + одитор-синтезатор, само четене; пълните текстове в изхода на wf_1c061d5b):** всичко долу е измерено, не предположено.

## 0 · Какво показа мярката

- **ВВМУ „Н. Й. Вапцаров"** е в доставката (университет, кв. Чайка, OSM) и се намира по „ввму", „вапцаров", „университет", „университет чайка"; не се намира по „военноморско", „военноморско училище", „морско училище". Причина: името е само съкращението, записът няма псевдоним. OSM таговете на обекта (way 41265447) НЕ носят official_name/alt_name/short_name — единственият извор за разгърнатото име е **Wikidata Q7035695** (CC0): „Висше военноморско училище, Варна".
- **Класът:** от 150 места 128 носят съкращение, но 118 са покрити от думата на вида (ДГ/ОУ/ДЯ/хоспис/болница са `kind` и речникът ги знае). Истинската дупка (б) е **10 места + 1 хотел**: ВВМУ; I ЕГ и IV ЕГ (без „езикова"/„гимназия"); 8 СОУПЧЕ; 7-те ДКЦ (регистърът сам пише „ДКЦ", „диагностично-консултативен център" дава 0 реда); хотел МДУ. Измерено с референцията: „езикова гимназия" → 12 реда, нула ЕГ; „първа езикова гимназия" → грешното училище; „диагностично-консултативен център" → 0.
- **Жив дефект на main (дълг):** 46 реда носят `old_names` при `src` ≠ OSM, 29 от тях са байт-идентични OSM имена; картончето изписва извора НА РЕД, тоест твърди „Регистър на лечебните заведения" за низ, който е OSM/ODbL. Атрибуцията лъже за до 29 реда.
- **Прототип (копие, нищо в репата):** 3 псевдонима → 0 от 122 реда на референцията мърдат; „военноморско училище" 5→6 с ВВМУ пръв; „езикова гимназия" 12→13 с I ЕГ пръв; „морско училище" пак не го намира („морско" ≠ „военноморско").
- **Адресите:** регистърът има адрес за 186/186 реда; 113 положени в доставката, от тях 74 с „улица + №" (49 % от 150); КАИС адресното поле покрива града 99,8 % (улица 70,8 %, номер 52,9 %), но доставката НЕ носи `i` на тялото (връзката е само във varna_3d); OSM addr тагове — 35 от 221 институционални реда. Пример ДГ№12 „Ян Бибиян": регистър „ул. „Дойран" №9" = КАИС „ул. ДОЙРАН 9" (тяло 54501); картончето казва „м-т Шашкъна", защото зоната идва от чертана обвивка (стъпка 3 на стълбата = 75 от 150). Хотелите: НТР `TOAddress` през `uins` — 104/222 с номер, 79 празни, 11 носят кадастрален идентификатор в самия адрес (маскиране задължително).
- **Пин, който не бива да се забравя:** `index.html` `PLACES2_SHA256` се изпълнява при зареждане — всяка промяна в `data/places.json` чупи живата карта, ако същият комит не обнови пина. `data/places.json` е генериран от varna_3d — write-set-ът започва там.

## 1 · ЛОТ 1в-А „Разгърнати имена" (малък; 0 регресии измерени)

**Правило:** псевдоним само с извор и лиценз; нищо измислено. Два канала:

1. **Речникът (думи на вида, подписва Петър):** `place_categories.json` форми → чип: „гимназия", „езикова гимназия"?→ НЕ (не е клас) · „гимназия" → училище; „диагностично-консултативен център", „диагностично консултативен център", „дкц", „поликлиника" → ДКЦ; „висше училище", „академия", „вуз" → университет; „ясли" вече е. Ефект: „гимназия богоров", „поликлиника младост", „диагностично-консултативен център 3" намират по клас + собствено име. Извор: определенията на видовете по закон (ЗЛЗ за ДКЦ; ЗПУО за гимназия; ЗВО за висше училище) — думи, не данни.
2. **Псевдоними на запис (`old_names` → `aliases` с извор на ред):** ВВМУ ← Wikidata Q7035695 „Висше военноморско училище, Варна" (CC0); I ЕГ ← Wikidata Q12291800 „Първа езикова гимназия" (CC0); IV ЕГ ← OSM (ODbL, вече разгърнато в снапшота); 8 СОУПЧЕ ← регистъра на училищата, ако носи разгърнатото име (мери се; иначе Wikidata/официален сайт като отделен факт с URL и дата); хотел МДУ ← OSM way 199237000 „Международен дом на учените „Фредерик Жолио-Кюри"" (ODbL). Канал: varna_3d `data/place_aliases.json` → `alias` → `export_fire_varna_places.py` → `old_names`/`aliases`; клиентска промяна НЕ се иска (A6 вече индексира old_names).
3. **Дългът с атрибуцията:** всяко старо име/псевдоним носи свой извор (`aliases: [{name, src}]` или паралелен `old_names_src`), картончето изписва извора на псевдонима, не на реда; гейт в износителя: псевдоним без извор = червено; гейт в Fire_Varna: `SRC_LINE` по псевдоним.
4. **Gate-ове:** референцията само расте (С6′ корпус +N), 0 презамразени реда (измерено за 3; мери се за всички); G3 байт-равен; `PLACES2_SHA256` в същия комит; тестове зелени + нов тест „всеки псевдоним има извор".
5. **Бюджет:** ≈ 0,5–1 KB данни.

**За подпис (Gate 1-А):** (i) Wikidata (CC0) като извор на имена — това е ADR-решение (нов извор → ред в лицензите); (ii) думите в речника по т. 1; (iii) 107,7 m разминаване Wikidata↔пин за ВВМУ достатъчно ли е за идентификация (I ЕГ 35,9 m); (iv) „морско училище" остава известна дупка (не се измисля псевдоним) — или Петър дава дума; (v) дългът с 29-те реда — в същия лот (препоръка: да).

## 2 · ЛОТ 1в-Б „Адреси на местата" (по-голям; след А)

1. **Поле `address` на всеки от 375-те записа, с извор:** предимство по полезност — извор, който дава „улица + номер": (1) КАИС адресното поле на тялото под пина (АГКК, вече ползвано за координатите; форматът се канонизира: „ул. ДОЙРАН 9" → „ул. „Дойран" 9"); (2) регистровият адрес (Община/МОН/ИАМН/НТР), когато носи улица и номер; (3) OSM addr:street + addr:housenumber (ODbL); (4) нищо → остава зоната, честно, без адрес. При разминаване (1)≠(2) (ДКЦ Младост-М: „Република 91" срещу „бул. Република №15") — бордът, окото на Петър. Хотелите: НТР адрес през `uins` с маскиране на кадастралните идентификатори (11 реда) — гейтът за кадастъра остава.
2. **Картончето:** ред „адрес: ул. „Дойран" 9 · КАИС" под вида и зоната; когато адресът е само квартал — не се повтаря зоната.
3. **Търсенето (само в `initPlacesSearch`, нито байт в `initAddressSearch`):** нов клас токени „улица" (`sset`), никога в `zkset` (иначе А3′ ще ги смеси с кварталните фрази); номерът е значещ само до уличен токен; правила: „<клас> <улица>" → местата от класа на тази улица (A3′-аналог по улица); „<улица> <номер>" → мястото/местата на този адрес (пред адресната търсачка? НЕ — местата се показват в своята секция, адресната търсачка остава каквато е); „<име>" както днес. Референцията огледално; нови гейт-редове („детска градина дойран" → ДГ№12; „дойран 9" → ДГ№12; „ул. дойран" → 1; контроли „владиславово 203"? — мери се).
4. **Мярка първо (във varna_3d, един ред):** за 375-те записа — колко тела имат `addr`/`street`/`num` (join по `i` през relations/poi/REGISTRY_ROWS), колко получават „улица + номер" от (1)/(2)/(3), таблица по вид, списък на разминаванията (1)≠(2), 10 примера. Тя дава числата за подписа.
5. **Gate-ове:** референцията се презамразява (адресните токени сменят отговорите на заявки с числа — мери се; подписан списък); G3 байт-равен; бюджет (~375 × ~40 B ≈ 15 KB); лиценз: КАИС атрибуция вече е на екрана; регистровият адрес на ред = отделен факт (същото правило като името); OSM адрес — ODbL, същата атрибуция като имената.

**За подпис (Gate 1-Б):** (i) предимството на изворите (препоръка: КАИС тяло > регистър > OSM); (ii) думата „адрес" в картончето и формата на канонизацията; (iii) правилата за търсене по улица и номер (т. 3) — това е клиентско правило, ново; (iv) списъкът на записите с адрес и извор (идва от мярката); (v) хотелите: НТР адресите — да или не.

## 2а · Подписи

- **Gate 1-А ПОДПИСАН (Петър, 04.09 ~03:20):** „подписвам gate 1-a и го пусни за проверка от кими и сол" — тоест лот А тръгва след атаките на Кими (данни/лицензи, бриф `scratch/places_search/lot1v_brief_kimi.md`) и Сол (механика/гейтове, бриф `scratch/places_search/lot1v_brief_sol.md`); присъдите им влизат тук като амандаменти с предимство; окончателната дума е на архитекта, подписът на Петър стои. Push-ът на main се отлага до затварянето на адресите (думите му: „или първо да поправим адресите и след това ще пушна").
- Проверката преди push (04.09 ~03:15, измерена): `data/hydrants.json` (7407 записа), `data/search_index.json`, `data/address_rows.json`, `sw.js`, `worker/` са байт-равни с `origin/main`; IIFE `initAddressSearch` байт-равен (sha cc0c6dbf…, 33 393 B) с `origin/main`; тестове 186 OK; `index.html` 526 153 → 532 231 B; service worker-ът се регистрира само зад комитнат `false` флаг (обикновените потребители не го пускат); трите blob-а на местата се теглят при инициализация на търсачката с sha пинове. main е 49 напред / 1 назад → преди push: `git rebase --autostash origin/main`.

## 2б · Присъдите на Кими (K3, 04.09 ~03:40; лещата данни/лицензи) — ПРИЛОЖЕНИ като амандамент А1

Кими вижда само подадените файлове (без интернет, без `data/places.json`/`hotels.json`, без OSM снапшота); присъдите по К1–К7 и промените, които приемам:

- **К1 Wikidata CC0 — ПРИЕМАМ с уговорка → приложено:** етикетът влиза само като псевдоним за търсене, не като показвано име; основанието за идентификация е **съкращение + вид + зона**, разстоянието (107,7 m / 35,9 m) е свидетел, не ключ; лицензен ред в `_meta.licence_wikidata`: „Разгърнати имена (псевдоними за търсене) на 2 места: Wikidata Q7035695, Q12291800, CC0 1.0 Universal, достъп 04.09.2026"; **низът се зафиксира в данните с датата на достъп** (К7-3) — не се дърпа живо.
- **К2 речникът — по дума → приложено:** „гимназия" → училище ДА (ЗПУО чл. 17–18; поднизът покрива и ПГ); „диагностично-консултативен център"/„дкц" → ДКЦ ДА (ЗЛЗ чл. 10); „висше училище" → университет ДА (ЗВО чл. 17); „академия" → университет ДА с уговорка (ЗВО чл. 19); **„поликлиника" и „вуз" = разговорни форми без законов довод, подписани изрично от Петър** (Gate 1-А ги покрива), с вписана известна дупка: медицинските центрове (МЦ, решение 7 — вън от доставката) не се намират по „поликлиника".
- **К3 псевдонимите → приложено:** ВВМУ ← Wikidata (регистър на висшите училища няма); I ЕГ ← Wikidata (манифестът school#39 носи само „I ЕГ"); **IV ЕГ ← регистъра на училищата, ред school#40 „IV ЕГ „Фредерик Жолио-Кюри"" (регистър > OSM; unplaced-статусът не пречи на името), OSM само втори свидетел**; **8 СОУПЧЕ: регистърът НЯМА ред (измерено от Кими: само в board_queue) → извор = официален сайт/Wikidata с URL и дата, иначе дупка**; хотел МДУ ← OSM way 199237000 (Кими не може да провери; изпълнителят цитира снапшота дословно); „морско училище" остава известна дупка.
- **К4 атрибуцията — ПРИЕМАМ с уговорка → приложено:** извор на всеки псевдоним; **всеки нов OSM псевдоним извън заварените 29 = ред в лицензите, не само `src` на реда**.
- **К5 адресите — ПРИЕМАМ по всички букви, с две вписвания:** (б) границата „само адреси на вече доставени места" се вписва изрично (чл. 4 ЗАПСП отделни факти; чл. 93б несъществена част — не се източва регистърът лот след лот); (в) условие 4 на ФАЗА_0 е ограничено до 3D-изхода (геометрията с КАИС базата), не до доставката на местата; (г) **маскирането на „ПИ №" е в извора (varna_3d) + publish-гейт по шаблон „ПИ|кадастр" върху целия публичен blob**, не само върху 11-те реда.
- **К6 предимството — ПРИЕМАМ:** КАИС адресът на тялото под пина е физическият адрес; **два номера на един комплекс ≠ разминаване** (ДКЦ Младост-М: №91 и №15 са един комплекс); на борда отиват само различни улици / различни тела; картончето показва КАИС адреса с „· КАИС", регистровият не се показва.
- **К7 → приложено:** (1) канонизацията на КАИС формата е независим препис → поуката от Амандамент №6/№11 поименно: всички гейтове с очаквани стойности във write-set-а по подразбиране; (2) блоковата номерация („владиславово 203" = блок, не адрес) — Gate 1-Б се подписва върху ИЗМЕРЕНИТЕ отговори, не върху правилото; (3) Wikidata с дата на достъп и зафиксиран низ (в К1); (4) **хотел без НТР адрес пада на КАИС адреса на тялото под пина** (същото правило като местата; 79 празни НТР реда не остават недефинирани); (5) **предимството на изворите се подписва СЛЕД таблицата от §2 т. 4** и се преподписва, ако мярката обърне реда.

## 2в · Мярката за адресите върху 375-те (04.09 ~04:30; проверяващият сам направи join-а по `place_identity.json` → `kais_i`; файл `scratchpad/addr_measure/auditor_join.json`) — числата за Gate 1-Б

- **Котвата към тялото вече съществува за 375/375** (ntr 192 · kais 144 · reg 35 · key 4); нова геометрия не трябва; живите разстояния пин↔тяло: 306 в тялото · 48 под 10 m · 19 под 100 m · 2 над 30 m (ТУ-Варна 37,4; ВВМУ 31,1).
- **Строг критерий „улица + номер"** = префикс `ул./бул./пл.` + номер ≠ „0" (наивният „street непразен" надува с ~25 %: 72 от 288 `street` стойности са местности или имена на хотели; 3 записа имат `num = 0`).
- **Покритие по извор (строго):** КАИС тяло **159/375** (места 104/150, хотели 55/225); регистър 74 (само места); НТР 80 от 122 непразни (хотели); OSM по име 14/375. **Обединение „поне един извор": 213/375 = 56,8 % (места 117/150, хотели 96/225); каскада КАИС → регистър → НТР: 177 · 6 · 30; без адрес: 162** (най-вече курортни хотели — КАИС там е ориентир „х-л …", 55 записа).
- **Разминавания КАИС↔регистър сред доставените: 30** (19 различна улица, 11 различен номер) — напр. ДГ-4 „Теменужка" филиал 1: регистър ул. „Шейново" 18 ↔ КАИС ул. „Цар Симеон I" 14 при d = 0. По К6: различна улица → бордът; различен номер на едно тяло → КАИС.
- **4 тела носят 11 записа** (бул. „Република" 91: ДКЦ Младост-М + 4 хосписа) — каскадата „КАИС пръв" би им дала един и същ низ, а регистърът ги различава („тяло В, ет. 6"). Правило: **когато няколко доставени записа делят едно тяло, регистровият адрес (с тяло/етаж) печели, ако носи улица + номер.**
- **Кадастърът:** суровият НТР файл носи 13 маркера `[кад. ид. №N]` в 11 реда (вече маскирани в извора); 21 доставени хотела носят имотен токен в НТР текста (ПИ/УПИ/кв. N/парцел/имот), 19 палят шаблона „ПИ|кадастр". Правило: имотните токени НЕ влизат в адреса изобщо (изрязват се до „улица + номер" или адресът се изпуска) + publish-гейт по шаблон върху целия blob.
- **Канонизация без измисляне:** (1) ако регистров ред съвпада по улица + номер с КАИС → регистровото изписване („ул. „Дойран" №9"); (2) иначе КАИС байт-в-байт с две хигиени (двойни интервали; висящо „-"/„," в края); никакъв title-case (ОСМИ ПРИМОРСКИ ПОЛК, КНЯЗ БОРИС I, СВ.СВ.КОНСТАНТИН И ЕЛЕНА, ЦАР ОСВОБОДИТЕЛ са неразрешими автоматично); (3) печатната грешка в КАИС речника „ДИМИТЬР ПОЛЯНОВ" (Ь вместо Ъ) → таблица с подписани поправки (една засега) — търсенето индексира и двете форми.
- **Бюджет:** 16 888 B стойности (средно 45 B), ~21,6 KB с ключа `address` — в бюджета.

**Списъкът за подпис е генериран** (`scratch/places_search/lot1v_addresses_for_signature.md`, 04.09 ~06:30) със СТРОГИТЕ правила (НТР само с префикс ул./бул./пл. и без имотни токени; регистър само с улица + №): **с адрес 179 / 375 (места 115/150, хотели 64/225); без адрес 196**; по извор: КАИС 151 · регистър 9 · регистър при споделено тяло 9 · НТР 9 · OSM 1; разминавания: различна улица **18** (→ бордът, §В на списъка), различен номер **6** (→ КАИС, §Г); споделени тела с няколко записа: 4 (бул. „Република" 91 ×5; ул. „Велчова завера" 12 ×2; бул. „Владислав Варненчик" 0 ×2 — КАИС номер „0", тоест без адрес; ул. „Върба" 24 ×2). Числото 213 на проверяващия е с по-хлабав НТР критерий (номер без улица — „к.к. Златни пясъци 12" не е адрес); строгото 179 е това, което влиза в картончето.

**За подпис (Gate 1-Б, с числата):** (i) предимството КАИС > регистър > НТР > OSM > нищо — потвърдено от числата (КАИС 151 срещу регистър 74), с изключението за споделени тела (регистърът печели); (ii) списъкът §А (179 записа с адрес и извор) и §Б (196 без адрес — само зона, честно); (iii) 18-те разминавания с различна улица — на борда (окото ти), 6-те с различен номер → КАИС; (iv) правилата за търсене по улица и номер (§2 т. 3) — след присъдата на Сол; (v) „ДИМИТЬР" поправката.

## 2г · Присъдите на Сол (Codex, 04.09 ~07:00; лещата механика/гейтове; снимка на плана a123de8…) — ПРИЛОЖЕНИ като амандамент А2

- **S1 ОБОРВА „клиентска промяна не се иска" → приложено:** A6 индексира `old_names` само като отделни токени; пълният псевдоним „Висше военноморско училище, Варна" губи от класовия ключ „училище" (A1 блокира fail-open), а „варна" става alias-токен и мърда броевете. **Клиентска промяна (минимална):** `buildIndex` пази нормализирания ЦЯЛ псевдоним в `EXACT_ALIAS` и проверява точно съвпадение на псевдоним преди A1 (както EXACT_NAME за имената); `aset` получава само единични не-генерични токени на псевдонима (без „варна", без класови думи); гейтове: пълният Wikidata низ, „военноморско" и „военноморско училище" → ВВМУ пръв; „морско училище" → 0 (известна дупка); „варна" не получава alias-only резултати. Огледално в референцията.
- **S2 ОБОРВА `aliases:[{name,src}]` → приложено:** публичната схема пази `old_names:[str]` и добавя **`old_names_src:[code]`** със същата дължина и ред (кодове: `OSM`, `REG`, `NTR`, `WD` (Wikidata), `WEB`); при съвпадение по псевдоним резултатът носи `matched_alias_i`, картончето изписва извора на псевдонима, търсенето по основното име ползва `row.src`; **и двата износителя** (места + хотели) + QA на извора + валидаторите (места 9 ключа, хотели 13) + референция/проба/bundle тестове; липсващ/непознат извор или различни дължини = STOP; отровен псевдоним се отхвърля и в двата репота; SHA/байт/gzip пиновете се обновяват атомарно.
- **S3 С УСЛОВИЕ → приложено:** думите на видовете са адитивно замразяване (прототип: 0/122 мръднали; ефекти: „гимназия" M3/12 → M1/57, „гимназия богоров" 13 → 1, „поликлиника" 1 → 7, „диагностично-консултативен център 3" 0 → 1); генераторът няма канал за курирани синоними и `qa_place_aliases.py` отхвърля 7/8 по морфологичния корен → **нов вход `CURATED_CLASS_FORMS [{form, chip, source}]`** в `build_place_categories.py` с гейт за произход и уникалност (чипът трябва да съществува и да има записи; морфологичният гейт остава за генерираните форми); 122-те реда остават равни по (q, branch, ordered name/zone); нови поименни редове `form` и `form+name`; мръднал стар ред = STOP → подписан списък.
- **S4 ОБОРВА наивното добавяне на улиците към A3 (лот Б) → влиза в Gate 1-Б:** от 236 join-нати записа 101 улични фрази/157 токена: 51 се пресичат с имена, 22 с псевдоними, 7 със зони; „училище владислав варненчик" би станало 4 → 6; „ул. дойран" дава два записа (ДГ№12 на №9 и очната болница на №15); „дойран 9" вече дава 8 реда в адресната търсачка → **адресите остават отгоре и Enter остава техен**. Правилото: износителят подава `address.street_phrase` (подредена фраза) и `address.house_key` (точен номер с буквен суфикс); клиентът НЕ ги извлича от текста и не ги смесва с `nset/aset/zkset/significant/A5`; нов клон `A3-street` след точно име/псевдоним и зонов приоритет, преди fuzzy/fail-open; при колизия улицата се избира само с „ул./бул./пл." или с house key; число без съвпаднала пълна улица не участва; безкласово улично търсене показва картончето на институцията ПОД непокътнатите адресни резултати. **Шестте гейта:** (1) „детска градина дойран" → само ДГ№12; (2) „дойран 9" → само ДГ№12 в местата, `hasKey=false`, адресите/Enter отгоре; (3) „ул. дойран" → ДГ№12, после очната болница; (4) „детска градина 12" остава M2 → ДГ№12; (5) „училище владислав варненчик" остава същите 4 зонови училища; (6) „хотел приморски" остава ПРИМОРСКИ, Маргарита, Вемара сити, Траката, Еллинис — без ПАНОРАМА.
- **S5 ОБОРВА → приложено в §2в:** `address` е `null | {text, src, street_phrase, house_key}` със затворен код на извора; parse-гейт (низове като „ул. "4 5" не са адреси — STOP/борд); шест хотела с повече от един различен НТР адрес → борд; крайният бюджет се МЕРИ (raw + gzip; частичният прототип: 20 933 B raw / 2 735 B gzip), не по формула; и двата пина (`PLACES2_SHA256`, `HOTELS_SHA256`) атомарно; целият публичен blob се сканира за PII, „ПИ|УПИ|кадастр" и маскиращи маркери.
- **S6 С УСЛОВИЕ → приложено:** референцията се сравнява (финален кандидат срещу комитната котва след лот А) по (bucket, q, branch, hasKey, ordered [(name, zone, kind)]); неподписано движение, изчезнал/неочакван bucket или подписан-но-немръднал ред = STOP; при 0 движения — без презамразяване, шестте S4 заявки влизат като нов bucket `gate_lot1v_b`; `REF_BUCKETS`/тестовите buckets стават fail-closed (днес `.filter` би подминал тихо нов bucket); M5 = (122 + N_A + 6)/…; G3 байт-равен при sameEnv; паритетът на токените включва псевдоними/улици/номера.
- **S7 ОБОРВА pre-push гейта (за протокола, push-ът вече мина чисто):** IIFE `initPlacesSearch` е eager в същия `<script>` преди `bootApp()` — синтактична грешка би оставила празна карта; затова гейтът преди push = браузърна проба 375×812 с `pageerror`/console/network: 7407 хидранта, Топ 5/Близо/Всички, follow, report modal, без place заявки преди фокус, повторено със забавен/отказан blob и със/без SW. Поправка на моята мярка: правилният IIFE диапазон е 84 766 B / d706a650… (аз извадих 33 393 B / cc0c… — грешен край на извличането); байт-равен с origin и в двата случая. Сол не можа да пусне 186-те теста в read-only runner (33 грешки от temp) — при мен минаха в writable runner.
- **S8 пет STOP-а → приложено:** (1) **ADR-добавка** (нова `docs/decisions/008_places_aliases_and_addresses.md`, Proposed) — схемата `old_names_src`/`address`, съвпадението (EXACT_ALIAS, A3-street), произходът, кешът; (2) измервателният артефакт (join-ът на 375-те, маскиран) се комитва в `scratch/places_search/lot1v_addr_join.json`, планът и списъкът също; (3) ADR 006 казва `fire-varna-hotels-v2-226`, кодът е `v2-225`, `sw.js::PROTECTED_CACHES` не носи нито едното → ADR 008 го изчиства (кешът на местата се управлява от index.html; SW не го пази); (4) байтовете на Windows работното дърво ≠ blob (CRLF) — правилото „sha на доставка = git show HEAD:" вече е в паметта; (5) rebase преди кандидат-билда — направен от Петър (main = origin/main = a58010e).

## 2д · Амандамент А3 (04.09 ~09:00, след STOP на P3-а преди първата буква — измерено: 22 от 77 заварени псевдонима нямат код в ADR 008 D1)

1. **Затвореният списък на изворите става {OSM, REG, NTR, WD, WEB, KAIS, CUR}:** `KAIS` = КАИС/АГКК адресното поле на тялото (15-те „старо кадастрално име" на хотелите + „Явор"; картончето: „старо име: КАИС адресно поле (АГКК)"); `CUR` = курирано съкращение от речника `data/place_aliases.json` с произход в полето `why` (ВМЕИ, ИУ, ВИНС, МУ, ВМИ, ВСУ, ВУМ; картончето: „съкращение (речник)"). Не е нов извор на данни — имена на извори, които вече доставяме; ADR 008 D1 се поправя.
2. **I ЕГ:** низът дословно от снапшота — „Първа езикова гимназия, Варна" (Q12291800), както ВВМУ.
3. **Датата на достъп до Wikidata = 03.09.2026** (датата на снапшота `wikidata_raw_2026-09-03.json`, sha256 488a4a87…); лицензният ред я носи.
4. **IV ЕГ получава и Q12299161 „Четвърта езикова гимназия „Фредерик Жолио-Кюри""** (WD, 16,8 m) наред с регистровото „IV ЕГ „Фредерик Жолио-Кюри"" (REG) — регистровото не затваря измерената дупка („езикова"/„гимназия"), Wikidata го прави; в подписания обхват (Wikidata като извор на псевдоними, Gate 1-А).
5. **8 СОУПЧЕ остава на борда:** регистърът има ред school#37 „СУЕО „Александър Сергеевич Пушкин"" на същия адрес (ул. „Проф. Николай Державин" 12 — OSM way 93373842 носи същия адрес), но това е преименуваната форма, не разгъването на съкращението; равенството не се твърди без извор (Кими К3 е оборен по фактите, не по извода).
6. Хотел МДУ ← OSM way 199237000 дословно (прави кавички), ред в лицензите (К4).

## 2е · Амандамент А4 (04.09 ~10:30, след P3-а = varna_3d `1d5ec9a` и STOP на F5-а с измерени алтернативи)

1. **`aset` на псевдонима (S1/D2 поправка):** влизат всички единични токени на псевдонима освен „варна" (и другите генерични географски: „варна", „гр", „град"); класовите думи ОСТАВАТ — измерено: без „училище" в aset „военноморско училище" дава ВВМУ втори (П2-покритието губи от „Спортно училище"), с него — ВВМУ пръв, 6 реда; „варна" без alias-only редове и в двата варианта. Клаузата на S1 за класовите думи не беше премерена срещу собствения си гейт — отпада.
2. **`EXACT_ALIAS` само за заявки с ≥ 2 значещи токена.** Едносрични заявки („синчец", „военноморско") вървят по днешния ред (alias-токените вече са в aset; „военноморско" дава ВВМУ пръв и без EXACT_ALIAS). Така „синчец" остава ДГ 30 „Синчец" → ДАНА ПАЛАС и **0/122 замразени реда мърдат**; сегашното име не губи от старо име при частично съвпадение.
3. **README + тест:** Wikidata е нов извор на имена → ред в лицензната таблица на README (BG + EN) + тест, който пинва `_meta.licence_wikidata` (места) и `_meta.licence_osm` (хотели) — в същия комит F5-а (К1/К4).
4. **IV ЕГ ← REG school#40 влиза (вариант А на изпълнителя):** скелетът на псевдонима е скелетът на регистровия ред → `place_identity` полага school#40 (правилно: доставеният ред Е това училище); манифестът: положени 113 → 114, без пин 73 → 72; бордът §4 „4-те общински без пин" става „3-те общински" (I ОУ „Св. княз Борис І", ОУ „Раковски", ГПЧЕ „Йоан Екзарх"); препоръките П10 се четат с това. Малък комит **P3-б** във varna_3d преди F5-а.
5. Датата на достъп до Wikidata остава 03.09.2026 (А3 т. 3; заданието на изпълнителя казваше 04.09 по грешка).
6. За протокола: по произход OSM-псевдонимите под регистрови редове са 47, не 29 (планът броеше само байт-съвпадащите); дългът е затворен по конструкция в P3-а.

## 3 · Ред на изпълнение

Gate 1-А (думи + Wikidata) → лот А (varna_3d: речник + псевдоними + извори; Fire_Varna: blob + пин + картончето; одит) → мярката за адресите → Gate 1-Б → лот Б → референцията презамразена → одит → Gate 2 → push. Всичко върху пушнатия main.
