# ПЛАН · Местата, фаза 2: училища, университети, болници, детски градини — v1 (изпълним; по думата на Петър от 02.09 вечерта: „преди да push-на")

**Файл:** `docs/plans/places_phase2_plan.md` · **Дата:** 02.09.2026 (v0 = мярка; v1 = изпълнение) · **Статус:** v1 — под атака (Кими: данните; Сол: механиката) → изпълнение (varna_3d износ → Fire_Varna) → одит → пробата на Петър → push (негов). **v1.5 (03.09, §10):** C10–C13 изпълнени, одит C14 ГОДНО С УСЛОВИЯ (условията в §10); П7 (кварталните псевдоними) чака Gate 1; Д5 (регистровите градини) = отделен план.
**Възложител:** Петър, 02.09 ~19:30: „не виждам да ми излизат училища и университети. искам да го поправим това преди да push-на каквото и да е в живата карта. ползвай кими и сол за атаки. Искам търсеният резултат да излиза най-отгоре — а не като напиша хотел адмирал да ми излизат адреси с име хотел."
**Механизмът:** същият клон `initPlacesSearch` (планът `places_search_plan_2026-09-02.md` v2.6 + §16) — ключовете вече са всички чипове на речника; нужни са данни + класове + подредба.

## 0 · Какво разбрах (закрит кръг)

1. Училища/университети/болници/градини НЕ са в живата карта, защото доставката е само хотели (по конструкция). Данните ги ИМА в 3D-картата и вече се показват там: `web/varna_poi_names.json` (OSM, **ODbL 1.0**, атрибуция дословно в `_meta`; училище 67 · университет 20 · болница 27 · детска градина 60; 19 училища с регистрово име (МОН/НЕИСПУО), одобрено от Петър на 21.08 — борд, секция В) и `data/places.json` (35 регистрови места: болница 8 · ДКЦ 4 · МЦ 12 · хоспис 3 · детска градина 6 · училище 2 — ИАМН/Община Варна, П3 21.08).
2. „Не измисляме": нито едно име не се пише на ръка; всеки ред носи `src`; всяко изключване е поименно с довод в `_meta.excluded`.
3. Лицензът: OSM-редовете влизат с ODbL-атрибуцията дословно (както в 3D-то); регистровите (МОН/ИАМН/Община) влизат по прецедента на 3D-картата (показвани там от 21.08) с ред-източник, който НАЗОВАВА регистъра; лицензният въпрос (правило 3 от 19.08) остава отворен в доклада — Петър решава преди push дали регистровите редове остават.
4. „Търсеният резултат най-отгоре": при ЗАЯВКА С КЛЮЧ („хотел адмирал", „училище вапцаров", „университет") групата на местата застава НАД адресния списък; без ключ — както днес (адресите първи).

## 1 · Мярката (02.09; командите: `python -c` върху трите файла)

| извор | клас | редове | какво пада (кандидати за `excluded`, решава атаката на Кими) |
|---|---|---|---|
| `web/varna_poi_names.json` | училище 67 | 67 | дублети по име (Международното Френско училище ×2; Професионална Техническа Гимназия ×3 варианта; ПГ „Сендов" ×2); „бивше ОУ „Д-р Никола Димитров"" → `status: бивш`; не-училища с чип училище (Езиков център „Ориндж хаус", „Маестро", „Димчо Дебелянов" без тип — за присъда); ЦПЛР общежитие „Михаил Колони" (общежитие — влиза като училище? за присъда) |
| същият | университет 20 | 20 | вътрешнокампусни имена (Лекционна зала, Нов Учебен Корпус ×2, Пазвантницата ×2, Топла връзка — `src` `osm:building`; „сграда към ТУ – Варна" — `правило У6`) → ВЪН; „Морска гимназия „Св. Николай Чудотворец"" е училище (МОН регистър №46) → чип училище с довод; ВУМ/ВУМ-двойник (Варненски Университет по Мениджмънт + Висше Училище по Мениджмънт, една сграда) → един ред + псевдоним; „База БМКЦ", „Институт по океанология при БАН", „Полицейска академия към МВР" (×2 с колеж) — за присъда |
| същият | болница 27 | 27 | мис-чипове (МЦ Нова, МЦ Оксигена, Медик Дент ЕООД, ФАМИЛИЯ ДЕНТ ЕООД → не са болници; Хоспис Филаретова → хоспис) — за присъда; PII: лични практики НЕ влизат (правилото на v0 §2) |
| същият | детска градина 60 | 60 | частни ДГ/занимални без номер в еднофамилна сграда (Кими: 353, 354, 357–360, 180, 304, 321, 338, 343, 344) — домашен-адрес профил → ВЪН без потвърждение; дублети (ДГ 32 „Моряче" ×2) |
| `data/places.json` (регистър) | училище 2 · ДГ 6 · болница 8 · ДКЦ 4 · хоспис 3 · (МЦ 12) | 35 | МЦ (извънболнични, 5 на жилищна сграда) — **не влизат** във фаза 2 (v0 §2 PII; решение на Петър за по-късно); останалите 23 влизат; при съвпадение с OSM ред (същата сграда/име) регистровото име ВОДИ (прецедент М2 в `poi-search.js`) |

Гейтът на извора: `src/qa_poi_names.py` зелен; `data/places_approved_health_edu.json` = 35; ODbL атрибуция: `_meta.attribution` = „имена на обекти © OpenStreetMap contributors, ODbL".

## 2 · Дизайнът

- **Д1 Износ (varna_3d, по неговия ред):** `src/export_fire_varna_places.py` (по образеца на `export_fire_varna_hotels.py`) → `data/fire_varna_places.json` = `{_meta:{generated, count, by_kind, licence_osm (дословно от poi `_meta.attribution` + „ODbL 1.0"), licence_registry (образецът на НТР: „отделни факти от регистъра …, източникът на всеки ред стои в src"), excluded:[{name, kind, src, why}]}, places:[{name, kind, lat, lon, zone, src, old_names, status}]}` — **8 ключа**, без `i`, без бележки, без кадастрални идентификатори. Подбор: (а) `places.json` venues със subtype ∈ {училище, детска градина, болница, ДКЦ, хоспис, университет}; (б) `poi_names` rows с chip ∈ {училище, университет, болница, детска градина}, БЕЗ `src ∈ {osm:building, правило У6}`, минус изключенията от атаката на Кими (таблица `EXCLUDED` в скрипта, поименно, с довод); дедупликация: регистровото място води; OSM ред, чиято сграда (`i`/`yards`) е на място от (а) или чието име (skel) съвпада на ≤ 60 m — отпада поименно в `excluded` („дублет на …"); `old_names` = OSM-псевдонимите (`alias`) + старото OSM име при регистров override; `status` = „бивш" при „бивше …" в името; `zone` = `place_zones.zone_of(lat, lon)`; `src` ∈ {„OSM", „Регистър на училищата (МОН/НЕИСПУО), одобрено 21.08", „Регистър на лечебните заведения (ИАМН)", „Регистър на училищата и детските заведения (Община Варна)"}; `kind` ∈ {училище, университет, болница, ДКЦ, хоспис, детска градина}. Гейт `src/qa_fire_varna_places_export.py`: count == `_meta.count`; 8 ключа точно; enum-и; кутията; 0 кадастрални; **PII**: телефон/имейл/ЕГН = 0; „д-р/доктор + фамилия" без институционална дума (ОУ/СУ/ПГ/ДГ/МГ/МУ/МБАЛ/СБАЛ/СБАГАЛ/УМБАЛ/гимназия/училище/университет/болница) = 0; уникалност (skel(name), zone) = 0 дублети; всяко изключване поименно; UTF-8 LF; лицензните редове дословно; тича и пада (нарочна повреда).
- **Д2 Fire_Varna данни:** `data/places.json` = проследеният LF-blob на varna_3d (`git show HEAD:data/fire_varna_places.json`, §14-поуката); sha/байтове се мерят при копирането и се заковават (тест + константа `PLACES2_SHA256`).
- **Д3 Клиентът (`initPlacesSearch`, само добавки):** втори товар през същия `fetchValidatedJson` (валидатор за 8-те ключа/enum-и/кутия/`_meta.count`); записите влизат в същия индекс с `kind`; **класът на записа** = `kind` (K2 важи: формите на речника „училище/училища/училището…", „университет…", „болница…", „детска градина/градина", „дкц", „хоспис"); **групи в рендера**: редовете се групират по клас в реда на най-добрия резултат във всяка група; хедър по клас: Хотели · Училища · Университети · Болници · ДКЦ · Хосписи · Детски градини (думите за подпис на Петър при Gate 2); чип по клас: хотел · училище · университет · болница · ДКЦ · хоспис · детска градина; попъп: заглавие = име (+ „(бивш)"), ред 2 = вид · зона (за хотел: вид · ★ · легла · зона), „старо име: …", **ред-източник** по `src`: OSM → `име: © OpenStreetMap contributors (ODbL) · координати: КАИС Отворени данни (АГКК)`; регистрите → `име: <регистърът дословно от src> · координати: КАИС Отворени данни (АГКК)`; nav-линковете; оранжев пин за всички класове (цветът по клас — не тази нощ).
- **Д4 Подредбата при ключ (Петър):** ако заявката носи КЛЮЧ и имаме ≥1 ред → нашият контейнер стои на мястото на адресния (top: calc(100% − 2px)), а `#addrSearchResults` се измества под нас с inline `style.top = (ours.offsetTop + ours.offsetHeight) + 'px'`; при скриване на нашия списък / без ключ → `removeAttribute('style')` на техния (никакъв остатък `style=""` — G3-байт-равенството за корпуса без ключ остава); преизчислява се при всяка мутация (наблюдателят от Д5/В6). Без ключ — както днес (адресите първи). Enter при ключ: ако нашият списък е видим за текущия текст с ≥1 ред, Enter избира нашия първи ред (ключът е изричното намерение) — освен ако адресният списък съдържа ред с ТОЧНО съвпадение на всички токени (тяхната `allTokens`-логика не е достъпна; приближение: техният първи ред без размити маркери — Сол да каже дали е измеримо отвън; иначе Enter при ключ = наш).
- **Д5 Попъпът (бъгът на Петър):** самовъзстановяване само ако ДРУГ попъп (елемент ≠ нашия) е заел слота; × затваря (C10, вече поправено отделно).
- **Д6 Недокоснати:** адресният код; `sw.js`; хотелската доставка (`data/hotels.json` остава както е — двата файла се четат независимо).

## 3 · Гейтът на интуитивните заявки (фаза 2; падне ли — правилото)

| заявка | очаквано |
|---|---|
| „училище" · „училища" · „университет" · „болница" · „детска градина" · „дкц" | категорийният списък на класа (най-близките 8 + „…и още N"), НАД адресните редове |
| „7 су" · „седмо су" · „vii су" · „7-мо су" | VII СУ „Найден Геров" първи |
| „2 оу вапцаров" · „вапцаров" | II ОУ „Никола Йонков Вапцаров" |
| „1 ег" · „i ег" | I ЕГ първи |
| „дкц 2" · „2 дкц" · „дкц св иван рилски" | II ДКЦ Св. Иван Рилски (ако е в извора; иначе ДКЦ 2 по регистъра) |
| „болница света марина" · „св марина" · „умбал" | УМБАЛ „Св. Марина" (по извора) |
| „градина 12" · „дг 12" · „детска градина 12" | ДГ №12 (по извора) |
| „икономически университет" · „ту варна" · „технически университет" · „медицински университет" · „ввму" · „вму" · „всу" | съответният университет първи |
| „хотел адмирал" | групата **Хотели** НАД адресните редове „хотел бриз/мак/…"; трите Адмирала |
| „адмирал" (без ключ) | както днес: адресите първи, Хотели отдолу |
| „ьььь" · „бл. 402 вх. 3" · „макгахан 15" | 0 наши реда; адресният контейнер байт-равен на before.json (G3) |

## 4 · Комитите

| # | репо | write-set | съобщение | гейт |
|---|---|---|---|---|
| P1 | varna_3d (rezhimi) | `src/export_fire_varna_places.py`, `src/qa_fire_varna_places_export.py`, `data/fire_varna_places.json`, този план (копие в `docs/plans/ПЛАН_фаза2_места.md`) | `места: износ за Fire_Varna — училища/университети/болници/градини + гейт` (≤72) | гейтът зелен, тича и пада; `qa_no_cad_ids` зелен; count по клас = мярката минус изключенията (поименно) |
| C11 | Fire_Varna | `data/places.json` (LF-blob), `tests/test_places_public_bundle.py`, `tests/test_places_search_primitives.py` (константата) | `data(search): publish the places bundle (schools, universities, hospitals, kindergartens) from varna_3d` | G1/G2 (тича и пада) |
| C12 | Fire_Varna | `index.html`, `scratch/places_search/probe_places_fv.mjs` (+G4 фаза 2, подредбата), `probe_out/after*.json`, `g4.json`, `recall_sweep.py` + `rows.json` (еталонът с двата файла) | `feat(search): places phase 2 — schools, universities, hospitals, kindergartens; class groups; key-first ordering` | G3 байт-равен за адресния корпус; G4 §3; G5 измерен (записва се); G6; G7; G8; G12 |
| C13 | Fire_Varna | `README.md` (ODbL ред + регистрите), тестът за README | `docs: places phase 2 sources and licence lines in README (BG+EN)` | G9 |
| C14 | — | одитор | — | всички |

## 5 · Въпросите за Петър (не блокират нощта; подразбиране в скоби)

1. Регистровите имена (19 училища МОН/НЕИСПУО, 23 места ИАМН/Община) — остават ли в живата карта без назован лиценз, с ред-източник (ДА по прецедента на 3D-то)? 2. МЦ (12) — вън тази нощ (ДА). 3. Хедърите/чиповете по клас — думите (изброените). 4. Enter при ключ = наш (ДА).

## 6 · Амандаменти v1.1 — атаката на Кими върху данните (02.09 ~19:50, `git.kimi.log`); стават таблици в износителя, поименно, с довод

- **К1 Училища (67):** ВЪН — 163 Езиков център „Ориндж хаус" (курсистки център), 192 Маестро (частна школа без тип), 147 Димчо Дебелянов (без тип/източник — условно, за борда), 311 ЦПЛР общежитие „Михаил Колони" (общежитие, не училище — въпреки REG); 79 „бивше ОУ" → `status: бивш`; ДУБЛЕТИ → един ред: 215/216 (Международното Френско училище, втори корпус в `old_names`/бележка), 280/281/282 (Професионална Техническа Гимназия — 280+282 една сграда, 281 съседна), 260/283 (ПГ КМКС „Сендов", два варианта → alias); ЗА БОРДА (не се сливат без източник): 261/262 („Кольо Фичето" ↔ ПГСАГ „Васил Левски"), 263/277 (ПГ по текстил ×2 на 4,3 km); НЕ дублети (влизат и двата): 259/356 (Екзюпери ×2 различни типове), 238/239 (Арабаджиев + филиал), 240↔268 (Славейков и III ПМГ в една сграда); 358 ЧОУ „Феникс 2020" ВЛИЗА (училище, не ДГ); 219 Морска гимназия → ПРЕЧИПВАНЕ училище (МОН №46). Останалите 47 ВЛИЗАТ (84 само като `old_name` на регистровия ред 226).
- **К2 Университети (20):** ВЪН — 363 (правило У6), 364–369 (`osm:building`: Лекционна зала, Нов Учебен Корпус ×2, Пазвантницата ×2, Топла връзка), 325 Факултет по фармация (факултет на МУ), 78 База БМКЦ, 174 Институт по океанология (БАН), 272 Полицейска академия (условно — без регистров довод); 86/88 ВУМ ↔ Висше училище по мениджмънт → един ред (86), 88 в `old_names` (старо официално име); 173 ИУ 2-ри корпус ВЛИЗА (реален корпус). ВЛИЗАТ: 85 ВСУ, 87 ВВМУ, 172 ИУ, 173, 212 МУ, 323 ТУ, 86 ВУМ.
- **К3 Болници (27):** ПРЕЧИПВАНЕ → ДКЦ: 30 (II ДКЦ), 31 (IV ДКЦ — **ФЛАГ**: пинът е на ~2 km от регистровото ДКЦ 4, places №210 — влиза с флаг „локация непотвърдена" или ВЪН; решение: ВЪН до борда), 63, 150, 152, 148 (Диспансер — **ФЛАГ**: 3 m от ДГ №52, различни институции, не се гасят взаимно); ПРЕЧИПВАНЕ → хоспис: 336; ВЪН: 209 Медик Дент ЕООД, 326 ФАМИЛИЯ ДЕНТ ЕООД (дентални), 222 МЦ Нова, 223 МЦ Оксигена, 233 Олимед (МЦ), 284 Психиатрична клиника към УМБАЛ (звено); ЗА БОРДА: 255/256 очни клиники (без регистър — ВЪН до борда); 208/324 МБАЛ/УМБАЛ „Св. Марина" → дублети на places №205 → един регистров ред, OSM-имената в `old_names`. ВЛИЗАТ: 26, 65, 90, 206, 207, 211, 234, 296, 298, 335 (+ регистровите).
- **К4 Детски градини (60):** ВЪН — 353, 354, 357, 360 (частни без номер, домашен-адрес профил), 180, 304, 321 (точка без сграда), 338, 343, 344, 362 (без тип/номер, условно), 139, 170 (занимални), 153 (дневен център), 346 (Карин дом — терапевтичен център); ДУБЛЕТИ → един ред: 117/118 (ДГ 32 „Моряче"), 1/142 (ДЯ №11 „Иглика", артефакт на override); 359 ЧСУ Монтесори (REG, борд 21.08) ВЛИЗА като училище. Останалите ~42 номерирани/публични ВЛИЗАТ (345, 127 като филиали).
- **К5 Регистър ↔ OSM (15 двойки, регистърът води, OSM-името в `old_names`):** 199↔206, 200↔207, 201↔296, 204↔335, 205↔208+324, 206↔298, 207↔150, 208↔63, 209↔152, 224↔336, 226↔84, 227↔278, 228↔5, 230↔341 (**ФЛАГ**: ДГ №26 „Изворче" vs OSM „ЦДГ 7 Изворче" — номерът спори, регистърът води, бележка), 231↔124. Не дублети (не се гасят): 201↔325, 202/203↔207, 204↔233, 223↔326; №232 ДГ №52 ↔ 148 (различни институции); №210 ДКЦ 4 без OSM двойник (влиза сам); без двойник: 225, 229, 233.
- **К6 Псевдонимите:** факт са само тези с източник в данните — `data/place_aliases.json` (речникът, одобрен 21.08: официални съкращения като ТУ, ВВМУ, ДКС…) влиза в `old_names` на съответния ред (източник: речникът); „вторица", „морската", „окръжна" — прякори, ВЪН; „7-мо СУ"/„3-та ПМГ"/„1-ва ЕГ" не са псевдоними — токенизаторът ги покрива (Т1).
- **К7 PII-гейтът:** списъкът на институционалните думи += СУУНЗ, ПГИ, ПГ*, ЧОУ, ЧСУ, ЧДГ, ЦДГ, ОДЗ, ДЯ, НУ, ВТГ, ПМГ, ЕГ, СОУ, СУ, АМЦСМП, СБАЛ*, УСБ*, МЦ, ДКЦ, хоспис, диспансер, клиника, академия, институт, гимназия, училище, университет, болница, градина, ясла — и съвпадението е ПРЕФИКСНО по дума (не цяла дума); гейтът е втори ред зад класовите правила (денталните/МЦ ги хващат К3, не PII-регексът).
- **К8 Лицензът (ODbL §4.4/§4.5–4.6 по памет; сверява се с пълния текст преди push):** показването в попъп е Produced Work → атрибуцията стига; **самият bundle е Derivative Database** (систематична извадка) → **share-alike**: `data/places.json` се публикува под ODbL 1.0 с бележката дословно; регистровите редове в него са „отделни факти" (чл. 4 ЗАПСП — същият довод като НТР) и могат да стоят в ODbL-база; README го казва; **решение на Петър преди push** (в §5 в-1). Един файл, две лицензни бележки в `_meta`, `src` на всеки ред.

## 7 · Амандаменти v1.2 — атаката на Сол върху механиката (02.09 ~19:55, `git.gpt.log`, HEAD 46987c2); **при противоречие §7 има предимство пред §2 Д3–Д5 и §16 на search-плана**

- **С1 Подредбата при ключ (Д4′).** `search()` връща и `hasKey`. Редът на операциите е фиксиран и некомутативен: (1) `theirs.removeAttribute('style')`; (2) нашият контейнер на CSS-позицията си; (3) само ако `hasKey && rows.length` → `theirs.style.top = (box.offsetTop + box.offsetHeight) + 'px'` и `theirs.style.maxHeight = (innerHeight − theirs.offsetTop − 12) + 'px'` (иначе долният списък е недостижим на 375×812); нашият `maxHeight` при ключ = `min(32vh, …)`, за да остане място. **Остатъкът:** при ВСЕКИ `input` (нашият слушател е синхронен, преди техния 120-ms debounce) — ако новият текст няма ключ или е < MIN_Q → `theirs.removeAttribute('style')` ВЕДНАГА (преди първата им мутация; G3 гледа първата мутация); `hideMine()` също маха атрибута; никога `style.top = ''` (сериализира се като `style=""`). Наблюдателят следи и `attributes: ['style']` върху техния контейнер (само за да не остане чужд остатък) и преизчислява при промяна на нашата височина (ResizeObserver върху нашия контейнер). `tidyTheirs()` действа само ако `shownFor === value.trim()` (stale-списък не гаси новото „Няма съвпадения"). При ключ и 0 наши реда → без стил, техният на мястото си.
- **С2 Enter при ключ (Д4″).** Собственикът се решава ДЕТЕРМИНИСТИЧНО, не по тайминг: наш `keydown` слушател с `{capture: true}` върху `#addrSearchInput` (при target-фаза capture-слушателите тичат ПРЕДИ bubble-регистрираните — Chrome ≥ 89); условие: `isTrusted && key === 'Enter' && hasKey && shownFor === value.trim() && rows.length` → `ev.preventDefault(); ev.stopImmediatePropagation(); pick(rows[0])` — техният handler не тръгва изобщо; техният код е непипнат. Във всеки друг случай не пипаме събитието (адресът е собственик, както днес; Д9′ остава за случая „техен статус Няма съвпадения"). Пробата: „хотел шипка" + Enter (топъл и задържан индекс) → един `anchorHydrantsAt`, един попъп (нашият), техният контейнер скрит без стил.
- **С3 Вторият товар (Д3′).** Валидатор ПО ФАЙЛ (`validateHotels` за 12-те ключа/`hotels`, `validatePlaces2` за 8-те/`places`); `ensurePlaces` = `Promise.allSettled` над трите; индексът се строи от това, което е валидно: хотели без места, места без хотели; **речникът е незадължителен** (без него: без ключове, маркерното правило). SHA-fallback по файл. Кешът: `cachePut` се await-ва (в race с 2 s), но отказът му не гаси товара. **Бъг, намерен от Сол — прагът ≤300 (Б2) се прилага върху целия `RECS`, не по клас** → поправя се: щедростта без ключ се решава по размера на КЛАСА на записа. Попъпът за не-хотел не чете `cat`/`beds` (guard по наличие). **Групите:** `KIND_GROUP` таблица: четирите хотелски `kind` → група „Хотели"/чип „хотел"; училище/университет/болница/ДКЦ/хоспис/детска градина → своите; в рендера: групите в реда на най-добрия им ред, **по 8 реда на група, общо ≤16, „…и още N" по група**; `shown` = точно DOM-редът (data-idx подравнен по групите — Сол: иначе кликът избира друг запис).
- **С4 Попъпът (Ж1′).** Самовъзстановяването ОТПАДА (Leaflet няма признак „затворен от човек"; хидрантен попъп и × върху заменен попъп го подвеждат). Замяна, тясна: наблюдател на popup-pane-а — ако СЛЕД наш избор (`mineActive`) и БЕЗ междувременен техен пин се появи `.leaflet-popup` с `.search-popup` вътре (техният закъснял fail-soft `openSearchPopup`), той се затваря с `map.closePopup()` и нашият се отваря наново; хидрантни попъпи и всичко друго — недокоснати. × затваря, Escape затваря, клик по картата затваря — както Leaflet.
- **С5 Пробата (G3/G4 +):** реални кликове по координати (Input.dispatchMouseEvent), не `element.click()`; геометрия на двата pane-а; корпус: „хотел" и „училище"/„болница"/„детска градина" на 375×812 (скрол и tap върху последния адресен ред); „хотел шипка" + Enter (топъл/задържан); „хотел адмирал → адмирал" без празно поле (първата адресна мутация без `style`); „училище вапцаров → вапцаров", „болница света марина → св марина"; „хотел адмирал → ьььь" (новото „Няма съвпадения" остава); „хотел 999999" (ключ, 0 наши реда → техният без стил); keyed → Escape → „чайка" и keyed → tap извън → „виница" (без остатък); задържан `search_index.json` / задържан `places.json`; „свети"/„марина" (две групи, клик върху ред от втората); хидрант при избран хотел (хидрантният попъп остава); × върху заменен попъп. G3-корпусът (9 без ключ, след празно поле) остава байт-равен.

## 8 · Амандамент v1.3 — кварталът/районът на всяко място (Петър, 02.09 ~20:20: „за всички детски градини и училища, болници и хотели да е включен и кварталът/районът")

**Мярката (02.09 ~20:25):** P1 изнесе зоните през старата `zone_of` → 110/135 места „гр. Варна" (безполезно). Кварталният слой на 3D-то (`quarters_layer.geojson`) дава предимно РАЙОНИ; оградите от КАИС парцелите (`Varna_buildings/output/quarname_enclosures.geojson`, 93 имена) покриват 25/135 места и 106/226 хотела (центърът няма квартал в кадастъра). **Но `data/places.json` (поколение 31.08, след кварталния цикъл на 3D-то, Gate 1b затворен) вече носи НОВАТА стълба** `zone_label()` (курорт → КАИС-кварталът на закотвената сграда → чертаната обвивка → некурортна кутия → районът): хотелите там са к.к. Златни пясъци 85 · к.к. Чайка 37 · к.к. Св. Константин 37 · район Одесос 22 · Морска градина 18 · район Приморски 4 · кв. Галата 2 · кв. Аспарухово 2 · ж.к. Владислав Варненчик 2 · Западна пром. зона, к.з. Прибой, с.о. Зеленика, кв. Чайка, кв. Виница, ж.к. Възраждане, м-т Шашкъна, кв. Изгрев, кв. Свети Никола по 1. Доставката към Fire_Varna е от 23.08 — ПРЕДИ това.

**Решението (по думата на Петър; „не измисляме" — стълбата е подписана на Gate 1b, центърът остава честно на района):**
- **P1b (varna_3d):** (а) `export_fire_varna_places.py`: зоната на регистровите редове = `zone` от `places.json`; на OSM-редовете = `zone_label(lat, lon, kais_quar=quar на сградата i)` (`web/varna_buildings_info.json`, колона `quar` през `dict`) — стъпката се брои и се записва в `_meta.by_zone_step`; (б) **прегенериране на хотелите**: `python src/export_fire_varna_hotels.py` (чете `zone` от places.json 31.08) → нов LF-blob; `qa_fire_varna_export.py` зелен; Counter на зоните преди/след в тялото на комита; (в) `qa_fire_varna_places_export.py` зелен; комит на rezhimi с двата данни файла + износителя.
- **C11′ (Fire_Varna):** `data/hotels.json` и `data/places.json` = новите проследени blob-ове (OID-идентичност); `tests/test_hotels_public_bundle.py` презакован (sha/bytes/gzip; зоновият enum от 10 стойности → „непразен низ", защото зоните вече са квартали — 20+ стойности; KINDS/STATUS/SRC остават); `tests/test_places_public_bundle.py`; **`index.html` — само константата `HOTELS_SHA256`** (иначе G12г е червен до C12); клиентският валидатор проверява `zone` само като непразен низ — без промяна.
- **C12′:** както §7 + `PLACES2_SHA256`; еталонът се регенерира с новите зони („хотел чайка" → к.к. Чайка + кв. Чайка; „хотел златни" → 85).
- Показът: редът-meta и попъпът вече показват `zone` — „кв. Чайка", „район Одесос", „ж.к. Възраждане"; при район без квартал — районът (както решението от Gate 1b). Ключ + квартал („училище чайка", „хотел одесос") = филтриран списък по А3.

## 9 · Амандамент v1.4 — C12 спря на гейта §3 (65/73; 02.09 ~22:05); правилото се поправя, фактите се поправят, едно очакване отпада

- **П6 (над А5 — правилото се поправя, както иска гейтът):** без ключ дву-знаков токен, който съвпада ТОЧНО с именен токен на записа, става значещ, когато заявката носи поне още един токен, който също съвпада точно със същия запис (числото е конюнктивно по П3). Така „7 су"/„седмо су"/„vii су"/„7-мо су" → VII СУ „Найден Геров"; „1 ег"/„i ег" → I ЕГ (и I ЕГ става намираем по собственото си име); „су"/„ег" сами → 0 както досега; „бл 402"/„вх 3" → 0 (адресните маркери никога не са значещи — А5). Измерено от изпълнителя: 6 от 8-те падащи реда висят точно на това.
- **„вапцаров" — факт, не правило:** в данните са ДВЕ институции с това име (II ОУ „Никола Йонков Вапцаров" и ВВМУ „Н. Й. Вапцаров"); и двете съвпадат точно; редът между тях е по покритието на името (П2). Очакването става: двата реда в първите два (редът им — по П2); „2 оу вапцаров" → училището първо (проверено).
- **„ту варна" — отпада от гейта:** псевдонимът „ТУ" стои в `rejected` на одобрения речник `varna_3d/data/place_aliases.json` (правилото на Петър от 21.08: само официални съкращения; решение за него — в 3D-то, не тук); „технически университет" и „технически" намират ТУ-Варна. Същото за „ИУ"/„МУ"/„ВСУ"/„ВУМ" — влизат само ако са в `aliases` (7 реда паднаха върху нашите записи по К6).
- **Хотелът „ГРАДИНА":** с фаза 2 „градина" е ключ (класът детски градини е населен) → хотелът излиза с „хотел градина"; отбелязва се в доклада (А1 работи както е подписано).
- Историята на комитите: C13 (README, ba1919b) е комитнат ПРЕДИ C12 заради дефект в скрипта на архитекта (текст в полето за комит); нищо не се пренаписва — C12 влиза след C13.

## 10 · Амандамент v1.5 — „владиславово детска градина" → 1 (Петър, 03.09 ~00:00: „само една детска градина ли има във Владиславово? мисля че са повече"); мярката, правилото П7, лотът Д5, присъдата на C14

**Мярката (03.09 ~00:10; командите: `python -c` върху `data/places.json`, `data/hotels.json`, `Varna_buildings/config/quarter_registry.json`, `varna_3d/web/varna_buildings_info.json` + `_3d.geojson`, `varna_3d/scratch/refactor/_addr/kimi_obrazovanie.txt`):**

- **Доставката** носи в семейството „Владиславово" 9 записа: 4 училища (І ОУ „Св. княз Борис I", ОУ „Патриарх Евтимий", ОУ „Стоян Михайловски", СУ „П. К. Яворов"), ДКЦ 3, **2 детски градини** — ДГ 39 „Приказка" (зона „ж.к. Владислав Варненчик") и ОДЗ „Маргаритка" (зона „кв. Владиславово") — и 2 хотела. Двете зони са ЕДНО семейство в регистъра (`vladislavovo`: display „кв. Владиславово", 11 псевдонима, вкл. „ж.к. Владислав Варненчик"); стълбата от Gate 1b нарочно НЕ преименува КАИС-квартала на сградата, затова двете изписвания са легитимни — но търсачката ги брои за различни.
- **Механизмът на „1":** А3′ филтрира по ТОЧНО членство на токена в `zkset` (зона + вид). „владиславово" ∈ zkset само на Маргаритка; „владислав" — само на Приказка. Оттук едната. Не е грешка в данните, а липса на псевдонимите на квартала в индекса.
- **Изворите за градините във Владиславово (Петър е прав — повече са):** OSM знае 2 (Приказка i=18482, Маргаритка i=18342). КАИС (`func` = „Сграда за детско заведение", quar Владислав*) — 12 сгради = **4 парцела** (80 m): 43.24947,27.84419 (3 сгр., без име в изворите ни) · 43.24480,27.84587 (5 сгр., без име) · 43.24798,27.85094 (Маргаритка) · 43.23869,27.86035 (КАИС адрес „ЦДГ 10 - ПРИКАЗКА", OSM „ДГ 39" — номерът спори; регистърът: №39). Градски: 214 такива сгради, 129 без quar. **Общинският регистър** (извадката 19.08, „всички 53 общински ДГ с адреси и RAJON_ID"; район 6 = Владислав Варненчик): ДГ№37 „Пламъче" (до бл. 402) · ДГ№38 „Маргаритка" (до бл. 20) · ДГ№39 „Приказка" (до бл. 208; филиали до бл. 209/210) · ДГ№41 „Първи юни" (до бл. 309) · ДГ№42 „Българче" (до бл. 21); ДЯ №6 „Мечо Пух" (ул. „Ниш" 29); в стария списък и ОДЗ 11 „Детски свят" (ул. „6-ти септември" до бл. 9). Бордът от 21.08 одобри само 6 регистрови ДГ-двойника (№17/23/26/44/52 + ЧДГ „Приятели"); останалите общински градини не са минавали през канала на местата. → **Две дупки, различни по природа:** (а) търсачката не знае, че „Владиславово" и „Владислав Варненчик" са едно (правило); (б) доставката има 2 от поне 5 общински градини в квартала (данни, лот Д5).

**П7 — кварталните псевдоними в зоновите токени (правило; малко, гейтвано, речникът остава извор):**
- Речникът `data/place_categories.json` (varna_3d `src/build_place_categories.py`) получава ключ `zones`: `{ <зона дословно както е в доставката>: [псевдоними] }`, където псевдонимите са display + `aliases` на регистровия запис на семейството (`Varna_buildings/config/quarter_registry.json`), без самата зона — САМО за зоновите стойности, налични в двата данни файла (35 стойности: 21 са display, 6 са alias на семейство, **8 без запис** — „район Одесос", „к.к. Св. Константин", „район Приморски", „Виница/север", „Изгрев 1", „Аспарухово/Галата", „к.з. Прибой" и „м. Св. Никола"-варианти извън списъка — остават без псевдоними и се изброяват в `_meta.zones_unmatched`; нищо не се съчинява). Съвпадението е по низ (display или alias), не по прилика.
- Клиентът (`buildIndex`): токените на `cats.zones[h.zone]` през `placeTokens`, **минус числата, минус ≤2-знаковите, минус адресните маркери** (както А6 за старите имена — иначе „II м.р." вкарва числото 2 в zkset и П3 го брои за покрито), влизат САМО в `zkset` (зона-точно изяжда токена — А4; филтърът А3′) — НЕ в `ntk`/`nset` (качеството и покритието на името, П2, са непроменени). Речникът остава незадължителен (С3): без него — както днес. Бюджет: ≤15 реда JS.
- Референцията `scratch/places_search/recall_sweep.py` — същото правило; редовете се регенерират; JS = Python по ред (М5).
- **Гейт (добавка към §3):** „владиславово детска градина" → 2 (Маргаритка, Приказка) · „детска градина владислав варненчик" → 2 · „владиславово училище" → 4 · „хотел владиславово" → 2 · „дкц владиславово" → ДКЦ 3 · „хотел свети никола" → трите изписвания на м-т Свети Никола в един списък · „хотел зеленика" → с.о. + м. Зеленика · „изгрев болница"/„училище изгрев" непроменени; контролите: §3 72/72 остават, „хотел чайка" непроменен (к.к. Чайка и кв. Чайка са два регистрови записа), G3-корпусът байт-равен (адресният код непипнат), „2 оу"/„1 ег" както в §9 (числата не влизат в псевдонимите).
- **Комити:** P1c (varna_3d, rezhimi): `build_place_categories.py` + `data/place_categories.json` + QA-проверка (всяка зона от доставката или има запис, или е в `zones_unmatched`; всеки псевдоним е дословен низ от регистъра). C16 (Fire_Varna): `data/place_categories.json` (проследеният LF-blob, OID-идентичност) + `CATS_SHA256` + `buildIndex` + `recall_sweep.py` (плюс `if __name__ == "__main__"` и валидиран argv — находка №3 на C14) + `recall_sweep_rows.json` + `tests/test_places_search_primitives.py` (sha пин) + G4-проба за новите заявки. Изисква Gate 1 (Петър) — само П7; Д5 е отделен подпис.

**Лот Д5 — регистровите детски градини върху КАИС-парцелите (за отделен план и подпис; не тази нощ):** 53-те общински ДГ (+ яслите) от извадката 19.08 → адресът „до бл. N"/улица → през адресния индекс на Varna_buildings (бл. 402 е в `search_index`) → най-близката КАИС сграда с `func` = „Сграда за детско заведение" в ≤150 m → място по канала на местата (регистърът води, К5; OSM-двойникът става старо име). Мярката преди плана: колко от 53-те падат еднозначно (една ДГ-сграда в радиуса), колко спорят (номер 10 срещу 39), колко нямат КАИС-сграда с това предназначение. Фикс-извадката за окото на Петър (протоколът 5–6 места): Владиславово №37/38/39/41/42 + ДЯ №6. Кими върху данните, преди изпълнение.

**C14 (03.09 ~00:05): ГОДНО С УСЛОВИЯ** — как се затварят: (1) плановете v1.5/§16 в git → C15 (този комит); (2) `aria-label="Хотели"` → решение на Петър (§5 т.5; един ред); (3) преходът „хотел адмирал → адмирал" с редактиране не е гейтван (пробата вкарва празно поле) → проверка на живо + сценарий без `clearField` в C16; (4) „липсва бъндъл за C10" — **невярно**: `Fire_Varna_C10_46987c2.bundle` е в `plan_2026-09-01_files/backup/` от 19:17; `git fsck` дава 48 „invalid reflog entry" от сутрешния ремонт на пакета, `--no-reflogs` е чист → гейтът се чете с `--no-reflogs`; reflog не се чисти без думата на Петър; (5) `recall_sweep.py` презаписва еталона при импорт (fail-silent) → в C16; (6) PII-гейтът пропуска стационарен телефон (9 цифри) и „д-р Мутафов" (префикс „Му") → тестът се разширява в C16 (регекс за 0XX/XXX XXX; префиксът става цяла дума), доставката е чиста (0/0/0 по собствения скан на одитора); (7) съобщението на C11 ≠ §4 (write-set-ът се смени с §8) — записано, не се пренаписва; (8) „градина 12"/„дг 12"/„детска градина 12" → 0 по извора (няма №12 в нито един извор) — очакването е в `g4.json`, тук се записва като факт.

*Planner (Fable), 03.09.2026 — v1.5 (§10). Изпълнение: C15 (документите) → Gate 1 за П7 → P1c/C16 → C14′; Д5 = отделен план.*
