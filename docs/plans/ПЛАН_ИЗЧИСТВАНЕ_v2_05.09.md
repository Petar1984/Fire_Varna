# ПЛАН „ИЗЧИСТВАНЕ“ v2 — за подпис от Петър (05.09.2026, 02:40)

Заменя `ПЛАН_ИЗЧИСТВАНЕ_05.09.md` (v1 = анализът + атаките на Кими К1–К8 и Astra S1–S7, всичките приети). Тук са само стъпките. Диагнозата (§1 на v1) стои: два корена — **даннов** (няма ключ за едно място; истината на 9 места; подписите се разминават) и **процесен** (червеното нямаше авторитет; планове по-бързи от мерките; растящ обхват).

## 0 · Правилата, които важат от този момент
1. **Един лот в движение.** Нищо ново не се отваря, докато текущият не е пушнат. Идеи → `docs/plans/PARKED.md`, един ред.
2. **Амандамент = нов датиран файл**, старият получава статус в заглавието. Никакви „§3й-б“ в тялото на подписан план.
3. **План, изпълнение, одит = три различни агента.** Всеки план/амандамент минава през Кими и Astra преди изпълнение.
4. **Червено = блокирано.** Куката pre-push пуска unittest + release-гейта; break-glass се премахва (или остава само с команда, пускана от Петър — той решава, т. Q7). „Червено по замисъл“ не съществува: очакване, което чака подпис, е ред в опашката, не червен тест.
5. **Подписва само Петър, с ръката си.** Изпълнителите пишат само `pending`; „да/не“ ги прилага `python -m gates.sign <id> да|не`, която Петър пуска сам, в негов комит; агентите комитват с отделен автор (`Claude Executor`); гейт проверява авторството на всеки `signed` ред. Без ръката на Петър бариерата е процесна — записано.
6. **Една опашка** (`scratch/places_search/ЗА_ПОДПИС_<дата>.md`), ред = `id · питане · текущо → предложено · котва · извор (цитат) · координати/разстояние · последица от ДА · последица от НЕ`. „не“ = терминално „отказано“; `pending` брои само неотговорените. **Три класа редове, три артефакта:** release-разрешение (важи за една доставка), трайен факт (по `pid`, след Фаза C), baseline-подпис.
7. **Мярка преди план; гейт, който е падал на счупен вход, преди данни; дефиниция за „готово“ на всеки лот.**

## A · Доставката СЕГА — F12 под сегашната схема 13/17 (Fire_Varna; 1 кръг изпълнение + одит; докосване №1 на Петър)
- **A.1** F12-а..г както са в `ПЛАН_ЛОТ0_гейтове_P7_F12.md` + Амандамент №1 D, №2 т. 5–6: blob-овете от P7 (LF от blob-а), SHA, cache v6, `'mladost'` в затворените списъци, М7 клон (`bare_location_rows`, само quarter/locality токени), два report-only манифеста (BASE→P7 данни; P7→F12 само М7) с ВСИЧКИ редове, `qa_fire_varna_export.py:230` научава М2 (varna_3d, един ред — единственият комит там в тази фаза).
- **A.2 Release-гейтът** (`gates/release.py`, вика се от `run_gates` като проверка 6 и от куката): връзва digest-ите на историческата референция, кандидата, входовете и двата манифеста; всяка точна делта в кофа иска подписан ред от опашката по id; сравнява пълни подредени резултати, етикети, клонове, бройки. Инжекции (липсващ подпис, застоял хеш, неразрешена промяна на кофа, махнат/пренареден резултат, съгласувана мутация двигател + референция) → блокира. Куката пуска `python -m unittest discover -s tests` + `python -m gates.run_gates` (с проверка 6). Тестовете „чакат подпис“ се прекласифицират ЕДИН ПО ЕДИН: алгоритмичните твърдения остават тестове; готовността на доставката отива в release-гейта; фикстура „неподписан манифест → BLOCKED“ е тест.
- **A.3 Опашка ЗА_ПОДПИС_A.md (release-разрешения за ТАЗИ доставка):** R1 allow-файл: 18 „Морска градина“ + 16 реда P7-делта (по схемата на реда, с координати и цитат); R2 „подписвам baseline f06ac06“; R3 манифест BASE→P7; R4 манифест P7→F12 + думите на М7 („зпз“ = locality); R5 8-те сблъсъка — ТУК само като „остават pending и картата показва район X“ (потвърждение), трайното решение е във Фаза D; R6 кодът `mladost`; Q7 break-glass: вън или само с ръката на Петър.
- **A.4** Петър пуска `gates.sign` за R1–R6/Q7 → прилагане → `--freeze` ЕДИН път (през същото разрешение) → unittest 0 червени → `run_gates` 0 червени/0 жълти (baseline подписан, allow подписан, pending 8 = жълто? НЕ: pending се брои в опашката, G-M6 код 3 остава ЧЕРВЕН за публикация, докато Фаза D не затвори 8-те — ИЛИ Петър решава 8-те още сега в A.3 като release-решения по `(файл, име)`, знаейки, че Фаза D ги преподписва по `pid`) → одит → **докосване №2: `git push`** → **проверка след пуша:** SHA на публикуваните blob-ове = локалните, кешът v6, Адмирал на Златни пясъци, „златни“ вади хотелите.
- Готово-гейт на A: живата карта без регресията; unittest 0 червени; run_gates 0 червени; двата манифеста подписани; PARKED.md съществува; `activeContext.md` казва състоянието.

## B · ADR 009 ПРЕДИ кода (документ; докосване №3)
„Идентичност, подписи, гейтове“: `pid` (UUID4, монтиран веднъж, записан в `place_identity.json`) + публична котва `anchor: {src: UIN|REG, id}` в blob-а (14/18 ключа); един файл за подписите (`Fire_Varna/data/signed_facts.json` v2: `pid`, роля `assert|override`, `status pending|signed`, кодове, цитат-доказателство вътре), кръстосаното четене varna_3d → Fire_Varna по път + blob SHA при билд (пин = HEAD на файла, не константа); три артефакта (факти / release-разрешения / baseline); куката (unittest + release); „червено = блокирано“; правилата от §0. Кими + Astra оборват ADR-а; Петър го приема.

## C · Идентичността (varna_3d, 3 комита + миграционен гейт)
- **C.1 Ключ без зона + биекция.** Замразява се старият `place_identity.json` (SHA); новият ключ: УИН → регистров ред → КАИС тяло (в рамките на файла) → `файл|скелет`; **биекция ред → UUID** доказана: 375 активни + 8 пенсионирани UUID запазени, всяко присвояване запазено (не само бройка), членство в площадките запазено; 4-те групи с еднакво име (АДМИРАЛ, ПЕРЛА, РОЯЛ, РУСАЛКА) разрешени само по УИН/регистров ред, никога по ординал/координата; двусмислие = STOP с явно съответствие. Отделен гейт `qa_place_identity_migration.py` (0 fresh, 0 retired, 0 missing, 0 orphans през два билда; фикстури: дублирана котва, разменени UUID, липсващо доказателство → червено; смяна на зона/ред/координата → същото присвояване). `qa_place_sites` остава.
- **C.2 `pid` + котва в публичния ред** атомно: износи, QA схеми на износа, `EXPECT_KEYS`/`EXPECT2_KEYS` (14/18), валидатори, `run_gates.ROW_FILES`, `legacy_bundle_sha` връзки, референцията (идентичност на реда `pid`; миграция на 2 121 срещания през пиннатите исторически двойки → записи → UUID; махането на новите полета възпроизвежда старата структура), пробата, cache v7, тестовете с бройки. Гейтове: валиден уникален UUID4 на всеки ред; повреден/дублиран/кадастрално-подобен `pid` → червено; `qa_no_cad_ids` тича и пада на нарочно вкаран ПИ.
- **C.3 Метаданни без часовник** — два изолирани билда по различно време → еднакви нормализирани байтове.

## D · Един файл за подписите + трайните факти (Fire_Varna + varna_3d)
- **D.1** `signed_facts.json` v2 по ADR 009; миграция: 11 курортни override (по `pid`), Адмирал, Вихрен (решение в опашката D), 8-те сблъсъка (трайно решение: НТР или КАИС за всеки, по `pid`), кандидатите Гео Милев/Иван Рилски.
- **D.2** `fire_varna_locations.py`: `SIGNED_QUARTER` изведен от файла (път + blob SHA при билд, четене веднъж, строг парс); литерали 0 (grep-гейт); ledger записва id на факт + digest на поле; манифестът връзва линията с изнесените байтове; **точно равенство факти ↔ ledger ↔ изнесени `SIGNED_OVERRIDE`** (двупосочен гейт); assert-ите съдят.
- **D.3** `run_gates` проверка 5 по `pid` + роля + подпис + авторство на комита; tracked allow-файлове с blob SHA в таблицата; minor-ите от одита на F0-ж.
- Опашка ЗА_ПОДПИС_D.md: 8 сблъсъка трайно, Вихрен, 12 факта, Гео Милев/Иван Рилски (ако има свидетел).

## E · Редът
Статуси в заглавията на всички планове (изпълнен / заменен / паркиран); `ИДЕЯ_златен_списък` → оборена като изход, §10 изпълнен; OSM-hints и ЛОТ 1г → PARKED; briefs/отчети → `scratch/places_search/2026-09_архив/` + индекс; `activeContext.md` = състоянието; `AGENTS.md`/`CLAUDE.md` на двете репа получават §0; памет.

## F · Готово (изпълними проверки)
`python -m unittest discover -s tests` → 0 червени · `python -m gates.run_gates` → 0 червени/0 жълти през инсталираната кука (инжекции блокират) · `qa_place_identity_migration` зелен · G-ISO 1–4 зелени · G-M6 код 0 · grep `SIGNED_QUARTER = {` → 0 · всеки публичен ред с валиден `pid` + котва · два билда = еднакви байтове · `origin/main == main` (Fire_Varna) и решение за `rezhimi` записано · публикуваните хешове = локалните.

## G · Докосванията на Петър (честно: минимум пет, не три)
1. Подпис на v2 (този файл). 2. `gates.sign` на опашката A + push на A + проверка на живата карта. 3. Приемане на ADR 009. 4. `gates.sign` на опашката D + push на C/D. 5. Приемане на реда (E). Всяка промяна на кандидат-байтове след подпис = нов преглед.

## H · Времена
A: до опашката ~3 ч изпълнение + одит; след подписа ~2 ч до push. B: 1 ч + Кими/Astra. C: 4–5 ч. D: 3 ч. E: 1 ден. Общо ~3 работни дни при 1 лот в движение.
