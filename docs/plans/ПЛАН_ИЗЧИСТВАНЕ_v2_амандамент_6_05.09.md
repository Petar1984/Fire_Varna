# Амандамент №6 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 10:20) — след одита на A.2-5 (НЕГОДНО: 2 major, 4 minor); всичко се приема; отговорите на изпълнителя

A.2-5 (e94aac8) е доставен: замразяването чете опашката, отказът преживява, `sign` пише последно, проверка 7 сверява тялото, паритетът се пише последен, release чете HEAD blob; решаващият експеримент повторен (271 теста, 0 червени, всички 7 гейта зелени в изолиран клон). Одиторът намери два начина да се замрази неправомерно. Решения → **A.2-6** (един комит, Fire_Varna; write-set: `gates/release.py`, `gates/sign.py`, `gates/run_gates.py`, `scratch/places_search/recall_sweep.py`, `tests/test_places_search_gate.py`):

## Major
1. **`--freeze` чете само `uncovered`/`refused`, не присъдата на release-гейта** → замразява при BLOCKED (напр. работно дърво ≠ HEAD blob на подписан артефакт) с изход 0. → блокер при `exit_code != 0` с ВСИЧКИ `blocked` редове на гейта; замразяване САМО при зелен release. Счупен вход: нехомогенен подписан артефакт в работното дърво → `--freeze` ≠ 0, нищо не е написано. Плюс: при липсващ blob на референцията release връща BLOCKED с празни списъци → пак трябва да блокира.
2. **Терминалното „не“ се заглушава от по-ранен ред „да“ с `кофа/*`** — присъдата зависи от реда във файла. → за всяка делта се събират ВСИЧКИ съвпадащи редове; „не“ печели винаги; по-специфичен шаблон бие `кофа/*`; тест с двата реда в двете подредби. Правило за опашката (и валидатор в `gates.sign`): **ред с „не“ носи точна заявка (`покрива: кофа/точната заявка`), никога `кофа/*`.**

## Minor
3. Проверка 7: авторството на опашката се проверява ПРЕДИ да се четат `row_digests`; дайджест от опашка с последен агентски комит не се приема и редът не казва „Петър записа“.
4. Изведеният от байтовете отказ се мери спрямо референцията, срещу която е писана ОПАШКАТА (`_meta.reference` на подписаните очаквания), не спрямо f06ac06.
5. Фикстурата на `RefusalSurvivesTheFreezeTest` → `tempfile.TemporaryDirectory()`.
6. `test_etr_kmz_adapter.DeterminismTest` мига под натоварване (1 от 5 прогона) — извън write-set-а; влиза в PARKED като известен дълг за Фаза E (вероятно секундна граница в zip timestamp).

## Отговори на изпълнителя
- **Кой комитва замразяването:** Петър. `--freeze` се пуска от изпълнителя (единственият разрешен път), но комитът на замразените тела го прави Петър с командата, която изпълнителят отпечатва — така подписаното тяло е в комит на Petar1984 и проверка 7 е ✓ без дописване на дайджести. Влиза в „следващите стъпки“ на опашка A.
- **Широк шаблон при „не“:** не се допуска (т. 2) — „не“ само по точна заявка.
- **„не“ е терминално; смяна = ръчна редакция + нов кръг:** ДА.
- **Разчитането от амандамент №5 т. 7 остава:** ДА.

## Ред
A.2-6 → одит (двете подредби на опашката; freeze при BLOCKED; повторен решаващ експеримент) → A.3 опашката → Петър: `gates.sign` + неговият комит → изпълнител: `--freeze` → Петър: комит на замразяването → unittest 0 червени, run_gates 7/7 → одит → push (Петър) → проверка на публикуваните хешове → локалната карта с чисти данни.
