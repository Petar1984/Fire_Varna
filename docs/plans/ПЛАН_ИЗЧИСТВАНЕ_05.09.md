# ПЛАН „ИЗЧИСТВАНЕ“ v1 — СТАТУС: анализ + атаки; стъпките са ЗАМЕНЕНИ от `ПЛАН_ИЗЧИСТВАНЕ_v2_05.09.md` (05.09, 02:40). Оригинално заглавие: какво се случва и как излизаме (05.09.2026, 02:10; чернова за оборване от Кими и Astra, после думата на Петър)

**Думите на Петър:** „искам ясни и конкретни стъпки по изчистването… план за изпълнение, който ще разреши тези разминавания и ще сложи ред… имам чувството, че сме затънали до шия… пълен анализ на това какво се случва!!!“

## 1 · Какво се случва — шест измерени причини

| № | причина | мярката (05.09, 02:00) |
|---|---|---|
| 1 | **Няма стабилна идентичност на мястото.** Ключът на самоличността в `place_identity.py` носи ЗОНАТА (`файл|скелет|зона`) — полето, което лотовете менят. Публичните редове нямат id; override-ите са по `(файл, име)`; референцията сравнява по `(име, зона)`. | P7 смени зоната на 77 реда → 77 сирака и 77 без id в `place_identity.json`; `qa_place_sites.py` ЧЕРВЕН (одит P7). „РОЯЛ“ ×2 неразличими по име. |
| 2 | **Истината е на ≥ 9 места без един писател.** blob-ове в две репа · `signed_facts.json` · `SIGNED_QUARTER` в кода · `fire_varna_location_inputs.json` · ledger · `registry_manifest.json` · `quarter_registry.json` (трето репо) · `place_categories.json`/legacy · замразената референция · манифести · baseline/allow. | Подписите вече се разминават: Вихрен е override в кода, кандидат във файла. Планът цитираше грешен G-ISO BASE. Два гейта носят две версии на М2 (`qa_fire_varna_export.py:230` още старата). |
| 3 | **Поправяме с глобални правила, не с факти по ред; тестваме срещу собствения си изход.** Референцията „замразява каквото кодът направи“; всяка смяна на правило мести стотици редове, а преместен ред не е нито грешка, нито поправка до окото на Петър. | Лот В: 209/375 сменени, 103 хотела свалени на район, пушнато с 20 червени теста „по замисъл“ — червеното стана нормално и куката не спираше нищо. |
| 4 | **Планираме по-бързо, отколкото мерим.** 6 плана + 3 амандамента + 1 идея за 3 дни; изпълнителите спират на противоречия В плана (P7-б/в за НЕПТУН; „нетен спад“ срещу allow-файла). | 24 ч: 3 честни STOP, 1 НЕГОДНО, 3 „ГОДНО с бележки“ — всичките с корен „планът си противоречи“ или „застоял гейт извън write-set-а“. |
| 5 | **Доставката е разцепена и решенията на Петър са разпръснати.** varna_3d вече има поправените данни (квартал 201/375), живата карта носи регресията (96); 10+ решения чакат в 5 файла. | Fire_Varna main 23 напред, varna_3d rezhimi 40 напред, нищо пушнато; pending 8, allow 18, baseline, 2 манифеста, зпз, Вихрен, break-glass, код `mladost`, OSM crosswalk. |
| 6 | **Обхватът расте по време на изпълнение.** Докато лот В не е затворен, отворихме златен списък, OSM-hints, ЛОТ 1г, Astra миграция. Всяко от тях е разумно; заедно са батакът. | 4 паралелни теми при 1 човек, който подписва. |

**Изводът в едно изречение:** батакът не е в данните (те са по-верни от вчера: 201 квартала с извор срещу 145), а в това, че НЯМА ключ, по който едно решение да се закове за едно място, и НЯМА един файл, в който да живеят подписите. Всичко останало е следствие.

## 2 · Принципите на изчистването (правила, не пожелания)
1. **Едно място = един непроменим ключ** (`place_id`, UUID, закотвен за УИН / регистров ред / КАИС тяло — НИКОГА за зона или име). Ключът излиза в публичния blob. Всичко се съединява по него: подписи, allow-списъци, манифести, sidecar-и, референция.
2. **Един файл за подписите** (`Fire_Varna/data/signed_facts.json`): всяко решение на Петър е ред там (роля `assert` или `override`), с котва, дата, свидетели. Кодът ИЗВЕЖДА override-ите от него; втора таблица няма.
3. **Една опашка за решения** (`ЗА_ПОДПИС_<дата>.md`): всичко, което чака Петър, е в един файл, дедуплицирано, с „да/не“ на ред. Решение извън опашката не съществува.
4. **Червено = блокирано, винаги.** Няма „червено по замисъл“: тест, който чака подпис, не е тест, а ред в опашката; тестовият пакет е 0 червени или пушът не тръгва (куката вече го прави).
5. **Един лот в движение.** Нов лот не се отваря, докато текущият не е пушнат. Идеи се паркират в `docs/plans/PARKED.md` с един ред.
6. **Един амандамент на кръг.** Изпълнител спира → архитектът решава ВСИЧКО отворено в един амандамент → следващ кръг. Не по едно.
7. **Мярка преди план, гейт преди данни.** Всяко число в план идва от команда; всяко правило има гейт, който е падал на счупен вход.

## 3 · Стъпките — пет фази, три докосвания на Петър

### Фаза 0 · Спиране на кръвта (днес, без Петър)
- **0.1** Замразяване на обхвата: `docs/plans/PARKED.md` с ЛОТ OSM-hints (v2, §7), ЛОТ 1г „Телата“, ИДЕЯ златен списък (§10 изпълнен като ЛОТ 0; останалото паркирано). Нито един нов лот до края на Фаза 3.
- **0.2** Комит на висящите документи (амандамент №3, OSM §6–§7, времената поправени), `activeContext.md` = една страница „състояние 05.09“: числата от §1, отворените решения (§5), реда на фазите.
- **0.3** Една опашка: `scratch/places_search/ЗА_ПОДПИС_05.09.md` (пише го изпълнителят на Фаза 3, но скелетът се прави сега) — всичко от §5 по-долу, нищо друго никъде.
- Гейт: `git status` чист от документи; PARKED.md съществува; никакъв нов план-файл след този.

### Фаза 1 · Идентичността (varna_3d, 3 комита) — коренът
- **1.1 Ключ без зона.** `place_identity.py`: съставният ключ става `файл|скелет(име)` + котви (УИН → регистров ред → КАИС тяло), зоната излиза от ключа; `place_id` се ПАЗИ за всички 375 (миграцията се доказва: 375 стари id = 375 нови id, 0 сираци, 0 нови). Гейт `qa_place_sites.py` зелен. Съобщение: `идентичност: ключът без зоната — зоната е това, което лотовете менят (И1)`.
- **1.2 `place_id` в публичния blob.** Износите добавят поле `pid` (UUID, непрозрачен, без `kais_i`/evidence): места 13 → 14 ключа, хотели 17 → 18. `qa_no_cad_ids` не се пипа (UUID не е кадастрален идентификатор). Съобщение: `износ: pid в публичния ред — един ключ за подписи, allow-списъци, манифести (И2)`.
- **1.3 Метаданни без часовник.** `_meta.generated` = датата на входовете; два билда = еднакви байтове (доказано). Съобщение: `износ: метаданни без часовник — байтова идентичност между два билда (И3)`.
- Одит: read-only, с нарочно счупени входове (дублиран УИН; ред без котва; сменена зона → същият pid).

### Фаза 2 · Една истина за подписите (Fire_Varna + varna_3d, 3 комита)
- **2.1** `signed_facts.json` v2: ключ `pid` (плюс котвата за проверка), `role: assert|override`, `expect` по кодове, `signed.by` буквално „Петър“, дата, `evidence_src`. Мигрират: 11 подписани курортни хотела (override), АДМИРАЛ (assert → override след думата), кандидатите (Гео Милев, Иван Рилски, 8 сблъсъка). **Вихрен** влиза като ред за решение: подпис по котва `place_id`/REG или излиза от override-ите.
- **2.2** `fire_varna_locations.py`: `SIGNED_QUARTER` се ИЗВЕЖДА от `signed_facts.json` (път + SHA пин в `fire_varna_location_inputs.json`; единственото разрешено кръстосано четене, само за четене, записано в ADR 009); таблицата в кода се трие; двупосочен гейт (всеки override консумиран и изнесен със `src = SIGNED_OVERRIDE`; всеки изнесен цитира точно един факт; STOP иначе).
- **2.3** Гейтът (5) в `run_gates` сравнява по `pid`, не по име; `qa_fire_varna_export.py:230` научава М2 (същият ред като P7-е).
- Одит: подпис без консуматор → червено; консуматор без подпис → червено; Вихрен според решението.

### Фаза 3 · Доставката (Fire_Varna, F12 както е планиран + pid) — единственото докосване №1 на Петър
- **3.1** F12-а..г по плана (blob-ове с `pid`, валидатори 14/18, SHA, cache v6, М7 клон, два манифеста, документи) + allow-файлът на 18-те „Морска градина“ и **ЗА_ПОДПИС_05.09.md** — ЕДИННАТА опашка: 8 сблъсъка · 18 allow · baseline f06ac06 · манифест BASE→P7 · манифест P7→F12 + думите на М7 (зпз) · Вихрен · код `mladost` · break-glass следа · 12 факта.
- **Петър подписва опашката** (един файл, „да/не“ по ред; каквото е „не“ — остава pending и картата го показва честно като „район X“).
- **3.2** Прилагане на подписаното (override-ите през signed_facts; allow със `signed_by: Петър`; baseline подписан) → регенерация → `--freeze` ЕДИН път → **тестовият пакет 0 червени** (тестовете „чакат подпис“ се преработват да четат манифеста, не да са червени) → `run_gates` зелен → одит → **докосване №2: `git push`** през куката, която пуска гейта.
- Гейт за край на фазата: живата карта показва Адмирал на Златни пясъци; „златни“ вади хотелите; Гео Милев в Младост; покритие по файл ≥ baseline или подписано; 0 червени теста; 0 pending.

### Фаза 4 · Редът (документи, 1 ден, без Петър освен докосване №3)
- **4.1 ADR 009 „Идентичност, подписи, гейтове“**: `pid` в blob-а, един файл за подписи, кръстосаното четене, гейтът на пуша, „червено = блокирано“, „един лот в движение“, „един амандамент на кръг“. **Докосване №3: Петър приема ADR 009.**
- **4.2** Документите: `ПЛАН_ЛОТ1в-В`, `ПЛАН_ЛОТ0` → статус „изпълнен/заменен“ в заглавието с линк към ADR 009; `ИДЕЯ_златен_списък` → „оборена като изход; §10 изпълнен като ЛОТ 0“; OSM-hints, ЛОТ 1г → PARKED; briefs и отчети в `scratch/places_search/` → `scratch/places_search/2026-09_архив/` с индекс; `activeContext.md` = истината за състоянието.
- **4.3** Памет и правила: `AGENTS.md`/`CLAUDE.md` на двете репа получават §„Един лот, един амандамент, червено = блокирано, pid е ключът“.

### Фаза 5 · Чак тогава (по един лот, по реда на Петър)
ЛОТ 1г „Телата“ (с `pid` и подписи по ред — вече възможно) → OSM-hints минимален (crosswalk + опашка за окото; без търсене по родител, докато няма деца в доставката) → Фаза 2 на „обществените сгради“ (другата тема) върху същите правила.

## 4 · Какво НЕ правим
- Не пренаписваме търсачката, стълбата или регистъра. Не сменяме извори. Не добавяме нови правила за квартали.
- Не махаме гейтове, за да позеленее нещо; зелено се стига с подпис или с данни.
- Не отваряме OSM, тела, златен списък, докато Фаза 3 не е пушната.

## 5 · Отворените решения на Петър (в ЕДНА опашка, Фаза 3.1)
1. 8 сблъсъка (ДАЛИЯ ГАРДЪН · Фрегата · МАГНОЛИЯ 1 И 2 · Маяк · НЕПТУН · Романтика · РУСАЛКА · СТРАНДЖА) — НТР или КАИС за всеки.
2. Allow-списъкът: 18 „Морска градина“ + 16-те реда на P7-делтата (8 хотела × 2 полета).
3. „Подписвам baseline f06ac06“.
4. Двата манифеста (BASE→P7, P7→F12) + думите на М7 (вкл. „зпз“ като locality).
5. Вихрен: котва REG/`pid` и подпис, или пада от override-ите.
6. Кодът `mladost` за ж.к. Младост в затворения списък.
7. Break-glass следа: ефимерна (по подразбиране) или tracked.
8. 12-те факта в `signed_facts.json` (11 + Адмирал) — потвърждение.
9. ADR 009 (Фаза 4).

## 6 · Гейтове за „готово“ на изчистването
- `python -m gates.run_gates` → 0 червени, 0 жълти; `python -m unittest discover -s tests` → 0 червени; `qa_place_sites.py` зелен; G-ISO 1–4 зелени; G-M6 код 0 (pending = 0).
- `origin/main` = `main` в Fire_Varna; varna_3d `rezhimi` пушнат (или изрично „не се пушва“ записано).
- Един файл за подписи, нула таблици с подписи в кода; `pid` във всеки публичен ред; един жив план в `docs/plans`, всичко друго с статус.

## 7 · Времена (изпълнители на Opus, одити след всяка фаза)
Фаза 0 — 1 ч. Фаза 1 — 3–4 ч. Фаза 2 — 2–3 ч. Фаза 3 — 3 ч до опашката, после Петър, после 2 ч до пуша. Фаза 4 — 1 ден. Общо: до опашката за подпис — до края на деня 05.09; пуш — същия ден след подписа.

## 8 · Приложение А — какво завари планът от кръг 3 (05.09, 02:20)
- **F0-ж ГОТОВ и одитиран „ГОДНО с бележки“** (59076db, b2b5808): точен подпис (само „Петър“), нетен спад покрит само от подписан allow (изход 0), жълто блокира, схемата на allow-файла валидирана (изход 4), брояч по ключ (РОЯЛ ×2 иска два реда), `file_reordered`, ⚠ при `--base`, куката без подпорката. Четири minor за **Фаза 2.3**: (а) подписаният allow-файл трябва да е tracked (гейтът проверява `git ls-files` и печата blob SHA — иначе зеленото стои на непроследен файл); (б) редът „uncovered=0“ да печата и структурните; (в) неподписан + застоял allow да казва и двете; (г) кандидат с котва REG да не се нарича „слаба котва“.
- **P7 одитиран „ГОДНО с бележки“ с един MAJOR**, който е причина №1 в §1: ключът на самоличността носи зоната → 77 сирака в `place_identity.json` след P7; `qa_place_sites.py` ЧЕРВЕН. Това е **Фаза 1.1** — не се лекува с прегенерация „както е“, а с ключ без зона. Minor: `qa_fire_varna_export.py:230` (хотелите) още носи старото правило за М2 → Фаза 2.3.
- Отворени въпроси на изпълнителите, всички вливат в **опашката (§5)**: baseline f06ac06; схемата на allow-файла (структурни редове, изход 5, един ред = един преместен) — потвърждение; докстрингът на български в `qa_fire_varna_places_export.py` (файлът е на български; правилото „английски“ важи за нов код — оставя се).

## 9 · Кими (K3, ultra, 05.09) — К1–К8 върху този план; приемам всичко, с две уточнения
- **К1 (диагнозата):** два корена, не един — даннов (няма ключ, няма един файл) и **процесен (червеното нямаше авторитет: куката съществуваше, 20 червени минаха „по замисъл“)**; причина 5 е следствие. → §1 изводът се пренаписва така.
- **К2 (`pid`):** UUID трябва да е **случаен (UUID4), монтиран веднъж и записан в `place_identity.json`** — не UUID5 от `kais_i`/регистров №, иначе е кадастрален идентификатор под прикритие; гейт доказва случайността. Публичният факт „този pid = това място“ е непроверим без котва → в blob-а излиза и **котвата** (`anchor: {src: UIN|REG, id}`; УИН вече е публичен в хотелските редове; регистровият № е публична регистрова данна). → Фаза 1.2.
- **К3 (подписите) — най-важната поправка:** `signed.by: "Петър"` е низ, който всеки агент може да напише; git не различава (всички комити са с автора на Петър). Минималният честен механизъм: **изпълнителят пише само `status: pending`; „да/не“ го прилага команда, която Петър пуска САМ (`python -m gates.sign <id> да|не`), в негов комит; агентите комитват с отделен автор (`git -c user.name="Claude Executor"`), а гейт проверява, че всеки `signed` ред е въведен от комит с автора на Петър.** Записано в плана: без ръката на Петър бариерата е процесна, не техническа. Репото на файла остава Fire_Varna (там е гейтът на пуша), но всеки подписан ред носи доказателството ВЪТРЕ (цитат, извор, дата), не крос-репо път. → Фаза 2.1/2.2.
- **К4 (червено = блокирано):** тест срещу манифеста е позволен САМО за делти, проследими до ред от опашката по id; всяка делта без ред-id = червено. Инвариантите (всеки ред има pid; покритие ≥ baseline; 0 сираци) остават червени тестове; подпис-зависимите очаквания са редове в опашката с брояч `pending`; G-M6 `pending = 0` е жълт (блокиращ) гейт, не зелен. → Фаза 3.2.
- **К5 (опашката):** схема на ред: `id · питане · текущо → предложено · котва · извор (цитат) · координати/разстояние · последица от ДА · последица от НЕ`; за 8-те сблъсъка — двете страни с котва, координати, разстояние (праг 250 m), адрес по регистър срещу карта, цитат от НТР/КАИС; за 18-те „Морска градина“ — текущо → предложено, `evidence_src`, координати, „ако подпишеш ДА, картата се меня така“. → Фаза 0.3/3.1.
- **К6 (процесът):** принцип 8 — **план, изпълнение и одит са три различни агента; амандаментът е НОВ датиран файл, старият получава статус**; дефиниция за „готово“ на всеки лот, не само на цялото. Уточнение: планът и досега се пише от архитекта (Fable) и се изпълнява/одитира от Opus, но противоречията в плана ги поправя същият архитект — затова всеки амандамент минава през Кими/Astra преди изпълнение (както този).
- **К7 (редът):** приет; обосновката влиза в §3: „подпис преди `pid` = подпис срещу идентичност, която Фаза 1 заменя; тесното място е Петър, не 3–4-те часа“.
- **К8:** (1) „не“ затваря реда като „отказано, записано“; `pending` брои само неотговорените (иначе едно „не“ прави изчистването вечно неготово); (2) миграцията 375 → 375 доказва мощност, не правилност → гейтът иска и **съответствие по котва** (всеки нов pid носи котвата на стария ред); (3) твърд SHA-пин на `signed_facts.json` прави всяко решение два комита → пин = blob SHA на файла при билд, записан в ledger-а, с гейт „пинът е HEAD на файла“, не твърда константа в inputs.
- **Най-силното място (Кими):** единната опашка — за първи път решенията на Петър са изброими и `pending` е число с гейт.

## 10 · Astra (GPT-6, ultra, 05.09) — S1–S7; ОБОРВА реда на фазите и три механики; решенията на архитекта
- **S1 (идентичност):** 77-те са застояли join-ове, не загубени UUID-и — 375 активни уникални UUID + 8 пенсионирани; реплей без зона възстановява всичките 375 (192 по УИН, 35 по регистър, 148 по резервния ключ). Махането на зоната ражда 4 групи с еднакво име (АДМИРАЛ, ПЕРЛА, РОЯЛ, РУСАЛКА; двата РОЯЛ имат различни УИН); `qa_place_sites` проверява покритие и уникалност → **размяна на два UUID минава**. → Правило: замразен стар identity файл + SHA; **биекция ред → UUID** (375 активни + 8 пенсионирани + членство в площадки) като отделен миграционен гейт; съвпадение само при уникално и съгласувано доказателство; никакво сливане на редове с общо тяло, никакъв ординал/координатен ранг за двусмислие; фикстури: дублирани/конфликтни котви, липсващо доказателство, повредени/дублирани UUID, разменени присвоявания → червено; смяна на зона/ред/координата → същото присвояване.
- **S2 (`pid` в blob-а):** атомната промяна е по-широка: `legacy_bundle_sha` връзки, QA схеми на износа, `run_gates.ROW_FILES`, клиентските валидатори, пробата; референцията носи 2 121 срещания на 375 `(име, зона)` — срещу днешните blob-ове 1 163 не се съединяват, срещу f06ac06 всички. → **Ship F12 под 13/17 ПЪРВО; `pid` = отделна, прегледана доставка след ADR.**
- **S3 (подписите):** 12 факта / 11 кандидата във файла срещу 12 override-а в кода (вкл. кандидата Вихрен, без подписания АДМИРАЛ); гейтът не валидира подпис/роля/обратна provenance; ledger-ът има 12 `SIGNED_OVERRIDE`, но без id на факт и без digest на файла → съгласието не доказва нищо. → ADR ПРЕДИ имплементацията; `fire_varna_location_inputs.json` носи път + SHA256 на файла, четене веднъж, строг парс на v2; override само от подписани утвърдителни твърдения по `(pid, field)`; assert-ите съдят, не пишат; ledger записва id на факт + digest на поле; манифестът връзва тази линия с изнесените байтове; **три отделни артефакта — факти, точни release-разрешения (allow), baseline — с препратки; фактическо одобрение никога не разрешава автоматично загуба на покритие.**
- **S4 („червено = блокирано“):** 20-те червени не се прекласифицират на едро — всяко твърдение поотделно; `FrozenDiffTest` нарочно проверява и историческото очакване, и текущия двигател; `--freeze` проверява членство в кофи, не разрешение от манифест; куката пуска `run_gates`, но не unittest и не готовност на search-манифеста. → фикстура: неподписан манифест → `BLOCKED` и тестът го твърди; алгоритмичните твърдения се пазят; готовността на доставката е отделен задължителен release-гейт (връзва референция, кандидат, входове, digest на манифеста; изисква подпис за всяка точна делта; сравнява пълни подредени резултати); `--freeze` минава през същото разрешение; **куката пуска и unittest, и release-гейта**; доказва се с инжекции (липсващ подпис, застоял хеш, неразрешена промяна на кофа, махнат/пренареден резултат, съгласувана мутация двигател + референция) → всяка блокира.
- **S5 (редът) — ОБОРВА „идентичност първо“:** P7 връща 61 квартала, живата карта чака; „днешните blob-ове“ не са gate-ready (150 неразрешени разлики срещу f06ac06: 125 загубени + 25 сменени `zone_named`; 8 pending; двата манифеста още не съществуват); варианти: F12-първо = поне два цикъла преглед/замразяване/пуш; пакетно = един по-голям цикъл, но миграция на 375 идентичности + 2 121 срещания + двата манифеста ПРЕДИ облекчението за потребителя. → **Решение на архитекта (съгласува Кими К7 и Astra S5): F12 ПЪРВО под 13/17, но опашката се РАЗЦЕПВА: (а) release-разрешения за ТАЗИ доставка (allow 18 + 16, baseline f06ac06, двата манифеста, думите на М7) — подписват се сега и важат само за тя; (б) трайните факти (12 + Адмирал + Вихрен + 8 сблъсъка) чакат `pid` — подпис срещу `(име, зона)` е подпис срещу идентичност, която Фаза 1 заменя (Кими). Така няма двойно подписване и регресията пада от живата карта в първия цикъл.** Не обещаваме „три докосвания“: всяка промяна на кандидат-байтове иска нов преглед; всяка публикация е ръката на Петър.
- **S6 (готово):** „един източник на подписи“, „един жив план“, „пушнат или изрично не“ нямат изпълними дефиниции; „не“ + pending се бият с `pending = 0`; ADR след промените, които трябва да разреши; куката допуска break-glass успех след червено — противоречие с „червено = блокирано“; инсталираното копие не е проверено; ръчно `run_gates` не доказва пътя на пуша. → изпълними проверки: grep за литерали `SIGNED_QUARTER` (0, извлечени променливи разрешени); точно равенство факти ↔ ledger ↔ износ; запазени UUID присвоявания; схема/уникалност/поверителност на публичния ред; пълни подписани манифести; паритет референция/проба; **два изолирани билда в различно време → еднакви нормализирани байтове**; unittest + G-ISO 1–4 + G-M6 + release-гейтове през проверена инсталирана кука с инжекции без мрежа; **break-glass се решава изрично (по подразбиране: премахва се)**; статусите на плановете и SHA на клоновете се проверяват машинно; **след пуша — проверка на реалните хешове на публикуваните blob-ове и на кеша**.
- **S7 (четири счупвания):** размяна на UUID минава set-equality QA (`qa_place_sites.py:165`); 14/18 ключа отхвърлени от непроменени консуматори (`index.html:6272`); сменени пакети чупят SHA връзките на речника (`index.html:6285`); зелен 5-проверков гейт пуска публикация с неподписани промени в търсенето (`run_gates.py:414`). **Най-силното място:** независимо пиннатият исторически baseline — 209-те разлики остават видими, дори ако текущият изход се регенерира като собствена референция.

## 11 · Присъда → v2
Двата оборителя се съгласяват по всичко освен реда, и редът се решава с разцепването на опашката (§10 S5). **v2 на плана = нов датиран файл** (принцип 8 на Кими): Фаза A „Доставка F12 под 13/17 + release-разрешения“ → Фаза B „ADR 009 (одобрен ПРЕДИ кода)“ → Фаза C „Идентичност: биекция, `pid`, метаданни без часовник“ → Фаза D „Един файл за подписите + трайните факти“ → Фаза E „Ред“. Куката пуска unittest + release-гейт; break-glass вън (или само с ръката на Петър); „не“ е терминално състояние; след пуш — проверка на публикуваните хешове.
