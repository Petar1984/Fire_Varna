# Амандамент №1 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 03:05) — от картата на състоянието (три Opus-одитора, `scratch/places_search/КАРТА_на_състоянието_05.09.md`)

Нов датиран файл по правило §0.2 на v2. Променя САМО Фаза A. Подписан е с подписа на v2 („подписвам всичко“); Кими/Astra са го оборвали косвено (числата са от одиторите, не от архитекта).

## Какво завари картата (и планът A не знаеше)
1. **Три кода извън затворените списъци на клиента** в износа на P7: квартал `mladost` (5 места), квартал `briz` (1 хотел), допълнително `morska_gradina` (2 хотела + 1 място). Клиентът (`index.html` ~6519) отхвърля ЦЕЛИЯ файл при непознат код → F12-а без тях е доставка, която картата не зарежда. Планът покриваше само `mladost`.
2. **Allow-списъкът за подпис е 150 реда, не 18 + 16.** Гейтът мери срещу `f06ac06` (доставката преди лот В): места 127 → 49 (lost 78, changed 18), хотели 199 → 152 (lost 47, changed 7) = **150 непокрити**. Манифестът BASE→P7 мери срещу `c1fc541` (вече регресиралата доставка) и затова показва 16. „18-те Морска градина“ са 28 (18 хотела + 10 места).
3. **varna_3d работното дърво ≠ HEAD** за `data/fire_varna_hotels.json` (некомитнат прогон на износа). F12 копира от `git show HEAD:` (както е), а работното дърво се връща на HEAD, ако разликата е само `_meta.generated`.
4. **`place_categories.json`** се разминава (legacy_by_row 209 → 18; zones 19 → 20) — влиза в F12-а с новия `CATS_SHA256` (планът го споменаваше условно; сега е задължително).
5. **М6 и М7 са изпълнени без изричния подпис**, който §3й искаше — v2 („подписвам всичко“) ги покрива; записва се като ред R9 в опашка A за яснота.
6. `--no-verify` заобикаля куката; `gates/out/` е gitignored — признава се в ADR 009 (Фаза B): бариерата на пуша е процесна без сървърна проверка.

## Промени във Фаза A
- **A.1 (F12-а):** `QUARTER_CODES` + `'mladost'`, `'briz'`; `LOCALITY_CODES` + `'morska_gradina'`; същото в `recall_sweep.py` `LOCATIONS`; преди комит изпълнителят ПУСКА огледалната проверка на клиентския валидатор (python-огледало на `validLocation`/ключовите набори върху трите blob-а) и я доказва със счупен вход (непознат код → отказ). Копира и `data/place_categories.json` от `git show HEAD:` на varna_3d с нов `CATS_SHA256`.
- **A.1 (F12-а, allow-файл):** `gates/allow/2026-09-05_lot1v_v.json` покрива ТОЧНО това, което `python -m gates.coverage --*-base git:f06ac06:… --*-candidate <новите blob-ове>` докладва (очаквано 150 реда, числото се мери), всеки ред с клас на причината: `hull_artifact` (Морска градина 28, ЗПЗ 13, Шашкъна, Виница/север…: старата обвивка грешеше, новото е район/друг квартал със свидетел), `resort_pending` (8-те сблъсъка), `no_witness` (без нито един свидетел → район), `m6_changed` (сменен именуван → друг именуван със свидетел), `m2_gained` не влиза (печалбите не искат подпис). `signed_by: "pending — Петър"`.
- **A.0 (varna_3d, преди всичко):** `git status` на `data/`; ако `fire_varna_hotels.json` се различава от HEAD само по `_meta.generated` → `git checkout -- data/fire_varna_hotels.json`; иначе STOP.
- **A.3 (опашка A):** R1 = 150-те реда групирани по клас на причината (Петър подписва по клас, с възможност за изключение по ред); R9 = ретроактивно потвърждение на М6 и М7 (изпълнени по §3й; подписът на v2 ги покрива); числото „18“ навсякъде → „28 (18 хотела + 10 места)“.
- **A.2:** без промяна.

## Готово-гейтът на A получава още един ред
`python -c` огледало на клиентския валидатор → трите blob-а приети (0 непознати кода); `git -C C:/git/varna_3d status --short -- data/` чист.
