# Амандамент №9 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 14:10) — след одита на A.2-9 (НЕГОДНО: 2 major, 3 minor); последният кръг преди доклада

A.2-9 (4bbe626) е доставен: невалидни/неулучващи „не“ шаблони блокират по шаблон; агентски дайджести падат в проверка 7; котвата е референтният комит на Петър; решаващият експеримент повторен (292 теста, 0 червени, 7/7). Одиторът намери два нови пътя, и двата в четенето на ОПАШКАТА → **A.2-10** (един комит; write-set: `gates/release.py`, `gates/sign.py`, `gates/run_gates.py`, `tests/test_places_search_gate.py`):

## Major
1. **`gates.release` чете опашката от работното дърво, а пушът праща blob-а** → всички 7 гейта зелени с нула подписани реда в комита. → опашката се чете от `blob_at("HEAD", queue_rel)`; при `git status --porcelain -- <опашка>` непразен → BLOCK със същите думи като за другите артефакти („работното дърво носи друго тяло от блоба на HEAD (пушът праща блоба)“); оплакването на `queue_authorship` в проверка 7 се вдига безусловно, не само при `yes_rows` от blob-а.
2. **Проверка 7 пита кой е въвел дайджеста, но не кой е въвел реда „да“; парсерът чете редове от HTML-коментар и code fence.** → авторство ПО РЕД: за всеки ред „да“ `git log -S` върху блока `## <id> … до следващия ##` — приема се само комит на `HUMAN_AUTHOR`; `parse_queue_text` пропуска ``` fence и `<!-- -->` региони (машината чете само каквото човекът вижда), а гейтът блокира, ако такъв регион съдържа ред с `решение:`; дублирано поле (`решение:`/`покрива:`) в един ред = BLOCK.

## Minor
3. Безусловните причини REFUSAL_MALFORMED (без наклонена черта) и REFUSAL_EMPTY (празно `покрива`) се проверяват ПРЕДИ `if reached is None` — назовават се и без валидна котва.
4. Мъртвият `delta_patterns()` се маха; докстринговете сочат `patterns_that_cover`/`refusal_pattern_reason`.
5. **Класове редове в опашката** (отговор на въпросите на изпълнителя): ред без `артефакт` и без `покрива` е клас „въпрос“ — „не“ там е разрешено и НЕ влиза в release (записва се като решение); ред с `артефакт`, но без `покрива` (baseline, кодовете, break-glass) е клас „артефакт“ — „не“ означава „артефактът не се подписва“ и release блокира по артефакта, не по шаблон; ред с `покрива` е клас „делта“ — правилата на амандаменти 6–8. `gates.sign` разпознава класа и отказва само „не“ за клас „делта“ без валиден шаблон.
6. Текстът на проверка 7 с префикс `<път>:` и `HUMAN_AUTHOR` константа — ДА.
7. **Release пази цялото тяло на подписаните артефакти:** дайджест на тялото (без полетата на подписа) записан от `gates.sign` в реда на опашката (`- **тяло:** <sha256>`), сверен от гейт 6 — пренаписване, което не пипа вързано поле, пада в гейт 6, не само в проверка 7.

## Ред и край на деня
A.2-10 → одит (сценариите на одитора: мръсна опашка при чист blob; скрит ред в HTML-коментар/fence; дублирани полета; повторен решаващ експеримент) → A.3 опашката (ако одитът е ГОДНО/с бележки) → **доклад за Петър в `docs/audits/ДОКЛАД_05.09_изчистване.md` + приспиване на компютъра** (заповедта му от 13:00), независимо от изхода на одита. Ако одитът е НЕГОДНО — докладът го казва и следващият кръг чака думата му.
