# ПЛАН · ЛОТ 0 „Гейтове“ → P7 (varna_3d: М2+М3+М6) → F12 (Fire_Varna: М7 + два манифеста) — изпълним план, подписан по думите на Петър („действаме според препоръките ти“, 05.09 00:10)

Изворите на решенията: `ПЛАН_ЛОТ1в-В_кварталите.md` §3ж (подписаното правило), §3и (М1–М3), §3й (В-М6/В-М7), §3й-а (Кими К1–К5), §3й-б (Сол S1–S6); `ИДЕЯ_златен_списък.md` §10 (минималната v2 след трите атаки). Този файл ги превежда в комити, write-set-ове и гейтове. Изпълнителят НЕ архитектира: нещо, което не е тук и не е измеримо, е STOP и въпрос.

**Универсални правила за изпълнителите:** мярка преди промяна (числото ПРЕДИ в комит-съобщението или в доклада); един логически комит, стейдж по изричен път, точното съобщение от плана; `git status --short` след всеки комит — предварително мръсните файлове (`scratch/basemap_e0/*`, untracked briefs; във varna_3d `scratch/place_bodies/qa_place_bodies.md`, `docs/sessions/ПРОМПТ_20.08.md`, `scratch/refactor/_addr/_measure_*`, `kimi_атака_*`, `osm_poi_varna.md`) НЕ се пипат и НЕ се стейджват; никакъв push; никакъв `--freeze`; кодът и коментарите на английски, UI низовете на български; LF; никакви кадастрални идентификатори/ПИ/УПИ в публични файлове (гейт `qa_no_cad_ids`). Гейт, който не е ТИЧАЛ и не е ПАДАЛ поне веднъж по конструкция (нарочно счупен вход), не е гейт. Всеки STOP = честен доклад какво липсва, без импровизация.

---

## ЛОТ 0 · Fire_Varna „Гейтове“ (4 комита, преди всякакви данни)

### 0.0 Мярка (в доклада, преди F0-а)
- HEAD на Fire_Varna main и `origin/main`; сегашната кука `.git/hooks/pre-push` (10 реда, проверява само `[ -t 2 ]`); `python -m unittest discover -s tests` → броят червени (очаквано 20/259, „по замисъл“).
- Baseline за покритието: доставката ПРЕДИ лот В = blob-овете на `f06ac06` (`git show f06ac06:data/hotels.json`, `…places.json`) и текущата доставка `HEAD:data/*.json`. Покритие „именувана зона“ = `zone` не започва с „район “; типизирано покритие = `quarter`/`locality` не е null (само където полетата съществуват). Очаквано (панелът): хотели 199 → 96, места 127 → 44.

### F0-а · `gates/coverage.py` — покритието по (файл, поле) с класове
- Нов пакет `gates/` (`__init__.py`, чист Python 3.10, без външни зависимости, никакъв импорт от `tests/`).
- Вход: `--base <path|git:<rev>:<path>>` и `--candidate <path>` за всеки от двата файла (places, hotels); редовете се съединяват по `(file, name, ordinal)` (доставката няма `place_id`; ordinal-ът е позицията в масива; разминаване на името при същия ordinal → клас `reordered`, брои се отделно).
- Изход: `gates/out/coverage.md` + `coverage.json` с таблица по (файл, поле ∈ {zone_named, quarter, locality, district}): `before`, `after`, `lost` (именувано → null/„район“), `changed` (именувано → друго именувано), `gained`, `unchanged`, `reordered` + ПОИМЕННИ списъци за `lost` и `changed` (име · преди → след · src).
- Код за изход: `0` ако `lost == 0` и `changed == 0` за всички (файл, поле) ИЛИ всеки загубен/сменен ред е изброен поименно в allow-файл `gates/allow/<ГГГГ-ММ-ДД>_<тема>.json` (`{"signed_by": "Петър", "date": …, "rows": [{"file","name","field","from","to","why"}]}`); иначе `2`. Ред в allow-файла, който не се появява като загубен/сменен, = грешка `3` („изброен, но непроменен“ — Сол S6).
- Гейтът ТРЯБВА да падне върху `f06ac06 → HEAD` (лот В) с `lost` ≈ 103 при хотелите — това е доказателството, че работи; резултатът влиза в доклада.
- Съобщение: `gates: coverage delta per (file, field) with lost/changed classes and a signed allow-list (F0-а)`

### F0-б · `gates/run_gates.py` — единственият изпълним, който пушът чака
- `python -m gates.run_gates [--base git:<rev>]` тича последователно и печата таблица ✓/✗: (1) SHA пиновете в `index.html` (`HOTELS_SHA256`, `PLACES2_SHA256`, `CATS_SHA256`) срещу LF-нормализираните байтове на `data/hotels.json`, `data/places.json`, `data/place_categories.json` (както ги смята клиентът — виж `fetchValidatedJson`); (2) ключовите набори на редовете (места 13 / хотели 17, точна дължина, както `EXPECT_KEYS`/`EXPECT2_KEYS`); (3) `qa_no_cad_ids`-регексът върху публичните blob-ове; (4) `coverage.py` срещу base = последният подписан baseline (`gates/baseline/MANIFEST.json` → `{rev, files: {path: sha256}}`; при липса — STOP с текст „няма подписан baseline“); (5) `signed_facts` (F0-г). Изходен код ≠ 0 при първото ✗. Никога не импортира `unittest`, никога не чете `tests/`.
- Baseline за първото пускане: `gates/baseline/MANIFEST.json` сочи `f06ac06` (доставката преди лот В) с текст „подписан по думите на Петър 05.09: етикетите на курортите преди лот В са верни за хотелите; 18-те „Морска градина“ са известна грешка на обвивката и влизат в allow-файла на P7“. Изпълнителят НЕ подписва вместо Петър: файлът носи `"signed_by": "pending — Петър"` и гейтът печата това като предупреждение, не като зелено.
- Съобщение: `gates: run_gates — sha pins, key sets, cad-id regex, coverage vs signed baseline, signed facts; one exit code (F0-б)`

### F0-в · pre-push куката пуска гейта
- Нов tracked файл `scripts/hooks/pre-push` (sh): пази проверката за човек (`[ -t 2 ]`, иначе блок), после `python -m gates.run_gates` от корена на репото; код ≠ 0 → „⛔ push blocked — gates red“ + изход 1. Break-glass САМО с `FV_BREAK_GLASS="<довод>"` в средата — печата довода и датата в `gates/out/break_glass.log` (tracked? НЕ — `gates/out/` е в `.gitignore`).
- Инсталация: `cp scripts/hooks/pre-push .git/hooks/pre-push` (резервно копие `.git/hooks/pre-push.bak-20260905`), проверка с `git push --dry-run origin main` от неинтерактивен shell → трябва да блокира по `[ -t 2 ]` (както досега) — гейтовата част се доказва с директно `python -m gates.run_gates` (очаквано ЧЕРВЕНО днес: покритието спрямо f06ac06 пада; това е коректно и влиза в доклада).
- `.gitignore`: `gates/out/`. Съобщение: `gates: tracked pre-push hook runs run_gates before any push; break-glass only with an explicit reason (F0-в)`

### F0-г · `data/signed_facts.json` — малкият съдещ файл
- Схема (по Сол S4/S5, Кими К1/К4): `{"_meta": {"what","rule": "the gate only compares; it never writes","licence"}, "facts": [ {"anchor": {"src": "NTR", "id": "<УИН>"} | {"src": "REG", "id": "<регистров №>"}, "file": "hotels|places", "name": "<както в доставката>", "expect": {"quarter": {"is": "<code>"}} | {"locality": {"not": ["<code>"]}} | …, "witness": ["REG-TOCity", "KAIS-addr", …], "signed": {"by": "Петър", "date": "2026-09-0X", "how": "думата (04.09) / окото", "evidence_src": "<какво е гледал>"}} ], "candidates": [ … същата схема, без подпис … ]}`. Пропуснато поле = без твърдение; `null` не се ползва; кодовете са от затворените списъци в `index.html` (`QUARTER_CODES`, `LOCALITY_CODES`, `DISTRICT_CODES`).
- Семе `facts`: 11-те хотела от `SIGNED_QUARTER` с УИН (Вихрен няма УИН → в `candidates` с `anchor: weak`) — `expect.quarter.is = zlatni_pyasatsi`, `witness: ["SIGNED"]`, `signed.how: "решение 6 (б), 03.09"`; АДМИРАЛ (УИН `В1-1ШЯ-15Ж-А1`) — `expect.quarter.is = zlatni_pyasatsi`, `witness: ["REG-TOCity","KAIS-addr"]`, `signed.how: "думата на Петър 04.09 + НТР TOCity + КАИС addr"`. `candidates` (без подпис): СУ „Гео Милев“ → `quarter.is mladost` (регистров № от `registry_manifest.json`, ако има; иначе `anchor: weak`), ОУ „Св. Иван Рилски“ → `quarter.not ["vazrazhdane","vazrazhdane1","vazrazhdane2"]`, 8-те сблъсъка от `lot1v_v_m6_fix_sample.md`.
- Гейтът в `run_gates` (5): съединява `facts` към blob-а по УИН (хотели: `uins`), а при REG — по `(file, name)` с предупреждение „слаба котва“; всяко твърдение се оценява по `code`; ЧЕРВЕНО при несъвпадение или при факт, който не резолвира точно веднъж. Днес АДМИРАЛ ще е ЧЕРВЕН (quarter null) — това е очакваното и се пише в доклада.
- Съобщение: `data: signed_facts — the small judging file (12 facts with anchors, candidates without signature); gate compares, never writes (F0-г)`

### Одит на ЛОТ 0 (read-only, Opus): coverage.py пада върху f06ac06→HEAD с ~103 загубени хотела и минава върху HEAD→HEAD с 0; run_gates има един изходен код и не пипа tests/; куката е инсталирана и блокира неинтерактивен push; signed_facts е валиден JSON, 12 факта с котви, АДМИРАЛ червен по очакване; нищо извън write-set-а не е променено.

---

## P7 · varna_3d (клон `rezhimi`): М2 + М3 + М6 (5 комита)

### P7.0 Мярка
- `git show HEAD:data/places.json | sha256` и на generated артефактите срещу `data/fire_varna_location_inputs.json` `base.*` и срещу цитатите в `ПЛАН_ЛОТ1в-В §3ж` (`ba189e85…`/`d5562de6…` срещу `7118db4…`/`e2a616e…`, Сол S6-3). Който съвпада с `git show HEAD:` е BASE; планът се поправя с една дума в §7-бележка (не се пренаписва историята). Ако НИКОЙ не съвпада → STOP.
- `python src/qa_fire_varna_location_isolation.py --skip-regen` (G-ISO, проверки 1–3); `python src/fire_varna_locations.py --inputs` (пиновете); ledger-ът: 375 реда, `changed_zone` 209, `quarter null` 235.
- НТР суровият файл `scratch/refactor/_addr/ntr_hoteli_raw.json`: 222 реда, 219 уникални `UIKNumber`, 3 дублирани групи (Сол S6-1) — числото се проверява, не се вярва.

### P7-а · М2 + М3 (както §3и)
- М2: `TypedRegistry.classify` — `map_label: false` НЕ е DROP за `quarter`/`locality` (остава DROP само `map_drop`, `report_only`, `pending_p3`); очаквано 9 записа получават „ж.к. Младост“ (ДГ 32 „Моряче“, ДГ 36 „Морска звездица“ са сред тях — FIX_SAMPLE_NAMES в кода се преразглежда: причината им вече не е „map_label“).
- М3: `legacy_by_row` (в `build_place_categories.py`/износа) носи стари зонни думи САМО когато старата зона е псевдоним на новата (регистров alias) или подписан override — не при „обвивката грешеше“. Мярка преди/след: колко реда носят legacy думи (очаквано ↓; „хотел златни“ ще падне временно — това е известно и М6 го връща).
- Съобщение: `места: М2 Младост без DROP по етикета, М3 наследени думи само за преименувания (P7-а)`

### P7-б · М6 свидетелите — четири канала, затворени таблици, kind-таблицата върху сегментите
- `fire_varna_locations.py`: НТР се чете от `scratch/refactor/_addr/ntr_hoteli_raw.json`, индекс `UIKNumber → [rows]` (норм. ключ: главни букви, без разделители). За ред с УИН: (1) `TOCity` през ЗАТВОРЕНАТА таблица `NTR_CITY = {"Златни пясъци": "zlatni_pyasatsi", "Св св Константин и Елена": "kk_konstantin_elena", "Варна": None}`; друга стойност → `die`; различни `TOCity` в редовете на един УИН → `die`; (2) `TOAddress` — union/dedupe на сегментите (`[,;]`), всеки през `self.reg.resolve(seg, allow_untyped=False)`.
- КАИС адресен текст (`addr` колона на котвеното тяло): ЗАТВОРЕНА таблица `KAIS_ADDR_ABBREV` (regex → канон): `кк?\.?\s*зл\.?\s*пяс[ъа]ци|златни\s*пяс[ъа]ци` → „к.к. Златни пясъци“; `св\.?\s*св\.?\s*констан[тt]ин\s*и\s*елена` → „к.к. Св. Св. Константин и Елена“; `кк\s*чайка` → „к.к. Чайка“; `прим\.?\s*парк` → „парк Приморски парк“. Хвостът се реже на първото ` х-л | апартхотел | р-т | ваканционен | № `. Резултатът се приема САМО ако резолвира през `TypedRegistry.resolve(allow_untyped=False)`; иначе се игнорира с бележка (не е STOP — адресният текст е свободен). `парк` влиза в `TYPE_NORM`/`_TYPE_ALT`.
- Четири ОТДЕЛНИ канала в `candidates`: `REG-TOCity`, `REG-TOAddress`, `KAIS-quar`, `KAIS-addr` (+ съществуващият `REG-manifest` за местата). Всеки id минава `classify` ПРЕДИ подреждането; полето се избира по ранг `{REG-TOCity = REG-TOAddress = REG-manifest} > KAIS-quar > KAIS-addr`; в един ранг ≥ 2 несвързани валидни id за едно поле = `die` (никога `[0]`); дете печели само при доказан родител (`is_child`). Между ранговете: две несвързани валидни стойности = сблъсък (виж P7-в).
- Ledger v2: колона `witness` (кой канал даде полето) + `channels` (всичките четири с id-тата) + `notes`. Изход `--inputs` пинва НТР (`sha256` на LF байтовете, `bytes`, `fetched`, `rows: 222`, `uins: 219`, `dup_groups: 3`) и `sha256` на канонично сериализираните (`sort_keys`, `ensure_ascii=False`, LF) `NTR_CITY` и `KAIS_ADDR_ABBREV`; ledger-ът повтаря и проверява пиновете при всяко пускане — разлика = `die`.
- Съобщение: `места: М6 — НТР TOCity/TOAddress и КАИС адресният текст като свидетели в четири канала, затворени таблици, пинове (P7-б)`

### P7-в · сблъсъците и подписаните override-и по `place_id`
- `SIGNED_QUARTER` се ключира по `place_id` (12-те имена се резолвират ЕДНОКРАТНО при билд през `LocationBook.place_id_of` — не точно едно съвпадение = `die`) и носи `{code, why, expected_witnesses}`; проверката за сблъсък консултира подписа ПРЕДИ `die()`; стойността се прилага ПОСЛЕДНА (както днес). Различен/нов свидетел под подписан ред → бележка `signed_vs_witness` в ledger-а и ЧЕРВЕН ред в G-M6 (Сол S2), не тих победител.
- Неподписан сблъсък (REG ↔ KAIS): вместо `die` — редът получава `quarter: null` (компат-етикет „район X“), `pending_signature: {"reg": [...], "kais": [...]}` в ledger-а, и влиза в блок `pending_signature` на манифеста за Петър. **Билдът завършва**, но `--freeze`/публикация са забранени, докато `pending_signature` не е празен (гейт G-M6-pending). Очаквани днес: МАГНОЛИЯ 1 И 2, РУСАЛКА, СТРАНДЖА, Маяк, Фрегата, ДАЛИЯ ГАРДЪН, НЕПТУН, Романтика (8) — числото се измерва, не се вярва.
- Съобщение: `места: подписаните квартали по place_id; неподписан сблъсък = pending_signature, не монета и не мълчание (P7-в)`

### P7-г · G-M6 и G-ISO
- `qa_fire_varna_export.py` (или нов `qa_fire_varna_m6.py`): (a) хотел с `REG-TOCity` курорт и `quarter` null и без `pending_signature` → червено; (b) избраното `field + code` съвпада с класифициран свидетел от `channels` или с подписания override — non-null не стига; (c) `pending_signature` > 0 → червено за публикация (отделен код); (d) таблиците са затворени: стойност извън тях = `die` (доказано с нарочно счупен вход в теста); (e) G-ISO: `BASE_SOURCES`/`BASE_GENERATED` байт-равни на пина (проверки 1–3 с `--skip-regen`; проверка 4 остава известна невъзможност — вписва се, не се крие).
- Всеки гейт тича и ПАДА веднъж по конструкция (счупен вход) — командата и изходът в доклада.
- Съобщение: `места: G-M6 (свидетел ↔ поле, затворени таблици, pending) + G-ISO потвърден върху един подписан BASE (P7-г)`

### P7-д · износът и манифестът BASE → P7 за подпис
- `python src/export_fire_varna_places.py` + `export_fire_varna_hotels.py` → `data/fire_varna_*.json`; `_meta` без часовник в сравняваните обекти (сравнява се по редове, не по файл — Сол S3).
- Манифест `scratch/refactor/_addr/lot1v_v_manifest_BASE_P7.json` + `.md`: (1) покритие по (файл, поле) преди/след с класове `lost`/`changed`/`gained` и ПОИМЕННИ списъци (алгоритъмът на `gates/coverage.py` от Fire_Varna — копие на логиката, не импорт през репото; същите числа); (2) `pending_signature` блок (8); (3) корекциите „известна грешка на обвивката“ — 18-те „Морска градина“ + всеки друг ред `changed`/`lost` с довод (свидетел) — това е allow-файлът, който Петър подписва; (4) 12-те подписани: свидетелите им (10/12 TOCity Златни, Вихрен без УИН, Империя/Варна без кандидат); (5) пиновете. Очаквано: хотели с квартал 96 → ~150–170; места 44 → ~44–53 (М2 +9). Числата се измерват.
- Съобщение: `места: износ след М2+М3+М6 и манифест BASE→P7 с покритие, pending и allow-списък за подпис (P7-д)`

### Одит на P7 (read-only, Opus): всяко число в манифеста се възпроизвежда с команда; G-ISO 1–3 зелени; пиновете се проверяват от ledger-а; таблиците са затворени (счупен вход → die); никакъв ред не е „избран [0]“; `pending_signature` = измереният брой; никакви кадастрални идентификатори в публичните файлове; write-set-ът спазен (нищо в `output/`, нищо в `BASE_SOURCES`).

---

## F12 · Fire_Varna: М7 + blob-ове + втори манифест (4 комита) — БЕЗ замразяване

### F12-а · blob-овете от P7 + пинове + кеш
- Копие на `varna_3d/data/fire_varna_places.json` → `data/places.json`, `…hotels.json` → `data/hotels.json` (LF); SHA пиновете в `index.html` (`HOTELS_SHA256`, `PLACES2_SHA256`) преизчислени от LF байтовете; `PLACES_CACHE` → `fire-varna-hotels-v6-<n>`; валидаторите непроменени (ключовете 13/17 не се менят в P7 — проверява се). `python -m gates.run_gates --base git:f06ac06` → покритието трябва да е ≥ baseline при хотелите ИЛИ разликите да са в allow-файла от P7-д (копира се в `gates/allow/2026-09-05_lot1v_v.json` със `signed_by: "pending — Петър"` → гейтът предупреждава, не е зелен).
- Съобщение: `data(search): places/hotels after P7 (М2+М3+М6), sha pins, cache v6 (F12-а)`

### F12-б · М7 „голото място“ — `bare_location_rows` (Сол S4)
- `recall_sweep.py`: записът пази ОТДЕЛНО `qtk`, `ltk`, `legtk`, `ktk` (вече е така) и новият клон в `search()`: keyless `R` непразно, без `DISTRICT_MARK`, без `STREET_MARK`, без токен-число → ако всеки токен е в `qtk ∪ ltk` на ≥ 1 запис → `bare_location_rows(R, cls)` = `stable_unique(head + quarter_rows + locality_rows + district_rows_without_quarter)`, където `head` = точните имена/фрази (както днес), quarter_rows = записи с всички токени в `qtk`, locality_rows = всички в `ltk` (legacy `legtk` НЕ задейства и НЕ влиза, Кими К5-г), district_rows = записи без квартал, чийто `dph` съдържа фразата; клонът тича ПРЕДИ `gen_ok` и връща `hasKey=False`, branch `"M7-bare-location"`. `index.html`: записът пази отделни `qtk/ltk/legtk` (днес само `zkset`) и същият клон 1:1; М5 паритетът (probe) го покрива.
- Подписан списък: `scratch/places_search/m7_trigger_tokens.json` — всяка единична дума, която задейства клона, с брой редове по code и клас (очаквано ~33 по Сол; „зпз“ е и locality alias, и legacy → по правилото по-горе задейства САМО като locality; решението се показва на Петър в манифеста).
- Съобщение: `search: М7 bare location branch — quarter/locality tokens only, before gen_ok, hasKey false; client mirrors the reference (F12-б)`

### F12-в · двата манифеста и референцията (без freeze)
- `recall_sweep.py --manifest` (report-only) произвежда ДВА дифа: `lot1v_v_manifest_BASE_P7.json` (данните: кофите, които мърдат от М2+М3+М6, с before/P7 типизирани полета и `witness`) и `lot1v_v_manifest_P7_F12.json` (само М7: новите редове в СЕДМА кофа `gate_m7_bare`, 0 стари заявки пипнати — проверява се). Всеки `(bucket, q, row)` носи `bundle:ordinal`, before/P7/F12, branch, ordinal. Манифестът показва ВСЕКИ ред, не първите осем (Сол S5). Тестът на ред ~1236 („училище възраждане“ старото попадение) се преработва да чете очакването от манифеста, не от кода — но остава ЧЕРВЕН до подписа (по замисъл).
- НИКАКЪВ `--freeze`. Броят червени тестове след F12 се измерва и пише (очаквано ~20–25, всички от класа „чака подпис“; нула от друг клас — иначе STOP).
- Съобщение: `test(search): two report-only manifests (BASE→P7 data, P7→F12 М7) and the m7 trigger list for signature; no freeze (F12-в)`

### F12-г · документи
- ADR 008 §D9: таблицата вид → поле поправена по М2 (`map_label:false` не е DROP), четирите канала на М6, клонът М7; `docs/activeContext.md` (състояние: pending_signature 8, allow 18, два манифеста, замразяване чака подпис); `ПЛАН_ЛОТ1в-В_кварталите.md` §3к: изпълнено/измерено; `ИДЕЯ_златен_списък.md` §11: лот 0 изпълнен (какво е гейт, какво чака подпис).
- Съобщение: `docs: ADR 008 D9 (М2, М6 channels, М7), activeContext, lot В §3к, golden §11 (F12-г)`

### Одит на F12 (read-only, Opus): паритетът клиент ↔ референция за М7 (пробата `probe_places_fv.mjs` или еквивалентна мярка върху 10 голи заявки); двата манифеста се възпроизвеждат; 0 стари заявки в P7→F12 дифа; червените тестове са само от класа „чака подпис“; пиновете съвпадат с LF байтовете; `run_gates` червено САМО по покритие/pending (очаквано) и нищо друго.

---

## Какво подписва Петър сутринта (един файл за четене: `scratch/places_search/ЗА_ПОДПИС_05.09.md`, пише го изпълнителят на F12-г)
1. `pending_signature` — 8-те сблъсъка (таблицата от `lot1v_v_m6_fix_sample.md` + какво казват четирите канала); за всеки: НТР / КАИС / друго.
2. Allow-списъкът на P7: 18-те „Морска градина“ + всеки друг `lost`/`changed` ред с довода му.
3. Двата манифеста (BASE→P7, P7→F12) и списъкът на задействащите думи на М7 (вкл. „зпз“).
4. `signed_facts.json`: 12-те факта + кандидатите (Гео Милев, Иван Рилски).
След подписа: малък лот „замразяване“ — прилагане на подписаните override-и (P7-е), allow-файлът с `signed_by: Петър`, `--freeze` ЕДИН път, тестове зелени, `run_gates` зелен, одит, командата за push (`git rebase --autostash origin/main && git push origin main`) — куката вече ще пуска гейта.

---

## Амандамент №1 (05.09, 01:40) — след първия кръг: ЛОТ 0 ГОТОВ но одит НЕГОДНО (4 major), P7 честен STOP на P7-б, F12 неначнат

### A · Решения на архитекта (в обхвата на подписания план; каквото е за Петър е отбелязано)
1. **НЕПТУН и сблъсъкът в ранг 0.** Уточнение на P7-б/P7-в: `die` остава за ≥ 2 несвързани валидни id ОТ ЕДИН КАНАЛ (напр. два регистрови сегмента за квартал); разминаване МЕЖДУ два канала от един ранг (REG-TOCity ↔ REG-TOAddress; REG-manifest ↔ REG-TOAddress) е сблъсък по данни, не по билд → `pending_signature`, както при REG ↔ KAIS. Така 8-те от P7-в стоят (7 между ранговете + НЕПТУН), билдът завършва, публикацията е забранена до подпис. Довод: `TOCity` е груб курортен етикет, `TOAddress` е по-фин — несъгласието им е въпрос за окото, не дефект на конвейера.
2. **М2 = 5, не 9.** Приема се измереното (ledger-ът носи 6 реда с DROP по map_label от първия си запис; един вече има Младост 2). Числото 9 в §3и е заменено с 5; комитът 341b4d8 стои.
3. **„парк“ само като отварящ префикс (LEAD), не в детектора на композити (TYPE_TOKEN).** „БЕРЛИН ГРИЙН ПАРК“ не е композит. Ако `_TYPE_ALT` захранва и двата регекса — отделен списък за LEAD.
4. **Кодът на квартала „ж.к. Младост“ е регистровият id `mladost`** (различен списък от районните кодове — `QUARTER_CODES` и `DISTRICT_CODES` са отделни). F12-а добавя `'mladost'` в `QUARTER_CODES` на `index.html` и в `LOCATIONS[quarter]` на `recall_sweep.py`; кандидатът „Гео Милев → quarter.is mladost“ в `signed_facts.json` става валиден код.
5. **Котвите на кандидатите Гео Милев / Иван Рилски:** `registry_manifest.json` (varna_3d) няма `registry_row_id` за тях; котвата е `{"src": "REG", "id": "<place_id от манифеста>"}`: СУ „Гео Милев“ → `0155343d-955a-41d1-99ac-11828e46909b`, ОУ „Свети Иван Рилски“ → `a5be53e4-0b78-43e4-a161-bb4691e232d1` (адреси по регистъра: бул. „Република“ №124А; бул. „Владислав Варненчик“ №225). Изпълнителят на Fire_Varna НЕ чете varna_3d — стойностите са дадени тук.
6. **§7-бележката за G-ISO BASE в `ПЛАН_ЛОТ1в-В_кварталите.md`** (ba189e85…/d5562de6… → измереното 7118db4…/e2a616e…) я прави изпълнителят на F12 (Fire_Varna), с измерените стойности от отчета на P7.
7. **P7-а (341b4d8) е приет** на мярката (квартал 140 → 145, legacy_by_row 209 → 19; G-ISO 1–3 и qa_no_cad_ids доказано падат на счупен вход). G-M6 идва с P7-г.
8. **ЖЪЛТО блокира пуша.** `run_gates` връща 1 и при ⚠ (неподписан baseline/allow-файл): „pending — Петър“ не е подпис. Зелено има само след думата на Петър в `MANIFEST.json`/allow-файла. (Петър: кажи „подписвам baseline f06ac06“ и изпълнителят сменя `signed_by` в отделен комит.)
9. **Break-glass следа:** остава ефимерна (`gates/out/`, gitignored) + печат в терминала. За Петър: искаш ли tracked ред в `docs/` при всеки break-glass? По подразбиране — не.
10. **Одитните файлове** ги записва ИЗПЪЛНИТЕЛ от структурирания изход на одитора (одиторът е read-only по договор): `scratch/places_search/ОДИТ_ЛОТ0_05.09.md` — в F0-д.

### B · ЛОТ 0-fix · F0-д (Fire_Varna, един комит + един за одитния файл)
Поправки по четирите major на одита (всяка доказана със счупен вход, командата и изходът в отчета):
- (1) `run_gates.check_coverage` и `coverage.py`: чете `signed_by`/`date` на allow-файла; липсва ключ → ЧЕРВЕНО; не започва с „Петър“ → ЖЪЛТО (= блокира, т. 8); печата ги в таблицата.
- (2) Различен брой редове base ↔ candidate → ЧЕРВЕНО (изход 2), освен ако разликата е поименно в allow-файла; `rows_base`/`rows_candidate` в `coverage.md` и в реда на `run_gates`.
- (3) Съединяване по `(file, name)` с ordinal само като резервен ключ при дублирано име (РОЯЛ ×2 → по ordinal вътре в името); `reordered > 0` → ЧЕРВЕНО, освен ако е в allow-файла; и независимо от join-а: `after < before` по (файл, поле) → ЧЕРВЕНО.
- (4) `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` (и stderr) в началото на `main()` на `coverage.py` и `run_gates.py`; командата `python -m gates.run_gates` трябва да печата пълната таблица от Git Bash И от PowerShell без `PYTHONIOENCODING`.
- Двете minor: ⚠ → изход 1 (т. 8); „изход при първото ✗“ се заменя в плана с „всички проверки тичат, изходът е 1 при коя да е червена/жълта“ (пълна таблица за човека).
- Съобщения: `gates: coverage — signed allow-list enforced, row-count and reorder are red, utf-8 console; yellow blocks the push (F0-д)` и `docs: audit of ЛОТ 0 recorded from the auditor's structured output (F0-е)`.
- Одит на F0-д: одиторът повтаря четирите си счупени входа (200 изтрити реда; пълно пренареждане + унищожени квартали; allow без подпис; конзола cp1252) — всичките трябва да са ЧЕРВЕНИ/ЖЪЛТИ; HEAD→HEAD зелено; f06ac06→HEAD червено с 209.

### C · P7 продължение (varna_3d, от 341b4d8)
- P7-б: прилага се patch-ът `scratchpad/p7/P7-b_wip_M6_channels.patch` (517 реда; изпълнителят го проверява ред по ред срещу P7-б + A.1 + A.3), после P7-в, P7-г, P7-д както в плана, с уточненията от A. Очаквани числа се МЕРЯТ (квартал 145 → ?; pending 8; allow „Морска градина“ 18 + други).
- Одит на P7 както в плана + проверка, че НЕПТУН е `pending_signature`, „БЕРЛИН ГРИЙН ПАРК“ пази квартала си без компенсация, и че никакъв `die` не е разхлабен извън A.1.

### D · F12 допълнения
- F12-а: `QUARTER_CODES` + `'mladost'`; `LOCATIONS` в `recall_sweep.py` съответно; `signed_facts.json` кандидатите получават котвите от A.5 (REG place_id) и „Гео Милев“ вече с валиден код (остава кандидат — без подпис).
- F12-г: §7-бележката по A.6; `ЗА_ПОДПИС_05.09.md` включва и: „подписвам baseline f06ac06“ (A.8), break-glass следата (A.9), НЕПТУН и правилото A.1, М2 = 5.
