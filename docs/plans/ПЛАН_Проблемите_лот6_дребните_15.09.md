# ПЛАН · „Проблемите“ — **Лот 6 · Петте дребни реда на беклога**

`docs/plans/ПЛАН_Проблемите_лот6_дребните_15.09.md` · **15.09.2026** · **v1.2 (след Gate 2: Одитор, Astra, Кими; §7) — v1.1 ПОДПИСАН от Петър на 15.09.2026 (Gate 1): „давай с препоръките“ (Р1 а, Р2 а, Р3 а, Р4 да)** · Планировчик (Opus, read-only), транскрибиран от Архитекта; v1.1 = Архитект след лещите на Astra и Кими · база **37db9ca** (`HEAD` = `problems` = `main`) · дърво `C:/git/_wt/fv_problems` · верига: Планировчик → Gate 1 (Петър) → Изпълнител → Одитор → Astra (механика) + Кими (данни) → Gate 2 → **Петър пушва**.

**Клас на риска: 🟠 ЧУВСТВИТЕЛЕН** (Вариант Б: без ADR, отделен Одитор + външни лещи). Спуснати спусъци: (1) пипа се **подписан гейт-скрипт** (`b3_g11.py`, амандамент №1 S29-4) и той съди всяка доставка; (2) пипа се **ядрото на ингеста** (`hydrant_core.process`); (3) много файлове в един подсистем (гейтове+тестове). **Не са спуснати:** нула байта в `data/`, `index.html`, `worker/`, `sw.js`; нула публикуване/флаг; нула нов извор; нула кросрепо. Петте реда са S/low, без зависимости и без решение на Петър — затова вървят в един лот, **по един комит на ред**.

**Думата на Петър (15.09):** „давай да действаме подред — ползвай кими и астра за различни лещи“; „не искам регреси, само прогреси“ → Г1/Г6 доказват: index.html, data/, worker/, sw.js байт-равни на базата; всичките 435 теста на базата остават и са зелени; новите се добавят отгоре.

**CWD: `C:/git/_wt/fv_problems`**; пред всяка проба `PYTHONIOENCODING=utf-8`.

> **Ревизия v1.1 (Архитект, 15.09, преди К2, след Astra и Кими върху плана).** **Astra (механика, ultra):** два блокера в текста на плана, поправени тук — (1) Г6 казваше „точно осем пътя“, а К2–К6 пипат **девет** (четири тестови файла: `test_recall_sweep_import_clean.py`, `test_hydrant_core.py`, `test_first_load_budget.py`, `test_b3_g11_named_ring.py`); (2) К6 не вписваше своя нов тест в `ALLOWED_NEW` — възпроизведено в памет: `stray_new: ["tests/test_b3_g11_named_ring.py"]`, изход 1; К6 вече носи и този ред. Не оборено, с мярка: Г-H1 пада и се вдига (Windows CPython 3.10.6, `-X dev`: оригиналът дава три предупреждения, `with` — празен stderr, един върнат теч — едно; предупреждението не зависи от изключването на интерпретатора — идва и при `os._exit(0)`); `cats`/`hotels`/`places2`/`reference_rows()` байт-равни във всичките три варианта; единственият sha на скрипта е в `granitsi_fixtures_06.09.json` `_meta.engine.sha256` (историческа мярка, не се съди от тест); F-7 в памет: `skipped / not_a_hydrant_report`, записи и провенанс непроменени, паритетът 5 OK, `by_report_type={"app_feedback":1}`; H-10: 595 936 + 1 324 589 = 1 920 525, при 1 500 000 точно един тест пада; D-25 върху 19-те блоба на обхвата — **0 попадения, 0 фалшиви положителни** в places/hotels/removed; обявени пропуски на правилото (§6): пръстен с допълнителен ключ (`lat/lon/z`, `lat/lon/id`), съкратени имена (`la/lo`), вложени числови двойки; 443 = 435 + 8 е вярно (половините в подпроцес не се броят); `parse_error` стои и в `tests/granitsi_client_probe.mjs:219` — чужд контекст, не се пипа. **Кими (данни):** петте вида в `KNOWN_REPORT_TYPES` имат реални приложени доклади (exists_confirmed 570, new_hydrant 156, missing 71, wrong_location 38, damaged 14; 10 717 стари refs без вид от миграцията; 14 `imported_note` от `verify_from_notes_c36.py`, не от ингеста); за 40 цикъла **нула** следи от непознат вид или `parse_error` в дневника и провенанса; доклад не се губи — прескоченото issue остава `approved` и излиза при следващ прогон (`ingested_issue_numbers` връща само `applied`); `skip_reason` се вижда само в stdout на `apply_approved_reports.py` (`print_summary`) и в справката от `--report`, която не остава в репото → поправката е само в изхода на прогона. Кими предупреждава, че голдънът `golden_apply_v0.py:363` носи `parse_error` и паритетът би почервенял при непознат вид във фикстурите — Планировчикът измери, че никоя от осемте паритетни фикстури не носи такъв (§1 ред 6), затова голдънът остава непокътнат и служи за отрицателната половина.

---

## §0 · Червени редове

1. Агент не пуска `git push`; подагентите са на Opus.
2. Ръцете: код и тестове → `Claude Executor <executor@local>`; документи → `Claude Architect <architect@local>`.
3. Нула байта в `data/`, `index.html`, `worker/`, `sw.js`, `scripts/hooks/`. Съди се машинно (Г6).
4. **НЕ се пипат:** `tests/negative_halves_manifest.json`, `tests/test_negative_halves.py` (затворен набор от 23 реда, `:76`), `tests/golden_apply_v0.py`, `gates/release.py`, `gates/run_gates.py`, `gates/probe/lot_perimeter.py`, `gates/probe/ib1_g15.py`, `scratch/places_search/recall_sweep_rows.json`, `expectations.json`.
5. Гейт = команда с очакван изход + отрицателна половина, която **ТИЧА и ПАДА**. Половина без измерена червенина не се доставя.
6. Фикстури — **само** в `$TEMP/fv_lot6`; нула фикстури в дървото; нула истински координати (само 1.0/2.0-числа, за да мине и ИБ1-Г15).
7. Базата се пинова като литерал **`FIRE_VARNA_LOT6_BASE=37db9ca`**; никога `FIRE_VARNA_BASE_COMMIT` (той е на И-Б1, `test_negative_halves.py:53`).
8. Subject: английски, ≤ 72 знака; `git add` само с явни пътеки.

---

## §1 · Мярката (Планировчик, четене+grep при 37db9ca — никой да не мери наново)

| # | Мярка | Стойност |
|---|---|---|
| 1 | `recall_sweep.py` module-level течове | **три**: `:258` `cats`, `:259` `hotels`, `:260` `places2` — `json.load(open(…))`; други `open(` в файла: `:4055`, `:4138` — **вътре във функции** (пишат при `--freeze`/доклад), **не текат при импорт** |
| 2 | кой ИМПОРТИРА модула | `tests/test_places_search_gate.py:146`, `tests/test_granitsi_fixtures.py:54`, `gates/release.py:353` (`ENGINE_REL` `:154`) |
| 3 | „6 скрипта със същия шаблон“ — **опровергано** | `json.load(open(` в `scratch/` = **328 попадения в 158 файла**; в `scripts/` = 2 (`replay_historical_new_hydrant.py:52,:95`, **вътре във функции**); в `gates/` = **0**; в `tests/` = `verify_h4.py:5`, `verify_apply.py:5` (module-level, но **не се откриват** от `discover` — шаблонът е `test*.py` — и никой не ги внася). **Извод: точно ЕДИН модул прави шум в пакета — `recall_sweep.py`.** |
| 4 | пиновете, които НЕ зависят от тялото на `recall_sweep.py` | `expectations.json` пинова `recall_sweep_rows.json` (sha `0bc7a18…`/`6196758…`), **не** байтовете на скрипта → редакцията на трите реда не мърда нито един дайджест, стига `reference_rows()` да дава същите редове |
| 5 | F-7 | `hydrant_core.py:26-28` `KNOWN_REPORT_TYPES` (пет вида) · **`:582-583`** `if rtype not in KNOWN_REPORT_TYPES: _skip(report, "parse_error")` · `:603` `skipped_reasons`, **`:604`** `by_report_type` (беклогът казва `:590` — преместило се е) |
| 6 | F-7 · кой друг чете `"parse_error"` | **`tests/golden_apply_v0.py:362-363`** (замразеното копие) и `scripts/backfill_addresses_20260511.py:426` (чужд контекст, HTTP). **Нито един тест не пинова низа.** Паритетните фикстури (`test_apply_approved_reports_parity.py:74-105`, 8 доклада) **нямат непознат тип** → паритетът остава зелен, `golden_apply_v0.py` не се пипа |
| 7 | F-15 | `docs/moderation_log.md:415` → `index.html:2806`, `:416` → `index.html:3110`. Днес: `typeFieldsHTML` е на **`index.html:2924`**, а условието за `type` е в **`buildReportObject`, `index.html:3185-3188`** (`AT_HYDRANT_TYPES.has(reportType) || reportType === 'new_hydrant'` — подписаният ред на лот 1). Дупката е **затворена в `a3042ed`** (`docs/activeContext.md:15`, 13.09) |
| 8 | H-10 | `tests/test_first_load_budget.py:70` `EXPECTED_EAGER = {"data/hydrants.json"}`; `:262-267` сумата е само по eager url-ите; `delivered_size` `:229-239`; `HARD_CAP` `:67`. Договорът: `AGENTS.md:53` „`index.html` + `data/hydrants.json`“, `AGENTS.md:137` 5 MB |
| 9 | H-10 · числата | `index.html` **595 936 B** + `hydrants.json` **1 324 589 B** = **1 920 525 B** при таван 5 242 880 → остава зелено; при 1 500 000 → червено (сам товарът минава) |
| 10 | D-25 · **къде е правилото** | **Правилото „списък от ≥ 4 числа“ НЕ СЪЩЕСТВУВА в код.** То е планирано (И-А4) в `docs/plans/ПЛАН_Картата_imot_08.09.md:122,:130,:273` и никога не е доставено. Днес `gates/probe/b3_g11.py:37` съди **само по имена на ключове**: `GEO = {"coordinates","geometry","geometries"}`; `walk` `:120-126`; `main` `:170`. Значи fail-open е двоен — и за именувани точки, и за голи числови пръстени |
| 11 | D-25 · защо числовият пръстен е ВЪН от лота | правилото „≥ 4 числа“ пали `data/address_rows.json /rows` и `data/approx_addresses_v1.json /rows` (`ПЛАН_Картата_imot_08.09.md:126-130`) → гейтът става **червен по конструкция**. Влиза само **именуваният** пръстен |
| 12 | D-25 · защо не стигат имената на ключове | именувани `lat`/`lon` живеят законно в `data/places.json`, `data/hotels.json`, `data/removed_hydrants.json` → добавяне на `lat`/`lon` в `GEO` би направило гейта червен върху живото дърво. Затова правилото съди **формата на списъка**, не името на ключа |
| 13 | ALLOWED_NEW | `b3_g11.py:40-103`; прецедентът на лот 5: `:93-98` — блок с коментар кой лот ражда пътищата, **в същия комит, който добавя файла** |
| 14 | N₀ | **435** теста (`discover -s tests`) — Ф0 го премерва наново |
| 15 | НЕ е мерено (Ф0 го мери) | (а) точният stderr на детето при `-X dev` (пише ли се „ResourceWarning“ и сочи ли `recall_sweep.py`); (б) новото правило върху реалния обхват на Г11 → **очаквани 0 попадения**; (в) `git log -1 a3042ed` |

---

## §2 · Редовете, комит по комит (по един ред = един комит)

**Редът:** К2 носи реда на плана в `ALLOWED_NEW` (Г8 е червен по конструкция само между К1 и К2); след това по нарастващ радиус, а подписаният гейт е последен. Подредбата по цена е `F-15 < H-10 < F-7 < H-1 < D-25` — размяната е безплатна, стига редът на плана да влезе в **първия** комит на Изпълнителя.

### К2 · H-1 — трите манипулатора (`scratch/places_search/recall_sweep.py`, нов тест, `b3_g11.py`)
**Редакцията:** `:258-260` стават три `with open(…) as fh: … = json.load(fh)` (шест реда), плюс два реда коментар ЗАЩО. Имената `cats`/`hotels`/`places2` остават module-level и със същите стойности.
**Гейт Г-H1** (`tests/test_recall_sweep_import_clean.py`, нов): дете `python -X dev -W always::ResourceWarning -c "<spec_from_file_location + exec_module + print('IMPORTED', len(cats['chips']))>"`; твърди се: изход 0, stdout носи `IMPORTED`, и **нула реда в stderr съдържат „ResourceWarning“**. (Иглата е нужна: без `IMPORTED` паднало дете би дало празен stderr = фалшиво зелено.)
**Отрицателна половина:** огледало в `$TEMP/fv_lot6/h1/{scratch/places_search,data}` — копие на модула с върнат **един** `json.load(open(...))` + копия на `data/{hotels,places,place_categories}.json` (`REPO_ROOT` е `parents[2]`, затова огледалото е с тази дълбочина) → същата проба дава ≥ 1 „ResourceWarning“ → изход 1. **Ако огледалният импорт падне по друга причина → половината става синтетичният триредов модул в `$TEMP` и това се ЗАПИСВА, не се импровизира.**
**Комит (Executor).** Файлове: `scratch/places_search/recall_sweep.py`, `tests/test_recall_sweep_import_clean.py`, `gates/probe/b3_g11.py` (**два** реда в `ALLOWED_NEW`: новият тест и планът).
```
fix(sweep): close the three json handles the engine leaks at import

recall_sweep.py:258-260 read the three delivered payloads with
json.load(open(...)), so every test run that imports the engine - the
places gate, the granitsi fixtures and gates.release - dropped three file
objects on the floor and printed a ResourceWarning Petar reads in the
push hook. The three lines become `with open(...) as handle`; cats,
hotels and places2 stay module-level and byte-identical, so the frozen
reference and every pinned digest in expectations.json stay untouched.

tests/test_recall_sweep_import_clean.py imports the engine in a child
with -X dev and demands a stderr without one ResourceWarning line, with
an IMPORTED sentinel so a dead child cannot pass for a clean one; its
negative half doctors a copy of the module outside the tree and was
measured to fall. Measured: 328 more json.load(open(...)) stand in
scratch/, but not one of those modules is imported by a test or a gate -
a script that runs and exits makes no noise in the suite.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

### К3 · F-7 — непознатият тип не е `parse_error` (`scripts/lib/hydrant_core.py`, `tests/test_hydrant_core.py`)
**Редакцията:** `:583` `_skip(report, "parse_error")` → `_skip(report, "not_a_hydrant_report")` + два реда коментар (предпазителят е годен; името лъже модератора). Нула други байта.
**Гейт Г-F7** (нов клас в `tests/test_hydrant_core.py`, модулът под изпитание се зарежда през `FIRE_VARNA_INGEST_MODULE`, по подразбиране `scripts/lib/hydrant_core.py`): `process([{issue_number: 900, report_type: "app_feedback", comment: "…"}], …)` → `action == "skipped"`, `skip_reason == "not_a_hydrant_report"`, записите и provenance-ът **байт-равни** на входа, `build_report(...)` дава `output_count == input_count` и `by_report_type == {"app_feedback": 1}`.
**Отрицателна половина:** същият клас в подпроцес с `FIRE_VARNA_INGEST_MODULE=<repo>/tests/golden_apply_v0.py` (замразеното копие още казва `parse_error`, `:362-363`) → изход 1 с иглата `not_a_hydrant_report`. Половината е и **декларацията на разминаването** с голдъна.
```
fix(ingest): an unknown report type is not a parse error

hydrant_core.process skips a report whose report_type is outside
KNOWN_REPORT_TYPES (:26-28) and calls that skip "parse_error" (:583) -
the guard is right, the name sends a moderator hunting for a broken
report that does not exist. The reason becomes not_a_hydrant_report;
the behaviour does not move a byte: zero data changes, action stays
"skipped", output_count stays input_count.

The new test in tests/test_hydrant_core.py loads the module under test
from an env path and proves all four; its negative half runs the same
assertions against tests/golden_apply_v0.py, the frozen pre-refactor
copy that still says parse_error, and was measured to fall. The golden
and the parity fixtures are untouched: measured, not one of the eight
parity reports carries an unknown type, so the parity stays byte-equal.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

### К4 · F-15 — дневникът сочи имена, не редове (`docs/moderation_log.md`, **ръка: Architect**)
**Редакцията (три реда, дословно):**
- `:415` → `- \`index.html\` · \`typeFieldsHTML\` — покриваше \`exists_confirmed\`, \`missing\`, \`new_hydrant\``
- `:416` → `- \`index.html\` · \`buildReportObject\` — \`type\` се изпращаше само за \`exists_confirmed\` и \`new_hydrant\``
- нов ред след `:416`: `- **Поправено в \`a3042ed\` (13.09, „Проблемите“ лот 1):** и трите форми при хидрант носят блока „Тип + Работи ли?“; условието живее в \`buildReportObject\`.`

**Гейт Г-F15 (проба при доставката, не постоянен тест — поуката „периметърните тестове са гейтове на доставката“):** `grep -c 'index.html:2806\|index.html:3110' docs/moderation_log.md` → **0**; `grep -c 'function typeFieldsHTML(\|function buildReportObject(' index.html` → **2** (по едно); `grep -c 'a3042ed' docs/moderation_log.md` → ≥ 1. **Отрицателна половина:** същите три команди върху `git show 37db9ca:docs/moderation_log.md` → първата дава 2, третата 0 → пробата пада.
```
docs(log): name the functions, not the lines, and cite the fix

The cycle #38 entry addresses the gap of report #716 by line number
(index.html:2806 and :3110). Both rotted: typeFieldsHTML stands at :2924
today and the type condition moved into buildReportObject (:3185-3188),
so the next reader would open two innocent lines and conclude the hole
was closed by accident. The two pointers become FUNCTION names, which do
not move with an edit above them, and the entry gains the line that says
where the hole was really closed: a3042ed, 13.09, lot 1 of Проблемите.
The verdicts, the numbers and the rule about the note stay verbatim.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

### К5 · H-10 — първото зареждане носи и `index.html` (`tests/test_first_load_budget.py`)
**Редакцията:** нова константа до `:70` `SHELL_REL = "index.html"` с ред-цитат `AGENTS.md:53`; в `test_first_load_is_under_the_hard_cap` (`:262`): `total = delivered_size(SHELL_REL) + sum(delivered_size(rel) for rel in sorted(urls))`, а съобщението назовава двете събираеми. `EXPECTED_EAGER` **не се пипа** (`index.html` не е `fetch`, а обвивката). Докстрингът `:41-43` и `:48-51` получават по един ред. **Нула пиннати байтове** — инвариантът на файла остава.
**Гейт Г-H10:** `python -m unittest tests.test_first_load_budget` → **Ran 3, OK**. **Отрицателна половина:** `FIRE_VARNA_FIRST_LOAD_BUDGET=1500000` → изход 1 със съобщение „първото зареждане е **1920525** B при таван 1500000 B“ — то е и мярката (товарът сам е 1 324 589 B и би минал).
```
test(budget): the first load carries index.html, as AGENTS.md says

AGENTS.md:53 defines the first load as index.html + data/hydrants.json
and AGENTS.md:137 caps it at 5 MB, but the gate summed only the eager
fetch urls (:262-267), i.e. the payload alone - a gate that did not
measure what it claimed. The shell joins the sum through the existing
delivered_size(), which reads the blob a push publishes, and the failure
message now names both summands. EXPECTED_EAGER does not move: the shell
is not fetched, it is the page. Zero pinned byte counts, as before.

The negative half is the env one: FIRE_VARNA_FIRST_LOAD_BUDGET=1500000
goes red on 1920525 B, which the payload alone (1324589 B) would have
walked past - that is the proof the shell is really inside the sum.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

### К6 · D-25 — пръстен от именувани точки е геометрия (`gates/probe/b3_g11.py`, нов тест)
**Редакцията (адитивна, `walk` остава байт за байт):** нова функция `named_ring(o, path, hits)` + константи `RING_MIN = 4`, `LAT_NAMES = {"lat","latitude","y"}`, `LON_NAMES = {"lon","lng","longitude","x"}`. Попадение = **списък с ≥ 4 елемента, всеки от които е речник, чиито ключове са ПОДМНОЖЕСТВО на двата речника и съдържат поне едно име от всеки**. В `main` (`:170`): `hits = []; walk(doc, "", hits); named_ring(doc, "", hits)` — същият `bad`, същият печатан JSON, същите изходи. Докстрингът `:6` става „with THREE declared extensions“ + абзац защо (§1 редове 10–12).
**Защо тази форма:** ред на място (`name`+`lat`+`lon`) има ключ извън речника → **не** пали; полигон, скрит под безобидно име, пали. `LEGACY` не се пипа.
**`ALLOWED_NEW` (v1.1):** К6 вписва и `tests/test_b3_g11_named_ring.py` в `ALLOWED_NEW` в същия комит (Astra: без този ред Г4 дава `stray_new`, изход 1).
**Гейт Г-D25** (`tests/test_b3_g11_named_ring.py`, нов; гейт-модулът се зарежда през `FIRE_VARNA_B3_G11_PATH`): (1) фалшив пръстен от 4 обекта `{"lat":1.0,"lon":2.0}`… → 1 попадение с верния указател; (2) същото с 3 обекта → 0 (прагът е честен); (3) 150 обекта с ключове `name`+`lat`+`lon` → 0 (формата на живите товари остава зелена).
**Отрицателна половина:** копие на гейта в `$TEMP/fv_lot6/d25` с `RING_MIN = 99` → подпроцес с `FIRE_VARNA_B3_G11_PATH=<копието>` → изход 1 с иглата на твърдение 1.
**Задължителна мярка ПРЕДИ кода (Ф0-5):** правилото върху реалния обхват на гейта → **точно 0 попадения**. Различно число → **STOP-5** (правилото не се отслабва тайно).
```
fix(gate): a ring of named points is geometry too (B3 G11)

Measured at 37db9ca: the privacy rule the backlog calls "a list of >= 4
numbers" was never delivered - it stands in ПЛАН_Картата_imot_08.09.md
and nowhere in the code. G11 judges by three key NAMES (GEO, :37), so a
polygon serialised as [{"lat":..,"lon":..}, ...] under a harmless name
walks straight through it. The gate gains a THIRD declared extension of
the signed S29-4 script: named_ring() flags a list of four or more
dicts whose keys are a subset of the lat/lon vocabulary. walk(), GEO,
LEGACY and the printed JSON keep their signed shape byte for byte.

The rule judges the SHAPE of the list, not the name of the key, on
purpose: places.json, hotels.json and removed_hydrants.json carry lat
and lon lawfully, and a row with a name key is not a vertex. The bare
numeric ring stays out - it would fire on the two address payloads and
make the gate red by construction; that is its own row (backlog D-25b).
Measured before the edit: zero hits over the whole tracked scope.

tests/test_b3_g11_named_ring.py pins the hit, the honest threshold and
the live row shape on obviously fake numbers; its negative half raises
the threshold in a copy outside the tree and was measured to fall.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

---

## §3 · Гейтове на целия лот (след последния комит)

- **Г1 · Пакетът:** `python -m unittest discover -s tests` → **Ran 443, OK** (N₀ 435 + 2 H-1 + 2 F-7 + 4 D-25; H-10 и F-15 не раждат методи). Друга разлика без обяснение → STOP-6. **Без регреси:** нито един тест на базата не се маха и не се пренастройва.
- **Г2 · Гейтовете:** `python -m gates.run_gates` → 0, седем отметки. **Г3 · Доставката:** `python -m gates.release` → 0 делти (отделно и след Г2).
- **Г4 · B3:** `PYTHONIOENCODING=utf-8 python gates/probe/b3_g11.py 37db9ca` → **0**, `geo_hits: []`, `stray_new: []` (това е и живият тест на новото правило върху целия обхват).
- **Г5 · Приватност:** `python gates/probe/ib1_g15.py --added-lines 37db9ca -- <всеки изменен път>` (с доказано непразен диф) + целофайлово за трите нови файла → `IB1-G15 OK … hits=0`.
- **Г6 · Периметърът:** `git diff --name-only 37db9ca..HEAD | sort` → точно **девет** пътя (v1.1; планът, `recall_sweep.py`, четири тестови файла — `test_recall_sweep_import_clean.py`, `test_hydrant_core.py`, `test_first_load_budget.py`, `test_b3_g11_named_ring.py` — `hydrant_core.py`, `moderation_log.md`, `b3_g11.py`); после `git diff --quiet 37db9ca..HEAD -- data index.html worker sw.js tests/golden_apply_v0.py tests/negative_halves_manifest.json tests/test_negative_halves.py gates/release.py gates/run_gates.py gates/probe/lot_perimeter.py gates/probe/ib1_g15.py scratch/places_search/recall_sweep_rows.json scratch/places_search/expectations.json` → **0**. Половина с доказан диапазон: `git diff --exit-code --stat 8512ab6~1..8512ab6 -- data/` → **1**.
- **Г7 · Кодировка:** `python -m unittest tests.test_ui_ascii_mojibake` → 0; сканът за мохибейк от AGENTS.md върху трите нови файла и дневника → 0.
- **Г8 · Ръцете и редът:** `git log --format='%an <%ae>' 37db9ca..HEAD` → К1/К4 = Architect, останалите = Executor; `git merge-base --is-ancestor` К1 К2.
- **Г9 · Петте половини** (H-1, F-7, H-10, D-25, F-15-пробата) се пускат ПРЕД Одитора и всяка връща червено **със своята игла**.

---

## §4 · Решения за Gate 1 (малки, но твои)

**Р1 · Ръката на К4.** (а) **Architect** — документ, по §0.2 на предните лотове (**← препоръка**); (б) Executor, както беклогът пише „ръка: Executor“. Съдържанието е едно и също.
**Р2 · Обхватът на H-1.** (а) **само `recall_sweep.py`** — единственият модул, който тест или гейт ВНАСЯ (§1 ред 3) (**← препоръка**); (б) и `tests/verify_h4.py`/`verify_apply.py` — те не се откриват от `discover` и не шумят; (в) всичките 158 файла в `scratch/` — не.
**Р3 · D-25 · формата на правилото.** (а) **пръстен от именувани точки** (**← препоръка**, §1 редове 10–12); (б) плюс числовия пръстен — прави гейта червен по конструкция днес; (в) нови имена в `GEO` — прави гейта червен върху `places/hotels`.
**Р4 · F-15 без постоянен тест** — три grep-а при доставката (**← препоръка**); постоянен тест върху исторически ред е данък, а не гейт.

---

## §5 · STOP-условия

1. Дървото не е чисто или `HEAD ≠ main ≠ 37db9ca` → STOP.
2. Ф0 не възпроизвежда §1 (друг N₀ без обяснение, друга котва, друг ред) → STOP: мери се наново, не се кодира.
3. Пробата на H-1 не вижда „ResourceWarning“ дори с `-X dev` върху **днешния** файл → гейтът не може да падне → STOP и питай (кандидат: `tracemalloc`).
4. Паритетният тест или `tests/test_places_search_gate.py` почервенее → STOP; голдънът и замразената референция не се „поправят“.
5. Новото правило на Г11 дава ≠ 0 попадения върху реалния обхват → STOP, доклад до Петър (значи пръстен вече стои в HEAD).
6. Пакетът не дава 443 и разликата не се обяснява с тестове от `main` → STOP.
7. Нужда да се пипне нещо от §0.4, `data/`, `index.html` или `worker/` → STOP.
8. Половина, чиято червенина не е измерена във Ф0 → не се доставя.
9. Фикстура на път към дървото или истинска координата в проследен файл → STOP.

---

## §6 · Какво НЕ е в лота (нарочно)

- **Числовият пръстен „≥ 4 числа“** (`data/address_rows.json`, `approx_addresses_v1.json`) — свой ред с два sha-пина и подпис; тук би направил гейта червен по конструкция.
- **Обявени пропуски на именуваното правило (Astra, v1.1):** пръстен, чиито точки носят и друг ключ (`lat/lon/z`, `lat/lon/id`), съкратени имена (`la`/`lo`), вложени числови двойки `[[lat, lon], …]` — правилото съди подмножество на речника нарочно (§1 ред 12) и не претендира за повече; разширяването е отделен ред, не тихо отслабване.
- **`.md`-файловете и провенансните хешове** (D-33, D-35, D-24) — приватностната купчина има свой корен.
- **Двата теча при ЗАПИС в `recall_sweep.py` (`:4055`, `:4138`)** — не са шум при импорт; отделен ред заедно с H-2.
- **158-те скрипта в `scratch/`, `verify_h4.py`, `verify_apply.py`, `replay_historical_new_hydrant.py`** — скрипт, който тича и излиза, не мърси пакета.
- **`tests/golden_apply_v0.py`** — замразено поведение; разминаването по `parse_error` е обявено в К3 и се доказва от половината.
- **F-3/F-4 (каноничните полета при `wrong_location`)** — затворени в `a3042ed`; тук само дневникът го признава.
- **`docs/activeContext.md`** — един ред след сливането, по думата на Петър (лотът не мени байт на живата карта).
- **`docs/moderation_log.md:385-386`** („поправката чака темата с продукта“) — сочи записа на цикъл #31, който вече носи бележката; не се пренаписва.
- Историята не се пренаписва; непроследен файл не се трие от агент; пушът е само на Петър.

---

## §7 · Ревизия v1.2 — доставката, Gate 2 и К7 (Архитект, 15.09.2026)

**Доставено (Executor):** К2 `b86ca89` (H-1), К3 `6ced4e6` (F-7), К5 `bb7f253` (H-10), К6 `4b93a6e` (D-25); К1 `55ee979` и К4 `5dd2302` (Архитект). Ф0: 435 OK; детето с `-X dev` върху днешния файл дава **три** реда `ResourceWarning`, сочещи `recall_sweep.py:258/:259/:260`; правилото на D-25 върху 19-те блоба на обхвата — **0** попадения; `a3042ed` = 13.09. Гейтовете §3: **Ran 443 OK**; `run_gates` седем отметки; `release` 0 делти; `b3_g11 37db9ca` → `geo_hits []`, `stray_new []`; `ib1_g15 --added-lines` 9 операнда / 0 попадения (567 добавени реда), целофайлово трите нови файла 0; Г6 точно деветте пътя, `diff --quiet` върху data/index.html/worker/sw.js/§0.4 → 0 (index.html блобът е същият като при 37db9ca); Г7 OK; Г8 ръцете верни. Четирите половини падат със своята игла: H-1 „leaked 2 file object(s)“, F-7 `'not_a_hydrant_report' != 'parse_error'` (2 падания), H-10 „1920525 B при таван 1500000 B“ (точно едно), D-25 „named_ring did not flag the ring … /_meta/outline“. Пробата F-15: живо 0/2/1, база 2/–/0.

**Решения на Executor-а, приети от Архитекта (не бяха в §2):** (1) `FIRE_VARNA_RECALL_SWEEP_PATH` в теста на H-1 (огледало на `FIRE_VARNA_INGEST_MODULE`/`FIRE_VARNA_B3_G11_PATH`), за да може половината да сочи копие; (2) регистрация в `sys.modules` преди `exec_module` в зареждащия на F-7 — `@dataclass` чете `sys.modules[cls.__module__]`, иначе `AttributeError`; (3) тестът на D-25 има **четири** метода — четвъртият (`test_the_extension_is_additive`) пинва, че `walk()`/`GEO` продължават да палят и `named_ring` не добавя нищо към тази присъда; (4) правилото е в две функции, `named_point(v)` + `named_ring(o, path, hits)`; (5) коментарът на блока на лот 6 в `ALLOWED_NEW` е преизписан в К6 („И трите се раждат…“). Наблюдения, непипнати: докстрингът на `b3_g11.py` още твърди, че `ALLOWED_NEW` пази подписаната форма „byte for byte“, а всеки лот добавя към него (документен дълг, ред за Док); целофайловата проба върху `tests/test_hydrant_core.py` брои 10 стари попадения (редове 132/144/156/180/337) — същите стоят в базата; Кими провери: **измислена фикстура** (кръгли числа .90000/.20000, най-близък запис на ~231 m, не съвпада с нито един id), не координата на нещо реално; сканът за мохибейк дава едно „попадение“ на `hydrant_core.py:74` — това е самият регекс на детектора (AGENTS.md:242).

**Astra (механика, ultra, върху дифа) — три ръба, нито един не променя присъда върху реалното дърво → К7:** (1) пробата на H-1 би минала тихо, ако модулът под изпитание сам заглуши `sys.stderr` при импорт (детето печата празен stderr → „нула предупреждения“); (2) същата проба съвпада по голия подниз „ResourceWarning“ и пада фалшиво при чуждо предупреждение (напр. `DeprecationWarning`), чийто ТЕКСТ съдържа думата; (3) `named_ring` брои повторно пръстен под ключ от `GEO`, който `walk()` вече е хванал — същият път два пъти в `geo_hits`, присъдата вярна, диагностиката удвоена; четвъртият тест ползва низ за `geometry` и не покрива случая. Не оборено: несъществуващ път и ранен изход на детето → тестът пада (не минава тихо); `cats`/`hotels`/`places2`/`reference_rows()` байт-равни през `gates.release.load_engine()`; идентичността на модулите за другите тестове не се подменя (двата реда на изпълнение по 59 теста); голдънът дава точно двете очаквани падания, нула грешки при импорт; вложени пръстени се намират, лошо оформени елементи не чупят гейта; при инжектиран пръстен под безобиден ключ гейтът сочи точно него; 435 → 443 без грешки при зареждане; `release`/`run_gates` не внасят `b3_g11`.

**Р7 · К7 (Executor, един комит, четири файла):** (а) `tests/test_recall_sweep_import_clean.py`: иглата става категорийната форма, която Python печата за незатворен файл — `: ResourceWarning: unclosed file` — а не голият подниз (оправя и удвоения брой от одита: при `-X dev` всяко предупреждение дава два реда, „leaked 6“ за три манипулатора, и фалшивото червено при чуждо предупреждение с думата в текста); детето печата накрая `STDERR_ALIVE` в stderr и тестът го изисква (заглушен stderr = падане, не минаване); половината (върнат теч) продължава да пада и казва **1**; (б) `scratch/places_search/recall_sweep.py`: трите `with … as handle` блока (оставяха затворен файлов обект като модулен глобал `handle`, какъвто 37db9ca няма — одитът) стават триредова помощна функция `_read_json(path)` + три присвоявания; `cats`/`hotels`/`places2` байт-равни; (в) `gates/probe/b3_g11.py`: `main()` премахва дублите в списъка с попадения, като пази реда (`list(dict.fromkeys(hits))`) — печатаната форма е същата; (г) `tests/test_b3_g11_named_ring.py`: четвъртият тест получава и пръстен под ключ `geometry` и пинва **едно** попадение за него. Съобщение дословно: `scratchpad/lot6/msg_lot6_k7.txt` (`test(lot 6): harden the leak probe and report a GEO ring once`). Гейтове след К7: пакетът **443** (методите не се променят по брой), `run_gates`, `release`, `b3_g11 37db9ca` 0/[]/[], `ib1_g15 --added-lines`, четирите половини наново. Г6 остава девет пътя (К7 пипа четири от тях).

**Кими (данни, Gate 2):** в `data/` има точно две форми „списък от речници с lat/lon“ — `hotels.json /hotels` (225, +15 други ключа) и `places.json /places` (150, +11); чисти именувани пръстени (≥ 4, само речникови ключове) — **0**; вложени числови пръстени `[[число, число], …]` под какъвто и да е ключ в `data/` — **0**; `hydrants.json`/`removed_hydrants.json` ползват `coords: [lon, lat]`, извън правилото. Значи новото правило не е червено по конструкция и обявените му пропуски не крият нищо в данните днес. **Находка извън лота (за семейството П, ред D-24):** проследената папка `scratch/audit_2026-09-03/` (237 файла, комитнати от Петър на 03.09 при системния одит) носи под ключ `pins` пръстени от 4–19 реални варненски двойки в `входове-хидранти/entrance_duplicates.json`, `сгради/dup_physical.json` и `address_duplicates.json` (също `scratch/places_search/probe_out/g4.json`); старото правило не ги хваща (ключът не е в `GEO`), новото — също (числови двойки, не речници). Пиновете на входове и сгради са публични и днес (стоят в `data/search_index.json`), но фактът се записва: 8 проследени файла извън `data/` носят двойки 43.2xxxx/27.9xxxx (най-много `docs/audits/h2_kmz_consolidation_dry_run.json` 4 356, `address_backfill_dry_run_20260511.json` 3 023, `h2_kmz_flag_queue.json` 876 — одитни следи на хидранти, публични данни). Нищо от това не се пипа в лот 6.

**Одитор (Workflow, 4 лещи + присъда, Opus):** **ГОДНО**, нула блокери, три възпроизведени дефекта (не спират доставката): (1) пробата на H-1 брои редове с подниза и при -X dev всяко предупреждение дава два реда — „leaked 6“ за три манипулатора (→ К7); (2) К2 сменя байтовете на recall_sweep.py, а scratch/places_search/granitsi_fixtures_06.09.json носи под _meta.engine.sha256 точно sha-то на базата (0869f12c…, 229 775 B) — пинът беше жив, сега е исторически (HEAD: c33dee23…, 230 012 B); никой тест или гейт не го съди (test_granitsi_fixtures хешира само _meta.inputs; release 0 делти), поведението е байт-равно — ОБЯВЕНО, файлът с данни не се пипа; (3) изречението в К6 „zero hits over the whole tracked scope“ е вярно за собствения обхват на гейта (19-те data/*.json), но върху всичките 121 проследени JSON блоба правилото има едно попадение: scratch/pozarna_gz/px_wgs84.json /points (312 речника {lat, lon} — публичните хидранти от POZARNA.DWG, Златни пясъци); латентно: първият бъдещ комит, който пипне този файл, ще направи Г4 червен (обхват = data/*.json ∪ пипнатите .json) — ОБЯВЕНО, лекът тогава е ред в LEGACY по тройката (файл, указател, тип) или изваждане на файла, не отслабване на правилото. Бележки: модулен глобал handle (→ К7); дублирано попадение под GEO ключ (→ К7); две функции вместо една в подписания гейт-скрипт (адитивни, обявени по-горе); трите env-индиректности не могат да фабрикуват зелено при доставка (куката не чете FIRE_VARNA_*); гейтовете, половините, периметърът (девет пътя, 13-те пътя байт-равни) и авторството потвърдени наново; пакетът 443 по броене на зареждащия, нула грешки.
