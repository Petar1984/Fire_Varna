# ПЛАН · „Проблемите" — **Лот 4в · Размитият ръб на класовата дума + кварталът на бл. 11**

`docs/plans/ПЛАН_Проблемите_лот4в_квартал_13.09.md` · **13.09.2026** · **ревизия v2.3** (v2 след два независими състезателни кръга: 11 опровержения, доказани с команди; v2.1 след Astra — механика; v2.2 след Кими — данни; v2.3 = леджерът на двете лещи върху ДИФА на К3 `0ce4ce7`, §8; леджерите са в §8)

> **ПОДПИСАН от Петър, 13.09.2026 (Gate 1): „подписвам препоръките — ползвай кими и астра“** — Р1 **да** (V3 с пълната измерена цена), Р1-под **(а)** дословният предикат, Р2 **(а)** двата пина през 006b, Р3 **да и двата пазача**, Р4 **(а)**, Р5 **(а)** отделен файл, Р6 **(а)** три реда; като посока за 4в-Б: Р-Б1 **(а)**, Р-Б2 **(а)**, Р-Б3 **нула необяснени делти**, Р-Б4 **(а)** само името на клетката (с изключена гола „1“, трите числа се премерват преди данните), Р-Б5 **(а)** общото правило. Кими и Astra влизат на дифа на К3 (§8). Изпълнителят не пише в този файл. · база **59ffc4e** (= `main` = `problems` = merge-base; лот 4б е комитнат локално и **чака пуша на Петър**) · дърво `C:/git/_wt/fv_problems` · верига: **Планировчик → Gate 1 (Петър) → Изпълнител → Одитор → Кими/Astra → Gate 2 (Петър) → Петър пушва**.

**Клас на риска: 🔴 АРХИТЕКТУРЕН** (топология Вариант А: ADR-амандамент ПРЕДИ кода, отделен Одитор + външна леща). Спусъци: **подписано решение се разширява** (ADR 006 **D12**, вече изменен с 006a) · **логика на основния конвейер** · **два пина в проследен тест се пренастройват** · гейт-файл се редактира (`ALLOWED_NEW`) · промяната мени какво ВИЖДА колега на терен. **Не са спуснати:** нула байта в `data/`, нула публикуване/флаг, нула нов извор, нула кросрепо (4в-Б е БРИФ), нула `worker/`/`sw.js`/двигателя на местата.

**Два подлота с отделни доставки** (Архитект, реш. 1): **4в-А** — само клиент, само вмъкване, нула данни (§0–§10); **4в-Б** — кварталът по полигон става търсим + улицата в етикета (данни + билдър в `Varna_buildings`; тук само БРИФ, §11). Отделно **4г** — кирилските имена на 14-те хотела (§12, не е в този лот).

**CWD за всяка команда без `-C`: `C:/git/_wt/fv_problems`**; основното дърво — `-C C:/git/Fire_Varna`; **python няма `-C`**. **Пред всяка проба и пред `b3_g11`: `PYTHONIOENCODING=utf-8`**. Променливата на модула се задава според обвивката: Git Bash `M=tests.test_address_search_class_like`, PowerShell `$M='tests.test_address_search_class_like'`; **във всяка обвивка аргументът се пише в кавички** — `"$M.ClassLikeTest"` (голото `$M.ClassLikeTest` в PowerShell е достъп до член на низ → `$null` → `unittest` дискъвърва празно с изход 0 = фалшиво зелено).

**Думата на Петър (13.09):** „искам план за поправката — но без да чупим нещо друго." Трите му наблюдения: (1) „хоте" показва пет хотелски адресни реда, докато не напише „л"; (2) „Hotel Color" излиза на латиница; (3) „левски бл 11" не намира блок 11 на ул. Студентска. **(1) е 4в-А. (3) е 4в-Б. (2) е 4г.**

**Провенанс на числата.** „доклад A/B/C" = трите измерени доклада от 13.09. **„атака №N"** = опровержение N от състезателния кръг 13.09 (двама независими агенти, всяко доказано с изпълнена команда). Нищо в този план не е предположено; където няма мярка, пише „не е мерено".

---

## §0 · Червени редове

1. **Агент никога не пуска `git push`** (никакъв флаг, никакво `--no-verify`). Подагентите са на Opus.
2. **Ръцете:** код и тестове → `Claude Executor <executor@local>`; документи → `Claude Architect <architect@local>`; **данни → само Петър**. Идентичността се налага с `-c user.name/-c user.email`; периметърът се съди машинно (Г12, Г13).
3. **Нула байта в `data/`** в целия 4в-А. `data/search_index.json` и `data/address_quarters.json` се само ЧЕТАТ (пазачите на Р3).
4. **Двигателят на МЕСТАТА не се пипа** (`initPlacesSearch`, `place_categories.json`, `recall_sweep`).
5. **Приватност:** нула координати, имена на докладчици, **кадастрални номера**, imot провенанс, сурови дайджести, дълбоки линкове към хидрант в проследени файлове. Сградата на ул. Студентска се назовава с **ord 49241/49242** и **OSM way 118898404** — кадастралният номер се пропуска, макар брифът да го допуска, защото §0.5 на лот 4б го забранява в проследен файл. Всеки НОВ проследен път иска ред в `ALLOWED_NEW` в **същия** комит.
6. **Гейт = команда с очакван изход + отрицателна половина, която ТИЧА и ПАДА.** Половина, чиято червенина не е ИЗМЕРЕНА, не се пише (урок от атаки №1, №2, №3). Тежките прогони — един след друг.
7. **Един отворен лот.** 4в-А започва СЛЕД пуша на 4б. Ако 4б не е пушнат → STOP-1.
8. **ADR 006 D12 се разширява с ОТДЕЛЕН датиран амандамент 006b** (прецедент 006a `:165`); тялото на D12 не се пренаписва — под него влиза втори ред-указател.
9. **Правилото е ОБЕДИНЕНИЕ с точния набор на 006a, не замяна.**
10. **Червен гейт с причина извън лота → STOP и питай.** Изключение: двата пина на 4б, пренастроени **със знанието на подписа** през амандамент (Р2) — всяко друго пипане на тест е STOP.
11. **Целофайловият периметър е ПРОБА при доставката, не постоянен тест** (урок от 4б). Базата се пинова като литерал под името на лота: `FIRE_VARNA_LOT4V_BASE=59ffc4e`, никога `FIRE_VARNA_BASE_COMMIT`.
12. Subject: английски, ≤ 72 знака; `git add` само с явни пътеки; диапазоните в кавички `"$(git merge-base main HEAD)..HEAD"`.

---

## §1 · Мярката на базата

| Мярка | Стойност | Извор |
|---|---|---|
| база | `HEAD` = `main` = `problems` = **59ffc4e**; 4б комитнат, непушнат | брифът · `git rev-parse --short HEAD main` |
| **живата проба при 59ffc4e** (харнес `ask_client`, limit 10) | **х 3** · **хо 5** · **хот 5** · **хоте 5** · хотел 0 · хотели 0 · **хотек 5** · **хостел 5** · „хоте бриз" 3 · мотел 0 · мотела 0 · **апартамент 1** · **апартаменти 1** · **семее 1** · семеен 0 · апарт 0 · **апартх 1** · **апартхот 1** | доклад C §2.1 |
| защо гардът на 4б не ги лови | `CLASS_WORD_TOKENS` е ТОЧЕН набор (`index.html:5383`), матчърът не е: `matchKindSet` (`:5043`) дава 3 = точно, 2 = представка, 1 = Левенщайн (`cap = qt.length>=7 ? 2 : 1`; при дължина ≤ 1 само точно, при ≤ 3 без размитото рамо) | доклад C §2.2 · `index.html:5043` |
| речникът | `search_index.json` vocab = **2 889**; от 19-те подписани класови форми съществуват само **3**: `hotel`, `aparthotel`, `apartamenten` | доклад C §2.2 |
| ръбът | `matchKindSet('hote')` = **{hotel: 2, home: 1}**; `home/homes` са ЖИВИ токени в 12 записа (ords 3951–3961, 46743) | доклад C §2.2 |
| **V1** (правилото както бе зададено) | лекува седем от осемте, но **„хоте" остава 5** — точно клавишът на оплакването | доклад C §2.2 |
| **V3, четириредовият прототип** | същите + **„хоте" 5 → 0**; +600 B, 4 вмъкнати реда, НУЛА променени | доклад C §2.3–2.4 |
| **V3 без ранния изход (ТРИ реда) — това, което се доставя** | копие без ред „`if (toks.every(… classLike[qi] …)) return [];`" е **байт-идентично на четириредовия кандидат по 43 заявки** (и по 27 в независимия втори прогон): при всички класоподобни токени `matchedOutsideClass` става 0 и **а2-връщането на 4б (`:5446`) вече изпразва списъка** — редът е мъртъв код | **атаки №1 и №6** |
| **остатъкът** | **„х" 3 реда и „хо" 5 реда ОСТАВАТ** (`h` е истински vocab токен; `ho` вижда `horizont/home/homes/horozov/hortenzia`) | доклад C §2.4 |
| **пълната цена (не е 8 заявки, а 23)** | освен осемте: **апа 2→0 · апар 5→0 · апарта/апартам/апартаме/апартамен/апартаменте/апартамента 1→0 · апартхо 1→0 · апартхоте 1→0 · хостела 5→0** (19 движещи се в 186-заявков прогон) · **хотелск 5→0 · хостели 5→0** (67-заявков прогон) · смесените **хоте ялта 5→1** и **хоте бендита 7→5** | **атаки №9, №4, №5, №8** |
| **единствената загуба на ИСТИНСКИ адресни редове** | „**апар**": базата дава 2 хотелски + **3 реда „кв. Христо Ботев, prof par stoianov 1 / 3 / 11"** (токенът `par` в 16 записа, `lev('apar','par',1)=1`); кандидатът дава **0** | **атака №9** |
| повърхността е ИЗЧЕРПАНА | систематичен скан на всичките **8 050** представки на 2 889-те vocab токена през пренесен `matchKindSet`: точно **14** класоподобни токена; само два сенчат жива лексика — `hote` (home/homes, 12 записа, нито един видим при базата) и `apar` (3 видими реда) | **атака №9** |
| смесените заявки | „хотел бриз" 3 · „хотел шипка" 5 · „парк хотел одесос" 2 · „хотел мак" 5 — **байт-равни**; **„хоте бриз" 3 реда (к.к. Златни пясъци, хотел бриз · жк бриз · ж.к. Бриз, бриз 1) — базата НЕ носи „хотел мак" и кандидатът е байт-равен** (черновата v1 твърдеше обратното — оборено) | доклад C §2.5 · **атаки №3 и №5** |
| обикновените заявки | **10 от 11 байт за байт същите**; мърда само **„хостел" 5 → 0** — пината в `ORDINARY_QUERIES` (`tests/test_address_search_class_words.py:138`) | доклад C §2.5 · атака (непобито) |
| пазачите срещу кандидата | `test_address_quarter_render` + `test_quarter_index_bundle` + `test_negative_halves` + `test_places_search_primitives` + `test_first_load_budget` → **Ran 54, OK** (81,2 s) | доклад C §2.5 · непобито |
| 4б срещу кандидата | 9 теста, **точно 2 червени**: `:138` („хостел" вече не е байт-равна) и `:335` (count `CLASS_WORD_TOKENS` 3 → **5**); **двете отрицателни половини на 4б остават зелени** и при триредовия вариант (гард + а2 махнати → „хотел" пак 5 реда) | доклад C §2.6 · **атака №6** |
| периметърът | `lot_perimeter.py --base 59ffc4e --allow <3-те реда>` → `LOT-PERIMETER OK removed=3`; срещу базата на 4б `--base c8ba4ba --allow <9-те реда>` → `removed=9` | доклад C §2.3 · атака №1 (пресметнато за 3 реда) |
| котвите | `      const numericTok = …` и `          if (blockTyped && toks[qi] === qBlk) qBlkMatched = true;` — **count == 1** всяка в `git show 59ffc4e:index.html` | непобито (преписани от §4 и преброени) |
| референцията | `gates.release` → зелено, 0 делти; `grep -c 'address_rows\|addrSearch\|search_index\|FIRE_VARNA_INDEX_HTML_PATH' gates/release.py` → **0**; корпусът му = 139 различни заявки / 160 реда, всичките на клона на МЕСТАТА | доклад C §2.7 · доклад A §6 |
| адресните гейтове при базата | `tests.test_address_quarter_render` **28 OK** (34,7 s) · `tests.test_address_search_class_words` **9 OK** · `discover -s tests` = **396**, 0 loader грешки | доклад A §0 · непобито |
| **Ф0 (мери се в деня)** | `N₀`, `git rev-parse --short HEAD`, базовите отговори на 41-те пинати заявки, копието без декремента, списъкът на полигонните имена | §4 Ф0 |

---

## §2 · Диагнозата (по ИМЕ на функция; котви при 59ffc4e)

- **Защо петте реда още мигат.** Лот 4б сложи ТОЧЕН набор и два гарда (`:5391` ранен изход за голата класова заявка; `:5442` + `:5446` квалификацията а2). Матчърът обаче връща 2 за представка и 1 за Левенщайн ≤ 1: `хот`/`хоте` (представка на `hotel`), `хотек`/`хостел` (Левенщайн 1) и `апартамент`/`апартх` (представка на `apartamenten`/`aparthotel`) минават покрай набора. `DEBOUNCE_MS = 120` ги прави да мигат при писане.
- **Защо V1 не стига.** „Всичко, което съвпада, е класово" пада на `хоте`: то вижда и `home` (Левенщайн 1), а `home/homes` са живи адресни токени (доклад C §2.2).
- **Какво прави V3.** Токенът е **класоподобен**, когато има поне едно попадение, **нула** попадения от вид ≥ 2 извън класа, и (поне едно класово попадение от вид ≥ 2 **или** нула размити попадения извън класа). Второто рамо е носещо: `хостел` вижда `hotel` само от вид 1 — късата формулировка **не би излекувала „хостел"**.
- **Защо се доставят ТРИ реда, а не четири.** Декрементът сам изпразва `matchedOutsideClass` за класоподобните токени, а а2-връщането на 4б (`:5446`) вече отхвърля записа — затова ранният изход `return []` е **измерено ненаблюдаем** (атаки №1, №6: 43 заявки байт-идентични) и по CLAUDE.md § Code Style („No dead code") не се доставя. Цената е само микрооптимизация: класоподобната заявка минава през класирането и излиза празна.
- **Защо „мотел" остава 0 — поправено.** НЕ заради гарда на `:5391` (черновата v1 твърдеше това; **атаки №2 и №7 го обориха с копие без гарда: мотел/мотела/хотел си остават 0**). Собственикът е а2-връщането: `motel` Е в набора, затова `:5442` не увеличава брояча и записът се връща. Обединението обаче остава истинско: `classLike` отхвърля точния набор на първия си ред, иначе `classLike('hotel')` би било истина (ms = {hotel: 3}) и декрементът щеше да се прилага и за самата подписана дума — тогава за „хотел бриз" броячът става 1 − 1 = 0 и а2-връщането би изхвърлило истинския запис. **Това е ИЗВОД от кода, не мярка** — затова редът се пази структурно (двойката котва+ред, `count == 1`), не през половина (§8 О2).
- **Защо декрементът е компенсиращ.** Инкрементът на 4б (`:5442`) вдига брояча за всеки токен извън точния набор, значи и за `хоте`. Вместо този ред да се пренаписва (което би счупило дословността на тройката котва+ред на 4б), се вмъква `if (classLike[qi]) matchedOutsideClass -= 1;` след `:5443` — нетно 0 за класоподобния токен.
- **Управлението.** D12 обявява „every function of `initAddressSearch`" за недосегаема; 006a отвори изключение за `runGeocoderSearch` и една константа. 4в остава в същата функция, но разширява подписаното правило и пренастройва два пина в проследен тест → иска **свой датиран амандамент 006b**.
- **Какво НЕ лекува 4в-А.** Наблюдение (3): `address_quarters.json` влиза САМО в показването (`quarterOfHit :5093`, `buildExactItem`, GPS редът, попъпът) и **не се чете** от `prepareIndex`/`runGeocoderSearch`/`scoreEntry`/`dedupeDisplayRows` (доклад A §2) → 4в-Б е индексно семейство (§11).

---

## §3 · Решенията за подпис (Gate 1)

**Р1 · Приема се V3 с ИЗМЕРЕНАТА цена.** Печалба: **хот, хоте, хотек, хостел, апартамент, апартаменти, апартх, апартхот → 0 адресни реда**. Цената, която Петър ще види: (а) **„х" 3 реда, „хо" 5 реда и „ап" 3 реда (единият „апартаментен хотел ялта") остават** — нелечими по това правило (Кими: „ап" е представка и на `apostol`/`aprilov`); (б) **всяка представка/размита форма на класова дума БЕЗ некласов съсед също пада на 0** — форма с некласов Левенщайн-съсед остава: „хотев" (`lev` 1 и до `hotel`, и до `botev`) продължава да дава петте хотелски реда (Кими) — и се пинова като остатък (хотелск, хостели, хостела, апа, апар, апарта…, апартхо, апартхоте — 13 заявки, атаки №4/№9); (в) **смесена заявка с класоПОДОБНА дума губи редовете, дошли само през нея** — „хоте ялта" 5 → 1, „хоте бендита" 7 → 5 (атаки №5/№8); (г) **единствената загуба на истински адреси: „апар" губи трите реда на ул. проф. Пар. Стоянов** (атака №9). Повърхността е изчерпана със скана на 8 050-те представки: само `hote` и `apar` сенчат жива лексика. **„мотел"/„мотела" остават 0** (държат ги гардът `:5391` и а2-връщането на 4б — Кими: и без реда на обединението „мотел" е 0); **редът на обединението е носещ за самите ТОЧНИ думи**: без него `classLike('hotel')` става истина и „хотел бриз" губи истинския ред „хотел бриз" (измерено от Кими) — затова е ОБЕДИНЕНИЕ. **← препоръка: да.** Откат = премахване на трите реда.
**Р1-под · Формулировката.** (а) **измереният предикат дословно** (с рамото „или нула размити попадения извън класа") — лекува и „хостел"; редът е дълъг и е на ръба на „no clever one-liners", но се пише ДОСЛОВНО както е измерен и двукратно препотвърден, защото всяко пренаписване обезсилва мярката; четимостта носят коментарът над него и амандаментът. **← препоръка.** (б) късата форма от брифа — „хостел" остава 5 и цялата §1 се обезсилва → STOP-9.

**Р2 · Двата пина на лот 4б се пренастройват — през амандамент 006b, не мълчаливо.** Пробата `lot_perimeter.py` съди само `index.html` и само вмъквания, затова смяната се обявява поименно:
- **`tests/test_address_search_class_words.py:138`**: `                    u"левски бл 11", u"11", u"хостел")` → `                    u"левски бл 11", u"11")`. „хостел" излиза от `ORDINARY_QUERIES` и влиза в класоподобния метод (пината на 0 реда).
- **`:335`**: `self.assertEqual(candidate.count(u"CLASS_WORD_TOKENS"), 3,` → `, 5,`; **`:336`** („…не три") → „…не пет". Числото 5 е измерено и **не зависи от отпадналия ред** (той не съдържа константата).
- Общо **три променени реда + един коментарен ред (английски)**, нула други байта. **← препоръка: (а) така, с изричния ред в 006b.** (б) „хостел" да остане пината на 5 реда → противоречи на Р1.

**Р3 · Два пазача за данните (червенеят в деня, в който данните счупят правилото).** (а) **Речников пазач:** за токените **`hot`, `hote`, `hostel`, `apar`** нито един токен от `data/search_index.json.vocab` **извън** подписания набор (`DERIVED_TOKENS` на 4б, внесен с модулен внос — един източник на истина) не е **равен** на тях и не **започва** с тях (чисти низови операции, без порт на `lev`), плюс непразност (`vocab` непразен, `hotel` в него). Това е точно рамото „попадение от вид ≥ 2 извън класа"; днес е зелено и непразно (измерено: списъкът е празен за всеки от четирите). **Обявена граница (Кими):** за `hostel`/`hotek` лекуването идва от рамото „нула РАЗМИТИ извън класа", което този пазач не вижда (утрешна улица „Хостен", `lev` 1, го оставя зелен); детекторът за това е функционалният пин `test_a_class_like_token_returns_no_address_rows` (връщат се 5 реда → червен), а пазачът е ранното предупреждение само за near-рамото. (б) **Пазач на полигонните имена (нов, по атака №11):** нито едно име в `data/address_quarters.json` не внася класова дума в адреса — списъкът на имената, чиито кирилски думи попадат в подписания набор, е ЗАМРАЗЕН: очаквано точно **„Бизнес хотел"** (полигон с **416** записа). Ако утре се появи второ такова име, методът червенее ПРЕДИ 4в-Б да го вкара в `qtk`. **← препоръка: да и на двата.**

**Р4 · Какво вижда окото (Г11, 375 px, порт 8001).** Очаква се: „**хот**"/„**хоте**" → петте хотелски адресни реда ги няма; адресният списък се сваля от екрана от `tidyTheirs` (Д6/В5) — **това не е мерено** (доклад C §2.8) и се проверява в браузъра; ако остане гол ред „Няма съвпадения", гейтът НЕ е червен, но фактът се записва и се връща при Петър. „**хостел**" → празно. „**апар**" → празно (обявената загуба — Петър я вижда с очите си). „**хоте ялта**" → един адресен ред („апартаментен хотел ялта"). „**хотел бриз**" → хотелът + 3 адресни реда (поведението на 4б, непроменено). „**х**"/„**хо**" → остатъкът се вижда и се приема съзнателно. Нула нови грешки в конзолата извън CORS към Worker-а; броят хидранти в хедъра непроменен; **никакво подаване на форма**. **← препоръка: (а) приема се както е.**

**Р5 · Новият гейт е ОТДЕЛЕН файл.** (а) `tests/test_address_search_class_like.py` със своя база-литерал, свои вмъкнати редове и свои половини — един лот, една база, един файл. **← препоръка.** (б) в файла на 4б — две бази в един файл, отхвърля се.

**Р6 · Лотът доставя ТРИ реда, не четири (фолд на атаки №1 и №6).** Ранният изход `if (toks.every(… classLike[qi] …)) return [];` **не се доставя**: измерено е ненаблюдаем (43 заявки байт-идентични), значи е мъртъв код, а половината, която го пазеше, беше зелена — гейт, който не може да падне. Следствия: периметърът става `removed=3` (и `removed=9` срещу `c8ba4ba`), `count(u"classLike") == 2`, отрицателните половини стават **три** (две от тях махат декремента и червенеят ДВА различни метода — измерено), пакетът става `N₀ + 11`. **← препоръка: (а) три реда.** (б) четири реда със сляпа половина — отхвърля се (гейт, който никога не пада, е фалшиво зелено).

**Р-Б1 · (4в-Б) Етикетът на ord 49241.** (а) „**кв. Левски, бл. 11, ул. Студентска**" — кварталната дума е това, което колегата чете, а улицата е единственото, което ВСИЧКИ извори потвърждават, и тя лекува латентния дефект с двата еднакви етикета. **← препоръка.** (б) „ул. Студентска, бл. 11" (само това, което подкрепя кадастърът) — по-строго, но картата губи кварталната дума.

**Р-Б2 · (4в-Б) Записаният „Чайка" за 79-те записа в `levski1`.** (а) **остава ТЪРСИМ като свидетел, но НЕ се изписва** в етикета (координатата решава квартала, имената са свидетели; конфликтът отива в леджер, не в данните). Цена: „чайка бл 11" ще носи и двата блока — затова улицата в етикета е задължителна. **← препоръка.** (б) отпада напълно — колега, който помни сградата като „Чайка", спира да я намира.

**Р-Б3 · (4в-Б) Колко променени отговора приемаме.** Мери се ПРЕДИ подписа на данните: харнесът върху `scratch/places_search/ib_corpus_11.09.json` (33 заявки) + 28-те метода на флагманския тест. **← препоръка: нула НЕОБЯСНЕНИ делти** — всяка променена заявка влиза в подписан списък ред по ред с причината си; делта без причина = STOP, данните не се комитват.

**Р-Б4 · (4в-Б) Правилото за токенизация — числата зависят от него (фолд на атака №10).** (а) **само името на клетката** (`Левски 1` → `левски`, `1`): възпроизвежда числата на брифа — **914** нови съвпадения за `levski`, хедър „**4 блока № 11**"; печелят ≥ 1 нов токен **45 548 (52,8 %)**. (б) **име + родителя** („кв. Левски"): **2 318** нови за `levski` (влиза целият Цветен квартал, 1 404 записа, чийто родител е буквално „кв. Левски"), **15** записа с `bl`+`11` в 13 групи, хедър „**5 блока № 11**"; печелят ≥ 1 токен **46 168 (53,5 %)**. (в) име + родител без generic токени: **36 822 (42,7 %)** — НЕОПРЕДЕЛЕНО: generic-списъкът не е изброен никъде и Кими получава 38 883 с правдоподобен списък; (в) не се подписва, докато списъкът не е изписан. **Числото 39 385 (45,7 %) на доклад A не се възпроизвежда под нито едно от трите правила** → преди подписа на данните трите водещи числа се ПРЕМЕРВАТ под подписаното правило (STOP-Б1). Страничен ефект и при (а): клетка 39 се казва „Левски 1" и внася голата цифра **„1"** като търсим токен. **← препоръка: (а) само името, защото е измереното, най-тясното и единственото, което дава декларирания хедър; голата „1" се изключва изрично в билдъра.**

**Р-Б5 · (4в-Б) Полигонът „Бизнес хотел" (фолд на атака №11).** `skel('бизнес хотел') = ['biznes','hotel']`, а `hotel` Е класова дума: ако полигонните токени влязат в `qtk` без филтър, **416** записа получават адресен токен, който 4б обявява за категория — кварталът става НЕДОСТИЖИМ по собственото си име („хотел" → `[]` на `:5391`, „хот"/„хоте" → `[]` през класоподобното правило). (а) **класовите думи се изключват, когато полигонното име влиза в `qtk`** (общото правило). **← препоръка.** (б) само „Бизнес хотел" се изважда поименно — по-тясно, но не пази следващия такъв полигон. И в двата случая пазачът на Р3(б) е замразеният списък, който червенее при трето име.

---

## §4 · Стъпките (ръцете в §6)

**Ф0 · Мярката преди промяната (Изпълнител, read-only, нула комита, копия под `$TEMP/fv_lot4v`).**
- **Ф0.1** `git rev-parse --short HEAD` и `git rev-parse --short main` — и двете = `59ffc4e` (двата ref-а в една команда с `--short` дават `fatal: Needed a single revision` — Astra); `git status --porcelain` празен; `python -m unittest discover -s tests` → **N₀** (очаквано 396).
- **Ф0.2** базовите отговори на 41-те пинати заявки (§4 К3.2) през блоба — сверяват се с §1.
- **Ф0.3** копие с трите реда: осемте → 0; „апар" → 0; „хоте ялта" → 1; „хоте бендита" → 5; „хоте бриз", 10-те обикновени и 4-те „хотел …" байт-равни; „х" 3 / „хо" 5 / „семее" 1. После копие **без декремента**: „хот"/„хоте"/„хостел" пак 5 и „хоте ялта" пак 5 — **иначе половините са слепи → STOP-13**.
- **Ф0.4** списъкът на полигонните имена в `data/address_quarters.json`, чиито кирилски думи са в `lot4b.CLASS_WORDS` → очаквано точно **{„Бизнес хотел"}** (атака №11, 416 записа). Различен списък → STOP-12.
- **Ф0.5** `python -m unittest tests.test_address_search_class_words` срещу копието → 9 теста, **точно 2 червени** (`:138`, `:335`). Трети червен → STOP-6.
Разминаване с §1 → **STOP-2** (мери се наново, не се кодира).

**К1 · Планът (Architect).** Този файл.

**К2 · Амандаментът (Architect).** `docs/decisions/006b_амандамент_4в_13.09.md` по формата на 006a: статус, дата, „Изменя: ADR 006 D12, разширява 006a", контекст с мярката от §1, и решенията — **4в-D1** (изключението остава в `runGeocoderSearch` и константата до нея; **три вмъкнати реда, нула променени**; всяка друга функция остава недосегаема; откат = премахване на трите реда) · **4в-D2** (предикатът дословно + изричният ред, че е **ОБЕДИНЕНИЕ** с точния набор на 4б-D2, и че собственикът на „мотел" е а2-връщането, не гардът) · **4в-D3** (обявената цена: „х" 3 и „хо" 5 остават; 13-те представкови/размити форми падат на 0; „апар" губи трите реда на ул. проф. Пар. Стоянов; смесените „хоте ялта" 5→1 и „хоте бендита" 7→5) · **4в-D4** (двата пренастроени пина на 4б с новите им стойности дословно) · **4в-D5** (двата пазача на Р3 и какво означава червенеенето им). Плюс **един ред-указател** под D12 в `docs/decisions/006_places_in_search.md`, под реда на 006a (`:165`).

**К3 · Кодът, тестът, двата пина и `ALLOWED_NEW` (Executor). Четири файла, един комит.**

**К3.1 · `index.html` — точно ТРИ вмъкнати реда, нула променени.** Котвите са при 59ffc4e и всяка стои **веднъж**:

*Котва 1* — редът `      const numericTok = toks.map(function (t) { return /^[0-9]+$/.test(t); });` (`:5420`). **След него** влизат два реда:
1. `      // A query token is class-like when every vocabulary word it can reach names a CATEGORY too (ADR 006b).`
2. `      const classLike = toks.map(function (t, qi) { if (CLASS_WORD_TOKENS.has(t)) return false; let seen = false, near = false, far = true; for (const v in ms[qi]) { const isClass = CLASS_WORD_TOKENS.has(v); seen = true; if (ms[qi][v] >= 2) { if (!isClass) return false; near = true; } else if (!isClass) { far = false; } } return seen && (near || far); });`

*Котва 2* — редът `          if (blockTyped && toks[qi] === qBlk) qBlkMatched = true;` (`:5443`). **След него** влиза един ред:
3. `          if (classLike[qi]) matchedOutsideClass -= 1;`

Нищо друго не мърда: `norm`, `skel`, `lev`, `matchKindSet`, декларацията `CLASS_WORD_TOKENS`, двата гарда на 4б (`:5391`, `:5446`), инкрементът (`:5442`), `dedupeDisplayRows`, компараторът, `renderResults`, `resolveAndRender`, маркерите Т12, CSS, целият `initPlacesSearch`. Байтовете се ДОКЛАДВАТ след доставката (четириредовият прототип беше +600 B; трите реда са с един ред по-малко) и не се пиноват никъде.

**К3.2 · Новият тест `tests/test_address_search_class_like.py`** (кирилица само в низове; нула I/O на модулно ниво; нула координати, нула кадастрални номера):
- `sys.path.insert(0, …parent)`, после **модулен внос**: `import test_address_quarter_render as flagship` и `import test_address_search_class_words as lot4b` (оттам `DERIVED_TOKENS`, `CLASS_WORDS`, `ORDINARY_QUERIES` — един източник на истина).
- **Базата:** `BASE = os.environ.get("FIRE_VARNA_LOT4V_BASE") or "59ffc4e"`. Неразрешим `git show <BASE>:index.html` → `self.fail`. Отказът „base equals candidate" сравнява блоба с **проследения** `index.html` (`flagship.REPO / "index.html"`), не с `flagship.INDEX`.
- Реюз на `ask_pinned_base`-механиката на 4б (петте маркера Т12 + `flagship.run_probe(test, {…, "exports": flagship.EXPORTS_QUARTER, "asks": asks}, quarters_path=flagship.DELIVERED)`); най-много едно повикване към кандидата и едно към базата на метод.
- **Единайсет метода.**
  `ClassLikeTest` (7): `test_a_class_like_token_returns_no_address_rows` (осемте: базата 5/5/5/5/1/1/1/1 — твърди се и базата — кандидатът 0) · `test_the_declared_price_is_pinned` (13-те представкови/размити форми с базовите си числа → 0, а за „апар" се твърди и че базата носи иглата `par stoianov`, която кандидатът губи; плюс остатъкът „х" 3, „хо" 5, „семее" 1 **байт-равен** — цената не може да се промени мълчаливо) · `test_ordinary_queries_are_byte_equal_to_the_base` (`lot4b.ORDINARY_QUERIES` след пренастройката — 10 заявки — + четирите „хотел бриз/шипка/парк хотел одесос/хотел мак") · `test_a_class_like_word_with_a_name_drops_only_the_class_like_rows` („хоте ялта" база 5 → кандидат 1, като базата носи „хотел мак", а кандидатът — само „апартаментен хотел ялта"; „хоте бендита" база 7 → кандидат 5; **„хоте бриз" база 3 → байт-равна**, за да се види, че не всяка смесена заявка мърда; всяко базово число се твърди — мъртъв пин → `self.fail` и STOP-14) · `test_the_exact_set_still_owns_its_words` („мотел", „мотела", „семеен", „апарт" → 0; **обявено: това е регресионен пин, зелен и при базата** — доказателството за обединението е структурният пин на реда, не половина) · `test_the_vocabulary_carries_nothing_class_like_outside_the_class` (Р3а) · `test_no_polygon_name_collides_with_the_signed_class_set` (Р3б; замразеният списък от Ф0.4).
  `PerimeterTest` (1): `test_the_three_lines_are_the_signed_ones` — двете двойки **котва + вмъкнат ред** като един низ с `count == 1` (двойка 1 = котва 1 + ред 1 + ред 2; двойка 2 = котва 2 + ред 3 + `          if (best === 3) {`), плюс `candidate.count(u"classLike") == 2`. Броят на `CLASS_WORD_TOKENS` се пинова САМО във файла на 4б (`:335` = 5) — никаква втора истина.
  `NegativeHalfTest` (3, подпроцес върху копие под `$TEMP/fv_lot4v_fixtures`, очакван изход 1 + **игла = текстът на причината на самото твърдение** (не името на метода — то стои и в traceback на грешка ПРЕДИ твърдението, Astra), `run_half` първо твърди, че копието съществува, и НИКОГА „base equals candidate"): **H1** `test_removing_the_decrement_turns_the_class_like_method_red` (маха се ред 3 → „хот"/„хоте"/„хостел" пак дават 5 — измерено) · **H2** `test_removing_the_decrement_turns_the_mixed_method_red` (същото копие, друг метод: „хоте ялта" пак дава 5 — измерено) · **H3** `test_a_byte_in_an_untouched_function_turns_the_delivery_probe_red` (копие с един байт в `function dedupeDisplayRows(rows) {` (`:5314`); подпроцесът е пробата с `--base 59ffc4e --allow <3-те реда> --path <копието>` → изход 1 с иглата `first difference at line`).

**К3.3 · `tests/test_address_search_class_words.py` — трите реда на Р2** (`:138`, `:335`, `:336`) + един коментарен ред на английски до `ORDINARY_QUERIES`, че „хостел" е преместена от ADR 006b. Нула други байта.

**К3.4 · `gates/probe/b3_g11.py`, `ALLOWED_NEW`** — три реда с коментар кой лот ги ражда: `tests/test_address_search_class_like.py`, `docs/plans/ПЛАН_Проблемите_лот4в_квартал_13.09.md`, `docs/decisions/006b_амандамент_4в_13.09.md`.

**К4 · `docs/activeContext.md` (Architect, в ОСНОВНОТО дърво, след `merge --ff-only`, преди пуша). Един ред:**
`- **13.09 · Проблемите, лот 4в-А (размитият ръб):** „хот", „хоте", „хотек", „хостел", „апартамент" вече не носят хотелски адресни редове — класоподобният токен не е адрес (ADR 006b); „х", „хо" остават, а „апар" губи три реда по обявената цена. Комит <id>, index.html <размер> B; нула байта в data/.`

---

## §5 · Гейтовете (CWD = дървото; тежките един след друг; `M`/`$M` = `tests.test_address_search_class_like`)

Командите са в Git Bash форма. В PowerShell: `PYTHONIOENCODING=utf-8 …` става `$env:PYTHONIOENCODING='utf-8'; …`, `$TEMP` става `$env:TEMP` (голото `$TEMP` в PowerShell е празно и пътят се срива до `/fv_lot4v/…` — Astra), а **всяко `"$M.Class"` е в кавички**.

**Г1 · Класоподобните думи и цената.** `PYTHONIOENCODING=utf-8 python -m unittest "$M.ClassLikeTest"` → изход **0**. **Червено ПРЕДИ:** същата команда с `FIRE_VARNA_INDEX_HTML_PATH=<копие на git show 59ffc4e:index.html>` → изход **1** с иглата **на самата причина** (текстът на assertion-а: „класоподобният токен %r още дава %d адресни реда"), НЕ с името на метода — името стои и в traceback на `FileNotFoundError`/`ImportError`, паднали преди твърдението (Astra го възпроизведе с несъществуващ `FIRE_VARNA_INDEX_HTML_PATH`: изход 1, името в изхода, нула поведение); ако пише „base equals candidate" → STOP-5.
**Г2 · Периметърът.** (а) `python -m unittest "$M.PerimeterTest"` → **0**. (б) `PYTHONIOENCODING=utf-8 python gates/probe/lot_perimeter.py --base 59ffc4e --allow "$TEMP/fv_lot4v/allow_lines.txt"` → **0**, `LOT-PERIMETER OK removed=3 base=59ffc4e`; allow-файлът носи дословно трите реда, преписани от §4 К3.1 (Одиторът ги преписва сам от плана, не от дифа). (в) кръстосано: `--base c8ba4ba --allow "$TEMP/fv_lot4v/allow_lines_9.txt"` → **0**, `removed=9`. Половина: Г4.
**Г3 · Половина H1.** `python -m unittest "$M.NegativeHalfTest.test_removing_the_decrement_turns_the_class_like_method_red"` → **0**.
**Г3б · Половина H2.** `python -m unittest "$M.NegativeHalfTest.test_removing_the_decrement_turns_the_mixed_method_red"` → **0**.
**Г4 · Половина H3 (пробата пада).** `python -m unittest "$M.NegativeHalfTest.test_a_byte_in_an_untouched_function_turns_the_delivery_probe_red"` → **0**. Плюс ръчно: `--allow` с един ред по-малко → изход **1** с `first difference at line N`; крив ред (0 или 2 пъти в кандидата, или вече в базата), празен allow-файл, неразрешима база, кандидат == база → изход **2**.
**Г5 · Пазачите остават зелени.** `python -m unittest tests.test_address_quarter_render tests.test_quarter_index_bundle tests.test_negative_halves tests.test_places_search_primitives tests.test_first_load_budget` → **Ran 54, OK** (≈ 81 s). Половина: **собствените половини на пазачите** (`tests/test_negative_halves.py` върху манифеста, 23 половини) — Г4 доказва пробата, не 54-те теста (Astra).
**Г5б · Гейтът на 4б след пренастройката.** `python -m unittest tests.test_address_search_class_words` → **Ran 9, OK**. Червеното ПРЕДИ е вече измерено (точно 2 червени, Ф0.5); ТРЕТИ червен метод = регресия → STOP-6.
**Г6 · Пакетът.** `python -m unittest discover -s tests` → **Ran N₀ + 11 · OK** (очаквано 407 = 396 + 7 + 1 + 3). Друга разлика, необяснена с тестове от `main` (изброени с `-v`) → STOP-7.
**Г7 · Гейтовете.** `python -m gates.run_gates` → **0**, **7/7**.
**Г8 · Доставката.** `python -m gates.release` → **0 делти** (отделно и последователно от Г7). Обявено: корпусът му е клонът на МЕСТАТА (139 заявки) — зелено тук не доказва нищо от §3.
**Г9 · B3.** `PYTHONIOENCODING=utf-8 python gates/probe/b3_g11.py "$(git merge-base main HEAD)"` → **0**, `stray_new: []`. Между К1 и К3 е червен по конструкция и дотогава НЕ е STOP-условие.
**Г10 · Приватност.** Изменени: `python gates/probe/ib1_g15.py --added-lines 59ffc4e -- index.html gates/probe/b3_g11.py tests/test_address_search_class_words.py docs/decisions/006_places_in_search.md` (непразен diff за всеки, доказан с `git diff --unified=0 59ffc4e -- <path>`); нови — целофайлово: `python gates/probe/ib1_g15.py tests/test_address_search_class_like.py docs/plans/ПЛАН_Проблемите_лот4в_квартал_13.09.md docs/decisions/006b_амандамент_4в_13.09.md`. Очаквано `IB1-G15 OK operands=<n> hits=0`. Плюс ръчно: `grep -nE '4[0-9]\.[0-9]{4,}|2[0-9]\.[0-9]{4,}|10135\.[0-9]+' <четирите нови/изменени документа и теста>` → нула реда (нула координати, нула кадастрални номера).
**Г11 · Браузърът** (375 px, порт 8001, launch config `fire-varna-problems-8001`; никакво подаване) — очакванията са в Р4. Екранна снимка извън репото; какво прави `tidyTheirs` за „хот"/„хоте" се ЗАПИСВА дословно; „апар" се показва на Петър изрично.
**Г12 · Редът на комитите и ръцете.** `git merge-base --is-ancestor <К1> <К2>` → **0** и `git merge-base --is-ancestor <К2> <К3>` → **0**; разменени → 1 (само К2→К3 пропуска историята К2→К1→К3 — Astra). **Ръцете се МЕРЯТ, не се предполагат:** `git log -1 --format='%an <%ae>%n%cn <%ce>' <К3>` → точно `Claude Executor <executor@local>` два пъти, `<К1>`/`<К2>` → `Claude Architect <architect@local>` (флаговете `-c user.*` губят от `GIT_AUTHOR_NAME`/`GIT_COMMITTER_NAME` в средата — Astra); друго име → STOP.
**Г13 · Байтовете в `data/` и периметърът по пътища.** `git status --porcelain -- data/` празен · `git diff --exit-code --stat --cached -- data/` → **0** · `git diff --exit-code --stat "$(git merge-base main HEAD)..HEAD" -- data/` → **0** · `git diff --name-only "$(git merge-base main HEAD)..HEAD" | sort` → точно **СЕДЕМ** пътя: `docs/decisions/006_places_in_search.md`, `docs/decisions/006b_амандамент_4в_13.09.md`, `docs/plans/ПЛАН_Проблемите_лот4в_квартал_13.09.md`, `gates/probe/b3_g11.py`, `index.html`, `tests/test_address_search_class_like.py`, `tests/test_address_search_class_words.py` (`docs/activeContext.md` е К4 в основното дърво, извън диапазона) · забранена зона: `git diff --quiet --exit-code "$(git merge-base main HEAD)..HEAD" -- data scratch worker sw.js docs/moderation_log.md tests/negative_halves_manifest.json tests/test_negative_halves.py tests/test_address_quarter_render.py tests/address_slice_probe.mjs tests/test_report_form_operational.py gates/release.py gates/run_gates.py gates/probe/lot_perimeter.py` → **0**.
**Половините на Г13 (с ДОКАЗАН диапазон):** `git diff --exit-code --stat 8512ab6~1..8512ab6 -- data/` → **1** и `git diff --quiet --exit-code 8512ab6~1..8512ab6 -- data scratch worker sw.js …` → **1** (`8512ab6` = `data: ingest cycle #39`, наистина пипа `data/`). **НЕ се ползва `59ffc4e~1..59ffc4e`** — той е комит на 4б и не пипа `data/`: половината би излязла 0 и гейтът щеше да се води доказан, без никога да е падал.
**Г14 · Кодировката.** `python -m unittest tests.test_ui_ascii_mojibake` → **0**; мойбейк-сканът от AGENTS.md върху `index.html`, двата теста и двата документа → 0 маркера.

---

## §6 · Комитите (дословни)

**К1 · Architect** — `git add -- docs/plans/ПЛАН_Проблемите_лот4в_квартал_13.09.md` · `git -c user.name="Claude Architect" -c user.email="architect@local" commit -F <msg извън репото>`
```
docs(plan): lot 4v - a class-like token is not an address either

"хот", "хоте", "хотек" and "хостел" still put five hotel address rows under
the hotel list: the lot 4b guard is an exact set while the matcher also has a
prefix arm and a Levenshtein arm (measured 2026-09-13 at 59ffc4e). The plan
proposes three inserted lines in runGeocoderSearch, amendment 006b, a new gate
with three negative halves that were measured to fall, and two data guards. It
declares the full measured price: "х" and "хо" keep flashing, every prefix and
fuzzy form of a class word drops to zero, and the four-letter "апар" loses
three real address rows. Nothing in data/ changes. The quarter of the block on
ул. Студентска is a separate index-side sub-lot, briefed only.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

**К2 · Architect** — `git add -- docs/decisions/006b_амандамент_4в_13.09.md docs/decisions/006_places_in_search.md` · същите флагове
```
docs(adr): 006b amendment - class-like tokens in the address branch

ADR 006 D12 names every function of initAddressSearch untouchable and 006a
opened one exception for the exact set of class words. This second dated
amendment widens that same exception: runGeocoderSearch gains a derived
class-like predicate over the match kinds it already computes, plus one
compensating decrement, so a query whose every token can only reach category
words returns no address rows. The rule is a UNION with the exact set - the
predicate refuses the signed words on its first line, and the owner of "мотел"
is the a2 return of 4b, not the bare-query guard. The measured price is
written down: "х" and "хо" keep flashing, thirteen prefix and fuzzy forms drop
to zero, "апар" loses three real rows and a mixed query with a class-like word
loses the rows matched only through it. Two pins of the lot 4b test are re-set
here, with the knowledge of this signature.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

**К3 · Executor (един комит)** — `git add -- index.html tests/test_address_search_class_like.py tests/test_address_search_class_words.py gates/probe/b3_g11.py` · `git -c user.name="Claude Executor" -c user.email="executor@local" commit -F <msg>` · после `git status --short`
```
fix(search): a class-like token is not an address (T-01)

"хот", "хоте", "хотек" and "хостел" reach only category words through the
prefix and the Levenshtein arms of matchKindSet, yet the guard of ADR 006a is
an exact set, so all four still returned the five hotel address rows while the
colleague was still typing. Three inserted lines in runGeocoderSearch, zero
changed: a comment, the derived classLike vector over the match kinds already
computed in ms, and one compensating decrement, so an entry matched only
through class-like tokens leaves the ranking through the a2 return of lot 4b.
An early exit was measured to be unobservable given the decrement and is not
shipped. The predicate refuses the signed set on its first line: without that
line "хотел бриз" would lose its real hotel row, and lot 4b's negative halves
still fall.

tests/test_address_search_class_like.py pins the base as a literal under its
own env name, proves the eight class-like queries, pins the declared price
including the three real rows the four-letter "апар" loses, byte-compares the
ordinary and the mixed queries against the base, pins the anchors of the three
lines, guards the shipped vocabulary and the polygon names against future data
and carries three negative halves that run and fail: two remove the decrement
and turn two different methods red, the third turns the delivery probe red.

Two pins of tests/test_address_search_class_words.py are re-set with the
knowledge of the signature (ADR 006b): "хостел" leaves the ordinary queries
and enters the class-like method, and the constant is now mentioned five
times, not three. The new paths are declared in ALLOWED_NEW of
gates/probe/b3_g11.py.

Zero bytes in data/, worker/ and sw.js; initPlacesSearch is untouched.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```

**К4 · Architect, в основното дърво, след `merge --ff-only`** — `git -C C:/git/Fire_Varna add -- docs/activeContext.md` · `git -C C:/git/Fire_Varna -c user.name="Claude Architect" -c user.email="architect@local" commit -F <msg>`
```
docs(state): lot 4v - class-like tokens in the address branch

One line for the lot: what changed, the commit, and the new index.html
size. The stale byte rows of this page stay untouched - backlog D-52.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```
Преди К4: `git -C C:/git/Fire_Varna status --porcelain --untracked-files=no` празен, без прекъснат merge/rebase.

---

## §7 · STOP-условия

1. Лот 4б не е пушнат в момента на изпълнението → STOP, питай.
2. Ф0 не възпроизвежда §1 (друга база, друг брой тестове без обяснение, други базови отговори) → STOP: мери се наново, не се кодира по стар отчет.
3. Пазач на клона става червен (петте модула на Г5) → STOP, тестът не се „поправя".
4. Някоя от двете котви не стои точно веднъж в `index.html` при 59ffc4e, или маркер Т12 липсва в блоба/работния файл → STOP.
5. Червеното ПРЕДИ на Г1 идва с „base equals candidate" вместо с иглата си; или `git show 59ffc4e:index.html` е неразрешим → STOP.
6. Пада ТРЕТИ метод в `tests/test_address_search_class_words.py` (извън двата обявени пина) → STOP.
7. Пакетът не дава `N₀ + 11` и разликата не се обяснява с тестове от `main` → STOP.
8. Нужда да се пипне `initPlacesSearch`, `data/`, `worker/`, `sw.js`, двата товара, манифестът на половините, флагманският тест, `gates/probe/lot_perimeter.py`, или трета функция на `initAddressSearch` → STOP.
9. Петър подпише късата формулировка (Р1-под б) или отхвърли Р2 → планът е нищожен; пише се нов след нова мярка.
10. Координата, име на докладчик, кадастрален номер, imot провенанс, суров дайджест или дълбок линк към хидрант на път към проследен файл → STOP.
11. Червен гейт с причина извън лота; или `git diff --quiet 59ffc4e..main -- index.html` дава 1 след ребазирането (чужда тема е пипнала файла, срещу чийто литерал съди байт-равенството) → STOP.
12. Ф0.4 дава списък, различен от {„Бизнес хотел"} → STOP: пазачът се пинова само по измерен списък и Архитектът решава (не се „разширява", за да мине).
13. Копието без декремента НЕ връща „хот"/„хоте"/„хостел" на 5 или „хоте ялта" на 5 → половините H1/H2 са слепи → STOP: мярката на атаки №1/№6/№8 е остаряла.
14. Базата вече не дава пинатото число за някоя заявка („апар" 5 с иглата `par stoianov`, „хоте ялта" 5, „хоте бендита" 7, „хоте бриз" 3, „х" 3, „хо" 5, „семее" 1) → пинът е мъртъв → STOP и мярка.
15. Появи се движеща се заявка извън 23-те обявени (Г1 я хваща като byte-неравенство) → STOP: цената, която Петър подписа, е непълна.

---

## §8 · Одит и Gate 2

**Леджер на опроверженията (какво влезе в v2 и къде).**

| # | Опровергано твърдение от v1 | Как е сгънато |
|---|---|---|
| №1, №6 | „Половина H1: маха се ранният изход → хот/хоте пак 5" — копието без него е байт-идентично на кандидата (43 и 27 заявки) | **Р6:** редът ОТПАДА от лота (мъртъв код); лотът е 3 реда; `removed=3`/`removed=9`; `count(classLike)==2`; H1 маха ДЕКРЕМЕНТА (измерено червено) |
| №2, №7 | „Половина H2: маха се гардът на 4б → мотел се връща" — мотел остава 0; собственикът е а2-връщането | **H2 е изтрита**; §2 поправен; `test_the_exact_set_still_owns_its_words` е обявен за регресионен пин, зелен и при базата; обединението се пази структурно (двойката котва+ред) + собствената половина на 4б |
| №3, №5, №8 | „«хоте бриз»: базата носи «хотел мак», кандидатът 3 реда без него" — базата НЕ го носи, кандидатът е байт-равен | Смесеният метод се пинова на **„хоте ялта" 5→1** и **„хоте бендита" 7→5**; „хоте бриз" остава като пин за НЕпромяна; H2 (новата) маха декремента и червенее точно този метод |
| №4, №9 | „Цената са осемте заявки + «х»/«хо»" — мърдат 23; „апар" губи 3 ИСТИНСКИ реда | **Р1(б)(в)(г)**, 4в-D3, метод `test_the_declared_price_is_pinned` (13 форми + иглата `par stoianov`), Г11 показва „апар" на Петър, STOP-15 |
| №10 | числата на 4в-Б (914 / 4 блока / 39 385) принадлежат на друго правило за токенизация | **Р-Б4** с трите измерени комплекта (914/4/45 548 · 2 318/5/46 168 · 36 822) + STOP-Б1 „премери под подписаното правило"; голата „1" на клетка „Левски 1" е обявена |
| №11 | 4в-Б е безопасен за предаване както е написан — полигонът „Бизнес хотел" (416 записа) би станал недостижим | **Р-Б5** + нов пазач **Р3(б)** със замразен списък (Ф0.4, STOP-12) |
| (непобито) | `$M='…'` е PowerShell синтаксис в блок, обявен за Git Bash | Преамбюлът на §5 дава и двата синтаксиса (падаше шумно, не тихо) |

**Одиторът (Opus, read-only, различен агент) дава ГОДНО/НЕГОДНО по редове:** (1) дифът на К3 в `index.html` е точно **трите** вмъкнати реда, нула променени; в `test_address_search_class_words.py` — точно три променени реда + един коментар; (2) периметърът е машинно доказан (Г2 а/б/в с allow-файл, преписан от §4 К3.1; Г13 седем пътя); (3) единайсетте метода съществуват с обявените имена и пакетът дава `N₀ + 11`; (4) **трите половини падат пред очите му** (Г3, Г3б, Г4 — пуска ги сам; плюс пробата с крив `--allow` → 2) и половините на Г13 излизат 1; (5) червеното ПРЕДИ на Г1 носи иглата си; (6) предикатът е дословно измереният, включително `if (CLASS_WORD_TOKENS.has(t)) return false;`; (7) 006b носи „обединение, не замяна", ПЪЛНАТА цена (включително „апар" и смесените) и двата пренастроени пина; (8) `ALLOWED_NEW` носи трите нови пътя, `stray_new: []`; (9) ръцете, дословните съобщения, Г12; (10) Г10 с доказано непразен diff и нула координати/кадастрални номера; (11) Г11 цитиран, включително `tidyTheirs` и „апар"; (12) D12 е разширен с амандамент, не пренаписан; (13) **нито един гейт в лота не е зелен по конструкция** — за всеки червен резултат е записана измерената причина.

**Кими (еднократно копие, само одитор):**
```
mkdir -p C:/Users/Petar/AppData/Local/Temp/fv_lot4v
git diff "$(git merge-base main HEAD)..HEAD" > C:/Users/Petar/AppData/Local/Temp/fv_lot4v/lot4v_diff.patch
cp docs/plans/ПЛАН_Проблемите_лот4в_квартал_13.09.md docs/decisions/006b_амандамент_4в_13.09.md C:/Users/Petar/AppData/Local/Temp/fv_lot4v/
bash C:/Projects/Project_wiki/kimi.sh "Одит на лот 4в-А на Fire_Varna (класоподобният токен в адресния клон): сверѝ дифа срещу плана и амандамента — тихо разширяване, гейт или половина, която не може да падне, заявка, която губи адресни редове без да е обявена в цената, разминаване между предиката в кода и в амандамента, път по който се мени data/, и дали пренастроените пинове на 4б са точно двата обявени" ultra C:/Users/Petar/AppData/Local/Temp/fv_lot4v lot4v_diff.patch ПЛАН_Проблемите_лот4в_квартал_13.09.md 006b_амандамент_4в_13.09.md
```
Външната леща (Astra, ultra) получава същия пакет с въпроса „кой от трите реда може да е верен на грешно място и коя заявка мърда, без да е в списъка на цената" — информативно; решава Claude, подписва Петър.

**Пушът е само на Петър; ребазирането, сливането и К4 ги прави Claude по негова дума** — в Git Bash, редовете на блок А свързани с `&&`, така че червен пакет или червен гейт СПИРА преди сливането.
*Блок А (Gate 2):*
```
cd C:/git/_wt/fv_problems && \
git rebase main && \
python -m unittest discover -s tests && \
python -m gates.run_gates && \
git diff --quiet 59ffc4e..main -- index.html && \
git -C C:/git/Fire_Varna merge --ff-only problems
```
*Предаване:* Петър вика Архитекта за К4; после `cd C:/git/Fire_Varna && python gates/probe/ib1_g15.py --added-lines <sha на К4>^ -- docs/activeContext.md` → `IB1-G15 OK operands=1 hits=0`.
*Блок Б (само когато нищо не виси — командата е сама и последна, и е на Петър):*
```
git -C C:/git/Fire_Varna push origin main
```

---

**Леджер на външните лещи (v2.1, 13.09 — Astra, механика; Кими, данни — виж по-долу).** Astra (gpt-6-astra, ultra, read-only върху копие) не успя да пробие: периметъра (трите реда + двете котвени двойки: преместен ред → пробата зелена, двойката 0 → червено; един чужд байт → ред 5314), двата пренастроени пина (никакъв втори пин в `tests/`), базата (лотовият литерал не наследява `FIRE_VARNA_BASE_COMMIT`), Г10 (празен диф е зелен за пробата, но доказаният непразен диф го покрива), атомния комит на 4в-Б. Сгънато от Astra: (1) иглите на половините и на червеното ПРЕДИ са текстът на причината, не името на метода (Г1, К3.2); (2) `git rev-parse --short` с два ref-а пада (Ф0.1); (3) `$TEMP` → `$env:TEMP` в PowerShell (§5); (4) половината на Г5 са манифестните половини, не Г4; (5) Г12 проверява и К1→К2, а ръцете се мерят с `git log --format` (флаговете губят от env); (6) методите без червено ПРЕДИ са обявени поименно: `test_the_exact_set_still_owns_its_words`, `test_ordinary_queries_are_byte_equal_to_the_base` и двата пазача на Р3 са ПИНОВЕ (зелени и при базата, пазят от бъдеща регресия), не гейтове на промяната — гейтовете с червено ПРЕДИ са `test_a_class_like_token_returns_no_address_rows`, `test_the_declared_price_is_pinned`, `test_a_class_like_word_with_a_name_drops_only_the_class_like_rows` и `PerimeterTest`; (7) `ALLOWED_NEW` „в същия комит" срещу редовете на К1/К2, които идват с К3 — известен конфликт на инварианти по прецедента на лот 1 и 4б, обявен, не скрит; (8) в 4в-Б: остарял `ADDRESS_QUARTERS_SHA256` НЕ нулира кварталната дума на живата карта (константата е огледало за гейта; в паметта нулират само `entries_digest`/`entry_count` — измерено от Astra в харнеса), затова редът е: данните в един комит от Петър → константата от Изпълнителя в следващия; и **мярката преди подписа на Р-Б3 има собственик: Архитектът (Claude) я прави върху скреч-копие на дървото с кандидат-товарите ПРЕДИ комита на Петър** и я предава подписана. Известни граници на самата проба (Astra, извън лота, беклог): ред, който е опашка на друг ред без собствен начало-на-ред (`ab` → `aX
b`), минава; редът на allow-редовете при вложени низове има значение (броенето е върху постепенно изрязвания кандидат) — трите реда на 4в не са подниз един на друг (измерено).

**Кими (k3-max, данни; порт на скорера, валидиран срещу 18-те базови числа на §1 и ord-реда на „левски бл 11").** Не успя да намери незаявена загуба на истински адресни редове: скан на 8 056 представки → същите 14 класоподобни токена, само `apar` губи реални редове; `котел`/`семьон`/`мотор` недокоснати; „хост шипка" 5/5 и „хоте бриз" 3 байт-равни; „апар ялта" 5→1. Сгънато от Кими: (1) смесените заявки не само губят — печелят истински редове под тавана („хоте бендита": −4 хотелски, +2 „ул кк зл пясъци х л бонита") → 4в-D3 и `test_the_declared_price_is_pinned` пиноват и ПЕЧАЛБАТА (пълният отговор на „хоте бендита" байт за байт), не само загубата; (2) Р1(б) е стеснена („без некласов съсед") и „хотев" е обявен остатък; (3) остатъкът включва и „ап" 3 реда; (4) атрибуцията „мотел остава 0 заради обединението" е поправена в §1, Р1 и съобщението на К3 — обединението е носещо за точните думи („хотел бриз"); (5) Р3(а) е с обявена граница за far-рамото; (6) в 4в-Б: приемният гейт (2) е сменен (тайбрекът `len(tk)+len(qtk)` пренарежда всичко, което печели `qtk`), (в) на Р-Б4 е неопределено, `_areaVocab` 39–42; голите заявки при правило (а): левски 1 331→2 245, чайка 1 914→2 161, бриз 1 974→2 260, младост 6 025→6 069, възраждане 1 287→1 449, аспарухово 6 916→11 338, галата 362→1 909, център 873→2 567; „левски бл 11" под (а) → ord 49241 на позиция 3, „4 блока № 11"; „чайка бл 11" носи и двата блока (цената на Р-Б2 е реална). В батерия от 623 заявки мърдат 64 — числото 23 е вярно за нашите корпуси; всички извън 23-те са инстанции на класовете (б)/(в). Планът не казва нищо по-силно от доклад B за Студентска бл. 11.

**Леджер на двете лещи върху дифа на К3 = `0ce4ce7` (14.09, след Одитора ГОДНО).** *Astra (механика):* периметърът + котвените двойки държат (всеки от трите реда, преместен в началото на файла → пробата зелена `removed=3`, а двойката 0 → червено; един байт в `dedupeDisplayRows` → ред 5314); съобщенията, ръцете, редът К1→К2→К3 и пътищата на трите комита — точно по §6. Намерено: (1) половините H1/H2 могат да минат по грешна причина САМО ако някой сложи в средата `FIRE_VARNA_LOT4V_BASE` стойност, която съдържа самите текстове на иглите (базата става неразрешима, детето пада с изход 1 преди твърдението и отеква стойността) — изкуствен вход, обявен в беклога, не се поправя тук (поправката е H1/H2 да пинoват `FIRE_VARNA_LOT4V_BASE=<литерала>` в средата на детето — следващ лот); H3 е имунизирана (пробата връща 2); (2) пинът на „хоте бендита" в теста е брой 5 + присъствие на печалбата „бонита", НЕ байт-пин на целия отговор (обърнат ред или сменени `meta`/`html` минават) — текстът на §8/006b, който казваше „пълния отговор", е поправен (006b, гейтовете); (3) 4в-D4 „никой друг файл не пинова тези стойности" се уточнява: новият тест пинова СЛЕДСТВИЕТО (`ORDINARY_AFTER_THE_RESET = 10`), не самата константа — конкурентен пин на броя няма; (4) коментарният ред над предиката („every vocabulary word it can reach names a CATEGORY too") е стенография — far-рамото допуска некласов размит съсед (`hote` → `home`); авторитетът е 4в-D2, коментарът остава (промяна на реда би счупила подписаната дословност). *Кими (данни, порт на предиката върху речника):* нула тихо разширяване (седемте пътя, +3/−0 в `index.html`, +4/−3 в теста на 4б); нито една половина, която не може да падне (провери и обърнат знак на декремента → половината червенее); сканът на 8 056 представки дава точно 14 класоподобни, само `hote` (→ `home`, 1 запис) и `apar` (→ `par`, 16 записа, `prof par stoianov 1/3/11`) сенчат жива лексика; не-представковите заявки затворени структурно (`motel` го държи `kotel` и без първия ред); предикатът = 4в-D2 (i)–(iv) ред по ред; нула hunk-ове в `data/`; пиновете на 4б — точно двата. Границата на Кими: не е пускал харнеса и не вижда цялото `tests/`.

## §9 · Какво НЕ е в лота (4в-А)

- **„х" (3 реда) и „хо" (5 реда)** — обявената цена на Р1. „Нищо да не мига до 5-ия знак" е ДРУГО решение (праг по дължина) и друг лот.
- **Връщането на трите реда на ул. проф. Пар. Стоянов за „апар"** — те се губят по правилото; изключение по дължина на токена не се прави в този лот (то е същият праг по дължина).
- **Ранният изход** (четвъртият ред на прототипа) — отпада по Р6 като мъртъв код; ако някой ден а2-връщането на 4б бъде премахнато, той трябва да се върне заедно с половина, която пада.
- **Кварталът на ord 49241 („левски бл 11")** — 4в-Б, §11. Днешното поведение не мърда от 4в-А с нито един байт.
- **Латентният дефект с еднаквите заглавия:** за „бл 11" се връщат 73 реда, `RESULT_LIMIT = 10` рисува 10; на позиции 55 и 71 стоят два РАЗЛИЧНИ блока с едно и също заглавие „кв. Левски, бл. 11" (ords 32777 и 49241), защото `dedupeDisplayRows` (`:5314`) ключова по ЗАПИСАНИЯ етикет (доклад A §1). Лекува се от улицата в етикета (§11).
- **Имената на хотелите на латиница** — §12 (4г).
- **Семейство 1 (само клиент) за квартала** — отхвърлено с мярка: четири D12-пиннати функции, обръщане на реда в `ensureSearchData` (`:5628-5630`), 11 от 90 полигонни имена липсват в доставения `vocab` и 39–42 — в `_areaVocab` (зависи от токенизацията; Кими мери 39, доклад A — 42); кирилската улица я няма в клиента (доклад A §4).
- **`gates.release` като мярка за адресния клон** — корпусът му е клонът на местата (139 заявки).
- **Известна ерозия, обявена:** половината на 4б `test_a_byte_in_an_untouched_function_turns_the_perimeter_probe_red` пинова `--base c8ba4ba` с allow-файла на 4б; след 4в трите нови реда сами правят пробата червена, значи половината остава зелена по по-слаба причина. Не се пипа (STOP-8); 4в носи собствената си H3 срещу 59ffc4e. В беклога.
- **Приблизителната повърхност** (`APPROX_ADDRESS_SEARCH_ENABLED = false`) — зад флага; периметърът я пинова.
- Историята не се пренаписва; непроследен файл не се трие от агент.

---

## §10 · Преамбюл (AGENTS.md § Planner Plan Preamble Checklist)

**Обхват.** Лот 4в-А (T-01, продължение): класоподобният токен в адресния клон, в `C:/git/_wt/fv_problems` при 59ffc4e; плюс БРИФ за 4в-Б и раздел за 4г, които НЕ се изпълняват по този план.
**Инвентар.** `index.html` · `tests/test_address_search_class_like.py` (нов) · `tests/test_address_search_class_words.py` (три реда + коментар) · `gates/probe/b3_g11.py` · `docs/plans/ПЛАН_Проблемите_лот4в_квартал_13.09.md` (нов) · `docs/decisions/006b_амандамент_4в_13.09.md` (нов) · `docs/decisions/006_places_in_search.md` (един ред) · `docs/activeContext.md` (основното дърво, след сливането).
**Файлове, четени.** Брифът на Архитекта и докладите A (механизъм), B (истина на терена), C (хотели + размит ръб); единайсетте опровержения на състезателния кръг; `index.html` (`:5030` началото на IIFE, `:5037-5043` `norm/skel/lev/matchKindSet`, `:5093` `quarterOfHit`, `:5296-5335` `labelBlockNumber`/`dedupeDisplayRows`, `:5382-5474` константата, `runGeocoderSearch`, `scoreEntry`, компараторът); `tests/test_address_search_class_words.py` (целият: `:60` базата, `:89-117` вмъкнатите редове и котвите, `:136-138` `ORDINARY_QUERIES`, `:236` пазачът на данните, `:329-344` периметърът, `:365-434` половините); `tests/test_address_quarter_render.py` (`run_probe :434`, `ask_client :456`); `gates/probe/lot_perimeter.py` (целият, изходите 0/1/2); `gates/probe/b3_g11.py` (`ALLOWED_NEW :40`); `docs/plans/ПЛАН_Проблемите_лот4б_хотел_13.09.md` (форматът); `docs/decisions/006a_амандамент_4б_13.09.md`; `docs/decisions/006_places_in_search.md` (D12 `:157`, редът на 006a `:165`); `AGENTS.md` (§ Planner Plan Preamble Checklist `:170`, § System Invariants); `CLAUDE.md` (§ Code Style).
**Отрицателни находки.** Втори викащ на `runGeocoderSearch` — няма · тест или гейт, който пинова днешните 5 реда за „хот"/„хоте" — няма (затова Р2 мести само „хостел") · `gates/release.py` не чете адресния клон (0 попадения) · `address_quarters.json` не се чете от нито една функция на класирането · нов `.mjs`, CSS клас или ключ в данните — не трябва · промяна в `data/` — нула · **но НЕ е вярно, че цената е нула**: 23 заявки мърдат, „апар" губи 3 истински реда, два пина се пренастройват · **не е мерено:** какво прави `tidyTheirs` за „хот"/„хоте" (Г11) и какво би станало без изключващия ред в предиката (извод от кода, пазен структурно).
**Цитирани метаданни (дословно).** ADR 006 **D12**: „… every `asr-*` / `search-pin*` selector and every function of `initAddressSearch`." · 006a **4б-D2**: „Изрично НЕ са класови думи „парк" (376 живи адресни реда) и „комплекс" (26)." · `index.html:5382` „A class word names a CATEGORY, not an address; the places branch owns it (ADR 006a)." · `gates/probe/lot_perimeter.py:12` „A whole-file pin is true exactly once, at the delivery of the lot that wrote it." · `CLAUDE.md` § Code Style: „No clever one-liners." и „No dead code." · брифът, реш. 5: „базата се пинова като литерал под env име на лота (`FIRE_VARNA_LOT4V_BASE=59ffc4e`)".

**Леджер на решенията.**

| Решение | Източник | Доказателство | Обратимост | Статус |
|---|---|---|---|---|
| Р1 V3 с пълната цена | доклад C §2.3–2.4 + атаки №4, №9 | 8 излекувани, 23 движещи се, „апар" −3 реда | премахване на 3-те реда | чака Gate 1 |
| Р1-под измереният предикат | доклад C §2.3 | „хостел" минава само през второто рамо | пренаписване на ред 2 | чака Gate 1 |
| Р2 двата пина на 4б през 006b | доклад C §2.6 + непобита проверка | точно 2 червени метода (`:138`, `:335`) | връщане на трите реда | чака Gate 1 |
| Р3 двата пазача | доклад C §2.8 + атака №11 | vocab списъкът празен за 4-те токена; „Бизнес хотел" 416 записа | изтриване на методите | чака Gate 1 |
| Р4 очакванията в браузъра | доклад C §2.8 (не мерено) | Г11 ги мери за пръв път | — | чака Gate 1 |
| Р5 отделен тестов файл | §0.11, прецедент 4б | една база на файл | изтриване на файла + реда в `ALLOWED_NEW` | чака Gate 1 |
| Р6 три реда вместо четири | атаки №1, №6 | 43 заявки байт-идентични без реда | добавяне на реда + негова половина | чака Gate 1 |
| Р-Б1/Р-Б2/Р-Б3/Р-Б4/Р-Б5 (4в-Б) | доклади A §4–5, B §6–7, атаки №10, №11 | 914/4 срещу 2 318/5; 79 записа в `levski1`; 416 в „Бизнес хотел" | планът на 4в-Б още не е писан | чака Gate 1 (посока) |
| „нула байта в `data/`" | §0.3 | съдено машинно (Г13) | — | действащо |

**Гейтове за одобрение.** Gate 1 = подписът по §3 (Р1, Р1-под, Р2, Р3, Р4, Р5, Р6 за 4в-А; Р-Б1..Р-Б5 като посока). Gate 2 = Одитор + Кими/Astra + Г11; ребазирането, сливането и К4 ги прави Claude; **пушът е само на Петър**.
**Отворени въпроси (не блокират 4в-А).** (1) Сваля ли `tidyTheirs` празния адресен списък за „хот"/„хоте" — Г11 го записва. (2) Струва ли „апар" изключение по дължина — отделно решение, отделен лот. (3) Дали „чайка бл 11" трябва да носи и двата блока — Р-Б2, в 4в-Б.

---

## §11 · Подлот 4в-Б · БРИФ (не е изпълним по този план)

**Целта.** „левски бл 11" да намира блок 11 на ул. Студентска (ords 49241/49242), а етикетът да различава двата блока с еднакво заглавие.

**Земята (доклад B).** Сградата е истинска: **OSM way 118898404** (`addr:street=Студентска`, `бл. 11`, 17 етажа, непроменен адрес от 2016, 10 версии, 6 картографа); кадастърът дава „ул. СТУДЕНТСКА-, бл. 11", 17 етажа, **празен `quarname`**; точката е ВЪТРЕ в подписания полигон `levski1` (79,7 m от ръба, а отвъд ръба е **Бриз**), **264 m** извън Чайка; истинският Чайка бл. 11 (ords 38035/38036, 6 етажа) е на **879 m**. Записаният „Чайка" е доказан артефакт на „най-близък place-възел" (709 m, confidence low; 1014/709 = 1,43 = записаният margin_ratio) — същата грешка прави и Nominatim. **Никой регистър не дава квартал на тази сграда.** Обхват: **79** записа с етикет „кв. Чайка" лежат в `levski1`, **17** от тях са серията Студентска бл. 11–14 (ords 49241–49258).

**Механизмът (доклад A §1).** „левски бл 11" → 8 реда, ord 49241 не е сред тях: токенът `levski` не стои в нито едно търсимо поле (`tk/alias_tk/qtk/dtk/stk`) → `all=false`, а `all` е ПЪРВИЯТ ключ на сортирането (`:5462`); `SEARCH_LIMIT=8` (`:5351`) реже.

**Защо индексно, а не клиентски.** Индексното семейство пипа **нула функции** на `initAddressSearch` → **без D12 амандамент**. Това е най-силният му аргумент (доклад A §5).

**Изисквания към билдъра** (`C:/git/Varna_buildings/js/build_fire_varna_search_index.mjs` + `lib_fire_varna_block_labels.mjs`; отделна сесия, отделен план):
1. Полигонният квартал влиза в `qtk` на всеки запис с клетка — **по правилото, подписано в Р-Б4**, и числата се премерват под него (STOP-Б1).
2. **Класовите думи се изключват** от полигонните токени (Р-Б5): иначе „Бизнес хотел" (416 записа) става недостижим по собственото си име.
3. Колизионният ключ за добавянето на улицата се смята върху **полигонния** квартал, не върху записания (`lib_fire_varna_block_labels.mjs:184-206`).
4. `extractStreet` (`:107-125`) днес иска „ул./бул." в началото; „СТУДЕНТСКА-, бл. 11" няма такова начало. Разхлабването се мери върху **всичките 1027** блокови ключа (184 колидиращи, 510 групи, 448 голи двусегментни етикета, от които само 8 носят кирилска улица) ПРЕДИ да се пипне — отделна мярка за подпис.
5. Новите `qtk` токени влизат и във `vocab` (днес 2 889) — иначе `matchKindSet` не ги вижда.

**Данните (само Петър, ЕДИН комит):** `data/search_index.json` + `data/address_quarters.json` + `data/generation_manifest.json` заедно. Причината е fail-closed: `validateQuarterIndex` отхвърля целия квартален файл, ако `entries_digest`/`entry_count` (86 232) не съвпаднат със заредения индекс — прегенериране на единия без другия убива кварталната дума на четирите повърхности **мълчаливо**.

**Изпълнителят в `Fire_Varna` (след комита на данните):** `ADDRESS_QUARTERS_SHA256` (доклад A §5: `index.html:4839`, проверка 1 в `gates/run_gates.py:144` — сверява се по ИМЕ, не по номер на ред) и каквито пинове гейтовете поискат; ребазиране на флагманския `tests/test_address_quarter_render.py` (28 метода) с **ПОДПИСАН списък на променените отговори**.

**Приемни гейтове** (харнес: `ib_corpus_11.09.json` 33 заявки + 28-те метода): (1) „левски бл 11" връща групата на ord 49241 в първите 8 и етикетът ѝ съдържа „Студентска"; (2) „студентска 11" и „бл 11 студентска" носят ord 49241 на ПЪРВО място (Кими: байт-равенство е невъзможно под правило (а) — тайбрекът `len(tk)+len(qtk)` в компаратора наказва всеки запис, спечелил полигонен токен, и пренарежда дори заявки без квартална дума; планът на 4в-Б трябва да премери този ефект и да реши дали полигонните токени влизат в `qtk`, или в поле извън тайбрека, напр. `stk`/`dtk`, ако скорерът ги чете без наказание); (3) нито един от 79-те записа в `levski1` не носи етикет „кв. Чайка"; (4) за „бл 11" няма два видими реда с еднакво заглавие; (5) „хотел", „хот", „хоте" продължават да връщат празен адресен списък, а полигонът „Бизнес хотел" е достижим по „бизнес" (пазачът на Р3б); (6) делтите — по Р-Б3: нула необяснени; (7) клонът на местата (139 заявки) не мърда по конструкция.

**STOP-Б1.** Ако премерените под подписаното правило числа се разминат с Р-Б4, данните НЕ се комитват и решението се връща при Петър.

---

## §12 · 4г · Имената на хотелите (не е в този лот)

14 от 225 имена в `data/hotels.json` са само на латиница (всички с `src` „Sol/OSM идентификация", `old_names = []`); търсенето на кирилица ги намира („колор" → Hotel Color през Левенщайн 1), **само надписът е латински** (доклад C §1.1–1.2). Пътят е ДАННИ по вече съществуващия образец: кирилица и латиница в ЕДНОТО поле `name` („Hotel Color / Хотел Колор"), както при осемте записа, които вече го правят — **без** `old_names`, защото картата ги изписва „· старо: X", а за транслитерация това е фалшиво твърдение, и `ALIAS_SRC_LINE` няма ред „наша транслитерация". **Петър комитва `hotels.json`; Изпълнителят сменя три гейтнати константи:** `HOTELS_SHA256` (`index.html:6580`), `LEGACY_BUNDLE_SHA.hotels` (`:6607`), `PLACES_CACHE` (`:6557`, v6 → v7). Гейт: `tests.test_places_search_primitives` + `gates.release` с 0 делти. Нула редове в двигателя на местата.