# ПЛАН · Картата imot — под-лот **И-Б1** „Кварталът на ВСЕКИ адрес в живата карта по полигоните на v2“

> **ПОДПИСАН от Петър, 11.09.2026, ревизия `82797b4` (последен комит по пътя преди този ред; Gate 1).** Думата: „подписвам решенията. действай.“ — осемте решения по подразбиране; **Р6 = ДА по същата дума, изрично** (К44-10). Стъпка 3б (`git stash push --staged -m "B3 11.09 parked for I-B2"`) е изпълнена от ръката му на 11.09 22:0x ПРЕДИ 3а — **ИБ1-О17**. ~~ЧЕРНОВА — чака подписа на Петър (Gate 1).~~ Извлечена от `plan_ib/plan_v3.md` (осем кръга, 135 записа ИБ-О) по решението на Архитекта от 11.09 лотът да се раздели: **И-Б1 = адресният път и носителят; местата, хотелите, Б3 и правилата на търсачката → И-Б2 (§11).** Числата са от големия план или премерени наново 11.09. Кръгът с Кими К44 (2 блокера + 8 бележки) е внесен преди Gate 1 — записът е `scratch/places_search/tiles_kimi_K44_11.09.md`. Редът се заменя с „ПОДПИСАН от Петър, <дата>, ревизия <sha>“ (**К1**). Изпълнителят не пише в този файл.

`C:/git/Fire_Varna/docs/plans/ПЛАН_Картата_imot_И-Б1_11.09.md` · 11.09.2026 · Планировчик (само четене) → **подпис на Петър** → Изпълнител → Одитор → преглед → **Gate 2 и пуш от Петър**

**Клас на риска: 🔴 АРХИТЕКТУРЕН** — нов канон за кварталната дума в публичен payload · нов файл в `data/` · радиация `varna_3d → Fire_Varna` (D13) · амандамент на ADR 011 (изключение от **D12**) · стейджът на Б3 стои в дървото и лотът пипа два негови пътя (Р1).

**Думата на Петър (11.09, дословно):** „не само студентска 11 ами **всички адреси да са си по правилните квартали които идентифицирахме**“; „**само гледаме с амендаментите да не счупим нещо**“; „не се тревожи за тавана“. Координатата решава кварталната ДУМА на **ЧЕТИРИТЕ АДРЕСНИ повърхности**: редът (`buildExactItem` `index.html:5411-5423`), панелът (`selectResult:5865-5866` → `renderDetailSheet:5607`), попъпът (`:5871`, `:5876`), GPS-редът (`coordMeta:5027-5033` → `asr-meta :5043`). **ПЕТАТА е производна:** `coordMeta` има ДВА викащи — `:5042` и `:5890` (`openCoordPopup:5884-5900`, М) — тоест GPS-попъпът сменя надписа си ПО КОНСТРУКЦИЯ от Т10 (Г22 го съди). **Картончето и `pl-meta` са И-Б2 (ИБ2-3); в HEAD са `:7264-7267` и `:7236`** (в дървото със стейджа `:7345-7348` и `:7317`; Δ 81, М). Написаната дума остава свидетел и не се трие.

**Обявената цена (Ч `:5201`, `:5207`):** поправя се какво ПИШЕ на реда, не по коя дума се НАМИРА редът (двигателят точкува по `qtk` от билда). „чайка“ пак връща Студентска бл. 11, „левски 1“ не го връща; поправката иска нов билд във `Varna_buildings` → **ИБ2-7**, пазен от **STOP 15**.

**Котви: М** = премерено с команда 11.09 · **М-о** = число на лещите на големия план, възпроизвеждано на назованата стъпка · **Ч** = `път:ред`, сверен 11.09 · **М0** = мери се на стъпка 0 · **О<n>** = присъда ИБ-О<n> в §2.Б на големия план. Числа без котва няма.

---

## 0 · Червени редове

1. **Нищо не се редактира преди подписа** — първата команда е `git status --porcelain`.
2. **Никакъв `git push`** (само Петър), никакъв `--no-verify`.
3. **Данните и подписаните тела ги комитва Петър** (D16, `010:68`): К3, К6, К8 — поименно в амандамента (И-Б1-D5); Ф6 го съди машинно.
4. **Планове и решения — Архитектът**; код и гейтове — Изпълнителят; затварящият документен комит е СЛЕД пуша (D20, `010:76`).
5. **Нула геометрия и нула координати ОТ КВАРТАЛНИЯ ИЗВОР в проследен файл на Fire_Varna** (доставеният файл носи само цели числа и низове, §3.Г). Вече факт в HEAD и извън лота: `address_rows.json`, `pin` в `search_index.json`, `hydrants.json`, `places.json` (**300**), `hotels.json` (**450**) → Г15 има **затворен, поименен** списък операнди (О24).
6. **Написаната дума е свидетел, не данни** — нито един байт на двата товара не се мени (Г8 по sha).
7. **Гейт, който не може да върне ≠ 0, не е гейт**; всяка отрицателна половина ТРЯБВА да е тичала и да е паднала. **Харнес не е гейт:** пробите излизат 0 при всеки вдигнат срез (`granitsi_client_probe.mjs:265`, `:278`) — присъдата е python-тест, който ги държи като ПОДПРОЦЕС (О16).
8. **Изход 1 без машинна присъда = инфраструктурен провал (STOP 5).** Ф1, Ф2, Ф3, Ф10 започват със `sys.stdout.reconfigure(encoding='utf-8', errors='replace')` (образец `gates/coverage.py:660`) и печатат присъда на латиница (иначе кирилица → `UnicodeEncodeError`, изход 1 — М). **`b3_g11.py` е изключение на ниво КОМАНДА** (няма `reconfigure`; печата с `ensure_ascii=False`, `:117-118`) → **всяко повикване носи `PYTHONIOENCODING=utf-8`** (ИБ2-8).
9. **Нула регресии по база** — наборът падащи тестове се обявява по име (D19) и не расте.
10. **Fail-closed + kill switch:** крив ключ/дайджест/дължина → `quarterIndex = null`, нула изключения нагоре; `ADDRESS_QUARTER_V2` е откат с една дума (Р7).
11. **`C:/git/Varna_buildings` не се пипа** (Ч `AGENTS.md:381`) — нужда от ред = **STOP 14**.
12. **Кодът е на латиница**; лицензът не е тема.
13. **Едно дърво, един GIT_DIR** — никакъв комит и никакъв пуш от свързано дърво; `git rev-parse --show-toplevel` преди всеки комит.
14. **Sha на доставка = блобът** (`git show HEAD:<път>` / `:<път>`), никога работното копие.
15. **Позиционното слепване е двойка:** ребилд на `search_index.json` ⇒ ребилд на `address_quarters.json` в СЪЩИЯ комит; клиентът сверява два O(n) дайджеста **върху токените И пина** и при разлика гаси думата (само токените не са отпечатък — §3.Г).
16. **Ефемерните пътища се чистят** (`$IB_TMP/ib_a`, `ib_b`, `ib_fixtures`, `ib_clone`, `ib_b3`) и живеят под **КЪС корен** (§1.1); никакъв `git checkout .`. **Изключение: `…/ib_b3/B3_staged_11.09.patch` НЕ се трие.**

---

## 1 · Мярка (11.09; нула редакции)

### 1.1 · Репата

| | стойност | котва |
|---|---|---|
| `git --version` · HEAD | **2.55.0.windows.2** (`stash push --staged` съществува, Р1) · `Fire_Varna` **74c165f**, `rev-list --count origin/main..main` = **2** | М |
| стейдж (Б3) | **14 пътя: 4 `A`** (`b3_g11.py`, `granitsi_client_probe.mjs`, `test_b3_gates.py`, `granitsi_fixtures_07.09.json`) · **1 `D`** (`…_06.09.json`) · **9 `M`** (`hotels/place_categories/places.json`, `index.html`, `probe_places_fv.mjs`, `recall_sweep.py`, `test_granitsi_fixtures/test_hotels_public_bundle/test_places_public_bundle.py`); поименно с OID-ите в М0 | М |
| **пресечка с лота** | **точно ДВА пътя** — `index.html` и `gates/probe/b3_g11.py` | М |
| мръсни / непроследени | **2** (`…/lot1v_v_reference_manifest.json` 174 717 B, `…/probe_out/token_parity.json`) · **35**; `status --porcelain \| wc -l` = **51** = 14 + 2 + 35 | М |
| **`<БАЗА>`** | HEAD **непосредствено преди К2**, мери се на 3а; не е `74c165f` — К1 ражда нов проследен път (`A`), `changed()` (`b3_g11.py:70-83`) го брои за нов и `:116` пали гейта за грешна причина (О131) | М (3а) |
| чист клонинг `74c165f` | `unittest discover -s tests` → **Ran 315 · OK** (потвърдено) | М |
| **клонингът и `$IB_TMP`** (ИБ1-О9) | `git clone --no-hardlinks --branch <клон> <репо> "$IB_TMP/ib_clone"` → 0, HEAD `74c165f`, `status --porcelain` **0** реда. **`--no-local` е ЗАБРАНЕНА форма тук** (4/4 → `inflate returned -3`, изход 128, при чисти `fsck`/`bundle verify`). Най-дълъг проследен път **145**, лимит **259**, `core.longpaths` unset → **корен ≤ 113** (подразбиращият се 104 минава с 9 знака запас, но всяка подпапка го чупи) | М |
| дървото със стейджа | **Ran 342 · FAILED (24)** за 25,4 s; поименно: **3** в `tests/test_b3_gates.py` (`MapAttributionTest`) + **21** в `tests/test_places_search_gate.py`, който НЕ е в стейджа и е червен заради стейджнатите ДАННИ; наследено червено **0** | М |
| `varna_3d` | `rezhimi`, HEAD `1a0595d`, `status --porcelain` → **42 реда** — базова линия | М |
| `b3_g11.py` · дървета | 7 118 B, на диска, **в HEAD го НЯМА**; `GEO :37`, `LEGACY :39`, `ALLOWED_NEW :40-46`, `stray :116`, `reconfigure` → 0 · `worktree list` дава три дървета, третото е ЕФЕМЕРНО → инвариантът е „няма НОВО дърво“ (О118) | М |

**М0 (стъпка 0):** редовете горе + `ls-files -s -- <14-те пътя>` + `diff --cached --name-status` + 24-те падащи теста поименно **в ДВАТА модула** + `worktree list` в двете репа + изходите на `run_gates`, `release` и двата вектора на Г14 + **`test ${#IB_TMP} -le 40`** + клонингът по пинатата команда + параметрите на среза (Ф9). Разминаване = **STOP 1**. **Г8 и Г11 не тичат тук** (файловете им се раждат в К7 — О22); вместо тях sha на двата товара и `cat-file -s HEAD:index.html` → **560 855**, `…hydrants.json` → **1 321 623**.

**Котвите в `index.html` — ИЗМЕРЕНИ поединично (М, О23).** Б3 не пипа ред ПРЕДИ `:6256`: при **`git diff --cached -U0`** първият хънк е `@@ -6255,0 +6256 @@`, хънковете са **21** (при подразбиращ се контекст **16**), numstat **+106/−24** — формата `-U0` е част от котвата; независимо: `cmp` на първите 6 255 реда на HEAD и дървото мълчи. **Целият адресен път е преди него** (`initAddressSearch` = IIFE `:4836`–`:6237`) → котвите на §3.Б важат в двете дървета. Сверени: `inputEl` 4837 · `resultsEl` 4838 · `baseAddressLabel` 4881 · `formatAddressHit` 4893 · `buildCoordItem` 5035 (`asr-meta :5043`) · `dedupeDisplayRows` 5088 (ключ `:5107`) · `prepareIndex` 5129 (`_ord :5131`) · `renderResults` 5424 · `searchPinIcon` 5469 · `renderDetailSheet` 5607 · `openCoordPopup` 5884 (`coordMeta` на `:5890`) · `buildApproxItem` 6131 (`:6138`) · `fetchCachedJson` 5333 · `SEARCH_INDEX_URL` 5325 · `ADDRESS_ROWS_URL` 5326 · `SEARCH_CACHE` 5327 · `SEARCH_LIMIT` 5125 · `APPROX_…_ENABLED` 5926 · `:4835`, `:4839`, `:6199`, `:6237`, `:1774`. **Котвите НАД `:6256` са от дървото със стейджа** — след 3б важат HEAD-числата (`bits` 7338 → **7257**, картончето 7345-7348 → **7264-7267**, `pl-meta` 7317 → **7236**).

### 1.2 · Живата карта днес

- **Механиката:** двата товара са лениви, при `focus` (`ensureSearchData:5351-5372`); **кварталната дума е ТЕКСТ в `entry.label`** от билда, поле „код“ няма (М). Редът е чип + `formatAddressHit(r)` + `'район ' + districtNames[r.d]`. **`dedupeDisplayRows` строи ключа си от `formatAddressHit(r)`** → редакция само в `buildExactItem` не пипа слепването; **редовете са ПРОЕКЦИИ** (`:5124`) без `_ord` → без Т7 лотът доставя нула думи. **Вторият ред не се ражда без район** (`if (raionName)` `:5420`): **297** записа нямат `d`, **292** от тях падат в клетка → Т8 го сглобява от `bits` (образец HEAD `:7257`).
- **Флагманът (М):** ред `id 45949` „студентска бл 11“, сочен от 0 записа; запис `kind:"mf"`, `tk:["studentska","bl","11"]`, `qtk:["chaika"]`, `d:4`, `label:"кв. Чайка, бл. 11"`; v2 казва **`levski1`** („кв. Левски“). **145 записа с токен `studentska`**, всеки с `pin` → Ф11 е без координата (О25); поименният списък е в Г9.

| мярка | стойност |
|---|---|
| етикети с представка срещу v2 (М-о) | 9 002: **4 842** съвпадат · **4 121** се разминават · 39 вън |
| покритие | 86 232 записа · `label` 43 133 · `display_id` **43 295** (50,2 %) · 41 567 с `pin` = координатата на реда · **1 728** `mf` отместени (медиана 7,38 m, макс 83,70 m; О26) |
| класове етикети (О87) | 9 002–9 071 с представка · **1 717** голо име · 23 113 „гр варна район X“ + 90 „неизвестна“ (26,9 % от ЗАПИСИТЕ) |
| **точка-в-полигон срещу 90-те листа** (М, независим прогон, 2,4 s) | записи **83 896 · 1 · 2 335 = 86 232**; редове **78 235 · 1 · 2 274 = 80 510** — байт в байт с `_meta.mask` |
| **клетки клас `locality`** | **23 от 90** (`KIND_CLASS`, `fire_varna_locations.py:159-163`) → **15 132 записа · 14 823 реда**; не пълнят квартална дума (**D6**, `010:48`) |
| **дисплей-ключ** `norm(formatAddressHit(e))+'\|\|'+(e.g ?? '')` (`:5107`, `norm :4842`) | **27 928** ключа; многоклетъчни **294 / 30 777 записа** (с `-1` като клетка), 238 / 25 777 без него (ИБ1-О1) → в плана стои ТАВАН, точното число го мери Ф2 (Г18). Топ: „местност боровец юг“ 1 947 в 3 клетки · „гр варна район приморски“ 1 633 в 24 |
| **GPS-повърхността** (`nearestAddressTo:5023` → `coordMeta` → `asr-meta :5043`, по РЕДА) | в `normalized_address` **няма нито една запетая** (0 / 80 510) и водещите типови токени са безточкови: местност 3 130 · м 1 284 · кв 673 · жк 573 · со 285 · зона 245 · пз 129 · кк 73 · вз 1 = **6 393** (ИБ1-О4). Големият план брои 6 696 по ДРУГА дефиниция (`plan_v3.md:105`: 4 483 съвпадат · **1 871** противоречат · 342 вън) — знаменателите не се смесват |

### 1.3 · Носителят

**Вариантите (М-о):** **(а)** код-низ като 4-та колона на `address_rows` = 6 115 924 B — билдът пада (`field_order` е точно тройка, Ч `Varna_buildings/js/test_fire_varna_publish_gate.mjs:245-262`), а файлът е Untouchable (`007:13`); **(б)** цял индекс там = 5 305 473 B, същата пречка; **(в)** отделен файл по ЗАПИС ≈ 253 800 B / 15 500 gzip-9; **(в+) същият + `row_cell` (80 510 реда, носителят на GPS-реда) = 486 126 B raw / 27 950 gzip-9 — ИЗБРАНИЯТ**, нов проследен път → ред в `ALLOWED_NEW` (Т13), **под тавана 600 000 B** (Р2); **(г)** поле `q` в `search_index.json` = +594 412 B → ребилд във `Varna_buildings` (**STOP 14**) + `006 D12`.

Ред (в) е мерен от **ПЕТ лещи** (253 738 – 253 794 B) — затова §3.Г пинова извора дословно (О27) и **в плана не стои точен байт, а таван** (STOP 16).

**Разпределението В САМИЯ ФАЙЛ ≠ маската.** Маската (и леджерът Ф5) носят ИСТИНСКАТА клетка: 83 896 · 1 · 2 335 = 86 232 записа и 78 235 · 1 · 2 274 = 80 510 реда. **Ф4 гаси при билда** (§3.Д) → `entry_cell` носи `-1` за **37 839** записа (43,9 %: 2 335 вън ∪ 15 132 locality ∪ 30 777 в многоклетъчен ключ), `row_cell` — за **17 097** реда (21,2 %: 2 274 ∪ 14 823); с полигонна дума остават **48 393 записа** и **63 413 реда** (М). **Байтовете не растат** — `-1` е два знака, колкото и двуцифрен код.

### 1.4 · Таванът

Нетърпеливият `fetch('data/…')` е **един** (`:1774`). Блобовете на HEAD: **560 855 + 1 321 623 = 1 882 478 B** (М); дори при нетърпелив трети товар ≈ 2 368 600 B при таван **5 242 880 B** (5 MB hard cap, `AGENTS.md:137`, в MiB по `docs/activeContext.md:43`; О69). **Машинен гейт НЯМА** (М-о) — ражда се тук (Ф7, Г11).

### 1.5 · Страната varna_3d

- v2: `data/quarters_signed.geojson`, **321 285 B**, sha `64c65f2b…`, **95 Feature-а = 90 листа (`geometry_origin == "imot_bg_points"`) + 5 родителя**, `properties` **41** ключа, **17** листа с `parent_display`; полета `display`, `class` и `_meta.bare_display` **НЕ съществуват** (М). Тай-брейк-числото също не е поле — ИБ1-О12.
- `_meta.mask` (М): `in_one_cell 78235 · tie_break 1 · outside_all_95 2274 · excluded_cell 0 · deferred_cell 0`, `tie_break_rule "най-малък raion_num"`, `address_rows_sha256 2a4766bd…` — възпроизведено независимо, байт в байт; тай-брейкът е САМО по 90-те листа (с 95-те двусмислените стават **6 991**).
- 84 от 90 клетки имат ≥ 1 адрес; шест са с нула (`krushkite, lazur, orehcheto, pripek, pz_topoli, vz_zvezditsa`) → ИБ2-7. **36 листа носят ГОЛО име** (ИБ1-О11); `vz_zvezditsa` е `в.з.Звездица` **без интервал** и не се „поправя“.
- `src/build_quarters_signed_imot.py:704-748` (`build_mask`) **вече връща `words` — клетка за всяка от 80 510-те точки в реда на `address_rows`** (`:718-731`), но не го записва. **Ф1 стъпва на тази маска.**
- **Преди ВСЕКИ комит във varna_3d:** `qa_no_cad_ids.py` и `qa_no_extrude.py` (`varna_3d/CLAUDE.md`) → Г16. Друго не се пипа (§10 т. 8).

### 1.6 · Какво днес ЗАБРАНЯВА лота

ADR 006 **D12** (`006:157-163`, **Accepted** `:3`) пинова двата товара **и всяка функция на `initAddressSearch`**. ADR 007 (**Proposed**) пинова скорера, компаратора и `selectResult` (`:15`) и прави `address_rows.json` Untouchable (**D2**, `:13`). ADR 010: **D16** (`:68`) данните ги комитва Петър · **D18/G17** (`:72`) написаният квартал е байт-стабилен · **D3** (`:42`) публикува се само `quarter: {name, code, src}` · **D20** (`:76`) документният комит е след пуша. ADR 011 **G29** (`:51`) — геометрията никога публично. **Статусите (О107):** 006 Accepted, 007/010/011 Proposed; критерият е СИМЕТРИЧЕН: **007 отпада не заради статуса, а защото write-set-ът му се ЗАМЕСТВА от амандамента, подписан заедно с плана; в същия подпис 010 и 011 стават обвързващи** (И-Б1-D6). `sw.js` е недосегаем (`006:158`) → файлът не влиза в офлайн пакета (ИБ2-7; цената е в Р2).

---

## 2 · Дневник на отклоненията — **празен за ИЗПЪЛНЕНИЕТО; носи кръга на преработката**

Всяко отклонение ПРИ ИЗПЪЛНЕНИЕТО се вписва ТУК като **ИБ1-О17, ИБ1-О18 …**: какво казваше планът, какво е измерено, коя е новата команда, кой го намери. **Отклонение без ред = STOP 2.** Изпълнителят не пише в този файл. Присъдите по осемте кръга са в §2.Б на големия план (`ИБ-О1 … ИБ-О143`; номерата не са непрекъснати — липсващите са отхвърлени находки, 135 записа), цитирани тук като **О<n>**.

| **ИБ1-О17** | **Стъпка 3б преди 3а (11.09 22:06, ръката на Петър).** Планът казваше: 3а архивира дифа на Б3 в два екземпляра от стейджа и фиксира `<БАЗА>`, после 3б паркира. Измерено след командата: `stash@{0}` „B3 11.09 parked for I-B2“ (14 файла, 3 843+/1 256− срещу HEAD), в дървото остават само двата предсъществуващи мръсни scratch JSON-а, четирите `A`-пътя ги няма, `unittest discover` → 315 · OK. **Нова команда за 3а:** архивът се произвежда от стеша — `git diff --binary HEAD stash@{0} > <кръпка>` (двата екземпляра, sha256 в Д6), OID-ите са записани в Д5 §0.2 на И-А и в стеша; `git apply --check` на кръпката в чист клонинг на HEAD; `<БАЗА>` се фиксира на 3а както е писано. Кой го намери: Архитектът при К1. |
| **ИБ1-О18** | **Базата на архива на Б3 (стъпка 3а).** Планът/ИБ1-О17 даваха `git diff --binary HEAD stash@{0}`; HEAD вече носи К1 и голата форма връща 15 пътя (+3 844/−1 259, 15-ият е обратното на К1 в плана). Изпълнено: същата команда + pathspec на 14-те пътя на Б3 → 268 020 B, `cmp`-идентично с `git diff --binary stash@{0}^ stash@{0}` (14 пътя, +3 843/−1 256); двата екземпляра с един sha256 `11263583…8dd46`; `apply --check` и `--check --cached` → 0 в чист клонинг на `<БАЗА>` = 73dfc91. Кой го намери: Изпълнителят на 3а, потвърдено от Одитора. |
| **ИБ1-О19** | **Байтовете на Ф4 (стъпка 6).** §1.3 даваше диапазон 486 126–486 129 B; измерено при износа: **493 305 B raw / 23 400 gzip-9** (Ф5 232 645 / 12 780; Ф11 9 175 / 619) — разликата ~7 KB е от финалната сериализация на билдъра; таванът 600 000 държи, STOP 16 не се пали. Числото в подписа на К6 е измереното. Кой го намери: Изпълнителят на стъпка 6. |
| **ИБ1-О20** | **Мета-гейтът на половините (Ф3/Ф2) приемаше липсващ вход за „паднала половина“** — хванато при прогон на манифеста след чистене на temp-папката; поправено преди К5: в Ф3 червен ред „входът за проверката е налице“ не се брои за половина, в Ф2 клетка без `#raion=` пада като ред на Г2, не като липсващ вход. Правилата на плана не мърдат. Кой го намери: Изпълнителят на стъпка 6. |
| **ИБ1-О21** | **STOP 6 след К5 (стъпка 7, 11.09 23:3x): Г16 (`qa_no_cad_ids.py`) → 1 върху `tests/test_fire_varna_quarter_index_fixtures.py:211` — фикстурата на половина (б) носеше синтетичния кадастрален низ ЛИТЕРАЛНО; непроследен беше невидим, К5 го направи проследен (2 148 → 2 151 файла). Одиторът доказа каузалността (в клонинг на 1a0595d същият гейт дава 0). Решение на Архитекта: път (2), изцяло в write-set-а — Ф3 сглобява низа по време на изпълнение (никога буквално в проследен файл; префиксът 99999 от коментара на гейта не става, защото половината трябва да ПАДА), **нов комит К5а** от Изпълнителя върху К5: `test(export): build the cadastral probe string at runtime` (57 знака), само Ф3; после половина (б) се доказва наново в еднократното репо (→ 1), Ф3 → OK, Г16 → 0/0, и чак тогава К6. Пътищата (1) изключение в `ALLOW` на гейта (извън write-set-а, и с истинско ЕКАТТЕ) и (3) откат на К5 се отхвърлят. Отделно: Р4-А на стъпка 5 върна `lot1v_v_reference_manifest.json` към блоба на HEAD по подписаното подразбиране — локалната модификация (регенериран манифест със `signed_by: pending`) е записана като диф в Д6 и не е чужда ръчна работа. Кой го намери: Изпълнителят (STOP 6), Одиторът (каузалност, находки 1–3), Архитектът (решението). |
| **ИБ1-О22** | **`git add -f` за К6 (стъпка 8).** §6 пише формата `git add -- <пътища>`; `data/*` във varna_3d е игнориран (`.gitignore:4`, Ч), затова Ф4 и Ф5 влизат само с `git add -f -- data/fire_varna_quarter_index.json data/fire_varna_address_quarter_ledger.json` — същият прецедент като `data/fire_varna_places.json`/`…hotels.json`, проследени въпреки реда. `.gitignore` не се пипа (извън write-set-а). Комитът е с pathspec на двата пътя, ръката е `-c user.name=Petar1984`, преди него `git rev-parse --show-toplevel` (червен ред 13). К5а = 398b91d предхожда К6; К4 = 5cad1d7 също (Г21). Кой го намери: Изпълнителят (стъпка 7), Одиторът (форма по §6). |

**Кръг на преработката (11.09, ПРЕДИ подписа) — шестнайсет поправки, всяка премерена наново:**

**ИБ1-О1 · многоклетъчният ключ.** Гасене, обещано на три места и реализирано на нула → **Ф1 пише `-1` при билда**, Г18 съди. М: **294 ключа / 30 777 записа** (с `-1` като клетка), 238 / 25 777 без → в плана стои ТАВАН.
**ИБ1-О2 · `locality`.** Класът не е поле на v2, а `LOCALITY_CODES` е в забранената половина (`:6322`) → **гаси се при билда**; М: 23 клетки · 15 132 · 14 823; половина на Г19.
**ИБ1-О3 · Г4** става **двучастен**: 8 692 пряко сверени + 34 603 с ПРОВЕРЕНА причина, вместо „43 295 с 0 разминавания“.
**ИБ1-О4 · §3.Д режеше до първата `, `.** В `normalized_address` няма запетая (М: 0 / 80 510) → **клон (д) по ТОКЕН** (6 393 реда) + четвърта половина на Г23.
**ИБ1-О5 · дайджестите.** **79,2 % от записите и 88,5 % от редовете** се разместват невидимо (М) → отпечатъкът носи и `pin`/координатата; остават 36 записа и 0 реда.
**ИБ1-О6 · сериализацията.** **4 координати са ЦЕЛИ числа**: `String(43)` ≠ `repr(43.0)` → пинато `toFixed(7)`/`'%.7f'` + такава стойност в кръстосаната половина на Г3.
**ИБ1-О7 · `merge-base`** срещу К8/К9 на стъпка 11 и „след ВСЕКИ комит“: те се раждат на 12 и 13 → изход **128** без присъда; мести се на 16 и в следпроверката на К8/К9.
**ИБ1-О8 · третата половина на Г23** не падаше (нищо не хвърля, `rowIndex` = 0) → `strict: true` в ПРЯСЪН node-процес; Т11 твърдеше „изтича в `window`“ — вярното е `globalThis`.
**ИБ1-О9 · клонингът не беше пинат.** `git clone --no-local` пада **4/4** (`inflate returned -3`, различен офсет) → пината е `--no-hardlinks`; `$IB_TMP` получава спирачка за дължина (145 + корен ≤ 259).
**ИБ1-О10 · котвите на местата** са от дървото със стейджа; в HEAD са **7264-7267** и **7236** (Δ 81, М) — след 3б важат тези.
**ИБ1-О11 · „28 от 38 «кв» кода голи по Р22“.** М: **36 от 90-те листа** са голи — **23** в подписания Х15, **13** по регистъра (вкл. флагмана `levski1`); `_meta.bare_display` НЕ е в v2; `vz_zvezditsa` е „в.з.Звездица“ без интервал.
**ИБ1-О12 · тай-брейкът.** `raion_num` не е поле на v2 → числото е от `imported_from` `#raion=` (90/90, М) + половина на Г2.
**ИБ1-О13 · Ф7** броеше „достижимо от инициализацията“ (не е машинно решимо) → **операционна дефиниция** + шестте лениви константи поименно.
**ИБ1-О14 · Ф9.** Параметрите се мерят на стъпка **0**, не на 13; `POPUP_AUTOPAN_BR` отпада (0 употреби), влизат `resultsEl` (10) и `inputEl` (3).
**ИБ1-О15 · мета-покритие.** Г1, Г16 и Г19-клиент нямаха съдник, `index.html` и Ф10 не бяха операнди → втора таблица във Ф3, Ф14 става **15** гейта, Г15 получава `--added-lines` и себе си.
**ИБ1-О16 · четири дребни.** „24/24 на Б3“ → **3 + 21** (вторият модул е червен заради ДАННИТЕ); 685 е диф v1→v2 (`plan_v3.md:168`) и отпада от §8; ръката на Петър става ГЕЙТ във Ф6; подразбирането на Р5 става **Р5-Б** (буквата на D16).

**Отхвърлени находки (с довод):** (1) „`@@ -6255,0 +6256 @@` не съществува“ — съществува при **`-U0`** (21 хънка; при подразбиращ се контекст 16 и друг хедър) → внесена е само формата; (2) „`$IB_TMP` прави клонинга неизваждаем“ — **не се възпроизведе** при 104-знаков корен → остава мярка със спирачка, не блокер; (3) голите са 13, не 14 (`vz_zvezditsa` носи представка без интервал); (4) 6 696 срещу 6 393 — различни дефиниции, и двете са в §1.2; (5) „планът е 96 899 знака“ — това са **БАЙТОВЕ** (`wc -m` в C-локал); в знаци беше 68 000 → мярката е `LC_ALL=C.UTF-8 wc -m`.

---

## 3 · Write-set (път извън §3.А/§3.Б = **STOP 1**)

### 3.А · Нови файлове

| # | репо · път | какво |
|---|---|---|
| **Ф1** | `varna_3d` · `src/export_fire_varna_quarter_index.py` | Износителят: чете v2 + двата товара **като блобове** → Ф4 и Ф5. CLI без подразбирания (`--quarters --address-rows --search-index --decisions --out-public --out-ledger`), **абсолютни пътища**, детерминизъм (сортирани ключове, LF, `ensure_ascii=False`, `(",",":")`, нула часовници), utf-8 конзола. Произвежда **и `entry_cell`, и `row_cell`**. **ГАСЕНЕТО Е ТУК, не в клиента** (§3.Д): `-1` при вън от 90-те · клетка клас `locality` · многоклетъчен дисплей-ключ (само `entry_cell`). **Ф5 носи ИСТИНСКАТА клетка.** Тай-брейкът чете `#raion=` (§1.5). |
| **Ф2** | `varna_3d` · `src/qa_fire_varna_quarter_index.py` | Гейтът на износа: `--only ИБ1-Г<n>`, `--make-fixtures --fixtures-dir`, манифест `{file, gate, expected_exit, argv}`; машинна присъда. Пише Ф11 на абсолютен път. |
| **Ф3** | `varna_3d` · `tests/test_fire_varna_quarter_index_fixtures.py` | Мета-гейт: ражда фикстурите и настоява всяка да падне **по своя номер**. **Втора таблица „половини без `--only`“** `{gate, argv, expected_exit, cwd}` като ПОДПРОЦЕС: **Г1** (копие на Ф1 с `datetime.now(…)` → `cmp` → 1) и **двете на Г16**. Без нея три половини са самоатестация (червен ред 7). |
| **Ф4** | `varna_3d` · `data/fire_varna_quarter_index.json` | **Данни, ръката на Петър (К6)** — тялото по §3.Г. |
| **Ф5** | `varna_3d` · `data/fire_varna_address_quarter_ledger.json` | **Данни, ръката на Петър (К6).** Леджерът „адрес → клетка“: масив 80 510 в реда на `address_rows`, стойности `-1..89`, + `_meta` с трите числа на маската, `address_rows_sha256` и **`polygon_version: 2` + `polygon_sha256` `64c65f2b…`** (Р6). **НЕ пътува.** |
| **Ф6** | `Fire_Varna` · `tests/test_quarter_index_bundle.py` | Пинове по байтове/sha/брой (образец `test_places_public_bundle.py:44-59`). **И РЪКАТА (D16 като гейт):** `git log -1 --format=%an -- data/address_quarters.json` == `release.HUMAN_AUTHOR` (`release.py:214`) — белият списък няма `signed_by`, тоест файлът не влиза нито в `signable()` (`:189`), нито в проверка 7 (`run_gates.py:675`). Половина в `ib_clone`: същият файл от ръката на Изпълнителя → 1. |
| **Ф7** | `Fire_Varna` · `tests/test_first_load_budget.py` | Таванът. Правилото е **ОПЕРАЦИОННО, не „достижимо от инициализацията“** (достижимост в 7 561-редов HTML не е машинно решима → Г11 би паднала без дефект): наборът = литералните `fetch('data/…')` **на върхово ниво на `<script>`** + всеки `fetch`/`fetchCachedJson` върху КОНСТАНТА извън тяло на функция. **Шестте лениви константи са ПОИМЕННО „не влизат“** (затворен списък): `SEARCH_INDEX_URL :5325` · `ADDRESS_ROWS_URL :5326` · `APPROX_ADDRESS_URL :5927` · `PLACES_URL`/`CATS_URL :6256` · `PLACES2_URL :6257` (М) + `ADDRESS_QUARTERS_URL`. Израз само по литерала е fail-open (О54). |
| **Ф8** | `Fire_Varna` · `tests/test_address_quarter_render.py` | Присъдата за ЧЕТИРИТЕ повърхности; кара Ф9 като ПОДПРОЦЕС, съди отговора, сравнява с Ф11. **Координатата на `{ask:"coord"}` се сглобява по време на изпълнение или се чете по индекс — литерал не се пише** (О95). |
| **Ф9** | `Fire_Varna` · `tests/address_slice_probe.mjs` | **Харнесът на адресния срез** по договора на `granitsi_client_probe.mjs` (`:3-7`, `:212-260`; един json на STDIN, нула кирилица в командата). Входът носи `shared` и `slice`, слепени преди `new Function`. **ПЕТ форми:** `render` · `panel` · `coord` · `collisions` · **`digests`→`{entries_digest, rows_digest}`** (без нея кръстосаната половина на Г3 е неизградима — О127) + флаг **`strict: true`** САМО за третата половина на Г23. **Параметрите са ИЗМЕРЕНИ (М, срез 4840–6198 на HEAD) и се пиноват на стъпка 0:** `resultsEl` **10** (`:4838`) · `anchorHydrantsAt` **7** (`:2440`) · `buildNavActions` **5** (`:4255`) · `inputEl` **3** (`:4837`) · `WORKER_URL` **1** (`:2566`) · `POPUP_AUTOPAN_TL` **1** (`:4334`). **`POPUP_AUTOPAN_BR` отпада — 0 употреби.** |
| **Ф10** | `Fire_Varna` · `gates/probe/ib1_g15.py` | Увивка на координатния grep: **0 чисто, 1 попадение, 2 вход**. Образецът е ДВОЕН и дословен: `-e '\b4[23][.,][0-9]{3,}\b' -e '\b2[78][.,][0-9]{3,}\b'` — **два `-e`, никаква тръба** (само първият = fail-open; О80). **Втори режим `--added-lines <БАЗА> -- index.html`** (цял файл не може — М: 3 исторически попадения). **Ф10 е операнд и на себе си.** |
| **Ф11** | `Fire_Varna` · `scratch/places_search/ib_corpus_11.09.json` | Корпусът: 20-те заявки на kill switch-а + **145-те** записа с токен `studentska` с клетката им; пинва се по sha във Ф6. **ЗАТВОРЕН ФОРМАТ `{"queries": [низ…], "entries": [{tk, kind, cell}…]}` — два върхови ключа, три на елемент; нито `pin`, нито `label`, `d`, `qtk`** (О98) — иначе 145 координатни двойки излизат публично при зелени гейтове. |
| **Ф12** | `Fire_Varna` · `tests/test_kill_switch.py` | Присъдата на Г12: `cmp` на `outerHTML` срещу `git show <БАЗА>:index.html` (еталонът е пинат, не HEAD — Кими К44-2); координатното правило на Ф8. |
| **Ф13** | `Fire_Varna` · `tests/test_negative_halves.py` | **Мета-гейтът на Fire_Varna (Г20):** прогонва всяка половина от Ф14 и пада, ако ЕДНА върне 0 или чужд изход (О60). |
| **Ф14** | `Fire_Varna` · `tests/negative_halves_manifest.json` | Манифест `{gate, argv, expected_exit, fixture}`. **`len == 16`** (прецедент ИА-18 = 43) **и множеството `gate` е ТОЧНО `{Г3 (само кръстосаната половина), Г7, Г8, Г9к, Г10–Г15, Г17, Г19к, Г20–Г23}`** — **петнайсетте** на Fire_Varna плюс кръстосаната половина на Г3 (Кими К44-1/7) (Г19 е смесена: данните са при Ф3); останалите са при Ф3 (О139). Координатните фикстури се сглобяват по време на изпълнение. |
| **Ф15** | `Fire_Varna` · `tests/test_adr_amendment.py` | Г21: Д2 съдържа `И-Б1-D1 … И-Б1-D6`, `D12` и `G29`. |

**Доставеният път (без номер Ф):** `data/address_quarters.json` — **ръката на Петър (К8)**, байтов препис на Ф4. **Преносът се доказва по OID:** `rev-parse HEAD:data/fire_varna_quarter_index.json` (varna_3d) == `rev-parse :data/address_quarters.json` (Fire_Varna); разлика = **STOP 7** (О97).

### 3.Б · Точкови редакции

| # | котва (Ч) | редакция |
|---|---|---|
| **Т1** | нов **споделен блок** между `:4835` и `:4836` | **СПОДЕЛЕНИЯТ КВАРТАЛЕН ОБХВАТ (О77):** `initAddressSearch` = `:4836`–`:6237`, `initPlacesSearch` = от `:6248`, а `:6243-6244` казва „Nothing here is called from initAddressSearch…“ — състояние вътре в адресния IIFE е невидимо за пътя на местата (върху блока стъпва И-Б2). **Write-set: Т2 – Т5, нищо друго.** |
| **Т2** | в блока на Т1 | `ADDRESS_QUARTERS_URL = 'data/address_quarters.json'`, **`const ADDRESS_QUARTERS_SHA256 = '<64 МАЛКИ hex>';` — формата е ПИНАТА** (Ч `run_gates.py:123` и `release.py:1181` търсят дословно `const\s+<име>\s*=\s*'([0-9a-f]{64})'`; `let`, двойни кавички или главни букви дават „константата липсва“, а не „крив пин“ → фалшив STOP 4), `ADDRESS_QUARTER_V2`, `quarterIndex = null`. **`QUARTERS_CACHE` ОТПАДА (О79):** `fetchCachedJson` (`:5333-5349`) няма параметър за кеш (литералът е на `:5339` и `:5345`) и е пината от D12 → **новият файл дели `SEARCH_CACHE`; обявена цена**. |
| **Т3** | в блока на Т1 | `validateQuarterIndex(doc, idx)` — fail-closed по §3.Г; при отказ `quarterIndex = null`. **Дайджестите са върху `new TextEncoder().encode(s)` — UTF-8 БАЙТОВЕ, НИКОГА `charCodeAt`**; offset `0x811c9dc5`, prime `0x01000193`, mod 2^32, разделител `|`. Измерено (О103/О128): **80 476 от 80 510** реда са не-ASCII; `rows[7][0]` = „10 та“ → UTF-8 `96f3c8d7` срещу `charCodeAt` `afaaf0ae`. **Координатната част се форматира с `toFixed(7)` / `'%.7f'`; голата сериализация е ЗАБРАНЕНА (ИБ1-О6).** |
| **Т4** | в блока на Т1 | `quarterTitleWord(code, rest)` — името **ДОСЛОВНО от `names[]`** (`chaika_kv` → „кв. Чайка“, `levski1` → „Левски 1“); **нищо не се долепя и нищо не се маха**. **Голите имена (ИБ1-О11, М):** **36 от 90-те листа**, от които **23** в подписания Х15 (`ПЛАН_…И-А_08.09.md:347`) и **13 по регистъра**, вкл. флагмана `levski1` — „по Р22“ е грешна котва. Правилото „дословно от `names[]`“ не зависи от списък. „кв. Гръцката махала“ би бил ред във v2 → **STOP 14 / И-Б2**. |
| **Т5** | блокът на Т1 + `:5351-5372` | Третият товар в СЪЩИЯ `Promise.all`, **но не МОЖЕ да отхвърли (О78): `loadQuarterIndex()` винаги резолвва (`fetchCachedJson(...).catch(() => null)`)**, иначе `catch`-ът (`:5366-5369`) би убил ЦЯЛОТО адресно търсене при 404. **Тук се смятат и ДВАТА дайджеста: `rows_digest` след `:5360`; `entries_digest` в ДОПЪЛНИТЕЛЕН проход след `prepareIndex(idx)` на `:5363`, ПРЕДИ `searchIndex = idx` на `:5364`** (О102/О136) — `prepareIndex` не се пипа. **Цената е измерена (М-о: 42 ms + 30 ms, веднъж на `focus`)** и влиза в Д6. |
| **Т6** | нова функция до `:4892` | `quarterOfHit(hit)` → `{code, name, parent_display} \| null`; чете **`hit._ord`** и нищо друго; `_ord == null` или `entry_cell === -1` → `null`. |
| **Т7** | `:5303` и `:5311-5313` | двете проекции носят `_ord: e._ord`. **Без този ред лотът доставя нула думи.** |
| **Т8** | `:5411-5423` | Изписването по §3.Д; **вторият ред се сглобява от `bits` и се създава при район ИЛИ свидетел** (297 записа без `d`, 292 в клетка). `dedupeDisplayRows` не се пипа. |
| **Т9** | `:5865`, `:5866`, `:5871`, `:5876` | **Панелът и попъпът (О9/О92):** думата е от същия `quarterTitleWord`; низовете се различават ЗАКОННО (панелът е блокът без `· вх. N`, `:5861-5863`). `:5858`/`:5859` остават байт-идентични → `:5871`/`:5876` получават НОВА локална променлива. |
| **Т10** | `:5027-5033` и `:5043` | **GPS-редът:** етикетът минава през `quarterTitleWord` по **`row_cell[индекса на реда]`** + клон (д) и свидетеля. При `-1`, угасен `rows_digest` или `quarterIndex === null` редът е **точно днешният**. Днес **1 871** реда противоречат на v2. |
| **Т11** | `:5004`, `:5014`, `:5018`, `:5023` — **и само те** | **Предусловие на Т10 (О67/О108):** `nearestAddressTo` губи индекса; четирите реда пазят `bestI`/`streetI` и връщат `{label, metres, rowIndex}`. **`:5004` получава `bestI = -1, streetI = -1`** (ИБ1-О8: без декларация стойността изтича в `globalThis`, не в параметъра `window`). **Забранено:** индекс по съвпадение на низ (`:5023` капитализира и реже `\s0$`). |
| **Т12** | **ПЕТ маркера, ДВА СРЯЗА**, всеки на граница между ДЕКЛАРАЦИИ | **(1)** `shared quarter state start` **ПРЕДИ `:4835`** / `end` **ПРЕДИ `:4836`**. **(2)** `address slice start` **СЛЕД `:4839`** (`inputEl :4837`/`resultsEl :4838` остават ИЗВЪН среза и са ПАРАМЕТРИ на Ф9 — М: 4837, 4838 и 4840 се вдигат еднакво; изборът е подписан) · `core end` **ПРЕДИ `:5424`** · `end` **ПРЕДИ `:6199`**, НЕ преди `:6237`. **Нула изпълним код.** **Тичало и паднало (О123):** `new Function` върху `4835,6236` и `4835,5423` → `SyntaxError: Unexpected end of input`; върху `4840,6198` и `4840,5423` се вдига. **Маркер извън граница = STOP 1.** |
| **Т13** | `b3_g11.py` **`:40-46`** | `ALLOWED_NEW` += `data/address_quarters.json` + **поименно** десетте нови пътя (Ф6–Ф15, вкл. корпусът Ф11). Без него `stray_new` пали гейта (`:116`). **`LEGACY` (`:39`) НЕ се пипа** (О34); `reconfigure` не се добавя (ИБ2-8). **Планът-документ не влиза** (`<БАЗА>` е след К1); при база `74c165f` се добавя и се записва в Д6. |
| **Т14** | `gates/release.py:166-170` **и `gates/run_gates.py:68-72`** | Четвърти ред `("ADDRESS_QUARTERS_SHA256", "data/address_quarters.json")` **в ДВЕТЕ огледала**: `run_gates` носи СОБСТВЕНА тройка и не импортира `release.SHA_PINS` → без втория ред проверка 1 (`:139-153`) не съди новия пин (О38). Обединяването е ИБ2-7. |

### 3.В · Документи

**Д1** `docs/plans/ПЛАН_Картата_imot_И-Б1_11.09.md` (К1) · **Д2** амандаментът (К4) — по подразбиране **`docs/decisions/011a_амандамент_И-Б1_11.09.md`** + ред-указател в `011_kartata_imot.md` (Р5-Б); при Р5-А — редове в самия `011` и Ф15/Г21 съдят него · **Д3** `docs/activeContext.md` § Current State (**след пуша**, К10) · **Д4** `docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` §22 (К10) · **Д5** `scratch/boundary_gallery/granitsi_preview_05.09/ib1_review_11.09.html` (**игнориран**, `.gitignore:64`) · **Д6** `…/ib1_report_11.09.md` (**игнориран** — всеки изход на §5 дословно). Д1–Д4 са на Архитекта.

**Амандаментът (Д2), шест реда.** **D1** · write-set за адресния път, поименно: `buildExactItem`, двете проекции, `quarterOfHit`/`quarterTitleWord`/`validateQuarterIndex`/`loadQuarterIndex`, зареждането и двата дайджеста в `ensureSearchData`, `coordMeta`, **само `:5004`, `:5014`, `:5018`, `:5023`** на `nearestAddressTo`, **само `:5865`, `:5866`, `:5871`, `:5876`** на `selectResult`, петте маркера и споделеният блок — **и нищо друго**; `baseAddressLabel`, `formatAddressHit`, `dedupeDisplayRows`, скорерът, компараторът, `renderDetailSheet`, `prepareIndex`, `fetchCachedJson`, `:5858`/`:5859` и навигацията остават пиннати. **D2** · „написан“ ≠ „по полигон“ (D18/G17 важат за написаната дума). **D3** · D13: третото звено доставя и файл, пинат от `SHA_PINS` и огледалото. **D4** · публикацията: `_meta.publication` и **G29** + присъдата на Р6. **D5** · К3, К6, К8 са ръката на `Petar1984`; К10 е след пуша. **D6** · 007 остава Proposed и се ЗАМЕСТВА; 010 и 011 стават обвързващи, при конфликт **011 печели**. **ADR 006/007/008/010 — нула байта.**

### 3.Г · Схемата на доставения файл (затворена)

```
{ "schema_version": "1.0",
  "kind": "fire_varna_quarter_index",
  "generated_from": { "search_index_sha256": "<64 hex>", "entry_count": <int>,
                      "address_rows_sha256": "<64 hex>", "address_row_count": <int>,
                      "entries_digest": "<8 hex>",   // FNV-1a върху tk.join('|') + '~' + pin[0] + ',' + pin[1]
                      "rows_digest":    "<8 hex>",   // FNV-1a върху normalized_address + '~' + lat + ',' + lng
                      // ПИНАТО: offset 0x811c9dc5, prime 0x01000193, mod 2^32, върху UTF-8 БАЙТОВЕТЕ
                      // (в JS: new TextEncoder().encode(s) — НИКОГА charCodeAt), разделител `|` = 0x7C,
                      // координатите с toFixed(7) / '%.7f'; и двата се смятат в ensureSearchData (Т5).
                      "quarters_version": 2, "snapshot_date": "2026-09-08" },
  "codes":   ["<код на клетка>", ...],           // = features[i].properties.code
  "names":   ["<име>", ...],                     // = features[i].properties.name ДОСЛОВНО
  "parents": ["<parent_display или \"\">", ...], // "" при null
  "entry_cell": [<int>, ...],                    // 1:1 с entries (86 232); -1 = БЕЗ дума (вън ИЛИ угасена)
  "row_cell":   [<int>, ...] }                   // 1:1 с rows (80 510); същото. Носителят на GPS-реда
```

**Редът и изворът са ПИНАТИ (О27):** `codes`/`names`/`parents` обхождат **90-те Feature-а с `geometry_origin == "imot_bg_points"`, сортирани по `code`** (петте родителя влизат само през `parent_display`). **Инварианти fail-closed (Т3) — БЯЛ списък (О56/О65):** върховите ключове **ТОЧНО тези 8** · `generated_from` **ТОЧНО тези 8** · `kind` дословно · **`quarters_version === 2` като СТОЙНОСТ** · `entry_cell.length === entries.length === entry_count` · `row_cell.length === addressRows.length === address_row_count` · **двата дайджеста съвпадат с изчислените в `ensureSearchData`** · всяка стойност е цяло число в `[-1, codes.length-1]` · `codes[i]` пасва на `/^[a-z0-9_]{1,64}$/` · `names`/`parents` са низове. Отказ → `quarterIndex = null`, нула изключения, **думата гасне едновременно на ЧЕТИРИТЕ повърхности**. **Нулата float е НЕОБХОДИМО, не достатъчно условие** — 86 232-та записа вече носят публичен `pin`, а `entry_cell` ги етикетира: **обявена производна** (Р6). **Не влизат:** `coordinates`, `geometry`, `geometries`, представителна точка, площ, отсечка, `n_pts`, `precision_m`, `advert_id`, `raw_sha256`.

**Защо отпечатъкът носи пина (ИБ1-О5, М):** само по `tk` **68 316 от 86 232** записа (79,2 %) се разместват без дайджестът да мръдне; само по `normalized_address` — **71 273 от 80 510** реда (88,5 %); клетката идва от координатата, тоест тези пермутации дават ЧУЖД квартал при зелени гейтове (`index.html:5120`: подредбата стъпва на тай-брейкове при билда). С пина остават **36 записа** (еднаква клетка по конструкция) и **0** реда. Приватността не страда: 8 hex върху цял масив, а пиновете са публични.

### 3.Д · Правилото на изписването (Р3, дословно)

`title = quarterTitleWord(code, rest)` се строи в `buildExactItem`, без да мени ключа на слепването; `rest` идва от `formatAddressHit(r)` по клоновете, в този ред: **(а)** маха се водещият **СЕГМЕНТ до и включително първата `, `**, ако започва с елемент от затворения списък `{кв., кв, квартал, ж.к., жк, ж.к, жк., к.к., в.з., с.о., м-т, м., местност, ПЗ}` и остатъкът е непразен (М: 9 002–9 071) — **режем СЕГМЕНТ от остатъка, никога представка от името** (О52); **(б)** същото при водещ сегмент, който е **ДОСЛОВНО име от `names[]`** (М: **1 717**) — иначе „Евксиноград, Евксиноград…“; **(в)** ако `rest` започва с **„гр варна район“** или е **„неизвестна“** — думата не се прилага (М: 23 113 + 90); **(г)** иначе `rest` е дословният `formatAddressHit(r)`.

**(д) GPS-повърхността реже ПО ТОКЕН, не по запетая (Т10).** Входът ѝ е `normalized_address` (`:5023`) — **няма нито една запетая** (М: 0 от 80 510) и токените са БЕЗТОЧКОВИ, тоест клон (а) би изписал „Левски 1, Кв чайка бл 11“ на около **6 393** реда (дублирането, което Г19 обявява за провал). Затова тук се маха водещият **ТОКЕН** от затворен безточков списък `{кв, жк, кк, вз, со, м, мт, местност, зона, пз, квартал}` **плюс следващите токени, докато съвпадат с `norm(names[row_cell])`**; празен остатък → редът остава днешният. Г23 има ТАКАВА половина.

**Името дословно от `names[]`** → флагманът чете **„Левски 1, бл. 11“**. **В КЛИЕНТА правилото е ЕДНО, fail-closed: `entry_cell`/`row_cell` = `-1` → няма дума** (плюс `quarterIndex === null` и разминал се дайджест). **Трите причини за `-1` живеят при БИЛДА, във Ф1:** **(1)** вън от 90-те (2 335 · 2 274); **(2)** клетка клас `locality` — **D6** „Полигон-местност никога не пълни `quarter`“ (`010:48`); класът НЕ пътува (изводим е само от `kind`, `fire_varna_locations.py:155-163`, а `LOCALITY_CODES` е на `:6322`, в забранената половина); 23 клетки · **15 132 · 14 823**; **(3)** многоклетъчен дисплей-ключ — клиентът не го вижда (`ranked.slice(0, SEARCH_LIMIT)`, `:5311`, реже ПРЕДИ слепването) → съди го **Г18**; на екрана **294 от 27 928 ключа (1,05 %)**. Вторият ред: `район <X>` + `· написано: <дума>` при разминаване (D18), от `bits`, **и когато район няма, но свидетел има**. **Панелът и попъпът (Т9)** носят СЪЩАТА ДУМА (равна е думата, не низът — STOP 19). **GPS (Т10):** „≈ N м от `<quarterTitleWord(row_cell)>` …“ + свидетелят. `parents[i]` непразно → „част от `<parent_display>`“ в панела. `ADDRESS_QUARTER_V2 = false` → нищо от горното; DOM-ът е байт-равен на HEAD (Г12).

---

## 4 · Ред на изпълнение

| стъпка | кой | какво | спирачка |
|---|---|---|---|
| **0** | Изп | Пълната мярка на §1 **без нито една редакция** (М0) + `git show HEAD:index.html \| grep -n` за всяко име от §3.Б **и за трите котви на местата** (Δ 81 спрямо дървото) + `varna_3d status` + двата вектора на Г14 + **спирачката за `$IB_TMP`, клонингът по пинатата команда и списъкът параметри на среза (Ф9)**. **Г8/Г11 не се пускат; `<БАЗА>` не се фиксира тук.** | разминаване = **STOP 1** |
| **1** | **Петър** | **Gate 1** — подписва плана и **осемте** решения на §9. | без подпис нищо не тръгва |
| **2** | **Архитект** | **К1** — ред-статус „подписан“ в Д1; преди комита `python -c` обвивка около израза на Г15 с ДВАТА `-e` върху Д1 (О129). | координатно попадение = **STOP 12** |
| **3а** | Изп | Архивира дифа на Б3 **в ДВА екземпляра** (`git diff --cached --binary` → `scratch/boundary_gallery/granitsi_preview_05.09/B3_staged_11.09.patch` и `$IB_TMP/ib_b3/`; sha256 в Д6), записва **14-те OID-а и буквите**, доказва `git apply --check --cached` в ЧИСТ КЛОНИНГ (пинатата команда) и **репетира Р1 в `ib_clone`** (попът конфликтува — очаквано ДА). **ТУК СЕ ФИКСИРА `<БАЗА>`.** | архивът не се прилага чисто = **STOP 3** |
| **3б** | **Петър** | **Р1 с неговата ръка:** `git stash push --staged -m "B3 11.09 parked for I-B2"`. Изп доказва: `status --porcelain -- <14-те пътя>` празно · `stash list` с ТОЧНО един нов запис · `test ! -f` за четирите `A`-пътя · `unittest discover` → **315 · OK**. **Наборът на D19 се ражда празен.** | разлика с 315/OK = **STOP 1** |
| **4** | Изп | **К2** — връща САМО скелето по OID-ите (`b3_g11.py`, `granitsi_client_probe.mjs`; вече в `ALLOWED_NEW`, `:40`, `:45`), после **`PYTHONIOENCODING=utf-8 python gates/probe/b3_g11.py <БАЗА>` БЕЗ `--cached`** → 0 и `new` = ТОЧНО тези два пътя. **`HEAD --cached` след комит е ЗАБРАНЕНА форма** (О21/О70). | **STOP 8** |
| **5** | **Петър / Изп** | **Р4** за `…/lot1v_v_reference_manifest.json`: Изп печата дифа на ТЯЛОТО и спира; Петър избира `git restore --` (Р4-А) или **К3** (Р4-Б). После `release` → **очаквано 0**. | release ≠ 0 = **STOP 4** |
| **6** | Изп | Пише **Ф1, Ф2, Ф3**; строи **два пъти** → `cmp` (Г1); **мери времето на ЕДИН износ** (М-ориентир: пълен обход = **2,4 s**; над **2 минути** се записва като отклонение — спирачка „над 10 минути“ няма). Пуска **Г2–Г6, Г9 (данни), Г18, Г19 (данни)**; изброява 145-те записа в Ф11; записва байтовете на Ф4 raw/gzip-9 (в диапазона **486 126–486 129 B** според финалния разделител; в плана стои таванът **600 000** — STOP 16; Кими К44-5), маската (**83 896 · 1 · 2 335** и **78 235 · 1 · 2 274**), **броя `-1` във Ф4 (очаквано 37 839 записа · 17 097 реда) и точния брой многоклетъчни ключове**; `--make-fixtures` → всяка фикстура с `--only` и истинския ѝ изход. | фикстура с изход 0 = **STOP 5**; Ф4 > 600 000 B = **STOP 16** |
| **6а** | **Архитект** | **К4 — амандаментът (Д2), ПРЕДИ кода и данните** (О111). **Гейтовете тук са ГОЛИ команди** (Ф10/Ф15 се раждат в К7 — О126): `grep -c 'И-Б1-D'` = **6**, `'D12'` ≥ 1, `'G29'` ≥ 1 + `python -c` обвивката на Г15; **`merge-base` НЕ се пуска.** | **STOP 18** |
| **7** | Изп | **Г16** (от `C:/git/varna_3d`) → комит **К5**. | ≠ 0 = **STOP 6** |
| **8** | **Петър** | Преди комита Изп пуска **Г16** от `C:/git/varna_3d` (≠ 0 = STOP 6; Кими К44-4); комит **К6** (Ф4 + Ф5); мерките на стъпка 6 влизат в подписа му. **КОНТРОЛНА ТОЧКА А: `varna_3d` е затворена и подписана.** Лотът е 13 нови файла, 14 редакции, 23 гейта, ~58 половини и 9 комита в две репа (10 при Р4-Б) — ако Петър спре тук, доставеното е валидно само по себе си, а клиентът (Ф6–Ф15, Т1–Т12, К7–К9) е втора сесия. | — |
| **9** | Изп | Ф6–Ф15 + **Т13** + **Т14** → комит **К7**, преди клиентския код. **ИНТЕРВАЛ А, поименно:** `test_quarter_index_bundle`, `test_address_quarter_render`, `test_first_load_budget`, `test_kill_switch`, `test_negative_halves` — червени до К9; `test_adr_amendment` е ЗЕЛЕН от раждането си. **От К7 до К9 `release` и `run_gates` проверка 1 носят ДВА обявени блокера** (липсваща константа и липсващ блоб, `release.py:1180-1195`, `run_gates.py:139-152`) — не се пускат; **не е STOP 4** (О93). | нов червен извън списъка = **STOP 11** |
| **10** | Изп | Байтов препис на `data/address_quarters.json`, `git add --`, **без комит**; първо **OID-сверката**, после **Г7** (`<БАЗА> --cached`) и **Г8**. | **STOP 7** / **STOP 8** |
| **11** | **Архитект** | **Кръстосаният ред (О111):** `log -1 --format=%cI <К4>` **<** същото за `<К5>` и `<К6>` (`merge-base` не сравнява репа), изходите в Д6; пуска **САМО текстовата половина на Г21**. **`merge-base` НЕ се пуска тук — К8 и К9 се раждат на стъпки 12 и 13**, а срещу несъществуващ ref командата излиза **128** и пали червен ред 8. | **STOP 18** |
| **12** | **Петър** | Комит **К8**. Следпроверката я пуска Изпълнителят. | — |
| **13** | Изп | **Т1 ПЪРВО**, после **Т2–Т12**; пуска **Г9к, Г10, Г11, Г12, Г13 (`unittest discover`), Г17, Г19к, Г22, Г23**; сверява среза срещу пинатия списък параметри; **брои новите слети надписа** (`collisions`) — **над 500 спира и пита**. Комит **К9**; интервал А се затваря. | **STOP 9** / **STOP 20** |
| **14** | Изп + **Петър** | **Контролна точка Б:** §5 отгоре докрай + `unittest discover` (**0 паднали**). К8 и К9 вървят заедно или никое. | **STOP 11** |
| **15** | Изп | Произвежда **Д5** и **Д6** в игнорираната папка. | — |
| **16** | Изп | **Пълният §5** върху комитнатото състояние, вкл. **Г20** и **двете `merge-base` проверки**, + `unittest discover` (**0 паднали**) + инвариантът за дърветата (О118) + `varna_3d status` == 42 + sha256 на двата архива + `stash list` + чистене (`ib_b3` **остава**). | **STOP 11** |
| **17** | **Одитор** | Независим преглед на дифа срещу плана, ред по ред, с команди. | НЕГОДНО = **STOP 13** |
| **18** | **Петър** | §8 → **Gate 2** → пуш (varna_3d `rezhimi`, Fire_Varna `main`) **от главното дърво**; преди пуша `status --porcelain` е само базовата линия. | — |
| **19** | **Архитект** | **К10** — Д3 и Д4 **СЛЕД пуша**; Петър пуска втори път. | **STOP 18** |

---

## 5 · Гейтове

**Кой кого изпълнява:** Ф2 → Г1–Г6, Г9 (данните), Г18, Г19 (данните); Ф6/Ф7/Ф8/Ф9/Ф11/Ф12 → Г8–Г12, Г17, Г22, Г23; `b3_g11.py` → Г7; `unittest discover` → Г13; `run_gates`/`release`/`coverage` → Г14; Ф10 → Г15; varna_3d собствените → Г16; Ф13+Ф14 → Г20; Ф15 → Г21. **Мета-покритието е ПЪЛНО:** Ф3 съди половините на Ф2 **плюс Г1 и двете на Г16** (своя втора таблица), Ф13+Ф14 — петнайсетте на Fire_Varna, вкл. Г19-клиент; половина без мета-гейт = самоатестация и е забранена (червен ред 7).

**ШЕСТ правила:** (1) **харнес не е гейт** — никъде гола `node`-команда; (2) **всички пътни аргументи са АБСОЛЮТНИ**, относителен → изход **2** (ИА-19, `ПЛАН_…И-А_08.09.md:500`); (3) **utf-8 конзола и машинна присъда**, а всяко повикване на `b3_g11.py` носи `PYTHONIOENCODING=utf-8` (О133); (4) **черупката е `bash`**, навсякъде `"$IB_TMP/…"` (О117); (5) **отрицателните половини само в `$IB_TMP/ib_clone` / `ib_fixtures`** (**STOP 10**), клонингът — с пинатата команда на §1.1; (6) **половина в node иска ПРЯСЪН процес** — глобал от предишен прогон я прави зелена (М, Г23).

| # | гейт | команда · кога | очаквано | отрицателна половина (ТИЧАЛА и ПАДНАЛА) |
|---|---|---|---|---|
| **Г1** | два износа → едни байтове | `Ф1 … --out-public "$IB_TMP/ib_a/q.json"` и `… ib_b/q.json`, после `cmp` · 6, 16 | `cmp` мълчи, 0 | копие на Ф1 с `datetime.now(...)`, пуснато два пъти → `cmp` → 1 |
| **Г2** | маската == подписания леджер | `Ф2 --only ИБ1-Г2 --ledger <Ф5> --quarters <v2> --address-rows <блоба>` · 6, 16 | 0; **78 235 / 1 / 2 274 = 80 510**; `address_rows_sha256 == 2a4766bd…`; `polygon_sha256 == 64c65f2b…`; тай-брейк по **90** клетки, числото от `#raion=` | Ф5 с една стойност в съседна клетка → 1; копие по 95-те (6 991 двусмислени) → 1; Feature без `#raion=` → ≠ 0 |
| **Г3** | структурата на Ф4 | `… --only ИБ1-Г3 --public <Ф4> --search-index <блоба>` · 6 (първите четири половини), 16 (всичките пет — кръстосаната се ражда с Ф9 в К7 и влиза в манифеста на Ф14 като шестнадесети ред; Кими К44-1) | 0; броевете и типовете по §3.Г (`86 232` / `80 510`); `search_index_sha256 == c12f9425…`; двата дайджеста == преизчислените | `len(codes)` → 1; дължина −1 → 1; разместени `entry_cell` → 1; подменен `rows_digest` → 1; **ПЕТА, КРЪСТОСАНА (О127): 3 записа с КИРИЛИЦА и една от 4-те ЦЕЛИ координати — дайджестът на Ф2 срещу върнатия от Ф9 (`{ask:"digests"}`) → 1** (единствената, която лови крива сериализация при зелени Г3/Г17) |
| **Г4** | **Ф4 ↔ леджерът, ред по ред** | `… --only ИБ1-Г4 --public <Ф4> --ledger <Ф5>` · 6, 16 | 0, **ДВУЧАСТНО** (Ф4 гаси, леджерът носи истинската клетка): **(1)** за **8 692**-та с `display_id` и `entry_cell ≥ 0` — `entry_cell[i] == ledger[display_id]`, **0 разминавания**; **(2)** за **34 603**-та с `-1` всяко гасене има ПРИЧИНА по §3.Д (многоклетъчен 29 757 · locality 13 268 · вън; класовете се пресичат), **0 необяснени**. Печата и 1 728-те отместени `mf` пина при 41 567 точни. **При разминаване печата `display_id`-тата И ИЗЛИЗА 1** (О26) | подменена стойност на запис с `display_id` → 1; **`-1` без нито една причина → 1** |
| **Г5** | **приватност на Ф4 — БЯЛ списък** | `… --only ИБ1-Г5 --public <Ф4>` · 6, 10, 16 | 0; **белият списък на §3.Г** (8 + 8 ключа, `quarters_version` == **2**). Черният списък е по-слаб — пуска `centroid_e7`, защото микроградусите са ЦЕЛИ числа (О56) | `centroid_e7` от цели числа → 1; `n_pts` → 1; двойка float → 1; **без `row_cell` → 1**; **`quarters_version: 3` → 1** |
| **Г6** | **приватност на корпуса Ф11** | `… --only ИБ1-Г6 --corpus <абс. Ф11>` · 6, 9, 16 | 0; **бял списък на ДВА ЕТАЖА:** два върхови ключа `{"queries","entries"}`; всеки елемент на `entries` носи точно `{tk, kind, cell}` | ключ `pin` → 1; ключ `label` → 1; **ТРЕТИ върхов ключ → 1** |
| **Г7** | **приватност на снимката** | `PYTHONIOENCODING=utf-8 python gates/probe/b3_g11.py <БАЗА> --cached` преди комит, без `--cached` след него · след ВСЕКИ комит от стъпка 4 | `{"scope": …, "geo_hits": [], "unreadable": [], "stray_new": []}`, 0 | в `ib_clone`: стейджнат нов `data/*.json` с ключ `coordinates` → 1; нов път извън `ALLOWED_NEW` → 1 |
| **Г8** | **свидетелят е непокътнат** | `unittest tests.test_quarter_index_bundle` · 10, 16 | 0; `2a4766bd…` и `c12f9425…` непроменени; броевете **80 510 / 86 232**; байтовете на преписа == пинатите; ръката на К8 == `Petar1984` | копие с един изтрит ред през `FIRE_VARNA_*_PATH` → 1 |
| **Г9** | **флагманът и съседите** | данни: `… --only ИБ1-Г9 --public <Ф4> --corpus <Ф11>`; клиент: `python -m unittest tests.test_address_quarter_render` · 6, 13, 16 | 0; пинати поименно: `mf ['studentska','bl','11'/'12'/'13'/'14'/'3'/'4']`, `['studentska','1','bl']`, `['studentska','4','bl','5']`, `['ul','studentska','7']` → **levski1**; `['studentska','1'/'7'/'7','bl'/'6','bl']` → **levski2**; ключовете „studentska 1“ (**47** записа: 46 levski1 + 1 levski2, **0 с `display_id`**) и „ул студентска“ (**14**: 12 + 2, всички с `display_id`) са МНОГОКЛЕТЪЧНИ → **`-1`, думата ГАСНЕ** (М); 145-те съвпадат байт в байт. Клиентът: **„Левски 1, бл. 11“ · „район Приморски · написано: кв. Чайка“** | срещу `<БАЗА>:index.html` (пинатият еталон, не HEAD — Кими К44-2) → „кв. Чайка, бл. 11“ → 1; подменена стойност за флагмана → 1; **„евксиноград 20-та 16“ с ДВА пъти „Евксиноград“ → 1**; **запис без `d` без „написано: …“ на втория ред → 1** |
| **Г10** | пиновете == блобовете | `run_gates` проверка 1 **и** `release` · 13, 16 | 0 | `index.html` в `ib_clone` с една сменена цифра в **`ADDRESS_QUARTERS_SHA256`** → `run_gates` → 1 **и** `release` ≠ 0 (падне ли само едното, огледалото не е пипнато — О38) |
| **Г11** | **таванът на първото зареждане** | `unittest tests.test_first_load_budget` · 13, 16 | 0; **два инварианта, нула пинати байта (О57):** наборът е **точно `{data/hydrants.json}`**, сборът **≤ 5 242 880 B**; числата преди/след са в Д6 | нетърпелив `fetch('data/address_quarters.json')` → 1; **`fetchCachedJson(ADDRESS_QUARTERS_URL)` при init → 1** (О54); същият тест с таван 1 000 000 → 1 |
| **Г12** | **kill switch** | `unittest tests.test_kill_switch` — корпусът на Ф11, `flag:false`, **еталон `<БАЗА>:index.html`** (пинат, не HEAD — `<БАЗА>` е фиксиран на стъпка 3а; за `index.html` това е блобът преди К9; Кими К44-2) · 13, 16 | 0; байт-равно на еталона (входът е пинат по sha от Г8) | корпусът с `flag:true` → 1; **преместен ред в `buildExactItem` при `flag:false` → 1** (О84); **стъб на `fetch`, който ОТКАЗВА → `render` дава ДНЕШНИЯ ред; хвърли ли срезът → 1** (О78) |
| **Г13** | **обявените интервали (D19)** | `python -m unittest discover -s tests` · 0, 3б, 9, 13, 14, 16 | стъпка 0: **342 · FAILED (24)** поименно в ДВАТА модула; 3б: **315 · OK**; след К7 — подмножество на ИНТЕРВАЛ А; на 13, 14, 16 — **0 паднали**. **Сверката е ЧИСЛОВА (О82):** `discover` дава **M**, поименният прогон — **N**; зелено е **M == N** | копие на `tests/` в `ib_fixtures/tests_neg/` с един падащ тест → 1; **втора: падащият е ИЗВЪН списъка → M = N + 1 → 1** |
| **Г14** | release, run_gates, покритие | `run_gates` · `release` · **два вектора** на `coverage` върху `git:HEAD:data/places.json` и `…hotels.json` като база И кандидат: (1) без `--allow`; (2) + `--allow gates/allow/2026-09-05_lot1v_v.json` · 0, 5, 16 | `run_gates`, `release` → **0**; (1) → **0**; (2) → **3** (`EXIT_ALLOW_STALE`, `coverage.py:91`, `:618`) — **не е провал, не е STOP 4** (О33). Двата вектора са ДИМНА ПРОБА (блоб срещу себе си — О116); реалният е ИБ2-5 | в `ib_clone`: опашка с фалшив дайджест → `release` ≠ 0; **детерминистична (О115/О142): `--places-base "$IB_TMP/ib_nonexistent.json"` → 4; в Д6 влизат само `4` и `Errno 2`** |
| **Г15** | **прозата и новите json без координати** | `python gates/probe/ib1_g15.py <операндите>` · преди всеки Fire_Varna комит **от К7 нататък**; за К1–К4 — `python -c` обвивка около същия израз (О129) | 0. **Операндите са ЗАТВОРЕН списък (О24/О112):** Д1–Д4, `address_quarters.json`, `ib_corpus_11.09.json`, `negative_halves_manifest.json`, седемте нови теста/харнеса (Ф6–Ф9, Ф12, Ф13, Ф15), **самият Ф10** и **добавените редове на `index.html`** (`--added-lines <БАЗА>`; цял файл не може — М: HEAD носи 3 исторически попадения). **НЕ са операнди:** `granitsi_client_probe.mjs` (2 попадения в договора ѝ) и `places/hotels/place_categories/hydrants.json` (`lat`/`lon` по подписана схема) | двойка с ТОЧКА в Д1 → 1; със ЗАПЕТАЯ → 1; **само САМОТНО число от вида на дължината → 1** (лови липсващия втори `-e`); несъществуващ път → 2; **`test_kill_switch.py` с вписана двойка → 1**; **в `ib_clone`: добавен ред в `index.html` с двойка → 1**. И шестте са задължителни |
| **Г16** | твърдите гейтове на varna_3d | `python src/qa_no_cad_ids.py` и `qa_no_extrude.py` **от `C:/git/varna_3d`** · преди К5 и К6, и на 16 | 0 | **в `ib_clone`, никога в живото дърво (О59), съдени от Ф3:** (а) `qa_no_extrude.py --break-me` → ≠ 0 (вградена, Ч `:137`); (б) фикстура със синтетичен кадастрален префикс → `qa_no_cad_ids.py` → 1 |
| **Г17** | **слепването по позиция (`entries_digest`)** | `tests.test_address_quarter_render` с `{ask:"render"}` и подменен дайджест · 13, 16 | 0; при разлика клиентът гаси думата | (а) ДРУГ `entries_digest` при същия брой → **1**, ако думата оцелее; (б) **разместени ДВА записа с ИДЕНТИЧЕН `tk`, РАЗЛИЧЕН `pin` и РАЗЛИЧЕН `entry_cell`** (М: 9 315 класа с общ `tk` срещу 18 с общи `tk`+`pin`) → отпечатъкът с пина ги хваща → 1, ако се покаже крив квартал |
| **Г18** | **един ключ = една клетка (при билда)** | `… --only ИБ1-Г18 --public <Ф4> --search-index <блоб>` · 6, 16 | 0; Ф2 смята клетките за всеки ключ `norm(formatAddressHit) + '\|\|' + g`, като **`-1` брои за СОБСТВЕНА клетка** (ИБ1-О1). **Пада над тавана 300 ключа / 31 000 записа** И настоява **всеки запис в многоклетъчен ключ да носи `-1`**. Точното число → Д6. **Клиентска половина НЯМА** (О101) | на многоклетъчен ключ е вписана ЕДНА клетка → 1; брой многоклетъчни ключове НАД тавана → 1 |
| **Г19** | **редове без адресен текст, голо име, locality** | **данни:** `… --only ИБ1-Г19 --public <Ф4>` (при Ф3) · **клиент:** `tests.test_address_quarter_render` с корпус 5 „гр варна район X“ + 1 „неизвестна“ + 5 с ГОЛО име (при Ф14) · 6, 13, 16 | 0; за (б) и (в) заглавието е днешното или името се среща **ТОЧНО веднъж**; **всеки запис/ред в клетка `locality` носи `-1`** (23 клетки · 15 132 · 14 823) | без клона (в) → „Аспарухово, гр варна район аспарухово“ → 1; без (б) → „Евксиноград, Евксиноград…“ → 1; **запис в `zpz` ≠ `-1` → 1** |
| **Г20** | **мета-гейтът на половините** | `python -m unittest tests.test_negative_halves` · 16 | 0; всяка половина е ТИЧАЛА с **точно обявения си изход**; `len(манифест) == 16` и множеството `gate` == Ф14 | ред с `expected_exit: 0` за падаща половина → 1; **манифест с ЕДИН махнат ред → 1** |
| **Г21** | **амандаментът носи шестте си реда** | `python -m unittest tests.test_adr_amendment` · **текстовата половина на 11**, пълният гейт на **16** | 0; Д2 съдържа `И-Б1-D1 … D6`, `D12`, `G29` + времевият ред К4 < К5/К6 (на 11); **`merge-base --is-ancestor <К4> <К8>` и `<К4> <К9>` → 0 САМО на 16 и в следпроверката** — по-рано изходът е 128 без присъда | махнат `И-Б1-D5` → 1; **в `ib_clone`:** К4 след К8 → ≠ 0 → 1; ОБЪРНАТ времеви ред → 1 |
| **Г22** | **четирите повърхности говорят ЕДНА дума** | `tests.test_address_quarter_render` с `render`, `panel`, `coord` за един обект · 13, 16 | 0; редът, панелът, попъпът и GPS казват **една дума** („Левски 1“); при вход панелът е блокът без `· вх. N`, но със СЪЩАТА дума. **Петата повърхност (`openCoordPopup:5884-5900`) е равна ПО КОНСТРУКЦИЯ** (същият `coordMeta`, `:5890`) | без Т9 → панелът „кв. Чайка, бл. 11“ срещу реда „Левски 1, бл. 11“ → 1; пипнат само `:5866` → два надписа → 1 |
| **Г23** | **`row_cell` и `rows_digest` (GPS)** | `tests.test_address_quarter_render` с `coord` · 13, 16 | 0; `row_cell.length === addressRows.length`; при разлика в `rows_digest` думата гасне и на GPS; при `-1` низът е днешният | крива дължина → **1**, ако думата оцелее; **разместени редове срещу СТАРИЯ `address_rows.json` → тестът иска `null`** → 1; **ТРЕТА, ПОПРАВЕНА (О108): срез БЕЗ `:5004`, вдигнат с `strict: true` в ПРЯСЪН процес → `ReferenceError: bestI is not defined` → 1** (без `strict` нищо не хвърля и `rowIndex` = 0 — М; в замърсен процес и `strict` минава); **ЧЕТВЪРТА: ред с водещо „кв чайка“ при `row_cell` = `levski1` → ДВЕ квартални думи → 1** |

**Нерегрес на varna_3d (Н1–Н9 на И-А §1.2)** — стъпки 7 и 16, същите команди и изходи. Промяна = **STOP 6**.

---

## 6 · Комити

**Идентичности** (D16 `010:68`; D20 `:76`): `Claude Executor <executor@local>` за код/тестове/гейтове; `Claude Architect <architect@local>` за планове и решения (К1, К4, К10); `Petar1984 <petar.dikov2019@gmail.com>` за данни и подписани тела (К3, К6, К8). Формата: `git add -- <изричните пътища>`, после `git -c user.name=… -c user.email=… commit -m "…" -- <изричните пътища>`; никакъв `add -A`/`.`/`--no-verify`. Съобщенията на агент завършват с `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; за ръката на Петър — не. Тема **≤ 72 знака, на английски**. Преди всеки комит: `git rev-parse --show-toplevel`.

| # | стъпка | репо · ръка | съобщение (дословно) | зн. | пътища |
|---|---|---|---|---|---|
| **К1** | 2 | Fire_Varna · **Архитект** | `docs(plan): sub-lot I-B1 signed by Petar (status line)` | 54 | `docs/plans/ПЛАН_Картата_imot_И-Б1_11.09.md` |
| **К2** | 4 | Fire_Varna · Изп | `test(gates): reland the privacy and client probes` | 49 | `gates/probe/b3_g11.py`, `tests/granitsi_client_probe.mjs` |
| **К3** | 5 | Fire_Varna · **Петър** (само при Р4-Б) | `chore(gates): commit the reference manifest as it stands` | 56 | `scratch/places_search/lot1v_v_reference_manifest.json` |
| **К4** | 6а | Fire_Varna · **Архитект** | `docs(adr): ADR 011 amendment for the address quarter` | 52 | `docs/decisions/011a_амандамент_И-Б1_11.09.md` + указателят в `011_kartata_imot.md` (Р5-Б); при Р5-А само вторият |
| **К5** | 7 | varna_3d · Изп | `feat(export): quarter index for the live map from v2` | 52 | Ф1, Ф2, Ф3 (`src/export_…`, `src/qa_…`, `tests/test_…_fixtures.py`) |
| **К6** | 8 | varna_3d · **Петър** | `data: signed quarter index for the live map` | 43 | `data/fire_varna_quarter_index.json`, `data/fire_varna_address_quarter_ledger.json` |
| **К7** | 9 | Fire_Varna · Изп | `test(gates): gates and fixtures for the quarter index` | 53 | **десетте нови пътя от Т13** (Ф6–Ф15, вкл. корпусът Ф11) + `gates/probe/b3_g11.py`, `gates/release.py`, `gates/run_gates.py` — изрично, без глоб |
| **К8** | 12 | Fire_Varna · **Петър** | `data: the quarter index for the address search` | 46 | `data/address_quarters.json` |
| **К9** | 13 | Fire_Varna · Изп | `feat(search): the address quarter comes from the polygon` | 56 | `index.html` (Т1 – Т12) |
| **К10** | 19 | Fire_Varna · **Архитект**, СЛЕД пуша | `docs(state): current numbers after the quarter index` | 52 | `docs/activeContext.md`, `docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` |

**Следпроверка след всеки комит:** `git status --short` · `git log -1 --pretty='%h %an %s'` · дължина ≤ 72 · **`PYTHONIOENCODING=utf-8 python gates/probe/b3_g11.py <БАЗА>` = 0** (Fire_Varna, от К2; за varna_3d важи Г16) · **`merge-base --is-ancestor <К4> <К8>` → 0 САМО след К8 и `<К4> <К9>` → 0 САМО след К9** (по-рано целта не съществува → изход **128** без присъда) · Г15 от К7 нататък, за К1–К4 — `python -c` обвивката. **Следпроверката след комит от ръката на Петър я пуска ИЗПЪЛНИТЕЛЯТ.**

**Ред, който е част от подписа:** К2 преди всеки Fire_Varna комит с код/тестове/данни · **К4 преди К5, К6, К8 и К9** · К6 и К7 преди К8 · **К8 и К9 се публикуват заедно или никое** · К10 след пуша. **`run_gates.py` се пипа ЕДИН път — в К7, само за четвъртия `SHA_PINS`** (О125).

---

## 7 · STOP

| # | условие | действие |
|---|---|---|
| **STOP 1** | път извън §3, мярката на стъпка 0 се разминава с §1, или има НОВО свързано дърво | спри, покажи `status --short` и `worktree list` — **числото в плана не се „поправя“** |
| **STOP 2** | изпълнението се разминава с плана и няма ред в §2 | спри; редовете ги внася Архитектът |
| **STOP 3** | архивът на Б3 не се прилага чисто, OID-ите не се възпроизвеждат, или стешът не е един | спри — паркиране без доказан архив губи подписана работа |
| **STOP 4** | `release` дава блокер извън назованите в §1.1, стъпка 5 и прозореца К7→К9 | спри |
| **STOP 5** | обявена половина **не падна**, падна с чужд номер въпреки `--only`, или изход 1 без присъда | спри и разследвай |
| **STOP 6** | Н1–Н9 или Г16 сменят изхода си | спри — регресия в частното репо |
| **STOP 7** | `ls-files --error-unmatch` ≠ 0 след `add`, или OID-сверката се разминава | спри — препис от по-стара ревизия минава структурните гейтове |
| **STOP 8** | `b3_g11.py` ≠ 0 (вкл. непразен `stray_new`) или липсва, когато стъпка го иска | спри |
| **STOP 9** | изписването се разминава с §3.Д, слепването се е сменило, или `quarterOfHit` дава `null` за флагмана | спри — сменено слепване = сменени резултати |
| **STOP 10** | агент пише ПОДПИС (`gates/allow/`, `baseline/MANIFEST.json`, опашката) | спри — ръката на Петър (`gates/sign.py:139-147`) |
| **STOP 11** | падащ тест извън обявения набор | спри — нерегресът е гейт |
| **STOP 12** | Г15 намери координатна двойка в проследен документ | спри — червен ред 5 е нарушен |
| **STOP 13** | Одиторът връща НЕГОДНО | спри, върни се при Петър |
| **STOP 14** | нужда от ред във `Varna_buildings` (вкл. ребилд на товарите) | спри — ръката на Петър |
| **STOP 15** | нужда от ред в `sw.js`, `address_rows.json` или `search_index.json` | спри — нов ADR, не редакция |
| **STOP 16** | Ф4 > **600 000 B** или първото зареждане > **5 242 880 B** | спри — таванът е подписан |
| **STOP 17** | товарите и `address_quarters.json` се разминават по дайджест или по брой | спри — позиционното слепване е счупено; **пали се от Г3/Г8/Г17/Г23 на стъпки 10, 13 и 16 (изход 1 → STOP 5; този ред е подписът му — Кими К44-8)** |
| **STOP 18** | документен/ADR комит на грешна ръка или на грешно място във времето (`merge-base` ≠ 0, К10 преди пуша) | спри — разделението на властите е гейт |
| **STOP 19** | две от четирите адресни повърхности дават **различна квартална дума** за един обект | спри — Г22 го съди |
| **STOP 20** | правилото ражда **над 500** нови слети надписа върху Ф11 | спри и питай (ИБ2-7) |
| **STOP 21** | агент пипне стейджа на Б3, или непаркиран негов път влезе в път-списък | спри — Б3 е И-Б2 |

---

## 8 · Локален преглед (Gate 2)

Пробите са **в игнорираната папка** `C:/git/Fire_Varna/scratch/boundary_gallery/granitsi_preview_05.09/` (Ч `.gitignore:64`).

1. **Д5** — „днес → по v2“ за **извадка от 40 адреса**: всички записи на Студентска (levski1 и levski2 един до друг), по един от топ-10 разминаванията, пет от **2 274-те вън**, **пет от 14 823-те реда в клетка клас `locality`** (там думата ГАСНЕ по D6), два „голо име“, два „гр варна район X“, два многоклетъчни ключа. **685 не влиза** — то е дифът v1→v2 (Ч `plan_v3.md:168`), друго множество. Само текст: нула координати, нула геометрия.
2. **Живата карта локално** (`python -m http.server 8000`, 375 px): „студентска бл 11“ → **„Левски 1, бл. 11 · район Приморски · написано: кв. Чайка“**, и след тап **панелът чете СЪЩАТА дума** · „студентска бл 14 вх а“ → редът носи `· вх. А`, панелът — блока без входа · **„студентска 1“ дава ред БЕЗ дума** (47-те слепени са многоклетъчни) до ред „Левски 1 … написано: кв. Виница“ · **GPS:** подай координата → „≈ N м от …“ носи квартала по полигон + свидетеля · **„чайка“ ВСЕ ОЩЕ връща Студентска бл. 11**, а „левски 1“ НЕ го връща (обявената цена) · при изключен флаг всичко е като днес.
3. **Картончето и `pl-meta` НЕ се проверяват** (ИБ2-3); разминаване ред ↔ картонче е обявената цена на разделянето.
4. **Д6** — всеки изход на §5 дословно, байтовете raw/gzip, първото зареждане преди/след, червените тестове по стъпки, Студентска, времето на износа, параметрите на среза.
5. **„Примери за поправка“:** Петър избира 5–6 места и ги проверява на око; несъгласие = ред в §2. **Офлайн:** с инсталиран пакет и изключена мрежа четирите повърхности дават ДНЕШНАТА написана дума — обявената цена на Р2 (3).

---

## 9 · Решения за Петър (Gate 1) — **осем**

„Да“ на плана без възражение = приемане на предложенията по подразбиране. **Изключение: Р6 иска изрично „да“ или „не“ от Петър — по мълчание не се приема (Кими К44-10).**

| # | Въпрос | Измереното | **Предложение по подразбиране** |
|---|---|---|---|
| **Р1** | **Какво става със стейджа на Б3, докато И-Б1 пипа `index.html` и `b3_g11.py`?** | 14 стейджнати пътя, **пресечка с лота точно ДВА** (М). Комит с pathspec записва СЪДЪРЖАНИЕТО НА ДЪРВОТО — комит на `index.html` при неразчистен стейдж би публикувал и хънковете на Б3 под `:6256`. | **Р1-А: Б3 се ПАРКИРА, не се решава.** 3а архивира дифа в два екземпляра и репетира хода в клонинг; 3б е **ръката на Петър**; **К2 веднага връща САМО скелето** по OID-ите. **Стешът НЕ се попва тук** — попът конфликтува по конструкция. **Съдбата на Б3 е на И-Б2** (ИБ2-1), с ТРИ носителя: двата архива и стешът. **Р1-Б: И-Б1 спира пред клиента** — доставя varna_3d, гейтовете и данните, а `index.html` чака И-Б2; цената е, че думата на Петър не се изпълнява сега. |
| **Р2** | **Носителят на квартала на адрес** | Леджерът покрива **43 295 от 86 232**; всеки запис носи `pin`. Байтовете са в §1.3. | **(в+) отделен файл с ДВА индекса — `entry_cell` (86 232) и `row_cell` (80 510)** + леджерът Ф5. Публичен път `data/address_quarters.json`; **таван 600 000 B**; лениво зареждане; първото зареждане не се мени. **Отхвърлена (в):** GPS остава на грешна дума за **1 871** реда (О68). **Подточки:** (1) пътят влиза в `ALLOWED_NEW`; (2) четвърти ред в `SHA_PINS` в ДВЕТЕ огледала; (3) **`sw.js:76` не се пипа** (ИБ2-7) — **обявена цена: с ИНСТАЛИРАН офлайн пакет и без мрежа файлът не се сваля и четирите повърхности показват СТАРАТА написана дума, не „никаква“**; (4) **без крипто в браузъра** — брой, типове и двата FNV-1a дайджеста в `ensureSearchData`, не в `prepareIndex` (М-о: 42 + 30 ms, веднъж на `focus`); (5) **кодирането е пинато** — UTF-8 байтове, координатите с `toFixed(7)`/`'%.7f'`; **(6) отпечатъкът носи `pin`/координатата** (иначе 79,2 % / 88,5 % се разместват невидимо — §3.Г). |
| **Р3** | **Какво показва редът и КОГА думата гасне** | Заглавието днес е `formatAddressHit` и е ключът на слепването; вторият ред е само „район X“. **4 121 от 9 002** етикета противоречат на v2; `levski1` е ГОЛО по регистъра, не по Р22 (Т4); числата за вън / `locality` / многоклетъчни са в §1.2–1.3. | **Заглавието се СТРОИ в `buildExactItem` (Т8); слепването остава на стария ключ.** Ред: **„Левски 1, бл. 11“**; втори „район Приморски · написано: кв. Чайка“ **само при разминаване**; името **ДОСЛОВНО** от `names[]`. **Думата гасне при `-1`, раздаван при БИЛДА** (§3.Д) — три подточки с цена: **(1) `locality` ГАСНЕ** (D6) — 15 132 записа · 14 823 реда без дума; *алт.:* името се изписва, но НЕ като квартал; **(2) многоклетъчният ключ ГАСНЕ** — 294 от 27 928 ключа (**1,05 %**); *алт.:* думата на първия по ранг, тоест кварталът се решава от класирането, не от адреса; **(3) GPS реже по ТОКЕН** (6 393 реда); *алт.:* не реже, а долепя „написано: …“. **2 274-те вън остават само на адрес — лепене по близост: НЕ.** **Алт. на целия ред:** заглавието остава днешното, кварталът — само на втория. |
| **Р4** | **Мръсният подписан артефакт** | `…/lot1v_v_reference_manifest.json` (174 717 B): тялото ≠ блоба на HEAD → `release.py:1145-1166` блокира **и след паркирането на Б3**; изворът е `recall_sweep.py:4103 → :2707` (`datetime.date.today()`). | **Р4-А: `git restore -- <пътя>`** на стъпка 5, след дифа на ТЯЛОТО. **И-Б1 не пуска `--manifest` и не презамразява** → изборът е ОКОНЧАТЕЛЕН (О31). **Р4-Б:** Петър го комитва както е (**К3**). `token_parity.json` не е в `signable()`. |
| **Р5** | **Къде живее амандаментът** | **ДВА прецедента (О5/О121):** D16 иска **нов датиран файл** (`010:68`), Б3 — същото (`ПЛАН_Б3_07.09.md:895`); И-А е сложил своя като редове в 011. | **Р5-Б (обърнато подразбиране, ИБ1-О16): нов датиран файл `011a_амандамент_И-Б1_11.09.md` + ред-указател в 011** — лотът остава без конфликт с ТЕКСТА на подписано решение; цената е една: **Ф15 и Г21 съдят този път**. **Р5-А:** редове в `011_kartata_imot.md` по прецедента И-А, като амандаментът сам ОБЯВЯВА отклонението от буквата на D16 — тогава то е СОБСТВЕН ред, не бележка. |
| **Р6** | **Етикетът върху публични точки (производната)** | `entry_cell` етикетира 86 232-та публични `pin`-а → 84-те непразни клетки се възстановяват с точността на адресната плътност. `_meta.publication`: „публично излизат само име, код и src“; G29 е в сила. D3 иска `polygon_sha256`, но `_meta` на `place_categories.json` и `expectations.json` **не носят такъв ключ днес** (О83). | **ДА — обявена производна**, с довода на Петър („координатата решава квартала“), записана в амандамента (D4); Г5 пази от БУКВАЛНА геометрия. **Подточка: `polygon_sha256` НЕ пътува публично** — `quarters_version` е проверявана СТОЙНОСТ (`=== 2`), sha-та живее във Ф5 и Д6. **Алт.:** `generated_from` получава `polygon_version`/`polygon_sha256` (бял списък 10 ключа + нова половина). **Или НЕ** — тогава лотът няма носител. |
| **Р7** | **Флагът при доставка** | ADR 001 D5 държи `APPROX_ADDRESS_SEARCH_ENABLED = false` от 07.2026 (Ч `:5926`) — прецедентът за жив флаг. | **`ADDRESS_QUARTER_V2 = true` при доставката**, флагът остава в кода, Г12 доказва байт-равенство при `false`. Връщане назад = една дума, без ревърт (§12). |
| **Р8** | **Спящата прибл.-адресна повърхност** (`buildApproxItem:6131`, `asr-meta :6138`) | Флагът `APPROX_ADDRESS_SEARCH_ENABLED = false` (`:6025`, `:6175`), а `:6138` изписва НАПИСАНАТА дума от `approx_addresses_v1.json` — **мъртъв код днес**. `asr-meta` живее и на `:5043` (Т10), и на `:5419-5420` (Т8) — тези ДВЕ се поправят. | **НЕ влиза в И-Б1** (О91): пребазирането иска ребилд на `approx_addresses_v1.json` (**STOP 14**); дълг **ИБ2-7**. **Алт.:** `:6138` минава през `quarterTitleWord` СЕГА — цената е още една повърхност в амандамента, нов Т-ред и нова половина. **Редът е тук, за да е изваждането ПОДПИСАНО.** |

---

## 10 · Какво НЕ се пипа

1. **`data/address_rows.json` и `data/search_index.json`** — нито един байт (Ч `007:13`, `006:157-163`; Г8 по sha).
2. **`C:/git/Varna_buildings`** — целият checkout, вкл. `config/quarter_registry.json` и двата билдъра (STOP 14).
3. **`sw.js`** и офлайн пакетът, `worker/`, `data/hydrants.json`, докладите, компасът, режимите на картата.
4. **`baseAddressLabel`, `formatAddressHit`, `dedupeDisplayRows`, `prepareIndex`, `fetchCachedJson`, скорерът и компараторът, `renderDetailSheet`, навигацията, всеки `asr-*` / `search-pin*` селектор** — пиннати и след амандамента. **Изключения, само тези:** (а) проекциите получават `_ord` (Т7); (б) `selectResult` — само `:5865`, `:5866`, `:5871`, `:5876` (Т9); (в) `coordMeta :5027-5033` (Т10); (г) `nearestAddressTo` — само `:5004`, `:5014`, `:5018`, `:5023` (Т11); (д) петте маркера (Т12); (е) споделеният блок между `:4835` и `:4836` (Т1).
5. **Целият път на МЕСТАТА и ХОТЕЛИТЕ** — `initPlacesSearch` (`:6248` нататък), картончето (HEAD `:7264-7267`), `pl-meta` (`:7236`), `QUARTER_CODES`, `QUARTER_SRC`, `LOC_KEYS`, `validLocation`, `validTypedLocation`, `validateHotels`, `validatePlaces2`, `ATTRIB_*`, `WM_URL`, `M7_PREFIXES`, `PLACES_CACHE`, трите sha пина — **нула байта** (И-Б2).
6. **`gates/run_gates.py`** — освен четвъртия ред на `SHA_PINS` (Т14); `ROW_FILES :74-77` — нула байта (О99).
7. **Дванайсетте непаркирани пътя на Б3 и самият стеш** (STOP 21).
8. **`varna_3d`:** v2 (вход), архивът на v1, `fire_varna_locations.py`, `qa_p8a.py`, `qa_place_zone_aliases.py`, `qa_fire_varna_*`, `export_fire_varna_{places,hotels}.py`, `build_place_categories.py`, подписаните леджери на И-А.
9. **`docs/decisions/006_*`, `007_*`, `008_*`, `010_*`** — нула байта; амандаментът е в **011a** (или в 011 при Р5-А).
10. **~80-те проследени `.md` с координатни числа** — отделен лот; тук само **не се добавят нови** (Г15).
11. **Публикуването.** Нищо не се пуши; Worker, R2, Pages — на Петър.

---

## 11 · Пренесено към **И-Б2** (с номерата от големия план)

Всеки ред е ПРЕПРАТКА към големия план, не нов обхват; таблиците и делтите живеят там.

**ИБ2-1 · Съдбата на Б3** — 12-те непаркирани пътя, реланд по поименни таблици (**Т34** 21 хънка при `-U0`, **Т36, Т44, Т45, Т25/Т28, Т27**); носители: двата архива и стешът (**О14, О76, О104, О132, О134, Р1**).
**ИБ2-2 · Местата и хотелите на v2** — `varna_3d` **Т1–Т9, Т42, Т43, Ф6**; `Fire_Varna` **Т17–Т20, Т31, Т33, Т35, Т38, Т39**; `QUARTER_CODES` 20 → 51, локалности 5 → 9, 165 реда, праг ИБ-STOP 20; **ИБ-Г10, ИБ-Г19**.
**ИБ2-3 · Картончетата и `pl-meta`** (дословната дума на Петър) — **Т38** (HEAD `:7264-7267`, `:7236`), **Т23, Т31, Р13, ИБ-Г25**; **32** доставени реда с написан квартал срещу v2 (**О9, О75, О77, О89**).
**ИБ2-4 · Правилата на търсачката (ИБ-22)** — **Т21, Т22, Т24, ИБ-Г13, ИБ-Г14**, кандидат-представките (**Р16/ИВ-9**).
**ИБ2-5 · Презамразяването, опашката и allow-файлът** — **Р7**, К10а/К10б, `baseline/MANIFEST.json`, `ЗА_ПОДПИС_A.md`, петте тела на `--manifest`, третият вектор на `coverage` (**О13, О32, О63, О73, О110, О116**).
**ИБ2-6 · Атрибуционният ред** — **Р11, Т17, Т27, Т37** (338-знаковият низ, ръката на Петър), **Т39**, интервал В (**О40**).
**ИБ2-7 · Обявените дългове** ИВ-1 … ИВ-14, вкл. офлайн пакетът (ИВ-3, цената е в Р2), шестте празни клетки (ИВ-4), спящата повърхност (ИВ-11, Р8), `SHA_PINS` на две места (ИВ-13). **ИБ2-8 · `b3_g11.py` без `reconfigure`** — поправката е **решение, не редакция** (**О133**).

---

## 12 · Откат (след публикуване)

| състояние | откат | цена |
|---|---|---|
| **И-Б1 публикуван** (К8 + К9) | **`ADDRESS_QUARTER_V2 = false` — една дума**, комит на Изпълнителя по указание на Петър. **Файлът ОСТАВА** — пинът пасва, триенето на блоба пали `release`; кешът не пречи (`sw.js:178-179`, network-first) | една дума, един пуш |
| **дефект в САМИТЕ данни** | Флагът стига (при `false` нито една повърхност не чете файла). Поправката е нов износ → ново подписано тяло (К6) → препис (К8) → нов пин (Т14) | цикълът от стъпка 6 |
| **нужда да се махне файлът** | `git revert` на К8 **И** на реда от Т14 в двете огледала в ЕДИН чифт — иначе `release` блокира за липсващ блоб (`release.py:1180-1195`); К9 може да остане | два ревърта, един пуш |
| **Б3** | Стешът и двата архива са непокътнати; входът на И-Б2 е `B3_staged_11.09.patch` + `stash list` | нула |
