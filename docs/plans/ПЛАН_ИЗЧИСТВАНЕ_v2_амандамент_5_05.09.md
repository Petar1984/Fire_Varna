# Амандамент №5 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 09:00) — след одита на A.2 (НЕГОДНО: 2 major, 5 minor); всичко се приема; отговорите на изпълнителя

A.2 е доставен (46899d8 A.2-1, 73d0b2a A.2-2, ca88238 A.2-3, 8a9756d A.2-4): release-гейт с 7 инжекции, `gates.sign`, куката с unittest + run_gates без break-glass, `expectations.json` (321 040 B, 78 гейт-заявки, replay за 6 кофи), `--freeze` смята първо и пише последно, 11 котви от blob-а; **решаващият експеримент възпроизведен от одитора: подпис + едно замразяване в изолиран клон → 266 теста, 0 червени, release зелен.** Червените днес: 6 (всичките четат `expectations.json`).

## Major → A.2-5 (един комит, Fire_Varna)
1. **`--freeze` не чете опашката:** ред, ОТКАЗАН от Петър („не“, терминално), се заличава от едно замразяване и всичко позеленява. → `--freeze` добавя в блокерите резултата на `gates.release.run()` срещу ТЕКУЩАТА референция и опашката: замразяване само при 0 непокрити и 0 отказани делти; `build_expectations` записва отказаните делти в `_meta.refused`, а `gates/release.py` ги сверява и срещу ЗАМРАЗЕНАТА референция (отказът преживява замразяването). Счупен вход: опашка с „не“ над живи делти → `--freeze` ≠ 0, `git status` чист.
2. **`gates.sign` пише артефакти ПРЕДИ да реши да откаже** и после твърди „НИЩО НЕ Е ЗАПИСАНО“. → „смятай първо, пиши последно“: `apply_signature` връща буфери; записите (артефакти + опашка) се изпълняват само при празен `refusals`. Счупен вход: непознат артефакт в ред 2 → нула променени файлове.

## Minor → същият комит
3. Проверка 7 сверява и ТЯЛОТО: най-новият комит, пипал подписан артефакт, е на Petar1984 (или дайджестът на тялото е записан в подписващия комит/реда на опашката и съвпада) — иначе червено; счупен вход: агентски комит пренаписва `expectations.json` след подписа → ✗.
4. `token_parity.json` влиза в последния блок на записите („гейтовете първо, записът последен“).
5. Проверка 6 чете подписваемите артефакти през blob-а на HEAD (или сама сверява работно дърво ↔ blob).
6. Git авторът е свободен низ — бариерата е процесна; записва се в ADR 009 като такава (GPG подпис = бъдеща техническа гаранция, ако Петър поиска). Без промяна в кода.
7. Тестовете не проверяват подписа (само наличието на `expectations.json`) — **приема се разчитането на изпълнителя**: тестовете пазят „двигател = проследени очаквания“, подписът се пази от release-гейта (проверка 6) и от `--freeze`. Един ред в `require_expectations` връща старото поведение, ако Петър поиска.

## Отговори на въпросите на изпълнителя
- Разчитането на §0.4/§A.2 (6 червени, не 48) — ДА (т. 7).
- „русалка“ (падна още от лот В: РУСАЛКА напусна к.к. Златни пясъци; 61/62 в §10 sweep) — влиза като подписваема известна разлика в `expectations.json` и в опашка A (ред R8: „русалка — известна разлика след лот В; трайното решение е в 8-те сблъсъка“).
- Форматът на опашката: markdown `- **ключ:** стойност` с полетата на §0.6 — ДА.
- Покритие `кофа/*` за 150-те реда по клас — ДА (вързано за дайджеста на кандидата; ред-по-ред остава като избор на Петър при подписа).
- Контролата „гейт 6“ на Сол след М7 се държи от реда на клоновете; правилото за колизия остава, промяната е записана в манифеста P7→F12 (двата контрола) — без нови собствени редове.

## Ред
A.2-5 → одит (фокус: опашката преживява замразяването; sign без странични записи; проверка 7 по тялото; повторен решаващ експеримент с ред „не“) → A.3 опашката → Петър: една команда → изпълнител: едно замразяване, 0 червени, run_gates зелен → одит → команда за push → проверка на публикуваните хешове → локалната карта с чисти данни.
