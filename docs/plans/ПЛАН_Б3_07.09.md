# ПЛАН Б3 · Търсене по квартал над подписаните граници (Fire_Varna) — v3

**Дата:** 07.09.2026 · **Планер:** Claude Planner (read-only) · **Статус:** **ПОДПИСАН от Петър (07.09.2026): „подписвам плана Б3 v3 по препоръките“** — подписаното тяло е блобът в комит acddcd1, sha256 `51fde25f1be25a639c6f4f0a5a2b5c6dafe09f697498e8248b80c6cbdd7c343b`; §9 се чете по препоръките (виж §14) · **Заменя:** v2 (`e6ba50e`, чернова, неподписана), след две външни оборвания — **Astra S28** (механика, 10 условия) и **Кими К28** (данни/семантика, 7 условия), и двете **НЕГОДНО** (§12).
**Клас на риска:** 🔴 **Архитектурен** (ескалационно-доминантен). Спуснали се спусъци: (1) промяна на схема/ADR — `_meta` 9→10 и 7→8 ключа, затворените списъци `QUARTER_SRC`/`QUARTER_CODES`/новият `LOCALITY_SRC` са договор, ADR 010 D3/D12/D13 носят остарял текст; (2) необратима канонична мутация — замразената референция, `expectations.json`, подписаният allow и **изваждане на подписана опашка от глоба на release**; (3) release/publish верига — този комит е това, което Петър пуска към живата карта; (4) кръстосан blast radius varna_3d → Fire_Varna по подредената тройка D13. Топология: **Вариант А** (планер → подпис на Петър → изпълнител → одитор → преглед на Петър → пуш от Петър).
**Записване:** планерът е read-only; файлът е записан от архитекта (чернова acddcd1, подпис в следващия комит). §4 стъпка 1 е изпълнена с това; изпълнителят я пропуска.

> **Произход на числата (честно).** v2 беше писан без Bash — всяко число там беше или **(Ч)** прочетено, или **(М)** от чужда мярка. **v3 е измерен от планера със собствени команди.** Всяко число долу носи командата, с която е получено. Числата, които не съм могъл да измеря без запис в репото, са измерени в **клонинг** (`git clone --shared C:/git/Fire_Varna <scratchpad>/shadow`) — това чете източника и не пише в него; такива числа са маркирани **(К)** и изпълнителят ги преизмерва на място.
>
> **Нищо не е писано, комитвано или пускано в `C:/git/Fire_Varna` и `C:/git/varna_3d` от тази сесия.**

---

## 0 · Червени редове (наследени, без изключение)

Дословно от `C:/git/Fire_Varna/docs/plans/ПЛАН_НОЩНА_СМЯНА_Границите_06.09.md:11-23`:

1. **Агентите не пушат.** Никога, с никакъв флаг, без `--no-verify` (`:13`).
2. **Нула геометрия в проследен файл на Fire_Varna.** Публично пътуват само име, код и картата за атрибуция (`:14`).
3. **`Varna_buildings/config/quarter_registry.json` не се пипа** (`:15`).
4. **Написан квартал не се променя** (D18/G17) (`:16`).
5. **Малки, обратими комити**, изчерпателен write-set, дословно съобщение, автор `Claude Executor <executor@local>`; **подписаната геометрия и решенията чакат комит на `Petar1984`** (D16, G4 fail-closed) (`:17`). → О4 и **О24**: авторството се НАЛАГА с флагове, не се предполага.
6. **Всеки гейт е команда с изход ≠ 0 и с отрицателна фикстура, която ТРЯБВА да падне.** Паднал гейт СПИРА лота (`:18`).
7. **Само Opus** — изпълнители и одитори (`:19`).
8. **Одиторът е друг агент** (read-only) (`:20`).

Допълнителни за този лот:

9. **Кръстосаната изолация е еднопосочна:** от Fire_Varna се ЧЕТЕ блоб на varna_3d (`git -C ../varna_3d show <rev>:<път>`, командата е записана в `tests/test_places_public_bundle.py:49`). Нищо не се пише, комитва или пуска във varna_3d от тази сесия.
10. **Нищо не се публикува преди локалния преглед на Петър** (`python -m http.server 8000`, §8).
11. **„pending — Петър“ не е подпис.** `gates/run_gates.py:775-790` връща 1 и при жълто.
12. **Точно ЕДНА опашка в HEAD и точно ЕДИН allow-json.** (Ч) `gates/release.py:171-172` (`QUEUE_DIR = "scratch/places_search"`, `QUEUE_GLOB = "ЗА_ПОДПИС_*.md"`), `:437-458` (`queues_in_head` чете `git ls-tree -r -z HEAD` и сравнява **basename**-а), `:465-472` (>1 → `raise ValueError`), `:1259-1265` (изключението → BLOCKED). Allow: `gates/run_gates.py:199-217`.
13. 🔴 **Нов (О30): `recall_sweep.py --manifest` ПРЕЗАПИСВА `expectations.json`.** Вторият прогон в същия ден е зелен по конструкция. Доказателство се снема САМО от ПЪРВИЯ прогон срещу подписаното тяло.

---

## 1 · Мярка (базата, преди един ред промяна)

### 1.1 Състояние на репата — **преизмерено 07.09, котвата се движи**

| какво | стойност | команда |
|---|---|---|
| Fire_Varna HEAD | **`5fce47c`** „state: §8 — Б3 plan v2 measured findings, S28/К28 verdicts, revision 3 in progress“ | `git -C /c/git/Fire_Varna log -1 --pretty='%h %an %s'` |
| предходни котви | `1e6f85b` (К28), `8715a6c` (S28), `e6ba50e` (план v2), `f00dd78` (край на Б2) | `git log --oneline -5` |
| проследено дърво | **чисто** (0 реда без `??`) | `git status --porcelain \| grep -v "^??" \| wc -l` → `0` |
| ahead от origin | **85** | `git rev-list --count origin/main..HEAD` |
| varna_3d HEAD | `05f485e` (клон `rezhimi`) — трите блоба на доставката | `git -C /c/git/varna_3d log --oneline -3` |
| Node | **v22.19.0** | `node --version` |
| опашки в HEAD | **1** — `scratch/places_search/ЗА_ПОДПИС_A.md` | `git ls-tree -r --name-only HEAD -- scratch/places_search \| grep "ЗА_ПОДПИС"` |
| allow-файлове | **1** — `gates/allow/2026-09-05_lot1v_v.json` (150 реда, `signed_by: Петър`) | `python -c "import pathlib; print([p.name for p in pathlib.Path('gates/allow').glob('*.json')])"` |

**Стъпка 0 на §4 преизмерва и петте — HEAD се движи, докато планът чака подпис.**

### 1.2 Днешните котви в `C:/git/Fire_Varna/index.html`

| котва | ред | текуща стойност |
|---|---|---|
| `PLACES_CACHE` | `:6265` | `'fire-varna-hotels-v6-225'` |
| `HOTELS_SHA256` / `PLACES2_SHA256` / `CATS_SHA256` | `:6288` / `:6289` / `:6290` | `46a44ce8…` / `329310f5…` / `874e33cd…` |
| `LEGACY_BUNDLE_SHA` | `:6301-6304` | places `329310f5…`, hotels `46a44ce8…` |
| `QUARTER_SRC` | `:6313` | `['REG', 'KAIS', 'SIGNED_OVERRIDE']` |
| `DISTRICT_SRC` / `DISTRICT_CODES` | `:6314` / `:6315-6316` | 2 извора / 5 кода |
| `QUARTER_CODES` | `:6317-6321` | **20 кода** (масив, не `Set`) |
| `LOCALITY_CODES` | `:6322` | 5 кода |
| `isSignificantToken` | `:6495-6496` | `!t.num && t.orig.length > 2 && t.s.length > 2 && …ADDR… && !GENERIC_WORDS.has(t.s)` |
| `validLocation` | `:6528-6534` | `src ∈ srcs && code ∈ codes` |
| `validTypedLocation` | `:6535-6545` | **`:6537` валидира `locality` със `QUARTER_SRC`**; `:6544` `zone === quarter.name ?? 'район '+district.name` |
| `validateHotels` / `validatePlaces2` | `:6546-6562` / `:6563-6580` | **не броят ключовете на `_meta`, не четат `quarter_attribution`** |
| **`accept`** | **`:6602-6612`**, условието на **`:6605`** | `if (window.crypto && crypto.subtle) { … digest ≠ sha → return null }` — **валидаторът се вика САМО след успешна sha-сверка** (1.8.11) |
| `fetchValidatedJson` / `ensurePlaces` | `:6613-6625` / `:6626-6647` | `throw 'places payload unavailable'` само ако И двата масива са празни (`:6639`) |
| **`buildIndex`** | **`:6648`** | **приема ВЕЧЕ разпарсени масиви — не вика нито един валидатор** |
| `makeRec` qtk/qph | `:6749`, `:6782` | — |
| **М7 клонът на клиента** | **`:7058-7069`** | `R.every(isSignificantToken)` + `qHit`/`lHit`/`bphrase` — **тук няма списък с типови префикси** (К28-5) |
| районните клонове | `:7064`, `:7118` | `!r.e.quarter` — двете места на типовия капан |
| изричното „район X“ с ключ | `:7104-7109` | филтрира САМО по `r.dph`; **не гледа `quarter`** |
| `popupFor` | `:7253-7289` | редът „вид · квартал · район“ на `:7257-7271`; **ред за извор НЯМА и днес**; ползва `span` (`:7155`), `buildNavActions` (`:4255`), `SRC_LINE`/`ADDR_SRC_LINE`/`ALIAS_SRC_LINE` (`:6328-6360`) |
| Leaflet | `:1900` `zoomControl → bottomleft`; `:1901` `OSM_ATTRIB = '© OpenStreetMap'`; `:1917` `attribCtrl` (`position: 'bottomleft'`); `:1918` `attribCtrl.addAttribution(OSM_ATTRIB)` | размени: `:4543-4549`, `:4663`, `:4672-4676` |
| pmtiles флаг + override | `:4563` `false`; `:4571-4574` `isBasemapPmtilesActive()` чете `localStorage` | пътят е достижим и днес |
| **зареждане при първо отваряне** | `:1772-1774` `fetch('data/hydrants.json')` — **безусловен IIFE** | `ensurePlaces()` се вика САМО от `inputEl.addEventListener('focus', …)` (`:7393`); адресната търсачка е lazy (`:5325-5326`, `fetchCachedJson` на фокус); `data/approx_addresses_v1.json` е зад изключен флаг (`:5926 APPROX_ADDRESS_SEARCH_ENABLED = false`); `data/basemaps/basemap_manifest.json` — само при активен pmtiles (`:4563` false) |

Нула попадения за point-in-polygon. **Кварталът е доставено поле + речник от думи, не геометрия.**

### 1.3 Липсващите кодове

Доставката носи **28** квартални кода; `QUARTER_CODES` носи 20; липсват точно **8**:
`dolna_traka · grackata_mahala · hristo_botev · kolhozen_pazar · kv_levski · sv_ivan_rilski · trakia · tsentar`.
`set(QUARTER_CODES) − доставени = ∅`. Делтата е **+8**, не „+41“ (О3).
Речникът (`place_categories.json.locations.quarter`) носи **точно същите 28 кода** — `dict == delivered: True`, `parents` **0 от 28**, `locality` 5, `district` 5.

### 1.4 Новите пинове (LF-блобове от git, не от диска)

Команда:
```bash
git -C /c/git/varna_3d show 05f485e:data/fire_varna_places.json > <scratch>/new_data/places.json   # и другите два
python - <<'PY'
import hashlib, gzip, pathlib
for p in ['places.json','hotels.json','place_categories.json']:
    b = pathlib.Path('<scratch>/new_data/'+p).read_bytes().replace(b'\r\n', b'\n')
    print(p, len(b), hashlib.sha256(b).hexdigest(), len(gzip.compress(b, 9)))
PY
```

| файл (източник → цел) | sha256 | bytes | gzip9 |
|---|---|---|---|
| `varna_3d:data/fire_varna_places.json` → `data/places.json` | `3dffc2649b75792aaa94ede70cf9f3a0805d3f53c4721be48375c3e1a1533e66` | **128401** | **14748** |
| `varna_3d:data/fire_varna_hotels.json` → `data/hotels.json` | `eb8fa85cbb67265ad99cacbf12a7f9ccb84bb5431b27b6446837318e1ccfee51` | **151263** | **13642** |
| `varna_3d:data/place_categories.json` → `data/place_categories.json` | `9064705a06477c1a89079616ba7545ca8f51b08220a90b9cad9a915ec232b231` | **77998** | **9690** |

**Капан:** работното дърво на varna_3d е CRLF за два от трите — мери се `git show`, не `os.path.getsize`.

### 1.5 Какво носи доставката — **преизмерено, с поправка на v2**

- places **150** / hotels **225**; нула добавени, нула изтрити.
- Извори на `quarter`: places `{SIGNED_POLYGON: 42, REG: 39, KAIS: 10, null: 59}`; hotels `{REG: 115, KAIS: 25, SIGNED_OVERRIDE: 12, SIGNED_POLYGON: 15, null: 58}`. **`DISTRICT_FALLBACK`: 0 реда.**
- **`zone !== quarter.name`: 0 реда** в двата пакета (О20 остава верен).
- 🔴 **Поправка на v2 §1.5.** v2 пише „`quarter_attribution` = 18 кода“. Измереното: картата е **на всеки пакет поотделно** — **places 14** (13 wikimapia + 1 openstreetmap), **hotels 7** (всичките wikimapia); **обединението е 18**, общите са **3** (`kolhozen_pazar`, `kv_levski`, `tsentar`). Това е ЕДИНСТВЕНОТО правилно основание за G17.
- 🔴 **Всяка карта е ТОЧНО равна на множеството `SIGNED_POLYGON` кодове на своя пакет** (`qa == sp: True` и за двата; `qa−sp = ∅`, `sp−qa = ∅`). Гейтът е **равенство**, не „включване“ (по-силно от буквата на амандамент №2).
- Запис на всеки код: точно четирите ключа `{label, licence, src, url}` (едно-единствено ключово множество за всичките 21 записа).
- Единственият не-wikimapia запис: `mladost2 → {"src": "openstreetmap", "label": "© OpenStreetMap contributors", "licence": "ODbL", "url": "https://www.openstreetmap.org/copyright"}` — **url-ът НЕ е по формата на wikimapia** и всяко правило „един регекс за всички“ пада върху него.
- Wikimapia url-ите са **`http://wikimapia.org/<id>/bg/<път>`** (пример: `http://wikimapia.org/900042/bg/ж.к._Васил_Левски`) — `http`, с път след id-то (О17).
- Речник: зони 20→28, псевдоними 39→48; `parents` — **0 от 28**.
- Нула геометрия, нула кадастрален идентификатор (структурна проверка, 1.8.12).

### 1.5б 🔴 `_meta` — точните множества ключове (единственият авторитет на G17)

```
places._meta  (10): generated · count · by_kind · by_zone_src · licence_osm ·
                    licence_registry · licence_wikidata · licence_address ·
                    quarter_attribution · excluded
hotels._meta  (8):  generated · count · by_zone_src · licence · licence_osm ·
                    licence_address · quarter_attribution · excluded
```
Днешните (старите) са **9** и **7** — същите списъци без `quarter_attribution`. Броят е **следствие**; договорът е множеството.

### 1.6 Какво умира днес, ако блобовете влязат сами — **изпълнено, не изведено** 🔴

Node-проба върху среза `index.html:6256-7154` с ДНЕШНИТЕ списъци над НОВИТЕ байтове (стъбове: `window`, `caches`, `document`, `map`, `fetch`, `attribCtrl`, `L`, `console`, `crypto`):

```
validateHotels   = false        ← хотелите СЪЩО носят SIGNED_POLYGON (15 реда)
validatePlaces2  = false
accept(text, пинат sha, validatePlaces2) = null
await ensurePlaces()  →  ХВЪРЛЯ  Error: places payload unavailable      (:6639)
```

**Това оборва v2 §3.6 т. 4 („`RECS.length === 225` при среза с оригиналния `QUARTER_SRC`“): при стария списък НЯМА `RECS` — има изключение.** 225 е достижимо само когато **само** places е отказан (1.8.10, случай 9в).

### 1.7 Референцията и фикстурите

| прогон | резултат | exit |
|---|---|---|
| `python -m unittest tests.test_granitsi_fixtures -v` | `Ran 5 tests · OK` | 0 |
| `python -m unittest discover -s tests` | `Ran 315 tests · OK` | 0 |
| `python -m gates.run_gates` | 7/7 зелени | 0 |

Върху **новата** доставка (двигателят е `recall_sweep.py` с подменени пътища към новите блобове, `main()` не се вика — нула запис):

| заявка | днес | нова доставка |
|---|---|---|
| „детска градина дойран“ | `A3-street`, 1 | `A3-street`, **1** |
| „училище район младост“ | `A3-district`, 12 | **12** (9 с квартал) |
| „младост“ | `M7-bare`, 28 | **20** (8 с квартал / 12 без) |
| „училище младост“ | `A3-location`, 11 | **6** (3 с квартал) |
| „възраждане“ | — | `M7-bare`, **5** |
| „училище възраждане“ | — | **1** (`II ОУ „Никола Йонков Вапцаров“`) |

🔴 **К28-3 · Осемте изгубени реда на „младост“, поименно и с новия си квартал** (v2 назоваваше пет):

| # | ред | стар `zone` | нов квартал |
|---|---|---|---|
| 1 | `ОУ „Иван Вазов“` | район Младост | `pobeda` · ж.к. Победа |
| 2 | `Професионална гимназия по електротехника` | район Младост | `pobeda` · ж.к. Победа |
| 3 | `Професионална Техническа Гимназия` | район Младост | `pobeda` · ж.к. Победа |
| 4 | **`ДГ№53 „Слънчево зайче“`** | район Младост | `sv_ivan_rilski` · кв. Св. Иван Рилски |
| 5 | `ОУ Отец Паисий` | район Младост | `sv_ivan_rilski` · кв. Св. Иван Рилски |
| 6 | **`ДГ№26 „Изворче"`** | район Младост | `troshevo` · кв. Трошево |
| 7 | `ОУ Добри Чинтулов` | район Младост | `troshevo` · кв. Трошево |
| 8 | **`ДГ 24 "Детско градче"`** | район Младост | `troshevo` · кв. Трошево |

Механизмът (Ч): районът отговаря само докато `quarter is None` (`recall_sweep.py:1123`, `:1167`; `index.html:7064`, `:7118`). **Загуба по замисъл — иска подпис, и опашката ѝ дължи ОСЕМ делта-реда, не пет** (§4 стъпка 11, STOP 19). „училище младост“ губи 5 от същите осем (1, 2, 3, 5, 7).

### 1.8 🔴 Мерките, които оборванията добавиха (новите СТОП-числа)

| # | мярка | стойност | команда |
|---|---|---|---|
| 1.8.1 | покритие срещу подписания baseline `f06ac06` с новите блобове, `allow_path=None` | **exit 2 · uncovered 138** (places `zone_named` lost 44 + changed 44 = 88; hotels lost 39 + changed 11 = 50); нетен спад по `places.zone_named` (127→91) и `hotels.zone_named` (199→167) | §5 G18 |
| 1.8.1б 🔴 | **същото, но `base = f00dd78` (данните на днешния HEAD)** — дифът, който ОБЯСНЯВА 138-те (К28-6) | **exit 2 · uncovered 150** (places 78+18=96; hotels 47+7=54). Значи доставката **връща 12** от вече разрешените редове: **150 → 138** | §4 стъпка 2 |
| 1.8.2 | покритие при `base == candidate` | exit **3** · `allow-файл с изброен, но непроменен ред` · stale 150 | §11 т. 1 |
| 1.8.3 | покритие срещу `f06ac06` с новите блобове **и днешния подписан allow** | exit **2** · uncovered **30** · **stale 42** (150 − 42 = 108 ползвани; 108 + 30 = 138 ✓) | §4 стъпка 2 |
| 1.8.4 🔴 | „кв. Левски“ / „кв. Тракия“ / „кв. Христо Ботев“ / „ж.к. Св. Иван Рилски“ | `M3`, съответно **4 / 1 / 24 / 22** реда. **Поправка на v2 (К28-1): „кв. Христо Ботев“ носи ЕДИН ред с търсения код** — `ЦДГ 8 - Христо Ботев` (`hristo_botev`), и то **по ИМЕ**, не през кварталния механизъм. Другите три носят **нула** с търсения код | двата двигателя |
| 1.8.5 | „левски“ (голо) | `M7-bare`, **14** реда, **12** с код `kv_levski` | двата двигателя |
| 1.8.6 | „възраждане 2“ / „ж.к. възраждане 2“ / „младост 2“ | **0 реда** (`M3`) | двата двигателя |
| 1.8.7 | „чайка“ | `M7-bare`, **30** реда — `chaika_kk` 19 + `chaika_kv` 10 + 1 без квартал (`„ДКЦ Чайка“ ЕООД`) | двата двигателя |
| 1.8.8 | „аспарухово“ / „дружба“ | **16** (9 `asparuhovo` + 7 „район Аспарухово“ без квартал; **без** `ЦДГ 43 „Пинокио“`) / **2** | двата двигателя |
| 1.8.9 | „кайсиева градина“ / „училище възраждане 3“ | `M2-failopen` 3 / `M2` 2 (`3 ОУ Ангел Кънчев` [кв. Левски], `III ПМГ` [кв. Тракия]) | двата двигателя |
| **1.8.10** 🔴 | **Node-матрицата на приемането** (изпълнена, §3.6) | (а) стар `QUARTER_SRC` → `false/false`, `ensurePlaces` **ХВЪРЛЯ**; (б) след И2/И4/И5 → `true/true`, **`RECS === 375`**; (в) чужд код САМО в places (пинът = sha на мутираното тяло) → `validateHotels true`, `validatePlaces2 false`, **`RECS === 225`**, без хвърляне | §3.6 |
| **1.8.11** 🔴 | **crypto × sha × брой извиквания на валидатора** | `ON` + грешен sha → `null`, **0 извиквания**; `ON` + верен sha → `null`, 1; `OFF` + грешен sha → `null`, **1**; `OFF` + верен sha → `null`, 1. **Значи `null` сам по себе си НЕ доказва отказ на валидатора** | §3.6 |
| **1.8.12** 🔴 | **структурна геометрия** по 18-те проследени `data/**/*.json` | **0** ключа `coordinates`/`geometry`/`geometries` след `json.load`. Буквалният `grep coordinates` дава едно попадение в `data/hydrants_provenance.json` — **проза в стойност** (`"…replaced landmark label … with the hydrant coordinates"`), не ключ | §5 G11 |
| **1.8.13** 🔴 | **първо зареждане** (`AGENTS.md:53`: `index.html` + `data/hydrants.json`) | `index.html` **560 855** B + `data/hydrants.json` **1 317 355** B = **1 878 210 B ≈ 1,79 MB** при твърд таван 5 MB → **exit 0**. И двата файла са LF (0 CRLF). Отложената доставка на търсенето (hotels+places+categories) е **357 662 B ≈ 0,34 MB** и се брои отделно | §5 G10 |
| **1.8.14** 🔴 | **командата на v2 за G10** (`git ls-files index.html 'data/*.json'`) | **19 файла, 26 839 286 B, exit 1** — червено ПРЕДИ каквато и да е промяна: списъкът лови архивни снимки (`hydrants.json.pre_*`), `search_index.json` (11,2 MB), `address_rows.json` (5,07 MB), `hydrants_provenance*` (5,57 MB) и вложените basemap json-и | §13 |
| **1.8.15** 🔴 | **`recall_sweep.py --manifest` срещу ПОДПИСАНИТЕ очаквания, с новите блобове** (К) | **exit 1**; **59 реда `ЧЕРВЕНО`**; **41 уникални заявки** + 2 реда без заявка. Гейтове: П7 **16/32**, ЛОТ 1 **6/18**, 1в-А **5/12**, 1в-Б **5/6**, 1в-В **4/10** (квартал 258 · доп. 12 · район 375/375), М7 **17/17**, §10 sweep 60/62 · 10/10, „срещу подписа 1“; вътрешният release: `BLOCKED: 4` | §4 стъпка 10 |
| **1.8.16** 🔴🔴 | **ВТОРИЯТ `--manifest` прогон** (К) | **exit 0, 0 реда `ЧЕРВЕНО`** — защото първият вече е презаписал `expectations.json` (report-only, `signed_by: pending — Петър`). **Повторното генериране е вакуумно зелено** | червен ред 13, STOP 20 |
| **1.8.17** 🔴 | **`P7_ADDED` дрейф** (нова находка, нито S28, нито К28) | стари данни: **2 токена в 2 зони**; нови: **7 токена в 5 зони** (`Гръцката махала: gratska, gratski`; `к.к. Св. Св. Константин и Елена: konstanin`; `кв. Владиславово: varnenchik, vladislav`; `кв. Изгрев: zhkizgrev`; `кв. Левски: vasil`); засегнати записи **48 → 64**. Подписаното в `expectations.json.p7` е `tokens: 2, zones: 2`, а неговият `foreign_guard` изрично записва, че `vladislav`/`varnenchik` са БИЛИ отхвърлени като чужди. Новият речник ги допуска | §4 стъпка 2, опашка |
| **1.8.18** | `python -m gates.release` с новите блобове (К) | **exit 6**, `⛔ BLOCKED (2)`; **119 делти** (референция 203 заявки / 3160 реда · кандидат 226 / 3222); опашка `ЗА_ПОДПИС_A.md` — **27 реда** | §4 стъпка 10 |
| **1.8.19** 🔴 | **архивирането на старата опашка** (К) | `git mv … архив/ЗА_ПОДПИС_A_05.09.md` → `queues_in_head()` = **`['scratch/places_search/архив/ЗА_ПОДПИС_A_05.09.md']`** — подпапката **НЕ бяга** от глоба (`ls-tree -r` + `fnmatch` по **basename**). `git mv … архив/АРХИВ_A_05.09.md` → **`[]`**. **Спасява само СМЯНАТА НА ИМЕТО** | §3.8, G15 |
| **1.8.20** | старата опашка | **27 „да“, 0 „не“**; 10 реда `артефакт`, 16 реда `покрива` (делта), 1 ред `въпрос` | `grep -c` по `**решение:**` |
| **1.8.21** 🔴 | **по-евтината поправка на D12** (К28-5) — вж. §9 т. 1 | „кв. Левски“ **4 → 14** (12 с `kv_levski`); „кв. Тракия“ **1 → 4** (4 с `trakia`); „кв. Христо Ботев“ **24 → 4** (3 с `hristo_botev`); „ж.к. Св. Иван Рилски“ **22 → 4** (2 с `sv_ivan_rilski`). Цена на презамразяване: **0 промени** в 156-заявъчния подписан корпус И **байт-еднакъв** дъмп на референцията (1 273 012 B) | §9 т. 1 |
| **1.8.22** | `scripts/hooks/pre-push` ↔ `.git/hooks/pre-push` | **2 574 B, байт-еднакви**; в PowerShell `diff` е **`Alias → Compare-Object`** (сравнява обекти, не файлове) | §5 G14 |
| **1.8.23** | низът за атрибуция | **338 знака**; `sha256(utf-8)` = `c6d29d4e1d3c4fa916224ae03ceab3682fd43cc83d3f22b920183d9b82d4002d`; `gates.release.body_digest` на файла с решенията = `c57a79c222361dc5c803823883374f9e1dcd12d36e7b27f05788ac17b7c22bfc`; блобът на файла (LF) = `74f09d505e2b…`, 32 129 B | §5 G9 |

Механизмът зад 1.8.4–1.8.6 е прочетен, не предположен (Ч): `index.html:6495-6496` и `recall_sweep.py:424-431` изхвърлят числата (`t.num`) и всеки токен с `s.length ≤ 2` — „кв.“, „ж.к.“, „к.к.“ и „2“ не са значещи, а М7 клонът иска `R.every(isSignificantToken)` (`index.html:7059`). **Затова осемте нови квартала се търсят само с голото си име, а детето няма собствена фраза.**

### 1.9 Гейтът на доставката

- `gates/allow/` — точно един json: `2026-09-05_lot1v_v.json`, `signed_by: "Петър"`, 150 реда.
- `gates/baseline/MANIFEST.json` — `signed_by: "Петър"`, `rev: f06ac06…`. Ч: `gates/run_gates.py:247-255` сверява, че тази ревизия още носи подписаните sha; `:260-264` вика coverage с `base = git:<rev>:data/*.json`, `candidate = работното дърво`; `:199-217` избира allow-файла (>1 → fail).
- Ч: `gates/coverage.py:573-577` — неползван ред от allow е `allow_stale`; `:617-618` → `EXIT_ALLOW_STALE = 3`. Кодовете: `EXIT_OK 0`, `EXIT_UNCOVERED 2`, `EXIT_ALLOW_STALE 3`, `EXIT_USAGE 4`, `EXIT_ALLOW_UNSIGNED 5` (`:89-93`).
- `expectations.json` — `frozen: true`, `signed_by: "Петър"`; `INPUT_RELS` е **твърда тройка** (`recall_sweep.py:2815-2816`).
- Ч: `recall_sweep.py:90-97` — само `--freeze` и `--manifest`, **няма argparse и няма `--dump`**; `:68` `REPO_ROOT`; `:84` `OUTDIR = %TEMP%/fv_measures/`; `:79-80` `REPO_PARITY_OUT`; **`:4249-4253` всеки НЕ-freeze прогон презаписва този ПРОСЛЕДЕН файл**; `:4330` `return 1 if red else 0`.
- Ч 🔴: `gates/release.py:1367-1371` — `want = row.get("body"); entry = artefacts.get(row["artefact"]) if row["artefact"] else None; if not want or entry is None: continue`. **Ред без артефакт (клас „въпрос“) НИКОГА не се сверява по тяло.** Затова G9 сверява дайджеста САМ (§5).
- Ч: `gates/release.py:177-184` `SIGNABLE` е затворена шесторка — файлът с решенията НЕ е в нея.
- Куката `scripts/hooks/pre-push`: `[ ! -t 2 ]` блокира агент → `unittest` → `run_gates`; **няма break-glass**.
- Ч: `gates/sign.py:139-147` `refuse_if_agent()` хвърля САМО при изброени агентски идентичности.

### 1.10 Червената линия и размерът

(1.8.12) структурна геометрия: **0**. (1.8.13) първо зареждане: **1 878 210 B ≈ 1,79 MB** при таван 5 MB.

---

## 2 · Обявени отклонения от подписания текст (искат подписа на Петър)

| № | Подписано | Мерено / по-късно решено | Предложено решение |
|---|---|---|---|
| **О1** | план `:144` „картончето: редът за атрибуция…“ + думите **„attribution row“**; Б0 т. 5; G27(б) | решение 8 (`СЪСТОЯНИЕ:31`): в картончето НЯМА ред за извора | „attribution row“ **излиза** от съобщението; `label`/`url` остават машинен запис — **валидират се, не се рендерират**; ново място в write-set: `index.html:1901-1918`. |
| **О2** | „шестте S17 фикстури“ | `tests/test_granitsi_fixtures.py:48-49` живи 5; `:180-186` замразява две отложени | Б3 дължи двете + преизмерените пет + **четири нови** (§3.5). |
| **О3** | „`QUARTER_CODES` +41“ | доставката ползва 28 кода; липсват 8 | Съобщението носи **`QUARTER_CODES +8`**. |
| **О4** | „един атомен комит“ + автор `Claude Executor`, а ам.3 §7 прави комит на данни от агент STOP | взаимно противоречие | **Един атомен комит с автор `Petar1984`**; изпълнителят стейджва, Петър комитва. |
| **О5** | типово правило за `DISTRICT_FALLBACK` + положителна фикстура | 0 такива реда | `QUARTER_SRC` получава двете стойности; правилото се пише в **двата двигателя И в двата bundle теста** (О19); доказателството е синтетична фикстура. |
| **О6** | нищо; чака се решение 10 | речникът носи `с.о. Перчемлията` — районен произход неподписан | Решение 10 или поименен резерв **преди** комита, или изричен ред „отложено“ в опашката. |
| **О7** | районът от НАШИЯ `district.code` | решение 9 смени района на два квартал-полигона | В реда „вид · квартал · район“ районът остава **на МЯСТОТО** (375/375). |
| **О8** | D13 пинва поименно старите числа | всички се менят | D13 остава договор за местата; всяко число се премерва (§4 стъпка 2). |
| **О9** | ADR 010 D3 „затворен 9-ключов `_meta`“ | ам.2 прави набора 10-ключов; хотелският е 8-ключов | Документен дълг → **отделен комит СЛЕД пуша**. |
| **О10** | „подготовка на Б3 (кръпка)“ | кръпката не съществува | Б3 тръгва на чисто по този план. |
| **О11** | Б1 sha `3f423898…`/61 граници | комитът на Петър `6486b48`, 60 граници, sha `74f09d505e2b` | Всяка котва сочи `6486b48` / `74f09d505e2b`. |
| **О12** | — | наследени дългове P1–P3, `lot1v_v_manifest.py:30` | Приемат се поименно; не се поправят тук. |
| **О13** | гейт „grep `coordinates` → 0 извън `data/hydrants.json`“ | (1.8.12) буквалното попадение е **проза в стойност** на `data/hydrants_provenance.json`; ключове след parse: **0** | Гейтът става **структурен** (G11). |
| **О14** | „замразената референция (нула изгубени резултати)“ | „младост“ 28→20, „училище младост“ 11→6 | „Нула изгубени“ = **нула НЕПОДПИСАНИ изгубени**; **осемте** реда (1.7) минават през опашката поименно. |
| **О15** 🔴 | §Б3 `:144` „нормализаторът D12“; ADR 010 D12 иска `window.__places.locationKey(q)` | (1.8.4) четирите типови заявки не намират квартала си; (1.8.21) има **по-евтин** вариант, който v2 пропуска | **D12 се ОТЛАГА** — ИЛИ влиза евтиният вариант (§9 т. 1, ново решение А/Б/В). Ефектът се записва или се поправя, но не се премълчава. |
| **О16** 🔴 | „дете → детето; родител → родителят и подписаните му деца“; DAG-ът от Б0 т. 9 | (1.8.6) детската заявка → 0; (1.8.8) `druzhba ⊂ asparuhovo` не работи; (1.8.7) `chaika` слива братя | **Родство остава фразово и се ЗАМРАЗЯВА такова, каквото е** (фикстури 6–8). DAG-ът не влиза (иска нов износ на varna_3d). |
| **О17** | G27(б): три фикстури — липсващ запис ✗, `url` без формат ✗, `_meta` с 11 или 9 ключа ✗ | (1.5, 1.5б) картите са ДВЕ (14 и 7), url-ите са по ДВЕ правила (wikimapia `http://…/<id>/…`, openstreetmap `https://www.openstreetmap.org/copyright`), `_meta` е **множество**, не брой | И10 изпълнява и трите, но по **измереното**: точни множества ключове, затворено множество `src ∈ {wikimapia, openstreetmap}` с ПРАВИЛО ЗА ВСЕКИ, и **равенство** карта ↔ `SIGNED_POLYGON` кодове. Отклонението от буквата на ам.2 (`https`, без път) се обявява. |
| **О18** | §3.3 на v1: „`LOCALITY_CODES` не се пипат“ | (Ч) `index.html:6537` и `tests/test_places_public_bundle.py:292` валидират местността със `QUARTER_SRC` | Въвежда се **`LOCALITY_SRC = ['REG','KAIS','SIGNED_OVERRIDE','SIGNED_POLYGON']`** (без `DISTRICT_FALLBACK`). |
| **О19** 🔴 | „типово правило за `DISTRICT_FALLBACK`“ (само `index.html:7064`) | същият ред е ОБРАТЕН в три Python-проверки (`recall_sweep.py:2146`, `:2150`, `tests/test_places_public_bundle.py:291-293`, `:309`) | Правилото се пише **на шест места** (§3.4, §3.5). |
| **О20** | `index.html:6544` изисква `zone === quarter.name`; износителят следва това (375/375, преизмерено: 0 нарушения) | И6 на v1 сменя договора едностранно, преди износителя | Б3 приема **и двата** етикета за fallback ред. Затягането влиза с износния гейт във varna_3d. |
| **О21** 🔴 | v1 нарича низа в подножието „подписан“ | (Ч) файлът с решенията не е в `SIGNABLE`; последният комит по него е на агент; **(1.9) `release.py:1367-1371` пропуска ред без артефакт** | Низът влиза в опашката като ред **клас „въпрос“** с полета `тяло: c6d29d4e…` (sha256 на НИЗА) и `тяло-на-файла: c57a79c2…` (`body_digest`). **release НЯМА да ги сверява — това го прави G9** (§5). Комитът не нарича низа „подписан“ преди подписа. |
| **О22** 🔴 | v1 §9 т. 9: baseline → комита на доставката в същата сесия | (1.8.2) `base == candidate` → exit 3, 150 stale | **Baseline ОСТАВА `f06ac06`.** Новият allow бюджетира **138-те** (1.8.1). **Семантиката се записва:** „lost“ = ред, който е имал назован `zone`/квартал в базата и няма в кандидата; „changed“ = имал е друг. 138 = 44+44 (places) + 39+11 (hotels). Обяснението е 1.8.1б: срещу `f00dd78` са **150**, значи доставката **връща 12**. |
| **О23** | v1 §4 стъпка 9: „`probe_out/token_parity.json` в `_meta.inputs`“ | (Ч) `INPUT_RELS` е твърда тройка | Очакването отпада; амандамент №11 т. 4 е поименен дълг. |
| **О24** 🔴 | червен ред 5 „автор `Claude Executor`“ | (Ч) `gates/sign.py:139-147`; git identity на репото е `Petar1984` | Всяка агентска команда носи `git -c user.name="Claude Executor" -c user.email=executor@local`. Проверката е **следпроверка**. |
| **О25** 🔴 | §Б3 не казва нищо за старата опашка | (1.8.19) 🔴 **подпапка `архив/` НЕ спасява** — `queues_in_head()` е рекурсивен и сравнява basename | `ЗА_ПОДПИС_A.md` се **преименува**: `git mv scratch/places_search/ЗА_ПОДПИС_A.md scratch/places_search/архив/АРХИВ_A_05.09.md` — **името губи префикса `ЗА_ПОДПИС_`**. Изтриване не е нужно: 27-те подписани реда остават четими. |
| **О26** | v1 G7: „`recall_sweep.py --dump`“ | (Ч) няма такъв флаг; артефактът е `%TEMP%/fv_measures/lot1v_v_candidate_rows.json`, **общ за всички прогони** | G7 се преписва (§5): два ОТДЕЛНО ЗАПАЗЕНИ дъмпа + изпълним байтов comparator + доказано падаща мутация. |
| **О27** 🔴 **нов (S28-10)** | нощен план `:144` забранява мълчалив отказ (`places2 = []` без видим знак) | (1.8.10 случай в) при чужд код в places клиентът показва **225 реда вместо 375** и **не казва нищо** | **Обявено отклонение: мълчаливият UI отказ ОСТАВА в Б3.** Той е поведението и днес; фикстура 9в го **пинва**, а видимият отказ е отделен малък лот. Петър подписва това изрично (§9 т. 10). |
| **О28** 🔴 **нов (собствена находка)** | `expectations.json.p7` е подписан с `tokens: 2, zones: 2` и `foreign_guard`, който изрично отхвърля `vladislav`/`varnenchik` | (1.8.17) новият речник добавя **7 токена в 5 зони**; засегнати записи 48 → 64 | Дрейфът е **следствие от доставката**, не от кода. Влиза като **отделен ред „въпрос“** в опашката с двата списъка дословно и с `foreign_guard` преди/след. STOP, ако одиторът го намери непокрит. |
| **О29** 🔴 **нов (S28-5)** | v2 §4 стъпка 10: „`--manifest` → 0“ | (1.8.15) първият прогон срещу подписаното е **1** | Стъпката очаква **exit 1** с ПРЕДВАРИТЕЛНО ИЗБРОЕН набор (§4 стъпка 10). Друго число = STOP. |
| **О30** 🔴 **нов (собствена находка)** | — | (1.8.16) вторият `--manifest` е **exit 0 / 0 червени**, защото първият е презаписал `expectations.json` | Червен ред 13. Доказателството се снема САМО от първия прогон; повторение е забранено като „доказателство“ (STOP 20). |

---

## 3 · Write-set (точните файлове и точните редакции)

### 3.1 Данни (байтов препис, нула трансформация)

```bash
cd /c/git/Fire_Varna
git -C /c/git/varna_3d show 05f485e:data/fire_varna_places.json > data/places.json
git -C /c/git/varna_3d show 05f485e:data/fire_varna_hotels.json > data/hotels.json
git -C /c/git/varna_3d show 05f485e:data/place_categories.json > data/place_categories.json
```
Git Bash — **не** PowerShell `>` (UTF-16 в PS 5.1).

### 3.2 `index.html` — четиринадесет редакции, всяка с котва

**И1 · `:6265`** `'fire-varna-hotels-v6-225'` → `'fire-varna-hotels-v7-225'` + два реда коментар защо (57 реда сменят `quarter`/`zone` при непроменен брой; `accept` сверява sha само при `crypto.subtle`, `:6605`).

**И2 · `:6288-6290`** трите пина → стойностите от §1.4.

**И3 · `:6301-6304`** `LEGACY_BUNDLE_SHA` → places `3dffc264…`, hotels `eb8fa85c…`.

**И4 · `:6313`** `const QUARTER_SRC = ['REG', 'KAIS', 'SIGNED_OVERRIDE', 'SIGNED_POLYGON', 'DISTRICT_FALLBACK'];`

**И4б · нов ред веднага след `:6313` (О18).**
```js
    // Б3 (О18): the LOCALITY has its own closed source list. It shared QUARTER_SRC
    // (:6537) and widening that list for the quarter silently widened the locality
    // too — a `locality` by DISTRICT_FALLBACK is a thing no decision allows.
    const LOCALITY_SRC = ['REG', 'KAIS', 'SIGNED_OVERRIDE', 'SIGNED_POLYGON'];
```
и `:6537` става `if (!validLocation(r.locality, LOCALITY_SRC, LOCALITY_CODES)) return false;`

**И5 · `:6317-6321` `QUARTER_CODES` → 28 кода** (масив, същият стил на пренасяне; азбучно):
```
'asparuhovo', 'borovets_yug', 'briz', 'chaika_kk', 'chaika_kv', 'dolna_traka',
'druzhba', 'galata', 'grackata_mahala', 'hristo_botev', 'izgrev_kv',
'kk_konstantin_elena', 'kolhozen_pazar', 'kv_levski', 'mladost', 'mladost2',
'pobeda', 'priboy', 'sv_ivan_rilski', 'trakia', 'troshevo', 'tsentar',
'vazrazhdane', 'vazrazhdane1', 'vazrazhdane2', 'vinitsa', 'vladislavovo',
'zlatni_pyasatsi'
```
`DISTRICT_CODES` (`:6315`) и `LOCALITY_CODES` (`:6322`) не се пипат.

**И6 · `:6535-6544` типовото правило за `DISTRICT_FALLBACK` (по О20 — двата етикета).**
```js
    // Б3 (Б0 т. 4, О19/О20): a DISTRICT_FALLBACK quarter is the DISTRICT wearing the
    // quarter field. Its code comes from DISTRICT_CODES, it must BE the row's own
    // district, and nothing downstream may read it as a quarter — otherwise the row
    // silently leaves the district branches (:7064, :7118) that are its only home.
    function isFallbackQuarter(q) { return !!q && q.src === 'DISTRICT_FALLBACK'; }
    function ownQuarter(r) { return (r.quarter && !isFallbackQuarter(r.quarter)) ? r.quarter : null; }
```
Първи клон на `validTypedLocation`:
```js
      if (isFallbackQuarter(r.quarter)) {
        if (!validLocation(r.quarter, QUARTER_SRC, DISTRICT_CODES)) return false;
        if (!r.district || r.quarter.code !== r.district.code
            || r.quarter.name !== r.district.name) return false;
      } else if (!validLocation(r.quarter, QUARTER_SRC, QUARTER_CODES)) return false;
```
Етикетът на `:6544` — **и двете форми** (О20; измерено: износителят пише `zone === quarter.name` за 375/375):
```js
      if (isFallbackQuarter(r.quarter))
        return r.zone === r.quarter.name || r.zone === DISTRICT_MARK + ' ' + r.district.name;
      return r.zone === (r.quarter ? r.quarter.name : DISTRICT_MARK + ' ' + r.district.name);
```

**И7 · `:6749`, `:6782`** — `locTokens('quarter', h.quarter)` → `locTokens('quarter', ownQuarter(h))`; същото за `locPhrases`.

**И8 · `:7064`, `:7118`** — `!r.e.quarter` → `!ownQuarter(r.e)`.

**И9 · `:7266`** — `const quarter = ownQuarter(h) ? h.quarter.name : '';` Нищо не се добавя към картончето.

**И10 · `_meta` — трите проверки на G27(б) по ИЗМЕРЕНОТО (О17), никакъв рендер.** До `validateCats` (`:6581`):
```js
    // Б3 (решение 8 + G27(б), О17): the provenance map is a MACHINE record.
    // Measured 07.09: the map is PER BUNDLE (places 14 codes, hotels 7) and it is
    // EXACTLY the set of SIGNED_POLYGON codes that bundle carries — equality, not
    // inclusion. `src` is a CLOSED set and each source has its own URL rule: the
    // single openstreetmap entry (`mladost2`) points at the ODbL copyright page and
    // matches no wikimapia pattern. Not one byte of any of it reaches a card.
    const ATTRIB_KEYS = ['src', 'url', 'label', 'licence'];
    const ATTRIB_SRC = ['wikimapia', 'openstreetmap'];
    // О17: pinned to the DELIVERED shape (http, id, path), not to the literal of
    // амандамент №2 („https://wikimapia.org/<wm_id>/“) — measured 07.09.
    const WM_URL = /^https?:\/\/wikimapia\.org\/\d+\//;
    const OSM_URL = 'https://www.openstreetmap.org/copyright';
    function validQuarterAttribution(meta, rows) {
      const map = meta && meta.quarter_attribution;
      if (!map || typeof map !== 'object' || Array.isArray(map)) return false;
      for (const k of Object.keys(map)) {
        const e = map[k];
        if (!e || typeof e !== 'object' || Array.isArray(e)) return false;
        if (Object.keys(e).length !== ATTRIB_KEYS.length) return false;
        for (const f of ATTRIB_KEYS) if (typeof e[f] !== 'string' || !e[f]) return false;
        if (ATTRIB_SRC.indexOf(e.src) < 0) return false;
        if (e.src === 'wikimapia' && !WM_URL.test(e.url)) return false;
        if (e.src === 'openstreetmap' && e.url !== OSM_URL) return false;
      }
      // equality with the SIGNED_POLYGON codes of THIS bundle (both directions)
      const need = new Set();
      for (const r of rows)
        if (r.quarter && r.quarter.src === 'SIGNED_POLYGON') need.add(r.quarter.code);
      if (Object.keys(map).length !== need.size) return false;
      for (const c of need) if (!map[c]) return false;
      return true;
    }
    const META_KEYS_HOTELS = ['generated', 'count', 'by_zone_src', 'licence',
      'licence_osm', 'licence_address', 'quarter_attribution', 'excluded'];
    const META_KEYS_PLACES = ['generated', 'count', 'by_kind', 'by_zone_src',
      'licence_osm', 'licence_registry', 'licence_wikidata', 'licence_address',
      'quarter_attribution', 'excluded'];
    function metaKeysAre(meta, want) {
      const got = Object.keys(meta || {});
      if (got.length !== want.length) return false;
      for (const k of want) if (got.indexOf(k) < 0) return false;
      return true;
    }
```
Извиква се веднъж в `validateHotels` (след `:6549`) и веднъж в `validatePlaces2` (след `:6568`):
```js
      if (!metaKeysAre(d._meta, META_KEYS_HOTELS)) return false;   // …_PLACES в другия
      if (!validQuarterAttribution(d._meta, d.hotels)) return false;   // …d.places
```
`EXPECT_KEYS`/`EXPECT2_KEYS`, `EXPECT_COUNT`/`EXPECT2_COUNT` не се пипат.

**И11 · Низът в подножието — влиза САМО след подписа на Петър (О21).** След `:1902`:
```js
  // Б3 (решение 8, 07.09): the line for the quarter boundaries lives in the map's
  // small print, next to the basemap credit, and never on a place card. It is added
  // ONCE and survives every basemap swap: `attribCtrl` is a single control (:1917)
  // and setBasemap/enable/removeOfflineBasemap (:4543-4549, :4663, :4672-4676)
  // remove only their OWN strings — including under the sticky override (:4571-4574).
  const QUARTERS_ATTRIB = 'Кварталните граници в този файл са по Wikimapia.org (© Wikimapia contributors, CC BY-SA, ToS §1.G: връзка към обекта + „Wikimapia.org“ с връзка към http://wikimapia.org); карето „Младост 2“ е по улични оси от OpenStreetMap (© OpenStreetMap contributors, ODbL, https://www.openstreetmap.org/copyright). Геометрия НИКОГА не пътува публично.';
```
и веднага след `:1918`: `attribCtrl.addAttribution(QUARTERS_ATTRIB);`
Низът е **дословно** полето `attribution` на `scratch/places_search/granitsi_decisions_4_2026-09-07_proba.json` — **338 знака**, нула апострофи, нула HTML (измерено).

**И12 · Три маркера за Node-срезовете** (нула преместен код):
- преди `:6256` → `// ---- Б3 slice start (pure, DOM-free: constants → validators → cache → index → search) ----`
- след `:7154` → `// ---- Б3 core slice end (everything below touches the DOM) ----`
- след `:7289` (края на `popupFor`) → `// ---- Б3 card slice end (popupFor included, DOM-stubbable) ----`
Границата `:7154` е измерена: първото докосване на DOM е `function span(` на `:7155`. **Третият маркер е нов и е задължителен за G8** — `popupFor` живее ИЗВЪН основния срез.

**И13 · Паритетната повърхност `:7470-7480`** — расте САМО ако Петър избере вариант Б/В по §9 т. 1.

### 3.3 Пиновете и правилата в двата bundle теста

`tests/test_places_public_bundle.py`: `:55-57` sha/bytes/gzip9 → `3dffc264…` / **128401** / **14748**; `:80` копието на `QUARTER_CODES` (28) + `QUARTER_SRC` (5) + **нов `LOCALITY_SRC` (4)**; `:88` `QUARTER_BY_SRC = {"REG": 39, "KAIS": 10, "SIGNED_POLYGON": 42}`; `:89` `LOCALITY_COUNT = 5`.
`tests/test_hotels_public_bundle.py`: `:61-63` `eb8fa85c…`/**151263**/**13642**; `:83-85` `9064705a…`/**77998**/**9690**; `:88` `LOCATION_COUNTS = {"quarter": 28, "district": 5, "locality": 5}`; `:115` копието на кодовете; `:124` `QUARTER_BY_SRC = {"REG": 115, "KAIS": 25, "SIGNED_OVERRIDE": 12, "SIGNED_POLYGON": 15}`.

**Четири нови правила в двата теста (О17, О18, О19):**
1. `:291-293` (и близнакът): `("locality", LOCALITY_SRC, LOCALITY_CODES)`; за `quarter` — ако `src == "DISTRICT_FALLBACK"`, кодът се търси в `DISTRICT_CODES` и трябва да е кодът на СОБСТВЕНИЯ район.
2. `:309` (и близнакът): `want` за fallback ред е `quarter["name"]` **или** `"район " + district["name"]`.
3. `set(QUARTER_CODES) == set(place_categories["locations"]["quarter"])` — измерено днес: **True**, 28 = 28.
4. 🔴 **`_meta` и `quarter_attribution` в Python, огледало на И10:** точното множество ключове (10 / 8 дословно), `set(quarter_attribution) == {code : row.quarter.src == "SIGNED_POLYGON"}` (14 / 7), `src ∈ {wikimapia, openstreetmap}`, `^https?://wikimapia\.org/\d+/` за wikimapia и точното `https://www.openstreetmap.org/copyright` за openstreetmap.

Лицензните низове се сравняват дословно (`:263`, `:384`, `:397-412`).

### 3.4 Двигателят на референцията — шест места, не две (О19)

`scratch/places_search/recall_sweep.py`, един помощник `own_quarter(rec)` и шестте му ползвания:
`:621` `self.qtk`, `:623` `self.qph`, `:1123` `d_rows`, `:1167` `district_hit`, `:2146` `want` (двете форми), `:2150` кодът на fallback ред срещу `DISTRICT_CODES`.

**Приемане:** кандидатският дъмп преди/след промяната е **байт-еднакъв** над днешната доставка (G7).
**Забранено:** `INPUT_RELS` (`:2815-2816`) НЕ се пипа (О23). `CENTER` (`:102-104`) НЕ се пипа.
**Обявено:** всеки НЕ-freeze прогон презаписва проследения `scratch/places_search/probe_out/token_parity.json` (`:4249-4253`) — очакван изход, не мръсно дърво (О26). **Измерено на живо:** трите прогона на G7 наистина го пренаписаха.

### 3.5 Фикстурите — единадесет, всяка с ДОКАЗАНО падащ `_broken`

`tests/test_granitsi_fixtures.py` (пренаписан) + `scratch/places_search/granitsi_fixtures_07.09.json` (`git mv` от `…_06.09.json`) + **нов** `tests/granitsi_client_probe.mjs`.

**Промени в харнеса:**
- докстрингът `:8-13` се пренаписва; `_meta.status` → `"signed_by_petar"` + дата; `_meta.inputs` (трите нови sha) и `_meta.engine.sha256` в същия комит (иначе `:188` пада пръв); `EXPECTED_NAMES` (`:48-49`) става единадесеторка; `test_the_two_deferred_fixtures_are_named_with_their_reason` (`:180-186`) → „нула отложени“.
- 🔴 **`assert_case` става МНОГОЗАЯВЪЧЕН (К28-2 бонус).** Днес (`:197-204`) той сравнява `case["today"]` за `case["query"]` — една заявка. Новата форма:
```python
    def assert_case(self, case, measured_by_query):
        """`case["queries"]` е списък от {q, branch, n, with_quarter, without_quarter,
        identities}; едно-заявъчните фикстури носят списък от един елемент."""
        for want in case["queries"]:
            got = measured_by_query[want["q"]]
            self.assertEqual(got["branch"], want["branch"], want["q"])
            self.assertEqual(got["n"], want["n"], want["q"])
            self.assertEqual(got["with_quarter"], want["with_quarter"], want["q"])
            self.assertEqual(got["without_quarter"], want["without_quarter"], want["q"])
            self.assertEqual([list(r) for r in got["identities"]],
                             [list(r) for r in want["identities"]], want["q"])
```
  🔴 **и негативната половина назовава ЗАЩО пада:** `_broken` носи `fails_on: "<заявка>"`; тестът твърди, че `assert_case` хвърля **и** че точно тази заявка е сменена, а останалите заявки на фикстурата са непроменени (иначе мутация, която мърда друга заявка, минава за доказателство).
- ключът за мутациите остава по име (`apply_ops`, `:67-84`, „op, който не улучва точно 1 ред, е грешка“); за хотелите — защита срещу дубликата **`РОЯЛ` (2 реда)**: фикстура, която го докосва, ключова по ординал, иначе `LookupError`. (Измерено: в places няма дублирани имена; в hotels само `РОЯЛ`.)

| # | фикстура | твърди (числата се ПРЕИЗМЕРВАТ) | `_broken` — **измерената** мутация и **измерената** щета |
|---|---|---|---|
| 1 | `street_collision` | `A3-street`, 1, ДГ№12 „Ян Бибиян“ | заварената + нова: улицата получава код `dolna_traka` |
| 2 | `district_retention` | `A3-district`, 12, 9 с квартал | заварената (`DISTRICT_FALLBACK` + `district: null` → 11) |
| 3 | `mladost_kind_collision` | `M7-bare-location`, 20, 8/12 | заварената + `SIGNED_POLYGON` с код извън 28-те |
| 4 | `order_identity` | същите 20, **подредени** | заварената |
| 5 | `parent_child` (Младост) | `A3-location`, 6, 3 с квартал | заварената (код `mladost2_new`) — **измерено: пада (5)** |
| 6 | `parent_child_vazrazhdane` **(нова, О16)** | (а) „възраждане“ → **5** (`ДГ 29 „Звънче“`, `ДГ 31 „Крилатко“`, `II ОУ „Никола Йонков Вапцаров“`, `ДГ 33 „Делфинче“`, `ДГ№34 „Лястовичка“`); (б) „възраждане 2“ → **0**; (в) „ж.к. възраждане 2“ → **0** | 🔴 **v2 беше сгрешен.** Измерено: смяна САМО на КОДА (`vazrazhdane1` → `vazrazhdane1_new`) дава **пак 5** — фикстурата НЕ пада, защото М7 чете `qtk`, строен от ИМЕТО (`recall_sweep.py:672`, `:1159`), а кодът важи само за `qph`/A3 (`:686`, `:1121`). **Правилната мутация — един op по име:** `{bundle: "places", name: "ДГ№34 „Лястовичка“", set: {quarter: {name: "ж.к. Лястовиче", code: "vazrazhdane1", src: "SIGNED_POLYGON"}, zone: "ж.к. Лястовиче"}}` → **„възраждане“ 5 → 4** (пада `assertEqual(n, 5)`); заявки (б) и (в) остават 0 → `fails_on: "възраждане"` |
| 7 | `chaika_sibling_merge` **(нова, О16)** | „чайка“ → **30** реда през `chaika_kk` (19) И `chaika_kv` (10) + 1 без квартал | 🔴 **изброена поименно** (`apply_ops` мутира ред по ред): десет op-а, по един на всеки `chaika_kv` ред — `АМИРАЛ` (hotels), `Математическа гимназия „Доктор Петър Берон“`, `ОУ "Г.С.Раковски"`, `ОУ "Захари Стоянов"`, `Спортно училище Георги Бенковски`, `4 ОДЗ Чайка`, `ДГ№21 „Калина Малина“`, `ДГ№22 „Мечо Пух“`, `Оздравителна Детска Градина №9 "Ален Мак"`, `ДЯ №2 „Моряче“` — всеки получава `quarter.name` и `zone` = `"кв. Крайморие"`. **Измерено: 30 → 21** (не 20 и не 21-само-при-zone: и двата варианта дават 21). Оцеляват 19-те `chaika_kk` + `4 ОДЗ Чайка` (носи думата в ИМЕТО си) + `„ДКЦ Чайка“ ЕООД` |
| 8 | `asparuhovo_no_child_rows` **(нова, О16)** | „аспарухово“ → **16** (9 `asparuhovo` + 7 „район Аспарухово“), **без** `ЦДГ 43 „Пинокио“`; „дружба“ → **2** | 🔴 **v2 беше сгрешен.** Alias „Аспарухово“ към `druzhba` е **невъзможен като op** (`apply_ops` мутира РЕДОВЕ, не речника) И е **безсилен**: измерено `location_alias_tokens` го изхвърля — `dropped["druzhba"] = ["short:zh", "short:k", "own:druzhba", "foreign:asparuhovo:asparuhovo"]` (`recall_sweep.py:477` (д)/(д′)). **Работещата мутация (един op):** `{bundle: "places", name: 'ЦДГ 43 "Пинокио"', set: {quarter: {name: "кв. Аспарухово", code: "druzhba", src: "SIGNED_POLYGON"}, zone: "кв. Аспарухово"}}` → **„аспарухово“ 16 → 17** (детето влиза при родителя) И **„дружба“ 2 → 1**. `fails_on: "аспарухово"`, а вторият ред се пази като второ твърдение |
| 9 | `client_cache_matrix` **(нова, Node)** | §3.6 — **три случая + матрица crypto/sha** | §3.6 |
| 10 | `district_fallback_typed` **(нова, синтетична, положителна, О19)** | синтетичен ред с `quarter {name: <район>, code: <district code>, src: DISTRICT_FALLBACK}`: (а) минава `validTypedLocation`; (б) **остава в „училище младост“** (`:7118`) и в „младост“ (`:7064`); (в) не носи квартални думи; (г) картончето не печата квартал; (д) минава `qa` (`:2146`, `:2150`); (е) минава двата bundle теста | **разделен `_broken`:** махни `ownQuarter` от `:7118` → „училище младост“ 6 → 5; отделно `district: null` за изричното „училище район младост“ (`:7104-7109`) |
| 11 | `client_reference_parity` **(нова, Node)** | шест заявки през среза и през `recall_sweep.search()` → **едни и същи имена в един и същи ред** | махни `map` стъба → подредбата се срутва |

### 3.6 Node-фикстурите — как се викат и какво доказват (**изпълнено от планера, не обещано**)

Механиката: Python чете `index.html`, изрязва **по маркерите И12** (липсващ маркер = `AssertionError`, по образеца `tests/test_places_search_gate.py:503-530`); `shutil.which("node")`, липсващ Node = `self.fail("node липсва — фикстурата е гейт")`; `subprocess.run([node, "tests/granitsi_client_probe.mjs"], input=json.dumps({...}))` — срезът, товарите и случаите по stdin, отговорът по stdout; нула временни файлове, нула кирилица в командния ред.

`granitsi_client_probe.mjs` вдига среза с
```js
new Function('window','caches','document','map','fetch','attribCtrl','__calls','L','console','crypto', slice + ';return {…}')
```
🔴 **Десетте параметъра са задължителни и всеки е измерен:**
- **`map`** — `centre()` (`:6952-6955`) вика `map.getCenter()` в `try/catch`; без стъб `distOf` връща 0 и подредбата се срутва. Стъбът е `{ getCenter: () => ({ lat: 43.2141, lng: 27.9147 }) }` (центърът, който референцията пинва, `recall_sweep.py:102-104`).
- **`fetch`** — `fetchValidatedJson` (`:6613-6625`) вика глобалния `fetch`; Node 22 има свой и без стъб тестът излиза в мрежата.
- 🔴 **`crypto` — НОВО и КРИТИЧНО.** `accept` (`:6605`) чете `window.crypto && crypto.subtle` — **две различни имена**. В браузъра са едно и също; **в Node голото `crypto` е WebCrypto на средата**, така че `window.crypto = {}` НЕ изключва проверката. Измерено: без сянка върху `crypto` половината „crypto OFF“ мълчаливо мери ON-пътя. Стъбът е `undefined` (OFF) или `{ subtle: webcrypto.subtle }` (ON), подаден **и на двете места**.
- **`__calls`** — броячът; всеки валидатор се обвива, за да се брои дали изобщо е викан.
- **`document`** — DOM стъб (`createElement` → възел с `appendChild`/`textContent`/`className`/`classList`/`style`), нужен само за среза с картончето.

**Фикстура 9 — трите случая, всеки ИЗМЕРЕН от планера:**

| случай | срез | товар | измерено |
|---|---|---|---|
| **9а** — стар списък, двата пакета отказани | `index.html` **както е днес** (`QUARTER_SRC` тройка, `QUARTER_CODES` 20, старите пинове) | новите три блоба | `validateHotels === false` · `validatePlaces2 === false` · `accept(text, пинат sha, validatePlaces2) === null` · **`await ensurePlaces()` ХВЪРЛЯ `Error: places payload unavailable`** (`:6639`). **Няма `RECS`.** Твърдението е `assertRaises`, не число |
| **9б** — след И2/И4/И5, валидната доставка | срез с петте извора, 28-те кода, новите пинове | новите три блоба | `validateHotels === true` · `validatePlaces2 === true` · `accept → обект` · **`RECS.length === 375`** |
| **9в** — чужд код САМО в places | същият срез, но `PLACES2_SHA256` = sha на **мутираното** тяло | places с един `SIGNED_POLYGON` код сменен на `tsentar_new` (тялото е `128402` B, sha `6bfbcbf2adced671fb60126fb97ef597c7f214c18466d3c0507bbd53d3ded745`) | `validateHotels === true` · `validatePlaces2 === false` · `ensurePlaces()` **НЕ хвърля** · **`RECS.length === 225`** · **`__calls.validatePlaces2 === 1`** |

🔴 **Защо пинът трябва да е sha на МУТИРАНОТО тяло** (S28-1): измерената матрица на `accept` (1.8.11):

| `window.crypto`/`crypto` | sha | резултат | извиквания на валидатора |
|---|---|---|---|
| ON | несъвпадащ | `null` | **0** |
| ON | верен | `null` (валидаторът отказва) | **1** |
| OFF | несъвпадащ | `null` (валидаторът отказва) | 1 |
| OFF | верен | `null` (валидаторът отказва) | 1 |

**Значи при crypto ON и несъвпадащ sha `null` е отказ на ДАЙДЖЕСТА, не на списъка** — фикстура, която подава грешен sha, доказва нещо друго. Фикстура 9в подава **правилния** sha на мутираното тяло и твърди `__calls.validatePlaces2 === 1` заедно с `false`.

**9г (кешът):** `PLACES_CACHE === 'fire-varna-hotels-v7-225'` и `!== 'fire-varna-hotels-v6-225'`; кеш-стъб с v6 тяло **не** се намира.
**9д (О27):** наблюдаемият отказ се **назовава**: при чужд код `places2 = []`, `hotels.length > 0`, UI показва **225 вместо 375** и **мълчи**. Това е обявено отклонение, не пропуск.
Отрицателната половина се прави **върху текста на среза** (замяна на реда `:6313`), не върху константа в паметта.

---

### 3.7 Родство — какво работи, какво не, поименно (О16)

| двойка | състояние (измерено) |
|---|---|
| `mladost2 ⊂ mladost`, `vazrazhdane1 ⊂ vazrazhdane`, `vazrazhdane2 ⊂ vazrazhdane` | **работи по фраза** (случайно съвпадение, не договор) |
| `druzhba ⊂ asparuhovo` | **НЕ работи** — „аспарухово“ 16 без `ЦДГ 43 „Пинокио“`, „дружба“ 2 |
| `kaisieva ⊂ vladislavovo`, `vazrazhdane3 ⊂ vazrazhdane` | детето няма доставени редове |
| `abatko`, `rozova_dolina`, `kokardzha_generic`, `mladost1`, `saltanat` | нула редове |
| **`chaika_kk` + `chaika_kv`** | **сливат се без подписано родство** (30 реда) |

Речникът носи `parents: []` за **0 от 28** кода. **Б3 НЕ изнася DAG-а**; замразява измереното с фикстури 5–8. Ако Петър иска само подписани връзки — лот **Б5 „Родство“**.

### 3.8 Опашката и allow-ът (О22, О25)

- 🔴 **`git mv scratch/places_search/ЗА_ПОДПИС_A.md scratch/places_search/архив/АРХИВ_A_05.09.md`** — **в същия комит** с новата `scratch/places_search/ЗА_ПОДПИС_Б3_07.09.md`. Измерено (1.8.19): само подпапка НЕ бяга от глоба; името трябва да загуби префикса `ЗА_ПОДПИС_`.
- Новият `gates/allow/2026-09-07_b3.json` бюджетира **измерените 138** реда, **изведен машинно** от `gates/out/coverage.json`; старият `2026-09-05_lot1v_v.json` се маха в същия комит (в папката остава точно един json).
- `gates/baseline/MANIFEST.json` **не се пипа**.

---

## 4 · Ред на изпълнение

**[И]** изпълнител (`git -c user.name="Claude Executor" -c user.email=executor@local …`, О24) · **[П]** Петър. Между стъпка 7 и стъпка 13 пакетът е **ЧЕРВЕН по конструкция** (D19): изпълнителят записва ТОЧНИЯ набор падащи тестове (G20); всеки тест извън набора = STOP.

**0. [И] Санитарна проверка.** `git status --porcelain` (0 реда без `??`), `git log -1 --pretty='%h %an %s'`, `git rev-list --count origin/main..HEAD`, `git ls-tree -r --name-only HEAD -- scratch/places_search | grep -c "ЗА_ПОДПИС"` → **1**, `node --version` → v22.19.0. **HEAD и ahead се ЗАПИСВАТ такива, каквито са** — §1.1 е снимка от 07.09 и котвата се движи.

**1. [И] Записва плана.** `docs/plans/ПЛАН_Б3_07.09.md` → комит `docs(plan): Б3 quarter search delivery plan v3 (signed 07.09)`.

**2. [И] Препис на трите блоба + ПЪЛНАТА мярка ПРЕДИ редакцията.**
```bash
export PYTHONIOENCODING=utf-8
python - <<'PY'
import hashlib, gzip, pathlib
for p in ['data/places.json','data/hotels.json','data/place_categories.json']:
    b = pathlib.Path(p).read_bytes().replace(b'\r\n', b'\n')
    print(p, len(b), hashlib.sha256(b).hexdigest(), len(gzip.compress(b, 9)))
PY
```
Очаквано: точно шестте числа и трите sha от §1.4. В същия прогон се мерят `QUARTER_BY_SRC`, `LOCATION_COUNTS`, `LOCALITY_COUNT`, множеството квартални кодове, лицензните низове, **двете `_meta` МНОЖЕСТВА (§1.5б)**, **двете `quarter_attribution` карти (14 / 7) и равенството им със `SIGNED_POLYGON` кодовете**, и `P7_ADDED` (1.8.17).

**Трите мерки на покритието, преди един ред промяна** (`sys.exit(result["exit_code"])` — дословно):
```bash
python -c "from gates import coverage; import json, sys; r=coverage.run(places_base='git:f06ac06:data/places.json', places_candidate='data/places.json', hotels_base='git:f06ac06:data/hotels.json', hotels_candidate='data/hotels.json', allow_path=None, out_dir='gates/out'); print(json.dumps({'exit':r['exit_code'],'uncovered':r['uncovered'],'verdict':r['verdict']}, ensure_ascii=False)); sys.exit(r['exit_code'])"
```
очаквано **exit 2 · uncovered 138** (1.8.1).
```bash
python -c "from gates import coverage; import json, sys; r=coverage.run(places_base='git:f06ac06:data/places.json', places_candidate='git:f00dd78:data/places.json', hotels_base='git:f06ac06:data/hotels.json', hotels_candidate='git:f00dd78:data/hotels.json', allow_path=None, out_dir='gates/out'); print(json.dumps({'exit':r['exit_code'],'uncovered':r['uncovered']}, ensure_ascii=False)); sys.exit(r['exit_code'])"
```
очаквано **exit 2 · uncovered 150** — **това е дифът, който обяснява 138-те** (К28-6, О22): доставката ВРЪЩА 12 реда.
```bash
python -c "from gates import coverage; import json, sys; r=coverage.run(places_base='git:f06ac06:data/places.json', places_candidate='data/places.json', hotels_base='git:f06ac06:data/hotels.json', hotels_candidate='data/hotels.json', allow_path='gates/allow/2026-09-05_lot1v_v.json', out_dir='gates/out'); print(json.dumps({'exit':r['exit_code'],'uncovered':r['uncovered'],'stale':len(r['allow_stale'])}, ensure_ascii=False)); sys.exit(r['exit_code'])"
```
очаквано **exit 2 · uncovered 30 · stale 42** (1.8.3; 108 + 30 = 138 ✓).

И деветте заявки 1.8.4–1.8.9 през `recall_sweep`. **Всяко разминаване = STOP.**
Отрицателно доказателство: `python -m unittest tests.test_places_public_bundle tests.test_hotels_public_bundle` **сега е ЧЕРВЕН** — първият ред на G20-набора.

**3. [И] Трите маркера И12 + Node-сондата.** Първо `index.html` получава маркерите, после се създава `tests/granitsi_client_probe.mjs` — **преди** редакциите, за да има с какво да се снеме отрицателното доказателство.
Мярка: срезът с ДНЕШНИТЕ списъци над НОВИТЕ байтове → `validateHotels === false`, `validatePlaces2 === false`, **`ensurePlaces()` хвърля** (случай 9а).

**4. [И] `index.html` — четиринадесетте редакции** (§3.2). **И11 се пропуска, ако низът още не е подписан (О21)** — тогава влиза в стъпка 12б.
Мярка след: `validatePlaces2 === true`, `RECS.length === 375` (9б) и `RECS.length === 225` при мутирания places с верен пин (9в).

**5. [И] Двата bundle теста + четирите нови правила** (§3.3). `python -m unittest tests.test_places_public_bundle tests.test_hotels_public_bundle` → **0**. Отрицателно: една сменена цифра в `PLACES2_SHA256` → ≠ 0.

**6. [И] Двигателят + фикстурите** (§3.4–3.6), включително `git mv` на фикстурния файл. `python -m unittest tests.test_granitsi_fixtures -v` → **0**, единадесет фикстури, всяка с падащ `_broken`.

**7. [И] G7 — двата дъмпа** (§5 G7), с копиране след ВСЕКИ прогон.

**8. [И] Стейджва точния write-set** (`git add` с изрични пътища, никога `-A`), после `git status --short` и `git diff --cached --stat`. **Обявено:** `scratch/places_search/probe_out/token_parity.json` ще е модифициран от прогоните (О26) — той НЕ влиза в този комит, а в комит 5. Показва на Петър `scratch/places_search/COMMIT_MSG_B3.txt`.

**9. [П] Атомният комит** — §6.

**10. [И] Отваря D19-интервала и го записва.** `python -m unittest discover -s tests`; `python -m gates.run_gates` → и двете ≠ 0. Записва **поименно** какво пада, включително очакваното **coverage изход 2** на проверка 4.

**Очакванията и манифестите (report-only) — с ПРЕДВАРИТЕЛНО ИЗБРОЕНИЯ набор (О29):**
```powershell
python scratch\places_search\recall_sweep.py --manifest
```
🔴 **Очакван изход: 1** (не 0). Очакван набор — **59 реда `ЧЕРВЕНО` върху 41 уникални заявки + 2 реда без заявка**, и точно тези гейт-резултати: П7 **16/32**, ЛОТ 1 **6/18**, ЛОТ 1в-А **5/12**, ЛОТ 1в-Б **5/6**, ЛОТ 1в-В **4/10**, М7 **17/17**, §10 sweep 60/62 · 10/10 („срещу подписа 1“). Допуснатите разминавания, поименно:
- **зона-преписване без промяна на броя** (редът е същият, `zone` е новият квартал): `хотел зпз`, `горчива чешма хотел`, `училище свети никола`, `менделеев`, `хотел марина парк`, `училище менделеев стефан`, `училище аспарухово`, `детска градина аспарухово`, `1 ег`, `училище морска градина`, `ГРАДИНА`, `градина`, `детско заведение`, `детски заведения`, `ясла`, `детска ясла`, `детска градина`, `военноморско`, `военноморско училище`, `гимназия`, `поликлиника`, `езикова гимназия`, `морско училище`, `варна`, `училище район младост`, `район младост`, `детска градина владислав варненчик`;
- **сменен брой** (числото е в скобите, старо → ново): `владиславово детска градина` (4→5), `детска градина владиславово` (4→5), `хотел чайка` (18→19), `хотел златни` (96→98), `хотел приморски` (29→22), `детска градина приморски` (12→7), `училище приморски` (14→9), `университет приморски` (5→3), `хотел одесос` (40→32), **`училище младост` (11→6)**, `болница изгрев` (11→1, и клонът от `M2-failopen` на `A3-location`);
- **два реда без заявка:** `p7_added: 7 токена в 5 зони, подписани са 2 в 2` и списъкът на добавените (1.8.17, О28).
🔴 **Прогонът се прави ВЕДНЪЖ.** Втори прогон е зелен по конструкция (1.8.16, червен ред 13) — ако изпълнителят повтори командата, доказателството е унищожено и лотът СПИРА (STOP 20).

```powershell
python -m gates.release
```
Очаквано: **6 (BLOCKED)**; делтите по кофи се записват дословно (мярка в клонинг: **119 делти**, референция 203/3160 · кандидат 226/3222).

**11. [И] Опашката.** `scratch/places_search/ЗА_ПОДПИС_Б3_07.09.md`: три класа редове (въпрос / артефакт / делта), решения `pending`, без code fence и без HTML коментари. **Задължителни редове:**
- всяка делта на `gates.release`;
- шестте `SIGNABLE` + новият allow;
- **ред „въпрос“ за низа на атрибуцията** с `тяло: c6d29d4e1d3c4fa916224ae03ceab3682fd43cc83d3f22b920183d9b82d4002d` (sha256 на низа) и `тяло-на-файла: c57a79c222361dc5c803823883374f9e1dcd12d36e7b27f05788ac17b7c22bfc` (`body_digest`) — **сверява ги G9, не release** (О21);
- 🔴 **ОСЕМ делта-реда за загубените на „младост“** (1.7), всеки с името и новия квартал (К28-3);
- **ред „въпрос“ за `P7_ADDED` дрейфа** (О28) с двата списъка дословно;
- **ред „въпрос“ за мълчаливия UI отказ** (О27);
- ред за Перчемлията (О6);
- редовете по О15/О16/О20 („детската заявка не намира детето — прието“, „чайка се слива без подписано родство — прието“, „fallback етикетът се приема в двете форми“).

В същия комит: `git mv` на старата опашка към `архив/АРХИВ_A_05.09.md` (О25), новият allow (138 реда), махнатият стар allow.
Комит **[И]**: `docs: queue and manifests for the Б3 delivery signatures (07.09)`.
Мярка веднага след комита (семантиката на release, не `grep`):
```powershell
python -c "import subprocess, posixpath, fnmatch, sys; out=subprocess.run(['git','ls-tree','-r','--name-only','-z','HEAD','--','scratch/places_search'],capture_output=True).stdout.decode('utf-8'); q=sorted(n for n in out.split(chr(0)) if n.strip() and fnmatch.fnmatchcase(posixpath.basename(n),'ЗА_ПОДПИС_*.md')); print(len(q), q); sys.exit(0 if len(q)==1 else 1)"
```
→ **1** и точно `['scratch/places_search/ЗА_ПОДПИС_Б3_07.09.md']`.

**12. [П] Подписите.** `python -m gates.sign <id> да --all да --queue scratch/places_search/ЗА_ПОДПИС_Б3_07.09.md` + **неговият** комит, който писалката отпечатва.
**12б. [И]** Ако низът е подписан на този етап — И11 влиза сега, в комит с `index.html` само за този ред: `feat(map): signed quarter-boundary line in the map attribution (decision 8)`. **[П]** го комитва.

**13. [И] Едно-единствено замразяване.** `python scratch\places_search\recall_sweep.py --freeze` → 0.

**14. [П] Комит на замразените тела:** `freeze: reference + expectations + parity (Б3)`.

**15. [И] Пълният гейт** — §5. Всичко зелено → одитор (друг агент, read-only).

**16. [П] Локален преглед** — §8.

**17. [П] Пушът,** от интерактивен терминал: `git rebase --autostash origin/main`, после `git push origin main`.

**18. [И] След пуша:** `docs/activeContext.md` + `docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` §8; проверка, че публикуваните sha са равни на локалните; комит `docs: activeContext — Б3 delivery published, published hashes verified equal to local`; **[П]** втори пуш.

---

## 5 · Гейтове (всеки — команда с изход ≠ 0 и с ДОКАЗАНО падаща отрицателна половина)

| # | гейт | команда | очакван изход | отрицателното доказателство (ТРЯБВА да падне) |
|---|---|---|---|---|
| G1 | пиновете срещу LF-блобовете | `python -m unittest tests.test_places_public_bundle tests.test_hotels_public_bundle` | 0 | една сменена цифра в `PLACES2_SHA256` → ≠ 0 |
| G2 | единадесетте фикстури | `python -m unittest tests.test_granitsi_fixtures -v` | 0 | всеки `_broken` → `AssertionError` на назованата в `fails_on` заявка |
| **G3** 🔴 | **Node-матрицата през `ensurePlaces`** (S28-1) | същият прогон, фикстура 9 | 0 | **три отделни случая, не един:** (9а) срез с оригиналния `QUARTER_SRC` → `validateHotels === false` **И** `validatePlaces2 === false` **И** `assertRaises` върху `ensurePlaces()` с текст `places payload unavailable`; (9б) валидната доставка → `RECS === 375`; (9в) чужд код САМО в places + пин = sha на мутираното тяло (`6bfbcbf2…`, 128402 B) → `validateHotels === true`, `validatePlaces2 === false`, `RECS === 225`, **`__calls.validatePlaces2 === 1`**; (9г) crypto матрицата 1.8.11 — при ON+несъвпадащ sha **`__calls.validatePlaces2 === 0`**, така че `null` без брояч НЕ се приема за отказ; (9д) кеш v6 → стар запис се намира |
| G3б | паритет клиент ↔ референция | същият прогон, фикстура 11 | 0 | махнат `map` стъб → друг ред |
| G4 | пълният набор | `python -m unittest discover -s tests` | 0 | преди подписите: ≠ 0 — записаният G20 набор |
| G5 | седемте проверки | `python -m gates.run_gates` | **0** (7/7 ✓) | жълто („pending — Петър“) също дава 1 |
| G6 | нула **неподписани** изгубени резултати | `python -m gates.release` | 0 (преди подписите: 6) | делта без покриващ ред „да“ → 6 |
| **G7** 🔴 | **двигателят не мести кандидата** (S28-3) | вж. блока под таблицата | 0 | мутиран двигател → comparator **exit 1** (измерено) |
| **G8** 🔴 | **картончето няма ред за атрибуция — ИЗПЪЛНЕНО** (S28-7) | вж. блока под таблицата | 0 | добави `wrap.appendChild(span('pp-attrib', QUARTERS_ATTRIB))` в `popupFor` → ≠ 0 |
| **G9** 🔴 | **низът е ПОДАДЕН на `addAttribution` и е подписан** (S28-7) | вж. блока под таблицата | 0 | срезът без И11 → записаните извиквания са само `['© OpenStreetMap']` → ≠ 0 (измерено); една сменена буква → ≠ 0; неподписан ред → ≠ 0 |
| **G10** 🔴 | **първо зареждане ≤ 5 MB, по ИЗМЕРЕНИЯ набор** (S28-4) | `python -c "import pathlib,sys; f=['index.html','data/hydrants.json']; t=sum(pathlib.Path(x).stat().st_size for x in f); print(t); sys.exit(0 if t <= 5*1024*1024 else 1)"` | **0** (t = **1 878 210**) | същата команда с праг `1*1024*1024` → **exit 1** (1 878 210 > 1 048 576). Двата прага дават РАЗЛИЧНИ присъди |
| **G10б** | **отложената доставка на търсенето, отделно** | `python -c "import pathlib,sys; f=['data/hotels.json','data/places.json','data/place_categories.json']; t=sum(pathlib.Path(x).stat().st_size for x in f); print(t); sys.exit(0 if t <= 1024*1024 else 1)"` | 0 (t = **357 662**) | праг 256 KB → 1 |
| **G11** 🔴 | **нула геометрия — СТРУКТУРНО** (S28-8) | вж. блока под таблицата | 0 | ключ `"\u0063oordinates"` в мутирано копие: буквалният `grep` дава **0**, структурната проверка дава **1** (измерено) |
| G12 | нула кадастрален идентификатор | `python -m gates.run_gates` проверка 3 | зелена | вкарай `10135.1.2` → червено |
| G13 | точно един allow-json | `python -c "import pathlib,sys; n=len(list(pathlib.Path('gates/allow').glob('*.json'))); print(n); sys.exit(0 if n==1 else 1)"` | 0 | два файла → 1 |
| **G14** 🔴 | **куката е на място — БАЙТОВО в python** (S28-9) | `python -c "import pathlib,sys; a=pathlib.Path('scripts/hooks/pre-push').read_bytes(); b=pathlib.Path('.git/hooks/pre-push').read_bytes(); print(len(a),len(b),a==b); sys.exit(0 if a==b else 1)"` | 0 (2 574 = 2 574) | същата команда срещу подменено копие в скрачпада → **exit 1** (измерено). **`diff` в PowerShell е `Compare-Object`** и сравнява имена, не байтове |
| **G15** 🔴 | **точно една опашка в HEAD, по семантиката на release** (S28-10) | командата от §4 стъпка 11 | 0 | подпапка `архив/` със СЪЩОТО име → пак **1 попадение** и `release` пак BLOCKED (измерено); само смяна на името дава `[]` |
| G16 | местността не е отворена (О18) | тест в bundle-ите: `DISTRICT_FALLBACK not in LOCALITY_SRC` и всеки доставен `locality.src ∈ LOCALITY_SRC` | 0 | `locality.src = "DISTRICT_FALLBACK"` в мутиран кандидат → ≠ 0 |
| **G17** 🔴 | **`_meta` е ЗАТВОРЕНО МНОЖЕСТВО, `src` е затворен списък, url-ите са по извор** (S28-6, О17) | фикстура + правило 4 на §3.3 | 0 | вж. блока под таблицата — **четири мутации, всяка със СЪЩИЯ брой ключове** |
| **G18** | покритието (кодът се ЗАЧИТА) | трите команди от §4 стъпка 2, всяка със `sys.exit(r['exit_code'])` | 2 / 2 / 2 (преди новия allow) | без `sys.exit` командата връща **0** при вътрешен `exit_code = 2` (S28-5) |

**G7 — дословно (S28-3):**
```powershell
python scratch\places_search\recall_sweep.py
copy /Y "%TEMP%\fv_measures\lot1v_v_candidate_rows.json" "<scratch>\g7_dump_A.json"
```
после промяната в `recall_sweep.py`, същите блобове, същата ревизия на данните:
```powershell
python scratch\places_search\recall_sweep.py
copy /Y "%TEMP%\fv_measures\lot1v_v_candidate_rows.json" "<scratch>\g7_dump_B.json"
```
comparator (дословно, exit 1 при разлика):
```powershell
python -c "import pathlib,sys; a=pathlib.Path(r'<scratch>\g7_dump_A.json').read_bytes(); b=pathlib.Path(r'<scratch>\g7_dump_B.json').read_bytes(); print(len(a),len(b),a==b); sys.exit(0 if a==b else 1)"
```
**Измерено от планера в клонинг:** два прогона с един и същ двигател → **1 324 215 B, байт-еднакви, exit 0**. Отрицателна половина: `CENTER = (43.2000, 27.9000)` в копие на двигателя → **1 324 210 B, exit 1**. 🔴 **`OUTDIR` е ОБЩ (`recall_sweep.py:84`) — без `copy` след ВСЕКИ прогон гейтът сравнява последния файл със самия себе си.** Ревизиите на двигателя се пинват в отчета (`git rev-parse HEAD:scratch/places_search/recall_sweep.py` преди и след).

**G8 — дословно (S28-7):** тест, който изрязва среза по маркерите И12 (start … „card slice end“), вдига го с DOM стъб и **ИЗПЪЛНЯВА `popupFor` за всеки от 375-те записа**, после сравнява текста на върнатото поддърво:
```js
const FORBIDDEN = ['Wikimapia', 'wikimapia', 'quarter_attribution',
                   'QUARTERS_ATTRIB', 'CC BY-SA', 'Кварталните граници'];
```
Стъбовете: `document.createElement` → възел с `appendChild`/`textContent`/`className`; `buildNavActions` → `() => null` (живее на `:4255`, извън среза). **Измерено от планера: 375 картончета, 0 забранени низа.** Пример за картончето на `SIGNED_POLYGON` ред: `„Траката Семеен хотел · ★2 · 36 легла · Долна Трака · район Приморски адрес: …“`. 🔴 `ODbL`/`OpenStreetMap` **НЕ влизат в забранените** — те стоят законно в реда за извор на адреса/името и списък, който ги лови, е червен без причина (измерено).

**G9 — дословно (S28-7):** три твърдения, първите две ИЗПЪЛНЕНИ:
- **(а)** срезът `index.html:1900-1918` + И11 се вдига с `L` стъб, чийто `control.attribution()` връща **записващ** `attribCtrl`; след изпълнението `calls === ['© OpenStreetMap', QUARTERS_ATTRIB]` и `calls.includes(QUARTERS_ATTRIB) === true`. **Измерено:** преди И11 `calls === ['© OpenStreetMap']` (гейтът пада), след И11 — двата низа, вторият с дължина **338**.
- **(б)** низът се среща в `index.html` **точно веднъж** и е **дословно** полето `attribution` на файла с решенията (`sha256(utf-8) == c6d29d4e…`).
- **(в)** 🔴 **дайджестът се сверява ТУК, не от release.** Ч: `gates/release.py:1367-1371` — `entry = artefacts.get(row["artefact"]) if row["artefact"] else None; if not want or entry is None: continue` — ред клас „въпрос“ няма артефакт и **никога** не стига до сверката, а файлът с решенията не е в `SIGNABLE` (`:177-184`), тоест не може да стане артефактен ред без редакция на `release.py` (забранена, §10). Затова G9 чете реда от опашката, изисква `решение: да`, сверява `тяло:` със `sha256` на низа и `тяло-на-файла:` с `gates.release.body_digest` на блоба, и изисква **човешкия подписал** (`gates/release.py:644` `yes_row_authorship` — комитът, внесъл блока, да е на `Petar1984`).

**G11 — дословно (S28-8):**
```python
import json, subprocess, sys, pathlib
GEO = {"coordinates", "geometry", "geometries"}
def walk(o, path, hits):
    if isinstance(o, dict):
        for k, v in o.items():
            if k in GEO: hits.append(path + "/" + k)
            walk(v, path + "/" + k, hits)
    elif isinstance(o, list):
        for i, v in enumerate(o): walk(v, path + "[%d]" % i, hits)
files = [f for f in subprocess.run(["git","ls-files","data/"],capture_output=True,text=True).stdout.split() if f.endswith(".json")]
bad = []
for f in files:
    hits = []
    walk(json.loads(pathlib.Path(f).read_text(encoding="utf-8")), "", hits)
    if hits: bad.append((f, hits[:3]))
print(len(files), bad)
sys.exit(0 if not bad else 1)
```
**Измерено: 18 файла, 0 попадения.** Плюс **инвентар на НОВИТЕ проследени файлове** на комита срещу неговия родител:
```powershell
git diff --name-status --diff-filter=A <parent> HEAD
```
Позволени НОВИ пътища, поименно: `tests/granitsi_client_probe.mjs`, `scratch/places_search/granitsi_fixtures_07.09.json` (при `git mv` върви като R, не A), `scratch/places_search/ЗА_ПОДПИС_Б3_07.09.md`, `gates/allow/2026-09-07_b3.json`, `scratch/places_search/архив/АРХИВ_A_05.09.md`. **Всеки друг нов проследен файл = STOP.**
Отрицателна половина (измерена): файл с ключ `"\u0063oordinates"` — `grep -c coordinates` → **0** (exit 1), структурната проверка → **`/a/coordinates`** (exit 1 на гейта).

**G17 — дословно (S28-6, О17):** четирите отрицателни мутации, **всяка със СЪЩИЯ брой ключове**, всяка трябва да даде `false`:
1. `_meta.generated` преименуван на `unexpected` — **10 ключа, но друго множество** → `metaKeysAre` трябва да върне `false`. (v2 броеше и минаваше: измерено `true`.)
2. `quarter_attribution["kv_levski"].src = "openstreetmap"` при непроменен wikimapia url → `false` по правилото за извора.
3. Измислен `src: "wikidata"` с валиден url → `false` по затвореното множество `ATTRIB_SRC`.
4. Добавен запис `quarter_attribution["galata"]` (никой `SIGNED_POLYGON` ред не го носи) → `false` по **равенството** (v2 проверяваше само „всеки код има запис“ и това минаваше).
Положителните твърдения: places 10 ключа / 14 кода, hotels 8 ключа / 7 кода, всичките 21 записа с ключовото множество `{label, licence, src, url}`, `mladost2` с `url === 'https://www.openstreetmap.org/copyright'`.

---

## 6 · Комити (кой какво комитва)

Причината за атомността е механична: `index.html` пинва sha на данните — при какъвто и да е разрез съществува HEAD, в който пиновете и блобовете не си съответстват и търсенето на места умира.

| # | автор (наложен) | съдържание | съобщение (дословно) |
|---|---|---|---|
| 1 | `Claude Executor <executor@local>` (флагове, О24) | `docs/plans/ПЛАН_Б3_07.09.md` | `docs(plan): Б3 quarter search delivery plan v3 (signed 07.09)` |
| 2 | **`Petar1984`** (неговата ръка) | `data/places.json`, `data/hotels.json`, `data/place_categories.json`, `index.html`, `tests/test_places_public_bundle.py`, `tests/test_hotels_public_bundle.py`, `tests/test_granitsi_fixtures.py`, `tests/granitsi_client_probe.mjs`, `scratch/places_search/granitsi_fixtures_07.09.json` (+ изтритият `…_06.09.json`), `scratch/places_search/recall_sweep.py` | виж по-долу |
| 3 | `Claude Executor` | опашката Б3, **преименуваната `архив/АРХИВ_A_05.09.md`**, трите манифеста, новият `gates/allow/2026-09-07_b3.json` (138 реда, pending), махнатият стар allow. **`gates/baseline/MANIFEST.json` НЕ се пипа (О22).** | `docs: queue and manifests for the Б3 delivery signatures (07.09)` |
| 4 | **`Petar1984`** | каквото `gates.sign` е записало | съобщението, което писалката **отпечатва** |
| 4б | **`Petar1984`** (само ако низът се подписва отделно) | `index.html` (И11) | `feat(map): signed quarter-boundary line in the map attribution (decision 8)` |
| 5 | **`Petar1984`** | `recall_sweep_rows.json`, `expectations.json`, `probe_out/token_parity.json` | `freeze: reference + expectations + parity (Б3)` |
| 6 | `Claude Executor` (след пуша) | `docs/activeContext.md`, `docs/sessions/СЪСТОЯНИЕ_Границите_06.09.md` | `docs: activeContext — Б3 delivery published, published hashes verified equal to local` |

Съобщението на комит 2, дословно (`scratch/places_search/COMMIT_MSG_B3.txt`):

```
feat(search): quarter search over signed boundaries, QUARTER_CODES +8, cache v7 (D13 atomic delivery)

data: places delivery with quarter_attribution (D13 ordered) — the three blobs of
varna_3d 05f485e (places 3dffc264, hotels eb8fa85c, categories 9064705a), with the
closed lists, the three pins, LEGACY_BUNDLE_SHA and PLACES_CACHE v7 in this very
commit, because a HEAD whose pins and blobs disagree kills the place search.
tests: S17 fixtures fail on broken candidates
```

Командите за комит 2 (PowerShell, по една на ред, без `&&`):
```powershell
cd C:\git\Fire_Varna
git status --short
git diff --cached --stat
git commit -F scratch\places_search\COMMIT_MSG_B3.txt
git log -1 --pretty='%h %an <%ae> · %cn <%ce> · %s'
```
**Следпроверка (О24):** последният ред трябва да върне `Petar1984 <petar.dikov2019@gmail.com>` и като автор, и като комитър. Предварителното `git config user.name` **не доказва нищо**.

---

## 7 · STOP условия

1. **Всяко число от §1, премерено различно** — трите sha, шестте байта/gzip, осемте липсващи кода, числата на фикстурите, **и новите: 138 / 150 / (30 + 42) на покритието, 1 878 210 B първо зареждане, 1 опашка в HEAD, деветте заявки 1.8.4–1.8.9, двете `_meta` множества, двете карти 14/7**.
2. `gates.release` дава делта, която опашката не покрива, или ред „не“ (терминален).
3. `gates/out/coverage.json` иска allow-ред, който не се извежда механично.
4. Паднал тест **извън** записания D19/G20 набор.
5. `python -m gates.run_gates` жълто, третирано като зелено.
6. Дифът на кандидата преди/след промяната в `recall_sweep.py` е ≠ 0 байта (G7).
7. Липсващ `node`, липсващ маркер за среза, или Node-фикстура, минала **без** `map`/`fetch`/**`crypto`** стъб.
8. Опит за комит на данни от агент · `push` от агент · `--no-verify` · пипане на `.git/hooks`.
9. Работното дърво не е чисто по проследен файл при старта, или чужд стейджнат диф.
10. Решение 10 / резервът за Перчемлията (О6) не е подписан и одиторът настоява.
11. Износът е сменил лицензен низ, а константата в теста и README не са в същия комит.
12. Петър не е видял картата локално преди пуша.
13. **Повече от една опашка в HEAD** (по семантиката на `queues_in_head`, не по `grep`) или повече от един allow-json.
14. **`_meta.inputs` не съдържа обявеното множество**, или `INPUT_RELS` е пипнат.
15. **Комит с автор `Petar1984`, направен не от неговата ръка** (О24).
16. **Низът за атрибуция е влязъл в `index.html` преди подписания ред с двата му дайджеста** (О21).
17. **`gates/baseline/MANIFEST.json` е пипнат** (О22).
18. Проследеният `probe_out/token_parity.json` се появи в комит 2 вместо в комит 5 (О26).
19. 🔴 **Опашката носи по-малко от ОСЕМ делта-реда за загубените на „младост“** (1.7, К28-3).
20. 🔴 **`--manifest` е пуснат повече от веднъж преди подписите** (1.8.16, О30) — второто число е вакуумно зелено и не е доказателство.
21. 🔴 **Стар/нов файл със `SIGNED_POLYGON` код, който `quarter_attribution` не носи, ИЛИ запис в картата без такъв ред** — равенството е гейт и в двете посоки (G17).
22. 🔴 **Нов проследен файл извън поименния списък на G11.**

---

## 8 · Локален преглед (как Петър гледа)

```powershell
cd C:\git\Fire_Varna
python -m http.server 8000
```
`http://localhost:8000/index.html`, отворена конзола, ширина 375 px, Hard reload (кешът вече е v7). Първо: конзолата е чиста и **няма** реда `legacy_by_row: друг пакет…`.

| # | пишеш | очакваш (числата са измерени 07.09) |
|---|---|---|
| 1 | `Кайсиева градина` | в нашата секция — **3 реда `M2-failopen`**, нито един квартален; в адресната търсачка отгоре — 9 реда. |
| 2 | `училище Възраждане 3` | **2 реда по ИМЕТО**: `3 ОУ Ангел Кънчев` [**кв. Левски**] и `III ПМГ` [**кв. Тракия**] — **нито един не е във Възраждане 3**. Шум по името, не отговор по квартал. |
| 3 | `възраждане`, после `възраждане 2` | **5** реда (`ДГ 29 „Звънче“`, `ДГ 31 „Крилатко“`, `II ОУ „Никола Йонков Вапцаров“`, `ДГ 33 „Делфинче“`, `ДГ№34 „Лястовичка“`) и **0 реда** за детето. **Половината „дете → детето“ НЕ работи** (О16) — това подписваш. |
| 4 | `левски`, после `кв. Левски` | голото `левски` → **14 реда, 12 с код `kv_levski`**. `кв. Левски` → **4 ЧУЖДИ реда** (`НУ Васил Левски` [кв. Аспарухово], `ПГ „Васил Левски“`, `ДГ№40` [кв. Владиславово], болница). Същото за `кв. Тракия` (**1**, и той е `dolna_traka`), `кв. Христо Ботев` (**24**, от които **точно 1** е `hristo_botev` — `ЦДГ 8 - Христо Ботев`, и то по име), `ж.к. Св. Иван Рилски` (**22**, нула с кода). **Ако избереш вариант Б по §9 т. 1, тези четири стават 14 / 4 / 4 / 4 с 12 / 4 / 3 / 2 верни реда.** |
| 5 | `бриз` | 🔴 **4 реда** (не 2): `ПАРК ХОТЕЛ БРИЗ` [к.к. Златни пясъци — **по име**], `IV ЕГ Жолио Кюри` [ж.к. Бриз, нов], `ПГ „Д. И. Менделеев“` [ж.к. Бриз, нов], `Камелия` [ж.к. Бриз, заварен]. Двата нови са `SIGNED_POLYGON`. |
| 6 | `чайка` | **30 реда** — 19 `chaika_kk` + 10 `chaika_kv` + `„ДКЦ Чайка“ ЕООД` (без квартал). Фразата слива двата кода без подписано родство (О16). |
| 7 | `аспарухово`, после `дружба` | **16** реда (9 с `asparuhovo`, 7 „район Аспарухово“) **без** `ЦДГ 43 „Пинокио“`, и **2** реда. Подписаното родство `druzhba ⊂ asparuhovo` **не действа**. |
| 8 | `Никея` (хотел в Чайка) → картончето | ред **„вид · к.к. Чайка · район Приморски“**; **НЯМА ред за извор** (измерено: 375/375 картончета без нито един от забранените низове); долу вляво, до `© OpenStreetMap`, стои низът за границите. |
| 9 | `училище район Младост` / `младост` | **12** / **20**. Изричното районно търсене задържа и петте училища със собствен квартал. |
| 10 | **надписът при 375 px** | низът е **338 знака** и застава в същия долен ляв ъгъл, където са бутоните `+`/`−` (`:1900` и `:1917`). Проверява се **на екрана**: бутоните `+`/`−` и локацията остават **видими и натискаеми** при трите базови карти. Ако не са — §9 т. 7, вариант „къс низ“. |
| 11 🔴 | **отказът, който мълчи** (О27) | ако някога доставка донесе код извън списъка, в нашата секция ще има **225 реда вместо 375 и НУЛА съобщение**. Днес това не се вижда — подписваш го като известно поведение. |

---

## 9 · Решения за Петър

1. 🔴 **Типовият префикс („кв. Левски“ и трите му близнака).** Измерено (1.8.4): `кв. Левски` 4 чужди, `кв. Тракия` 1, `кв. Христо Ботев` 24 (1 верен по име), `ж.к. Св. Иван Рилски` 22. Причина: `index.html:6495-6496` / `recall_sweep.py:424-431` — токен ≤ 2 знака не е значещ, а М7 иска `R.every(isSignificantToken)`. **Три варианта:**
   - **(А) Отлагане** (позицията на v2): D12 + `locationKey` в паритетния корпус са лот **Б5**. Цена: осемте нови квартала се търсят само с голото си име. Цена днес: 0.
   - **(Б) Евтината поправка (К28-5), ПРЕПОРЪЧАНА.** Двигателят вече назовава затворения списък `M7_PREFIXES` (`recall_sweep.py:3093` = `("k","kv","zh","m","s","o","t","i","1","2")`). Пропускат се **само буквените** префикси (`k, kv, zh, m, s, o, t, i`) преди теста за значимост в М7 клона, в **двата двигателя**: `recall_sweep.py:1257`+`:1263` (2 реда) и `index.html:7058-7069` (клиентът **няма** такъв списък — иска новата константа + филтриран `R`, ~5 реда). **Измерено върху копие в скрачпада:** `кв. Левски` **4 → 14** (12 с `kv_levski`), `кв. Тракия` **1 → 4** (4/4), `кв. Христо Ботев` **24 → 4** (3/4), `ж.к. Св. Иван Рилски` **22 → 4** (2/4). **Цена на презамразяване: НУЛА** — 156-заявъчният подписан корпус дава **0 промени**, а дъмпът на референцията е **байт-еднакъв** (1 273 012 B). Остава честната цена: **паритетът** (клиентът трябва да получи същите ~5 реда, иначе двата двигателя се разминават) и **един нов кръг на фикстурите/паритетната сонда**. Числата (`2`, `1`) остават извън списъка: **„възраждане 2“ и „младост 2“ пак дават 0** — детската заявка не оживява.
   - **(В) Пълният D12** (нормализатор + `locationKey` в паритета): измерено, че вариант **А′** (пропускане и на числата `1`/`2`) прави „възраждане 2“ и „младост 2“ да върнат **5** и **20** — тоест ДЕТСКАТА заявка получава РОДИТЕЛСКИЯ отговор. Това е по-лошо от 0 и планерът го **отхвърля** (§13 т. 5).
   **Препоръка: (Б).** Ако Петър каже (А), §8 проба 4 остава както е и О15 се подписва изрично.
2. 🔴 **Правилото родител/дете.** Измерено: работи по фраза само за Младост/Възраждане, **не работи** за `druzhba ⊂ asparuhovo`, **не работи** за детската заявка, и **слива** `chaika_kk`+`chaika_kv`. **Препоръка: замразяваме измереното** (фикстури 5–8) и подписваш О16. Вариант Б: `locations.quarter[<code>].parents` в речника → днешната доставка отпада и цикълът се повтаря отначало.
3. **`QUARTER_CODES` +8 (=28)** + машинен гейт „списъкът == речникът“ (измерено днес: равни). **Препоръка: да.**
4. **Един атомен комит с твой автор** (О4) + наложено авторство на агентските комити (О24). **Препоръка: да.**
5. **`DISTRICT_FALLBACK`: пълното типово правило на ШЕСТ места** (О19) + синтетична положителна фикстура с разделен `_broken`. **Препоръка: пълното правило.**
6. **Етикетът на fallback ред се приема в ДВЕТЕ форми** (О20; измерено: износителят пише `zone === quarter.name` за 375/375). **Препоръка: да.**
7. 🔴 **Низът в подножието не е твой подпис днес** (О21). **Препоръка:** подписваш го като **ред „въпрос“** с `тяло: c6d29d4e…` (sha на низа) и `тяло-на-файла: c57a79c2…`; **сверката е в G9, защото `release.py:1367-1371` пропуска ред без артефакт** — това е механика, не небрежност. Две бележки: низът казва „в този файл“ (писан е за файла с решенията), а на 375 px заема долния ляв ъгъл при бутоните — §8 проба 10 решава дали остава дословен.
8. **`granitsi_fixtures_06.09.json` → `…_07.09.json`** (`git mv`). **Препоръка: да.**
9. 🔴 **baseline и allow (О22).** **Препоръка: baseline ОСТАВА `f06ac06`**; новият allow бюджетира **138-те** реда. Семантиката, дословно: „lost“ = ред с назован квартал/зона в базата и без такъв в кандидата (places 44, hotels 39); „changed“ = друг назован (places 44, hotels 11). Обяснението защо не са 150: срещу `f00dd78` са **150**, значи доставката **връща 12**. Пренасочването на baseline е **отделен по-късен подписан лот**.
10. 🔴 **Мълчаливият UI отказ (О27, ново).** Нощният план `:144` го забранява, а Б3 го запазва — при чужд код клиентът показва 225 вместо 375 и мълчи. **Препоръка:** подписваш го като **обявено отклонение** и фикстура 9в го пинва; видимият отказ е отделен малък лот.
11. **Перчемлията / решение 10** (О6) — подпиши поименния резерв **преди** комита или сложи изричен „отложено“ ред в опашката.
12. **Документният дълг** (О9 + О23) — **отделен комит след пуша**.
13. **Node-паритетната фикстура е ПОСТРОЕНА** (фикстура 11). **Препоръка: да.**
14. 🔴 **Старата опашка (О25) — поправено срещу v2.** Измерено: `архив/ЗА_ПОДПИС_A_05.09.md` **пак се брои** за опашка (`queues_in_head` е рекурсивен и гледа basename), тоест release остава BLOCKED. **Препоръка: `git mv` към `scratch/places_search/архив/АРХИВ_A_05.09.md`** — името губи префикса. Изтриване не е нужно: 27-те подписани реда остават четими и в историята, и в архива.
15. **`LOCALITY_SRC` (О18)** — стеснението, което пази местността от `DISTRICT_FALLBACK`. **Препоръка: да.**
16. **`_meta` става затворено МНОЖЕСТВО в клиента (О17)** — 10/8 ключа поименно, `src ∈ {wikimapia, openstreetmap}` с правило за всеки, и **равенство** карта ↔ `SIGNED_POLYGON` кодове (14 / 7). **Препоръка: да** — това е половината от G27(б), която иначе остава неналожена.
17. 🔴 **`P7_ADDED` дрейфът (О28, ново).** Новият речник добавя **5 нови токена** (`gratska`, `gratski`, `varnenchik`, `vladislav`, `vasil`), а подписаният `foreign_guard` изрично казва, че `vladislav`/`varnenchik` са били отхвърлени като чужди. **Препоръка:** ред „въпрос“ в опашката с двата списъка дословно; ако Петър не иска новите псевдоними, това е промяна в **износа на varna_3d**, не в Fire_Varna, и доставката се връща.

---

## 10 · Какво НЕ се пипа

- `Varna_buildings/config/quarter_registry.json` и изобщо репото `Varna_buildings`.
- `varna_3d`: чете се **само** блоб; `data/quarters_signed.geojson` никога не влиза във Fire_Varna.
- **Написаните 201 квартала** (D18/G17) и леджерът.
- `DISTRICT_CODES` (`:6315`), `LOCALITY_CODES` (`:6322`), `EXPECT_KEYS`/`EXPECT2_KEYS`, `EXPECT_COUNT`/`EXPECT2_COUNT`, `TOP`/`MIN_Q`/`DEBOUNCE_MS`, `KINDS`/`KINDS2`/`STATUSES`.
- **`gates/baseline/MANIFEST.json`** (О22), **`INPUT_RELS`** (`recall_sweep.py:2815-2816`, О23) и **`CENTER`** (`:102-104`).
- Адресната търсачка (`index.html:5928-6070`, `data/approx_addresses_v1.json`), `data/search_index.json`, `data/address_rows.json`.
- Хидрантите, формата за докладване, Worker-ът, базовите карти и `BASEMAP_PMTILES_ENABLED` (`:4563`).
- Двигателите на гейта: `gates/run_gates.py`, `gates/release.py`, `gates/sign.py`, `gates/coverage.py`, `scripts/hooks/pre-push` — **нула редакции** (затова G9 сверява дайджеста сам).
- Числата на `granitsi_fixtures_06.09.json` като исторически запис.
- Публикуваните sha и `origin/main` — до ръката на Петър.

---

## 11 · Отхвърлени находки (от кръговете на v1/v2)

| # | находка (дословно запазена) | измерената причина да не се приложи както е написана |
|---|---|---|
| 1 | оборител **gates**: „baseline се пренасочва към `f00dd78` … тогава allow изобщо не е нужен“ | **Частично отхвърлено.** Измерено: неползван ред от allow е `allow_stale` (`coverage.py:573-577`) → `EXIT_ALLOW_STALE = 3` (`:617-618`); днешният подписан allow носи 150 реда → изход 3, не 0. Вариант (б) е изпълним само ако старият allow бъде изтрит в същия комит — самостоятелен подписан въпрос. Планът избира (а). |
| 2 | „338 знака ще се пренасят над бутоните `+/−` при 375 px“ | **Отхвърлено като твърдение, прието като проба.** Планерът няма браузър. `:1900` и `:1917` слагат двата контрола в един и същ ъглов контейнер, а Leaflet ги подрежда един под друг. Рискът влиза като §8 проба 10. |
| 3 | „`recall_sweep.py:67 REPO_ROOT`“ | **Само корекция на цитата.** `REPO_ROOT` е на **`:68`**. Същината (няма argparse, няма `--dump`, `OUTDIR` е `%TEMP%/fv_measures/`, `:4249-4253` презаписва проследения паритетен файл) е потвърдена. |
| 4 | v1 И11: „`BASEMAP_PMTILES_ENABLED = false` значи второто място не е живо“ | **Отхвърлено по мярка.** `:4571-4574` връща `true` и при `localStorage` override. Изводът оцелява по друга причина: `attribCtrl` е един и размените махат само собствените си низове. |

---

## 12 · Оборване

| оборител | присъда | приложено в v3 |
|---|---|---|
| **gates / search / delivery** (кръг на v1) | **НЕГОДНО** × 3 | Всичките 8 блокера приложени в v2 и запазени тук (О15–О26). |
| **Astra S28** (механика) | **НЕГОДНО**, 10 условия | **S28-1** ✅ — измерено със собствена Node-проба: хотелите също носят `SIGNED_POLYGON` (15 реда), старият списък отказва **и двата** пакета и `ensurePlaces` **хвърля**; фикстура 9 разделена на 9а/9б/9в + матрицата crypto×sha×брояч (1.8.11) и **брояч на извикванията** в твърдението. **S28-2** ✅ — двете мутации преработени по измереното (§3.5 фикстури 6, 7, 8); alias-трикът доказано изхвърлен като `foreign:asparuhovo`. **S28-3** ✅ — G7 с два ОТДЕЛНО ЗАПАЗЕНИ дъмпа, дословен байтов comparator със `sys.exit`, пинати ревизии и **доказана падаща мутация** (1 324 215 → 1 324 210 B). **S28-4** ✅ — измерен е ДЕЙСТВИТЕЛНИЯТ набор на първото зареждане (`AGENTS.md:53`): `index.html` + `data/hydrants.json` = **1 878 210 B**; командата на v2 наистина дава 19 файла / 26 839 286 B / exit 1 (§13 т. 1); отложената доставка е G10б. **S28-5** ✅ — `--manifest` очаква **1**, наборът е предварително изброен, coverage командите носят `sys.exit(r['exit_code'])`; **плюс собствената находка О30** (вторият прогон е вакуумно зелен). **S28-6** ✅ — G17 сверява точните МНОЖЕСТВА (10/8 дословно), затворен `src`, правило за всеки извор, и **равенство** карта ↔ кодове; четирите отрицателни мутации са със същия брой ключове. **S28-7** ✅ — G8 **изпълнява** `popupFor` върху 375 записа с DOM стъб (0 забранени низа); G9 **изпълнява** среза на атрибуцията със записващ `attribCtrl`; редът на опашката и `release.py:1367-1371` са разгледани — сверката минава в G9. **S28-8** ✅ — G11 е структурен обход + инвентар на новите проследени файлове; escape-пробата е измерена. **S28-9** ✅ — G14 е python байтово сравнение; `diff` в PowerShell потвърден като `Compare-Object`. **S28-10** ✅ с **поправка** — мълчаливият UI отказ става обявено отклонение О27; архивирането е измерено и **само подпапка НЕ стига** (§13 т. 2); G15 ползва семантиката на `queues_in_head`. |
| **Кими К28** (данни/семантика) | **НЕГОДНО**, 7 условия | **К28-1** ✅ — 1.8.4 поправено: „кв. Христо Ботев“ носи **1** ред с кода (`ЦДГ 8 - Христо Ботев`, по име). **К28-2** ✅ — фикстура 6 мутира ИМЕТО (5 → 4 измерено), кодовата мутация доказано НЕ пада (пак 5); `assert_case` става многозаявъчен с `fails_on`. **К28-3** ✅ — осемте изгубени реда назовани с новите си квартали; опашката дължи **8** делта-реда (STOP 19). **К28-4** ✅ — §8 проба 5 „бриз“ = **4** реда, поименно. **К28-5** ✅ — §9 т. 1 получава вариант (Б) с **измерени** резултати и **измерена нулева** цена на презамразяване; клиентската цена е назована честно (клиентът няма `M7_PREFIXES`). **К28-6** ✅ — О22 записва семантиката на coverage и §4 стъпка 2 иска и дифа `f06ac06↔f00dd78` (**150**, измерен от планера). **К28-7** ✅ — фикстура 7 изброява десетте op-а поименно; **числото е 21, не 20** (§13 т. 3). |

---

## 13 · Отхвърлени външни находки (с измерената причина)

| # | находка | измерената причина |
|---|---|---|
| 1 | **S28-1:** „с несъвпадащ SHA измерих **0 извиквания**, следователно `null` самостоятелно допуска false pass“ — като общо твърдение | **Прието по същество, поправено по обхват.** Измерено (1.8.11): 0 извиквания има САМО при **crypto ON**. При crypto OFF `accept` не сверява дайджест изобщо (`:6605`) и валидаторът се вика (1 извикване) дори с грешен sha. Твърдението „подай грешен sha и виж 0“ е вярно за ON и невярно за OFF; затова фикстурата подава **верния** sha на мутираното тяло и брои извикванията. |
| 2 | **S28-10 / заданието:** „архивиране извън точния glob е достатъчно“ / `git mv` към `scratch/places_search/архив/ЗА_ПОДПИС_A_05.09.md` | **Отхвърлено по мярка (1.8.19).** `queues_in_head()` (`release.py:437-458`) прави `git ls-tree **-r**` и сравнява **`posixpath.basename(n)`** с глоба. Проба в клонинг: след `git mv` в `архив/` със същото име резултатът е `['scratch/places_search/архив/ЗА_ПОДПИС_A_05.09.md']` — **пак една опашка, release пак BLOCKED**. Едва преименуването без префикса дава `[]`. Планът пише `архив/АРХИВ_A_05.09.md`. |
| 3 | **К28-7:** „преименуване на всичките 10 реда `chaika_kv` дава **30→20**“ | **Отхвърлено по число, прието по метод.** Измерено: **21** и при смяна само на `quarter.name`, и при смяна на `quarter.name` + `zone`. Оцеляват 19-те `chaika_kk` + `4 ОДЗ Чайка` (думата е в ИМЕТО ѝ) + `„ДКЦ Чайка“ ЕООД`. К28 сам допуска 21 в скоба; фикстурата пинва **21**. |
| 4 | **S28-5:** „измерени **8 несъответствия**“ | **Прието по същество, поправено по число.** Измерено в клонинг (1.8.15): първият `--manifest` дава **59 реда `ЧЕРВЕНО` върху 41 уникални заявки** плюс 2 реда без заявка. „училище младост: 6, подписано 11“ е потвърдено и е сред тях. Планът изброява целия набор (§4 стъпка 10). |
| 5 | **К28-5, разширението „пропускане на `M7_PREFIXES` токени“ дословно** (списъкът включва `"1"` и `"2"`) | **Частично отхвърлено по мярка.** Ако се пропуснат и числата, „възраждане 2“ връща **5** и „младост 2“ връща **20** — тоест ДЕТСКАТА заявка получава отговора на РОДИТЕЛЯ, което противоречи на измереното 0 и на О16. Приема се само буквената половина (`k, kv, zh, m, s, o, t, i`) — тогава „възраждане 2“/„младост 2“ остават **0**. |
| 6 | **v2 §1.5:** „`quarter_attribution` = 18 кода (17 wikimapia + 1 openstreetmap)“ | **Отхвърлено като основание за гейт.** 18 е ОБЕДИНЕНИЕТО. Измерено: places носи **14**, hotels **7**, общи **3**. Гейт, писан срещу 18, ще е червен и на двата пакета. |
| 7 | **v2 §3.6 т. 4:** „`RECS.length === 225` при среза с оригиналния `QUARTER_SRC`“ | **Отхвърлено по мярка (1.6).** При оригиналния списък и двата пакета падат и `ensurePlaces` **хвърля** `places payload unavailable` — няма `RECS`. 225 идва само от случая, в който **само** places е отказан (9в). |
| 8 | **v2 G10:** `git ls-files index.html 'data/*.json'` като „проследените файлове“ | **Отхвърлено по мярка (1.8.14).** 19 файла, **26 839 286 B**, exit **1** — червено преди промяната. Наборът включва архивни снимки на хидрантите, `search_index.json` (11,2 MB) и `address_rows.json` (5,07 MB), които клиентът НЕ тегли при първо отваряне. |
| 9 | **v2 G17:** „`Object.keys(d._meta).length !== META_KEYS_*`“ | **Отхвърлено по мярка.** Изпълнено: преименуване на `generated` на `unexpected` пази 10 ключа и връща **true**. Броят не е множество. |
| 10 | **v2 G14:** `diff scripts/hooks/pre-push .git/hooks/pre-push` | **Отхвърлено по мярка (1.8.22).** В PowerShell `diff` е `Alias → Compare-Object`; командата сравнява имена и излиза с 0 при подменена кука. |
| 11 | **v2 G11:** буквален `grep coordinates` | **Отхвърлено по мярка (1.8.12).** Ключ `"\u0063oordinates"`: grep → 0 попадения (exit 1), parse → `coordinates`. Освен това единственото буквално попадение днес е ПРОЗА в стойност, не ключ. |

## 14 · Подпис и прочит на §9 „по препоръките“ (07.09.2026)
Петър: „подписвам плана Б3 v3 по препоръките“. Прочитът на седемнадесетте решения, дословно по препоръките на §9:
1 → **(Б)** евтината префиксна поправка в двата двигателя (само буквените `M7_PREFIXES`; числата остават извън) · 2 → родството се **замразява** каквото е (фикстури 5–8), DAG-ът не влиза · 3 → да, +8 (=28) + гейт „списъкът == речникът“ · 4 → един атомен комит с автор `Petar1984`, наложено авторство на агентските комити · 5 → пълното типово правило на шест места + синтетична фикстура · 6 → двата етикета за fallback ред · 7 → низът в подножието влиза като ред „въпрос“ с `тяло`/`тяло-на-файла`, G9 сверява · 8 → `git mv` на фикстурния файл към 07.09 · 9 → baseline **остава** `f06ac06`, allow = 138 · 10 → мълчаливият UI отказ = обявено отклонение (О27), фикстура 9в го пинва · 11 → **ред „отложено“ за Перчемлията** в опашката (решение 10 остава отворено; §9 не дава препоръка — това е прочитът на архитекта, Петър го коригира, ако иска резерва сега) · 12 → документният дълг в отделен комит след публикуването · 13 → да, фикстура 11 · 14 → `git mv` към `scratch/places_search/архив/АРХИВ_A_05.09.md` · 15 → да, `LOCALITY_SRC` · 16 → да, затворени множества · 17 → ред „въпрос“ за `P7_ADDED` с двата списъка (псевдонимите остават, докато Петър не каже друго при подписа на опашката).
Повторните проверки Astra S29 / Кими К29 върху v3 текат по време на подписа; условие от тях влиза като амандамент (нов датиран файл) и се подписва отделно, само за делтата.

