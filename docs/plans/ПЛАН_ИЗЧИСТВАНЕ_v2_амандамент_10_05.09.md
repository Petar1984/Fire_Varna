# Амандамент №10 към ПЛАН „ИЗЧИСТВАНЕ“ v2 (05.09.2026, 15:30) — след одита на A.2-10 (НЕГОДНО: 1 blocker, 2 major, 3 minor). НЕ Е ИЗПЪЛНЕН — чака думата на Петър (заповед 13:00: доклад + приспиване след този кръг)

A.2-10 (cf3441c) е доставен: опашката се чете от HEAD blob и мръсна опашка блокира; авторство по ред за „да“; парсерът пропуска HTML-коментари и fence; дублирани полета блокират; класове редове; дайджест на тялото. 306 теста, същите 6 червени; решаващият експеримент: 0 червени, 7/7 в изолиран клон. Одиторът с кода на репото намери:

## Blocker
1. **Писалката пише „тяло“ в скрит регион.** `gates/sign.py:decide_row` обхожда ВСИЧКИ редове, включително тези във fence/коментар; при пример във fence след полетата на реда дайджестът попада вътре, гейт 6 не го вижда, тялото не е вързано, `--freeze` минава над пренаписано подписано тяло. → `decide_row` работи само върху ВИДИМИТЕ редове (`release.visible_and_hidden`, една истина с парсера); скрит регион между полетата на реда и следващото заглавие = отказ на писалката; гейт 6 блокира, когда ред „да“ няма видимо поле „тяло“ за артефакт с дайджест.

## Major
2. **Върнат блок:** агент връща дословно блок „да“, който Петър някога е комитвал (после махнат в нов кръг) → `introduced_by_text` взима НАЙ-СТАРИЯ комит → Petar1984 → зелено. → `yes_row_authorship` иска и най-новият, и най-старият комит, въвели блока, да са на Petar1984; който и да е агентски = BLOCK.
3. **Коя опашка се съди е по диска (glob):** изтрита от диска проследена опашка с терминално „не“ изчезва от присъдата. → `find_queue` изброява от `git ls-tree -r --name-only HEAD -- scratch/places_search` (блок при > 1 в HEAD) и блокира изрично, когда проследена опашка липсва/изтрита в работното дърво.

## Minor
4. Обявеното отклонение от т. 7 на амандамент №9 (несъвпадение на тялото се прощава, когато последният комит по артефакта е на Petar1984 — иначе решаващият експеримент не може да е зелен, защото `--freeze` пренаписва `expectations.json` след подписа) → **решение на Петър:** (а) приема се и се записва тук дословно; или (б) пълна строгост: `--freeze` преподписва реда (промяна в `recall_sweep.py`, нов кръг). Препоръка на архитекта: (а) за доставка A, (б) във Фаза D, когато подписите живеят в един файл.
5. Проверка 7 не вижда скрити решения и мълчи при липсваща опашка на диска → вика `release.hidden_decisions` върху blob-а и се оплаква, когда `find_queue` върне None при опашка в HEAD.
6. `docs/audits/ДОКЛАД_05.09_изчистване.md` е на архитекта (дневният доклад за Петър) — комитва се като Claude Architect с този амандамент.

## Отговори на въпросите на изпълнителя (за следващия кръг)
- Блокът на реда спира на последното поле, не на следващото заглавие — ДА (мерено: иначе тривиален комит на Петър става „въвел“).
- Ред с `покрива:` и празна стойност = клас „делта“ (REFUSAL_EMPTY остава жив); ред без полето = „въпрос“/„артефакт“ — записва се изрично в шаблона на опашка A.
- `<!--` в средата на ред скрива целия ред (fail-closed) — ДА; опашката няма право на проза с `<!--`.

## Ред (след думата на Петър)
A.2-11 (т. 1–3, 5) → одит (сценариите на одитора + два нови) → A.3 опашка → Петър: подпис → `--freeze` → комит → 0 червени → 7/7 → одит → push → проверка на публикуваните хешове → карта с чисти данни.
